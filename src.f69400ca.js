// modules are defined as an array
// [ module function, map of requires ]
//
// map of requires is short require name -> numeric require
//
// anything defined in a previous bundle is accessed via the
// orig method which is the require for previous bundles
parcelRequire = (function (modules, cache, entry, globalName) {
  // Save the require from previous bundle to this closure if any
  var previousRequire = typeof parcelRequire === 'function' && parcelRequire;
  var nodeRequire = typeof require === 'function' && require;

  function newRequire(name, jumped) {
    if (!cache[name]) {
      if (!modules[name]) {
        // if we cannot find the module within our internal map or
        // cache jump to the current global require ie. the last bundle
        // that was added to the page.
        var currentRequire = typeof parcelRequire === 'function' && parcelRequire;
        if (!jumped && currentRequire) {
          return currentRequire(name, true);
        }

        // If there are other bundles on this page the require from the
        // previous one is saved to 'previousRequire'. Repeat this as
        // many times as there are bundles until the module is found or
        // we exhaust the require chain.
        if (previousRequire) {
          return previousRequire(name, true);
        }

        // Try the node require function if it exists.
        if (nodeRequire && typeof name === 'string') {
          return nodeRequire(name);
        }

        var err = new Error('Cannot find module \'' + name + '\'');
        err.code = 'MODULE_NOT_FOUND';
        throw err;
      }

      localRequire.resolve = resolve;
      localRequire.cache = {};

      var module = cache[name] = new newRequire.Module(name);

      modules[name][0].call(module.exports, localRequire, module, module.exports, this);
    }

    return cache[name].exports;

    function localRequire(x){
      return newRequire(localRequire.resolve(x));
    }

    function resolve(x){
      return modules[name][1][x] || x;
    }
  }

  function Module(moduleName) {
    this.id = moduleName;
    this.bundle = newRequire;
    this.exports = {};
  }

  newRequire.isParcelRequire = true;
  newRequire.Module = Module;
  newRequire.modules = modules;
  newRequire.cache = cache;
  newRequire.parent = previousRequire;
  newRequire.register = function (id, exports) {
    modules[id] = [function (require, module) {
      module.exports = exports;
    }, {}];
  };

  var error;
  for (var i = 0; i < entry.length; i++) {
    try {
      newRequire(entry[i]);
    } catch (e) {
      // Save first error but execute all entries
      if (!error) {
        error = e;
      }
    }
  }

  if (entry.length) {
    // Expose entry point to Node, AMD or browser globals
    // Based on https://github.com/ForbesLindesay/umd/blob/master/template.js
    var mainExports = newRequire(entry[entry.length - 1]);

    // CommonJS
    if (typeof exports === "object" && typeof module !== "undefined") {
      module.exports = mainExports;

    // RequireJS
    } else if (typeof define === "function" && define.amd) {
     define(function () {
       return mainExports;
     });

    // <script>
    } else if (globalName) {
      this[globalName] = mainExports;
    }
  }

  // Override the current require with this new one
  parcelRequire = newRequire;

  if (error) {
    // throw error from earlier, _after updating parcelRequire_
    throw error;
  }

  return newRequire;
})({"../node_modules/object-assign/index.js":[function(require,module,exports) {
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/
'use strict';
/* eslint-disable no-unused-vars */

var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var hasOwnProperty = Object.prototype.hasOwnProperty;
var propIsEnumerable = Object.prototype.propertyIsEnumerable;

function toObject(val) {
  if (val === null || val === undefined) {
    throw new TypeError('Object.assign cannot be called with null or undefined');
  }

  return Object(val);
}

function shouldUseNative() {
  try {
    if (!Object.assign) {
      return false;
    } // Detect buggy property enumeration order in older V8 versions.
    // https://bugs.chromium.org/p/v8/issues/detail?id=4118


    var test1 = new String('abc'); // eslint-disable-line no-new-wrappers

    test1[5] = 'de';

    if (Object.getOwnPropertyNames(test1)[0] === '5') {
      return false;
    } // https://bugs.chromium.org/p/v8/issues/detail?id=3056


    var test2 = {};

    for (var i = 0; i < 10; i++) {
      test2['_' + String.fromCharCode(i)] = i;
    }

    var order2 = Object.getOwnPropertyNames(test2).map(function (n) {
      return test2[n];
    });

    if (order2.join('') !== '0123456789') {
      return false;
    } // https://bugs.chromium.org/p/v8/issues/detail?id=3056


    var test3 = {};
    'abcdefghijklmnopqrst'.split('').forEach(function (letter) {
      test3[letter] = letter;
    });

    if (Object.keys(Object.assign({}, test3)).join('') !== 'abcdefghijklmnopqrst') {
      return false;
    }

    return true;
  } catch (err) {
    // We don't expect any of the above to throw, but better to be safe.
    return false;
  }
}

module.exports = shouldUseNative() ? Object.assign : function (target, source) {
  var from;
  var to = toObject(target);
  var symbols;

  for (var s = 1; s < arguments.length; s++) {
    from = Object(arguments[s]);

    for (var key in from) {
      if (hasOwnProperty.call(from, key)) {
        to[key] = from[key];
      }
    }

    if (getOwnPropertySymbols) {
      symbols = getOwnPropertySymbols(from);

      for (var i = 0; i < symbols.length; i++) {
        if (propIsEnumerable.call(from, symbols[i])) {
          to[symbols[i]] = from[symbols[i]];
        }
      }
    }
  }

  return to;
};
},{}],"../node_modules/prop-types/lib/ReactPropTypesSecret.js":[function(require,module,exports) {
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

'use strict';

var ReactPropTypesSecret = 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED';

module.exports = ReactPropTypesSecret;

},{}],"../node_modules/prop-types/checkPropTypes.js":[function(require,module,exports) {
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
'use strict';

var printWarning = function () {};

if ("development" !== 'production') {
  var ReactPropTypesSecret = require('./lib/ReactPropTypesSecret');

  var loggedTypeFailures = {};
  var has = Function.call.bind(Object.prototype.hasOwnProperty);

  printWarning = function (text) {
    var message = 'Warning: ' + text;

    if (typeof console !== 'undefined') {
      console.error(message);
    }

    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };
}
/**
 * Assert that the values match with the type specs.
 * Error messages are memorized and will only be shown once.
 *
 * @param {object} typeSpecs Map of name to a ReactPropType
 * @param {object} values Runtime values that need to be type-checked
 * @param {string} location e.g. "prop", "context", "child context"
 * @param {string} componentName Name of the component for error messages.
 * @param {?Function} getStack Returns the component stack.
 * @private
 */


function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
  if ("development" !== 'production') {
    for (var typeSpecName in typeSpecs) {
      if (has(typeSpecs, typeSpecName)) {
        var error; // Prop type validation may throw. In case they do, we don't want to
        // fail the render phase where it didn't fail before. So we log it.
        // After these have been cleaned up, we'll let them throw.

        try {
          // This is intentionally an invariant that gets caught. It's the same
          // behavior as without this statement except with a better message.
          if (typeof typeSpecs[typeSpecName] !== 'function') {
            var err = Error((componentName || 'React class') + ': ' + location + ' type `' + typeSpecName + '` is invalid; ' + 'it must be a function, usually from the `prop-types` package, but received `' + typeof typeSpecs[typeSpecName] + '`.');
            err.name = 'Invariant Violation';
            throw err;
          }

          error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
        } catch (ex) {
          error = ex;
        }

        if (error && !(error instanceof Error)) {
          printWarning((componentName || 'React class') + ': type specification of ' + location + ' `' + typeSpecName + '` is invalid; the type checker ' + 'function must return `null` or an `Error` but returned a ' + typeof error + '. ' + 'You may have forgotten to pass an argument to the type checker ' + 'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' + 'shape all require an argument).');
        }

        if (error instanceof Error && !(error.message in loggedTypeFailures)) {
          // Only monitor this failure once because there tends to be a lot of the
          // same error.
          loggedTypeFailures[error.message] = true;
          var stack = getStack ? getStack() : '';
          printWarning('Failed ' + location + ' type: ' + error.message + (stack != null ? stack : ''));
        }
      }
    }
  }
}
/**
 * Resets warning cache when testing.
 *
 * @private
 */


checkPropTypes.resetWarningCache = function () {
  if ("development" !== 'production') {
    loggedTypeFailures = {};
  }
};

module.exports = checkPropTypes;
},{"./lib/ReactPropTypesSecret":"../node_modules/prop-types/lib/ReactPropTypesSecret.js"}],"../node_modules/react/cjs/react.development.js":[function(require,module,exports) {
/** @license React v16.10.1
 * react.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
'use strict';

if ("development" !== "production") {
  (function () {
    'use strict';

    var _assign = require('object-assign');

    var checkPropTypes = require('prop-types/checkPropTypes'); // TODO: this is special because it gets imported during build.


    var ReactVersion = '16.10.1'; // The Symbol used to tag the ReactElement-like types. If there is no native Symbol
    // nor polyfill, then a plain number is used for performance.

    var hasSymbol = typeof Symbol === 'function' && Symbol.for;
    var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for('react.element') : 0xeac7;
    var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for('react.portal') : 0xeaca;
    var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for('react.fragment') : 0xeacb;
    var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for('react.strict_mode') : 0xeacc;
    var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for('react.profiler') : 0xead2;
    var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for('react.provider') : 0xeacd;
    var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for('react.context') : 0xeace; // TODO: We don't use AsyncMode or ConcurrentMode anymore. They were temporary
    // (unstable) APIs that have been removed. Can we remove the symbols?

    var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for('react.concurrent_mode') : 0xeacf;
    var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for('react.forward_ref') : 0xead0;
    var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for('react.suspense') : 0xead1;
    var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for('react.suspense_list') : 0xead8;
    var REACT_MEMO_TYPE = hasSymbol ? Symbol.for('react.memo') : 0xead3;
    var REACT_LAZY_TYPE = hasSymbol ? Symbol.for('react.lazy') : 0xead4;
    var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for('react.fundamental') : 0xead5;
    var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for('react.responder') : 0xead6;
    var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for('react.scope') : 0xead7;
    var MAYBE_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
    var FAUX_ITERATOR_SYMBOL = '@@iterator';

    function getIteratorFn(maybeIterable) {
      if (maybeIterable === null || typeof maybeIterable !== 'object') {
        return null;
      }

      var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];

      if (typeof maybeIterator === 'function') {
        return maybeIterator;
      }

      return null;
    } // Do not require this module directly! Use normal `invariant` calls with
    // template literal strings. The messages will be converted to ReactError during
    // build, and in production they will be minified.
    // Do not require this module directly! Use normal `invariant` calls with
    // template literal strings. The messages will be converted to ReactError during
    // build, and in production they will be minified.


    function ReactError(error) {
      error.name = 'Invariant Violation';
      return error;
    }
    /**
     * Use invariant() to assert state which your program assumes to be true.
     *
     * Provide sprintf-style format (only %s is supported) and arguments
     * to provide information about what broke and what you were
     * expecting.
     *
     * The invariant message will be stripped in production, but the invariant
     * will remain to ensure logic does not differ in production.
     */

    /**
     * Forked from fbjs/warning:
     * https://github.com/facebook/fbjs/blob/e66ba20ad5be433eb54423f2b097d829324d9de6/packages/fbjs/src/__forks__/warning.js
     *
     * Only change is we use console.warn instead of console.error,
     * and do nothing when 'console' is not supported.
     * This really simplifies the code.
     * ---
     * Similar to invariant but only logs a warning if the condition is not met.
     * This can be used to log issues in development environments in critical
     * paths. Removing the logging code for production environments will keep the
     * same logic and follow the same code paths.
     */


    var lowPriorityWarningWithoutStack = function () {};

    {
      var printWarning = function (format) {
        for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
          args[_key - 1] = arguments[_key];
        }

        var argIndex = 0;
        var message = 'Warning: ' + format.replace(/%s/g, function () {
          return args[argIndex++];
        });

        if (typeof console !== 'undefined') {
          console.warn(message);
        }

        try {
          // --- Welcome to debugging React ---
          // This error was thrown as a convenience so that you can use this stack
          // to find the callsite that caused this warning to fire.
          throw new Error(message);
        } catch (x) {}
      };

      lowPriorityWarningWithoutStack = function (condition, format) {
        if (format === undefined) {
          throw new Error('`lowPriorityWarningWithoutStack(condition, format, ...args)` requires a warning ' + 'message argument');
        }

        if (!condition) {
          for (var _len2 = arguments.length, args = new Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
            args[_key2 - 2] = arguments[_key2];
          }

          printWarning.apply(void 0, [format].concat(args));
        }
      };
    }
    var lowPriorityWarningWithoutStack$1 = lowPriorityWarningWithoutStack;
    /**
     * Similar to invariant but only logs a warning if the condition is not met.
     * This can be used to log issues in development environments in critical
     * paths. Removing the logging code for production environments will keep the
     * same logic and follow the same code paths.
     */

    var warningWithoutStack = function () {};

    {
      warningWithoutStack = function (condition, format) {
        for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
          args[_key - 2] = arguments[_key];
        }

        if (format === undefined) {
          throw new Error('`warningWithoutStack(condition, format, ...args)` requires a warning ' + 'message argument');
        }

        if (args.length > 8) {
          // Check before the condition to catch violations early.
          throw new Error('warningWithoutStack() currently supports at most 8 arguments.');
        }

        if (condition) {
          return;
        }

        if (typeof console !== 'undefined') {
          var argsWithFormat = args.map(function (item) {
            return '' + item;
          });
          argsWithFormat.unshift('Warning: ' + format); // We intentionally don't use spread (or .apply) directly because it
          // breaks IE9: https://github.com/facebook/react/issues/13610

          Function.prototype.apply.call(console.error, console, argsWithFormat);
        }

        try {
          // --- Welcome to debugging React ---
          // This error was thrown as a convenience so that you can use this stack
          // to find the callsite that caused this warning to fire.
          var argIndex = 0;
          var message = 'Warning: ' + format.replace(/%s/g, function () {
            return args[argIndex++];
          });
          throw new Error(message);
        } catch (x) {}
      };
    }
    var warningWithoutStack$1 = warningWithoutStack;
    var didWarnStateUpdateForUnmountedComponent = {};

    function warnNoop(publicInstance, callerName) {
      {
        var _constructor = publicInstance.constructor;
        var componentName = _constructor && (_constructor.displayName || _constructor.name) || 'ReactClass';
        var warningKey = componentName + "." + callerName;

        if (didWarnStateUpdateForUnmountedComponent[warningKey]) {
          return;
        }

        warningWithoutStack$1(false, "Can't call %s on a component that is not yet mounted. " + 'This is a no-op, but it might indicate a bug in your application. ' + 'Instead, assign to `this.state` directly or define a `state = {};` ' + 'class property with the desired state in the %s component.', callerName, componentName);
        didWarnStateUpdateForUnmountedComponent[warningKey] = true;
      }
    }
    /**
     * This is the abstract API for an update queue.
     */


    var ReactNoopUpdateQueue = {
      /**
       * Checks whether or not this composite component is mounted.
       * @param {ReactClass} publicInstance The instance we want to test.
       * @return {boolean} True if mounted, false otherwise.
       * @protected
       * @final
       */
      isMounted: function (publicInstance) {
        return false;
      },

      /**
       * Forces an update. This should only be invoked when it is known with
       * certainty that we are **not** in a DOM transaction.
       *
       * You may want to call this when you know that some deeper aspect of the
       * component's state has changed but `setState` was not called.
       *
       * This will not invoke `shouldComponentUpdate`, but it will invoke
       * `componentWillUpdate` and `componentDidUpdate`.
       *
       * @param {ReactClass} publicInstance The instance that should rerender.
       * @param {?function} callback Called after component is updated.
       * @param {?string} callerName name of the calling function in the public API.
       * @internal
       */
      enqueueForceUpdate: function (publicInstance, callback, callerName) {
        warnNoop(publicInstance, 'forceUpdate');
      },

      /**
       * Replaces all of the state. Always use this or `setState` to mutate state.
       * You should treat `this.state` as immutable.
       *
       * There is no guarantee that `this.state` will be immediately updated, so
       * accessing `this.state` after calling this method may return the old value.
       *
       * @param {ReactClass} publicInstance The instance that should rerender.
       * @param {object} completeState Next state.
       * @param {?function} callback Called after component is updated.
       * @param {?string} callerName name of the calling function in the public API.
       * @internal
       */
      enqueueReplaceState: function (publicInstance, completeState, callback, callerName) {
        warnNoop(publicInstance, 'replaceState');
      },

      /**
       * Sets a subset of the state. This only exists because _pendingState is
       * internal. This provides a merging strategy that is not available to deep
       * properties which is confusing. TODO: Expose pendingState or don't use it
       * during the merge.
       *
       * @param {ReactClass} publicInstance The instance that should rerender.
       * @param {object} partialState Next partial state to be merged with state.
       * @param {?function} callback Called after component is updated.
       * @param {?string} Name of the calling function in the public API.
       * @internal
       */
      enqueueSetState: function (publicInstance, partialState, callback, callerName) {
        warnNoop(publicInstance, 'setState');
      }
    };
    var emptyObject = {};
    {
      Object.freeze(emptyObject);
    }
    /**
     * Base class helpers for the updating state of a component.
     */

    function Component(props, context, updater) {
      this.props = props;
      this.context = context; // If a component has string refs, we will assign a different object later.

      this.refs = emptyObject; // We initialize the default updater but the real one gets injected by the
      // renderer.

      this.updater = updater || ReactNoopUpdateQueue;
    }

    Component.prototype.isReactComponent = {};
    /**
     * Sets a subset of the state. Always use this to mutate
     * state. You should treat `this.state` as immutable.
     *
     * There is no guarantee that `this.state` will be immediately updated, so
     * accessing `this.state` after calling this method may return the old value.
     *
     * There is no guarantee that calls to `setState` will run synchronously,
     * as they may eventually be batched together.  You can provide an optional
     * callback that will be executed when the call to setState is actually
     * completed.
     *
     * When a function is provided to setState, it will be called at some point in
     * the future (not synchronously). It will be called with the up to date
     * component arguments (state, props, context). These values can be different
     * from this.* because your function may be called after receiveProps but before
     * shouldComponentUpdate, and this new state, props, and context will not yet be
     * assigned to this.
     *
     * @param {object|function} partialState Next partial state or function to
     *        produce next partial state to be merged with current state.
     * @param {?function} callback Called after state is updated.
     * @final
     * @protected
     */

    Component.prototype.setState = function (partialState, callback) {
      (function () {
        if (!(typeof partialState === 'object' || typeof partialState === 'function' || partialState == null)) {
          {
            throw ReactError(Error("setState(...): takes an object of state variables to update or a function which returns an object of state variables."));
          }
        }
      })();

      this.updater.enqueueSetState(this, partialState, callback, 'setState');
    };
    /**
     * Forces an update. This should only be invoked when it is known with
     * certainty that we are **not** in a DOM transaction.
     *
     * You may want to call this when you know that some deeper aspect of the
     * component's state has changed but `setState` was not called.
     *
     * This will not invoke `shouldComponentUpdate`, but it will invoke
     * `componentWillUpdate` and `componentDidUpdate`.
     *
     * @param {?function} callback Called after update is complete.
     * @final
     * @protected
     */


    Component.prototype.forceUpdate = function (callback) {
      this.updater.enqueueForceUpdate(this, callback, 'forceUpdate');
    };
    /**
     * Deprecated APIs. These APIs used to exist on classic React classes but since
     * we would like to deprecate them, we're not going to move them over to this
     * modern base class. Instead, we define a getter that warns if it's accessed.
     */


    {
      var deprecatedAPIs = {
        isMounted: ['isMounted', 'Instead, make sure to clean up subscriptions and pending requests in ' + 'componentWillUnmount to prevent memory leaks.'],
        replaceState: ['replaceState', 'Refactor your code to use setState instead (see ' + 'https://github.com/facebook/react/issues/3236).']
      };

      var defineDeprecationWarning = function (methodName, info) {
        Object.defineProperty(Component.prototype, methodName, {
          get: function () {
            lowPriorityWarningWithoutStack$1(false, '%s(...) is deprecated in plain JavaScript React classes. %s', info[0], info[1]);
            return undefined;
          }
        });
      };

      for (var fnName in deprecatedAPIs) {
        if (deprecatedAPIs.hasOwnProperty(fnName)) {
          defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);
        }
      }
    }

    function ComponentDummy() {}

    ComponentDummy.prototype = Component.prototype;
    /**
     * Convenience component with default shallow equality check for sCU.
     */

    function PureComponent(props, context, updater) {
      this.props = props;
      this.context = context; // If a component has string refs, we will assign a different object later.

      this.refs = emptyObject;
      this.updater = updater || ReactNoopUpdateQueue;
    }

    var pureComponentPrototype = PureComponent.prototype = new ComponentDummy();
    pureComponentPrototype.constructor = PureComponent; // Avoid an extra prototype jump for these methods.

    _assign(pureComponentPrototype, Component.prototype);

    pureComponentPrototype.isPureReactComponent = true; // an immutable object with a single mutable value

    function createRef() {
      var refObject = {
        current: null
      };
      {
        Object.seal(refObject);
      }
      return refObject;
    }
    /**
     * Keeps track of the current dispatcher.
     */


    var ReactCurrentDispatcher = {
      /**
       * @internal
       * @type {ReactComponent}
       */
      current: null
    };
    /**
     * Keeps track of the current batch's configuration such as how long an update
     * should suspend for if it needs to.
     */

    var ReactCurrentBatchConfig = {
      suspense: null
    };
    /**
     * Keeps track of the current owner.
     *
     * The current owner is the component who should own any components that are
     * currently being constructed.
     */

    var ReactCurrentOwner = {
      /**
       * @internal
       * @type {ReactComponent}
       */
      current: null
    };
    var BEFORE_SLASH_RE = /^(.*)[\\\/]/;

    var describeComponentFrame = function (name, source, ownerName) {
      var sourceInfo = '';

      if (source) {
        var path = source.fileName;
        var fileName = path.replace(BEFORE_SLASH_RE, '');
        {
          // In DEV, include code for a common special case:
          // prefer "folder/index.js" instead of just "index.js".
          if (/^index\./.test(fileName)) {
            var match = path.match(BEFORE_SLASH_RE);

            if (match) {
              var pathBeforeSlash = match[1];

              if (pathBeforeSlash) {
                var folderName = pathBeforeSlash.replace(BEFORE_SLASH_RE, '');
                fileName = folderName + '/' + fileName;
              }
            }
          }
        }
        sourceInfo = ' (at ' + fileName + ':' + source.lineNumber + ')';
      } else if (ownerName) {
        sourceInfo = ' (created by ' + ownerName + ')';
      }

      return '\n    in ' + (name || 'Unknown') + sourceInfo;
    };

    var Resolved = 1;

    function refineResolvedLazyComponent(lazyComponent) {
      return lazyComponent._status === Resolved ? lazyComponent._result : null;
    }

    function getWrappedName(outerType, innerType, wrapperName) {
      var functionName = innerType.displayName || innerType.name || '';
      return outerType.displayName || (functionName !== '' ? wrapperName + "(" + functionName + ")" : wrapperName);
    }

    function getComponentName(type) {
      if (type == null) {
        // Host root, text node or just invalid type.
        return null;
      }

      {
        if (typeof type.tag === 'number') {
          warningWithoutStack$1(false, 'Received an unexpected object in getComponentName(). ' + 'This is likely a bug in React. Please file an issue.');
        }
      }

      if (typeof type === 'function') {
        return type.displayName || type.name || null;
      }

      if (typeof type === 'string') {
        return type;
      }

      switch (type) {
        case REACT_FRAGMENT_TYPE:
          return 'Fragment';

        case REACT_PORTAL_TYPE:
          return 'Portal';

        case REACT_PROFILER_TYPE:
          return "Profiler";

        case REACT_STRICT_MODE_TYPE:
          return 'StrictMode';

        case REACT_SUSPENSE_TYPE:
          return 'Suspense';

        case REACT_SUSPENSE_LIST_TYPE:
          return 'SuspenseList';
      }

      if (typeof type === 'object') {
        switch (type.$$typeof) {
          case REACT_CONTEXT_TYPE:
            return 'Context.Consumer';

          case REACT_PROVIDER_TYPE:
            return 'Context.Provider';

          case REACT_FORWARD_REF_TYPE:
            return getWrappedName(type, type.render, 'ForwardRef');

          case REACT_MEMO_TYPE:
            return getComponentName(type.type);

          case REACT_LAZY_TYPE:
            {
              var thenable = type;
              var resolvedThenable = refineResolvedLazyComponent(thenable);

              if (resolvedThenable) {
                return getComponentName(resolvedThenable);
              }

              break;
            }
        }
      }

      return null;
    }

    var ReactDebugCurrentFrame = {};
    var currentlyValidatingElement = null;

    function setCurrentlyValidatingElement(element) {
      {
        currentlyValidatingElement = element;
      }
    }

    {
      // Stack implementation injected by the current renderer.
      ReactDebugCurrentFrame.getCurrentStack = null;

      ReactDebugCurrentFrame.getStackAddendum = function () {
        var stack = ''; // Add an extra top frame while an element is being validated

        if (currentlyValidatingElement) {
          var name = getComponentName(currentlyValidatingElement.type);
          var owner = currentlyValidatingElement._owner;
          stack += describeComponentFrame(name, currentlyValidatingElement._source, owner && getComponentName(owner.type));
        } // Delegate to the injected renderer-specific implementation


        var impl = ReactDebugCurrentFrame.getCurrentStack;

        if (impl) {
          stack += impl() || '';
        }

        return stack;
      };
    }
    /**
     * Used by act() to track whether you're inside an act() scope.
     */

    var IsSomeRendererActing = {
      current: false
    };
    var ReactSharedInternals = {
      ReactCurrentDispatcher: ReactCurrentDispatcher,
      ReactCurrentBatchConfig: ReactCurrentBatchConfig,
      ReactCurrentOwner: ReactCurrentOwner,
      IsSomeRendererActing: IsSomeRendererActing,
      // Used by renderers to avoid bundling object-assign twice in UMD bundles:
      assign: _assign
    };
    {
      _assign(ReactSharedInternals, {
        // These should not be included in production.
        ReactDebugCurrentFrame: ReactDebugCurrentFrame,
        // Shim for React DOM 16.0.0 which still destructured (but not used) this.
        // TODO: remove in React 17.0.
        ReactComponentTreeHook: {}
      });
    }
    /**
     * Similar to invariant but only logs a warning if the condition is not met.
     * This can be used to log issues in development environments in critical
     * paths. Removing the logging code for production environments will keep the
     * same logic and follow the same code paths.
     */

    var warning = warningWithoutStack$1;
    {
      warning = function (condition, format) {
        if (condition) {
          return;
        }

        var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
        var stack = ReactDebugCurrentFrame.getStackAddendum(); // eslint-disable-next-line react-internal/warning-and-invariant-args

        for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
          args[_key - 2] = arguments[_key];
        }

        warningWithoutStack$1.apply(void 0, [false, format + '%s'].concat(args, [stack]));
      };
    }
    var warning$1 = warning;
    var hasOwnProperty = Object.prototype.hasOwnProperty;
    var RESERVED_PROPS = {
      key: true,
      ref: true,
      __self: true,
      __source: true
    };
    var specialPropKeyWarningShown;
    var specialPropRefWarningShown;

    function hasValidRef(config) {
      {
        if (hasOwnProperty.call(config, 'ref')) {
          var getter = Object.getOwnPropertyDescriptor(config, 'ref').get;

          if (getter && getter.isReactWarning) {
            return false;
          }
        }
      }
      return config.ref !== undefined;
    }

    function hasValidKey(config) {
      {
        if (hasOwnProperty.call(config, 'key')) {
          var getter = Object.getOwnPropertyDescriptor(config, 'key').get;

          if (getter && getter.isReactWarning) {
            return false;
          }
        }
      }
      return config.key !== undefined;
    }

    function defineKeyPropWarningGetter(props, displayName) {
      var warnAboutAccessingKey = function () {
        if (!specialPropKeyWarningShown) {
          specialPropKeyWarningShown = true;
          warningWithoutStack$1(false, '%s: `key` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);
        }
      };

      warnAboutAccessingKey.isReactWarning = true;
      Object.defineProperty(props, 'key', {
        get: warnAboutAccessingKey,
        configurable: true
      });
    }

    function defineRefPropWarningGetter(props, displayName) {
      var warnAboutAccessingRef = function () {
        if (!specialPropRefWarningShown) {
          specialPropRefWarningShown = true;
          warningWithoutStack$1(false, '%s: `ref` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);
        }
      };

      warnAboutAccessingRef.isReactWarning = true;
      Object.defineProperty(props, 'ref', {
        get: warnAboutAccessingRef,
        configurable: true
      });
    }
    /**
     * Factory method to create a new React element. This no longer adheres to
     * the class pattern, so do not use new to call it. Also, no instanceof check
     * will work. Instead test $$typeof field against Symbol.for('react.element') to check
     * if something is a React Element.
     *
     * @param {*} type
     * @param {*} props
     * @param {*} key
     * @param {string|object} ref
     * @param {*} owner
     * @param {*} self A *temporary* helper to detect places where `this` is
     * different from the `owner` when React.createElement is called, so that we
     * can warn. We want to get rid of owner and replace string `ref`s with arrow
     * functions, and as long as `this` and owner are the same, there will be no
     * change in behavior.
     * @param {*} source An annotation object (added by a transpiler or otherwise)
     * indicating filename, line number, and/or other information.
     * @internal
     */


    var ReactElement = function (type, key, ref, self, source, owner, props) {
      var element = {
        // This tag allows us to uniquely identify this as a React Element
        $$typeof: REACT_ELEMENT_TYPE,
        // Built-in properties that belong on the element
        type: type,
        key: key,
        ref: ref,
        props: props,
        // Record the component responsible for creating this element.
        _owner: owner
      };
      {
        // The validation flag is currently mutative. We put it on
        // an external backing store so that we can freeze the whole object.
        // This can be replaced with a WeakMap once they are implemented in
        // commonly used development environments.
        element._store = {}; // To make comparing ReactElements easier for testing purposes, we make
        // the validation flag non-enumerable (where possible, which should
        // include every environment we run tests in), so the test framework
        // ignores it.

        Object.defineProperty(element._store, 'validated', {
          configurable: false,
          enumerable: false,
          writable: true,
          value: false
        }); // self and source are DEV only properties.

        Object.defineProperty(element, '_self', {
          configurable: false,
          enumerable: false,
          writable: false,
          value: self
        }); // Two elements created in two different places should be considered
        // equal for testing purposes and therefore we hide it from enumeration.

        Object.defineProperty(element, '_source', {
          configurable: false,
          enumerable: false,
          writable: false,
          value: source
        });

        if (Object.freeze) {
          Object.freeze(element.props);
          Object.freeze(element);
        }
      }
      return element;
    };
    /**
     * https://github.com/reactjs/rfcs/pull/107
     * @param {*} type
     * @param {object} props
     * @param {string} key
     */

    /**
     * https://github.com/reactjs/rfcs/pull/107
     * @param {*} type
     * @param {object} props
     * @param {string} key
     */


    function jsxDEV(type, config, maybeKey, source, self) {
      var propName; // Reserved names are extracted

      var props = {};
      var key = null;
      var ref = null; // Currently, key can be spread in as a prop. This causes a potential
      // issue if key is also explicitly declared (ie. <div {...props} key="Hi" />
      // or <div key="Hi" {...props} /> ). We want to deprecate key spread,
      // but as an intermediary step, we will use jsxDEV for everything except
      // <div {...props} key="Hi" />, because we aren't currently able to tell if
      // key is explicitly declared to be undefined or not.

      if (maybeKey !== undefined) {
        key = '' + maybeKey;
      }

      if (hasValidKey(config)) {
        key = '' + config.key;
      }

      if (hasValidRef(config)) {
        ref = config.ref;
      } // Remaining properties are added to a new props object


      for (propName in config) {
        if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
          props[propName] = config[propName];
        }
      } // Resolve default props


      if (type && type.defaultProps) {
        var defaultProps = type.defaultProps;

        for (propName in defaultProps) {
          if (props[propName] === undefined) {
            props[propName] = defaultProps[propName];
          }
        }
      }

      if (key || ref) {
        var displayName = typeof type === 'function' ? type.displayName || type.name || 'Unknown' : type;

        if (key) {
          defineKeyPropWarningGetter(props, displayName);
        }

        if (ref) {
          defineRefPropWarningGetter(props, displayName);
        }
      }

      return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
    }
    /**
     * Create and return a new ReactElement of the given type.
     * See https://reactjs.org/docs/react-api.html#createelement
     */


    function createElement(type, config, children) {
      var propName; // Reserved names are extracted

      var props = {};
      var key = null;
      var ref = null;
      var self = null;
      var source = null;

      if (config != null) {
        if (hasValidRef(config)) {
          ref = config.ref;
        }

        if (hasValidKey(config)) {
          key = '' + config.key;
        }

        self = config.__self === undefined ? null : config.__self;
        source = config.__source === undefined ? null : config.__source; // Remaining properties are added to a new props object

        for (propName in config) {
          if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
            props[propName] = config[propName];
          }
        }
      } // Children can be more than one argument, and those are transferred onto
      // the newly allocated props object.


      var childrenLength = arguments.length - 2;

      if (childrenLength === 1) {
        props.children = children;
      } else if (childrenLength > 1) {
        var childArray = Array(childrenLength);

        for (var i = 0; i < childrenLength; i++) {
          childArray[i] = arguments[i + 2];
        }

        {
          if (Object.freeze) {
            Object.freeze(childArray);
          }
        }
        props.children = childArray;
      } // Resolve default props


      if (type && type.defaultProps) {
        var defaultProps = type.defaultProps;

        for (propName in defaultProps) {
          if (props[propName] === undefined) {
            props[propName] = defaultProps[propName];
          }
        }
      }

      {
        if (key || ref) {
          var displayName = typeof type === 'function' ? type.displayName || type.name || 'Unknown' : type;

          if (key) {
            defineKeyPropWarningGetter(props, displayName);
          }

          if (ref) {
            defineRefPropWarningGetter(props, displayName);
          }
        }
      }
      return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
    }
    /**
     * Return a function that produces ReactElements of a given type.
     * See https://reactjs.org/docs/react-api.html#createfactory
     */


    function cloneAndReplaceKey(oldElement, newKey) {
      var newElement = ReactElement(oldElement.type, newKey, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, oldElement.props);
      return newElement;
    }
    /**
     * Clone and return a new ReactElement using element as the starting point.
     * See https://reactjs.org/docs/react-api.html#cloneelement
     */


    function cloneElement(element, config, children) {
      (function () {
        if (!!(element === null || element === undefined)) {
          {
            throw ReactError(Error("React.cloneElement(...): The argument must be a React element, but you passed " + element + "."));
          }
        }
      })();

      var propName; // Original props are copied

      var props = _assign({}, element.props); // Reserved names are extracted


      var key = element.key;
      var ref = element.ref; // Self is preserved since the owner is preserved.

      var self = element._self; // Source is preserved since cloneElement is unlikely to be targeted by a
      // transpiler, and the original source is probably a better indicator of the
      // true owner.

      var source = element._source; // Owner will be preserved, unless ref is overridden

      var owner = element._owner;

      if (config != null) {
        if (hasValidRef(config)) {
          // Silently steal the ref from the parent.
          ref = config.ref;
          owner = ReactCurrentOwner.current;
        }

        if (hasValidKey(config)) {
          key = '' + config.key;
        } // Remaining properties override existing props


        var defaultProps;

        if (element.type && element.type.defaultProps) {
          defaultProps = element.type.defaultProps;
        }

        for (propName in config) {
          if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
            if (config[propName] === undefined && defaultProps !== undefined) {
              // Resolve default props
              props[propName] = defaultProps[propName];
            } else {
              props[propName] = config[propName];
            }
          }
        }
      } // Children can be more than one argument, and those are transferred onto
      // the newly allocated props object.


      var childrenLength = arguments.length - 2;

      if (childrenLength === 1) {
        props.children = children;
      } else if (childrenLength > 1) {
        var childArray = Array(childrenLength);

        for (var i = 0; i < childrenLength; i++) {
          childArray[i] = arguments[i + 2];
        }

        props.children = childArray;
      }

      return ReactElement(element.type, key, ref, self, source, owner, props);
    }
    /**
     * Verifies the object is a ReactElement.
     * See https://reactjs.org/docs/react-api.html#isvalidelement
     * @param {?object} object
     * @return {boolean} True if `object` is a ReactElement.
     * @final
     */


    function isValidElement(object) {
      return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
    }

    var SEPARATOR = '.';
    var SUBSEPARATOR = ':';
    /**
     * Escape and wrap key so it is safe to use as a reactid
     *
     * @param {string} key to be escaped.
     * @return {string} the escaped key.
     */

    function escape(key) {
      var escapeRegex = /[=:]/g;
      var escaperLookup = {
        '=': '=0',
        ':': '=2'
      };
      var escapedString = ('' + key).replace(escapeRegex, function (match) {
        return escaperLookup[match];
      });
      return '$' + escapedString;
    }
    /**
     * TODO: Test that a single child and an array with one item have the same key
     * pattern.
     */


    var didWarnAboutMaps = false;
    var userProvidedKeyEscapeRegex = /\/+/g;

    function escapeUserProvidedKey(text) {
      return ('' + text).replace(userProvidedKeyEscapeRegex, '$&/');
    }

    var POOL_SIZE = 10;
    var traverseContextPool = [];

    function getPooledTraverseContext(mapResult, keyPrefix, mapFunction, mapContext) {
      if (traverseContextPool.length) {
        var traverseContext = traverseContextPool.pop();
        traverseContext.result = mapResult;
        traverseContext.keyPrefix = keyPrefix;
        traverseContext.func = mapFunction;
        traverseContext.context = mapContext;
        traverseContext.count = 0;
        return traverseContext;
      } else {
        return {
          result: mapResult,
          keyPrefix: keyPrefix,
          func: mapFunction,
          context: mapContext,
          count: 0
        };
      }
    }

    function releaseTraverseContext(traverseContext) {
      traverseContext.result = null;
      traverseContext.keyPrefix = null;
      traverseContext.func = null;
      traverseContext.context = null;
      traverseContext.count = 0;

      if (traverseContextPool.length < POOL_SIZE) {
        traverseContextPool.push(traverseContext);
      }
    }
    /**
     * @param {?*} children Children tree container.
     * @param {!string} nameSoFar Name of the key path so far.
     * @param {!function} callback Callback to invoke with each child found.
     * @param {?*} traverseContext Used to pass information throughout the traversal
     * process.
     * @return {!number} The number of children in this subtree.
     */


    function traverseAllChildrenImpl(children, nameSoFar, callback, traverseContext) {
      var type = typeof children;

      if (type === 'undefined' || type === 'boolean') {
        // All of the above are perceived as null.
        children = null;
      }

      var invokeCallback = false;

      if (children === null) {
        invokeCallback = true;
      } else {
        switch (type) {
          case 'string':
          case 'number':
            invokeCallback = true;
            break;

          case 'object':
            switch (children.$$typeof) {
              case REACT_ELEMENT_TYPE:
              case REACT_PORTAL_TYPE:
                invokeCallback = true;
            }

        }
      }

      if (invokeCallback) {
        callback(traverseContext, children, // If it's the only child, treat the name as if it was wrapped in an array
        // so that it's consistent if the number of children grows.
        nameSoFar === '' ? SEPARATOR + getComponentKey(children, 0) : nameSoFar);
        return 1;
      }

      var child;
      var nextName;
      var subtreeCount = 0; // Count of children found in the current subtree.

      var nextNamePrefix = nameSoFar === '' ? SEPARATOR : nameSoFar + SUBSEPARATOR;

      if (Array.isArray(children)) {
        for (var i = 0; i < children.length; i++) {
          child = children[i];
          nextName = nextNamePrefix + getComponentKey(child, i);
          subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
        }
      } else {
        var iteratorFn = getIteratorFn(children);

        if (typeof iteratorFn === 'function') {
          {
            // Warn about using Maps as children
            if (iteratorFn === children.entries) {
              !didWarnAboutMaps ? warning$1(false, 'Using Maps as children is unsupported and will likely yield ' + 'unexpected results. Convert it to a sequence/iterable of keyed ' + 'ReactElements instead.') : void 0;
              didWarnAboutMaps = true;
            }
          }
          var iterator = iteratorFn.call(children);
          var step;
          var ii = 0;

          while (!(step = iterator.next()).done) {
            child = step.value;
            nextName = nextNamePrefix + getComponentKey(child, ii++);
            subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
          }
        } else if (type === 'object') {
          var addendum = '';
          {
            addendum = ' If you meant to render a collection of children, use an array ' + 'instead.' + ReactDebugCurrentFrame.getStackAddendum();
          }
          var childrenString = '' + children;

          (function () {
            {
              {
                throw ReactError(Error("Objects are not valid as a React child (found: " + (childrenString === '[object Object]' ? 'object with keys {' + Object.keys(children).join(', ') + '}' : childrenString) + ")." + addendum));
              }
            }
          })();
        }
      }

      return subtreeCount;
    }
    /**
     * Traverses children that are typically specified as `props.children`, but
     * might also be specified through attributes:
     *
     * - `traverseAllChildren(this.props.children, ...)`
     * - `traverseAllChildren(this.props.leftPanelChildren, ...)`
     *
     * The `traverseContext` is an optional argument that is passed through the
     * entire traversal. It can be used to store accumulations or anything else that
     * the callback might find relevant.
     *
     * @param {?*} children Children tree object.
     * @param {!function} callback To invoke upon traversing each child.
     * @param {?*} traverseContext Context for traversal.
     * @return {!number} The number of children in this subtree.
     */


    function traverseAllChildren(children, callback, traverseContext) {
      if (children == null) {
        return 0;
      }

      return traverseAllChildrenImpl(children, '', callback, traverseContext);
    }
    /**
     * Generate a key string that identifies a component within a set.
     *
     * @param {*} component A component that could contain a manual key.
     * @param {number} index Index that is used if a manual key is not provided.
     * @return {string}
     */


    function getComponentKey(component, index) {
      // Do some typechecking here since we call this blindly. We want to ensure
      // that we don't block potential future ES APIs.
      if (typeof component === 'object' && component !== null && component.key != null) {
        // Explicit key
        return escape(component.key);
      } // Implicit key determined by the index in the set


      return index.toString(36);
    }

    function forEachSingleChild(bookKeeping, child, name) {
      var func = bookKeeping.func,
          context = bookKeeping.context;
      func.call(context, child, bookKeeping.count++);
    }
    /**
     * Iterates through children that are typically specified as `props.children`.
     *
     * See https://reactjs.org/docs/react-api.html#reactchildrenforeach
     *
     * The provided forEachFunc(child, index) will be called for each
     * leaf child.
     *
     * @param {?*} children Children tree container.
     * @param {function(*, int)} forEachFunc
     * @param {*} forEachContext Context for forEachContext.
     */


    function forEachChildren(children, forEachFunc, forEachContext) {
      if (children == null) {
        return children;
      }

      var traverseContext = getPooledTraverseContext(null, null, forEachFunc, forEachContext);
      traverseAllChildren(children, forEachSingleChild, traverseContext);
      releaseTraverseContext(traverseContext);
    }

    function mapSingleChildIntoContext(bookKeeping, child, childKey) {
      var result = bookKeeping.result,
          keyPrefix = bookKeeping.keyPrefix,
          func = bookKeeping.func,
          context = bookKeeping.context;
      var mappedChild = func.call(context, child, bookKeeping.count++);

      if (Array.isArray(mappedChild)) {
        mapIntoWithKeyPrefixInternal(mappedChild, result, childKey, function (c) {
          return c;
        });
      } else if (mappedChild != null) {
        if (isValidElement(mappedChild)) {
          mappedChild = cloneAndReplaceKey(mappedChild, // Keep both the (mapped) and old keys if they differ, just as
          // traverseAllChildren used to do for objects as children
          keyPrefix + (mappedChild.key && (!child || child.key !== mappedChild.key) ? escapeUserProvidedKey(mappedChild.key) + '/' : '') + childKey);
        }

        result.push(mappedChild);
      }
    }

    function mapIntoWithKeyPrefixInternal(children, array, prefix, func, context) {
      var escapedPrefix = '';

      if (prefix != null) {
        escapedPrefix = escapeUserProvidedKey(prefix) + '/';
      }

      var traverseContext = getPooledTraverseContext(array, escapedPrefix, func, context);
      traverseAllChildren(children, mapSingleChildIntoContext, traverseContext);
      releaseTraverseContext(traverseContext);
    }
    /**
     * Maps children that are typically specified as `props.children`.
     *
     * See https://reactjs.org/docs/react-api.html#reactchildrenmap
     *
     * The provided mapFunction(child, key, index) will be called for each
     * leaf child.
     *
     * @param {?*} children Children tree container.
     * @param {function(*, int)} func The map function.
     * @param {*} context Context for mapFunction.
     * @return {object} Object containing the ordered map of results.
     */


    function mapChildren(children, func, context) {
      if (children == null) {
        return children;
      }

      var result = [];
      mapIntoWithKeyPrefixInternal(children, result, null, func, context);
      return result;
    }
    /**
     * Count the number of children that are typically specified as
     * `props.children`.
     *
     * See https://reactjs.org/docs/react-api.html#reactchildrencount
     *
     * @param {?*} children Children tree container.
     * @return {number} The number of children.
     */


    function countChildren(children) {
      return traverseAllChildren(children, function () {
        return null;
      }, null);
    }
    /**
     * Flatten a children object (typically specified as `props.children`) and
     * return an array with appropriately re-keyed children.
     *
     * See https://reactjs.org/docs/react-api.html#reactchildrentoarray
     */


    function toArray(children) {
      var result = [];
      mapIntoWithKeyPrefixInternal(children, result, null, function (child) {
        return child;
      });
      return result;
    }
    /**
     * Returns the first child in a collection of children and verifies that there
     * is only one child in the collection.
     *
     * See https://reactjs.org/docs/react-api.html#reactchildrenonly
     *
     * The current implementation of this function assumes that a single child gets
     * passed without a wrapper, but the purpose of this helper function is to
     * abstract away the particular structure of children.
     *
     * @param {?object} children Child collection structure.
     * @return {ReactElement} The first and only `ReactElement` contained in the
     * structure.
     */


    function onlyChild(children) {
      (function () {
        if (!isValidElement(children)) {
          {
            throw ReactError(Error("React.Children.only expected to receive a single React element child."));
          }
        }
      })();

      return children;
    }

    function createContext(defaultValue, calculateChangedBits) {
      if (calculateChangedBits === undefined) {
        calculateChangedBits = null;
      } else {
        {
          !(calculateChangedBits === null || typeof calculateChangedBits === 'function') ? warningWithoutStack$1(false, 'createContext: Expected the optional second argument to be a ' + 'function. Instead received: %s', calculateChangedBits) : void 0;
        }
      }

      var context = {
        $$typeof: REACT_CONTEXT_TYPE,
        _calculateChangedBits: calculateChangedBits,
        // As a workaround to support multiple concurrent renderers, we categorize
        // some renderers as primary and others as secondary. We only expect
        // there to be two concurrent renderers at most: React Native (primary) and
        // Fabric (secondary); React DOM (primary) and React ART (secondary).
        // Secondary renderers store their context values on separate fields.
        _currentValue: defaultValue,
        _currentValue2: defaultValue,
        // Used to track how many concurrent renderers this context currently
        // supports within in a single renderer. Such as parallel server rendering.
        _threadCount: 0,
        // These are circular
        Provider: null,
        Consumer: null
      };
      context.Provider = {
        $$typeof: REACT_PROVIDER_TYPE,
        _context: context
      };
      var hasWarnedAboutUsingNestedContextConsumers = false;
      var hasWarnedAboutUsingConsumerProvider = false;
      {
        // A separate object, but proxies back to the original context object for
        // backwards compatibility. It has a different $$typeof, so we can properly
        // warn for the incorrect usage of Context as a Consumer.
        var Consumer = {
          $$typeof: REACT_CONTEXT_TYPE,
          _context: context,
          _calculateChangedBits: context._calculateChangedBits
        }; // $FlowFixMe: Flow complains about not setting a value, which is intentional here

        Object.defineProperties(Consumer, {
          Provider: {
            get: function () {
              if (!hasWarnedAboutUsingConsumerProvider) {
                hasWarnedAboutUsingConsumerProvider = true;
                warning$1(false, 'Rendering <Context.Consumer.Provider> is not supported and will be removed in ' + 'a future major release. Did you mean to render <Context.Provider> instead?');
              }

              return context.Provider;
            },
            set: function (_Provider) {
              context.Provider = _Provider;
            }
          },
          _currentValue: {
            get: function () {
              return context._currentValue;
            },
            set: function (_currentValue) {
              context._currentValue = _currentValue;
            }
          },
          _currentValue2: {
            get: function () {
              return context._currentValue2;
            },
            set: function (_currentValue2) {
              context._currentValue2 = _currentValue2;
            }
          },
          _threadCount: {
            get: function () {
              return context._threadCount;
            },
            set: function (_threadCount) {
              context._threadCount = _threadCount;
            }
          },
          Consumer: {
            get: function () {
              if (!hasWarnedAboutUsingNestedContextConsumers) {
                hasWarnedAboutUsingNestedContextConsumers = true;
                warning$1(false, 'Rendering <Context.Consumer.Consumer> is not supported and will be removed in ' + 'a future major release. Did you mean to render <Context.Consumer> instead?');
              }

              return context.Consumer;
            }
          }
        }); // $FlowFixMe: Flow complains about missing properties because it doesn't understand defineProperty

        context.Consumer = Consumer;
      }
      {
        context._currentRenderer = null;
        context._currentRenderer2 = null;
      }
      return context;
    }

    function lazy(ctor) {
      var lazyType = {
        $$typeof: REACT_LAZY_TYPE,
        _ctor: ctor,
        // React uses these fields to store the result.
        _status: -1,
        _result: null
      };
      {
        // In production, this would just set it on the object.
        var defaultProps;
        var propTypes;
        Object.defineProperties(lazyType, {
          defaultProps: {
            configurable: true,
            get: function () {
              return defaultProps;
            },
            set: function (newDefaultProps) {
              warning$1(false, 'React.lazy(...): It is not supported to assign `defaultProps` to ' + 'a lazy component import. Either specify them where the component ' + 'is defined, or create a wrapping component around it.');
              defaultProps = newDefaultProps; // Match production behavior more closely:

              Object.defineProperty(lazyType, 'defaultProps', {
                enumerable: true
              });
            }
          },
          propTypes: {
            configurable: true,
            get: function () {
              return propTypes;
            },
            set: function (newPropTypes) {
              warning$1(false, 'React.lazy(...): It is not supported to assign `propTypes` to ' + 'a lazy component import. Either specify them where the component ' + 'is defined, or create a wrapping component around it.');
              propTypes = newPropTypes; // Match production behavior more closely:

              Object.defineProperty(lazyType, 'propTypes', {
                enumerable: true
              });
            }
          }
        });
      }
      return lazyType;
    }

    function forwardRef(render) {
      {
        if (render != null && render.$$typeof === REACT_MEMO_TYPE) {
          warningWithoutStack$1(false, 'forwardRef requires a render function but received a `memo` ' + 'component. Instead of forwardRef(memo(...)), use ' + 'memo(forwardRef(...)).');
        } else if (typeof render !== 'function') {
          warningWithoutStack$1(false, 'forwardRef requires a render function but was given %s.', render === null ? 'null' : typeof render);
        } else {
          !( // Do not warn for 0 arguments because it could be due to usage of the 'arguments' object
          render.length === 0 || render.length === 2) ? warningWithoutStack$1(false, 'forwardRef render functions accept exactly two parameters: props and ref. %s', render.length === 1 ? 'Did you forget to use the ref parameter?' : 'Any additional parameter will be undefined.') : void 0;
        }

        if (render != null) {
          !(render.defaultProps == null && render.propTypes == null) ? warningWithoutStack$1(false, 'forwardRef render functions do not support propTypes or defaultProps. ' + 'Did you accidentally pass a React component?') : void 0;
        }
      }
      return {
        $$typeof: REACT_FORWARD_REF_TYPE,
        render: render
      };
    }

    function isValidElementType(type) {
      return typeof type === 'string' || typeof type === 'function' || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
      type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === 'object' && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE);
    }

    function memo(type, compare) {
      {
        if (!isValidElementType(type)) {
          warningWithoutStack$1(false, 'memo: The first argument must be a component. Instead ' + 'received: %s', type === null ? 'null' : typeof type);
        }
      }
      return {
        $$typeof: REACT_MEMO_TYPE,
        type: type,
        compare: compare === undefined ? null : compare
      };
    }

    function resolveDispatcher() {
      var dispatcher = ReactCurrentDispatcher.current;

      (function () {
        if (!(dispatcher !== null)) {
          {
            throw ReactError(Error("Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://fb.me/react-invalid-hook-call for tips about how to debug and fix this problem."));
          }
        }
      })();

      return dispatcher;
    }

    function useContext(Context, unstable_observedBits) {
      var dispatcher = resolveDispatcher();
      {
        !(unstable_observedBits === undefined) ? warning$1(false, 'useContext() second argument is reserved for future ' + 'use in React. Passing it is not supported. ' + 'You passed: %s.%s', unstable_observedBits, typeof unstable_observedBits === 'number' && Array.isArray(arguments[2]) ? '\n\nDid you call array.map(useContext)? ' + 'Calling Hooks inside a loop is not supported. ' + 'Learn more at https://fb.me/rules-of-hooks' : '') : void 0; // TODO: add a more generic warning for invalid values.

        if (Context._context !== undefined) {
          var realContext = Context._context; // Don't deduplicate because this legitimately causes bugs
          // and nobody should be using this in existing code.

          if (realContext.Consumer === Context) {
            warning$1(false, 'Calling useContext(Context.Consumer) is not supported, may cause bugs, and will be ' + 'removed in a future major release. Did you mean to call useContext(Context) instead?');
          } else if (realContext.Provider === Context) {
            warning$1(false, 'Calling useContext(Context.Provider) is not supported. ' + 'Did you mean to call useContext(Context) instead?');
          }
        }
      }
      return dispatcher.useContext(Context, unstable_observedBits);
    }

    function useState(initialState) {
      var dispatcher = resolveDispatcher();
      return dispatcher.useState(initialState);
    }

    function useReducer(reducer, initialArg, init) {
      var dispatcher = resolveDispatcher();
      return dispatcher.useReducer(reducer, initialArg, init);
    }

    function useRef(initialValue) {
      var dispatcher = resolveDispatcher();
      return dispatcher.useRef(initialValue);
    }

    function useEffect(create, inputs) {
      var dispatcher = resolveDispatcher();
      return dispatcher.useEffect(create, inputs);
    }

    function useLayoutEffect(create, inputs) {
      var dispatcher = resolveDispatcher();
      return dispatcher.useLayoutEffect(create, inputs);
    }

    function useCallback(callback, inputs) {
      var dispatcher = resolveDispatcher();
      return dispatcher.useCallback(callback, inputs);
    }

    function useMemo(create, inputs) {
      var dispatcher = resolveDispatcher();
      return dispatcher.useMemo(create, inputs);
    }

    function useImperativeHandle(ref, create, inputs) {
      var dispatcher = resolveDispatcher();
      return dispatcher.useImperativeHandle(ref, create, inputs);
    }

    function useDebugValue(value, formatterFn) {
      {
        var dispatcher = resolveDispatcher();
        return dispatcher.useDebugValue(value, formatterFn);
      }
    }

    var emptyObject$1 = {};

    function useResponder(responder, listenerProps) {
      var dispatcher = resolveDispatcher();
      {
        if (responder == null || responder.$$typeof !== REACT_RESPONDER_TYPE) {
          warning$1(false, 'useResponder: invalid first argument. Expected an event responder, but instead got %s', responder);
          return;
        }
      }
      return dispatcher.useResponder(responder, listenerProps || emptyObject$1);
    }

    function withSuspenseConfig(scope, config) {
      var previousConfig = ReactCurrentBatchConfig.suspense;
      ReactCurrentBatchConfig.suspense = config === undefined ? null : config;

      try {
        scope();
      } finally {
        ReactCurrentBatchConfig.suspense = previousConfig;
      }
    }
    /**
     * ReactElementValidator provides a wrapper around a element factory
     * which validates the props passed to the element. This is intended to be
     * used only in DEV and could be replaced by a static type checker for languages
     * that support it.
     */


    var propTypesMisspellWarningShown;
    {
      propTypesMisspellWarningShown = false;
    }
    var hasOwnProperty$1 = Object.prototype.hasOwnProperty;

    function getDeclarationErrorAddendum() {
      if (ReactCurrentOwner.current) {
        var name = getComponentName(ReactCurrentOwner.current.type);

        if (name) {
          return '\n\nCheck the render method of `' + name + '`.';
        }
      }

      return '';
    }

    function getSourceInfoErrorAddendum(source) {
      if (source !== undefined) {
        var fileName = source.fileName.replace(/^.*[\\\/]/, '');
        var lineNumber = source.lineNumber;
        return '\n\nCheck your code at ' + fileName + ':' + lineNumber + '.';
      }

      return '';
    }

    function getSourceInfoErrorAddendumForProps(elementProps) {
      if (elementProps !== null && elementProps !== undefined) {
        return getSourceInfoErrorAddendum(elementProps.__source);
      }

      return '';
    }
    /**
     * Warn if there's no key explicitly set on dynamic arrays of children or
     * object keys are not valid. This allows us to keep track of children between
     * updates.
     */


    var ownerHasKeyUseWarning = {};

    function getCurrentComponentErrorInfo(parentType) {
      var info = getDeclarationErrorAddendum();

      if (!info) {
        var parentName = typeof parentType === 'string' ? parentType : parentType.displayName || parentType.name;

        if (parentName) {
          info = "\n\nCheck the top-level render call using <" + parentName + ">.";
        }
      }

      return info;
    }
    /**
     * Warn if the element doesn't have an explicit key assigned to it.
     * This element is in an array. The array could grow and shrink or be
     * reordered. All children that haven't already been validated are required to
     * have a "key" property assigned to it. Error statuses are cached so a warning
     * will only be shown once.
     *
     * @internal
     * @param {ReactElement} element Element that requires a key.
     * @param {*} parentType element's parent's type.
     */


    function validateExplicitKey(element, parentType) {
      if (!element._store || element._store.validated || element.key != null) {
        return;
      }

      element._store.validated = true;
      var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);

      if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
        return;
      }

      ownerHasKeyUseWarning[currentComponentErrorInfo] = true; // Usually the current owner is the offender, but if it accepts children as a
      // property, it may be the creator of the child that's responsible for
      // assigning it a key.

      var childOwner = '';

      if (element && element._owner && element._owner !== ReactCurrentOwner.current) {
        // Give the component that originally created this child.
        childOwner = " It was passed a child from " + getComponentName(element._owner.type) + ".";
      }

      setCurrentlyValidatingElement(element);
      {
        warning$1(false, 'Each child in a list should have a unique "key" prop.' + '%s%s See https://fb.me/react-warning-keys for more information.', currentComponentErrorInfo, childOwner);
      }
      setCurrentlyValidatingElement(null);
    }
    /**
     * Ensure that every element either is passed in a static location, in an
     * array with an explicit keys property defined, or in an object literal
     * with valid key property.
     *
     * @internal
     * @param {ReactNode} node Statically passed child of any type.
     * @param {*} parentType node's parent's type.
     */


    function validateChildKeys(node, parentType) {
      if (typeof node !== 'object') {
        return;
      }

      if (Array.isArray(node)) {
        for (var i = 0; i < node.length; i++) {
          var child = node[i];

          if (isValidElement(child)) {
            validateExplicitKey(child, parentType);
          }
        }
      } else if (isValidElement(node)) {
        // This element was passed in a valid location.
        if (node._store) {
          node._store.validated = true;
        }
      } else if (node) {
        var iteratorFn = getIteratorFn(node);

        if (typeof iteratorFn === 'function') {
          // Entry iterators used to provide implicit keys,
          // but now we print a separate warning for them later.
          if (iteratorFn !== node.entries) {
            var iterator = iteratorFn.call(node);
            var step;

            while (!(step = iterator.next()).done) {
              if (isValidElement(step.value)) {
                validateExplicitKey(step.value, parentType);
              }
            }
          }
        }
      }
    }
    /**
     * Given an element, validate that its props follow the propTypes definition,
     * provided by the type.
     *
     * @param {ReactElement} element
     */


    function validatePropTypes(element) {
      var type = element.type;

      if (type === null || type === undefined || typeof type === 'string') {
        return;
      }

      var name = getComponentName(type);
      var propTypes;

      if (typeof type === 'function') {
        propTypes = type.propTypes;
      } else if (typeof type === 'object' && (type.$$typeof === REACT_FORWARD_REF_TYPE || // Note: Memo only checks outer props here.
      // Inner props are checked in the reconciler.
      type.$$typeof === REACT_MEMO_TYPE)) {
        propTypes = type.propTypes;
      } else {
        return;
      }

      if (propTypes) {
        setCurrentlyValidatingElement(element);
        checkPropTypes(propTypes, element.props, 'prop', name, ReactDebugCurrentFrame.getStackAddendum);
        setCurrentlyValidatingElement(null);
      } else if (type.PropTypes !== undefined && !propTypesMisspellWarningShown) {
        propTypesMisspellWarningShown = true;
        warningWithoutStack$1(false, 'Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?', name || 'Unknown');
      }

      if (typeof type.getDefaultProps === 'function') {
        !type.getDefaultProps.isReactClassApproved ? warningWithoutStack$1(false, 'getDefaultProps is only used on classic React.createClass ' + 'definitions. Use a static property named `defaultProps` instead.') : void 0;
      }
    }
    /**
     * Given a fragment, validate that it can only be provided with fragment props
     * @param {ReactElement} fragment
     */


    function validateFragmentProps(fragment) {
      setCurrentlyValidatingElement(fragment);
      var keys = Object.keys(fragment.props);

      for (var i = 0; i < keys.length; i++) {
        var key = keys[i];

        if (key !== 'children' && key !== 'key') {
          warning$1(false, 'Invalid prop `%s` supplied to `React.Fragment`. ' + 'React.Fragment can only have `key` and `children` props.', key);
          break;
        }
      }

      if (fragment.ref !== null) {
        warning$1(false, 'Invalid attribute `ref` supplied to `React.Fragment`.');
      }

      setCurrentlyValidatingElement(null);
    }

    function jsxWithValidation(type, props, key, isStaticChildren, source, self) {
      var validType = isValidElementType(type); // We warn in this case but don't throw. We expect the element creation to
      // succeed and there will likely be errors in render.

      if (!validType) {
        var info = '';

        if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {
          info += ' You likely forgot to export your component from the file ' + "it's defined in, or you might have mixed up default and named imports.";
        }

        var sourceInfo = getSourceInfoErrorAddendum(source);

        if (sourceInfo) {
          info += sourceInfo;
        } else {
          info += getDeclarationErrorAddendum();
        }

        var typeString;

        if (type === null) {
          typeString = 'null';
        } else if (Array.isArray(type)) {
          typeString = 'array';
        } else if (type !== undefined && type.$$typeof === REACT_ELEMENT_TYPE) {
          typeString = "<" + (getComponentName(type.type) || 'Unknown') + " />";
          info = ' Did you accidentally export a JSX literal instead of a component?';
        } else {
          typeString = typeof type;
        }

        warning$1(false, 'React.jsx: type is invalid -- expected a string (for ' + 'built-in components) or a class/function (for composite ' + 'components) but got: %s.%s', typeString, info);
      }

      var element = jsxDEV(type, props, key, source, self); // The result can be nullish if a mock or a custom function is used.
      // TODO: Drop this when these are no longer allowed as the type argument.

      if (element == null) {
        return element;
      } // Skip key warning if the type isn't valid since our key validation logic
      // doesn't expect a non-string/function type and can throw confusing errors.
      // We don't want exception behavior to differ between dev and prod.
      // (Rendering will throw with a helpful message and as soon as the type is
      // fixed, the key warnings will appear.)


      if (validType) {
        var children = props.children;

        if (children !== undefined) {
          if (isStaticChildren) {
            if (Array.isArray(children)) {
              for (var i = 0; i < children.length; i++) {
                validateChildKeys(children[i], type);
              }

              if (Object.freeze) {
                Object.freeze(children);
              }
            } else {
              warning$1(false, 'React.jsx: Static children should always be an array. ' + 'You are likely explicitly calling React.jsxs or React.jsxDEV. ' + 'Use the Babel transform instead.');
            }
          } else {
            validateChildKeys(children, type);
          }
        }
      }

      if (hasOwnProperty$1.call(props, 'key')) {
        warning$1(false, 'React.jsx: Spreading a key to JSX is a deprecated pattern. ' + 'Explicitly pass a key after spreading props in your JSX call. ' + 'E.g. <ComponentName {...props} key={key} />');
      }

      if (type === REACT_FRAGMENT_TYPE) {
        validateFragmentProps(element);
      } else {
        validatePropTypes(element);
      }

      return element;
    } // These two functions exist to still get child warnings in dev
    // even with the prod transform. This means that jsxDEV is purely
    // opt-in behavior for better messages but that we won't stop
    // giving you warnings if you use production apis.


    function jsxWithValidationStatic(type, props, key) {
      return jsxWithValidation(type, props, key, true);
    }

    function jsxWithValidationDynamic(type, props, key) {
      return jsxWithValidation(type, props, key, false);
    }

    function createElementWithValidation(type, props, children) {
      var validType = isValidElementType(type); // We warn in this case but don't throw. We expect the element creation to
      // succeed and there will likely be errors in render.

      if (!validType) {
        var info = '';

        if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {
          info += ' You likely forgot to export your component from the file ' + "it's defined in, or you might have mixed up default and named imports.";
        }

        var sourceInfo = getSourceInfoErrorAddendumForProps(props);

        if (sourceInfo) {
          info += sourceInfo;
        } else {
          info += getDeclarationErrorAddendum();
        }

        var typeString;

        if (type === null) {
          typeString = 'null';
        } else if (Array.isArray(type)) {
          typeString = 'array';
        } else if (type !== undefined && type.$$typeof === REACT_ELEMENT_TYPE) {
          typeString = "<" + (getComponentName(type.type) || 'Unknown') + " />";
          info = ' Did you accidentally export a JSX literal instead of a component?';
        } else {
          typeString = typeof type;
        }

        warning$1(false, 'React.createElement: type is invalid -- expected a string (for ' + 'built-in components) or a class/function (for composite ' + 'components) but got: %s.%s', typeString, info);
      }

      var element = createElement.apply(this, arguments); // The result can be nullish if a mock or a custom function is used.
      // TODO: Drop this when these are no longer allowed as the type argument.

      if (element == null) {
        return element;
      } // Skip key warning if the type isn't valid since our key validation logic
      // doesn't expect a non-string/function type and can throw confusing errors.
      // We don't want exception behavior to differ between dev and prod.
      // (Rendering will throw with a helpful message and as soon as the type is
      // fixed, the key warnings will appear.)


      if (validType) {
        for (var i = 2; i < arguments.length; i++) {
          validateChildKeys(arguments[i], type);
        }
      }

      if (type === REACT_FRAGMENT_TYPE) {
        validateFragmentProps(element);
      } else {
        validatePropTypes(element);
      }

      return element;
    }

    function createFactoryWithValidation(type) {
      var validatedFactory = createElementWithValidation.bind(null, type);
      validatedFactory.type = type; // Legacy hook: remove it

      {
        Object.defineProperty(validatedFactory, 'type', {
          enumerable: false,
          get: function () {
            lowPriorityWarningWithoutStack$1(false, 'Factory.type is deprecated. Access the class directly ' + 'before passing it to createFactory.');
            Object.defineProperty(this, 'type', {
              value: type
            });
            return type;
          }
        });
      }
      return validatedFactory;
    }

    function cloneElementWithValidation(element, props, children) {
      var newElement = cloneElement.apply(this, arguments);

      for (var i = 2; i < arguments.length; i++) {
        validateChildKeys(arguments[i], newElement.type);
      }

      validatePropTypes(newElement);
      return newElement;
    }

    var hasBadMapPolyfill;
    {
      hasBadMapPolyfill = false;

      try {
        var frozenObject = Object.freeze({});
        var testMap = new Map([[frozenObject, null]]);
        var testSet = new Set([frozenObject]); // This is necessary for Rollup to not consider these unused.
        // https://github.com/rollup/rollup/issues/1771
        // TODO: we can remove these if Rollup fixes the bug.

        testMap.set(0, 0);
        testSet.add(0);
      } catch (e) {
        // TODO: Consider warning about bad polyfills
        hasBadMapPolyfill = true;
      }
    }

    function createFundamentalComponent(impl) {
      // We use responder as a Map key later on. When we have a bad
      // polyfill, then we can't use it as a key as the polyfill tries
      // to add a property to the object.
      if (true && !hasBadMapPolyfill) {
        Object.freeze(impl);
      }

      var fundamantalComponent = {
        $$typeof: REACT_FUNDAMENTAL_TYPE,
        impl: impl
      };
      {
        Object.freeze(fundamantalComponent);
      }
      return fundamantalComponent;
    }

    function createEventResponder(displayName, responderConfig) {
      var getInitialState = responderConfig.getInitialState,
          onEvent = responderConfig.onEvent,
          onMount = responderConfig.onMount,
          onUnmount = responderConfig.onUnmount,
          onRootEvent = responderConfig.onRootEvent,
          rootEventTypes = responderConfig.rootEventTypes,
          targetEventTypes = responderConfig.targetEventTypes,
          targetPortalPropagation = responderConfig.targetPortalPropagation;
      var eventResponder = {
        $$typeof: REACT_RESPONDER_TYPE,
        displayName: displayName,
        getInitialState: getInitialState || null,
        onEvent: onEvent || null,
        onMount: onMount || null,
        onRootEvent: onRootEvent || null,
        onUnmount: onUnmount || null,
        rootEventTypes: rootEventTypes || null,
        targetEventTypes: targetEventTypes || null,
        targetPortalPropagation: targetPortalPropagation || false
      }; // We use responder as a Map key later on. When we have a bad
      // polyfill, then we can't use it as a key as the polyfill tries
      // to add a property to the object.

      if (true && !hasBadMapPolyfill) {
        Object.freeze(eventResponder);
      }

      return eventResponder;
    }

    function createScope(fn) {
      var scopeComponent = {
        $$typeof: REACT_SCOPE_TYPE,
        fn: fn
      };
      {
        Object.freeze(scopeComponent);
      }
      return scopeComponent;
    } // Helps identify side effects in begin-phase lifecycle hooks and setState reducers:
    // In some cases, StrictMode should also double-render lifecycles.
    // This can be confusing for tests though,
    // And it can be bad for performance in production.
    // This feature flag can be used to control the behavior:
    // To preserve the "Pause on caught exceptions" behavior of the debugger, we
    // replay the begin phase of a failed component inside invokeGuardedCallback.
    // Warn about deprecated, async-unsafe lifecycles; relates to RFC #6:
    // Gather advanced timing metrics for Profiler subtrees.
    // Trace which interactions trigger each commit.
    // Only used in www builds.
    // TODO: true? Here it might just be false.
    // Only used in www builds.
    // Only used in www builds.
    // Disable javascript: URL strings in href for XSS protection.
    // React Fire: prevent the value and checked attributes from syncing
    // with their related DOM properties
    // These APIs will no longer be "unstable" in the upcoming 16.7 release,
    // Control this behavior with a flag to support 16.6 minor releases in the meanwhile.
    // See https://github.com/react-native-community/discussions-and-proposals/issues/72 for more information
    // This is a flag so we can fix warnings in RN core before turning it on
    // Experimental React Flare event system and event components support.


    var enableFlareAPI = false; // Experimental Host Component support.

    var enableFundamentalAPI = false; // Experimental Scope support.

    var enableScopeAPI = false; // New API for JSX transforms to target - https://github.com/reactjs/rfcs/pull/107

    var enableJSXTransformAPI = false; // We will enforce mocking scheduler with scheduler/unstable_mock at some point. (v17?)
    // Till then, we warn about the missing mock, but still fallback to a sync mode compatible version
    // For tests, we flush suspense fallbacks in an act scope;
    // *except* in some of our own tests, where we test incremental loading states.
    // Changes priority of some events like mousemove to user-blocking priority,
    // but without making them discrete. The flag exists in case it causes
    // starvation problems.
    // Add a callback property to suspense to notify which promises are currently
    // in the update queue. This allows reporting and tracing of what is causing
    // the user to see a loading state.
    // Also allows hydration callbacks to fire when a dehydrated boundary gets
    // hydrated or deleted.
    // Part of the simplification of React.createElement so we can eventually move
    // from React.createElement to React.jsx
    // https://github.com/reactjs/rfcs/blob/createlement-rfc/text/0000-create-element-changes.md

    var React = {
      Children: {
        map: mapChildren,
        forEach: forEachChildren,
        count: countChildren,
        toArray: toArray,
        only: onlyChild
      },
      createRef: createRef,
      Component: Component,
      PureComponent: PureComponent,
      createContext: createContext,
      forwardRef: forwardRef,
      lazy: lazy,
      memo: memo,
      useCallback: useCallback,
      useContext: useContext,
      useEffect: useEffect,
      useImperativeHandle: useImperativeHandle,
      useDebugValue: useDebugValue,
      useLayoutEffect: useLayoutEffect,
      useMemo: useMemo,
      useReducer: useReducer,
      useRef: useRef,
      useState: useState,
      Fragment: REACT_FRAGMENT_TYPE,
      Profiler: REACT_PROFILER_TYPE,
      StrictMode: REACT_STRICT_MODE_TYPE,
      Suspense: REACT_SUSPENSE_TYPE,
      unstable_SuspenseList: REACT_SUSPENSE_LIST_TYPE,
      createElement: createElementWithValidation,
      cloneElement: cloneElementWithValidation,
      createFactory: createFactoryWithValidation,
      isValidElement: isValidElement,
      version: ReactVersion,
      unstable_withSuspenseConfig: withSuspenseConfig,
      __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED: ReactSharedInternals
    };

    if (enableFlareAPI) {
      React.unstable_useResponder = useResponder;
      React.unstable_createResponder = createEventResponder;
    }

    if (enableFundamentalAPI) {
      React.unstable_createFundamental = createFundamentalComponent;
    }

    if (enableScopeAPI) {
      React.unstable_createScope = createScope;
    } // Note: some APIs are added with feature flags.
    // Make sure that stable builds for open source
    // don't modify the React object to avoid deopts.
    // Also let's not expose their names in stable builds.


    if (enableJSXTransformAPI) {
      {
        React.jsxDEV = jsxWithValidation;
        React.jsx = jsxWithValidationDynamic;
        React.jsxs = jsxWithValidationStatic;
      }
    }

    var React$2 = Object.freeze({
      default: React
    });
    var React$3 = React$2 && React || React$2; // TODO: decide on the top-level export form.
    // This is hacky but makes it work with both Rollup and Jest.

    var react = React$3.default || React$3;
    module.exports = react;
  })();
}
},{"object-assign":"../node_modules/object-assign/index.js","prop-types/checkPropTypes":"../node_modules/prop-types/checkPropTypes.js"}],"../node_modules/react/index.js":[function(require,module,exports) {
'use strict';

if ("development" === 'production') {
  module.exports = require('./cjs/react.production.min.js');
} else {
  module.exports = require('./cjs/react.development.js');
}
},{"./cjs/react.development.js":"../node_modules/react/cjs/react.development.js"}],"../node_modules/scheduler/cjs/scheduler.development.js":[function(require,module,exports) {
/** @license React v0.16.1
 * scheduler.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
'use strict';

if ("development" !== "production") {
  (function () {
    'use strict';

    Object.defineProperty(exports, '__esModule', {
      value: true
    });
    var enableSchedulerDebugging = false;
    var enableIsInputPending = false;
    var enableMessageLoopImplementation = true;
    var enableProfiling = true; // works by scheduling a requestAnimationFrame, storing the time for the start
    // of the frame, then scheduling a postMessage which gets scheduled after paint.
    // Within the postMessage handler do as much work as possible until time + frame
    // rate. By separating the idle call into a separate event tick we ensure that
    // layout, paint and other browser work is counted against the available time.
    // The frame rate is dynamically adjusted.

    var requestHostCallback;
    var requestHostTimeout;
    var cancelHostTimeout;
    var shouldYieldToHost;
    var requestPaint;

    if ( // If Scheduler runs in a non-DOM environment, it falls back to a naive
    // implementation using setTimeout.
    typeof window === 'undefined' || // Check if MessageChannel is supported, too.
    typeof MessageChannel !== 'function') {
      // If this accidentally gets imported in a non-browser environment, e.g. JavaScriptCore,
      // fallback to a naive implementation.
      var _callback = null;
      var _timeoutID = null;

      var _flushCallback = function () {
        if (_callback !== null) {
          try {
            var currentTime = exports.unstable_now();
            var hasRemainingTime = true;

            _callback(hasRemainingTime, currentTime);

            _callback = null;
          } catch (e) {
            setTimeout(_flushCallback, 0);
            throw e;
          }
        }
      };

      var initialTime = Date.now();

      exports.unstable_now = function () {
        return Date.now() - initialTime;
      };

      requestHostCallback = function (cb) {
        if (_callback !== null) {
          // Protect against re-entrancy.
          setTimeout(requestHostCallback, 0, cb);
        } else {
          _callback = cb;
          setTimeout(_flushCallback, 0);
        }
      };

      requestHostTimeout = function (cb, ms) {
        _timeoutID = setTimeout(cb, ms);
      };

      cancelHostTimeout = function () {
        clearTimeout(_timeoutID);
      };

      shouldYieldToHost = function () {
        return false;
      };

      requestPaint = exports.unstable_forceFrameRate = function () {};
    } else {
      // Capture local references to native APIs, in case a polyfill overrides them.
      var performance = window.performance;
      var _Date = window.Date;
      var _setTimeout = window.setTimeout;
      var _clearTimeout = window.clearTimeout;
      var requestAnimationFrame = window.requestAnimationFrame;
      var cancelAnimationFrame = window.cancelAnimationFrame;

      if (typeof console !== 'undefined') {
        // TODO: Remove fb.me link
        if (typeof requestAnimationFrame !== 'function') {
          console.error("This browser doesn't support requestAnimationFrame. " + 'Make sure that you load a ' + 'polyfill in older browsers. https://fb.me/react-polyfills');
        }

        if (typeof cancelAnimationFrame !== 'function') {
          console.error("This browser doesn't support cancelAnimationFrame. " + 'Make sure that you load a ' + 'polyfill in older browsers. https://fb.me/react-polyfills');
        }
      }

      if (typeof performance === 'object' && typeof performance.now === 'function') {
        exports.unstable_now = function () {
          return performance.now();
        };
      } else {
        var _initialTime = _Date.now();

        exports.unstable_now = function () {
          return _Date.now() - _initialTime;
        };
      }

      var isRAFLoopRunning = false;
      var isMessageLoopRunning = false;
      var scheduledHostCallback = null;
      var rAFTimeoutID = -1;
      var taskTimeoutID = -1;
      var frameLength = enableMessageLoopImplementation ? // We won't attempt to align with the vsync. Instead we'll yield multiple
      // times per frame, often enough to keep it responsive even at really
      // high frame rates > 120.
      5 : // Use a heuristic to measure the frame rate and yield at the end of the
      // frame. We start out assuming that we run at 30fps but then the
      // heuristic tracking will adjust this value to a faster fps if we get
      // more frequent animation frames.
      33.33;
      var prevRAFTime = -1;
      var prevRAFInterval = -1;
      var frameDeadline = 0;
      var fpsLocked = false; // TODO: Make this configurable
      // TODO: Adjust this based on priority?

      var maxFrameLength = 300;
      var needsPaint = false;

      if (enableIsInputPending && navigator !== undefined && navigator.scheduling !== undefined && navigator.scheduling.isInputPending !== undefined) {
        var scheduling = navigator.scheduling;

        shouldYieldToHost = function () {
          var currentTime = exports.unstable_now();

          if (currentTime >= frameDeadline) {
            // There's no time left in the frame. We may want to yield control of
            // the main thread, so the browser can perform high priority tasks. The
            // main ones are painting and user input. If there's a pending paint or
            // a pending input, then we should yield. But if there's neither, then
            // we can yield less often while remaining responsive. We'll eventually
            // yield regardless, since there could be a pending paint that wasn't
            // accompanied by a call to `requestPaint`, or other main thread tasks
            // like network events.
            if (needsPaint || scheduling.isInputPending()) {
              // There is either a pending paint or a pending input.
              return true;
            } // There's no pending input. Only yield if we've reached the max
            // frame length.


            return currentTime >= frameDeadline + maxFrameLength;
          } else {
            // There's still time left in the frame.
            return false;
          }
        };

        requestPaint = function () {
          needsPaint = true;
        };
      } else {
        // `isInputPending` is not available. Since we have no way of knowing if
        // there's pending input, always yield at the end of the frame.
        shouldYieldToHost = function () {
          return exports.unstable_now() >= frameDeadline;
        }; // Since we yield every frame regardless, `requestPaint` has no effect.


        requestPaint = function () {};
      }

      exports.unstable_forceFrameRate = function (fps) {
        if (fps < 0 || fps > 125) {
          console.error('forceFrameRate takes a positive int between 0 and 125, ' + 'forcing framerates higher than 125 fps is not unsupported');
          return;
        }

        if (fps > 0) {
          frameLength = Math.floor(1000 / fps);
          fpsLocked = true;
        } else {
          // reset the framerate
          frameLength = 33.33;
          fpsLocked = false;
        }
      };

      var performWorkUntilDeadline = function () {
        if (enableMessageLoopImplementation) {
          if (scheduledHostCallback !== null) {
            var currentTime = exports.unstable_now(); // Yield after `frameLength` ms, regardless of where we are in the vsync
            // cycle. This means there's always time remaining at the beginning of
            // the message event.

            frameDeadline = currentTime + frameLength;
            var hasTimeRemaining = true;

            try {
              var hasMoreWork = scheduledHostCallback(hasTimeRemaining, currentTime);

              if (!hasMoreWork) {
                isMessageLoopRunning = false;
                scheduledHostCallback = null;
              } else {
                // If there's more work, schedule the next message event at the end
                // of the preceding one.
                port.postMessage(null);
              }
            } catch (error) {
              // If a scheduler task throws, exit the current browser task so the
              // error can be observed.
              port.postMessage(null);
              throw error;
            }
          } else {
            isMessageLoopRunning = false;
          } // Yielding to the browser will give it a chance to paint, so we can
          // reset this.


          needsPaint = false;
        } else {
          if (scheduledHostCallback !== null) {
            var _currentTime = exports.unstable_now();

            var _hasTimeRemaining = frameDeadline - _currentTime > 0;

            try {
              var _hasMoreWork = scheduledHostCallback(_hasTimeRemaining, _currentTime);

              if (!_hasMoreWork) {
                scheduledHostCallback = null;
              }
            } catch (error) {
              // If a scheduler task throws, exit the current browser task so the
              // error can be observed, and post a new task as soon as possible
              // so we can continue where we left off.
              port.postMessage(null);
              throw error;
            }
          } // Yielding to the browser will give it a chance to paint, so we can
          // reset this.


          needsPaint = false;
        }
      };

      var channel = new MessageChannel();
      var port = channel.port2;
      channel.port1.onmessage = performWorkUntilDeadline;

      var onAnimationFrame = function (rAFTime) {
        if (scheduledHostCallback === null) {
          // No scheduled work. Exit.
          prevRAFTime = -1;
          prevRAFInterval = -1;
          isRAFLoopRunning = false;
          return;
        } // Eagerly schedule the next animation callback at the beginning of the
        // frame. If the scheduler queue is not empty at the end of the frame, it
        // will continue flushing inside that callback. If the queue *is* empty,
        // then it will exit immediately. Posting the callback at the start of the
        // frame ensures it's fired within the earliest possible frame. If we
        // waited until the end of the frame to post the callback, we risk the
        // browser skipping a frame and not firing the callback until the frame
        // after that.


        isRAFLoopRunning = true;
        requestAnimationFrame(function (nextRAFTime) {
          _clearTimeout(rAFTimeoutID);

          onAnimationFrame(nextRAFTime);
        }); // requestAnimationFrame is throttled when the tab is backgrounded. We
        // don't want to stop working entirely. So we'll fallback to a timeout loop.
        // TODO: Need a better heuristic for backgrounded work.

        var onTimeout = function () {
          frameDeadline = exports.unstable_now() + frameLength / 2;
          performWorkUntilDeadline();
          rAFTimeoutID = _setTimeout(onTimeout, frameLength * 3);
        };

        rAFTimeoutID = _setTimeout(onTimeout, frameLength * 3);

        if (prevRAFTime !== -1 && // Make sure this rAF time is different from the previous one. This check
        // could fail if two rAFs fire in the same frame.
        rAFTime - prevRAFTime > 0.1) {
          var rAFInterval = rAFTime - prevRAFTime;

          if (!fpsLocked && prevRAFInterval !== -1) {
            // We've observed two consecutive frame intervals. We'll use this to
            // dynamically adjust the frame rate.
            //
            // If one frame goes long, then the next one can be short to catch up.
            // If two frames are short in a row, then that's an indication that we
            // actually have a higher frame rate than what we're currently
            // optimizing. For example, if we're running on 120hz display or 90hz VR
            // display. Take the max of the two in case one of them was an anomaly
            // due to missed frame deadlines.
            if (rAFInterval < frameLength && prevRAFInterval < frameLength) {
              frameLength = rAFInterval < prevRAFInterval ? prevRAFInterval : rAFInterval;

              if (frameLength < 8.33) {
                // Defensive coding. We don't support higher frame rates than 120hz.
                // If the calculated frame length gets lower than 8, it is probably
                // a bug.
                frameLength = 8.33;
              }
            }
          }

          prevRAFInterval = rAFInterval;
        }

        prevRAFTime = rAFTime;
        frameDeadline = rAFTime + frameLength; // We use the postMessage trick to defer idle work until after the repaint.

        port.postMessage(null);
      };

      requestHostCallback = function (callback) {
        scheduledHostCallback = callback;

        if (enableMessageLoopImplementation) {
          if (!isMessageLoopRunning) {
            isMessageLoopRunning = true;
            port.postMessage(null);
          }
        } else {
          if (!isRAFLoopRunning) {
            // Start a rAF loop.
            isRAFLoopRunning = true;
            requestAnimationFrame(function (rAFTime) {
              onAnimationFrame(rAFTime);
            });
          }
        }
      };

      requestHostTimeout = function (callback, ms) {
        taskTimeoutID = _setTimeout(function () {
          callback(exports.unstable_now());
        }, ms);
      };

      cancelHostTimeout = function () {
        _clearTimeout(taskTimeoutID);

        taskTimeoutID = -1;
      };
    }

    function push(heap, node) {
      var index = heap.length;
      heap.push(node);
      siftUp(heap, node, index);
    }

    function peek(heap) {
      var first = heap[0];
      return first === undefined ? null : first;
    }

    function pop(heap) {
      var first = heap[0];

      if (first !== undefined) {
        var last = heap.pop();

        if (last !== first) {
          heap[0] = last;
          siftDown(heap, last, 0);
        }

        return first;
      } else {
        return null;
      }
    }

    function siftUp(heap, node, i) {
      var index = i;

      while (true) {
        var parentIndex = Math.floor((index - 1) / 2);
        var parent = heap[parentIndex];

        if (parent !== undefined && compare(parent, node) > 0) {
          // The parent is larger. Swap positions.
          heap[parentIndex] = node;
          heap[index] = parent;
          index = parentIndex;
        } else {
          // The parent is smaller. Exit.
          return;
        }
      }
    }

    function siftDown(heap, node, i) {
      var index = i;
      var length = heap.length;

      while (index < length) {
        var leftIndex = (index + 1) * 2 - 1;
        var left = heap[leftIndex];
        var rightIndex = leftIndex + 1;
        var right = heap[rightIndex]; // If the left or right node is smaller, swap with the smaller of those.

        if (left !== undefined && compare(left, node) < 0) {
          if (right !== undefined && compare(right, left) < 0) {
            heap[index] = right;
            heap[rightIndex] = node;
            index = rightIndex;
          } else {
            heap[index] = left;
            heap[leftIndex] = node;
            index = leftIndex;
          }
        } else if (right !== undefined && compare(right, node) < 0) {
          heap[index] = right;
          heap[rightIndex] = node;
          index = rightIndex;
        } else {
          // Neither child is smaller. Exit.
          return;
        }
      }
    }

    function compare(a, b) {
      // Compare sort index first, then task id.
      var diff = a.sortIndex - b.sortIndex;
      return diff !== 0 ? diff : a.id - b.id;
    } // TODO: Use symbols?


    var NoPriority = 0;
    var ImmediatePriority = 1;
    var UserBlockingPriority = 2;
    var NormalPriority = 3;
    var LowPriority = 4;
    var IdlePriority = 5;
    var runIdCounter = 0;
    var mainThreadIdCounter = 0;
    var profilingStateSize = 4;
    var sharedProfilingBuffer = enableProfiling ? // $FlowFixMe Flow doesn't know about SharedArrayBuffer
    typeof SharedArrayBuffer === 'function' ? new SharedArrayBuffer(profilingStateSize * Int32Array.BYTES_PER_ELEMENT) : // $FlowFixMe Flow doesn't know about ArrayBuffer
    typeof ArrayBuffer === 'function' ? new ArrayBuffer(profilingStateSize * Int32Array.BYTES_PER_ELEMENT) : null // Don't crash the init path on IE9
    : null;
    var profilingState = enableProfiling && sharedProfilingBuffer !== null ? new Int32Array(sharedProfilingBuffer) : []; // We can't read this but it helps save bytes for null checks

    var PRIORITY = 0;
    var CURRENT_TASK_ID = 1;
    var CURRENT_RUN_ID = 2;
    var QUEUE_SIZE = 3;

    if (enableProfiling) {
      profilingState[PRIORITY] = NoPriority; // This is maintained with a counter, because the size of the priority queue
      // array might include canceled tasks.

      profilingState[QUEUE_SIZE] = 0;
      profilingState[CURRENT_TASK_ID] = 0;
    } // Bytes per element is 4


    var INITIAL_EVENT_LOG_SIZE = 131072;
    var MAX_EVENT_LOG_SIZE = 524288; // Equivalent to 2 megabytes

    var eventLogSize = 0;
    var eventLogBuffer = null;
    var eventLog = null;
    var eventLogIndex = 0;
    var TaskStartEvent = 1;
    var TaskCompleteEvent = 2;
    var TaskErrorEvent = 3;
    var TaskCancelEvent = 4;
    var TaskRunEvent = 5;
    var TaskYieldEvent = 6;
    var SchedulerSuspendEvent = 7;
    var SchedulerResumeEvent = 8;

    function logEvent(entries) {
      if (eventLog !== null) {
        var offset = eventLogIndex;
        eventLogIndex += entries.length;

        if (eventLogIndex + 1 > eventLogSize) {
          eventLogSize *= 2;

          if (eventLogSize > MAX_EVENT_LOG_SIZE) {
            console.error("Scheduler Profiling: Event log exceeded maximum size. Don't " + 'forget to call `stopLoggingProfilingEvents()`.');
            stopLoggingProfilingEvents();
            return;
          }

          var newEventLog = new Int32Array(eventLogSize * 4);
          newEventLog.set(eventLog);
          eventLogBuffer = newEventLog.buffer;
          eventLog = newEventLog;
        }

        eventLog.set(entries, offset);
      }
    }

    function startLoggingProfilingEvents() {
      eventLogSize = INITIAL_EVENT_LOG_SIZE;
      eventLogBuffer = new ArrayBuffer(eventLogSize * 4);
      eventLog = new Int32Array(eventLogBuffer);
      eventLogIndex = 0;
    }

    function stopLoggingProfilingEvents() {
      var buffer = eventLogBuffer;
      eventLogSize = 0;
      eventLogBuffer = null;
      eventLog = null;
      eventLogIndex = 0;
      return buffer;
    }

    function markTaskStart(task, time) {
      if (enableProfiling) {
        profilingState[QUEUE_SIZE]++;

        if (eventLog !== null) {
          logEvent([TaskStartEvent, time, task.id, task.priorityLevel]);
        }
      }
    }

    function markTaskCompleted(task, time) {
      if (enableProfiling) {
        profilingState[PRIORITY] = NoPriority;
        profilingState[CURRENT_TASK_ID] = 0;
        profilingState[QUEUE_SIZE]--;

        if (eventLog !== null) {
          logEvent([TaskCompleteEvent, time, task.id]);
        }
      }
    }

    function markTaskCanceled(task, time) {
      if (enableProfiling) {
        profilingState[QUEUE_SIZE]--;

        if (eventLog !== null) {
          logEvent([TaskCancelEvent, time, task.id]);
        }
      }
    }

    function markTaskErrored(task, time) {
      if (enableProfiling) {
        profilingState[PRIORITY] = NoPriority;
        profilingState[CURRENT_TASK_ID] = 0;
        profilingState[QUEUE_SIZE]--;

        if (eventLog !== null) {
          logEvent([TaskErrorEvent, time, task.id]);
        }
      }
    }

    function markTaskRun(task, time) {
      if (enableProfiling) {
        runIdCounter++;
        profilingState[PRIORITY] = task.priorityLevel;
        profilingState[CURRENT_TASK_ID] = task.id;
        profilingState[CURRENT_RUN_ID] = runIdCounter;

        if (eventLog !== null) {
          logEvent([TaskRunEvent, time, task.id, runIdCounter]);
        }
      }
    }

    function markTaskYield(task, time) {
      if (enableProfiling) {
        profilingState[PRIORITY] = NoPriority;
        profilingState[CURRENT_TASK_ID] = 0;
        profilingState[CURRENT_RUN_ID] = 0;

        if (eventLog !== null) {
          logEvent([TaskYieldEvent, time, task.id, runIdCounter]);
        }
      }
    }

    function markSchedulerSuspended(time) {
      if (enableProfiling) {
        mainThreadIdCounter++;

        if (eventLog !== null) {
          logEvent([SchedulerSuspendEvent, time, mainThreadIdCounter]);
        }
      }
    }

    function markSchedulerUnsuspended(time) {
      if (enableProfiling) {
        if (eventLog !== null) {
          logEvent([SchedulerResumeEvent, time, mainThreadIdCounter]);
        }
      }
    }
    /* eslint-disable no-var */
    // Math.pow(2, 30) - 1
    // 0b111111111111111111111111111111


    var maxSigned31BitInt = 1073741823; // Times out immediately

    var IMMEDIATE_PRIORITY_TIMEOUT = -1; // Eventually times out

    var USER_BLOCKING_PRIORITY = 250;
    var NORMAL_PRIORITY_TIMEOUT = 5000;
    var LOW_PRIORITY_TIMEOUT = 10000; // Never times out

    var IDLE_PRIORITY = maxSigned31BitInt; // Tasks are stored on a min heap

    var taskQueue = [];
    var timerQueue = []; // Incrementing id counter. Used to maintain insertion order.

    var taskIdCounter = 1; // Pausing the scheduler is useful for debugging.

    var isSchedulerPaused = false;
    var currentTask = null;
    var currentPriorityLevel = NormalPriority; // This is set while performing work, to prevent re-entrancy.

    var isPerformingWork = false;
    var isHostCallbackScheduled = false;
    var isHostTimeoutScheduled = false;

    function advanceTimers(currentTime) {
      // Check for tasks that are no longer delayed and add them to the queue.
      var timer = peek(timerQueue);

      while (timer !== null) {
        if (timer.callback === null) {
          // Timer was cancelled.
          pop(timerQueue);
        } else if (timer.startTime <= currentTime) {
          // Timer fired. Transfer to the task queue.
          pop(timerQueue);
          timer.sortIndex = timer.expirationTime;
          push(taskQueue, timer);

          if (enableProfiling) {
            markTaskStart(timer, currentTime);
            timer.isQueued = true;
          }
        } else {
          // Remaining timers are pending.
          return;
        }

        timer = peek(timerQueue);
      }
    }

    function handleTimeout(currentTime) {
      isHostTimeoutScheduled = false;
      advanceTimers(currentTime);

      if (!isHostCallbackScheduled) {
        if (peek(taskQueue) !== null) {
          isHostCallbackScheduled = true;
          requestHostCallback(flushWork);
        } else {
          var firstTimer = peek(timerQueue);

          if (firstTimer !== null) {
            requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
          }
        }
      }
    }

    function flushWork(hasTimeRemaining, initialTime) {
      if (enableProfiling) {
        markSchedulerUnsuspended(initialTime);
      } // We'll need a host callback the next time work is scheduled.


      isHostCallbackScheduled = false;

      if (isHostTimeoutScheduled) {
        // We scheduled a timeout but it's no longer needed. Cancel it.
        isHostTimeoutScheduled = false;
        cancelHostTimeout();
      }

      isPerformingWork = true;
      var previousPriorityLevel = currentPriorityLevel;

      try {
        if (enableProfiling) {
          try {
            return workLoop(hasTimeRemaining, initialTime);
          } catch (error) {
            if (currentTask !== null) {
              var currentTime = exports.unstable_now();
              markTaskErrored(currentTask, currentTime);
              currentTask.isQueued = false;
            }

            throw error;
          }
        } else {
          // No catch in prod codepath.
          return workLoop(hasTimeRemaining, initialTime);
        }
      } finally {
        currentTask = null;
        currentPriorityLevel = previousPriorityLevel;
        isPerformingWork = false;

        if (enableProfiling) {
          var _currentTime = exports.unstable_now();

          markSchedulerSuspended(_currentTime);
        }
      }
    }

    function workLoop(hasTimeRemaining, initialTime) {
      var currentTime = initialTime;
      advanceTimers(currentTime);
      currentTask = peek(taskQueue);

      while (currentTask !== null && !(enableSchedulerDebugging && isSchedulerPaused)) {
        if (currentTask.expirationTime > currentTime && (!hasTimeRemaining || shouldYieldToHost())) {
          // This currentTask hasn't expired, and we've reached the deadline.
          break;
        }

        var callback = currentTask.callback;

        if (callback !== null) {
          currentTask.callback = null;
          currentPriorityLevel = currentTask.priorityLevel;
          var didUserCallbackTimeout = currentTask.expirationTime <= currentTime;
          markTaskRun(currentTask, currentTime);
          var continuationCallback = callback(didUserCallbackTimeout);
          currentTime = exports.unstable_now();

          if (typeof continuationCallback === 'function') {
            currentTask.callback = continuationCallback;
            markTaskYield(currentTask, currentTime);
          } else {
            if (enableProfiling) {
              markTaskCompleted(currentTask, currentTime);
              currentTask.isQueued = false;
            }

            if (currentTask === peek(taskQueue)) {
              pop(taskQueue);
            }
          }

          advanceTimers(currentTime);
        } else {
          pop(taskQueue);
        }

        currentTask = peek(taskQueue);
      } // Return whether there's additional work


      if (currentTask !== null) {
        return true;
      } else {
        var firstTimer = peek(timerQueue);

        if (firstTimer !== null) {
          requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
        }

        return false;
      }
    }

    function unstable_runWithPriority(priorityLevel, eventHandler) {
      switch (priorityLevel) {
        case ImmediatePriority:
        case UserBlockingPriority:
        case NormalPriority:
        case LowPriority:
        case IdlePriority:
          break;

        default:
          priorityLevel = NormalPriority;
      }

      var previousPriorityLevel = currentPriorityLevel;
      currentPriorityLevel = priorityLevel;

      try {
        return eventHandler();
      } finally {
        currentPriorityLevel = previousPriorityLevel;
      }
    }

    function unstable_next(eventHandler) {
      var priorityLevel;

      switch (currentPriorityLevel) {
        case ImmediatePriority:
        case UserBlockingPriority:
        case NormalPriority:
          // Shift down to normal priority
          priorityLevel = NormalPriority;
          break;

        default:
          // Anything lower than normal priority should remain at the current level.
          priorityLevel = currentPriorityLevel;
          break;
      }

      var previousPriorityLevel = currentPriorityLevel;
      currentPriorityLevel = priorityLevel;

      try {
        return eventHandler();
      } finally {
        currentPriorityLevel = previousPriorityLevel;
      }
    }

    function unstable_wrapCallback(callback) {
      var parentPriorityLevel = currentPriorityLevel;
      return function () {
        // This is a fork of runWithPriority, inlined for performance.
        var previousPriorityLevel = currentPriorityLevel;
        currentPriorityLevel = parentPriorityLevel;

        try {
          return callback.apply(this, arguments);
        } finally {
          currentPriorityLevel = previousPriorityLevel;
        }
      };
    }

    function timeoutForPriorityLevel(priorityLevel) {
      switch (priorityLevel) {
        case ImmediatePriority:
          return IMMEDIATE_PRIORITY_TIMEOUT;

        case UserBlockingPriority:
          return USER_BLOCKING_PRIORITY;

        case IdlePriority:
          return IDLE_PRIORITY;

        case LowPriority:
          return LOW_PRIORITY_TIMEOUT;

        case NormalPriority:
        default:
          return NORMAL_PRIORITY_TIMEOUT;
      }
    }

    function unstable_scheduleCallback(priorityLevel, callback, options) {
      var currentTime = exports.unstable_now();
      var startTime;
      var timeout;

      if (typeof options === 'object' && options !== null) {
        var delay = options.delay;

        if (typeof delay === 'number' && delay > 0) {
          startTime = currentTime + delay;
        } else {
          startTime = currentTime;
        }

        timeout = typeof options.timeout === 'number' ? options.timeout : timeoutForPriorityLevel(priorityLevel);
      } else {
        timeout = timeoutForPriorityLevel(priorityLevel);
        startTime = currentTime;
      }

      var expirationTime = startTime + timeout;
      var newTask = {
        id: taskIdCounter++,
        callback: callback,
        priorityLevel: priorityLevel,
        startTime: startTime,
        expirationTime: expirationTime,
        sortIndex: -1
      };

      if (enableProfiling) {
        newTask.isQueued = false;
      }

      if (startTime > currentTime) {
        // This is a delayed task.
        newTask.sortIndex = startTime;
        push(timerQueue, newTask);

        if (peek(taskQueue) === null && newTask === peek(timerQueue)) {
          // All tasks are delayed, and this is the task with the earliest delay.
          if (isHostTimeoutScheduled) {
            // Cancel an existing timeout.
            cancelHostTimeout();
          } else {
            isHostTimeoutScheduled = true;
          } // Schedule a timeout.


          requestHostTimeout(handleTimeout, startTime - currentTime);
        }
      } else {
        newTask.sortIndex = expirationTime;
        push(taskQueue, newTask);

        if (enableProfiling) {
          markTaskStart(newTask, currentTime);
          newTask.isQueued = true;
        } // Schedule a host callback, if needed. If we're already performing work,
        // wait until the next time we yield.


        if (!isHostCallbackScheduled && !isPerformingWork) {
          isHostCallbackScheduled = true;
          requestHostCallback(flushWork);
        }
      }

      return newTask;
    }

    function unstable_pauseExecution() {
      isSchedulerPaused = true;
    }

    function unstable_continueExecution() {
      isSchedulerPaused = false;

      if (!isHostCallbackScheduled && !isPerformingWork) {
        isHostCallbackScheduled = true;
        requestHostCallback(flushWork);
      }
    }

    function unstable_getFirstCallbackNode() {
      return peek(taskQueue);
    }

    function unstable_cancelCallback(task) {
      if (enableProfiling) {
        if (task.isQueued) {
          var currentTime = exports.unstable_now();
          markTaskCanceled(task, currentTime);
          task.isQueued = false;
        }
      } // Null out the callback to indicate the task has been canceled. (Can't
      // remove from the queue because you can't remove arbitrary nodes from an
      // array based heap, only the first one.)


      task.callback = null;
    }

    function unstable_getCurrentPriorityLevel() {
      return currentPriorityLevel;
    }

    function unstable_shouldYield() {
      var currentTime = exports.unstable_now();
      advanceTimers(currentTime);
      var firstTask = peek(taskQueue);
      return firstTask !== currentTask && currentTask !== null && firstTask !== null && firstTask.callback !== null && firstTask.startTime <= currentTime && firstTask.expirationTime < currentTask.expirationTime || shouldYieldToHost();
    }

    var unstable_requestPaint = requestPaint;
    var unstable_Profiling = enableProfiling ? {
      startLoggingProfilingEvents: startLoggingProfilingEvents,
      stopLoggingProfilingEvents: stopLoggingProfilingEvents,
      sharedProfilingBuffer: sharedProfilingBuffer
    } : null;
    exports.unstable_ImmediatePriority = ImmediatePriority;
    exports.unstable_UserBlockingPriority = UserBlockingPriority;
    exports.unstable_NormalPriority = NormalPriority;
    exports.unstable_IdlePriority = IdlePriority;
    exports.unstable_LowPriority = LowPriority;
    exports.unstable_runWithPriority = unstable_runWithPriority;
    exports.unstable_next = unstable_next;
    exports.unstable_scheduleCallback = unstable_scheduleCallback;
    exports.unstable_cancelCallback = unstable_cancelCallback;
    exports.unstable_wrapCallback = unstable_wrapCallback;
    exports.unstable_getCurrentPriorityLevel = unstable_getCurrentPriorityLevel;
    exports.unstable_shouldYield = unstable_shouldYield;
    exports.unstable_requestPaint = unstable_requestPaint;
    exports.unstable_continueExecution = unstable_continueExecution;
    exports.unstable_pauseExecution = unstable_pauseExecution;
    exports.unstable_getFirstCallbackNode = unstable_getFirstCallbackNode;
    exports.unstable_Profiling = unstable_Profiling;
  })();
}
},{}],"../node_modules/scheduler/index.js":[function(require,module,exports) {
'use strict';

if ("development" === 'production') {
  module.exports = require('./cjs/scheduler.production.min.js');
} else {
  module.exports = require('./cjs/scheduler.development.js');
}
},{"./cjs/scheduler.development.js":"../node_modules/scheduler/cjs/scheduler.development.js"}],"../node_modules/scheduler/cjs/scheduler-tracing.development.js":[function(require,module,exports) {
/** @license React v0.16.1
 * scheduler-tracing.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
'use strict';

if ("development" !== "production") {
  (function () {
    'use strict';

    Object.defineProperty(exports, '__esModule', {
      value: true
    }); // Helps identify side effects in begin-phase lifecycle hooks and setState reducers:
    // In some cases, StrictMode should also double-render lifecycles.
    // This can be confusing for tests though,
    // And it can be bad for performance in production.
    // This feature flag can be used to control the behavior:
    // To preserve the "Pause on caught exceptions" behavior of the debugger, we
    // replay the begin phase of a failed component inside invokeGuardedCallback.
    // Warn about deprecated, async-unsafe lifecycles; relates to RFC #6:
    // Gather advanced timing metrics for Profiler subtrees.
    // Trace which interactions trigger each commit.

    var enableSchedulerTracing = true; // Only used in www builds.
    // TODO: true? Here it might just be false.
    // Only used in www builds.
    // Only used in www builds.
    // Disable javascript: URL strings in href for XSS protection.
    // React Fire: prevent the value and checked attributes from syncing
    // with their related DOM properties
    // These APIs will no longer be "unstable" in the upcoming 16.7 release,
    // Control this behavior with a flag to support 16.6 minor releases in the meanwhile.
    // See https://github.com/react-native-community/discussions-and-proposals/issues/72 for more information
    // This is a flag so we can fix warnings in RN core before turning it on
    // Experimental React Flare event system and event components support.
    // Experimental Host Component support.
    // Experimental Scope support.
    // New API for JSX transforms to target - https://github.com/reactjs/rfcs/pull/107
    // We will enforce mocking scheduler with scheduler/unstable_mock at some point. (v17?)
    // Till then, we warn about the missing mock, but still fallback to a sync mode compatible version
    // For tests, we flush suspense fallbacks in an act scope;
    // *except* in some of our own tests, where we test incremental loading states.
    // Changes priority of some events like mousemove to user-blocking priority,
    // but without making them discrete. The flag exists in case it causes
    // starvation problems.
    // Add a callback property to suspense to notify which promises are currently
    // in the update queue. This allows reporting and tracing of what is causing
    // the user to see a loading state.
    // Also allows hydration callbacks to fire when a dehydrated boundary gets
    // hydrated or deleted.
    // Part of the simplification of React.createElement so we can eventually move
    // from React.createElement to React.jsx
    // https://github.com/reactjs/rfcs/blob/createlement-rfc/text/0000-create-element-changes.md

    var DEFAULT_THREAD_ID = 0; // Counters used to generate unique IDs.

    var interactionIDCounter = 0;
    var threadIDCounter = 0; // Set of currently traced interactions.
    // Interactions "stack"–
    // Meaning that newly traced interactions are appended to the previously active set.
    // When an interaction goes out of scope, the previous set (if any) is restored.

    exports.__interactionsRef = null; // Listener(s) to notify when interactions begin and end.

    exports.__subscriberRef = null;

    if (enableSchedulerTracing) {
      exports.__interactionsRef = {
        current: new Set()
      };
      exports.__subscriberRef = {
        current: null
      };
    }

    function unstable_clear(callback) {
      if (!enableSchedulerTracing) {
        return callback();
      }

      var prevInteractions = exports.__interactionsRef.current;
      exports.__interactionsRef.current = new Set();

      try {
        return callback();
      } finally {
        exports.__interactionsRef.current = prevInteractions;
      }
    }

    function unstable_getCurrent() {
      if (!enableSchedulerTracing) {
        return null;
      } else {
        return exports.__interactionsRef.current;
      }
    }

    function unstable_getThreadID() {
      return ++threadIDCounter;
    }

    function unstable_trace(name, timestamp, callback) {
      var threadID = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : DEFAULT_THREAD_ID;

      if (!enableSchedulerTracing) {
        return callback();
      }

      var interaction = {
        __count: 1,
        id: interactionIDCounter++,
        name: name,
        timestamp: timestamp
      };
      var prevInteractions = exports.__interactionsRef.current; // Traced interactions should stack/accumulate.
      // To do that, clone the current interactions.
      // The previous set will be restored upon completion.

      var interactions = new Set(prevInteractions);
      interactions.add(interaction);
      exports.__interactionsRef.current = interactions;
      var subscriber = exports.__subscriberRef.current;
      var returnValue;

      try {
        if (subscriber !== null) {
          subscriber.onInteractionTraced(interaction);
        }
      } finally {
        try {
          if (subscriber !== null) {
            subscriber.onWorkStarted(interactions, threadID);
          }
        } finally {
          try {
            returnValue = callback();
          } finally {
            exports.__interactionsRef.current = prevInteractions;

            try {
              if (subscriber !== null) {
                subscriber.onWorkStopped(interactions, threadID);
              }
            } finally {
              interaction.__count--; // If no async work was scheduled for this interaction,
              // Notify subscribers that it's completed.

              if (subscriber !== null && interaction.__count === 0) {
                subscriber.onInteractionScheduledWorkCompleted(interaction);
              }
            }
          }
        }
      }

      return returnValue;
    }

    function unstable_wrap(callback) {
      var threadID = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : DEFAULT_THREAD_ID;

      if (!enableSchedulerTracing) {
        return callback;
      }

      var wrappedInteractions = exports.__interactionsRef.current;
      var subscriber = exports.__subscriberRef.current;

      if (subscriber !== null) {
        subscriber.onWorkScheduled(wrappedInteractions, threadID);
      } // Update the pending async work count for the current interactions.
      // Update after calling subscribers in case of error.


      wrappedInteractions.forEach(function (interaction) {
        interaction.__count++;
      });
      var hasRun = false;

      function wrapped() {
        var prevInteractions = exports.__interactionsRef.current;
        exports.__interactionsRef.current = wrappedInteractions;
        subscriber = exports.__subscriberRef.current;

        try {
          var returnValue;

          try {
            if (subscriber !== null) {
              subscriber.onWorkStarted(wrappedInteractions, threadID);
            }
          } finally {
            try {
              returnValue = callback.apply(undefined, arguments);
            } finally {
              exports.__interactionsRef.current = prevInteractions;

              if (subscriber !== null) {
                subscriber.onWorkStopped(wrappedInteractions, threadID);
              }
            }
          }

          return returnValue;
        } finally {
          if (!hasRun) {
            // We only expect a wrapped function to be executed once,
            // But in the event that it's executed more than once–
            // Only decrement the outstanding interaction counts once.
            hasRun = true; // Update pending async counts for all wrapped interactions.
            // If this was the last scheduled async work for any of them,
            // Mark them as completed.

            wrappedInteractions.forEach(function (interaction) {
              interaction.__count--;

              if (subscriber !== null && interaction.__count === 0) {
                subscriber.onInteractionScheduledWorkCompleted(interaction);
              }
            });
          }
        }
      }

      wrapped.cancel = function cancel() {
        subscriber = exports.__subscriberRef.current;

        try {
          if (subscriber !== null) {
            subscriber.onWorkCanceled(wrappedInteractions, threadID);
          }
        } finally {
          // Update pending async counts for all wrapped interactions.
          // If this was the last scheduled async work for any of them,
          // Mark them as completed.
          wrappedInteractions.forEach(function (interaction) {
            interaction.__count--;

            if (subscriber && interaction.__count === 0) {
              subscriber.onInteractionScheduledWorkCompleted(interaction);
            }
          });
        }
      };

      return wrapped;
    }

    var subscribers = null;

    if (enableSchedulerTracing) {
      subscribers = new Set();
    }

    function unstable_subscribe(subscriber) {
      if (enableSchedulerTracing) {
        subscribers.add(subscriber);

        if (subscribers.size === 1) {
          exports.__subscriberRef.current = {
            onInteractionScheduledWorkCompleted: onInteractionScheduledWorkCompleted,
            onInteractionTraced: onInteractionTraced,
            onWorkCanceled: onWorkCanceled,
            onWorkScheduled: onWorkScheduled,
            onWorkStarted: onWorkStarted,
            onWorkStopped: onWorkStopped
          };
        }
      }
    }

    function unstable_unsubscribe(subscriber) {
      if (enableSchedulerTracing) {
        subscribers.delete(subscriber);

        if (subscribers.size === 0) {
          exports.__subscriberRef.current = null;
        }
      }
    }

    function onInteractionTraced(interaction) {
      var didCatchError = false;
      var caughtError = null;
      subscribers.forEach(function (subscriber) {
        try {
          subscriber.onInteractionTraced(interaction);
        } catch (error) {
          if (!didCatchError) {
            didCatchError = true;
            caughtError = error;
          }
        }
      });

      if (didCatchError) {
        throw caughtError;
      }
    }

    function onInteractionScheduledWorkCompleted(interaction) {
      var didCatchError = false;
      var caughtError = null;
      subscribers.forEach(function (subscriber) {
        try {
          subscriber.onInteractionScheduledWorkCompleted(interaction);
        } catch (error) {
          if (!didCatchError) {
            didCatchError = true;
            caughtError = error;
          }
        }
      });

      if (didCatchError) {
        throw caughtError;
      }
    }

    function onWorkScheduled(interactions, threadID) {
      var didCatchError = false;
      var caughtError = null;
      subscribers.forEach(function (subscriber) {
        try {
          subscriber.onWorkScheduled(interactions, threadID);
        } catch (error) {
          if (!didCatchError) {
            didCatchError = true;
            caughtError = error;
          }
        }
      });

      if (didCatchError) {
        throw caughtError;
      }
    }

    function onWorkStarted(interactions, threadID) {
      var didCatchError = false;
      var caughtError = null;
      subscribers.forEach(function (subscriber) {
        try {
          subscriber.onWorkStarted(interactions, threadID);
        } catch (error) {
          if (!didCatchError) {
            didCatchError = true;
            caughtError = error;
          }
        }
      });

      if (didCatchError) {
        throw caughtError;
      }
    }

    function onWorkStopped(interactions, threadID) {
      var didCatchError = false;
      var caughtError = null;
      subscribers.forEach(function (subscriber) {
        try {
          subscriber.onWorkStopped(interactions, threadID);
        } catch (error) {
          if (!didCatchError) {
            didCatchError = true;
            caughtError = error;
          }
        }
      });

      if (didCatchError) {
        throw caughtError;
      }
    }

    function onWorkCanceled(interactions, threadID) {
      var didCatchError = false;
      var caughtError = null;
      subscribers.forEach(function (subscriber) {
        try {
          subscriber.onWorkCanceled(interactions, threadID);
        } catch (error) {
          if (!didCatchError) {
            didCatchError = true;
            caughtError = error;
          }
        }
      });

      if (didCatchError) {
        throw caughtError;
      }
    }

    exports.unstable_clear = unstable_clear;
    exports.unstable_getCurrent = unstable_getCurrent;
    exports.unstable_getThreadID = unstable_getThreadID;
    exports.unstable_trace = unstable_trace;
    exports.unstable_wrap = unstable_wrap;
    exports.unstable_subscribe = unstable_subscribe;
    exports.unstable_unsubscribe = unstable_unsubscribe;
  })();
}
},{}],"../node_modules/scheduler/tracing.js":[function(require,module,exports) {
'use strict';

if ("development" === 'production') {
  module.exports = require('./cjs/scheduler-tracing.production.min.js');
} else {
  module.exports = require('./cjs/scheduler-tracing.development.js');
}
},{"./cjs/scheduler-tracing.development.js":"../node_modules/scheduler/cjs/scheduler-tracing.development.js"}],"../node_modules/react-dom/cjs/react-dom.development.js":[function(require,module,exports) {
/** @license React v16.10.1
 * react-dom.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
'use strict';

if ("development" !== "production") {
  (function () {
    'use strict';

    var React = require('react');

    var _assign = require('object-assign');

    var Scheduler = require('scheduler');

    var checkPropTypes = require('prop-types/checkPropTypes');

    var tracing = require('scheduler/tracing'); // Do not require this module directly! Use normal `invariant` calls with
    // template literal strings. The messages will be converted to ReactError during
    // build, and in production they will be minified.
    // Do not require this module directly! Use normal `invariant` calls with
    // template literal strings. The messages will be converted to ReactError during
    // build, and in production they will be minified.


    function ReactError(error) {
      error.name = 'Invariant Violation';
      return error;
    }
    /**
     * Use invariant() to assert state which your program assumes to be true.
     *
     * Provide sprintf-style format (only %s is supported) and arguments
     * to provide information about what broke and what you were
     * expecting.
     *
     * The invariant message will be stripped in production, but the invariant
     * will remain to ensure logic does not differ in production.
     */


    (function () {
      if (!React) {
        {
          throw ReactError(Error("ReactDOM was loaded before React. Make sure you load the React package before loading ReactDOM."));
        }
      }
    })();
    /**
     * Injectable ordering of event plugins.
     */


    var eventPluginOrder = null;
    /**
     * Injectable mapping from names to event plugin modules.
     */

    var namesToPlugins = {};
    /**
     * Recomputes the plugin list using the injected plugins and plugin ordering.
     *
     * @private
     */

    function recomputePluginOrdering() {
      if (!eventPluginOrder) {
        // Wait until an `eventPluginOrder` is injected.
        return;
      }

      for (var pluginName in namesToPlugins) {
        var pluginModule = namesToPlugins[pluginName];
        var pluginIndex = eventPluginOrder.indexOf(pluginName);

        (function () {
          if (!(pluginIndex > -1)) {
            {
              throw ReactError(Error("EventPluginRegistry: Cannot inject event plugins that do not exist in the plugin ordering, `" + pluginName + "`."));
            }
          }
        })();

        if (plugins[pluginIndex]) {
          continue;
        }

        (function () {
          if (!pluginModule.extractEvents) {
            {
              throw ReactError(Error("EventPluginRegistry: Event plugins must implement an `extractEvents` method, but `" + pluginName + "` does not."));
            }
          }
        })();

        plugins[pluginIndex] = pluginModule;
        var publishedEvents = pluginModule.eventTypes;

        for (var eventName in publishedEvents) {
          (function () {
            if (!publishEventForPlugin(publishedEvents[eventName], pluginModule, eventName)) {
              {
                throw ReactError(Error("EventPluginRegistry: Failed to publish event `" + eventName + "` for plugin `" + pluginName + "`."));
              }
            }
          })();
        }
      }
    }
    /**
     * Publishes an event so that it can be dispatched by the supplied plugin.
     *
     * @param {object} dispatchConfig Dispatch configuration for the event.
     * @param {object} PluginModule Plugin publishing the event.
     * @return {boolean} True if the event was successfully published.
     * @private
     */


    function publishEventForPlugin(dispatchConfig, pluginModule, eventName) {
      (function () {
        if (!!eventNameDispatchConfigs.hasOwnProperty(eventName)) {
          {
            throw ReactError(Error("EventPluginHub: More than one plugin attempted to publish the same event name, `" + eventName + "`."));
          }
        }
      })();

      eventNameDispatchConfigs[eventName] = dispatchConfig;
      var phasedRegistrationNames = dispatchConfig.phasedRegistrationNames;

      if (phasedRegistrationNames) {
        for (var phaseName in phasedRegistrationNames) {
          if (phasedRegistrationNames.hasOwnProperty(phaseName)) {
            var phasedRegistrationName = phasedRegistrationNames[phaseName];
            publishRegistrationName(phasedRegistrationName, pluginModule, eventName);
          }
        }

        return true;
      } else if (dispatchConfig.registrationName) {
        publishRegistrationName(dispatchConfig.registrationName, pluginModule, eventName);
        return true;
      }

      return false;
    }
    /**
     * Publishes a registration name that is used to identify dispatched events.
     *
     * @param {string} registrationName Registration name to add.
     * @param {object} PluginModule Plugin publishing the event.
     * @private
     */


    function publishRegistrationName(registrationName, pluginModule, eventName) {
      (function () {
        if (!!registrationNameModules[registrationName]) {
          {
            throw ReactError(Error("EventPluginHub: More than one plugin attempted to publish the same registration name, `" + registrationName + "`."));
          }
        }
      })();

      registrationNameModules[registrationName] = pluginModule;
      registrationNameDependencies[registrationName] = pluginModule.eventTypes[eventName].dependencies;
      {
        var lowerCasedName = registrationName.toLowerCase();
        possibleRegistrationNames[lowerCasedName] = registrationName;

        if (registrationName === 'onDoubleClick') {
          possibleRegistrationNames.ondblclick = registrationName;
        }
      }
    }
    /**
     * Registers plugins so that they can extract and dispatch events.
     *
     * @see {EventPluginHub}
     */

    /**
     * Ordered list of injected plugins.
     */


    var plugins = [];
    /**
     * Mapping from event name to dispatch config
     */

    var eventNameDispatchConfigs = {};
    /**
     * Mapping from registration name to plugin module
     */

    var registrationNameModules = {};
    /**
     * Mapping from registration name to event name
     */

    var registrationNameDependencies = {};
    /**
     * Mapping from lowercase registration names to the properly cased version,
     * used to warn in the case of missing event handlers. Available
     * only in true.
     * @type {Object}
     */

    var possibleRegistrationNames = {}; // Trust the developer to only use possibleRegistrationNames in true

    /**
     * Injects an ordering of plugins (by plugin name). This allows the ordering
     * to be decoupled from injection of the actual plugins so that ordering is
     * always deterministic regardless of packaging, on-the-fly injection, etc.
     *
     * @param {array} InjectedEventPluginOrder
     * @internal
     * @see {EventPluginHub.injection.injectEventPluginOrder}
     */

    function injectEventPluginOrder(injectedEventPluginOrder) {
      (function () {
        if (!!eventPluginOrder) {
          {
            throw ReactError(Error("EventPluginRegistry: Cannot inject event plugin ordering more than once. You are likely trying to load more than one copy of React."));
          }
        }
      })(); // Clone the ordering so it cannot be dynamically mutated.


      eventPluginOrder = Array.prototype.slice.call(injectedEventPluginOrder);
      recomputePluginOrdering();
    }
    /**
     * Injects plugins to be used by `EventPluginHub`. The plugin names must be
     * in the ordering injected by `injectEventPluginOrder`.
     *
     * Plugins can be injected as part of page initialization or on-the-fly.
     *
     * @param {object} injectedNamesToPlugins Map from names to plugin modules.
     * @internal
     * @see {EventPluginHub.injection.injectEventPluginsByName}
     */


    function injectEventPluginsByName(injectedNamesToPlugins) {
      var isOrderingDirty = false;

      for (var pluginName in injectedNamesToPlugins) {
        if (!injectedNamesToPlugins.hasOwnProperty(pluginName)) {
          continue;
        }

        var pluginModule = injectedNamesToPlugins[pluginName];

        if (!namesToPlugins.hasOwnProperty(pluginName) || namesToPlugins[pluginName] !== pluginModule) {
          (function () {
            if (!!namesToPlugins[pluginName]) {
              {
                throw ReactError(Error("EventPluginRegistry: Cannot inject two different event plugins using the same name, `" + pluginName + "`."));
              }
            }
          })();

          namesToPlugins[pluginName] = pluginModule;
          isOrderingDirty = true;
        }
      }

      if (isOrderingDirty) {
        recomputePluginOrdering();
      }
    }

    var invokeGuardedCallbackImpl = function (name, func, context, a, b, c, d, e, f) {
      var funcArgs = Array.prototype.slice.call(arguments, 3);

      try {
        func.apply(context, funcArgs);
      } catch (error) {
        this.onError(error);
      }
    };

    {
      // In DEV mode, we swap out invokeGuardedCallback for a special version
      // that plays more nicely with the browser's DevTools. The idea is to preserve
      // "Pause on exceptions" behavior. Because React wraps all user-provided
      // functions in invokeGuardedCallback, and the production version of
      // invokeGuardedCallback uses a try-catch, all user exceptions are treated
      // like caught exceptions, and the DevTools won't pause unless the developer
      // takes the extra step of enabling pause on caught exceptions. This is
      // unintuitive, though, because even though React has caught the error, from
      // the developer's perspective, the error is uncaught.
      //
      // To preserve the expected "Pause on exceptions" behavior, we don't use a
      // try-catch in DEV. Instead, we synchronously dispatch a fake event to a fake
      // DOM node, and call the user-provided callback from inside an event handler
      // for that fake event. If the callback throws, the error is "captured" using
      // a global event handler. But because the error happens in a different
      // event loop context, it does not interrupt the normal program flow.
      // Effectively, this gives us try-catch behavior without actually using
      // try-catch. Neat!
      // Check that the browser supports the APIs we need to implement our special
      // DEV version of invokeGuardedCallback
      if (typeof window !== 'undefined' && typeof window.dispatchEvent === 'function' && typeof document !== 'undefined' && typeof document.createEvent === 'function') {
        var fakeNode = document.createElement('react');

        var invokeGuardedCallbackDev = function (name, func, context, a, b, c, d, e, f) {
          // If document doesn't exist we know for sure we will crash in this method
          // when we call document.createEvent(). However this can cause confusing
          // errors: https://github.com/facebookincubator/create-react-app/issues/3482
          // So we preemptively throw with a better message instead.
          (function () {
            if (!(typeof document !== 'undefined')) {
              {
                throw ReactError(Error("The `document` global was defined when React was initialized, but is not defined anymore. This can happen in a test environment if a component schedules an update from an asynchronous callback, but the test has already finished running. To solve this, you can either unmount the component at the end of your test (and ensure that any asynchronous operations get canceled in `componentWillUnmount`), or you can change the test itself to be asynchronous."));
              }
            }
          })();

          var evt = document.createEvent('Event'); // Keeps track of whether the user-provided callback threw an error. We
          // set this to true at the beginning, then set it to false right after
          // calling the function. If the function errors, `didError` will never be
          // set to false. This strategy works even if the browser is flaky and
          // fails to call our global error handler, because it doesn't rely on
          // the error event at all.

          var didError = true; // Keeps track of the value of window.event so that we can reset it
          // during the callback to let user code access window.event in the
          // browsers that support it.

          var windowEvent = window.event; // Keeps track of the descriptor of window.event to restore it after event
          // dispatching: https://github.com/facebook/react/issues/13688

          var windowEventDescriptor = Object.getOwnPropertyDescriptor(window, 'event'); // Create an event handler for our fake event. We will synchronously
          // dispatch our fake event using `dispatchEvent`. Inside the handler, we
          // call the user-provided callback.

          var funcArgs = Array.prototype.slice.call(arguments, 3);

          function callCallback() {
            // We immediately remove the callback from event listeners so that
            // nested `invokeGuardedCallback` calls do not clash. Otherwise, a
            // nested call would trigger the fake event handlers of any call higher
            // in the stack.
            fakeNode.removeEventListener(evtType, callCallback, false); // We check for window.hasOwnProperty('event') to prevent the
            // window.event assignment in both IE <= 10 as they throw an error
            // "Member not found" in strict mode, and in Firefox which does not
            // support window.event.

            if (typeof window.event !== 'undefined' && window.hasOwnProperty('event')) {
              window.event = windowEvent;
            }

            func.apply(context, funcArgs);
            didError = false;
          } // Create a global error event handler. We use this to capture the value
          // that was thrown. It's possible that this error handler will fire more
          // than once; for example, if non-React code also calls `dispatchEvent`
          // and a handler for that event throws. We should be resilient to most of
          // those cases. Even if our error event handler fires more than once, the
          // last error event is always used. If the callback actually does error,
          // we know that the last error event is the correct one, because it's not
          // possible for anything else to have happened in between our callback
          // erroring and the code that follows the `dispatchEvent` call below. If
          // the callback doesn't error, but the error event was fired, we know to
          // ignore it because `didError` will be false, as described above.


          var error; // Use this to track whether the error event is ever called.

          var didSetError = false;
          var isCrossOriginError = false;

          function handleWindowError(event) {
            error = event.error;
            didSetError = true;

            if (error === null && event.colno === 0 && event.lineno === 0) {
              isCrossOriginError = true;
            }

            if (event.defaultPrevented) {
              // Some other error handler has prevented default.
              // Browsers silence the error report if this happens.
              // We'll remember this to later decide whether to log it or not.
              if (error != null && typeof error === 'object') {
                try {
                  error._suppressLogging = true;
                } catch (inner) {// Ignore.
                }
              }
            }
          } // Create a fake event type.


          var evtType = "react-" + (name ? name : 'invokeguardedcallback'); // Attach our event handlers

          window.addEventListener('error', handleWindowError);
          fakeNode.addEventListener(evtType, callCallback, false); // Synchronously dispatch our fake event. If the user-provided function
          // errors, it will trigger our global error handler.

          evt.initEvent(evtType, false, false);
          fakeNode.dispatchEvent(evt);

          if (windowEventDescriptor) {
            Object.defineProperty(window, 'event', windowEventDescriptor);
          }

          if (didError) {
            if (!didSetError) {
              // The callback errored, but the error event never fired.
              error = new Error('An error was thrown inside one of your components, but React ' + "doesn't know what it was. This is likely due to browser " + 'flakiness. React does its best to preserve the "Pause on ' + 'exceptions" behavior of the DevTools, which requires some ' + "DEV-mode only tricks. It's possible that these don't work in " + 'your browser. Try triggering the error in production mode, ' + 'or switching to a modern browser. If you suspect that this is ' + 'actually an issue with React, please file an issue.');
            } else if (isCrossOriginError) {
              error = new Error("A cross-origin error was thrown. React doesn't have access to " + 'the actual error object in development. ' + 'See https://fb.me/react-crossorigin-error for more information.');
            }

            this.onError(error);
          } // Remove our event listeners


          window.removeEventListener('error', handleWindowError);
        };

        invokeGuardedCallbackImpl = invokeGuardedCallbackDev;
      }
    }
    var invokeGuardedCallbackImpl$1 = invokeGuardedCallbackImpl;
    var hasError = false;
    var caughtError = null; // Used by event system to capture/rethrow the first error.

    var hasRethrowError = false;
    var rethrowError = null;
    var reporter = {
      onError: function (error) {
        hasError = true;
        caughtError = error;
      }
    };
    /**
     * Call a function while guarding against errors that happens within it.
     * Returns an error if it throws, otherwise null.
     *
     * In production, this is implemented using a try-catch. The reason we don't
     * use a try-catch directly is so that we can swap out a different
     * implementation in DEV mode.
     *
     * @param {String} name of the guard to use for logging or debugging
     * @param {Function} func The function to invoke
     * @param {*} context The context to use when calling the function
     * @param {...*} args Arguments for function
     */

    function invokeGuardedCallback(name, func, context, a, b, c, d, e, f) {
      hasError = false;
      caughtError = null;
      invokeGuardedCallbackImpl$1.apply(reporter, arguments);
    }
    /**
     * Same as invokeGuardedCallback, but instead of returning an error, it stores
     * it in a global so it can be rethrown by `rethrowCaughtError` later.
     * TODO: See if caughtError and rethrowError can be unified.
     *
     * @param {String} name of the guard to use for logging or debugging
     * @param {Function} func The function to invoke
     * @param {*} context The context to use when calling the function
     * @param {...*} args Arguments for function
     */


    function invokeGuardedCallbackAndCatchFirstError(name, func, context, a, b, c, d, e, f) {
      invokeGuardedCallback.apply(this, arguments);

      if (hasError) {
        var error = clearCaughtError();

        if (!hasRethrowError) {
          hasRethrowError = true;
          rethrowError = error;
        }
      }
    }
    /**
     * During execution of guarded functions we will capture the first error which
     * we will rethrow to be handled by the top level error handler.
     */


    function rethrowCaughtError() {
      if (hasRethrowError) {
        var error = rethrowError;
        hasRethrowError = false;
        rethrowError = null;
        throw error;
      }
    }

    function hasCaughtError() {
      return hasError;
    }

    function clearCaughtError() {
      if (hasError) {
        var error = caughtError;
        hasError = false;
        caughtError = null;
        return error;
      } else {
        (function () {
          {
            {
              throw ReactError(Error("clearCaughtError was called but no error was captured. This error is likely caused by a bug in React. Please file an issue."));
            }
          }
        })();
      }
    }
    /**
     * Similar to invariant but only logs a warning if the condition is not met.
     * This can be used to log issues in development environments in critical
     * paths. Removing the logging code for production environments will keep the
     * same logic and follow the same code paths.
     */


    var warningWithoutStack = function () {};

    {
      warningWithoutStack = function (condition, format) {
        for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
          args[_key - 2] = arguments[_key];
        }

        if (format === undefined) {
          throw new Error('`warningWithoutStack(condition, format, ...args)` requires a warning ' + 'message argument');
        }

        if (args.length > 8) {
          // Check before the condition to catch violations early.
          throw new Error('warningWithoutStack() currently supports at most 8 arguments.');
        }

        if (condition) {
          return;
        }

        if (typeof console !== 'undefined') {
          var argsWithFormat = args.map(function (item) {
            return '' + item;
          });
          argsWithFormat.unshift('Warning: ' + format); // We intentionally don't use spread (or .apply) directly because it
          // breaks IE9: https://github.com/facebook/react/issues/13610

          Function.prototype.apply.call(console.error, console, argsWithFormat);
        }

        try {
          // --- Welcome to debugging React ---
          // This error was thrown as a convenience so that you can use this stack
          // to find the callsite that caused this warning to fire.
          var argIndex = 0;
          var message = 'Warning: ' + format.replace(/%s/g, function () {
            return args[argIndex++];
          });
          throw new Error(message);
        } catch (x) {}
      };
    }
    var warningWithoutStack$1 = warningWithoutStack;
    var getFiberCurrentPropsFromNode = null;
    var getInstanceFromNode = null;
    var getNodeFromInstance = null;

    function setComponentTree(getFiberCurrentPropsFromNodeImpl, getInstanceFromNodeImpl, getNodeFromInstanceImpl) {
      getFiberCurrentPropsFromNode = getFiberCurrentPropsFromNodeImpl;
      getInstanceFromNode = getInstanceFromNodeImpl;
      getNodeFromInstance = getNodeFromInstanceImpl;
      {
        !(getNodeFromInstance && getInstanceFromNode) ? warningWithoutStack$1(false, 'EventPluginUtils.setComponentTree(...): Injected ' + 'module is missing getNodeFromInstance or getInstanceFromNode.') : void 0;
      }
    }

    var validateEventDispatches;
    {
      validateEventDispatches = function (event) {
        var dispatchListeners = event._dispatchListeners;
        var dispatchInstances = event._dispatchInstances;
        var listenersIsArr = Array.isArray(dispatchListeners);
        var listenersLen = listenersIsArr ? dispatchListeners.length : dispatchListeners ? 1 : 0;
        var instancesIsArr = Array.isArray(dispatchInstances);
        var instancesLen = instancesIsArr ? dispatchInstances.length : dispatchInstances ? 1 : 0;
        !(instancesIsArr === listenersIsArr && instancesLen === listenersLen) ? warningWithoutStack$1(false, 'EventPluginUtils: Invalid `event`.') : void 0;
      };
    }
    /**
     * Dispatch the event to the listener.
     * @param {SyntheticEvent} event SyntheticEvent to handle
     * @param {function} listener Application-level callback
     * @param {*} inst Internal component instance
     */

    function executeDispatch(event, listener, inst) {
      var type = event.type || 'unknown-event';
      event.currentTarget = getNodeFromInstance(inst);
      invokeGuardedCallbackAndCatchFirstError(type, listener, undefined, event);
      event.currentTarget = null;
    }
    /**
     * Standard/simple iteration through an event's collected dispatches.
     */


    function executeDispatchesInOrder(event) {
      var dispatchListeners = event._dispatchListeners;
      var dispatchInstances = event._dispatchInstances;
      {
        validateEventDispatches(event);
      }

      if (Array.isArray(dispatchListeners)) {
        for (var i = 0; i < dispatchListeners.length; i++) {
          if (event.isPropagationStopped()) {
            break;
          } // Listeners and Instances are two parallel arrays that are always in sync.


          executeDispatch(event, dispatchListeners[i], dispatchInstances[i]);
        }
      } else if (dispatchListeners) {
        executeDispatch(event, dispatchListeners, dispatchInstances);
      }

      event._dispatchListeners = null;
      event._dispatchInstances = null;
    }
    /**
     * @see executeDispatchesInOrderStopAtTrueImpl
     */

    /**
     * Execution of a "direct" dispatch - there must be at most one dispatch
     * accumulated on the event or it is considered an error. It doesn't really make
     * sense for an event with multiple dispatches (bubbled) to keep track of the
     * return values at each dispatch execution, but it does tend to make sense when
     * dealing with "direct" dispatches.
     *
     * @return {*} The return value of executing the single dispatch.
     */

    /**
     * @param {SyntheticEvent} event
     * @return {boolean} True iff number of dispatches accumulated is greater than 0.
     */

    /**
     * Accumulates items that must not be null or undefined into the first one. This
     * is used to conserve memory by avoiding array allocations, and thus sacrifices
     * API cleanness. Since `current` can be null before being passed in and not
     * null after this function, make sure to assign it back to `current`:
     *
     * `a = accumulateInto(a, b);`
     *
     * This API should be sparingly used. Try `accumulate` for something cleaner.
     *
     * @return {*|array<*>} An accumulation of items.
     */


    function accumulateInto(current, next) {
      (function () {
        if (!(next != null)) {
          {
            throw ReactError(Error("accumulateInto(...): Accumulated items must not be null or undefined."));
          }
        }
      })();

      if (current == null) {
        return next;
      } // Both are not empty. Warning: Never call x.concat(y) when you are not
      // certain that x is an Array (x could be a string with concat method).


      if (Array.isArray(current)) {
        if (Array.isArray(next)) {
          current.push.apply(current, next);
          return current;
        }

        current.push(next);
        return current;
      }

      if (Array.isArray(next)) {
        // A bit too dangerous to mutate `next`.
        return [current].concat(next);
      }

      return [current, next];
    }
    /**
     * @param {array} arr an "accumulation" of items which is either an Array or
     * a single item. Useful when paired with the `accumulate` module. This is a
     * simple utility that allows us to reason about a collection of items, but
     * handling the case when there is exactly one item (and we do not need to
     * allocate an array).
     * @param {function} cb Callback invoked with each element or a collection.
     * @param {?} [scope] Scope used as `this` in a callback.
     */


    function forEachAccumulated(arr, cb, scope) {
      if (Array.isArray(arr)) {
        arr.forEach(cb, scope);
      } else if (arr) {
        cb.call(scope, arr);
      }
    }
    /**
     * Internal queue of events that have accumulated their dispatches and are
     * waiting to have their dispatches executed.
     */


    var eventQueue = null;
    /**
     * Dispatches an event and releases it back into the pool, unless persistent.
     *
     * @param {?object} event Synthetic event to be dispatched.
     * @private
     */

    var executeDispatchesAndRelease = function (event) {
      if (event) {
        executeDispatchesInOrder(event);

        if (!event.isPersistent()) {
          event.constructor.release(event);
        }
      }
    };

    var executeDispatchesAndReleaseTopLevel = function (e) {
      return executeDispatchesAndRelease(e);
    };

    function runEventsInBatch(events) {
      if (events !== null) {
        eventQueue = accumulateInto(eventQueue, events);
      } // Set `eventQueue` to null before processing it so that we can tell if more
      // events get enqueued while processing.


      var processingEventQueue = eventQueue;
      eventQueue = null;

      if (!processingEventQueue) {
        return;
      }

      forEachAccumulated(processingEventQueue, executeDispatchesAndReleaseTopLevel);

      (function () {
        if (!!eventQueue) {
          {
            throw ReactError(Error("processEventQueue(): Additional events were enqueued while processing an event queue. Support for this has not yet been implemented."));
          }
        }
      })(); // This would be a good time to rethrow if any of the event handlers threw.


      rethrowCaughtError();
    }

    function isInteractive(tag) {
      return tag === 'button' || tag === 'input' || tag === 'select' || tag === 'textarea';
    }

    function shouldPreventMouseEvent(name, type, props) {
      switch (name) {
        case 'onClick':
        case 'onClickCapture':
        case 'onDoubleClick':
        case 'onDoubleClickCapture':
        case 'onMouseDown':
        case 'onMouseDownCapture':
        case 'onMouseMove':
        case 'onMouseMoveCapture':
        case 'onMouseUp':
        case 'onMouseUpCapture':
          return !!(props.disabled && isInteractive(type));

        default:
          return false;
      }
    }
    /**
     * This is a unified interface for event plugins to be installed and configured.
     *
     * Event plugins can implement the following properties:
     *
     *   `extractEvents` {function(string, DOMEventTarget, string, object): *}
     *     Required. When a top-level event is fired, this method is expected to
     *     extract synthetic events that will in turn be queued and dispatched.
     *
     *   `eventTypes` {object}
     *     Optional, plugins that fire events must publish a mapping of registration
     *     names that are used to register listeners. Values of this mapping must
     *     be objects that contain `registrationName` or `phasedRegistrationNames`.
     *
     *   `executeDispatch` {function(object, function, string)}
     *     Optional, allows plugins to override how an event gets dispatched. By
     *     default, the listener is simply invoked.
     *
     * Each plugin that is injected into `EventsPluginHub` is immediately operable.
     *
     * @public
     */

    /**
     * Methods for injecting dependencies.
     */


    var injection = {
      /**
       * @param {array} InjectedEventPluginOrder
       * @public
       */
      injectEventPluginOrder: injectEventPluginOrder,

      /**
       * @param {object} injectedNamesToPlugins Map from names to plugin modules.
       */
      injectEventPluginsByName: injectEventPluginsByName
    };
    /**
     * @param {object} inst The instance, which is the source of events.
     * @param {string} registrationName Name of listener (e.g. `onClick`).
     * @return {?function} The stored callback.
     */

    function getListener(inst, registrationName) {
      var listener; // TODO: shouldPreventMouseEvent is DOM-specific and definitely should not
      // live here; needs to be moved to a better place soon

      var stateNode = inst.stateNode;

      if (!stateNode) {
        // Work in progress (ex: onload events in incremental mode).
        return null;
      }

      var props = getFiberCurrentPropsFromNode(stateNode);

      if (!props) {
        // Work in progress.
        return null;
      }

      listener = props[registrationName];

      if (shouldPreventMouseEvent(registrationName, inst.type, props)) {
        return null;
      }

      (function () {
        if (!(!listener || typeof listener === 'function')) {
          {
            throw ReactError(Error("Expected `" + registrationName + "` listener to be a function, instead got a value of `" + typeof listener + "` type."));
          }
        }
      })();

      return listener;
    }
    /**
     * Allows registered plugins an opportunity to extract events from top-level
     * native browser events.
     *
     * @return {*} An accumulation of synthetic events.
     * @internal
     */


    function extractPluginEvents(topLevelType, eventSystemFlags, targetInst, nativeEvent, nativeEventTarget) {
      var events = null;

      for (var i = 0; i < plugins.length; i++) {
        // Not every plugin in the ordering may be loaded at runtime.
        var possiblePlugin = plugins[i];

        if (possiblePlugin) {
          var extractedEvents = possiblePlugin.extractEvents(topLevelType, eventSystemFlags, targetInst, nativeEvent, nativeEventTarget);

          if (extractedEvents) {
            events = accumulateInto(events, extractedEvents);
          }
        }
      }

      return events;
    }

    function runExtractedPluginEventsInBatch(topLevelType, eventSystemFlags, targetInst, nativeEvent, nativeEventTarget) {
      var events = extractPluginEvents(topLevelType, eventSystemFlags, targetInst, nativeEvent, nativeEventTarget);
      runEventsInBatch(events);
    }

    var FunctionComponent = 0;
    var ClassComponent = 1;
    var IndeterminateComponent = 2; // Before we know whether it is function or class

    var HostRoot = 3; // Root of a host tree. Could be nested inside another node.

    var HostPortal = 4; // A subtree. Could be an entry point to a different renderer.

    var HostComponent = 5;
    var HostText = 6;
    var Fragment = 7;
    var Mode = 8;
    var ContextConsumer = 9;
    var ContextProvider = 10;
    var ForwardRef = 11;
    var Profiler = 12;
    var SuspenseComponent = 13;
    var MemoComponent = 14;
    var SimpleMemoComponent = 15;
    var LazyComponent = 16;
    var IncompleteClassComponent = 17;
    var DehydratedFragment = 18;
    var SuspenseListComponent = 19;
    var FundamentalComponent = 20;
    var ScopeComponent = 21;
    var ReactSharedInternals = React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED; // Prevent newer renderers from RTE when used with older react package versions.
    // Current owner and dispatcher used to share the same ref,
    // but PR #14548 split them out to better support the react-debug-tools package.

    if (!ReactSharedInternals.hasOwnProperty('ReactCurrentDispatcher')) {
      ReactSharedInternals.ReactCurrentDispatcher = {
        current: null
      };
    }

    if (!ReactSharedInternals.hasOwnProperty('ReactCurrentBatchConfig')) {
      ReactSharedInternals.ReactCurrentBatchConfig = {
        suspense: null
      };
    }

    var BEFORE_SLASH_RE = /^(.*)[\\\/]/;

    var describeComponentFrame = function (name, source, ownerName) {
      var sourceInfo = '';

      if (source) {
        var path = source.fileName;
        var fileName = path.replace(BEFORE_SLASH_RE, '');
        {
          // In DEV, include code for a common special case:
          // prefer "folder/index.js" instead of just "index.js".
          if (/^index\./.test(fileName)) {
            var match = path.match(BEFORE_SLASH_RE);

            if (match) {
              var pathBeforeSlash = match[1];

              if (pathBeforeSlash) {
                var folderName = pathBeforeSlash.replace(BEFORE_SLASH_RE, '');
                fileName = folderName + '/' + fileName;
              }
            }
          }
        }
        sourceInfo = ' (at ' + fileName + ':' + source.lineNumber + ')';
      } else if (ownerName) {
        sourceInfo = ' (created by ' + ownerName + ')';
      }

      return '\n    in ' + (name || 'Unknown') + sourceInfo;
    }; // The Symbol used to tag the ReactElement-like types. If there is no native Symbol
    // nor polyfill, then a plain number is used for performance.


    var hasSymbol = typeof Symbol === 'function' && Symbol.for;
    var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for('react.element') : 0xeac7;
    var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for('react.portal') : 0xeaca;
    var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for('react.fragment') : 0xeacb;
    var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for('react.strict_mode') : 0xeacc;
    var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for('react.profiler') : 0xead2;
    var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for('react.provider') : 0xeacd;
    var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for('react.context') : 0xeace; // TODO: We don't use AsyncMode or ConcurrentMode anymore. They were temporary
    // (unstable) APIs that have been removed. Can we remove the symbols?

    var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for('react.concurrent_mode') : 0xeacf;
    var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for('react.forward_ref') : 0xead0;
    var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for('react.suspense') : 0xead1;
    var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for('react.suspense_list') : 0xead8;
    var REACT_MEMO_TYPE = hasSymbol ? Symbol.for('react.memo') : 0xead3;
    var REACT_LAZY_TYPE = hasSymbol ? Symbol.for('react.lazy') : 0xead4;
    var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for('react.fundamental') : 0xead5;
    var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for('react.responder') : 0xead6;
    var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for('react.scope') : 0xead7;
    var MAYBE_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
    var FAUX_ITERATOR_SYMBOL = '@@iterator';

    function getIteratorFn(maybeIterable) {
      if (maybeIterable === null || typeof maybeIterable !== 'object') {
        return null;
      }

      var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];

      if (typeof maybeIterator === 'function') {
        return maybeIterator;
      }

      return null;
    }
    /**
     * Similar to invariant but only logs a warning if the condition is not met.
     * This can be used to log issues in development environments in critical
     * paths. Removing the logging code for production environments will keep the
     * same logic and follow the same code paths.
     */


    var warning = warningWithoutStack$1;
    {
      warning = function (condition, format) {
        if (condition) {
          return;
        }

        var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
        var stack = ReactDebugCurrentFrame.getStackAddendum(); // eslint-disable-next-line react-internal/warning-and-invariant-args

        for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
          args[_key - 2] = arguments[_key];
        }

        warningWithoutStack$1.apply(void 0, [false, format + '%s'].concat(args, [stack]));
      };
    }
    var warning$1 = warning;
    var Uninitialized = -1;
    var Pending = 0;
    var Resolved = 1;
    var Rejected = 2;

    function refineResolvedLazyComponent(lazyComponent) {
      return lazyComponent._status === Resolved ? lazyComponent._result : null;
    }

    function initializeLazyComponentType(lazyComponent) {
      if (lazyComponent._status === Uninitialized) {
        lazyComponent._status = Pending;
        var ctor = lazyComponent._ctor;
        var thenable = ctor();
        lazyComponent._result = thenable;
        thenable.then(function (moduleObject) {
          if (lazyComponent._status === Pending) {
            var defaultExport = moduleObject.default;
            {
              if (defaultExport === undefined) {
                warning$1(false, 'lazy: Expected the result of a dynamic import() call. ' + 'Instead received: %s\n\nYour code should look like: \n  ' + "const MyComponent = lazy(() => import('./MyComponent'))", moduleObject);
              }
            }
            lazyComponent._status = Resolved;
            lazyComponent._result = defaultExport;
          }
        }, function (error) {
          if (lazyComponent._status === Pending) {
            lazyComponent._status = Rejected;
            lazyComponent._result = error;
          }
        });
      }
    }

    function getWrappedName(outerType, innerType, wrapperName) {
      var functionName = innerType.displayName || innerType.name || '';
      return outerType.displayName || (functionName !== '' ? wrapperName + "(" + functionName + ")" : wrapperName);
    }

    function getComponentName(type) {
      if (type == null) {
        // Host root, text node or just invalid type.
        return null;
      }

      {
        if (typeof type.tag === 'number') {
          warningWithoutStack$1(false, 'Received an unexpected object in getComponentName(). ' + 'This is likely a bug in React. Please file an issue.');
        }
      }

      if (typeof type === 'function') {
        return type.displayName || type.name || null;
      }

      if (typeof type === 'string') {
        return type;
      }

      switch (type) {
        case REACT_FRAGMENT_TYPE:
          return 'Fragment';

        case REACT_PORTAL_TYPE:
          return 'Portal';

        case REACT_PROFILER_TYPE:
          return "Profiler";

        case REACT_STRICT_MODE_TYPE:
          return 'StrictMode';

        case REACT_SUSPENSE_TYPE:
          return 'Suspense';

        case REACT_SUSPENSE_LIST_TYPE:
          return 'SuspenseList';
      }

      if (typeof type === 'object') {
        switch (type.$$typeof) {
          case REACT_CONTEXT_TYPE:
            return 'Context.Consumer';

          case REACT_PROVIDER_TYPE:
            return 'Context.Provider';

          case REACT_FORWARD_REF_TYPE:
            return getWrappedName(type, type.render, 'ForwardRef');

          case REACT_MEMO_TYPE:
            return getComponentName(type.type);

          case REACT_LAZY_TYPE:
            {
              var thenable = type;
              var resolvedThenable = refineResolvedLazyComponent(thenable);

              if (resolvedThenable) {
                return getComponentName(resolvedThenable);
              }

              break;
            }
        }
      }

      return null;
    }

    var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;

    function describeFiber(fiber) {
      switch (fiber.tag) {
        case HostRoot:
        case HostPortal:
        case HostText:
        case Fragment:
        case ContextProvider:
        case ContextConsumer:
          return '';

        default:
          var owner = fiber._debugOwner;
          var source = fiber._debugSource;
          var name = getComponentName(fiber.type);
          var ownerName = null;

          if (owner) {
            ownerName = getComponentName(owner.type);
          }

          return describeComponentFrame(name, source, ownerName);
      }
    }

    function getStackByFiberInDevAndProd(workInProgress) {
      var info = '';
      var node = workInProgress;

      do {
        info += describeFiber(node);
        node = node.return;
      } while (node);

      return info;
    }

    var current = null;
    var phase = null;

    function getCurrentFiberOwnerNameInDevOrNull() {
      {
        if (current === null) {
          return null;
        }

        var owner = current._debugOwner;

        if (owner !== null && typeof owner !== 'undefined') {
          return getComponentName(owner.type);
        }
      }
      return null;
    }

    function getCurrentFiberStackInDev() {
      {
        if (current === null) {
          return '';
        } // Safe because if current fiber exists, we are reconciling,
        // and it is guaranteed to be the work-in-progress version.


        return getStackByFiberInDevAndProd(current);
      }
      return '';
    }

    function resetCurrentFiber() {
      {
        ReactDebugCurrentFrame.getCurrentStack = null;
        current = null;
        phase = null;
      }
    }

    function setCurrentFiber(fiber) {
      {
        ReactDebugCurrentFrame.getCurrentStack = getCurrentFiberStackInDev;
        current = fiber;
        phase = null;
      }
    }

    function setCurrentPhase(lifeCyclePhase) {
      {
        phase = lifeCyclePhase;
      }
    }

    var canUseDOM = !!(typeof window !== 'undefined' && typeof window.document !== 'undefined' && typeof window.document.createElement !== 'undefined');

    function endsWith(subject, search) {
      var length = subject.length;
      return subject.substring(length - search.length, length) === search;
    }

    var PLUGIN_EVENT_SYSTEM = 1;
    var RESPONDER_EVENT_SYSTEM = 1 << 1;
    var IS_PASSIVE = 1 << 2;
    var IS_ACTIVE = 1 << 3;
    var PASSIVE_NOT_SUPPORTED = 1 << 4;
    var IS_REPLAYED = 1 << 5;
    var restoreImpl = null;
    var restoreTarget = null;
    var restoreQueue = null;

    function restoreStateOfTarget(target) {
      // We perform this translation at the end of the event loop so that we
      // always receive the correct fiber here
      var internalInstance = getInstanceFromNode(target);

      if (!internalInstance) {
        // Unmounted
        return;
      }

      (function () {
        if (!(typeof restoreImpl === 'function')) {
          {
            throw ReactError(Error("setRestoreImplementation() needs to be called to handle a target for controlled events. This error is likely caused by a bug in React. Please file an issue."));
          }
        }
      })();

      var props = getFiberCurrentPropsFromNode(internalInstance.stateNode);
      restoreImpl(internalInstance.stateNode, internalInstance.type, props);
    }

    function setRestoreImplementation(impl) {
      restoreImpl = impl;
    }

    function enqueueStateRestore(target) {
      if (restoreTarget) {
        if (restoreQueue) {
          restoreQueue.push(target);
        } else {
          restoreQueue = [target];
        }
      } else {
        restoreTarget = target;
      }
    }

    function needsStateRestore() {
      return restoreTarget !== null || restoreQueue !== null;
    }

    function restoreStateIfNeeded() {
      if (!restoreTarget) {
        return;
      }

      var target = restoreTarget;
      var queuedTargets = restoreQueue;
      restoreTarget = null;
      restoreQueue = null;
      restoreStateOfTarget(target);

      if (queuedTargets) {
        for (var i = 0; i < queuedTargets.length; i++) {
          restoreStateOfTarget(queuedTargets[i]);
        }
      }
    }

    var enableUserTimingAPI = true; // Helps identify side effects in begin-phase lifecycle hooks and setState reducers:

    var debugRenderPhaseSideEffects = false; // In some cases, StrictMode should also double-render lifecycles.
    // This can be confusing for tests though,
    // And it can be bad for performance in production.
    // This feature flag can be used to control the behavior:

    var debugRenderPhaseSideEffectsForStrictMode = true; // To preserve the "Pause on caught exceptions" behavior of the debugger, we
    // replay the begin phase of a failed component inside invokeGuardedCallback.

    var replayFailedUnitOfWorkWithInvokeGuardedCallback = true; // Warn about deprecated, async-unsafe lifecycles; relates to RFC #6:

    var warnAboutDeprecatedLifecycles = true; // Gather advanced timing metrics for Profiler subtrees.

    var enableProfilerTimer = true; // Trace which interactions trigger each commit.

    var enableSchedulerTracing = true; // Only used in www builds.

    var enableSuspenseServerRenderer = false; // TODO: true? Here it might just be false.

    var enableSelectiveHydration = false; // Only used in www builds.
    // Only used in www builds.
    // Disable javascript: URL strings in href for XSS protection.

    var disableJavaScriptURLs = false; // React Fire: prevent the value and checked attributes from syncing
    // with their related DOM properties

    var disableInputAttributeSyncing = false; // These APIs will no longer be "unstable" in the upcoming 16.7 release,
    // Control this behavior with a flag to support 16.6 minor releases in the meanwhile.

    var enableStableConcurrentModeAPIs = false;
    var warnAboutShorthandPropertyCollision = false; // See https://github.com/react-native-community/discussions-and-proposals/issues/72 for more information
    // This is a flag so we can fix warnings in RN core before turning it on
    // Experimental React Flare event system and event components support.

    var enableFlareAPI = false; // Experimental Host Component support.

    var enableFundamentalAPI = false; // Experimental Scope support.

    var enableScopeAPI = false; // New API for JSX transforms to target - https://github.com/reactjs/rfcs/pull/107
    // We will enforce mocking scheduler with scheduler/unstable_mock at some point. (v17?)
    // Till then, we warn about the missing mock, but still fallback to a sync mode compatible version

    var warnAboutUnmockedScheduler = false; // For tests, we flush suspense fallbacks in an act scope;
    // *except* in some of our own tests, where we test incremental loading states.

    var flushSuspenseFallbacksInTests = true; // Changes priority of some events like mousemove to user-blocking priority,
    // but without making them discrete. The flag exists in case it causes
    // starvation problems.

    var enableUserBlockingEvents = false; // Add a callback property to suspense to notify which promises are currently
    // in the update queue. This allows reporting and tracing of what is causing
    // the user to see a loading state.
    // Also allows hydration callbacks to fire when a dehydrated boundary gets
    // hydrated or deleted.

    var enableSuspenseCallback = false; // Part of the simplification of React.createElement so we can eventually move
    // from React.createElement to React.jsx
    // https://github.com/reactjs/rfcs/blob/createlement-rfc/text/0000-create-element-changes.md

    var warnAboutDefaultPropsOnFunctionComponents = false;
    var warnAboutStringRefs = false;
    var disableLegacyContext = false;
    var disableSchedulerTimeoutBasedOnReactExpirationTime = false;
    var enableTrustedTypesIntegration = false; // the renderer. Such as when we're dispatching events or if third party
    // libraries need to call batchedUpdates. Eventually, this API will go away when
    // everything is batched by default. We'll then have a similar API to opt-out of
    // scheduled work and instead do synchronous work.
    // Defaults

    var batchedUpdatesImpl = function (fn, bookkeeping) {
      return fn(bookkeeping);
    };

    var discreteUpdatesImpl = function (fn, a, b, c) {
      return fn(a, b, c);
    };

    var flushDiscreteUpdatesImpl = function () {};

    var batchedEventUpdatesImpl = batchedUpdatesImpl;
    var isInsideEventHandler = false;
    var isBatchingEventUpdates = false;

    function finishEventHandler() {
      // Here we wait until all updates have propagated, which is important
      // when using controlled components within layers:
      // https://github.com/facebook/react/issues/1698
      // Then we restore state of any controlled component.
      var controlledComponentsHavePendingUpdates = needsStateRestore();

      if (controlledComponentsHavePendingUpdates) {
        // If a controlled event was fired, we may need to restore the state of
        // the DOM node back to the controlled value. This is necessary when React
        // bails out of the update without touching the DOM.
        flushDiscreteUpdatesImpl();
        restoreStateIfNeeded();
      }
    }

    function batchedUpdates(fn, bookkeeping) {
      if (isInsideEventHandler) {
        // If we are currently inside another batch, we need to wait until it
        // fully completes before restoring state.
        return fn(bookkeeping);
      }

      isInsideEventHandler = true;

      try {
        return batchedUpdatesImpl(fn, bookkeeping);
      } finally {
        isInsideEventHandler = false;
        finishEventHandler();
      }
    }

    function batchedEventUpdates(fn, a, b) {
      if (isBatchingEventUpdates) {
        // If we are currently inside another batch, we need to wait until it
        // fully completes before restoring state.
        return fn(a, b);
      }

      isBatchingEventUpdates = true;

      try {
        return batchedEventUpdatesImpl(fn, a, b);
      } finally {
        isBatchingEventUpdates = false;
        finishEventHandler();
      }
    } // This is for the React Flare event system


    function executeUserEventHandler(fn, value) {
      var previouslyInEventHandler = isInsideEventHandler;

      try {
        isInsideEventHandler = true;
        var type = typeof value === 'object' && value !== null ? value.type : '';
        invokeGuardedCallbackAndCatchFirstError(type, fn, undefined, value);
      } finally {
        isInsideEventHandler = previouslyInEventHandler;
      }
    }

    function discreteUpdates(fn, a, b, c) {
      var prevIsInsideEventHandler = isInsideEventHandler;
      isInsideEventHandler = true;

      try {
        return discreteUpdatesImpl(fn, a, b, c);
      } finally {
        isInsideEventHandler = prevIsInsideEventHandler;

        if (!isInsideEventHandler) {
          finishEventHandler();
        }
      }
    }

    var lastFlushedEventTimeStamp = 0;

    function flushDiscreteUpdatesIfNeeded(timeStamp) {
      // event.timeStamp isn't overly reliable due to inconsistencies in
      // how different browsers have historically provided the time stamp.
      // Some browsers provide high-resolution time stamps for all events,
      // some provide low-resolution time stamps for all events. FF < 52
      // even mixes both time stamps together. Some browsers even report
      // negative time stamps or time stamps that are 0 (iOS9) in some cases.
      // Given we are only comparing two time stamps with equality (!==),
      // we are safe from the resolution differences. If the time stamp is 0
      // we bail-out of preventing the flush, which can affect semantics,
      // such as if an earlier flush removes or adds event listeners that
      // are fired in the subsequent flush. However, this is the same
      // behaviour as we had before this change, so the risks are low.
      if (!isInsideEventHandler && (!enableFlareAPI || timeStamp === 0 || lastFlushedEventTimeStamp !== timeStamp)) {
        lastFlushedEventTimeStamp = timeStamp;
        flushDiscreteUpdatesImpl();
      }
    }

    function setBatchingImplementation(_batchedUpdatesImpl, _discreteUpdatesImpl, _flushDiscreteUpdatesImpl, _batchedEventUpdatesImpl) {
      batchedUpdatesImpl = _batchedUpdatesImpl;
      discreteUpdatesImpl = _discreteUpdatesImpl;
      flushDiscreteUpdatesImpl = _flushDiscreteUpdatesImpl;
      batchedEventUpdatesImpl = _batchedEventUpdatesImpl;
    }

    var DiscreteEvent = 0;
    var UserBlockingEvent = 1;
    var ContinuousEvent = 2; // CommonJS interop named imports.

    var UserBlockingPriority = Scheduler.unstable_UserBlockingPriority;
    var runWithPriority = Scheduler.unstable_runWithPriority;
    var listenToResponderEventTypesImpl;

    function setListenToResponderEventTypes(_listenToResponderEventTypesImpl) {
      listenToResponderEventTypesImpl = _listenToResponderEventTypesImpl;
    }

    var activeTimeouts = new Map();
    var rootEventTypesToEventResponderInstances = new Map();
    var DoNotPropagateToNextResponder = 0;
    var PropagateToNextResponder = 1;
    var currentTimeStamp = 0;
    var currentTimers = new Map();
    var currentInstance = null;
    var currentTimerIDCounter = 0;
    var currentDocument = null;
    var currentPropagationBehavior = DoNotPropagateToNextResponder;
    var eventResponderContext = {
      dispatchEvent: function (eventValue, eventListener, eventPriority) {
        validateResponderContext();
        validateEventValue(eventValue);

        switch (eventPriority) {
          case DiscreteEvent:
            {
              flushDiscreteUpdatesIfNeeded(currentTimeStamp);
              discreteUpdates(function () {
                return executeUserEventHandler(eventListener, eventValue);
              });
              break;
            }

          case UserBlockingEvent:
            {
              if (enableUserBlockingEvents) {
                runWithPriority(UserBlockingPriority, function () {
                  return executeUserEventHandler(eventListener, eventValue);
                });
              } else {
                executeUserEventHandler(eventListener, eventValue);
              }

              break;
            }

          case ContinuousEvent:
            {
              executeUserEventHandler(eventListener, eventValue);
              break;
            }
        }
      },
      isTargetWithinResponder: function (target) {
        validateResponderContext();

        if (target != null) {
          var fiber = getClosestInstanceFromNode(target);
          var responderFiber = currentInstance.fiber;

          while (fiber !== null) {
            if (fiber === responderFiber || fiber.alternate === responderFiber) {
              return true;
            }

            fiber = fiber.return;
          }
        }

        return false;
      },
      isTargetWithinResponderScope: function (target) {
        validateResponderContext();
        var componentInstance = currentInstance;
        var responder = componentInstance.responder;

        if (target != null) {
          var fiber = getClosestInstanceFromNode(target);
          var responderFiber = currentInstance.fiber;

          while (fiber !== null) {
            if (fiber === responderFiber || fiber.alternate === responderFiber) {
              return true;
            }

            if (doesFiberHaveResponder(fiber, responder)) {
              return false;
            }

            fiber = fiber.return;
          }
        }

        return false;
      },
      isTargetWithinNode: function (childTarget, parentTarget) {
        validateResponderContext();
        var childFiber = getClosestInstanceFromNode(childTarget);
        var parentFiber = getClosestInstanceFromNode(parentTarget);

        if (childFiber != null && parentFiber != null) {
          var parentAlternateFiber = parentFiber.alternate;
          var node = childFiber;

          while (node !== null) {
            if (node === parentFiber || node === parentAlternateFiber) {
              return true;
            }

            node = node.return;
          }

          return false;
        } // Fallback to DOM APIs


        return parentTarget.contains(childTarget);
      },
      addRootEventTypes: function (rootEventTypes) {
        validateResponderContext();
        listenToResponderEventTypesImpl(rootEventTypes, currentDocument);

        for (var i = 0; i < rootEventTypes.length; i++) {
          var rootEventType = rootEventTypes[i];
          var eventResponderInstance = currentInstance;
          registerRootEventType(rootEventType, eventResponderInstance);
        }
      },
      removeRootEventTypes: function (rootEventTypes) {
        validateResponderContext();

        for (var i = 0; i < rootEventTypes.length; i++) {
          var rootEventType = rootEventTypes[i];
          var rootEventResponders = rootEventTypesToEventResponderInstances.get(rootEventType);
          var rootEventTypesSet = currentInstance.rootEventTypes;

          if (rootEventTypesSet !== null) {
            rootEventTypesSet.delete(rootEventType);
          }

          if (rootEventResponders !== undefined) {
            rootEventResponders.delete(currentInstance);
          }
        }
      },
      setTimeout: function (func, delay) {
        validateResponderContext();

        if (currentTimers === null) {
          currentTimers = new Map();
        }

        var timeout = currentTimers.get(delay);
        var timerId = currentTimerIDCounter++;

        if (timeout === undefined) {
          var timers = new Map();
          var id = setTimeout(function () {
            processTimers(timers, delay);
          }, delay);
          timeout = {
            id: id,
            timers: timers
          };
          currentTimers.set(delay, timeout);
        }

        timeout.timers.set(timerId, {
          instance: currentInstance,
          func: func,
          id: timerId,
          timeStamp: currentTimeStamp
        });
        activeTimeouts.set(timerId, timeout);
        return timerId;
      },
      clearTimeout: function (timerId) {
        validateResponderContext();
        var timeout = activeTimeouts.get(timerId);

        if (timeout !== undefined) {
          var timers = timeout.timers;
          timers.delete(timerId);

          if (timers.size === 0) {
            clearTimeout(timeout.id);
          }
        }
      },
      getActiveDocument: getActiveDocument,
      objectAssign: _assign,
      getTimeStamp: function () {
        validateResponderContext();
        return currentTimeStamp;
      },
      isTargetWithinHostComponent: function (target, elementType) {
        validateResponderContext();
        var fiber = getClosestInstanceFromNode(target);

        while (fiber !== null) {
          if (fiber.tag === HostComponent && fiber.type === elementType) {
            return true;
          }

          fiber = fiber.return;
        }

        return false;
      },
      continuePropagation: function () {
        currentPropagationBehavior = PropagateToNextResponder;
      },
      enqueueStateRestore: enqueueStateRestore,
      getResponderNode: function () {
        validateResponderContext();
        var responderFiber = currentInstance.fiber;

        if (responderFiber.tag === ScopeComponent) {
          return null;
        }

        return responderFiber.stateNode;
      }
    };

    function validateEventValue(eventValue) {
      if (typeof eventValue === 'object' && eventValue !== null) {
        var target = eventValue.target,
            type = eventValue.type,
            timeStamp = eventValue.timeStamp;

        if (target == null || type == null || timeStamp == null) {
          throw new Error('context.dispatchEvent: "target", "timeStamp", and "type" fields on event object are required.');
        }

        var showWarning = function (name) {
          {
            warning$1(false, '%s is not available on event objects created from event responder modules (React Flare). ' + 'Try wrapping in a conditional, i.e. `if (event.type !== "press") { event.%s }`', name, name);
          }
        };

        eventValue.isDefaultPrevented = function () {
          {
            showWarning('isDefaultPrevented()');
          }
        };

        eventValue.isPropagationStopped = function () {
          {
            showWarning('isPropagationStopped()');
          }
        }; // $FlowFixMe: we don't need value, Flow thinks we do


        Object.defineProperty(eventValue, 'nativeEvent', {
          get: function () {
            {
              showWarning('nativeEvent');
            }
          }
        });
      }
    }

    function doesFiberHaveResponder(fiber, responder) {
      var tag = fiber.tag;

      if (tag === HostComponent || tag === ScopeComponent) {
        var dependencies = fiber.dependencies;

        if (dependencies !== null) {
          var respondersMap = dependencies.responders;

          if (respondersMap !== null && respondersMap.has(responder)) {
            return true;
          }
        }
      }

      return false;
    }

    function getActiveDocument() {
      return currentDocument;
    }

    function processTimers(timers, delay) {
      var timersArr = Array.from(timers.values());
      var previousInstance = currentInstance;
      var previousTimers = currentTimers;

      try {
        batchedEventUpdates(function () {
          for (var i = 0; i < timersArr.length; i++) {
            var _timersArr$i = timersArr[i],
                instance = _timersArr$i.instance,
                func = _timersArr$i.func,
                id = _timersArr$i.id,
                timeStamp = _timersArr$i.timeStamp;
            currentInstance = instance;
            currentTimeStamp = timeStamp + delay;

            try {
              func();
            } finally {
              activeTimeouts.delete(id);
            }
          }
        });
      } finally {
        currentTimers = previousTimers;
        currentInstance = previousInstance;
        currentTimeStamp = 0;
      }
    }

    function createDOMResponderEvent(topLevelType, nativeEvent, nativeEventTarget, passive, passiveSupported) {
      var _ref = nativeEvent,
          buttons = _ref.buttons,
          pointerType = _ref.pointerType;
      var eventPointerType = '';

      if (pointerType !== undefined) {
        eventPointerType = pointerType;
      } else if (nativeEvent.key !== undefined) {
        eventPointerType = 'keyboard';
      } else if (buttons !== undefined) {
        eventPointerType = 'mouse';
      } else if (nativeEvent.changedTouches !== undefined) {
        eventPointerType = 'touch';
      }

      return {
        nativeEvent: nativeEvent,
        passive: passive,
        passiveSupported: passiveSupported,
        pointerType: eventPointerType,
        target: nativeEventTarget,
        type: topLevelType
      };
    }

    function responderEventTypesContainType(eventTypes, type) {
      for (var i = 0, len = eventTypes.length; i < len; i++) {
        if (eventTypes[i] === type) {
          return true;
        }
      }

      return false;
    }

    function validateResponderTargetEventTypes(eventType, responder) {
      var targetEventTypes = responder.targetEventTypes; // Validate the target event type exists on the responder

      if (targetEventTypes !== null) {
        return responderEventTypesContainType(targetEventTypes, eventType);
      }

      return false;
    }

    function traverseAndHandleEventResponderInstances(topLevelType, targetFiber, nativeEvent, nativeEventTarget, eventSystemFlags) {
      var isPassiveEvent = (eventSystemFlags & IS_PASSIVE) !== 0;
      var isPassiveSupported = (eventSystemFlags & PASSIVE_NOT_SUPPORTED) === 0;
      var isPassive = isPassiveEvent || !isPassiveSupported;
      var eventType = isPassive ? topLevelType : topLevelType + '_active'; // Trigger event responders in this order:
      // - Bubble target responder phase
      // - Root responder phase

      var visitedResponders = new Set();
      var responderEvent = createDOMResponderEvent(topLevelType, nativeEvent, nativeEventTarget, isPassiveEvent, isPassiveSupported);
      var node = targetFiber;
      var insidePortal = false;

      while (node !== null) {
        var _node = node,
            dependencies = _node.dependencies,
            tag = _node.tag;

        if (tag === HostPortal) {
          insidePortal = true;
        } else if ((tag === HostComponent || tag === ScopeComponent) && dependencies !== null) {
          var respondersMap = dependencies.responders;

          if (respondersMap !== null) {
            var responderInstances = Array.from(respondersMap.values());

            for (var i = 0, length = responderInstances.length; i < length; i++) {
              var responderInstance = responderInstances[i];
              var props = responderInstance.props,
                  responder = responderInstance.responder,
                  state = responderInstance.state;

              if (!visitedResponders.has(responder) && validateResponderTargetEventTypes(eventType, responder) && (!insidePortal || responder.targetPortalPropagation)) {
                visitedResponders.add(responder);
                var onEvent = responder.onEvent;

                if (onEvent !== null) {
                  currentInstance = responderInstance;
                  onEvent(responderEvent, eventResponderContext, props, state);

                  if (currentPropagationBehavior === PropagateToNextResponder) {
                    visitedResponders.delete(responder);
                    currentPropagationBehavior = DoNotPropagateToNextResponder;
                  }
                }
              }
            }
          }
        }

        node = node.return;
      } // Root phase


      var rootEventResponderInstances = rootEventTypesToEventResponderInstances.get(eventType);

      if (rootEventResponderInstances !== undefined) {
        var _responderInstances = Array.from(rootEventResponderInstances);

        for (var _i = 0; _i < _responderInstances.length; _i++) {
          var _responderInstance = _responderInstances[_i];
          var props = _responderInstance.props,
              responder = _responderInstance.responder,
              state = _responderInstance.state;
          var onRootEvent = responder.onRootEvent;

          if (onRootEvent !== null) {
            currentInstance = _responderInstance;
            onRootEvent(responderEvent, eventResponderContext, props, state);
          }
        }
      }
    }

    function mountEventResponder(responder, responderInstance, props, state) {
      var onMount = responder.onMount;

      if (onMount !== null) {
        var previousInstance = currentInstance;
        var previousTimers = currentTimers;
        currentInstance = responderInstance;

        try {
          batchedEventUpdates(function () {
            onMount(eventResponderContext, props, state);
          });
        } finally {
          currentInstance = previousInstance;
          currentTimers = previousTimers;
        }
      }
    }

    function unmountEventResponder(responderInstance) {
      var responder = responderInstance.responder;
      var onUnmount = responder.onUnmount;

      if (onUnmount !== null) {
        var props = responderInstance.props,
            state = responderInstance.state;
        var previousInstance = currentInstance;
        var previousTimers = currentTimers;
        currentInstance = responderInstance;

        try {
          batchedEventUpdates(function () {
            onUnmount(eventResponderContext, props, state);
          });
        } finally {
          currentInstance = previousInstance;
          currentTimers = previousTimers;
        }
      }

      var rootEventTypesSet = responderInstance.rootEventTypes;

      if (rootEventTypesSet !== null) {
        var rootEventTypes = Array.from(rootEventTypesSet);

        for (var i = 0; i < rootEventTypes.length; i++) {
          var topLevelEventType = rootEventTypes[i];
          var rootEventResponderInstances = rootEventTypesToEventResponderInstances.get(topLevelEventType);

          if (rootEventResponderInstances !== undefined) {
            rootEventResponderInstances.delete(responderInstance);
          }
        }
      }
    }

    function validateResponderContext() {
      (function () {
        if (!(currentInstance !== null)) {
          {
            throw ReactError(Error("An event responder context was used outside of an event cycle. Use context.setTimeout() to use asynchronous responder context outside of event cycle ."));
          }
        }
      })();
    }

    function dispatchEventForResponderEventSystem(topLevelType, targetFiber, nativeEvent, nativeEventTarget, eventSystemFlags) {
      if (enableFlareAPI) {
        var previousInstance = currentInstance;
        var previousTimers = currentTimers;
        var previousTimeStamp = currentTimeStamp;
        var previousDocument = currentDocument;
        var previousPropagationBehavior = currentPropagationBehavior;
        currentPropagationBehavior = DoNotPropagateToNextResponder;
        currentTimers = null; // nodeType 9 is DOCUMENT_NODE

        currentDocument = nativeEventTarget.nodeType === 9 ? nativeEventTarget : nativeEventTarget.ownerDocument; // We might want to control timeStamp another way here

        currentTimeStamp = nativeEvent.timeStamp;

        try {
          batchedEventUpdates(function () {
            traverseAndHandleEventResponderInstances(topLevelType, targetFiber, nativeEvent, nativeEventTarget, eventSystemFlags);
          });
        } finally {
          currentTimers = previousTimers;
          currentInstance = previousInstance;
          currentTimeStamp = previousTimeStamp;
          currentDocument = previousDocument;
          currentPropagationBehavior = previousPropagationBehavior;
        }
      }
    }

    function addRootEventTypesForResponderInstance(responderInstance, rootEventTypes) {
      for (var i = 0; i < rootEventTypes.length; i++) {
        var rootEventType = rootEventTypes[i];
        registerRootEventType(rootEventType, responderInstance);
      }
    }

    function registerRootEventType(rootEventType, eventResponderInstance) {
      var rootEventResponderInstances = rootEventTypesToEventResponderInstances.get(rootEventType);

      if (rootEventResponderInstances === undefined) {
        rootEventResponderInstances = new Set();
        rootEventTypesToEventResponderInstances.set(rootEventType, rootEventResponderInstances);
      }

      var rootEventTypesSet = eventResponderInstance.rootEventTypes;

      if (rootEventTypesSet === null) {
        rootEventTypesSet = eventResponderInstance.rootEventTypes = new Set();
      }

      (function () {
        if (!!rootEventTypesSet.has(rootEventType)) {
          {
            throw ReactError(Error("addRootEventTypes() found a duplicate root event type of \"" + rootEventType + "\". This might be because the event type exists in the event responder \"rootEventTypes\" array or because of a previous addRootEventTypes() using this root event type."));
          }
        }
      })();

      rootEventTypesSet.add(rootEventType);
      rootEventResponderInstances.add(eventResponderInstance);
    } // A reserved attribute.
    // It is handled by React separately and shouldn't be written to the DOM.


    var RESERVED = 0; // A simple string attribute.
    // Attributes that aren't in the whitelist are presumed to have this type.

    var STRING = 1; // A string attribute that accepts booleans in React. In HTML, these are called
    // "enumerated" attributes with "true" and "false" as possible values.
    // When true, it should be set to a "true" string.
    // When false, it should be set to a "false" string.

    var BOOLEANISH_STRING = 2; // A real boolean attribute.
    // When true, it should be present (set either to an empty string or its name).
    // When false, it should be omitted.

    var BOOLEAN = 3; // An attribute that can be used as a flag as well as with a value.
    // When true, it should be present (set either to an empty string or its name).
    // When false, it should be omitted.
    // For any other value, should be present with that value.

    var OVERLOADED_BOOLEAN = 4; // An attribute that must be numeric or parse as a numeric.
    // When falsy, it should be removed.

    var NUMERIC = 5; // An attribute that must be positive numeric or parse as a positive numeric.
    // When falsy, it should be removed.

    var POSITIVE_NUMERIC = 6;
    /* eslint-disable max-len */

    var ATTRIBUTE_NAME_START_CHAR = ":A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD";
    /* eslint-enable max-len */

    var ATTRIBUTE_NAME_CHAR = ATTRIBUTE_NAME_START_CHAR + "\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040";
    var ROOT_ATTRIBUTE_NAME = 'data-reactroot';
    var VALID_ATTRIBUTE_NAME_REGEX = new RegExp('^[' + ATTRIBUTE_NAME_START_CHAR + '][' + ATTRIBUTE_NAME_CHAR + ']*$');
    var hasOwnProperty = Object.prototype.hasOwnProperty;
    var illegalAttributeNameCache = {};
    var validatedAttributeNameCache = {};

    function isAttributeNameSafe(attributeName) {
      if (hasOwnProperty.call(validatedAttributeNameCache, attributeName)) {
        return true;
      }

      if (hasOwnProperty.call(illegalAttributeNameCache, attributeName)) {
        return false;
      }

      if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName)) {
        validatedAttributeNameCache[attributeName] = true;
        return true;
      }

      illegalAttributeNameCache[attributeName] = true;
      {
        warning$1(false, 'Invalid attribute name: `%s`', attributeName);
      }
      return false;
    }

    function shouldIgnoreAttribute(name, propertyInfo, isCustomComponentTag) {
      if (propertyInfo !== null) {
        return propertyInfo.type === RESERVED;
      }

      if (isCustomComponentTag) {
        return false;
      }

      if (name.length > 2 && (name[0] === 'o' || name[0] === 'O') && (name[1] === 'n' || name[1] === 'N')) {
        return true;
      }

      return false;
    }

    function shouldRemoveAttributeWithWarning(name, value, propertyInfo, isCustomComponentTag) {
      if (propertyInfo !== null && propertyInfo.type === RESERVED) {
        return false;
      }

      switch (typeof value) {
        case 'function': // $FlowIssue symbol is perfectly valid here

        case 'symbol':
          // eslint-disable-line
          return true;

        case 'boolean':
          {
            if (isCustomComponentTag) {
              return false;
            }

            if (propertyInfo !== null) {
              return !propertyInfo.acceptsBooleans;
            } else {
              var prefix = name.toLowerCase().slice(0, 5);
              return prefix !== 'data-' && prefix !== 'aria-';
            }
          }

        default:
          return false;
      }
    }

    function shouldRemoveAttribute(name, value, propertyInfo, isCustomComponentTag) {
      if (value === null || typeof value === 'undefined') {
        return true;
      }

      if (shouldRemoveAttributeWithWarning(name, value, propertyInfo, isCustomComponentTag)) {
        return true;
      }

      if (isCustomComponentTag) {
        return false;
      }

      if (propertyInfo !== null) {
        switch (propertyInfo.type) {
          case BOOLEAN:
            return !value;

          case OVERLOADED_BOOLEAN:
            return value === false;

          case NUMERIC:
            return isNaN(value);

          case POSITIVE_NUMERIC:
            return isNaN(value) || value < 1;
        }
      }

      return false;
    }

    function getPropertyInfo(name) {
      return properties.hasOwnProperty(name) ? properties[name] : null;
    }

    function PropertyInfoRecord(name, type, mustUseProperty, attributeName, attributeNamespace, sanitizeURL) {
      this.acceptsBooleans = type === BOOLEANISH_STRING || type === BOOLEAN || type === OVERLOADED_BOOLEAN;
      this.attributeName = attributeName;
      this.attributeNamespace = attributeNamespace;
      this.mustUseProperty = mustUseProperty;
      this.propertyName = name;
      this.type = type;
      this.sanitizeURL = sanitizeURL;
    } // When adding attributes to this list, be sure to also add them to
    // the `possibleStandardNames` module to ensure casing and incorrect
    // name warnings.


    var properties = {}; // These props are reserved by React. They shouldn't be written to the DOM.

    ['children', 'dangerouslySetInnerHTML', // TODO: This prevents the assignment of defaultValue to regular
    // elements (not just inputs). Now that ReactDOMInput assigns to the
    // defaultValue property -- do we need this?
    'defaultValue', 'defaultChecked', 'innerHTML', 'suppressContentEditableWarning', 'suppressHydrationWarning', 'style'].forEach(function (name) {
      properties[name] = new PropertyInfoRecord(name, RESERVED, false, // mustUseProperty
      name, // attributeName
      null, // attributeNamespace
      false);
    }); // A few React string attributes have a different name.
    // This is a mapping from React prop names to the attribute names.

    [['acceptCharset', 'accept-charset'], ['className', 'class'], ['htmlFor', 'for'], ['httpEquiv', 'http-equiv']].forEach(function (_ref) {
      var name = _ref[0],
          attributeName = _ref[1];
      properties[name] = new PropertyInfoRecord(name, STRING, false, // mustUseProperty
      attributeName, // attributeName
      null, // attributeNamespace
      false);
    }); // These are "enumerated" HTML attributes that accept "true" and "false".
    // In React, we let users pass `true` and `false` even though technically
    // these aren't boolean attributes (they are coerced to strings).

    ['contentEditable', 'draggable', 'spellCheck', 'value'].forEach(function (name) {
      properties[name] = new PropertyInfoRecord(name, BOOLEANISH_STRING, false, // mustUseProperty
      name.toLowerCase(), // attributeName
      null, // attributeNamespace
      false);
    }); // These are "enumerated" SVG attributes that accept "true" and "false".
    // In React, we let users pass `true` and `false` even though technically
    // these aren't boolean attributes (they are coerced to strings).
    // Since these are SVG attributes, their attribute names are case-sensitive.

    ['autoReverse', 'externalResourcesRequired', 'focusable', 'preserveAlpha'].forEach(function (name) {
      properties[name] = new PropertyInfoRecord(name, BOOLEANISH_STRING, false, // mustUseProperty
      name, // attributeName
      null, // attributeNamespace
      false);
    }); // These are HTML boolean attributes.

    ['allowFullScreen', 'async', // Note: there is a special case that prevents it from being written to the DOM
    // on the client side because the browsers are inconsistent. Instead we call focus().
    'autoFocus', 'autoPlay', 'controls', 'default', 'defer', 'disabled', 'disablePictureInPicture', 'formNoValidate', 'hidden', 'loop', 'noModule', 'noValidate', 'open', 'playsInline', 'readOnly', 'required', 'reversed', 'scoped', 'seamless', // Microdata
    'itemScope'].forEach(function (name) {
      properties[name] = new PropertyInfoRecord(name, BOOLEAN, false, // mustUseProperty
      name.toLowerCase(), // attributeName
      null, // attributeNamespace
      false);
    }); // These are the few React props that we set as DOM properties
    // rather than attributes. These are all booleans.

    ['checked', // Note: `option.selected` is not updated if `select.multiple` is
    // disabled with `removeAttribute`. We have special logic for handling this.
    'multiple', 'muted', 'selected'].forEach(function (name) {
      properties[name] = new PropertyInfoRecord(name, BOOLEAN, true, // mustUseProperty
      name, // attributeName
      null, // attributeNamespace
      false);
    }); // These are HTML attributes that are "overloaded booleans": they behave like
    // booleans, but can also accept a string value.

    ['capture', 'download'].forEach(function (name) {
      properties[name] = new PropertyInfoRecord(name, OVERLOADED_BOOLEAN, false, // mustUseProperty
      name, // attributeName
      null, // attributeNamespace
      false);
    }); // These are HTML attributes that must be positive numbers.

    ['cols', 'rows', 'size', 'span'].forEach(function (name) {
      properties[name] = new PropertyInfoRecord(name, POSITIVE_NUMERIC, false, // mustUseProperty
      name, // attributeName
      null, // attributeNamespace
      false);
    }); // These are HTML attributes that must be numbers.

    ['rowSpan', 'start'].forEach(function (name) {
      properties[name] = new PropertyInfoRecord(name, NUMERIC, false, // mustUseProperty
      name.toLowerCase(), // attributeName
      null, // attributeNamespace
      false);
    });
    var CAMELIZE = /[\-\:]([a-z])/g;

    var capitalize = function (token) {
      return token[1].toUpperCase();
    }; // This is a list of all SVG attributes that need special casing, namespacing,
    // or boolean value assignment. Regular attributes that just accept strings
    // and have the same names are omitted, just like in the HTML whitelist.
    // Some of these attributes can be hard to find. This list was created by
    // scrapping the MDN documentation.


    ['accent-height', 'alignment-baseline', 'arabic-form', 'baseline-shift', 'cap-height', 'clip-path', 'clip-rule', 'color-interpolation', 'color-interpolation-filters', 'color-profile', 'color-rendering', 'dominant-baseline', 'enable-background', 'fill-opacity', 'fill-rule', 'flood-color', 'flood-opacity', 'font-family', 'font-size', 'font-size-adjust', 'font-stretch', 'font-style', 'font-variant', 'font-weight', 'glyph-name', 'glyph-orientation-horizontal', 'glyph-orientation-vertical', 'horiz-adv-x', 'horiz-origin-x', 'image-rendering', 'letter-spacing', 'lighting-color', 'marker-end', 'marker-mid', 'marker-start', 'overline-position', 'overline-thickness', 'paint-order', 'panose-1', 'pointer-events', 'rendering-intent', 'shape-rendering', 'stop-color', 'stop-opacity', 'strikethrough-position', 'strikethrough-thickness', 'stroke-dasharray', 'stroke-dashoffset', 'stroke-linecap', 'stroke-linejoin', 'stroke-miterlimit', 'stroke-opacity', 'stroke-width', 'text-anchor', 'text-decoration', 'text-rendering', 'underline-position', 'underline-thickness', 'unicode-bidi', 'unicode-range', 'units-per-em', 'v-alphabetic', 'v-hanging', 'v-ideographic', 'v-mathematical', 'vector-effect', 'vert-adv-y', 'vert-origin-x', 'vert-origin-y', 'word-spacing', 'writing-mode', 'xmlns:xlink', 'x-height'].forEach(function (attributeName) {
      var name = attributeName.replace(CAMELIZE, capitalize);
      properties[name] = new PropertyInfoRecord(name, STRING, false, // mustUseProperty
      attributeName, null, // attributeNamespace
      false);
    }); // String SVG attributes with the xlink namespace.

    ['xlink:actuate', 'xlink:arcrole', 'xlink:role', 'xlink:show', 'xlink:title', 'xlink:type'].forEach(function (attributeName) {
      var name = attributeName.replace(CAMELIZE, capitalize);
      properties[name] = new PropertyInfoRecord(name, STRING, false, // mustUseProperty
      attributeName, 'http://www.w3.org/1999/xlink', false);
    }); // String SVG attributes with the xml namespace.

    ['xml:base', 'xml:lang', 'xml:space'].forEach(function (attributeName) {
      var name = attributeName.replace(CAMELIZE, capitalize);
      properties[name] = new PropertyInfoRecord(name, STRING, false, // mustUseProperty
      attributeName, 'http://www.w3.org/XML/1998/namespace', false);
    }); // These attribute exists both in HTML and SVG.
    // The attribute name is case-sensitive in SVG so we can't just use
    // the React name like we do for attributes that exist only in HTML.

    ['tabIndex', 'crossOrigin'].forEach(function (attributeName) {
      properties[attributeName] = new PropertyInfoRecord(attributeName, STRING, false, // mustUseProperty
      attributeName.toLowerCase(), // attributeName
      null, // attributeNamespace
      false);
    }); // These attributes accept URLs. These must not allow javascript: URLS.
    // These will also need to accept Trusted Types object in the future.

    var xlinkHref = 'xlinkHref';
    properties[xlinkHref] = new PropertyInfoRecord('xlinkHref', STRING, false, // mustUseProperty
    'xlink:href', 'http://www.w3.org/1999/xlink', true);
    ['src', 'href', 'action', 'formAction'].forEach(function (attributeName) {
      properties[attributeName] = new PropertyInfoRecord(attributeName, STRING, false, // mustUseProperty
      attributeName.toLowerCase(), // attributeName
      null, // attributeNamespace
      true);
    });
    var ReactDebugCurrentFrame$1 = null;
    {
      ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
    } // A javascript: URL can contain leading C0 control or \u0020 SPACE,
    // and any newline or tab are filtered out as if they're not part of the URL.
    // https://url.spec.whatwg.org/#url-parsing
    // Tab or newline are defined as \r\n\t:
    // https://infra.spec.whatwg.org/#ascii-tab-or-newline
    // A C0 control is a code point in the range \u0000 NULL to \u001F
    // INFORMATION SEPARATOR ONE, inclusive:
    // https://infra.spec.whatwg.org/#c0-control-or-space

    /* eslint-disable max-len */

    var isJavaScriptProtocol = /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*\:/i;
    var didWarn = false;

    function sanitizeURL(url) {
      if (disableJavaScriptURLs) {
        (function () {
          if (!!isJavaScriptProtocol.test(url)) {
            {
              throw ReactError(Error("React has blocked a javascript: URL as a security precaution." + ReactDebugCurrentFrame$1.getStackAddendum()));
            }
          }
        })();
      } else if (true && !didWarn && isJavaScriptProtocol.test(url)) {
        didWarn = true;
        warning$1(false, 'A future version of React will block javascript: URLs as a security precaution. ' + 'Use event handlers instead if you can. If you need to generate unsafe HTML try ' + 'using dangerouslySetInnerHTML instead. React was passed %s.', JSON.stringify(url));
      }
    } // Flow does not allow string concatenation of most non-string types. To work
    // around this limitation, we use an opaque type that can only be obtained by
    // passing the value through getToStringValue first.


    function toString(value) {
      return '' + value;
    }

    function getToStringValue(value) {
      switch (typeof value) {
        case 'boolean':
        case 'number':
        case 'object':
        case 'string':
        case 'undefined':
          return value;

        default:
          // function, symbol are assigned as empty strings
          return '';
      }
    }
    /** Trusted value is a wrapper for "safe" values which can be assigned to DOM execution sinks. */

    /**
     * We allow passing objects with toString method as element attributes or in dangerouslySetInnerHTML
     * and we do validations that the value is safe. Once we do validation we want to use the validated
     * value instead of the object (because object.toString may return something else on next call).
     *
     * If application uses Trusted Types we don't stringify trusted values, but preserve them as objects.
     */


    var toStringOrTrustedType = toString;

    if (enableTrustedTypesIntegration && typeof trustedTypes !== 'undefined') {
      var isHTML = trustedTypes.isHTML;
      var isScript = trustedTypes.isScript;
      var isScriptURL = trustedTypes.isScriptURL; // TrustedURLs are deprecated and will be removed soon: https://github.com/WICG/trusted-types/pull/204

      var isURL = trustedTypes.isURL ? trustedTypes.isURL : function (value) {
        return false;
      };

      toStringOrTrustedType = function (value) {
        if (typeof value === 'object' && (isHTML(value) || isScript(value) || isScriptURL(value) || isURL(value))) {
          // Pass Trusted Types through.
          return value;
        }

        return toString(value);
      };
    }
    /**
     * Set attribute for a node. The attribute value can be either string or
     * Trusted value (if application uses Trusted Types).
     */


    function setAttribute(node, attributeName, attributeValue) {
      node.setAttribute(attributeName, attributeValue);
    }
    /**
     * Set attribute with namespace for a node. The attribute value can be either string or
     * Trusted value (if application uses Trusted Types).
     */


    function setAttributeNS(node, attributeNamespace, attributeName, attributeValue) {
      node.setAttributeNS(attributeNamespace, attributeName, attributeValue);
    }
    /**
     * Get the value for a property on a node. Only used in DEV for SSR validation.
     * The "expected" argument is used as a hint of what the expected value is.
     * Some properties have multiple equivalent values.
     */


    function getValueForProperty(node, name, expected, propertyInfo) {
      {
        if (propertyInfo.mustUseProperty) {
          var propertyName = propertyInfo.propertyName;
          return node[propertyName];
        } else {
          if (!disableJavaScriptURLs && propertyInfo.sanitizeURL) {
            // If we haven't fully disabled javascript: URLs, and if
            // the hydration is successful of a javascript: URL, we
            // still want to warn on the client.
            sanitizeURL('' + expected);
          }

          var attributeName = propertyInfo.attributeName;
          var stringValue = null;

          if (propertyInfo.type === OVERLOADED_BOOLEAN) {
            if (node.hasAttribute(attributeName)) {
              var value = node.getAttribute(attributeName);

              if (value === '') {
                return true;
              }

              if (shouldRemoveAttribute(name, expected, propertyInfo, false)) {
                return value;
              }

              if (value === '' + expected) {
                return expected;
              }

              return value;
            }
          } else if (node.hasAttribute(attributeName)) {
            if (shouldRemoveAttribute(name, expected, propertyInfo, false)) {
              // We had an attribute but shouldn't have had one, so read it
              // for the error message.
              return node.getAttribute(attributeName);
            }

            if (propertyInfo.type === BOOLEAN) {
              // If this was a boolean, it doesn't matter what the value is
              // the fact that we have it is the same as the expected.
              return expected;
            } // Even if this property uses a namespace we use getAttribute
            // because we assume its namespaced name is the same as our config.
            // To use getAttributeNS we need the local name which we don't have
            // in our config atm.


            stringValue = node.getAttribute(attributeName);
          }

          if (shouldRemoveAttribute(name, expected, propertyInfo, false)) {
            return stringValue === null ? expected : stringValue;
          } else if (stringValue === '' + expected) {
            return expected;
          } else {
            return stringValue;
          }
        }
      }
    }
    /**
     * Get the value for a attribute on a node. Only used in DEV for SSR validation.
     * The third argument is used as a hint of what the expected value is. Some
     * attributes have multiple equivalent values.
     */


    function getValueForAttribute(node, name, expected) {
      {
        if (!isAttributeNameSafe(name)) {
          return;
        }

        if (!node.hasAttribute(name)) {
          return expected === undefined ? undefined : null;
        }

        var value = node.getAttribute(name);

        if (value === '' + expected) {
          return expected;
        }

        return value;
      }
    }
    /**
     * Sets the value for a property on a node.
     *
     * @param {DOMElement} node
     * @param {string} name
     * @param {*} value
     */


    function setValueForProperty(node, name, value, isCustomComponentTag) {
      var propertyInfo = getPropertyInfo(name);

      if (shouldIgnoreAttribute(name, propertyInfo, isCustomComponentTag)) {
        return;
      }

      if (shouldRemoveAttribute(name, value, propertyInfo, isCustomComponentTag)) {
        value = null;
      } // If the prop isn't in the special list, treat it as a simple attribute.


      if (isCustomComponentTag || propertyInfo === null) {
        if (isAttributeNameSafe(name)) {
          var _attributeName = name;

          if (value === null) {
            node.removeAttribute(_attributeName);
          } else {
            setAttribute(node, _attributeName, toStringOrTrustedType(value));
          }
        }

        return;
      }

      var mustUseProperty = propertyInfo.mustUseProperty;

      if (mustUseProperty) {
        var propertyName = propertyInfo.propertyName;

        if (value === null) {
          var type = propertyInfo.type;
          node[propertyName] = type === BOOLEAN ? false : '';
        } else {
          // Contrary to `setAttribute`, object properties are properly
          // `toString`ed by IE8/9.
          node[propertyName] = value;
        }

        return;
      } // The rest are treated as attributes with special cases.


      var attributeName = propertyInfo.attributeName,
          attributeNamespace = propertyInfo.attributeNamespace;

      if (value === null) {
        node.removeAttribute(attributeName);
      } else {
        var _type = propertyInfo.type;
        var attributeValue;

        if (_type === BOOLEAN || _type === OVERLOADED_BOOLEAN && value === true) {
          // If attribute type is boolean, we know for sure it won't be an execution sink
          // and we won't require Trusted Type here.
          attributeValue = '';
        } else {
          // `setAttribute` with objects becomes only `[object]` in IE8/9,
          // ('' + value) makes it output the correct toString()-value.
          attributeValue = toStringOrTrustedType(value);

          if (propertyInfo.sanitizeURL) {
            sanitizeURL(attributeValue.toString());
          }
        }

        if (attributeNamespace) {
          setAttributeNS(node, attributeNamespace, attributeName, attributeValue);
        } else {
          setAttribute(node, attributeName, attributeValue);
        }
      }
    }

    var ReactDebugCurrentFrame$2 = null;
    var ReactControlledValuePropTypes = {
      checkPropTypes: null
    };
    {
      ReactDebugCurrentFrame$2 = ReactSharedInternals.ReactDebugCurrentFrame;
      var hasReadOnlyValue = {
        button: true,
        checkbox: true,
        image: true,
        hidden: true,
        radio: true,
        reset: true,
        submit: true
      };
      var propTypes = {
        value: function (props, propName, componentName) {
          if (hasReadOnlyValue[props.type] || props.onChange || props.readOnly || props.disabled || props[propName] == null || enableFlareAPI && props.listeners) {
            return null;
          }

          return new Error('You provided a `value` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultValue`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
        },
        checked: function (props, propName, componentName) {
          if (props.onChange || props.readOnly || props.disabled || props[propName] == null || enableFlareAPI && props.listeners) {
            return null;
          }

          return new Error('You provided a `checked` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultChecked`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
        }
      };
      /**
       * Provide a linked `value` attribute for controlled forms. You should not use
       * this outside of the ReactDOM controlled form components.
       */

      ReactControlledValuePropTypes.checkPropTypes = function (tagName, props) {
        checkPropTypes(propTypes, props, 'prop', tagName, ReactDebugCurrentFrame$2.getStackAddendum);
      };
    }

    function isCheckable(elem) {
      var type = elem.type;
      var nodeName = elem.nodeName;
      return nodeName && nodeName.toLowerCase() === 'input' && (type === 'checkbox' || type === 'radio');
    }

    function getTracker(node) {
      return node._valueTracker;
    }

    function detachTracker(node) {
      node._valueTracker = null;
    }

    function getValueFromNode(node) {
      var value = '';

      if (!node) {
        return value;
      }

      if (isCheckable(node)) {
        value = node.checked ? 'true' : 'false';
      } else {
        value = node.value;
      }

      return value;
    }

    function trackValueOnNode(node) {
      var valueField = isCheckable(node) ? 'checked' : 'value';
      var descriptor = Object.getOwnPropertyDescriptor(node.constructor.prototype, valueField);
      var currentValue = '' + node[valueField]; // if someone has already defined a value or Safari, then bail
      // and don't track value will cause over reporting of changes,
      // but it's better then a hard failure
      // (needed for certain tests that spyOn input values and Safari)

      if (node.hasOwnProperty(valueField) || typeof descriptor === 'undefined' || typeof descriptor.get !== 'function' || typeof descriptor.set !== 'function') {
        return;
      }

      var get = descriptor.get,
          set = descriptor.set;
      Object.defineProperty(node, valueField, {
        configurable: true,
        get: function () {
          return get.call(this);
        },
        set: function (value) {
          currentValue = '' + value;
          set.call(this, value);
        }
      }); // We could've passed this the first time
      // but it triggers a bug in IE11 and Edge 14/15.
      // Calling defineProperty() again should be equivalent.
      // https://github.com/facebook/react/issues/11768

      Object.defineProperty(node, valueField, {
        enumerable: descriptor.enumerable
      });
      var tracker = {
        getValue: function () {
          return currentValue;
        },
        setValue: function (value) {
          currentValue = '' + value;
        },
        stopTracking: function () {
          detachTracker(node);
          delete node[valueField];
        }
      };
      return tracker;
    }

    function track(node) {
      if (getTracker(node)) {
        return;
      } // TODO: Once it's just Fiber we can move this to node._wrapperState


      node._valueTracker = trackValueOnNode(node);
    }

    function updateValueIfChanged(node) {
      if (!node) {
        return false;
      }

      var tracker = getTracker(node); // if there is no tracker at this point it's unlikely
      // that trying again will succeed

      if (!tracker) {
        return true;
      }

      var lastValue = tracker.getValue();
      var nextValue = getValueFromNode(node);

      if (nextValue !== lastValue) {
        tracker.setValue(nextValue);
        return true;
      }

      return false;
    } // TODO: direct imports like some-package/src/* are bad. Fix me.


    var didWarnValueDefaultValue = false;
    var didWarnCheckedDefaultChecked = false;
    var didWarnControlledToUncontrolled = false;
    var didWarnUncontrolledToControlled = false;

    function isControlled(props) {
      var usesChecked = props.type === 'checkbox' || props.type === 'radio';
      return usesChecked ? props.checked != null : props.value != null;
    }
    /**
     * Implements an <input> host component that allows setting these optional
     * props: `checked`, `value`, `defaultChecked`, and `defaultValue`.
     *
     * If `checked` or `value` are not supplied (or null/undefined), user actions
     * that affect the checked state or value will trigger updates to the element.
     *
     * If they are supplied (and not null/undefined), the rendered element will not
     * trigger updates to the element. Instead, the props must change in order for
     * the rendered element to be updated.
     *
     * The rendered element will be initialized as unchecked (or `defaultChecked`)
     * with an empty value (or `defaultValue`).
     *
     * See http://www.w3.org/TR/2012/WD-html5-20121025/the-input-element.html
     */


    function getHostProps(element, props) {
      var node = element;
      var checked = props.checked;

      var hostProps = _assign({}, props, {
        defaultChecked: undefined,
        defaultValue: undefined,
        value: undefined,
        checked: checked != null ? checked : node._wrapperState.initialChecked
      });

      return hostProps;
    }

    function initWrapperState(element, props) {
      {
        ReactControlledValuePropTypes.checkPropTypes('input', props);

        if (props.checked !== undefined && props.defaultChecked !== undefined && !didWarnCheckedDefaultChecked) {
          warning$1(false, '%s contains an input of type %s with both checked and defaultChecked props. ' + 'Input elements must be either controlled or uncontrolled ' + '(specify either the checked prop, or the defaultChecked prop, but not ' + 'both). Decide between using a controlled or uncontrolled input ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components', getCurrentFiberOwnerNameInDevOrNull() || 'A component', props.type);
          didWarnCheckedDefaultChecked = true;
        }

        if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValueDefaultValue) {
          warning$1(false, '%s contains an input of type %s with both value and defaultValue props. ' + 'Input elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled input ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components', getCurrentFiberOwnerNameInDevOrNull() || 'A component', props.type);
          didWarnValueDefaultValue = true;
        }
      }
      var node = element;
      var defaultValue = props.defaultValue == null ? '' : props.defaultValue;
      node._wrapperState = {
        initialChecked: props.checked != null ? props.checked : props.defaultChecked,
        initialValue: getToStringValue(props.value != null ? props.value : defaultValue),
        controlled: isControlled(props)
      };
    }

    function updateChecked(element, props) {
      var node = element;
      var checked = props.checked;

      if (checked != null) {
        setValueForProperty(node, 'checked', checked, false);
      }
    }

    function updateWrapper(element, props) {
      var node = element;
      {
        var controlled = isControlled(props);

        if (!node._wrapperState.controlled && controlled && !didWarnUncontrolledToControlled) {
          warning$1(false, 'A component is changing an uncontrolled input of type %s to be controlled. ' + 'Input elements should not switch from uncontrolled to controlled (or vice versa). ' + 'Decide between using a controlled or uncontrolled input ' + 'element for the lifetime of the component. More info: https://fb.me/react-controlled-components', props.type);
          didWarnUncontrolledToControlled = true;
        }

        if (node._wrapperState.controlled && !controlled && !didWarnControlledToUncontrolled) {
          warning$1(false, 'A component is changing a controlled input of type %s to be uncontrolled. ' + 'Input elements should not switch from controlled to uncontrolled (or vice versa). ' + 'Decide between using a controlled or uncontrolled input ' + 'element for the lifetime of the component. More info: https://fb.me/react-controlled-components', props.type);
          didWarnControlledToUncontrolled = true;
        }
      }
      updateChecked(element, props);
      var value = getToStringValue(props.value);
      var type = props.type;

      if (value != null) {
        if (type === 'number') {
          if (value === 0 && node.value === '' || // We explicitly want to coerce to number here if possible.
          // eslint-disable-next-line
          node.value != value) {
            node.value = toString(value);
          }
        } else if (node.value !== toString(value)) {
          node.value = toString(value);
        }
      } else if (type === 'submit' || type === 'reset') {
        // Submit/reset inputs need the attribute removed completely to avoid
        // blank-text buttons.
        node.removeAttribute('value');
        return;
      }

      if (disableInputAttributeSyncing) {
        // When not syncing the value attribute, React only assigns a new value
        // whenever the defaultValue React prop has changed. When not present,
        // React does nothing
        if (props.hasOwnProperty('defaultValue')) {
          setDefaultValue(node, props.type, getToStringValue(props.defaultValue));
        }
      } else {
        // When syncing the value attribute, the value comes from a cascade of
        // properties:
        //  1. The value React property
        //  2. The defaultValue React property
        //  3. Otherwise there should be no change
        if (props.hasOwnProperty('value')) {
          setDefaultValue(node, props.type, value);
        } else if (props.hasOwnProperty('defaultValue')) {
          setDefaultValue(node, props.type, getToStringValue(props.defaultValue));
        }
      }

      if (disableInputAttributeSyncing) {
        // When not syncing the checked attribute, the attribute is directly
        // controllable from the defaultValue React property. It needs to be
        // updated as new props come in.
        if (props.defaultChecked == null) {
          node.removeAttribute('checked');
        } else {
          node.defaultChecked = !!props.defaultChecked;
        }
      } else {
        // When syncing the checked attribute, it only changes when it needs
        // to be removed, such as transitioning from a checkbox into a text input
        if (props.checked == null && props.defaultChecked != null) {
          node.defaultChecked = !!props.defaultChecked;
        }
      }
    }

    function postMountWrapper(element, props, isHydrating) {
      var node = element; // Do not assign value if it is already set. This prevents user text input
      // from being lost during SSR hydration.

      if (props.hasOwnProperty('value') || props.hasOwnProperty('defaultValue')) {
        var type = props.type;
        var isButton = type === 'submit' || type === 'reset'; // Avoid setting value attribute on submit/reset inputs as it overrides the
        // default value provided by the browser. See: #12872

        if (isButton && (props.value === undefined || props.value === null)) {
          return;
        }

        var initialValue = toString(node._wrapperState.initialValue); // Do not assign value if it is already set. This prevents user text input
        // from being lost during SSR hydration.

        if (!isHydrating) {
          if (disableInputAttributeSyncing) {
            var value = getToStringValue(props.value); // When not syncing the value attribute, the value property points
            // directly to the React prop. Only assign it if it exists.

            if (value != null) {
              // Always assign on buttons so that it is possible to assign an
              // empty string to clear button text.
              //
              // Otherwise, do not re-assign the value property if is empty. This
              // potentially avoids a DOM write and prevents Firefox (~60.0.1) from
              // prematurely marking required inputs as invalid. Equality is compared
              // to the current value in case the browser provided value is not an
              // empty string.
              if (isButton || value !== node.value) {
                node.value = toString(value);
              }
            }
          } else {
            // When syncing the value attribute, the value property should use
            // the wrapperState._initialValue property. This uses:
            //
            //   1. The value React property when present
            //   2. The defaultValue React property when present
            //   3. An empty string
            if (initialValue !== node.value) {
              node.value = initialValue;
            }
          }
        }

        if (disableInputAttributeSyncing) {
          // When not syncing the value attribute, assign the value attribute
          // directly from the defaultValue React property (when present)
          var defaultValue = getToStringValue(props.defaultValue);

          if (defaultValue != null) {
            node.defaultValue = toString(defaultValue);
          }
        } else {
          // Otherwise, the value attribute is synchronized to the property,
          // so we assign defaultValue to the same thing as the value property
          // assignment step above.
          node.defaultValue = initialValue;
        }
      } // Normally, we'd just do `node.checked = node.checked` upon initial mount, less this bug
      // this is needed to work around a chrome bug where setting defaultChecked
      // will sometimes influence the value of checked (even after detachment).
      // Reference: https://bugs.chromium.org/p/chromium/issues/detail?id=608416
      // We need to temporarily unset name to avoid disrupting radio button groups.


      var name = node.name;

      if (name !== '') {
        node.name = '';
      }

      if (disableInputAttributeSyncing) {
        // When not syncing the checked attribute, the checked property
        // never gets assigned. It must be manually set. We don't want
        // to do this when hydrating so that existing user input isn't
        // modified
        if (!isHydrating) {
          updateChecked(element, props);
        } // Only assign the checked attribute if it is defined. This saves
        // a DOM write when controlling the checked attribute isn't needed
        // (text inputs, submit/reset)


        if (props.hasOwnProperty('defaultChecked')) {
          node.defaultChecked = !node.defaultChecked;
          node.defaultChecked = !!props.defaultChecked;
        }
      } else {
        // When syncing the checked attribute, both the checked property and
        // attribute are assigned at the same time using defaultChecked. This uses:
        //
        //   1. The checked React property when present
        //   2. The defaultChecked React property when present
        //   3. Otherwise, false
        node.defaultChecked = !node.defaultChecked;
        node.defaultChecked = !!node._wrapperState.initialChecked;
      }

      if (name !== '') {
        node.name = name;
      }
    }

    function restoreControlledState$1(element, props) {
      var node = element;
      updateWrapper(node, props);
      updateNamedCousins(node, props);
    }

    function updateNamedCousins(rootNode, props) {
      var name = props.name;

      if (props.type === 'radio' && name != null) {
        var queryRoot = rootNode;

        while (queryRoot.parentNode) {
          queryRoot = queryRoot.parentNode;
        } // If `rootNode.form` was non-null, then we could try `form.elements`,
        // but that sometimes behaves strangely in IE8. We could also try using
        // `form.getElementsByName`, but that will only return direct children
        // and won't include inputs that use the HTML5 `form=` attribute. Since
        // the input might not even be in a form. It might not even be in the
        // document. Let's just use the local `querySelectorAll` to ensure we don't
        // miss anything.


        var group = queryRoot.querySelectorAll('input[name=' + JSON.stringify('' + name) + '][type="radio"]');

        for (var i = 0; i < group.length; i++) {
          var otherNode = group[i];

          if (otherNode === rootNode || otherNode.form !== rootNode.form) {
            continue;
          } // This will throw if radio buttons rendered by different copies of React
          // and the same name are rendered into the same form (same as #1939).
          // That's probably okay; we don't support it just as we don't support
          // mixing React radio buttons with non-React ones.


          var otherProps = getFiberCurrentPropsFromNode$1(otherNode);

          (function () {
            if (!otherProps) {
              {
                throw ReactError(Error("ReactDOMInput: Mixing React and non-React radio inputs with the same `name` is not supported."));
              }
            }
          })(); // We need update the tracked value on the named cousin since the value
          // was changed but the input saw no event or value set


          updateValueIfChanged(otherNode); // If this is a controlled radio button group, forcing the input that
          // was previously checked to update will cause it to be come re-checked
          // as appropriate.

          updateWrapper(otherNode, otherProps);
        }
      }
    } // In Chrome, assigning defaultValue to certain input types triggers input validation.
    // For number inputs, the display value loses trailing decimal points. For email inputs,
    // Chrome raises "The specified value <x> is not a valid email address".
    //
    // Here we check to see if the defaultValue has actually changed, avoiding these problems
    // when the user is inputting text
    //
    // https://github.com/facebook/react/issues/7253


    function setDefaultValue(node, type, value) {
      if ( // Focused number inputs synchronize on blur. See ChangeEventPlugin.js
      type !== 'number' || node.ownerDocument.activeElement !== node) {
        if (value == null) {
          node.defaultValue = toString(node._wrapperState.initialValue);
        } else if (node.defaultValue !== toString(value)) {
          node.defaultValue = toString(value);
        }
      }
    }

    var didWarnSelectedSetOnOption = false;
    var didWarnInvalidChild = false;

    function flattenChildren(children) {
      var content = ''; // Flatten children. We'll warn if they are invalid
      // during validateProps() which runs for hydration too.
      // Note that this would throw on non-element objects.
      // Elements are stringified (which is normally irrelevant
      // but matters for <fbt>).

      React.Children.forEach(children, function (child) {
        if (child == null) {
          return;
        }

        content += child; // Note: we don't warn about invalid children here.
        // Instead, this is done separately below so that
        // it happens during the hydration codepath too.
      });
      return content;
    }
    /**
     * Implements an <option> host component that warns when `selected` is set.
     */


    function validateProps(element, props) {
      {
        // This mirrors the codepath above, but runs for hydration too.
        // Warn about invalid children here so that client and hydration are consistent.
        // TODO: this seems like it could cause a DEV-only throw for hydration
        // if children contains a non-element object. We should try to avoid that.
        if (typeof props.children === 'object' && props.children !== null) {
          React.Children.forEach(props.children, function (child) {
            if (child == null) {
              return;
            }

            if (typeof child === 'string' || typeof child === 'number') {
              return;
            }

            if (typeof child.type !== 'string') {
              return;
            }

            if (!didWarnInvalidChild) {
              didWarnInvalidChild = true;
              warning$1(false, 'Only strings and numbers are supported as <option> children.');
            }
          });
        } // TODO: Remove support for `selected` in <option>.


        if (props.selected != null && !didWarnSelectedSetOnOption) {
          warning$1(false, 'Use the `defaultValue` or `value` props on <select> instead of ' + 'setting `selected` on <option>.');
          didWarnSelectedSetOnOption = true;
        }
      }
    }

    function postMountWrapper$1(element, props) {
      // value="" should make a value attribute (#6219)
      if (props.value != null) {
        element.setAttribute('value', toString(getToStringValue(props.value)));
      }
    }

    function getHostProps$1(element, props) {
      var hostProps = _assign({
        children: undefined
      }, props);

      var content = flattenChildren(props.children);

      if (content) {
        hostProps.children = content;
      }

      return hostProps;
    } // TODO: direct imports like some-package/src/* are bad. Fix me.


    var didWarnValueDefaultValue$1;
    {
      didWarnValueDefaultValue$1 = false;
    }

    function getDeclarationErrorAddendum() {
      var ownerName = getCurrentFiberOwnerNameInDevOrNull();

      if (ownerName) {
        return '\n\nCheck the render method of `' + ownerName + '`.';
      }

      return '';
    }

    var valuePropNames = ['value', 'defaultValue'];
    /**
     * Validation function for `value` and `defaultValue`.
     */

    function checkSelectPropTypes(props) {
      ReactControlledValuePropTypes.checkPropTypes('select', props);

      for (var i = 0; i < valuePropNames.length; i++) {
        var propName = valuePropNames[i];

        if (props[propName] == null) {
          continue;
        }

        var isArray = Array.isArray(props[propName]);

        if (props.multiple && !isArray) {
          warning$1(false, 'The `%s` prop supplied to <select> must be an array if ' + '`multiple` is true.%s', propName, getDeclarationErrorAddendum());
        } else if (!props.multiple && isArray) {
          warning$1(false, 'The `%s` prop supplied to <select> must be a scalar ' + 'value if `multiple` is false.%s', propName, getDeclarationErrorAddendum());
        }
      }
    }

    function updateOptions(node, multiple, propValue, setDefaultSelected) {
      var options = node.options;

      if (multiple) {
        var selectedValues = propValue;
        var selectedValue = {};

        for (var i = 0; i < selectedValues.length; i++) {
          // Prefix to avoid chaos with special keys.
          selectedValue['$' + selectedValues[i]] = true;
        }

        for (var _i = 0; _i < options.length; _i++) {
          var selected = selectedValue.hasOwnProperty('$' + options[_i].value);

          if (options[_i].selected !== selected) {
            options[_i].selected = selected;
          }

          if (selected && setDefaultSelected) {
            options[_i].defaultSelected = true;
          }
        }
      } else {
        // Do not set `select.value` as exact behavior isn't consistent across all
        // browsers for all cases.
        var _selectedValue = toString(getToStringValue(propValue));

        var defaultSelected = null;

        for (var _i2 = 0; _i2 < options.length; _i2++) {
          if (options[_i2].value === _selectedValue) {
            options[_i2].selected = true;

            if (setDefaultSelected) {
              options[_i2].defaultSelected = true;
            }

            return;
          }

          if (defaultSelected === null && !options[_i2].disabled) {
            defaultSelected = options[_i2];
          }
        }

        if (defaultSelected !== null) {
          defaultSelected.selected = true;
        }
      }
    }
    /**
     * Implements a <select> host component that allows optionally setting the
     * props `value` and `defaultValue`. If `multiple` is false, the prop must be a
     * stringable. If `multiple` is true, the prop must be an array of stringables.
     *
     * If `value` is not supplied (or null/undefined), user actions that change the
     * selected option will trigger updates to the rendered options.
     *
     * If it is supplied (and not null/undefined), the rendered options will not
     * update in response to user actions. Instead, the `value` prop must change in
     * order for the rendered options to update.
     *
     * If `defaultValue` is provided, any options with the supplied values will be
     * selected.
     */


    function getHostProps$2(element, props) {
      return _assign({}, props, {
        value: undefined
      });
    }

    function initWrapperState$1(element, props) {
      var node = element;
      {
        checkSelectPropTypes(props);
      }
      node._wrapperState = {
        wasMultiple: !!props.multiple
      };
      {
        if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValueDefaultValue$1) {
          warning$1(false, 'Select elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled select ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components');
          didWarnValueDefaultValue$1 = true;
        }
      }
    }

    function postMountWrapper$2(element, props) {
      var node = element;
      node.multiple = !!props.multiple;
      var value = props.value;

      if (value != null) {
        updateOptions(node, !!props.multiple, value, false);
      } else if (props.defaultValue != null) {
        updateOptions(node, !!props.multiple, props.defaultValue, true);
      }
    }

    function postUpdateWrapper(element, props) {
      var node = element;
      var wasMultiple = node._wrapperState.wasMultiple;
      node._wrapperState.wasMultiple = !!props.multiple;
      var value = props.value;

      if (value != null) {
        updateOptions(node, !!props.multiple, value, false);
      } else if (wasMultiple !== !!props.multiple) {
        // For simplicity, reapply `defaultValue` if `multiple` is toggled.
        if (props.defaultValue != null) {
          updateOptions(node, !!props.multiple, props.defaultValue, true);
        } else {
          // Revert the select back to its default unselected state.
          updateOptions(node, !!props.multiple, props.multiple ? [] : '', false);
        }
      }
    }

    function restoreControlledState$2(element, props) {
      var node = element;
      var value = props.value;

      if (value != null) {
        updateOptions(node, !!props.multiple, value, false);
      }
    }

    var didWarnValDefaultVal = false;
    /**
     * Implements a <textarea> host component that allows setting `value`, and
     * `defaultValue`. This differs from the traditional DOM API because value is
     * usually set as PCDATA children.
     *
     * If `value` is not supplied (or null/undefined), user actions that affect the
     * value will trigger updates to the element.
     *
     * If `value` is supplied (and not null/undefined), the rendered element will
     * not trigger updates to the element. Instead, the `value` prop must change in
     * order for the rendered element to be updated.
     *
     * The rendered element will be initialized with an empty value, the prop
     * `defaultValue` if specified, or the children content (deprecated).
     */

    function getHostProps$3(element, props) {
      var node = element;

      (function () {
        if (!(props.dangerouslySetInnerHTML == null)) {
          {
            throw ReactError(Error("`dangerouslySetInnerHTML` does not make sense on <textarea>."));
          }
        }
      })(); // Always set children to the same thing. In IE9, the selection range will
      // get reset if `textContent` is mutated.  We could add a check in setTextContent
      // to only set the value if/when the value differs from the node value (which would
      // completely solve this IE9 bug), but Sebastian+Sophie seemed to like this
      // solution. The value can be a boolean or object so that's why it's forced
      // to be a string.


      var hostProps = _assign({}, props, {
        value: undefined,
        defaultValue: undefined,
        children: toString(node._wrapperState.initialValue)
      });

      return hostProps;
    }

    function initWrapperState$2(element, props) {
      var node = element;
      {
        ReactControlledValuePropTypes.checkPropTypes('textarea', props);

        if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValDefaultVal) {
          warning$1(false, '%s contains a textarea with both value and defaultValue props. ' + 'Textarea elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled textarea ' + 'and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components', getCurrentFiberOwnerNameInDevOrNull() || 'A component');
          didWarnValDefaultVal = true;
        }
      }
      var initialValue = props.value; // Only bother fetching default value if we're going to use it

      if (initialValue == null) {
        var defaultValue = props.defaultValue; // TODO (yungsters): Remove support for children content in <textarea>.

        var children = props.children;

        if (children != null) {
          {
            warning$1(false, 'Use the `defaultValue` or `value` props instead of setting ' + 'children on <textarea>.');
          }

          (function () {
            if (!(defaultValue == null)) {
              {
                throw ReactError(Error("If you supply `defaultValue` on a <textarea>, do not pass children."));
              }
            }
          })();

          if (Array.isArray(children)) {
            (function () {
              if (!(children.length <= 1)) {
                {
                  throw ReactError(Error("<textarea> can only have at most one child."));
                }
              }
            })();

            children = children[0];
          }

          defaultValue = children;
        }

        if (defaultValue == null) {
          defaultValue = '';
        }

        initialValue = defaultValue;
      }

      node._wrapperState = {
        initialValue: getToStringValue(initialValue)
      };
    }

    function updateWrapper$1(element, props) {
      var node = element;
      var value = getToStringValue(props.value);
      var defaultValue = getToStringValue(props.defaultValue);

      if (value != null) {
        // Cast `value` to a string to ensure the value is set correctly. While
        // browsers typically do this as necessary, jsdom doesn't.
        var newValue = toString(value); // To avoid side effects (such as losing text selection), only set value if changed

        if (newValue !== node.value) {
          node.value = newValue;
        }

        if (props.defaultValue == null && node.defaultValue !== newValue) {
          node.defaultValue = newValue;
        }
      }

      if (defaultValue != null) {
        node.defaultValue = toString(defaultValue);
      }
    }

    function postMountWrapper$3(element, props) {
      var node = element; // This is in postMount because we need access to the DOM node, which is not
      // available until after the component has mounted.

      var textContent = node.textContent; // Only set node.value if textContent is equal to the expected
      // initial value. In IE10/IE11 there is a bug where the placeholder attribute
      // will populate textContent as well.
      // https://developer.microsoft.com/microsoft-edge/platform/issues/101525/

      if (textContent === node._wrapperState.initialValue) {
        if (textContent !== '' && textContent !== null) {
          node.value = textContent;
        }
      }
    }

    function restoreControlledState$3(element, props) {
      // DOM component is still mounted; update
      updateWrapper$1(element, props);
    }

    var HTML_NAMESPACE$1 = 'http://www.w3.org/1999/xhtml';
    var MATH_NAMESPACE = 'http://www.w3.org/1998/Math/MathML';
    var SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
    var Namespaces = {
      html: HTML_NAMESPACE$1,
      mathml: MATH_NAMESPACE,
      svg: SVG_NAMESPACE
    }; // Assumes there is no parent namespace.

    function getIntrinsicNamespace(type) {
      switch (type) {
        case 'svg':
          return SVG_NAMESPACE;

        case 'math':
          return MATH_NAMESPACE;

        default:
          return HTML_NAMESPACE$1;
      }
    }

    function getChildNamespace(parentNamespace, type) {
      if (parentNamespace == null || parentNamespace === HTML_NAMESPACE$1) {
        // No (or default) parent namespace: potential entry point.
        return getIntrinsicNamespace(type);
      }

      if (parentNamespace === SVG_NAMESPACE && type === 'foreignObject') {
        // We're leaving SVG.
        return HTML_NAMESPACE$1;
      } // By default, pass namespace below.


      return parentNamespace;
    }
    /* globals MSApp */

    /**
     * Create a function which has 'unsafe' privileges (required by windows8 apps)
     */


    var createMicrosoftUnsafeLocalFunction = function (func) {
      if (typeof MSApp !== 'undefined' && MSApp.execUnsafeLocalFunction) {
        return function (arg0, arg1, arg2, arg3) {
          MSApp.execUnsafeLocalFunction(function () {
            return func(arg0, arg1, arg2, arg3);
          });
        };
      } else {
        return func;
      }
    };

    var reusableSVGContainer;
    /**
     * Set the innerHTML property of a node
     *
     * @param {DOMElement} node
     * @param {string} html
     * @internal
     */

    var setInnerHTML = createMicrosoftUnsafeLocalFunction(function (node, html) {
      if (node.namespaceURI === Namespaces.svg) {
        {
          if (enableTrustedTypesIntegration) {
            // TODO: reconsider the text of this warning and when it should show
            // before enabling the feature flag.
            !(typeof trustedTypes === 'undefined') ? warning$1(false, "Using 'dangerouslySetInnerHTML' in an svg element with " + 'Trusted Types enabled in an Internet Explorer will cause ' + 'the trusted value to be converted to string. Assigning string ' + "to 'innerHTML' will throw an error if Trusted Types are enforced. " + "You can try to wrap your svg element inside a div and use 'dangerouslySetInnerHTML' " + 'on the enclosing div instead.') : void 0;
          }
        }

        if (!('innerHTML' in node)) {
          // IE does not have innerHTML for SVG nodes, so instead we inject the
          // new markup in a temp node and then move the child nodes across into
          // the target node
          reusableSVGContainer = reusableSVGContainer || document.createElement('div');
          reusableSVGContainer.innerHTML = '<svg>' + html.valueOf().toString() + '</svg>';
          var svgNode = reusableSVGContainer.firstChild;

          while (node.firstChild) {
            node.removeChild(node.firstChild);
          }

          while (svgNode.firstChild) {
            node.appendChild(svgNode.firstChild);
          }

          return;
        }
      }

      node.innerHTML = html;
    });
    /**
     * HTML nodeType values that represent the type of the node
     */

    var ELEMENT_NODE = 1;
    var TEXT_NODE = 3;
    var COMMENT_NODE = 8;
    var DOCUMENT_NODE = 9;
    var DOCUMENT_FRAGMENT_NODE = 11;
    /**
     * Set the textContent property of a node. For text updates, it's faster
     * to set the `nodeValue` of the Text node directly instead of using
     * `.textContent` which will remove the existing node and create a new one.
     *
     * @param {DOMElement} node
     * @param {string} text
     * @internal
     */

    var setTextContent = function (node, text) {
      if (text) {
        var firstChild = node.firstChild;

        if (firstChild && firstChild === node.lastChild && firstChild.nodeType === TEXT_NODE) {
          firstChild.nodeValue = text;
          return;
        }
      }

      node.textContent = text;
    }; // Do not use the below two methods directly!
    // Instead use constants exported from DOMTopLevelEventTypes in ReactDOM.
    // (It is the only module that is allowed to access these methods.)


    function unsafeCastStringToDOMTopLevelType(topLevelType) {
      return topLevelType;
    }

    function unsafeCastDOMTopLevelTypeToString(topLevelType) {
      return topLevelType;
    }
    /**
     * Generate a mapping of standard vendor prefixes using the defined style property and event name.
     *
     * @param {string} styleProp
     * @param {string} eventName
     * @returns {object}
     */


    function makePrefixMap(styleProp, eventName) {
      var prefixes = {};
      prefixes[styleProp.toLowerCase()] = eventName.toLowerCase();
      prefixes['Webkit' + styleProp] = 'webkit' + eventName;
      prefixes['Moz' + styleProp] = 'moz' + eventName;
      return prefixes;
    }
    /**
     * A list of event names to a configurable list of vendor prefixes.
     */


    var vendorPrefixes = {
      animationend: makePrefixMap('Animation', 'AnimationEnd'),
      animationiteration: makePrefixMap('Animation', 'AnimationIteration'),
      animationstart: makePrefixMap('Animation', 'AnimationStart'),
      transitionend: makePrefixMap('Transition', 'TransitionEnd')
    };
    /**
     * Event names that have already been detected and prefixed (if applicable).
     */

    var prefixedEventNames = {};
    /**
     * Element to check for prefixes on.
     */

    var style = {};
    /**
     * Bootstrap if a DOM exists.
     */

    if (canUseDOM) {
      style = document.createElement('div').style; // On some platforms, in particular some releases of Android 4.x,
      // the un-prefixed "animation" and "transition" properties are defined on the
      // style object but the events that fire will still be prefixed, so we need
      // to check if the un-prefixed events are usable, and if not remove them from the map.

      if (!('AnimationEvent' in window)) {
        delete vendorPrefixes.animationend.animation;
        delete vendorPrefixes.animationiteration.animation;
        delete vendorPrefixes.animationstart.animation;
      } // Same as above


      if (!('TransitionEvent' in window)) {
        delete vendorPrefixes.transitionend.transition;
      }
    }
    /**
     * Attempts to determine the correct vendor prefixed event name.
     *
     * @param {string} eventName
     * @returns {string}
     */


    function getVendorPrefixedEventName(eventName) {
      if (prefixedEventNames[eventName]) {
        return prefixedEventNames[eventName];
      } else if (!vendorPrefixes[eventName]) {
        return eventName;
      }

      var prefixMap = vendorPrefixes[eventName];

      for (var styleProp in prefixMap) {
        if (prefixMap.hasOwnProperty(styleProp) && styleProp in style) {
          return prefixedEventNames[eventName] = prefixMap[styleProp];
        }
      }

      return eventName;
    }
    /**
     * To identify top level events in ReactDOM, we use constants defined by this
     * module. This is the only module that uses the unsafe* methods to express
     * that the constants actually correspond to the browser event names. This lets
     * us save some bundle size by avoiding a top level type -> event name map.
     * The rest of ReactDOM code should import top level types from this file.
     */


    var TOP_ABORT = unsafeCastStringToDOMTopLevelType('abort');
    var TOP_ANIMATION_END = unsafeCastStringToDOMTopLevelType(getVendorPrefixedEventName('animationend'));
    var TOP_ANIMATION_ITERATION = unsafeCastStringToDOMTopLevelType(getVendorPrefixedEventName('animationiteration'));
    var TOP_ANIMATION_START = unsafeCastStringToDOMTopLevelType(getVendorPrefixedEventName('animationstart'));
    var TOP_BLUR = unsafeCastStringToDOMTopLevelType('blur');
    var TOP_CAN_PLAY = unsafeCastStringToDOMTopLevelType('canplay');
    var TOP_CAN_PLAY_THROUGH = unsafeCastStringToDOMTopLevelType('canplaythrough');
    var TOP_CANCEL = unsafeCastStringToDOMTopLevelType('cancel');
    var TOP_CHANGE = unsafeCastStringToDOMTopLevelType('change');
    var TOP_CLICK = unsafeCastStringToDOMTopLevelType('click');
    var TOP_CLOSE = unsafeCastStringToDOMTopLevelType('close');
    var TOP_COMPOSITION_END = unsafeCastStringToDOMTopLevelType('compositionend');
    var TOP_COMPOSITION_START = unsafeCastStringToDOMTopLevelType('compositionstart');
    var TOP_COMPOSITION_UPDATE = unsafeCastStringToDOMTopLevelType('compositionupdate');
    var TOP_CONTEXT_MENU = unsafeCastStringToDOMTopLevelType('contextmenu');
    var TOP_COPY = unsafeCastStringToDOMTopLevelType('copy');
    var TOP_CUT = unsafeCastStringToDOMTopLevelType('cut');
    var TOP_DOUBLE_CLICK = unsafeCastStringToDOMTopLevelType('dblclick');
    var TOP_AUX_CLICK = unsafeCastStringToDOMTopLevelType('auxclick');
    var TOP_DRAG = unsafeCastStringToDOMTopLevelType('drag');
    var TOP_DRAG_END = unsafeCastStringToDOMTopLevelType('dragend');
    var TOP_DRAG_ENTER = unsafeCastStringToDOMTopLevelType('dragenter');
    var TOP_DRAG_EXIT = unsafeCastStringToDOMTopLevelType('dragexit');
    var TOP_DRAG_LEAVE = unsafeCastStringToDOMTopLevelType('dragleave');
    var TOP_DRAG_OVER = unsafeCastStringToDOMTopLevelType('dragover');
    var TOP_DRAG_START = unsafeCastStringToDOMTopLevelType('dragstart');
    var TOP_DROP = unsafeCastStringToDOMTopLevelType('drop');
    var TOP_DURATION_CHANGE = unsafeCastStringToDOMTopLevelType('durationchange');
    var TOP_EMPTIED = unsafeCastStringToDOMTopLevelType('emptied');
    var TOP_ENCRYPTED = unsafeCastStringToDOMTopLevelType('encrypted');
    var TOP_ENDED = unsafeCastStringToDOMTopLevelType('ended');
    var TOP_ERROR = unsafeCastStringToDOMTopLevelType('error');
    var TOP_FOCUS = unsafeCastStringToDOMTopLevelType('focus');
    var TOP_GOT_POINTER_CAPTURE = unsafeCastStringToDOMTopLevelType('gotpointercapture');
    var TOP_INPUT = unsafeCastStringToDOMTopLevelType('input');
    var TOP_INVALID = unsafeCastStringToDOMTopLevelType('invalid');
    var TOP_KEY_DOWN = unsafeCastStringToDOMTopLevelType('keydown');
    var TOP_KEY_PRESS = unsafeCastStringToDOMTopLevelType('keypress');
    var TOP_KEY_UP = unsafeCastStringToDOMTopLevelType('keyup');
    var TOP_LOAD = unsafeCastStringToDOMTopLevelType('load');
    var TOP_LOAD_START = unsafeCastStringToDOMTopLevelType('loadstart');
    var TOP_LOADED_DATA = unsafeCastStringToDOMTopLevelType('loadeddata');
    var TOP_LOADED_METADATA = unsafeCastStringToDOMTopLevelType('loadedmetadata');
    var TOP_LOST_POINTER_CAPTURE = unsafeCastStringToDOMTopLevelType('lostpointercapture');
    var TOP_MOUSE_DOWN = unsafeCastStringToDOMTopLevelType('mousedown');
    var TOP_MOUSE_MOVE = unsafeCastStringToDOMTopLevelType('mousemove');
    var TOP_MOUSE_OUT = unsafeCastStringToDOMTopLevelType('mouseout');
    var TOP_MOUSE_OVER = unsafeCastStringToDOMTopLevelType('mouseover');
    var TOP_MOUSE_UP = unsafeCastStringToDOMTopLevelType('mouseup');
    var TOP_PASTE = unsafeCastStringToDOMTopLevelType('paste');
    var TOP_PAUSE = unsafeCastStringToDOMTopLevelType('pause');
    var TOP_PLAY = unsafeCastStringToDOMTopLevelType('play');
    var TOP_PLAYING = unsafeCastStringToDOMTopLevelType('playing');
    var TOP_POINTER_CANCEL = unsafeCastStringToDOMTopLevelType('pointercancel');
    var TOP_POINTER_DOWN = unsafeCastStringToDOMTopLevelType('pointerdown');
    var TOP_POINTER_MOVE = unsafeCastStringToDOMTopLevelType('pointermove');
    var TOP_POINTER_OUT = unsafeCastStringToDOMTopLevelType('pointerout');
    var TOP_POINTER_OVER = unsafeCastStringToDOMTopLevelType('pointerover');
    var TOP_POINTER_UP = unsafeCastStringToDOMTopLevelType('pointerup');
    var TOP_PROGRESS = unsafeCastStringToDOMTopLevelType('progress');
    var TOP_RATE_CHANGE = unsafeCastStringToDOMTopLevelType('ratechange');
    var TOP_RESET = unsafeCastStringToDOMTopLevelType('reset');
    var TOP_SCROLL = unsafeCastStringToDOMTopLevelType('scroll');
    var TOP_SEEKED = unsafeCastStringToDOMTopLevelType('seeked');
    var TOP_SEEKING = unsafeCastStringToDOMTopLevelType('seeking');
    var TOP_SELECTION_CHANGE = unsafeCastStringToDOMTopLevelType('selectionchange');
    var TOP_STALLED = unsafeCastStringToDOMTopLevelType('stalled');
    var TOP_SUBMIT = unsafeCastStringToDOMTopLevelType('submit');
    var TOP_SUSPEND = unsafeCastStringToDOMTopLevelType('suspend');
    var TOP_TEXT_INPUT = unsafeCastStringToDOMTopLevelType('textInput');
    var TOP_TIME_UPDATE = unsafeCastStringToDOMTopLevelType('timeupdate');
    var TOP_TOGGLE = unsafeCastStringToDOMTopLevelType('toggle');
    var TOP_TOUCH_CANCEL = unsafeCastStringToDOMTopLevelType('touchcancel');
    var TOP_TOUCH_END = unsafeCastStringToDOMTopLevelType('touchend');
    var TOP_TOUCH_MOVE = unsafeCastStringToDOMTopLevelType('touchmove');
    var TOP_TOUCH_START = unsafeCastStringToDOMTopLevelType('touchstart');
    var TOP_TRANSITION_END = unsafeCastStringToDOMTopLevelType(getVendorPrefixedEventName('transitionend'));
    var TOP_VOLUME_CHANGE = unsafeCastStringToDOMTopLevelType('volumechange');
    var TOP_WAITING = unsafeCastStringToDOMTopLevelType('waiting');
    var TOP_WHEEL = unsafeCastStringToDOMTopLevelType('wheel'); // List of events that need to be individually attached to media elements.
    // Note that events in this list will *not* be listened to at the top level
    // unless they're explicitly whitelisted in `ReactBrowserEventEmitter.listenTo`.

    var mediaEventTypes = [TOP_ABORT, TOP_CAN_PLAY, TOP_CAN_PLAY_THROUGH, TOP_DURATION_CHANGE, TOP_EMPTIED, TOP_ENCRYPTED, TOP_ENDED, TOP_ERROR, TOP_LOADED_DATA, TOP_LOADED_METADATA, TOP_LOAD_START, TOP_PAUSE, TOP_PLAY, TOP_PLAYING, TOP_PROGRESS, TOP_RATE_CHANGE, TOP_SEEKED, TOP_SEEKING, TOP_STALLED, TOP_SUSPEND, TOP_TIME_UPDATE, TOP_VOLUME_CHANGE, TOP_WAITING];

    function getRawEventName(topLevelType) {
      return unsafeCastDOMTopLevelTypeToString(topLevelType);
    }

    var attemptSynchronousHydration;

    function setAttemptSynchronousHydration(fn) {
      attemptSynchronousHydration = fn;
    } // TODO: Upgrade this definition once we're on a newer version of Flow that
    // has this definition built-in.


    var hasScheduledReplayAttempt = false; // The queue of discrete events to be replayed.

    var queuedDiscreteEvents = []; // Indicates if any continuous event targets are non-null for early bailout.
    // if the last target was dehydrated.

    var queuedFocus = null;
    var queuedDrag = null;
    var queuedMouse = null; // For pointer events there can be one latest event per pointerId.

    var queuedPointers = new Map();
    var queuedPointerCaptures = new Map(); // We could consider replaying selectionchange and touchmoves too.

    function hasQueuedDiscreteEvents() {
      return queuedDiscreteEvents.length > 0;
    }

    var discreteReplayableEvents = [TOP_MOUSE_DOWN, TOP_MOUSE_UP, TOP_TOUCH_CANCEL, TOP_TOUCH_END, TOP_TOUCH_START, TOP_AUX_CLICK, TOP_DOUBLE_CLICK, TOP_POINTER_CANCEL, TOP_POINTER_DOWN, TOP_POINTER_UP, TOP_DRAG_END, TOP_DRAG_START, TOP_DROP, TOP_COMPOSITION_END, TOP_COMPOSITION_START, TOP_KEY_DOWN, TOP_KEY_PRESS, TOP_KEY_UP, TOP_INPUT, TOP_TEXT_INPUT, TOP_CLOSE, TOP_CANCEL, TOP_COPY, TOP_CUT, TOP_PASTE, TOP_CLICK, TOP_CHANGE, TOP_CONTEXT_MENU, TOP_RESET, TOP_SUBMIT];
    var continuousReplayableEvents = [TOP_FOCUS, TOP_BLUR, TOP_DRAG_ENTER, TOP_DRAG_LEAVE, TOP_MOUSE_OVER, TOP_MOUSE_OUT, TOP_POINTER_OVER, TOP_POINTER_OUT, TOP_GOT_POINTER_CAPTURE, TOP_LOST_POINTER_CAPTURE];

    function isReplayableDiscreteEvent(eventType) {
      return discreteReplayableEvents.indexOf(eventType) > -1;
    }

    function trapReplayableEvent(topLevelType, document, listeningSet) {
      listenToTopLevel(topLevelType, document, listeningSet);

      if (enableFlareAPI) {
        // Trap events for the responder system.
        var passiveEventKey = unsafeCastDOMTopLevelTypeToString(topLevelType) + '_passive';

        if (!listeningSet.has(passiveEventKey)) {
          trapEventForResponderEventSystem(document, topLevelType, true);
          listeningSet.add(passiveEventKey);
        } // TODO: This listens to all events as active which might have
        // undesirable effects. It's also unnecessary to have both
        // passive and active listeners. Instead, we could start with
        // a passive and upgrade it to an active one if needed.
        // For replaying purposes the active is never needed since we
        // currently don't preventDefault.


        var activeEventKey = unsafeCastDOMTopLevelTypeToString(topLevelType) + '_active';

        if (!listeningSet.has(activeEventKey)) {
          trapEventForResponderEventSystem(document, topLevelType, false);
          listeningSet.add(activeEventKey);
        }
      }
    }

    function eagerlyTrapReplayableEvents(document) {
      var listeningSet = getListeningSetForElement(document); // Discrete

      discreteReplayableEvents.forEach(function (topLevelType) {
        trapReplayableEvent(topLevelType, document, listeningSet);
      }); // Continuous

      continuousReplayableEvents.forEach(function (topLevelType) {
        trapReplayableEvent(topLevelType, document, listeningSet);
      });
    }

    function createQueuedReplayableEvent(blockedOn, topLevelType, eventSystemFlags, nativeEvent) {
      return {
        blockedOn: blockedOn,
        topLevelType: topLevelType,
        eventSystemFlags: eventSystemFlags | IS_REPLAYED,
        nativeEvent: nativeEvent
      };
    }

    function queueDiscreteEvent(blockedOn, topLevelType, eventSystemFlags, nativeEvent) {
      var queuedEvent = createQueuedReplayableEvent(blockedOn, topLevelType, eventSystemFlags, nativeEvent);
      queuedDiscreteEvents.push(queuedEvent);

      if (enableSelectiveHydration) {
        if (queuedDiscreteEvents.length === 1) {
          // If this was the first discrete event, we might be able to
          // synchronously unblock it so that preventDefault still works.
          while (queuedEvent.blockedOn !== null) {
            var _fiber = getInstanceFromNode$1(queuedEvent.blockedOn);

            if (_fiber === null) {
              break;
            }

            attemptSynchronousHydration(_fiber);

            if (queuedEvent.blockedOn === null) {
              // We got unblocked by hydration. Let's try again.
              replayUnblockedEvents(); // If we're reblocked, on an inner boundary, we might need
              // to attempt hydrating that one.

              continue;
            } else {
              // We're still blocked from hydation, we have to give up
              // and replay later.
              break;
            }
          }
        }
      }
    } // Resets the replaying for this type of continuous event to no event.


    function clearIfContinuousEvent(topLevelType, nativeEvent) {
      switch (topLevelType) {
        case TOP_FOCUS:
        case TOP_BLUR:
          queuedFocus = null;
          break;

        case TOP_DRAG_ENTER:
        case TOP_DRAG_LEAVE:
          queuedDrag = null;
          break;

        case TOP_MOUSE_OVER:
        case TOP_MOUSE_OUT:
          queuedMouse = null;
          break;

        case TOP_POINTER_OVER:
        case TOP_POINTER_OUT:
          {
            var pointerId = nativeEvent.pointerId;
            queuedPointers.delete(pointerId);
            break;
          }

        case TOP_GOT_POINTER_CAPTURE:
        case TOP_LOST_POINTER_CAPTURE:
          {
            var _pointerId = nativeEvent.pointerId;
            queuedPointerCaptures.delete(_pointerId);
            break;
          }
      }
    }

    function accumulateOrCreateQueuedReplayableEvent(existingQueuedEvent, blockedOn, topLevelType, eventSystemFlags, nativeEvent) {
      if (existingQueuedEvent === null || existingQueuedEvent.nativeEvent !== nativeEvent) {
        return createQueuedReplayableEvent(blockedOn, topLevelType, eventSystemFlags, nativeEvent);
      } // If we have already queued this exact event, then it's because
      // the different event systems have different DOM event listeners.
      // We can accumulate the flags and store a single event to be
      // replayed.


      existingQueuedEvent.eventSystemFlags |= eventSystemFlags;
      return existingQueuedEvent;
    }

    function queueIfContinuousEvent(blockedOn, topLevelType, eventSystemFlags, nativeEvent) {
      // These set relatedTarget to null because the replayed event will be treated as if we
      // moved from outside the window (no target) onto the target once it hydrates.
      // Instead of mutating we could clone the event.
      switch (topLevelType) {
        case TOP_FOCUS:
          {
            var focusEvent = nativeEvent;
            queuedFocus = accumulateOrCreateQueuedReplayableEvent(queuedFocus, blockedOn, topLevelType, eventSystemFlags, focusEvent);
            return true;
          }

        case TOP_DRAG_ENTER:
          {
            var dragEvent = nativeEvent;
            queuedDrag = accumulateOrCreateQueuedReplayableEvent(queuedDrag, blockedOn, topLevelType, eventSystemFlags, dragEvent);
            return true;
          }

        case TOP_MOUSE_OVER:
          {
            var mouseEvent = nativeEvent;
            queuedMouse = accumulateOrCreateQueuedReplayableEvent(queuedMouse, blockedOn, topLevelType, eventSystemFlags, mouseEvent);
            return true;
          }

        case TOP_POINTER_OVER:
          {
            var pointerEvent = nativeEvent;
            var pointerId = pointerEvent.pointerId;
            queuedPointers.set(pointerId, accumulateOrCreateQueuedReplayableEvent(queuedPointers.get(pointerId) || null, blockedOn, topLevelType, eventSystemFlags, pointerEvent));
            return true;
          }

        case TOP_GOT_POINTER_CAPTURE:
          {
            var _pointerEvent = nativeEvent;
            var _pointerId2 = _pointerEvent.pointerId;
            queuedPointerCaptures.set(_pointerId2, accumulateOrCreateQueuedReplayableEvent(queuedPointerCaptures.get(_pointerId2) || null, blockedOn, topLevelType, eventSystemFlags, _pointerEvent));
            return true;
          }
      }

      return false;
    }

    function attemptReplayQueuedEvent(queuedEvent) {
      if (queuedEvent.blockedOn !== null) {
        return false;
      }

      var nextBlockedOn = attemptToDispatchEvent(queuedEvent.topLevelType, queuedEvent.eventSystemFlags, queuedEvent.nativeEvent);

      if (nextBlockedOn !== null) {
        // We're still blocked. Try again later.
        queuedEvent.blockedOn = nextBlockedOn;
        return false;
      }

      return true;
    }

    function attemptReplayQueuedEventInMap(queuedEvent, key, map) {
      if (attemptReplayQueuedEvent(queuedEvent)) {
        map.delete(key);
      }
    }

    function replayUnblockedEvents() {
      hasScheduledReplayAttempt = false; // First replay discrete events.

      while (queuedDiscreteEvents.length > 0) {
        var nextDiscreteEvent = queuedDiscreteEvents[0];

        if (nextDiscreteEvent.blockedOn !== null) {
          // We're still blocked.
          break;
        }

        var nextBlockedOn = attemptToDispatchEvent(nextDiscreteEvent.topLevelType, nextDiscreteEvent.eventSystemFlags, nextDiscreteEvent.nativeEvent);

        if (nextBlockedOn !== null) {
          // We're still blocked. Try again later.
          nextDiscreteEvent.blockedOn = nextBlockedOn;
        } else {
          // We've successfully replayed the first event. Let's try the next one.
          queuedDiscreteEvents.shift();
        }
      } // Next replay any continuous events.


      if (queuedFocus !== null && attemptReplayQueuedEvent(queuedFocus)) {
        queuedFocus = null;
      }

      if (queuedDrag !== null && attemptReplayQueuedEvent(queuedDrag)) {
        queuedDrag = null;
      }

      if (queuedMouse !== null && attemptReplayQueuedEvent(queuedMouse)) {
        queuedMouse = null;
      }

      queuedPointers.forEach(attemptReplayQueuedEventInMap);
      queuedPointerCaptures.forEach(attemptReplayQueuedEventInMap);
    }

    function scheduleCallbackIfUnblocked(queuedEvent, unblocked) {
      if (queuedEvent.blockedOn === unblocked) {
        queuedEvent.blockedOn = null;

        if (!hasScheduledReplayAttempt) {
          hasScheduledReplayAttempt = true; // Schedule a callback to attempt replaying as many events as are
          // now unblocked. This first might not actually be unblocked yet.
          // We could check it early to avoid scheduling an unnecessary callback.

          Scheduler.unstable_scheduleCallback(Scheduler.unstable_NormalPriority, replayUnblockedEvents);
        }
      }
    }

    function retryIfBlockedOn(unblocked) {
      // Mark anything that was blocked on this as no longer blocked
      // and eligible for a replay.
      if (queuedDiscreteEvents.length > 0) {
        scheduleCallbackIfUnblocked(queuedDiscreteEvents[0], unblocked); // This is a exponential search for each boundary that commits. I think it's
        // worth it because we expect very few discrete events to queue up and once
        // we are actually fully unblocked it will be fast to replay them.

        for (var i = 1; i < queuedDiscreteEvents.length; i++) {
          var queuedEvent = queuedDiscreteEvents[i];

          if (queuedEvent.blockedOn === unblocked) {
            queuedEvent.blockedOn = null;
          }
        }
      }

      if (queuedFocus !== null) {
        scheduleCallbackIfUnblocked(queuedFocus, unblocked);
      }

      if (queuedDrag !== null) {
        scheduleCallbackIfUnblocked(queuedDrag, unblocked);
      }

      if (queuedMouse !== null) {
        scheduleCallbackIfUnblocked(queuedMouse, unblocked);
      }

      var unblock = function (queuedEvent) {
        return scheduleCallbackIfUnblocked(queuedEvent, unblocked);
      };

      queuedPointers.forEach(unblock);
      queuedPointerCaptures.forEach(unblock);
    }
    /**
     * `ReactInstanceMap` maintains a mapping from a public facing stateful
     * instance (key) and the internal representation (value). This allows public
     * methods to accept the user facing instance as an argument and map them back
     * to internal methods.
     *
     * Note that this module is currently shared and assumed to be stateless.
     * If this becomes an actual Map, that will break.
     */

    /**
     * This API should be called `delete` but we'd have to make sure to always
     * transform these to strings for IE support. When this transform is fully
     * supported we can rename it.
     */


    function get(key) {
      return key._reactInternalFiber;
    }

    function has(key) {
      return key._reactInternalFiber !== undefined;
    }

    function set(key, value) {
      key._reactInternalFiber = value;
    } // Don't change these two values. They're used by React Dev Tools.


    var NoEffect =
    /*              */
    0;
    var PerformedWork =
    /*         */
    1; // You can change the rest (and add more).

    var Placement =
    /*             */
    2;
    var Update =
    /*                */
    4;
    var PlacementAndUpdate =
    /*    */
    6;
    var Deletion =
    /*              */
    8;
    var ContentReset =
    /*          */
    16;
    var Callback =
    /*              */
    32;
    var DidCapture =
    /*            */
    64;
    var Ref =
    /*                   */
    128;
    var Snapshot =
    /*              */
    256;
    var Passive =
    /*               */
    512;
    var Hydrating =
    /*             */
    1024;
    var HydratingAndUpdate =
    /*    */
    1028; // Passive & Update & Callback & Ref & Snapshot

    var LifecycleEffectMask =
    /*   */
    932; // Union of all host effects

    var HostEffectMask =
    /*        */
    2047;
    var Incomplete =
    /*            */
    2048;
    var ShouldCapture =
    /*         */
    4096;
    var ReactCurrentOwner$1 = ReactSharedInternals.ReactCurrentOwner;

    function getNearestMountedFiber(fiber) {
      var node = fiber;
      var nearestMounted = fiber;

      if (!fiber.alternate) {
        // If there is no alternate, this might be a new tree that isn't inserted
        // yet. If it is, then it will have a pending insertion effect on it.
        var nextNode = node;

        do {
          node = nextNode;

          if ((node.effectTag & (Placement | Hydrating)) !== NoEffect) {
            // This is an insertion or in-progress hydration. The nearest possible
            // mounted fiber is the parent but we need to continue to figure out
            // if that one is still mounted.
            nearestMounted = node.return;
          }

          nextNode = node.return;
        } while (nextNode);
      } else {
        while (node.return) {
          node = node.return;
        }
      }

      if (node.tag === HostRoot) {
        // TODO: Check if this was a nested HostRoot when used with
        // renderContainerIntoSubtree.
        return nearestMounted;
      } // If we didn't hit the root, that means that we're in an disconnected tree
      // that has been unmounted.


      return null;
    }

    function getSuspenseInstanceFromFiber(fiber) {
      if (fiber.tag === SuspenseComponent) {
        var suspenseState = fiber.memoizedState;

        if (suspenseState === null) {
          var current = fiber.alternate;

          if (current !== null) {
            suspenseState = current.memoizedState;
          }
        }

        if (suspenseState !== null) {
          return suspenseState.dehydrated;
        }
      }

      return null;
    }

    function getContainerFromFiber(fiber) {
      return fiber.tag === HostRoot ? fiber.stateNode.containerInfo : null;
    }

    function isFiberMounted(fiber) {
      return getNearestMountedFiber(fiber) === fiber;
    }

    function isMounted(component) {
      {
        var owner = ReactCurrentOwner$1.current;

        if (owner !== null && owner.tag === ClassComponent) {
          var ownerFiber = owner;
          var instance = ownerFiber.stateNode;
          !instance._warnedAboutRefsInRender ? warningWithoutStack$1(false, '%s is accessing isMounted inside its render() function. ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', getComponentName(ownerFiber.type) || 'A component') : void 0;
          instance._warnedAboutRefsInRender = true;
        }
      }
      var fiber = get(component);

      if (!fiber) {
        return false;
      }

      return getNearestMountedFiber(fiber) === fiber;
    }

    function assertIsMounted(fiber) {
      (function () {
        if (!(getNearestMountedFiber(fiber) === fiber)) {
          {
            throw ReactError(Error("Unable to find node on an unmounted component."));
          }
        }
      })();
    }

    function findCurrentFiberUsingSlowPath(fiber) {
      var alternate = fiber.alternate;

      if (!alternate) {
        // If there is no alternate, then we only need to check if it is mounted.
        var nearestMounted = getNearestMountedFiber(fiber);

        (function () {
          if (!(nearestMounted !== null)) {
            {
              throw ReactError(Error("Unable to find node on an unmounted component."));
            }
          }
        })();

        if (nearestMounted !== fiber) {
          return null;
        }

        return fiber;
      } // If we have two possible branches, we'll walk backwards up to the root
      // to see what path the root points to. On the way we may hit one of the
      // special cases and we'll deal with them.


      var a = fiber;
      var b = alternate;

      while (true) {
        var parentA = a.return;

        if (parentA === null) {
          // We're at the root.
          break;
        }

        var parentB = parentA.alternate;

        if (parentB === null) {
          // There is no alternate. This is an unusual case. Currently, it only
          // happens when a Suspense component is hidden. An extra fragment fiber
          // is inserted in between the Suspense fiber and its children. Skip
          // over this extra fragment fiber and proceed to the next parent.
          var nextParent = parentA.return;

          if (nextParent !== null) {
            a = b = nextParent;
            continue;
          } // If there's no parent, we're at the root.


          break;
        } // If both copies of the parent fiber point to the same child, we can
        // assume that the child is current. This happens when we bailout on low
        // priority: the bailed out fiber's child reuses the current child.


        if (parentA.child === parentB.child) {
          var child = parentA.child;

          while (child) {
            if (child === a) {
              // We've determined that A is the current branch.
              assertIsMounted(parentA);
              return fiber;
            }

            if (child === b) {
              // We've determined that B is the current branch.
              assertIsMounted(parentA);
              return alternate;
            }

            child = child.sibling;
          } // We should never have an alternate for any mounting node. So the only
          // way this could possibly happen is if this was unmounted, if at all.


          (function () {
            {
              {
                throw ReactError(Error("Unable to find node on an unmounted component."));
              }
            }
          })();
        }

        if (a.return !== b.return) {
          // The return pointer of A and the return pointer of B point to different
          // fibers. We assume that return pointers never criss-cross, so A must
          // belong to the child set of A.return, and B must belong to the child
          // set of B.return.
          a = parentA;
          b = parentB;
        } else {
          // The return pointers point to the same fiber. We'll have to use the
          // default, slow path: scan the child sets of each parent alternate to see
          // which child belongs to which set.
          //
          // Search parent A's child set
          var didFindChild = false;
          var _child = parentA.child;

          while (_child) {
            if (_child === a) {
              didFindChild = true;
              a = parentA;
              b = parentB;
              break;
            }

            if (_child === b) {
              didFindChild = true;
              b = parentA;
              a = parentB;
              break;
            }

            _child = _child.sibling;
          }

          if (!didFindChild) {
            // Search parent B's child set
            _child = parentB.child;

            while (_child) {
              if (_child === a) {
                didFindChild = true;
                a = parentB;
                b = parentA;
                break;
              }

              if (_child === b) {
                didFindChild = true;
                b = parentB;
                a = parentA;
                break;
              }

              _child = _child.sibling;
            }

            (function () {
              if (!didFindChild) {
                {
                  throw ReactError(Error("Child was not found in either parent set. This indicates a bug in React related to the return pointer. Please file an issue."));
                }
              }
            })();
          }
        }

        (function () {
          if (!(a.alternate === b)) {
            {
              throw ReactError(Error("Return fibers should always be each others' alternates. This error is likely caused by a bug in React. Please file an issue."));
            }
          }
        })();
      } // If the root is not a host container, we're in a disconnected tree. I.e.
      // unmounted.


      (function () {
        if (!(a.tag === HostRoot)) {
          {
            throw ReactError(Error("Unable to find node on an unmounted component."));
          }
        }
      })();

      if (a.stateNode.current === a) {
        // We've determined that A is the current branch.
        return fiber;
      } // Otherwise B has to be current branch.


      return alternate;
    }

    function findCurrentHostFiber(parent) {
      var currentParent = findCurrentFiberUsingSlowPath(parent);

      if (!currentParent) {
        return null;
      } // Next we'll drill down this component to find the first HostComponent/Text.


      var node = currentParent;

      while (true) {
        if (node.tag === HostComponent || node.tag === HostText) {
          return node;
        } else if (node.child) {
          node.child.return = node;
          node = node.child;
          continue;
        }

        if (node === currentParent) {
          return null;
        }

        while (!node.sibling) {
          if (!node.return || node.return === currentParent) {
            return null;
          }

          node = node.return;
        }

        node.sibling.return = node.return;
        node = node.sibling;
      } // Flow needs the return null here, but ESLint complains about it.
      // eslint-disable-next-line no-unreachable


      return null;
    }

    function findCurrentHostFiberWithNoPortals(parent) {
      var currentParent = findCurrentFiberUsingSlowPath(parent);

      if (!currentParent) {
        return null;
      } // Next we'll drill down this component to find the first HostComponent/Text.


      var node = currentParent;

      while (true) {
        if (node.tag === HostComponent || node.tag === HostText || enableFundamentalAPI && node.tag === FundamentalComponent) {
          return node;
        } else if (node.child && node.tag !== HostPortal) {
          node.child.return = node;
          node = node.child;
          continue;
        }

        if (node === currentParent) {
          return null;
        }

        while (!node.sibling) {
          if (!node.return || node.return === currentParent) {
            return null;
          }

          node = node.return;
        }

        node.sibling.return = node.return;
        node = node.sibling;
      } // Flow needs the return null here, but ESLint complains about it.
      // eslint-disable-next-line no-unreachable


      return null;
    }

    function addEventBubbleListener(element, eventType, listener) {
      element.addEventListener(eventType, listener, false);
    }

    function addEventCaptureListener(element, eventType, listener) {
      element.addEventListener(eventType, listener, true);
    }

    function addEventCaptureListenerWithPassiveFlag(element, eventType, listener, passive) {
      element.addEventListener(eventType, listener, {
        capture: true,
        passive: passive
      });
    }
    /**
     * Gets the target node from a native browser event by accounting for
     * inconsistencies in browser DOM APIs.
     *
     * @param {object} nativeEvent Native browser event.
     * @return {DOMEventTarget} Target node.
     */


    function getEventTarget(nativeEvent) {
      // Fallback to nativeEvent.srcElement for IE9
      // https://github.com/facebook/react/issues/12506
      var target = nativeEvent.target || nativeEvent.srcElement || window; // Normalize SVG <use> element events #4963

      if (target.correspondingUseElement) {
        target = target.correspondingUseElement;
      } // Safari may fire events on text nodes (Node.TEXT_NODE is 3).
      // @see http://www.quirksmode.org/js/events_properties.html


      return target.nodeType === TEXT_NODE ? target.parentNode : target;
    }

    function getParent(inst) {
      do {
        inst = inst.return; // TODO: If this is a HostRoot we might want to bail out.
        // That is depending on if we want nested subtrees (layers) to bubble
        // events to their parent. We could also go through parentNode on the
        // host node but that wouldn't work for React Native and doesn't let us
        // do the portal feature.
      } while (inst && inst.tag !== HostComponent);

      if (inst) {
        return inst;
      }

      return null;
    }
    /**
     * Return the lowest common ancestor of A and B, or null if they are in
     * different trees.
     */


    function getLowestCommonAncestor(instA, instB) {
      var depthA = 0;

      for (var tempA = instA; tempA; tempA = getParent(tempA)) {
        depthA++;
      }

      var depthB = 0;

      for (var tempB = instB; tempB; tempB = getParent(tempB)) {
        depthB++;
      } // If A is deeper, crawl up.


      while (depthA - depthB > 0) {
        instA = getParent(instA);
        depthA--;
      } // If B is deeper, crawl up.


      while (depthB - depthA > 0) {
        instB = getParent(instB);
        depthB--;
      } // Walk in lockstep until we find a match.


      var depth = depthA;

      while (depth--) {
        if (instA === instB || instA === instB.alternate) {
          return instA;
        }

        instA = getParent(instA);
        instB = getParent(instB);
      }

      return null;
    }
    /**
     * Return if A is an ancestor of B.
     */

    /**
     * Return the parent instance of the passed-in instance.
     */

    /**
     * Simulates the traversal of a two-phase, capture/bubble event dispatch.
     */


    function traverseTwoPhase(inst, fn, arg) {
      var path = [];

      while (inst) {
        path.push(inst);
        inst = getParent(inst);
      }

      var i;

      for (i = path.length; i-- > 0;) {
        fn(path[i], 'captured', arg);
      }

      for (i = 0; i < path.length; i++) {
        fn(path[i], 'bubbled', arg);
      }
    }
    /**
     * Traverses the ID hierarchy and invokes the supplied `cb` on any IDs that
     * should would receive a `mouseEnter` or `mouseLeave` event.
     *
     * Does not invoke the callback on the nearest common ancestor because nothing
     * "entered" or "left" that element.
     */


    function traverseEnterLeave(from, to, fn, argFrom, argTo) {
      var common = from && to ? getLowestCommonAncestor(from, to) : null;
      var pathFrom = [];

      while (true) {
        if (!from) {
          break;
        }

        if (from === common) {
          break;
        }

        var alternate = from.alternate;

        if (alternate !== null && alternate === common) {
          break;
        }

        pathFrom.push(from);
        from = getParent(from);
      }

      var pathTo = [];

      while (true) {
        if (!to) {
          break;
        }

        if (to === common) {
          break;
        }

        var _alternate = to.alternate;

        if (_alternate !== null && _alternate === common) {
          break;
        }

        pathTo.push(to);
        to = getParent(to);
      }

      for (var i = 0; i < pathFrom.length; i++) {
        fn(pathFrom[i], 'bubbled', argFrom);
      }

      for (var _i = pathTo.length; _i-- > 0;) {
        fn(pathTo[_i], 'captured', argTo);
      }
    }
    /**
     * Some event types have a notion of different registration names for different
     * "phases" of propagation. This finds listeners by a given phase.
     */


    function listenerAtPhase(inst, event, propagationPhase) {
      var registrationName = event.dispatchConfig.phasedRegistrationNames[propagationPhase];
      return getListener(inst, registrationName);
    }
    /**
     * A small set of propagation patterns, each of which will accept a small amount
     * of information, and generate a set of "dispatch ready event objects" - which
     * are sets of events that have already been annotated with a set of dispatched
     * listener functions/ids. The API is designed this way to discourage these
     * propagation strategies from actually executing the dispatches, since we
     * always want to collect the entire set of dispatches before executing even a
     * single one.
     */

    /**
     * Tags a `SyntheticEvent` with dispatched listeners. Creating this function
     * here, allows us to not have to bind or create functions for each event.
     * Mutating the event's members allows us to not have to create a wrapping
     * "dispatch" object that pairs the event with the listener.
     */


    function accumulateDirectionalDispatches(inst, phase, event) {
      {
        !inst ? warningWithoutStack$1(false, 'Dispatching inst must not be null') : void 0;
      }
      var listener = listenerAtPhase(inst, event, phase);

      if (listener) {
        event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
        event._dispatchInstances = accumulateInto(event._dispatchInstances, inst);
      }
    }
    /**
     * Collect dispatches (must be entirely collected before dispatching - see unit
     * tests). Lazily allocate the array to conserve memory.  We must loop through
     * each event and perform the traversal for each one. We cannot perform a
     * single traversal for the entire collection of events because each event may
     * have a different target.
     */


    function accumulateTwoPhaseDispatchesSingle(event) {
      if (event && event.dispatchConfig.phasedRegistrationNames) {
        traverseTwoPhase(event._targetInst, accumulateDirectionalDispatches, event);
      }
    }
    /**
     * Accumulates without regard to direction, does not look for phased
     * registration names. Same as `accumulateDirectDispatchesSingle` but without
     * requiring that the `dispatchMarker` be the same as the dispatched ID.
     */


    function accumulateDispatches(inst, ignoredDirection, event) {
      if (inst && event && event.dispatchConfig.registrationName) {
        var registrationName = event.dispatchConfig.registrationName;
        var listener = getListener(inst, registrationName);

        if (listener) {
          event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
          event._dispatchInstances = accumulateInto(event._dispatchInstances, inst);
        }
      }
    }
    /**
     * Accumulates dispatches on an `SyntheticEvent`, but only for the
     * `dispatchMarker`.
     * @param {SyntheticEvent} event
     */


    function accumulateDirectDispatchesSingle(event) {
      if (event && event.dispatchConfig.registrationName) {
        accumulateDispatches(event._targetInst, null, event);
      }
    }

    function accumulateTwoPhaseDispatches(events) {
      forEachAccumulated(events, accumulateTwoPhaseDispatchesSingle);
    }

    function accumulateEnterLeaveDispatches(leave, enter, from, to) {
      traverseEnterLeave(from, to, accumulateDispatches, leave, enter);
    }

    function accumulateDirectDispatches(events) {
      forEachAccumulated(events, accumulateDirectDispatchesSingle);
    }
    /* eslint valid-typeof: 0 */


    var EVENT_POOL_SIZE = 10;
    /**
     * @interface Event
     * @see http://www.w3.org/TR/DOM-Level-3-Events/
     */

    var EventInterface = {
      type: null,
      target: null,
      // currentTarget is set when dispatching; no use in copying it here
      currentTarget: function () {
        return null;
      },
      eventPhase: null,
      bubbles: null,
      cancelable: null,
      timeStamp: function (event) {
        return event.timeStamp || Date.now();
      },
      defaultPrevented: null,
      isTrusted: null
    };

    function functionThatReturnsTrue() {
      return true;
    }

    function functionThatReturnsFalse() {
      return false;
    }
    /**
     * Synthetic events are dispatched by event plugins, typically in response to a
     * top-level event delegation handler.
     *
     * These systems should generally use pooling to reduce the frequency of garbage
     * collection. The system should check `isPersistent` to determine whether the
     * event should be released into the pool after being dispatched. Users that
     * need a persisted event should invoke `persist`.
     *
     * Synthetic events (and subclasses) implement the DOM Level 3 Events API by
     * normalizing browser quirks. Subclasses do not necessarily have to implement a
     * DOM interface; custom application-specific events can also subclass this.
     *
     * @param {object} dispatchConfig Configuration used to dispatch this event.
     * @param {*} targetInst Marker identifying the event target.
     * @param {object} nativeEvent Native browser event.
     * @param {DOMEventTarget} nativeEventTarget Target node.
     */


    function SyntheticEvent(dispatchConfig, targetInst, nativeEvent, nativeEventTarget) {
      {
        // these have a getter/setter for warnings
        delete this.nativeEvent;
        delete this.preventDefault;
        delete this.stopPropagation;
        delete this.isDefaultPrevented;
        delete this.isPropagationStopped;
      }
      this.dispatchConfig = dispatchConfig;
      this._targetInst = targetInst;
      this.nativeEvent = nativeEvent;
      var Interface = this.constructor.Interface;

      for (var propName in Interface) {
        if (!Interface.hasOwnProperty(propName)) {
          continue;
        }

        {
          delete this[propName]; // this has a getter/setter for warnings
        }
        var normalize = Interface[propName];

        if (normalize) {
          this[propName] = normalize(nativeEvent);
        } else {
          if (propName === 'target') {
            this.target = nativeEventTarget;
          } else {
            this[propName] = nativeEvent[propName];
          }
        }
      }

      var defaultPrevented = nativeEvent.defaultPrevented != null ? nativeEvent.defaultPrevented : nativeEvent.returnValue === false;

      if (defaultPrevented) {
        this.isDefaultPrevented = functionThatReturnsTrue;
      } else {
        this.isDefaultPrevented = functionThatReturnsFalse;
      }

      this.isPropagationStopped = functionThatReturnsFalse;
      return this;
    }

    _assign(SyntheticEvent.prototype, {
      preventDefault: function () {
        this.defaultPrevented = true;
        var event = this.nativeEvent;

        if (!event) {
          return;
        }

        if (event.preventDefault) {
          event.preventDefault();
        } else if (typeof event.returnValue !== 'unknown') {
          event.returnValue = false;
        }

        this.isDefaultPrevented = functionThatReturnsTrue;
      },
      stopPropagation: function () {
        var event = this.nativeEvent;

        if (!event) {
          return;
        }

        if (event.stopPropagation) {
          event.stopPropagation();
        } else if (typeof event.cancelBubble !== 'unknown') {
          // The ChangeEventPlugin registers a "propertychange" event for
          // IE. This event does not support bubbling or cancelling, and
          // any references to cancelBubble throw "Member not found".  A
          // typeof check of "unknown" circumvents this issue (and is also
          // IE specific).
          event.cancelBubble = true;
        }

        this.isPropagationStopped = functionThatReturnsTrue;
      },

      /**
       * We release all dispatched `SyntheticEvent`s after each event loop, adding
       * them back into the pool. This allows a way to hold onto a reference that
       * won't be added back into the pool.
       */
      persist: function () {
        this.isPersistent = functionThatReturnsTrue;
      },

      /**
       * Checks if this event should be released back into the pool.
       *
       * @return {boolean} True if this should not be released, false otherwise.
       */
      isPersistent: functionThatReturnsFalse,

      /**
       * `PooledClass` looks for `destructor` on each instance it releases.
       */
      destructor: function () {
        var Interface = this.constructor.Interface;

        for (var propName in Interface) {
          {
            Object.defineProperty(this, propName, getPooledWarningPropertyDefinition(propName, Interface[propName]));
          }
        }

        this.dispatchConfig = null;
        this._targetInst = null;
        this.nativeEvent = null;
        this.isDefaultPrevented = functionThatReturnsFalse;
        this.isPropagationStopped = functionThatReturnsFalse;
        this._dispatchListeners = null;
        this._dispatchInstances = null;
        {
          Object.defineProperty(this, 'nativeEvent', getPooledWarningPropertyDefinition('nativeEvent', null));
          Object.defineProperty(this, 'isDefaultPrevented', getPooledWarningPropertyDefinition('isDefaultPrevented', functionThatReturnsFalse));
          Object.defineProperty(this, 'isPropagationStopped', getPooledWarningPropertyDefinition('isPropagationStopped', functionThatReturnsFalse));
          Object.defineProperty(this, 'preventDefault', getPooledWarningPropertyDefinition('preventDefault', function () {}));
          Object.defineProperty(this, 'stopPropagation', getPooledWarningPropertyDefinition('stopPropagation', function () {}));
        }
      }
    });

    SyntheticEvent.Interface = EventInterface;
    /**
     * Helper to reduce boilerplate when creating subclasses.
     */

    SyntheticEvent.extend = function (Interface) {
      var Super = this;

      var E = function () {};

      E.prototype = Super.prototype;
      var prototype = new E();

      function Class() {
        return Super.apply(this, arguments);
      }

      _assign(prototype, Class.prototype);

      Class.prototype = prototype;
      Class.prototype.constructor = Class;
      Class.Interface = _assign({}, Super.Interface, Interface);
      Class.extend = Super.extend;
      addEventPoolingTo(Class);
      return Class;
    };

    addEventPoolingTo(SyntheticEvent);
    /**
     * Helper to nullify syntheticEvent instance properties when destructing
     *
     * @param {String} propName
     * @param {?object} getVal
     * @return {object} defineProperty object
     */

    function getPooledWarningPropertyDefinition(propName, getVal) {
      var isFunction = typeof getVal === 'function';
      return {
        configurable: true,
        set: set,
        get: get
      };

      function set(val) {
        var action = isFunction ? 'setting the method' : 'setting the property';
        warn(action, 'This is effectively a no-op');
        return val;
      }

      function get() {
        var action = isFunction ? 'accessing the method' : 'accessing the property';
        var result = isFunction ? 'This is a no-op function' : 'This is set to null';
        warn(action, result);
        return getVal;
      }

      function warn(action, result) {
        var warningCondition = false;
        !warningCondition ? warningWithoutStack$1(false, "This synthetic event is reused for performance reasons. If you're seeing this, " + "you're %s `%s` on a released/nullified synthetic event. %s. " + 'If you must keep the original synthetic event around, use event.persist(). ' + 'See https://fb.me/react-event-pooling for more information.', action, propName, result) : void 0;
      }
    }

    function getPooledEvent(dispatchConfig, targetInst, nativeEvent, nativeInst) {
      var EventConstructor = this;

      if (EventConstructor.eventPool.length) {
        var instance = EventConstructor.eventPool.pop();
        EventConstructor.call(instance, dispatchConfig, targetInst, nativeEvent, nativeInst);
        return instance;
      }

      return new EventConstructor(dispatchConfig, targetInst, nativeEvent, nativeInst);
    }

    function releasePooledEvent(event) {
      var EventConstructor = this;

      (function () {
        if (!(event instanceof EventConstructor)) {
          {
            throw ReactError(Error("Trying to release an event instance into a pool of a different type."));
          }
        }
      })();

      event.destructor();

      if (EventConstructor.eventPool.length < EVENT_POOL_SIZE) {
        EventConstructor.eventPool.push(event);
      }
    }

    function addEventPoolingTo(EventConstructor) {
      EventConstructor.eventPool = [];
      EventConstructor.getPooled = getPooledEvent;
      EventConstructor.release = releasePooledEvent;
    }
    /**
     * @interface Event
     * @see http://www.w3.org/TR/css3-animations/#AnimationEvent-interface
     * @see https://developer.mozilla.org/en-US/docs/Web/API/AnimationEvent
     */


    var SyntheticAnimationEvent = SyntheticEvent.extend({
      animationName: null,
      elapsedTime: null,
      pseudoElement: null
    });
    /**
     * @interface Event
     * @see http://www.w3.org/TR/clipboard-apis/
     */

    var SyntheticClipboardEvent = SyntheticEvent.extend({
      clipboardData: function (event) {
        return 'clipboardData' in event ? event.clipboardData : window.clipboardData;
      }
    });
    var SyntheticUIEvent = SyntheticEvent.extend({
      view: null,
      detail: null
    });
    /**
     * @interface FocusEvent
     * @see http://www.w3.org/TR/DOM-Level-3-Events/
     */

    var SyntheticFocusEvent = SyntheticUIEvent.extend({
      relatedTarget: null
    });
    /**
     * `charCode` represents the actual "character code" and is safe to use with
     * `String.fromCharCode`. As such, only keys that correspond to printable
     * characters produce a valid `charCode`, the only exception to this is Enter.
     * The Tab-key is considered non-printable and does not have a `charCode`,
     * presumably because it does not produce a tab-character in browsers.
     *
     * @param {object} nativeEvent Native browser event.
     * @return {number} Normalized `charCode` property.
     */

    function getEventCharCode(nativeEvent) {
      var charCode;
      var keyCode = nativeEvent.keyCode;

      if ('charCode' in nativeEvent) {
        charCode = nativeEvent.charCode; // FF does not set `charCode` for the Enter-key, check against `keyCode`.

        if (charCode === 0 && keyCode === 13) {
          charCode = 13;
        }
      } else {
        // IE8 does not implement `charCode`, but `keyCode` has the correct value.
        charCode = keyCode;
      } // IE and Edge (on Windows) and Chrome / Safari (on Windows and Linux)
      // report Enter as charCode 10 when ctrl is pressed.


      if (charCode === 10) {
        charCode = 13;
      } // Some non-printable keys are reported in `charCode`/`keyCode`, discard them.
      // Must not discard the (non-)printable Enter-key.


      if (charCode >= 32 || charCode === 13) {
        return charCode;
      }

      return 0;
    }
    /**
     * Normalization of deprecated HTML5 `key` values
     * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
     */


    var normalizeKey = {
      Esc: 'Escape',
      Spacebar: ' ',
      Left: 'ArrowLeft',
      Up: 'ArrowUp',
      Right: 'ArrowRight',
      Down: 'ArrowDown',
      Del: 'Delete',
      Win: 'OS',
      Menu: 'ContextMenu',
      Apps: 'ContextMenu',
      Scroll: 'ScrollLock',
      MozPrintableKey: 'Unidentified'
    };
    /**
     * Translation from legacy `keyCode` to HTML5 `key`
     * Only special keys supported, all others depend on keyboard layout or browser
     * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
     */

    var translateToKey = {
      '8': 'Backspace',
      '9': 'Tab',
      '12': 'Clear',
      '13': 'Enter',
      '16': 'Shift',
      '17': 'Control',
      '18': 'Alt',
      '19': 'Pause',
      '20': 'CapsLock',
      '27': 'Escape',
      '32': ' ',
      '33': 'PageUp',
      '34': 'PageDown',
      '35': 'End',
      '36': 'Home',
      '37': 'ArrowLeft',
      '38': 'ArrowUp',
      '39': 'ArrowRight',
      '40': 'ArrowDown',
      '45': 'Insert',
      '46': 'Delete',
      '112': 'F1',
      '113': 'F2',
      '114': 'F3',
      '115': 'F4',
      '116': 'F5',
      '117': 'F6',
      '118': 'F7',
      '119': 'F8',
      '120': 'F9',
      '121': 'F10',
      '122': 'F11',
      '123': 'F12',
      '144': 'NumLock',
      '145': 'ScrollLock',
      '224': 'Meta'
    };
    /**
     * @param {object} nativeEvent Native browser event.
     * @return {string} Normalized `key` property.
     */

    function getEventKey(nativeEvent) {
      if (nativeEvent.key) {
        // Normalize inconsistent values reported by browsers due to
        // implementations of a working draft specification.
        // FireFox implements `key` but returns `MozPrintableKey` for all
        // printable characters (normalized to `Unidentified`), ignore it.
        var key = normalizeKey[nativeEvent.key] || nativeEvent.key;

        if (key !== 'Unidentified') {
          return key;
        }
      } // Browser does not implement `key`, polyfill as much of it as we can.


      if (nativeEvent.type === 'keypress') {
        var charCode = getEventCharCode(nativeEvent); // The enter-key is technically both printable and non-printable and can
        // thus be captured by `keypress`, no other non-printable key should.

        return charCode === 13 ? 'Enter' : String.fromCharCode(charCode);
      }

      if (nativeEvent.type === 'keydown' || nativeEvent.type === 'keyup') {
        // While user keyboard layout determines the actual meaning of each
        // `keyCode` value, almost all function keys have a universal value.
        return translateToKey[nativeEvent.keyCode] || 'Unidentified';
      }

      return '';
    }
    /**
     * Translation from modifier key to the associated property in the event.
     * @see http://www.w3.org/TR/DOM-Level-3-Events/#keys-Modifiers
     */


    var modifierKeyToProp = {
      Alt: 'altKey',
      Control: 'ctrlKey',
      Meta: 'metaKey',
      Shift: 'shiftKey'
    }; // Older browsers (Safari <= 10, iOS Safari <= 10.2) do not support
    // getModifierState. If getModifierState is not supported, we map it to a set of
    // modifier keys exposed by the event. In this case, Lock-keys are not supported.

    function modifierStateGetter(keyArg) {
      var syntheticEvent = this;
      var nativeEvent = syntheticEvent.nativeEvent;

      if (nativeEvent.getModifierState) {
        return nativeEvent.getModifierState(keyArg);
      }

      var keyProp = modifierKeyToProp[keyArg];
      return keyProp ? !!nativeEvent[keyProp] : false;
    }

    function getEventModifierState(nativeEvent) {
      return modifierStateGetter;
    }
    /**
     * @interface KeyboardEvent
     * @see http://www.w3.org/TR/DOM-Level-3-Events/
     */


    var SyntheticKeyboardEvent = SyntheticUIEvent.extend({
      key: getEventKey,
      location: null,
      ctrlKey: null,
      shiftKey: null,
      altKey: null,
      metaKey: null,
      repeat: null,
      locale: null,
      getModifierState: getEventModifierState,
      // Legacy Interface
      charCode: function (event) {
        // `charCode` is the result of a KeyPress event and represents the value of
        // the actual printable character.
        // KeyPress is deprecated, but its replacement is not yet final and not
        // implemented in any major browser. Only KeyPress has charCode.
        if (event.type === 'keypress') {
          return getEventCharCode(event);
        }

        return 0;
      },
      keyCode: function (event) {
        // `keyCode` is the result of a KeyDown/Up event and represents the value of
        // physical keyboard key.
        // The actual meaning of the value depends on the users' keyboard layout
        // which cannot be detected. Assuming that it is a US keyboard layout
        // provides a surprisingly accurate mapping for US and European users.
        // Due to this, it is left to the user to implement at this time.
        if (event.type === 'keydown' || event.type === 'keyup') {
          return event.keyCode;
        }

        return 0;
      },
      which: function (event) {
        // `which` is an alias for either `keyCode` or `charCode` depending on the
        // type of the event.
        if (event.type === 'keypress') {
          return getEventCharCode(event);
        }

        if (event.type === 'keydown' || event.type === 'keyup') {
          return event.keyCode;
        }

        return 0;
      }
    });
    var previousScreenX = 0;
    var previousScreenY = 0; // Use flags to signal movementX/Y has already been set

    var isMovementXSet = false;
    var isMovementYSet = false;
    /**
     * @interface MouseEvent
     * @see http://www.w3.org/TR/DOM-Level-3-Events/
     */

    var SyntheticMouseEvent = SyntheticUIEvent.extend({
      screenX: null,
      screenY: null,
      clientX: null,
      clientY: null,
      pageX: null,
      pageY: null,
      ctrlKey: null,
      shiftKey: null,
      altKey: null,
      metaKey: null,
      getModifierState: getEventModifierState,
      button: null,
      buttons: null,
      relatedTarget: function (event) {
        return event.relatedTarget || (event.fromElement === event.srcElement ? event.toElement : event.fromElement);
      },
      movementX: function (event) {
        if ('movementX' in event) {
          return event.movementX;
        }

        var screenX = previousScreenX;
        previousScreenX = event.screenX;

        if (!isMovementXSet) {
          isMovementXSet = true;
          return 0;
        }

        return event.type === 'mousemove' ? event.screenX - screenX : 0;
      },
      movementY: function (event) {
        if ('movementY' in event) {
          return event.movementY;
        }

        var screenY = previousScreenY;
        previousScreenY = event.screenY;

        if (!isMovementYSet) {
          isMovementYSet = true;
          return 0;
        }

        return event.type === 'mousemove' ? event.screenY - screenY : 0;
      }
    });
    /**
     * @interface PointerEvent
     * @see http://www.w3.org/TR/pointerevents/
     */

    var SyntheticPointerEvent = SyntheticMouseEvent.extend({
      pointerId: null,
      width: null,
      height: null,
      pressure: null,
      tangentialPressure: null,
      tiltX: null,
      tiltY: null,
      twist: null,
      pointerType: null,
      isPrimary: null
    });
    /**
     * @interface DragEvent
     * @see http://www.w3.org/TR/DOM-Level-3-Events/
     */

    var SyntheticDragEvent = SyntheticMouseEvent.extend({
      dataTransfer: null
    });
    /**
     * @interface TouchEvent
     * @see http://www.w3.org/TR/touch-events/
     */

    var SyntheticTouchEvent = SyntheticUIEvent.extend({
      touches: null,
      targetTouches: null,
      changedTouches: null,
      altKey: null,
      metaKey: null,
      ctrlKey: null,
      shiftKey: null,
      getModifierState: getEventModifierState
    });
    /**
     * @interface Event
     * @see http://www.w3.org/TR/2009/WD-css3-transitions-20090320/#transition-events-
     * @see https://developer.mozilla.org/en-US/docs/Web/API/TransitionEvent
     */

    var SyntheticTransitionEvent = SyntheticEvent.extend({
      propertyName: null,
      elapsedTime: null,
      pseudoElement: null
    });
    /**
     * @interface WheelEvent
     * @see http://www.w3.org/TR/DOM-Level-3-Events/
     */

    var SyntheticWheelEvent = SyntheticMouseEvent.extend({
      deltaX: function (event) {
        return 'deltaX' in event ? event.deltaX : // Fallback to `wheelDeltaX` for Webkit and normalize (right is positive).
        'wheelDeltaX' in event ? -event.wheelDeltaX : 0;
      },
      deltaY: function (event) {
        return 'deltaY' in event ? event.deltaY : // Fallback to `wheelDeltaY` for Webkit and normalize (down is positive).
        'wheelDeltaY' in event ? -event.wheelDeltaY : // Fallback to `wheelDelta` for IE<9 and normalize (down is positive).
        'wheelDelta' in event ? -event.wheelDelta : 0;
      },
      deltaZ: null,
      // Browsers without "deltaMode" is reporting in raw wheel delta where one
      // notch on the scroll is always +/- 120, roughly equivalent to pixels.
      // A good approximation of DOM_DELTA_LINE (1) is 5% of viewport size or
      // ~40 pixels, for DOM_DELTA_SCREEN (2) it is 87.5% of viewport size.
      deltaMode: null
    });
    /**
     * Turns
     * ['abort', ...]
     * into
     * eventTypes = {
     *   'abort': {
     *     phasedRegistrationNames: {
     *       bubbled: 'onAbort',
     *       captured: 'onAbortCapture',
     *     },
     *     dependencies: [TOP_ABORT],
     *   },
     *   ...
     * };
     * topLevelEventsToDispatchConfig = new Map([
     *   [TOP_ABORT, { sameConfig }],
     * ]);
     */

    var eventTuples = [// Discrete events
    [TOP_BLUR, 'blur', DiscreteEvent], [TOP_CANCEL, 'cancel', DiscreteEvent], [TOP_CLICK, 'click', DiscreteEvent], [TOP_CLOSE, 'close', DiscreteEvent], [TOP_CONTEXT_MENU, 'contextMenu', DiscreteEvent], [TOP_COPY, 'copy', DiscreteEvent], [TOP_CUT, 'cut', DiscreteEvent], [TOP_AUX_CLICK, 'auxClick', DiscreteEvent], [TOP_DOUBLE_CLICK, 'doubleClick', DiscreteEvent], [TOP_DRAG_END, 'dragEnd', DiscreteEvent], [TOP_DRAG_START, 'dragStart', DiscreteEvent], [TOP_DROP, 'drop', DiscreteEvent], [TOP_FOCUS, 'focus', DiscreteEvent], [TOP_INPUT, 'input', DiscreteEvent], [TOP_INVALID, 'invalid', DiscreteEvent], [TOP_KEY_DOWN, 'keyDown', DiscreteEvent], [TOP_KEY_PRESS, 'keyPress', DiscreteEvent], [TOP_KEY_UP, 'keyUp', DiscreteEvent], [TOP_MOUSE_DOWN, 'mouseDown', DiscreteEvent], [TOP_MOUSE_UP, 'mouseUp', DiscreteEvent], [TOP_PASTE, 'paste', DiscreteEvent], [TOP_PAUSE, 'pause', DiscreteEvent], [TOP_PLAY, 'play', DiscreteEvent], [TOP_POINTER_CANCEL, 'pointerCancel', DiscreteEvent], [TOP_POINTER_DOWN, 'pointerDown', DiscreteEvent], [TOP_POINTER_UP, 'pointerUp', DiscreteEvent], [TOP_RATE_CHANGE, 'rateChange', DiscreteEvent], [TOP_RESET, 'reset', DiscreteEvent], [TOP_SEEKED, 'seeked', DiscreteEvent], [TOP_SUBMIT, 'submit', DiscreteEvent], [TOP_TOUCH_CANCEL, 'touchCancel', DiscreteEvent], [TOP_TOUCH_END, 'touchEnd', DiscreteEvent], [TOP_TOUCH_START, 'touchStart', DiscreteEvent], [TOP_VOLUME_CHANGE, 'volumeChange', DiscreteEvent], // User-blocking events
    [TOP_DRAG, 'drag', UserBlockingEvent], [TOP_DRAG_ENTER, 'dragEnter', UserBlockingEvent], [TOP_DRAG_EXIT, 'dragExit', UserBlockingEvent], [TOP_DRAG_LEAVE, 'dragLeave', UserBlockingEvent], [TOP_DRAG_OVER, 'dragOver', UserBlockingEvent], [TOP_MOUSE_MOVE, 'mouseMove', UserBlockingEvent], [TOP_MOUSE_OUT, 'mouseOut', UserBlockingEvent], [TOP_MOUSE_OVER, 'mouseOver', UserBlockingEvent], [TOP_POINTER_MOVE, 'pointerMove', UserBlockingEvent], [TOP_POINTER_OUT, 'pointerOut', UserBlockingEvent], [TOP_POINTER_OVER, 'pointerOver', UserBlockingEvent], [TOP_SCROLL, 'scroll', UserBlockingEvent], [TOP_TOGGLE, 'toggle', UserBlockingEvent], [TOP_TOUCH_MOVE, 'touchMove', UserBlockingEvent], [TOP_WHEEL, 'wheel', UserBlockingEvent], // Continuous events
    [TOP_ABORT, 'abort', ContinuousEvent], [TOP_ANIMATION_END, 'animationEnd', ContinuousEvent], [TOP_ANIMATION_ITERATION, 'animationIteration', ContinuousEvent], [TOP_ANIMATION_START, 'animationStart', ContinuousEvent], [TOP_CAN_PLAY, 'canPlay', ContinuousEvent], [TOP_CAN_PLAY_THROUGH, 'canPlayThrough', ContinuousEvent], [TOP_DURATION_CHANGE, 'durationChange', ContinuousEvent], [TOP_EMPTIED, 'emptied', ContinuousEvent], [TOP_ENCRYPTED, 'encrypted', ContinuousEvent], [TOP_ENDED, 'ended', ContinuousEvent], [TOP_ERROR, 'error', ContinuousEvent], [TOP_GOT_POINTER_CAPTURE, 'gotPointerCapture', ContinuousEvent], [TOP_LOAD, 'load', ContinuousEvent], [TOP_LOADED_DATA, 'loadedData', ContinuousEvent], [TOP_LOADED_METADATA, 'loadedMetadata', ContinuousEvent], [TOP_LOAD_START, 'loadStart', ContinuousEvent], [TOP_LOST_POINTER_CAPTURE, 'lostPointerCapture', ContinuousEvent], [TOP_PLAYING, 'playing', ContinuousEvent], [TOP_PROGRESS, 'progress', ContinuousEvent], [TOP_SEEKING, 'seeking', ContinuousEvent], [TOP_STALLED, 'stalled', ContinuousEvent], [TOP_SUSPEND, 'suspend', ContinuousEvent], [TOP_TIME_UPDATE, 'timeUpdate', ContinuousEvent], [TOP_TRANSITION_END, 'transitionEnd', ContinuousEvent], [TOP_WAITING, 'waiting', ContinuousEvent]];
    var eventTypes = {};
    var topLevelEventsToDispatchConfig = {};

    for (var i = 0; i < eventTuples.length; i++) {
      var eventTuple = eventTuples[i];
      var topEvent = eventTuple[0];
      var event = eventTuple[1];
      var eventPriority = eventTuple[2];
      var capitalizedEvent = event[0].toUpperCase() + event.slice(1);
      var onEvent = 'on' + capitalizedEvent;
      var config = {
        phasedRegistrationNames: {
          bubbled: onEvent,
          captured: onEvent + 'Capture'
        },
        dependencies: [topEvent],
        eventPriority: eventPriority
      };
      eventTypes[event] = config;
      topLevelEventsToDispatchConfig[topEvent] = config;
    } // Only used in DEV for exhaustiveness validation.


    var knownHTMLTopLevelTypes = [TOP_ABORT, TOP_CANCEL, TOP_CAN_PLAY, TOP_CAN_PLAY_THROUGH, TOP_CLOSE, TOP_DURATION_CHANGE, TOP_EMPTIED, TOP_ENCRYPTED, TOP_ENDED, TOP_ERROR, TOP_INPUT, TOP_INVALID, TOP_LOAD, TOP_LOADED_DATA, TOP_LOADED_METADATA, TOP_LOAD_START, TOP_PAUSE, TOP_PLAY, TOP_PLAYING, TOP_PROGRESS, TOP_RATE_CHANGE, TOP_RESET, TOP_SEEKED, TOP_SEEKING, TOP_STALLED, TOP_SUBMIT, TOP_SUSPEND, TOP_TIME_UPDATE, TOP_TOGGLE, TOP_VOLUME_CHANGE, TOP_WAITING];
    var SimpleEventPlugin = {
      eventTypes: eventTypes,
      getEventPriority: function (topLevelType) {
        var config = topLevelEventsToDispatchConfig[topLevelType];
        return config !== undefined ? config.eventPriority : ContinuousEvent;
      },
      extractEvents: function (topLevelType, eventSystemFlags, targetInst, nativeEvent, nativeEventTarget) {
        var dispatchConfig = topLevelEventsToDispatchConfig[topLevelType];

        if (!dispatchConfig) {
          return null;
        }

        var EventConstructor;

        switch (topLevelType) {
          case TOP_KEY_PRESS:
            // Firefox creates a keypress event for function keys too. This removes
            // the unwanted keypress events. Enter is however both printable and
            // non-printable. One would expect Tab to be as well (but it isn't).
            if (getEventCharCode(nativeEvent) === 0) {
              return null;
            }

          /* falls through */

          case TOP_KEY_DOWN:
          case TOP_KEY_UP:
            EventConstructor = SyntheticKeyboardEvent;
            break;

          case TOP_BLUR:
          case TOP_FOCUS:
            EventConstructor = SyntheticFocusEvent;
            break;

          case TOP_CLICK:
            // Firefox creates a click event on right mouse clicks. This removes the
            // unwanted click events.
            if (nativeEvent.button === 2) {
              return null;
            }

          /* falls through */

          case TOP_AUX_CLICK:
          case TOP_DOUBLE_CLICK:
          case TOP_MOUSE_DOWN:
          case TOP_MOUSE_MOVE:
          case TOP_MOUSE_UP: // TODO: Disabled elements should not respond to mouse events

          /* falls through */

          case TOP_MOUSE_OUT:
          case TOP_MOUSE_OVER:
          case TOP_CONTEXT_MENU:
            EventConstructor = SyntheticMouseEvent;
            break;

          case TOP_DRAG:
          case TOP_DRAG_END:
          case TOP_DRAG_ENTER:
          case TOP_DRAG_EXIT:
          case TOP_DRAG_LEAVE:
          case TOP_DRAG_OVER:
          case TOP_DRAG_START:
          case TOP_DROP:
            EventConstructor = SyntheticDragEvent;
            break;

          case TOP_TOUCH_CANCEL:
          case TOP_TOUCH_END:
          case TOP_TOUCH_MOVE:
          case TOP_TOUCH_START:
            EventConstructor = SyntheticTouchEvent;
            break;

          case TOP_ANIMATION_END:
          case TOP_ANIMATION_ITERATION:
          case TOP_ANIMATION_START:
            EventConstructor = SyntheticAnimationEvent;
            break;

          case TOP_TRANSITION_END:
            EventConstructor = SyntheticTransitionEvent;
            break;

          case TOP_SCROLL:
            EventConstructor = SyntheticUIEvent;
            break;

          case TOP_WHEEL:
            EventConstructor = SyntheticWheelEvent;
            break;

          case TOP_COPY:
          case TOP_CUT:
          case TOP_PASTE:
            EventConstructor = SyntheticClipboardEvent;
            break;

          case TOP_GOT_POINTER_CAPTURE:
          case TOP_LOST_POINTER_CAPTURE:
          case TOP_POINTER_CANCEL:
          case TOP_POINTER_DOWN:
          case TOP_POINTER_MOVE:
          case TOP_POINTER_OUT:
          case TOP_POINTER_OVER:
          case TOP_POINTER_UP:
            EventConstructor = SyntheticPointerEvent;
            break;

          default:
            {
              if (knownHTMLTopLevelTypes.indexOf(topLevelType) === -1) {
                warningWithoutStack$1(false, 'SimpleEventPlugin: Unhandled event type, `%s`. This warning ' + 'is likely caused by a bug in React. Please file an issue.', topLevelType);
              }
            } // HTML Events
            // @see http://www.w3.org/TR/html5/index.html#events-0

            EventConstructor = SyntheticEvent;
            break;
        }

        var event = EventConstructor.getPooled(dispatchConfig, targetInst, nativeEvent, nativeEventTarget);
        accumulateTwoPhaseDispatches(event);
        return event;
      }
    };
    var passiveBrowserEventsSupported = false; // Check if browser support events with passive listeners
    // https://developer.mozilla.org/en-US/docs/Web/API/EventTarget/addEventListener#Safely_detecting_option_support

    if (enableFlareAPI && canUseDOM) {
      try {
        var options = {}; // $FlowFixMe: Ignore Flow complaining about needing a value

        Object.defineProperty(options, 'passive', {
          get: function () {
            passiveBrowserEventsSupported = true;
          }
        });
        window.addEventListener('test', options, options);
        window.removeEventListener('test', options, options);
      } catch (e) {
        passiveBrowserEventsSupported = false;
      }
    } // Intentionally not named imports because Rollup would use dynamic dispatch for
    // CommonJS interop named imports.


    var UserBlockingPriority$1 = Scheduler.unstable_UserBlockingPriority;
    var runWithPriority$1 = Scheduler.unstable_runWithPriority;
    var getEventPriority = SimpleEventPlugin.getEventPriority;
    var CALLBACK_BOOKKEEPING_POOL_SIZE = 10;
    var callbackBookkeepingPool = [];
    /**
     * Find the deepest React component completely containing the root of the
     * passed-in instance (for use when entire React trees are nested within each
     * other). If React trees are not nested, returns null.
     */

    function findRootContainerNode(inst) {
      if (inst.tag === HostRoot) {
        return inst.stateNode.containerInfo;
      } // TODO: It may be a good idea to cache this to prevent unnecessary DOM
      // traversal, but caching is difficult to do correctly without using a
      // mutation observer to listen for all DOM changes.


      while (inst.return) {
        inst = inst.return;
      }

      if (inst.tag !== HostRoot) {
        // This can happen if we're in a detached tree.
        return null;
      }

      return inst.stateNode.containerInfo;
    } // Used to store ancestor hierarchy in top level callback


    function getTopLevelCallbackBookKeeping(topLevelType, nativeEvent, targetInst, eventSystemFlags) {
      if (callbackBookkeepingPool.length) {
        var instance = callbackBookkeepingPool.pop();
        instance.topLevelType = topLevelType;
        instance.eventSystemFlags = eventSystemFlags;
        instance.nativeEvent = nativeEvent;
        instance.targetInst = targetInst;
        return instance;
      }

      return {
        topLevelType: topLevelType,
        eventSystemFlags: eventSystemFlags,
        nativeEvent: nativeEvent,
        targetInst: targetInst,
        ancestors: []
      };
    }

    function releaseTopLevelCallbackBookKeeping(instance) {
      instance.topLevelType = null;
      instance.nativeEvent = null;
      instance.targetInst = null;
      instance.ancestors.length = 0;

      if (callbackBookkeepingPool.length < CALLBACK_BOOKKEEPING_POOL_SIZE) {
        callbackBookkeepingPool.push(instance);
      }
    }

    function handleTopLevel(bookKeeping) {
      var targetInst = bookKeeping.targetInst; // Loop through the hierarchy, in case there's any nested components.
      // It's important that we build the array of ancestors before calling any
      // event handlers, because event handlers can modify the DOM, leading to
      // inconsistencies with ReactMount's node cache. See #1105.

      var ancestor = targetInst;

      do {
        if (!ancestor) {
          var ancestors = bookKeeping.ancestors;
          ancestors.push(ancestor);
          break;
        }

        var root = findRootContainerNode(ancestor);

        if (!root) {
          break;
        }

        var tag = ancestor.tag;

        if (tag === HostComponent || tag === HostText) {
          bookKeeping.ancestors.push(ancestor);
        }

        ancestor = getClosestInstanceFromNode(root);
      } while (ancestor);

      for (var i = 0; i < bookKeeping.ancestors.length; i++) {
        targetInst = bookKeeping.ancestors[i];
        var eventTarget = getEventTarget(bookKeeping.nativeEvent);
        var topLevelType = bookKeeping.topLevelType;
        var nativeEvent = bookKeeping.nativeEvent;
        runExtractedPluginEventsInBatch(topLevelType, bookKeeping.eventSystemFlags, targetInst, nativeEvent, eventTarget);
      }
    } // TODO: can we stop exporting these?


    var _enabled = true;

    function setEnabled(enabled) {
      _enabled = !!enabled;
    }

    function isEnabled() {
      return _enabled;
    }

    function trapBubbledEvent(topLevelType, element) {
      trapEventForPluginEventSystem(element, topLevelType, false);
    }

    function trapCapturedEvent(topLevelType, element) {
      trapEventForPluginEventSystem(element, topLevelType, true);
    }

    function trapEventForResponderEventSystem(element, topLevelType, passive) {
      if (enableFlareAPI) {
        var rawEventName = getRawEventName(topLevelType);
        var eventFlags = RESPONDER_EVENT_SYSTEM; // If passive option is not supported, then the event will be
        // active and not passive, but we flag it as using not being
        // supported too. This way the responder event plugins know,
        // and can provide polyfills if needed.

        if (passive) {
          if (passiveBrowserEventsSupported) {
            eventFlags |= IS_PASSIVE;
          } else {
            eventFlags |= IS_ACTIVE;
            eventFlags |= PASSIVE_NOT_SUPPORTED;
            passive = false;
          }
        } else {
          eventFlags |= IS_ACTIVE;
        } // Check if interactive and wrap in discreteUpdates


        var listener = dispatchEvent.bind(null, topLevelType, eventFlags);

        if (passiveBrowserEventsSupported) {
          addEventCaptureListenerWithPassiveFlag(element, rawEventName, listener, passive);
        } else {
          addEventCaptureListener(element, rawEventName, listener);
        }
      }
    }

    function trapEventForPluginEventSystem(element, topLevelType, capture) {
      var listener;

      switch (getEventPriority(topLevelType)) {
        case DiscreteEvent:
          listener = dispatchDiscreteEvent.bind(null, topLevelType, PLUGIN_EVENT_SYSTEM);
          break;

        case UserBlockingEvent:
          listener = dispatchUserBlockingUpdate.bind(null, topLevelType, PLUGIN_EVENT_SYSTEM);
          break;

        case ContinuousEvent:
        default:
          listener = dispatchEvent.bind(null, topLevelType, PLUGIN_EVENT_SYSTEM);
          break;
      }

      var rawEventName = getRawEventName(topLevelType);

      if (capture) {
        addEventCaptureListener(element, rawEventName, listener);
      } else {
        addEventBubbleListener(element, rawEventName, listener);
      }
    }

    function dispatchDiscreteEvent(topLevelType, eventSystemFlags, nativeEvent) {
      flushDiscreteUpdatesIfNeeded(nativeEvent.timeStamp);
      discreteUpdates(dispatchEvent, topLevelType, eventSystemFlags, nativeEvent);
    }

    function dispatchUserBlockingUpdate(topLevelType, eventSystemFlags, nativeEvent) {
      if (enableUserBlockingEvents) {
        runWithPriority$1(UserBlockingPriority$1, dispatchEvent.bind(null, topLevelType, eventSystemFlags, nativeEvent));
      } else {
        dispatchEvent(topLevelType, eventSystemFlags, nativeEvent);
      }
    }

    function dispatchEventForPluginEventSystem(topLevelType, eventSystemFlags, nativeEvent, targetInst) {
      var bookKeeping = getTopLevelCallbackBookKeeping(topLevelType, nativeEvent, targetInst, eventSystemFlags);

      try {
        // Event queue being processed in the same cycle allows
        // `preventDefault`.
        batchedEventUpdates(handleTopLevel, bookKeeping);
      } finally {
        releaseTopLevelCallbackBookKeeping(bookKeeping);
      }
    }

    function dispatchEvent(topLevelType, eventSystemFlags, nativeEvent) {
      if (!_enabled) {
        return;
      }

      if (hasQueuedDiscreteEvents() && isReplayableDiscreteEvent(topLevelType)) {
        // If we already have a queue of discrete events, and this is another discrete
        // event, then we can't dispatch it regardless of its target, since they
        // need to dispatch in order.
        queueDiscreteEvent(null, // Flags that we're not actually blocked on anything as far as we know.
        topLevelType, eventSystemFlags, nativeEvent);
        return;
      }

      var blockedOn = attemptToDispatchEvent(topLevelType, eventSystemFlags, nativeEvent);

      if (blockedOn === null) {
        // We successfully dispatched this event.
        clearIfContinuousEvent(topLevelType, nativeEvent);
        return;
      }

      if (isReplayableDiscreteEvent(topLevelType)) {
        // This this to be replayed later once the target is available.
        queueDiscreteEvent(blockedOn, topLevelType, eventSystemFlags, nativeEvent);
        return;
      }

      if (queueIfContinuousEvent(blockedOn, topLevelType, eventSystemFlags, nativeEvent)) {
        return;
      } // We need to clear only if we didn't queue because
      // queueing is accummulative.


      clearIfContinuousEvent(topLevelType, nativeEvent); // This is not replayable so we'll invoke it but without a target,
      // in case the event system needs to trace it.

      if (enableFlareAPI) {
        if (eventSystemFlags & PLUGIN_EVENT_SYSTEM) {
          dispatchEventForPluginEventSystem(topLevelType, eventSystemFlags, nativeEvent, null);
        }

        if (eventSystemFlags & RESPONDER_EVENT_SYSTEM) {
          // React Flare event system
          dispatchEventForResponderEventSystem(topLevelType, null, nativeEvent, getEventTarget(nativeEvent), eventSystemFlags);
        }
      } else {
        dispatchEventForPluginEventSystem(topLevelType, eventSystemFlags, nativeEvent, null);
      }
    } // Attempt dispatching an event. Returns a SuspenseInstance or Container if it's blocked.


    function attemptToDispatchEvent(topLevelType, eventSystemFlags, nativeEvent) {
      // TODO: Warn if _enabled is false.
      var nativeEventTarget = getEventTarget(nativeEvent);
      var targetInst = getClosestInstanceFromNode(nativeEventTarget);

      if (targetInst !== null) {
        var nearestMounted = getNearestMountedFiber(targetInst);

        if (nearestMounted === null) {
          // This tree has been unmounted already. Dispatch without a target.
          targetInst = null;
        } else {
          var tag = nearestMounted.tag;

          if (tag === SuspenseComponent) {
            var instance = getSuspenseInstanceFromFiber(nearestMounted);

            if (instance !== null) {
              // Queue the event to be replayed later. Abort dispatching since we
              // don't want this event dispatched twice through the event system.
              // TODO: If this is the first discrete event in the queue. Schedule an increased
              // priority for this boundary.
              return instance;
            } // This shouldn't happen, something went wrong but to avoid blocking
            // the whole system, dispatch the event without a target.
            // TODO: Warn.


            targetInst = null;
          } else if (tag === HostRoot) {
            var root = nearestMounted.stateNode;

            if (root.hydrate) {
              // If this happens during a replay something went wrong and it might block
              // the whole system.
              return getContainerFromFiber(nearestMounted);
            }

            targetInst = null;
          } else if (nearestMounted !== targetInst) {
            // If we get an event (ex: img onload) before committing that
            // component's mount, ignore it for now (that is, treat it as if it was an
            // event on a non-React tree). We might also consider queueing events and
            // dispatching them after the mount.
            targetInst = null;
          }
        }
      }

      if (enableFlareAPI) {
        if (eventSystemFlags & PLUGIN_EVENT_SYSTEM) {
          dispatchEventForPluginEventSystem(topLevelType, eventSystemFlags, nativeEvent, targetInst);
        }

        if (eventSystemFlags & RESPONDER_EVENT_SYSTEM) {
          // React Flare event system
          dispatchEventForResponderEventSystem(topLevelType, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags);
        }
      } else {
        dispatchEventForPluginEventSystem(topLevelType, eventSystemFlags, nativeEvent, targetInst);
      } // We're not blocked on anything.


      return null;
    }
    /**
     * Checks if an event is supported in the current execution environment.
     *
     * NOTE: This will not work correctly for non-generic events such as `change`,
     * `reset`, `load`, `error`, and `select`.
     *
     * Borrows from Modernizr.
     *
     * @param {string} eventNameSuffix Event name, e.g. "click".
     * @return {boolean} True if the event is supported.
     * @internal
     * @license Modernizr 3.0.0pre (Custom Build) | MIT
     */


    function isEventSupported(eventNameSuffix) {
      if (!canUseDOM) {
        return false;
      }

      var eventName = 'on' + eventNameSuffix;
      var isSupported = eventName in document;

      if (!isSupported) {
        var element = document.createElement('div');
        element.setAttribute(eventName, 'return;');
        isSupported = typeof element[eventName] === 'function';
      }

      return isSupported;
    }
    /**
     * Summary of `ReactBrowserEventEmitter` event handling:
     *
     *  - Top-level delegation is used to trap most native browser events. This
     *    may only occur in the main thread and is the responsibility of
     *    ReactDOMEventListener, which is injected and can therefore support
     *    pluggable event sources. This is the only work that occurs in the main
     *    thread.
     *
     *  - We normalize and de-duplicate events to account for browser quirks. This
     *    may be done in the worker thread.
     *
     *  - Forward these native events (with the associated top-level type used to
     *    trap it) to `EventPluginHub`, which in turn will ask plugins if they want
     *    to extract any synthetic events.
     *
     *  - The `EventPluginHub` will then process each event by annotating them with
     *    "dispatches", a sequence of listeners and IDs that care about that event.
     *
     *  - The `EventPluginHub` then dispatches the events.
     *
     * Overview of React and the event system:
     *
     * +------------+    .
     * |    DOM     |    .
     * +------------+    .
     *       |           .
     *       v           .
     * +------------+    .
     * | ReactEvent |    .
     * |  Listener  |    .
     * +------------+    .                         +-----------+
     *       |           .               +--------+|SimpleEvent|
     *       |           .               |         |Plugin     |
     * +-----|------+    .               v         +-----------+
     * |     |      |    .    +--------------+                    +------------+
     * |     +-----------.--->|EventPluginHub|                    |    Event   |
     * |            |    .    |              |     +-----------+  | Propagators|
     * | ReactEvent |    .    |              |     |TapEvent   |  |------------|
     * |  Emitter   |    .    |              |<---+|Plugin     |  |other plugin|
     * |            |    .    |              |     +-----------+  |  utilities |
     * |     +-----------.--->|              |                    +------------+
     * |     |      |    .    +--------------+
     * +-----|------+    .                ^        +-----------+
     *       |           .                |        |Enter/Leave|
     *       +           .                +-------+|Plugin     |
     * +-------------+   .                         +-----------+
     * | application |   .
     * |-------------|   .
     * |             |   .
     * |             |   .
     * +-------------+   .
     *                   .
     *    React Core     .  General Purpose Event Plugin System
     */


    var PossiblyWeakMap = typeof WeakMap === 'function' ? WeakMap : Map;
    var elementListeningSets = new PossiblyWeakMap();

    function getListeningSetForElement(element) {
      var listeningSet = elementListeningSets.get(element);

      if (listeningSet === undefined) {
        listeningSet = new Set();
        elementListeningSets.set(element, listeningSet);
      }

      return listeningSet;
    }
    /**
     * We listen for bubbled touch events on the document object.
     *
     * Firefox v8.01 (and possibly others) exhibited strange behavior when
     * mounting `onmousemove` events at some node that was not the document
     * element. The symptoms were that if your mouse is not moving over something
     * contained within that mount point (for example on the background) the
     * top-level listeners for `onmousemove` won't be called. However, if you
     * register the `mousemove` on the document object, then it will of course
     * catch all `mousemove`s. This along with iOS quirks, justifies restricting
     * top-level listeners to the document object only, at least for these
     * movement types of events and possibly all events.
     *
     * @see http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
     *
     * Also, `keyup`/`keypress`/`keydown` do not bubble to the window on IE, but
     * they bubble to document.
     *
     * @param {string} registrationName Name of listener (e.g. `onClick`).
     * @param {object} mountAt Container where to mount the listener
     */


    function listenTo(registrationName, mountAt) {
      var listeningSet = getListeningSetForElement(mountAt);
      var dependencies = registrationNameDependencies[registrationName];

      for (var i = 0; i < dependencies.length; i++) {
        var dependency = dependencies[i];
        listenToTopLevel(dependency, mountAt, listeningSet);
      }
    }

    function listenToTopLevel(topLevelType, mountAt, listeningSet) {
      if (!listeningSet.has(topLevelType)) {
        switch (topLevelType) {
          case TOP_SCROLL:
            trapCapturedEvent(TOP_SCROLL, mountAt);
            break;

          case TOP_FOCUS:
          case TOP_BLUR:
            trapCapturedEvent(TOP_FOCUS, mountAt);
            trapCapturedEvent(TOP_BLUR, mountAt); // We set the flag for a single dependency later in this function,
            // but this ensures we mark both as attached rather than just one.

            listeningSet.add(TOP_BLUR);
            listeningSet.add(TOP_FOCUS);
            break;

          case TOP_CANCEL:
          case TOP_CLOSE:
            if (isEventSupported(getRawEventName(topLevelType))) {
              trapCapturedEvent(topLevelType, mountAt);
            }

            break;

          case TOP_INVALID:
          case TOP_SUBMIT:
          case TOP_RESET:
            // We listen to them on the target DOM elements.
            // Some of them bubble so we don't want them to fire twice.
            break;

          default:
            // By default, listen on the top level to all non-media events.
            // Media events don't bubble so adding the listener wouldn't do anything.
            var isMediaEvent = mediaEventTypes.indexOf(topLevelType) !== -1;

            if (!isMediaEvent) {
              trapBubbledEvent(topLevelType, mountAt);
            }

            break;
        }

        listeningSet.add(topLevelType);
      }
    }

    function isListeningToAllDependencies(registrationName, mountAt) {
      var listeningSet = getListeningSetForElement(mountAt);
      var dependencies = registrationNameDependencies[registrationName];

      for (var i = 0; i < dependencies.length; i++) {
        var dependency = dependencies[i];

        if (!listeningSet.has(dependency)) {
          return false;
        }
      }

      return true;
    } // List derived from Gecko source code:
    // https://github.com/mozilla/gecko-dev/blob/4e638efc71/layout/style/test/property_database.js


    var shorthandToLonghand = {
      animation: ['animationDelay', 'animationDirection', 'animationDuration', 'animationFillMode', 'animationIterationCount', 'animationName', 'animationPlayState', 'animationTimingFunction'],
      background: ['backgroundAttachment', 'backgroundClip', 'backgroundColor', 'backgroundImage', 'backgroundOrigin', 'backgroundPositionX', 'backgroundPositionY', 'backgroundRepeat', 'backgroundSize'],
      backgroundPosition: ['backgroundPositionX', 'backgroundPositionY'],
      border: ['borderBottomColor', 'borderBottomStyle', 'borderBottomWidth', 'borderImageOutset', 'borderImageRepeat', 'borderImageSlice', 'borderImageSource', 'borderImageWidth', 'borderLeftColor', 'borderLeftStyle', 'borderLeftWidth', 'borderRightColor', 'borderRightStyle', 'borderRightWidth', 'borderTopColor', 'borderTopStyle', 'borderTopWidth'],
      borderBlockEnd: ['borderBlockEndColor', 'borderBlockEndStyle', 'borderBlockEndWidth'],
      borderBlockStart: ['borderBlockStartColor', 'borderBlockStartStyle', 'borderBlockStartWidth'],
      borderBottom: ['borderBottomColor', 'borderBottomStyle', 'borderBottomWidth'],
      borderColor: ['borderBottomColor', 'borderLeftColor', 'borderRightColor', 'borderTopColor'],
      borderImage: ['borderImageOutset', 'borderImageRepeat', 'borderImageSlice', 'borderImageSource', 'borderImageWidth'],
      borderInlineEnd: ['borderInlineEndColor', 'borderInlineEndStyle', 'borderInlineEndWidth'],
      borderInlineStart: ['borderInlineStartColor', 'borderInlineStartStyle', 'borderInlineStartWidth'],
      borderLeft: ['borderLeftColor', 'borderLeftStyle', 'borderLeftWidth'],
      borderRadius: ['borderBottomLeftRadius', 'borderBottomRightRadius', 'borderTopLeftRadius', 'borderTopRightRadius'],
      borderRight: ['borderRightColor', 'borderRightStyle', 'borderRightWidth'],
      borderStyle: ['borderBottomStyle', 'borderLeftStyle', 'borderRightStyle', 'borderTopStyle'],
      borderTop: ['borderTopColor', 'borderTopStyle', 'borderTopWidth'],
      borderWidth: ['borderBottomWidth', 'borderLeftWidth', 'borderRightWidth', 'borderTopWidth'],
      columnRule: ['columnRuleColor', 'columnRuleStyle', 'columnRuleWidth'],
      columns: ['columnCount', 'columnWidth'],
      flex: ['flexBasis', 'flexGrow', 'flexShrink'],
      flexFlow: ['flexDirection', 'flexWrap'],
      font: ['fontFamily', 'fontFeatureSettings', 'fontKerning', 'fontLanguageOverride', 'fontSize', 'fontSizeAdjust', 'fontStretch', 'fontStyle', 'fontVariant', 'fontVariantAlternates', 'fontVariantCaps', 'fontVariantEastAsian', 'fontVariantLigatures', 'fontVariantNumeric', 'fontVariantPosition', 'fontWeight', 'lineHeight'],
      fontVariant: ['fontVariantAlternates', 'fontVariantCaps', 'fontVariantEastAsian', 'fontVariantLigatures', 'fontVariantNumeric', 'fontVariantPosition'],
      gap: ['columnGap', 'rowGap'],
      grid: ['gridAutoColumns', 'gridAutoFlow', 'gridAutoRows', 'gridTemplateAreas', 'gridTemplateColumns', 'gridTemplateRows'],
      gridArea: ['gridColumnEnd', 'gridColumnStart', 'gridRowEnd', 'gridRowStart'],
      gridColumn: ['gridColumnEnd', 'gridColumnStart'],
      gridColumnGap: ['columnGap'],
      gridGap: ['columnGap', 'rowGap'],
      gridRow: ['gridRowEnd', 'gridRowStart'],
      gridRowGap: ['rowGap'],
      gridTemplate: ['gridTemplateAreas', 'gridTemplateColumns', 'gridTemplateRows'],
      listStyle: ['listStyleImage', 'listStylePosition', 'listStyleType'],
      margin: ['marginBottom', 'marginLeft', 'marginRight', 'marginTop'],
      marker: ['markerEnd', 'markerMid', 'markerStart'],
      mask: ['maskClip', 'maskComposite', 'maskImage', 'maskMode', 'maskOrigin', 'maskPositionX', 'maskPositionY', 'maskRepeat', 'maskSize'],
      maskPosition: ['maskPositionX', 'maskPositionY'],
      outline: ['outlineColor', 'outlineStyle', 'outlineWidth'],
      overflow: ['overflowX', 'overflowY'],
      padding: ['paddingBottom', 'paddingLeft', 'paddingRight', 'paddingTop'],
      placeContent: ['alignContent', 'justifyContent'],
      placeItems: ['alignItems', 'justifyItems'],
      placeSelf: ['alignSelf', 'justifySelf'],
      textDecoration: ['textDecorationColor', 'textDecorationLine', 'textDecorationStyle'],
      textEmphasis: ['textEmphasisColor', 'textEmphasisStyle'],
      transition: ['transitionDelay', 'transitionDuration', 'transitionProperty', 'transitionTimingFunction'],
      wordWrap: ['overflowWrap']
    };
    /**
     * CSS properties which accept numbers but are not in units of "px".
     */

    var isUnitlessNumber = {
      animationIterationCount: true,
      borderImageOutset: true,
      borderImageSlice: true,
      borderImageWidth: true,
      boxFlex: true,
      boxFlexGroup: true,
      boxOrdinalGroup: true,
      columnCount: true,
      columns: true,
      flex: true,
      flexGrow: true,
      flexPositive: true,
      flexShrink: true,
      flexNegative: true,
      flexOrder: true,
      gridArea: true,
      gridRow: true,
      gridRowEnd: true,
      gridRowSpan: true,
      gridRowStart: true,
      gridColumn: true,
      gridColumnEnd: true,
      gridColumnSpan: true,
      gridColumnStart: true,
      fontWeight: true,
      lineClamp: true,
      lineHeight: true,
      opacity: true,
      order: true,
      orphans: true,
      tabSize: true,
      widows: true,
      zIndex: true,
      zoom: true,
      // SVG-related properties
      fillOpacity: true,
      floodOpacity: true,
      stopOpacity: true,
      strokeDasharray: true,
      strokeDashoffset: true,
      strokeMiterlimit: true,
      strokeOpacity: true,
      strokeWidth: true
    };
    /**
     * @param {string} prefix vendor-specific prefix, eg: Webkit
     * @param {string} key style name, eg: transitionDuration
     * @return {string} style name prefixed with `prefix`, properly camelCased, eg:
     * WebkitTransitionDuration
     */

    function prefixKey(prefix, key) {
      return prefix + key.charAt(0).toUpperCase() + key.substring(1);
    }
    /**
     * Support style names that may come passed in prefixed by adding permutations
     * of vendor prefixes.
     */


    var prefixes = ['Webkit', 'ms', 'Moz', 'O']; // Using Object.keys here, or else the vanilla for-in loop makes IE8 go into an
    // infinite loop, because it iterates over the newly added props too.

    Object.keys(isUnitlessNumber).forEach(function (prop) {
      prefixes.forEach(function (prefix) {
        isUnitlessNumber[prefixKey(prefix, prop)] = isUnitlessNumber[prop];
      });
    });
    /**
     * Convert a value into the proper css writable value. The style name `name`
     * should be logical (no hyphens), as specified
     * in `CSSProperty.isUnitlessNumber`.
     *
     * @param {string} name CSS property name such as `topMargin`.
     * @param {*} value CSS property value such as `10px`.
     * @return {string} Normalized style value with dimensions applied.
     */

    function dangerousStyleValue(name, value, isCustomProperty) {
      // Note that we've removed escapeTextForBrowser() calls here since the
      // whole string will be escaped when the attribute is injected into
      // the markup. If you provide unsafe user data here they can inject
      // arbitrary CSS which may be problematic (I couldn't repro this):
      // https://www.owasp.org/index.php/XSS_Filter_Evasion_Cheat_Sheet
      // http://www.thespanner.co.uk/2007/11/26/ultimate-xss-css-injection/
      // This is not an XSS hole but instead a potential CSS injection issue
      // which has lead to a greater discussion about how we're going to
      // trust URLs moving forward. See #2115901
      var isEmpty = value == null || typeof value === 'boolean' || value === '';

      if (isEmpty) {
        return '';
      }

      if (!isCustomProperty && typeof value === 'number' && value !== 0 && !(isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name])) {
        return value + 'px'; // Presumes implicit 'px' suffix for unitless numbers
      }

      return ('' + value).trim();
    }

    var uppercasePattern = /([A-Z])/g;
    var msPattern = /^ms-/;
    /**
     * Hyphenates a camelcased CSS property name, for example:
     *
     *   > hyphenateStyleName('backgroundColor')
     *   < "background-color"
     *   > hyphenateStyleName('MozTransition')
     *   < "-moz-transition"
     *   > hyphenateStyleName('msTransition')
     *   < "-ms-transition"
     *
     * As Modernizr suggests (http://modernizr.com/docs/#prefixed), an `ms` prefix
     * is converted to `-ms-`.
     */

    function hyphenateStyleName(name) {
      return name.replace(uppercasePattern, '-$1').toLowerCase().replace(msPattern, '-ms-');
    }

    var warnValidStyle = function () {};

    {
      // 'msTransform' is correct, but the other prefixes should be capitalized
      var badVendoredStyleNamePattern = /^(?:webkit|moz|o)[A-Z]/;
      var msPattern$1 = /^-ms-/;
      var hyphenPattern = /-(.)/g; // style values shouldn't contain a semicolon

      var badStyleValueWithSemicolonPattern = /;\s*$/;
      var warnedStyleNames = {};
      var warnedStyleValues = {};
      var warnedForNaNValue = false;
      var warnedForInfinityValue = false;

      var camelize = function (string) {
        return string.replace(hyphenPattern, function (_, character) {
          return character.toUpperCase();
        });
      };

      var warnHyphenatedStyleName = function (name) {
        if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
          return;
        }

        warnedStyleNames[name] = true;
        warning$1(false, 'Unsupported style property %s. Did you mean %s?', name, // As Andi Smith suggests
        // (http://www.andismith.com/blog/2012/02/modernizr-prefixed/), an `-ms` prefix
        // is converted to lowercase `ms`.
        camelize(name.replace(msPattern$1, 'ms-')));
      };

      var warnBadVendoredStyleName = function (name) {
        if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
          return;
        }

        warnedStyleNames[name] = true;
        warning$1(false, 'Unsupported vendor-prefixed style property %s. Did you mean %s?', name, name.charAt(0).toUpperCase() + name.slice(1));
      };

      var warnStyleValueWithSemicolon = function (name, value) {
        if (warnedStyleValues.hasOwnProperty(value) && warnedStyleValues[value]) {
          return;
        }

        warnedStyleValues[value] = true;
        warning$1(false, "Style property values shouldn't contain a semicolon. " + 'Try "%s: %s" instead.', name, value.replace(badStyleValueWithSemicolonPattern, ''));
      };

      var warnStyleValueIsNaN = function (name, value) {
        if (warnedForNaNValue) {
          return;
        }

        warnedForNaNValue = true;
        warning$1(false, '`NaN` is an invalid value for the `%s` css style property.', name);
      };

      var warnStyleValueIsInfinity = function (name, value) {
        if (warnedForInfinityValue) {
          return;
        }

        warnedForInfinityValue = true;
        warning$1(false, '`Infinity` is an invalid value for the `%s` css style property.', name);
      };

      warnValidStyle = function (name, value) {
        if (name.indexOf('-') > -1) {
          warnHyphenatedStyleName(name);
        } else if (badVendoredStyleNamePattern.test(name)) {
          warnBadVendoredStyleName(name);
        } else if (badStyleValueWithSemicolonPattern.test(value)) {
          warnStyleValueWithSemicolon(name, value);
        }

        if (typeof value === 'number') {
          if (isNaN(value)) {
            warnStyleValueIsNaN(name, value);
          } else if (!isFinite(value)) {
            warnStyleValueIsInfinity(name, value);
          }
        }
      };
    }
    var warnValidStyle$1 = warnValidStyle;
    /**
     * Operations for dealing with CSS properties.
     */

    /**
     * This creates a string that is expected to be equivalent to the style
     * attribute generated by server-side rendering. It by-passes warnings and
     * security checks so it's not safe to use this value for anything other than
     * comparison. It is only used in DEV for SSR validation.
     */

    function createDangerousStringForStyles(styles) {
      {
        var serialized = '';
        var delimiter = '';

        for (var styleName in styles) {
          if (!styles.hasOwnProperty(styleName)) {
            continue;
          }

          var styleValue = styles[styleName];

          if (styleValue != null) {
            var isCustomProperty = styleName.indexOf('--') === 0;
            serialized += delimiter + (isCustomProperty ? styleName : hyphenateStyleName(styleName)) + ':';
            serialized += dangerousStyleValue(styleName, styleValue, isCustomProperty);
            delimiter = ';';
          }
        }

        return serialized || null;
      }
    }
    /**
     * Sets the value for multiple styles on a node.  If a value is specified as
     * '' (empty string), the corresponding style property will be unset.
     *
     * @param {DOMElement} node
     * @param {object} styles
     */


    function setValueForStyles(node, styles) {
      var style = node.style;

      for (var styleName in styles) {
        if (!styles.hasOwnProperty(styleName)) {
          continue;
        }

        var isCustomProperty = styleName.indexOf('--') === 0;
        {
          if (!isCustomProperty) {
            warnValidStyle$1(styleName, styles[styleName]);
          }
        }
        var styleValue = dangerousStyleValue(styleName, styles[styleName], isCustomProperty);

        if (styleName === 'float') {
          styleName = 'cssFloat';
        }

        if (isCustomProperty) {
          style.setProperty(styleName, styleValue);
        } else {
          style[styleName] = styleValue;
        }
      }
    }

    function isValueEmpty(value) {
      return value == null || typeof value === 'boolean' || value === '';
    }
    /**
     * Given {color: 'red', overflow: 'hidden'} returns {
     *   color: 'color',
     *   overflowX: 'overflow',
     *   overflowY: 'overflow',
     * }. This can be read as "the overflowY property was set by the overflow
     * shorthand". That is, the values are the property that each was derived from.
     */


    function expandShorthandMap(styles) {
      var expanded = {};

      for (var key in styles) {
        var longhands = shorthandToLonghand[key] || [key];

        for (var i = 0; i < longhands.length; i++) {
          expanded[longhands[i]] = key;
        }
      }

      return expanded;
    }
    /**
     * When mixing shorthand and longhand property names, we warn during updates if
     * we expect an incorrect result to occur. In particular, we warn for:
     *
     * Updating a shorthand property (longhand gets overwritten):
     *   {font: 'foo', fontVariant: 'bar'} -> {font: 'baz', fontVariant: 'bar'}
     *   becomes .style.font = 'baz'
     * Removing a shorthand property (longhand gets lost too):
     *   {font: 'foo', fontVariant: 'bar'} -> {fontVariant: 'bar'}
     *   becomes .style.font = ''
     * Removing a longhand property (should revert to shorthand; doesn't):
     *   {font: 'foo', fontVariant: 'bar'} -> {font: 'foo'}
     *   becomes .style.fontVariant = ''
     */


    function validateShorthandPropertyCollisionInDev(styleUpdates, nextStyles) {
      if (!warnAboutShorthandPropertyCollision) {
        return;
      }

      if (!nextStyles) {
        return;
      }

      var expandedUpdates = expandShorthandMap(styleUpdates);
      var expandedStyles = expandShorthandMap(nextStyles);
      var warnedAbout = {};

      for (var key in expandedUpdates) {
        var originalKey = expandedUpdates[key];
        var correctOriginalKey = expandedStyles[key];

        if (correctOriginalKey && originalKey !== correctOriginalKey) {
          var warningKey = originalKey + ',' + correctOriginalKey;

          if (warnedAbout[warningKey]) {
            continue;
          }

          warnedAbout[warningKey] = true;
          warning$1(false, '%s a style property during rerender (%s) when a ' + 'conflicting property is set (%s) can lead to styling bugs. To ' + "avoid this, don't mix shorthand and non-shorthand properties " + 'for the same value; instead, replace the shorthand with ' + 'separate values.', isValueEmpty(styleUpdates[originalKey]) ? 'Removing' : 'Updating', originalKey, correctOriginalKey);
        }
      }
    } // For HTML, certain tags should omit their close tag. We keep a whitelist for
    // those special-case tags.


    var omittedCloseTags = {
      area: true,
      base: true,
      br: true,
      col: true,
      embed: true,
      hr: true,
      img: true,
      input: true,
      keygen: true,
      link: true,
      meta: true,
      param: true,
      source: true,
      track: true,
      wbr: true // NOTE: menuitem's close tag should be omitted, but that causes problems.

    }; // `omittedCloseTags` except that `menuitem` should still have its closing tag.

    var voidElementTags = _assign({
      menuitem: true
    }, omittedCloseTags); // or add stack by default to invariants where possible.


    var HTML$1 = '__html';
    var ReactDebugCurrentFrame$3 = null;
    {
      ReactDebugCurrentFrame$3 = ReactSharedInternals.ReactDebugCurrentFrame;
    }

    function assertValidProps(tag, props) {
      if (!props) {
        return;
      } // Note the use of `==` which checks for null or undefined.


      if (voidElementTags[tag]) {
        (function () {
          if (!(props.children == null && props.dangerouslySetInnerHTML == null)) {
            {
              throw ReactError(Error(tag + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`." + ReactDebugCurrentFrame$3.getStackAddendum()));
            }
          }
        })();
      }

      if (props.dangerouslySetInnerHTML != null) {
        (function () {
          if (!(props.children == null)) {
            {
              throw ReactError(Error("Can only set one of `children` or `props.dangerouslySetInnerHTML`."));
            }
          }
        })();

        (function () {
          if (!(typeof props.dangerouslySetInnerHTML === 'object' && HTML$1 in props.dangerouslySetInnerHTML)) {
            {
              throw ReactError(Error("`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://fb.me/react-invariant-dangerously-set-inner-html for more information."));
            }
          }
        })();
      }

      {
        !(props.suppressContentEditableWarning || !props.contentEditable || props.children == null) ? warning$1(false, 'A component is `contentEditable` and contains `children` managed by ' + 'React. It is now your responsibility to guarantee that none of ' + 'those nodes are unexpectedly modified or duplicated. This is ' + 'probably not intentional.') : void 0;
      }

      (function () {
        if (!(props.style == null || typeof props.style === 'object')) {
          {
            throw ReactError(Error("The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + 'em'}} when using JSX." + ReactDebugCurrentFrame$3.getStackAddendum()));
          }
        }
      })();
    }

    function isCustomComponent(tagName, props) {
      if (tagName.indexOf('-') === -1) {
        return typeof props.is === 'string';
      }

      switch (tagName) {
        // These are reserved SVG and MathML elements.
        // We don't mind this whitelist too much because we expect it to never grow.
        // The alternative is to track the namespace in a few places which is convoluted.
        // https://w3c.github.io/webcomponents/spec/custom/#custom-elements-core-concepts
        case 'annotation-xml':
        case 'color-profile':
        case 'font-face':
        case 'font-face-src':
        case 'font-face-uri':
        case 'font-face-format':
        case 'font-face-name':
        case 'missing-glyph':
          return false;

        default:
          return true;
      }
    } // When adding attributes to the HTML or SVG whitelist, be sure to
    // also add them to this module to ensure casing and incorrect name
    // warnings.


    var possibleStandardNames = {
      // HTML
      accept: 'accept',
      acceptcharset: 'acceptCharset',
      'accept-charset': 'acceptCharset',
      accesskey: 'accessKey',
      action: 'action',
      allowfullscreen: 'allowFullScreen',
      alt: 'alt',
      as: 'as',
      async: 'async',
      autocapitalize: 'autoCapitalize',
      autocomplete: 'autoComplete',
      autocorrect: 'autoCorrect',
      autofocus: 'autoFocus',
      autoplay: 'autoPlay',
      autosave: 'autoSave',
      capture: 'capture',
      cellpadding: 'cellPadding',
      cellspacing: 'cellSpacing',
      challenge: 'challenge',
      charset: 'charSet',
      checked: 'checked',
      children: 'children',
      cite: 'cite',
      class: 'className',
      classid: 'classID',
      classname: 'className',
      cols: 'cols',
      colspan: 'colSpan',
      content: 'content',
      contenteditable: 'contentEditable',
      contextmenu: 'contextMenu',
      controls: 'controls',
      controlslist: 'controlsList',
      coords: 'coords',
      crossorigin: 'crossOrigin',
      dangerouslysetinnerhtml: 'dangerouslySetInnerHTML',
      data: 'data',
      datetime: 'dateTime',
      default: 'default',
      defaultchecked: 'defaultChecked',
      defaultvalue: 'defaultValue',
      defer: 'defer',
      dir: 'dir',
      disabled: 'disabled',
      disablepictureinpicture: 'disablePictureInPicture',
      download: 'download',
      draggable: 'draggable',
      enctype: 'encType',
      for: 'htmlFor',
      form: 'form',
      formmethod: 'formMethod',
      formaction: 'formAction',
      formenctype: 'formEncType',
      formnovalidate: 'formNoValidate',
      formtarget: 'formTarget',
      frameborder: 'frameBorder',
      headers: 'headers',
      height: 'height',
      hidden: 'hidden',
      high: 'high',
      href: 'href',
      hreflang: 'hrefLang',
      htmlfor: 'htmlFor',
      httpequiv: 'httpEquiv',
      'http-equiv': 'httpEquiv',
      icon: 'icon',
      id: 'id',
      innerhtml: 'innerHTML',
      inputmode: 'inputMode',
      integrity: 'integrity',
      is: 'is',
      itemid: 'itemID',
      itemprop: 'itemProp',
      itemref: 'itemRef',
      itemscope: 'itemScope',
      itemtype: 'itemType',
      keyparams: 'keyParams',
      keytype: 'keyType',
      kind: 'kind',
      label: 'label',
      lang: 'lang',
      list: 'list',
      loop: 'loop',
      low: 'low',
      manifest: 'manifest',
      marginwidth: 'marginWidth',
      marginheight: 'marginHeight',
      max: 'max',
      maxlength: 'maxLength',
      media: 'media',
      mediagroup: 'mediaGroup',
      method: 'method',
      min: 'min',
      minlength: 'minLength',
      multiple: 'multiple',
      muted: 'muted',
      name: 'name',
      nomodule: 'noModule',
      nonce: 'nonce',
      novalidate: 'noValidate',
      open: 'open',
      optimum: 'optimum',
      pattern: 'pattern',
      placeholder: 'placeholder',
      playsinline: 'playsInline',
      poster: 'poster',
      preload: 'preload',
      profile: 'profile',
      radiogroup: 'radioGroup',
      readonly: 'readOnly',
      referrerpolicy: 'referrerPolicy',
      rel: 'rel',
      required: 'required',
      reversed: 'reversed',
      role: 'role',
      rows: 'rows',
      rowspan: 'rowSpan',
      sandbox: 'sandbox',
      scope: 'scope',
      scoped: 'scoped',
      scrolling: 'scrolling',
      seamless: 'seamless',
      selected: 'selected',
      shape: 'shape',
      size: 'size',
      sizes: 'sizes',
      span: 'span',
      spellcheck: 'spellCheck',
      src: 'src',
      srcdoc: 'srcDoc',
      srclang: 'srcLang',
      srcset: 'srcSet',
      start: 'start',
      step: 'step',
      style: 'style',
      summary: 'summary',
      tabindex: 'tabIndex',
      target: 'target',
      title: 'title',
      type: 'type',
      usemap: 'useMap',
      value: 'value',
      width: 'width',
      wmode: 'wmode',
      wrap: 'wrap',
      // SVG
      about: 'about',
      accentheight: 'accentHeight',
      'accent-height': 'accentHeight',
      accumulate: 'accumulate',
      additive: 'additive',
      alignmentbaseline: 'alignmentBaseline',
      'alignment-baseline': 'alignmentBaseline',
      allowreorder: 'allowReorder',
      alphabetic: 'alphabetic',
      amplitude: 'amplitude',
      arabicform: 'arabicForm',
      'arabic-form': 'arabicForm',
      ascent: 'ascent',
      attributename: 'attributeName',
      attributetype: 'attributeType',
      autoreverse: 'autoReverse',
      azimuth: 'azimuth',
      basefrequency: 'baseFrequency',
      baselineshift: 'baselineShift',
      'baseline-shift': 'baselineShift',
      baseprofile: 'baseProfile',
      bbox: 'bbox',
      begin: 'begin',
      bias: 'bias',
      by: 'by',
      calcmode: 'calcMode',
      capheight: 'capHeight',
      'cap-height': 'capHeight',
      clip: 'clip',
      clippath: 'clipPath',
      'clip-path': 'clipPath',
      clippathunits: 'clipPathUnits',
      cliprule: 'clipRule',
      'clip-rule': 'clipRule',
      color: 'color',
      colorinterpolation: 'colorInterpolation',
      'color-interpolation': 'colorInterpolation',
      colorinterpolationfilters: 'colorInterpolationFilters',
      'color-interpolation-filters': 'colorInterpolationFilters',
      colorprofile: 'colorProfile',
      'color-profile': 'colorProfile',
      colorrendering: 'colorRendering',
      'color-rendering': 'colorRendering',
      contentscripttype: 'contentScriptType',
      contentstyletype: 'contentStyleType',
      cursor: 'cursor',
      cx: 'cx',
      cy: 'cy',
      d: 'd',
      datatype: 'datatype',
      decelerate: 'decelerate',
      descent: 'descent',
      diffuseconstant: 'diffuseConstant',
      direction: 'direction',
      display: 'display',
      divisor: 'divisor',
      dominantbaseline: 'dominantBaseline',
      'dominant-baseline': 'dominantBaseline',
      dur: 'dur',
      dx: 'dx',
      dy: 'dy',
      edgemode: 'edgeMode',
      elevation: 'elevation',
      enablebackground: 'enableBackground',
      'enable-background': 'enableBackground',
      end: 'end',
      exponent: 'exponent',
      externalresourcesrequired: 'externalResourcesRequired',
      fill: 'fill',
      fillopacity: 'fillOpacity',
      'fill-opacity': 'fillOpacity',
      fillrule: 'fillRule',
      'fill-rule': 'fillRule',
      filter: 'filter',
      filterres: 'filterRes',
      filterunits: 'filterUnits',
      floodopacity: 'floodOpacity',
      'flood-opacity': 'floodOpacity',
      floodcolor: 'floodColor',
      'flood-color': 'floodColor',
      focusable: 'focusable',
      fontfamily: 'fontFamily',
      'font-family': 'fontFamily',
      fontsize: 'fontSize',
      'font-size': 'fontSize',
      fontsizeadjust: 'fontSizeAdjust',
      'font-size-adjust': 'fontSizeAdjust',
      fontstretch: 'fontStretch',
      'font-stretch': 'fontStretch',
      fontstyle: 'fontStyle',
      'font-style': 'fontStyle',
      fontvariant: 'fontVariant',
      'font-variant': 'fontVariant',
      fontweight: 'fontWeight',
      'font-weight': 'fontWeight',
      format: 'format',
      from: 'from',
      fx: 'fx',
      fy: 'fy',
      g1: 'g1',
      g2: 'g2',
      glyphname: 'glyphName',
      'glyph-name': 'glyphName',
      glyphorientationhorizontal: 'glyphOrientationHorizontal',
      'glyph-orientation-horizontal': 'glyphOrientationHorizontal',
      glyphorientationvertical: 'glyphOrientationVertical',
      'glyph-orientation-vertical': 'glyphOrientationVertical',
      glyphref: 'glyphRef',
      gradienttransform: 'gradientTransform',
      gradientunits: 'gradientUnits',
      hanging: 'hanging',
      horizadvx: 'horizAdvX',
      'horiz-adv-x': 'horizAdvX',
      horizoriginx: 'horizOriginX',
      'horiz-origin-x': 'horizOriginX',
      ideographic: 'ideographic',
      imagerendering: 'imageRendering',
      'image-rendering': 'imageRendering',
      in2: 'in2',
      in: 'in',
      inlist: 'inlist',
      intercept: 'intercept',
      k1: 'k1',
      k2: 'k2',
      k3: 'k3',
      k4: 'k4',
      k: 'k',
      kernelmatrix: 'kernelMatrix',
      kernelunitlength: 'kernelUnitLength',
      kerning: 'kerning',
      keypoints: 'keyPoints',
      keysplines: 'keySplines',
      keytimes: 'keyTimes',
      lengthadjust: 'lengthAdjust',
      letterspacing: 'letterSpacing',
      'letter-spacing': 'letterSpacing',
      lightingcolor: 'lightingColor',
      'lighting-color': 'lightingColor',
      limitingconeangle: 'limitingConeAngle',
      local: 'local',
      markerend: 'markerEnd',
      'marker-end': 'markerEnd',
      markerheight: 'markerHeight',
      markermid: 'markerMid',
      'marker-mid': 'markerMid',
      markerstart: 'markerStart',
      'marker-start': 'markerStart',
      markerunits: 'markerUnits',
      markerwidth: 'markerWidth',
      mask: 'mask',
      maskcontentunits: 'maskContentUnits',
      maskunits: 'maskUnits',
      mathematical: 'mathematical',
      mode: 'mode',
      numoctaves: 'numOctaves',
      offset: 'offset',
      opacity: 'opacity',
      operator: 'operator',
      order: 'order',
      orient: 'orient',
      orientation: 'orientation',
      origin: 'origin',
      overflow: 'overflow',
      overlineposition: 'overlinePosition',
      'overline-position': 'overlinePosition',
      overlinethickness: 'overlineThickness',
      'overline-thickness': 'overlineThickness',
      paintorder: 'paintOrder',
      'paint-order': 'paintOrder',
      panose1: 'panose1',
      'panose-1': 'panose1',
      pathlength: 'pathLength',
      patterncontentunits: 'patternContentUnits',
      patterntransform: 'patternTransform',
      patternunits: 'patternUnits',
      pointerevents: 'pointerEvents',
      'pointer-events': 'pointerEvents',
      points: 'points',
      pointsatx: 'pointsAtX',
      pointsaty: 'pointsAtY',
      pointsatz: 'pointsAtZ',
      prefix: 'prefix',
      preservealpha: 'preserveAlpha',
      preserveaspectratio: 'preserveAspectRatio',
      primitiveunits: 'primitiveUnits',
      property: 'property',
      r: 'r',
      radius: 'radius',
      refx: 'refX',
      refy: 'refY',
      renderingintent: 'renderingIntent',
      'rendering-intent': 'renderingIntent',
      repeatcount: 'repeatCount',
      repeatdur: 'repeatDur',
      requiredextensions: 'requiredExtensions',
      requiredfeatures: 'requiredFeatures',
      resource: 'resource',
      restart: 'restart',
      result: 'result',
      results: 'results',
      rotate: 'rotate',
      rx: 'rx',
      ry: 'ry',
      scale: 'scale',
      security: 'security',
      seed: 'seed',
      shaperendering: 'shapeRendering',
      'shape-rendering': 'shapeRendering',
      slope: 'slope',
      spacing: 'spacing',
      specularconstant: 'specularConstant',
      specularexponent: 'specularExponent',
      speed: 'speed',
      spreadmethod: 'spreadMethod',
      startoffset: 'startOffset',
      stddeviation: 'stdDeviation',
      stemh: 'stemh',
      stemv: 'stemv',
      stitchtiles: 'stitchTiles',
      stopcolor: 'stopColor',
      'stop-color': 'stopColor',
      stopopacity: 'stopOpacity',
      'stop-opacity': 'stopOpacity',
      strikethroughposition: 'strikethroughPosition',
      'strikethrough-position': 'strikethroughPosition',
      strikethroughthickness: 'strikethroughThickness',
      'strikethrough-thickness': 'strikethroughThickness',
      string: 'string',
      stroke: 'stroke',
      strokedasharray: 'strokeDasharray',
      'stroke-dasharray': 'strokeDasharray',
      strokedashoffset: 'strokeDashoffset',
      'stroke-dashoffset': 'strokeDashoffset',
      strokelinecap: 'strokeLinecap',
      'stroke-linecap': 'strokeLinecap',
      strokelinejoin: 'strokeLinejoin',
      'stroke-linejoin': 'strokeLinejoin',
      strokemiterlimit: 'strokeMiterlimit',
      'stroke-miterlimit': 'strokeMiterlimit',
      strokewidth: 'strokeWidth',
      'stroke-width': 'strokeWidth',
      strokeopacity: 'strokeOpacity',
      'stroke-opacity': 'strokeOpacity',
      suppresscontenteditablewarning: 'suppressContentEditableWarning',
      suppresshydrationwarning: 'suppressHydrationWarning',
      surfacescale: 'surfaceScale',
      systemlanguage: 'systemLanguage',
      tablevalues: 'tableValues',
      targetx: 'targetX',
      targety: 'targetY',
      textanchor: 'textAnchor',
      'text-anchor': 'textAnchor',
      textdecoration: 'textDecoration',
      'text-decoration': 'textDecoration',
      textlength: 'textLength',
      textrendering: 'textRendering',
      'text-rendering': 'textRendering',
      to: 'to',
      transform: 'transform',
      typeof: 'typeof',
      u1: 'u1',
      u2: 'u2',
      underlineposition: 'underlinePosition',
      'underline-position': 'underlinePosition',
      underlinethickness: 'underlineThickness',
      'underline-thickness': 'underlineThickness',
      unicode: 'unicode',
      unicodebidi: 'unicodeBidi',
      'unicode-bidi': 'unicodeBidi',
      unicoderange: 'unicodeRange',
      'unicode-range': 'unicodeRange',
      unitsperem: 'unitsPerEm',
      'units-per-em': 'unitsPerEm',
      unselectable: 'unselectable',
      valphabetic: 'vAlphabetic',
      'v-alphabetic': 'vAlphabetic',
      values: 'values',
      vectoreffect: 'vectorEffect',
      'vector-effect': 'vectorEffect',
      version: 'version',
      vertadvy: 'vertAdvY',
      'vert-adv-y': 'vertAdvY',
      vertoriginx: 'vertOriginX',
      'vert-origin-x': 'vertOriginX',
      vertoriginy: 'vertOriginY',
      'vert-origin-y': 'vertOriginY',
      vhanging: 'vHanging',
      'v-hanging': 'vHanging',
      videographic: 'vIdeographic',
      'v-ideographic': 'vIdeographic',
      viewbox: 'viewBox',
      viewtarget: 'viewTarget',
      visibility: 'visibility',
      vmathematical: 'vMathematical',
      'v-mathematical': 'vMathematical',
      vocab: 'vocab',
      widths: 'widths',
      wordspacing: 'wordSpacing',
      'word-spacing': 'wordSpacing',
      writingmode: 'writingMode',
      'writing-mode': 'writingMode',
      x1: 'x1',
      x2: 'x2',
      x: 'x',
      xchannelselector: 'xChannelSelector',
      xheight: 'xHeight',
      'x-height': 'xHeight',
      xlinkactuate: 'xlinkActuate',
      'xlink:actuate': 'xlinkActuate',
      xlinkarcrole: 'xlinkArcrole',
      'xlink:arcrole': 'xlinkArcrole',
      xlinkhref: 'xlinkHref',
      'xlink:href': 'xlinkHref',
      xlinkrole: 'xlinkRole',
      'xlink:role': 'xlinkRole',
      xlinkshow: 'xlinkShow',
      'xlink:show': 'xlinkShow',
      xlinktitle: 'xlinkTitle',
      'xlink:title': 'xlinkTitle',
      xlinktype: 'xlinkType',
      'xlink:type': 'xlinkType',
      xmlbase: 'xmlBase',
      'xml:base': 'xmlBase',
      xmllang: 'xmlLang',
      'xml:lang': 'xmlLang',
      xmlns: 'xmlns',
      'xml:space': 'xmlSpace',
      xmlnsxlink: 'xmlnsXlink',
      'xmlns:xlink': 'xmlnsXlink',
      xmlspace: 'xmlSpace',
      y1: 'y1',
      y2: 'y2',
      y: 'y',
      ychannelselector: 'yChannelSelector',
      z: 'z',
      zoomandpan: 'zoomAndPan'
    };
    var ariaProperties = {
      'aria-current': 0,
      // state
      'aria-details': 0,
      'aria-disabled': 0,
      // state
      'aria-hidden': 0,
      // state
      'aria-invalid': 0,
      // state
      'aria-keyshortcuts': 0,
      'aria-label': 0,
      'aria-roledescription': 0,
      // Widget Attributes
      'aria-autocomplete': 0,
      'aria-checked': 0,
      'aria-expanded': 0,
      'aria-haspopup': 0,
      'aria-level': 0,
      'aria-modal': 0,
      'aria-multiline': 0,
      'aria-multiselectable': 0,
      'aria-orientation': 0,
      'aria-placeholder': 0,
      'aria-pressed': 0,
      'aria-readonly': 0,
      'aria-required': 0,
      'aria-selected': 0,
      'aria-sort': 0,
      'aria-valuemax': 0,
      'aria-valuemin': 0,
      'aria-valuenow': 0,
      'aria-valuetext': 0,
      // Live Region Attributes
      'aria-atomic': 0,
      'aria-busy': 0,
      'aria-live': 0,
      'aria-relevant': 0,
      // Drag-and-Drop Attributes
      'aria-dropeffect': 0,
      'aria-grabbed': 0,
      // Relationship Attributes
      'aria-activedescendant': 0,
      'aria-colcount': 0,
      'aria-colindex': 0,
      'aria-colspan': 0,
      'aria-controls': 0,
      'aria-describedby': 0,
      'aria-errormessage': 0,
      'aria-flowto': 0,
      'aria-labelledby': 0,
      'aria-owns': 0,
      'aria-posinset': 0,
      'aria-rowcount': 0,
      'aria-rowindex': 0,
      'aria-rowspan': 0,
      'aria-setsize': 0
    };
    var warnedProperties = {};
    var rARIA = new RegExp('^(aria)-[' + ATTRIBUTE_NAME_CHAR + ']*$');
    var rARIACamel = new RegExp('^(aria)[A-Z][' + ATTRIBUTE_NAME_CHAR + ']*$');
    var hasOwnProperty$1 = Object.prototype.hasOwnProperty;

    function validateProperty(tagName, name) {
      if (hasOwnProperty$1.call(warnedProperties, name) && warnedProperties[name]) {
        return true;
      }

      if (rARIACamel.test(name)) {
        var ariaName = 'aria-' + name.slice(4).toLowerCase();
        var correctName = ariaProperties.hasOwnProperty(ariaName) ? ariaName : null; // If this is an aria-* attribute, but is not listed in the known DOM
        // DOM properties, then it is an invalid aria-* attribute.

        if (correctName == null) {
          warning$1(false, 'Invalid ARIA attribute `%s`. ARIA attributes follow the pattern aria-* and must be lowercase.', name);
          warnedProperties[name] = true;
          return true;
        } // aria-* attributes should be lowercase; suggest the lowercase version.


        if (name !== correctName) {
          warning$1(false, 'Invalid ARIA attribute `%s`. Did you mean `%s`?', name, correctName);
          warnedProperties[name] = true;
          return true;
        }
      }

      if (rARIA.test(name)) {
        var lowerCasedName = name.toLowerCase();
        var standardName = ariaProperties.hasOwnProperty(lowerCasedName) ? lowerCasedName : null; // If this is an aria-* attribute, but is not listed in the known DOM
        // DOM properties, then it is an invalid aria-* attribute.

        if (standardName == null) {
          warnedProperties[name] = true;
          return false;
        } // aria-* attributes should be lowercase; suggest the lowercase version.


        if (name !== standardName) {
          warning$1(false, 'Unknown ARIA attribute `%s`. Did you mean `%s`?', name, standardName);
          warnedProperties[name] = true;
          return true;
        }
      }

      return true;
    }

    function warnInvalidARIAProps(type, props) {
      var invalidProps = [];

      for (var key in props) {
        var isValid = validateProperty(type, key);

        if (!isValid) {
          invalidProps.push(key);
        }
      }

      var unknownPropString = invalidProps.map(function (prop) {
        return '`' + prop + '`';
      }).join(', ');

      if (invalidProps.length === 1) {
        warning$1(false, 'Invalid aria prop %s on <%s> tag. ' + 'For details, see https://fb.me/invalid-aria-prop', unknownPropString, type);
      } else if (invalidProps.length > 1) {
        warning$1(false, 'Invalid aria props %s on <%s> tag. ' + 'For details, see https://fb.me/invalid-aria-prop', unknownPropString, type);
      }
    }

    function validateProperties(type, props) {
      if (isCustomComponent(type, props)) {
        return;
      }

      warnInvalidARIAProps(type, props);
    }

    var didWarnValueNull = false;

    function validateProperties$1(type, props) {
      if (type !== 'input' && type !== 'textarea' && type !== 'select') {
        return;
      }

      if (props != null && props.value === null && !didWarnValueNull) {
        didWarnValueNull = true;

        if (type === 'select' && props.multiple) {
          warning$1(false, '`value` prop on `%s` should not be null. ' + 'Consider using an empty array when `multiple` is set to `true` ' + 'to clear the component or `undefined` for uncontrolled components.', type);
        } else {
          warning$1(false, '`value` prop on `%s` should not be null. ' + 'Consider using an empty string to clear the component or `undefined` ' + 'for uncontrolled components.', type);
        }
      }
    }

    var validateProperty$1 = function () {};

    {
      var warnedProperties$1 = {};
      var _hasOwnProperty = Object.prototype.hasOwnProperty;
      var EVENT_NAME_REGEX = /^on./;
      var INVALID_EVENT_NAME_REGEX = /^on[^A-Z]/;
      var rARIA$1 = new RegExp('^(aria)-[' + ATTRIBUTE_NAME_CHAR + ']*$');
      var rARIACamel$1 = new RegExp('^(aria)[A-Z][' + ATTRIBUTE_NAME_CHAR + ']*$');

      validateProperty$1 = function (tagName, name, value, canUseEventSystem) {
        if (_hasOwnProperty.call(warnedProperties$1, name) && warnedProperties$1[name]) {
          return true;
        }

        var lowerCasedName = name.toLowerCase();

        if (lowerCasedName === 'onfocusin' || lowerCasedName === 'onfocusout') {
          warning$1(false, 'React uses onFocus and onBlur instead of onFocusIn and onFocusOut. ' + 'All React events are normalized to bubble, so onFocusIn and onFocusOut ' + 'are not needed/supported by React.');
          warnedProperties$1[name] = true;
          return true;
        } // We can't rely on the event system being injected on the server.


        if (canUseEventSystem) {
          if (registrationNameModules.hasOwnProperty(name)) {
            return true;
          }

          var registrationName = possibleRegistrationNames.hasOwnProperty(lowerCasedName) ? possibleRegistrationNames[lowerCasedName] : null;

          if (registrationName != null) {
            warning$1(false, 'Invalid event handler property `%s`. Did you mean `%s`?', name, registrationName);
            warnedProperties$1[name] = true;
            return true;
          }

          if (EVENT_NAME_REGEX.test(name)) {
            warning$1(false, 'Unknown event handler property `%s`. It will be ignored.', name);
            warnedProperties$1[name] = true;
            return true;
          }
        } else if (EVENT_NAME_REGEX.test(name)) {
          // If no event plugins have been injected, we are in a server environment.
          // So we can't tell if the event name is correct for sure, but we can filter
          // out known bad ones like `onclick`. We can't suggest a specific replacement though.
          if (INVALID_EVENT_NAME_REGEX.test(name)) {
            warning$1(false, 'Invalid event handler property `%s`. ' + 'React events use the camelCase naming convention, for example `onClick`.', name);
          }

          warnedProperties$1[name] = true;
          return true;
        } // Let the ARIA attribute hook validate ARIA attributes


        if (rARIA$1.test(name) || rARIACamel$1.test(name)) {
          return true;
        }

        if (lowerCasedName === 'innerhtml') {
          warning$1(false, 'Directly setting property `innerHTML` is not permitted. ' + 'For more information, lookup documentation on `dangerouslySetInnerHTML`.');
          warnedProperties$1[name] = true;
          return true;
        }

        if (lowerCasedName === 'aria') {
          warning$1(false, 'The `aria` attribute is reserved for future use in React. ' + 'Pass individual `aria-` attributes instead.');
          warnedProperties$1[name] = true;
          return true;
        }

        if (lowerCasedName === 'is' && value !== null && value !== undefined && typeof value !== 'string') {
          warning$1(false, 'Received a `%s` for a string attribute `is`. If this is expected, cast ' + 'the value to a string.', typeof value);
          warnedProperties$1[name] = true;
          return true;
        }

        if (typeof value === 'number' && isNaN(value)) {
          warning$1(false, 'Received NaN for the `%s` attribute. If this is expected, cast ' + 'the value to a string.', name);
          warnedProperties$1[name] = true;
          return true;
        }

        var propertyInfo = getPropertyInfo(name);
        var isReserved = propertyInfo !== null && propertyInfo.type === RESERVED; // Known attributes should match the casing specified in the property config.

        if (possibleStandardNames.hasOwnProperty(lowerCasedName)) {
          var standardName = possibleStandardNames[lowerCasedName];

          if (standardName !== name) {
            warning$1(false, 'Invalid DOM property `%s`. Did you mean `%s`?', name, standardName);
            warnedProperties$1[name] = true;
            return true;
          }
        } else if (!isReserved && name !== lowerCasedName) {
          // Unknown attributes should have lowercase casing since that's how they
          // will be cased anyway with server rendering.
          warning$1(false, 'React does not recognize the `%s` prop on a DOM element. If you ' + 'intentionally want it to appear in the DOM as a custom ' + 'attribute, spell it as lowercase `%s` instead. ' + 'If you accidentally passed it from a parent component, remove ' + 'it from the DOM element.', name, lowerCasedName);
          warnedProperties$1[name] = true;
          return true;
        }

        if (typeof value === 'boolean' && shouldRemoveAttributeWithWarning(name, value, propertyInfo, false)) {
          if (value) {
            warning$1(false, 'Received `%s` for a non-boolean attribute `%s`.\n\n' + 'If you want to write it to the DOM, pass a string instead: ' + '%s="%s" or %s={value.toString()}.', value, name, name, value, name);
          } else {
            warning$1(false, 'Received `%s` for a non-boolean attribute `%s`.\n\n' + 'If you want to write it to the DOM, pass a string instead: ' + '%s="%s" or %s={value.toString()}.\n\n' + 'If you used to conditionally omit it with %s={condition && value}, ' + 'pass %s={condition ? value : undefined} instead.', value, name, name, value, name, name, name);
          }

          warnedProperties$1[name] = true;
          return true;
        } // Now that we've validated casing, do not validate
        // data types for reserved props


        if (isReserved) {
          return true;
        } // Warn when a known attribute is a bad type


        if (shouldRemoveAttributeWithWarning(name, value, propertyInfo, false)) {
          warnedProperties$1[name] = true;
          return false;
        } // Warn when passing the strings 'false' or 'true' into a boolean prop


        if ((value === 'false' || value === 'true') && propertyInfo !== null && propertyInfo.type === BOOLEAN) {
          warning$1(false, 'Received the string `%s` for the boolean attribute `%s`. ' + '%s ' + 'Did you mean %s={%s}?', value, name, value === 'false' ? 'The browser will interpret it as a truthy value.' : 'Although this works, it will not work as expected if you pass the string "false".', name, value);
          warnedProperties$1[name] = true;
          return true;
        }

        return true;
      };
    }

    var warnUnknownProperties = function (type, props, canUseEventSystem) {
      var unknownProps = [];

      for (var key in props) {
        var isValid = validateProperty$1(type, key, props[key], canUseEventSystem);

        if (!isValid) {
          unknownProps.push(key);
        }
      }

      var unknownPropString = unknownProps.map(function (prop) {
        return '`' + prop + '`';
      }).join(', ');

      if (unknownProps.length === 1) {
        warning$1(false, 'Invalid value for prop %s on <%s> tag. Either remove it from the element, ' + 'or pass a string or number value to keep it in the DOM. ' + 'For details, see https://fb.me/react-attribute-behavior', unknownPropString, type);
      } else if (unknownProps.length > 1) {
        warning$1(false, 'Invalid values for props %s on <%s> tag. Either remove them from the element, ' + 'or pass a string or number value to keep them in the DOM. ' + 'For details, see https://fb.me/react-attribute-behavior', unknownPropString, type);
      }
    };

    function validateProperties$2(type, props, canUseEventSystem) {
      if (isCustomComponent(type, props)) {
        return;
      }

      warnUnknownProperties(type, props, canUseEventSystem);
    } // TODO: direct imports like some-package/src/* are bad. Fix me.


    var didWarnInvalidHydration = false;
    var didWarnShadyDOM = false;
    var didWarnScriptTags = false;
    var DANGEROUSLY_SET_INNER_HTML = 'dangerouslySetInnerHTML';
    var SUPPRESS_CONTENT_EDITABLE_WARNING = 'suppressContentEditableWarning';
    var SUPPRESS_HYDRATION_WARNING$1 = 'suppressHydrationWarning';
    var AUTOFOCUS = 'autoFocus';
    var CHILDREN = 'children';
    var STYLE$1 = 'style';
    var HTML = '__html';
    var LISTENERS = 'listeners';
    var HTML_NAMESPACE = Namespaces.html;
    var warnedUnknownTags;
    var suppressHydrationWarning;
    var validatePropertiesInDevelopment;
    var warnForTextDifference;
    var warnForPropDifference;
    var warnForExtraAttributes;
    var warnForInvalidEventListener;
    var canDiffStyleForHydrationWarning;
    var normalizeMarkupForTextOrAttribute;
    var normalizeHTML;
    {
      warnedUnknownTags = {
        // Chrome is the only major browser not shipping <time>. But as of July
        // 2017 it intends to ship it due to widespread usage. We intentionally
        // *don't* warn for <time> even if it's unrecognized by Chrome because
        // it soon will be, and many apps have been using it anyway.
        time: true,
        // There are working polyfills for <dialog>. Let people use it.
        dialog: true,
        // Electron ships a custom <webview> tag to display external web content in
        // an isolated frame and process.
        // This tag is not present in non Electron environments such as JSDom which
        // is often used for testing purposes.
        // @see https://electronjs.org/docs/api/webview-tag
        webview: true
      };

      validatePropertiesInDevelopment = function (type, props) {
        validateProperties(type, props);
        validateProperties$1(type, props);
        validateProperties$2(type, props,
        /* canUseEventSystem */
        true);
      }; // IE 11 parses & normalizes the style attribute as opposed to other
      // browsers. It adds spaces and sorts the properties in some
      // non-alphabetical order. Handling that would require sorting CSS
      // properties in the client & server versions or applying
      // `expectedStyle` to a temporary DOM node to read its `style` attribute
      // normalized. Since it only affects IE, we're skipping style warnings
      // in that browser completely in favor of doing all that work.
      // See https://github.com/facebook/react/issues/11807


      canDiffStyleForHydrationWarning = canUseDOM && !document.documentMode; // HTML parsing normalizes CR and CRLF to LF.
      // It also can turn \u0000 into \uFFFD inside attributes.
      // https://www.w3.org/TR/html5/single-page.html#preprocessing-the-input-stream
      // If we have a mismatch, it might be caused by that.
      // We will still patch up in this case but not fire the warning.

      var NORMALIZE_NEWLINES_REGEX = /\r\n?/g;
      var NORMALIZE_NULL_AND_REPLACEMENT_REGEX = /\u0000|\uFFFD/g;

      normalizeMarkupForTextOrAttribute = function (markup) {
        var markupString = typeof markup === 'string' ? markup : '' + markup;
        return markupString.replace(NORMALIZE_NEWLINES_REGEX, '\n').replace(NORMALIZE_NULL_AND_REPLACEMENT_REGEX, '');
      };

      warnForTextDifference = function (serverText, clientText) {
        if (didWarnInvalidHydration) {
          return;
        }

        var normalizedClientText = normalizeMarkupForTextOrAttribute(clientText);
        var normalizedServerText = normalizeMarkupForTextOrAttribute(serverText);

        if (normalizedServerText === normalizedClientText) {
          return;
        }

        didWarnInvalidHydration = true;
        warningWithoutStack$1(false, 'Text content did not match. Server: "%s" Client: "%s"', normalizedServerText, normalizedClientText);
      };

      warnForPropDifference = function (propName, serverValue, clientValue) {
        if (didWarnInvalidHydration) {
          return;
        }

        var normalizedClientValue = normalizeMarkupForTextOrAttribute(clientValue);
        var normalizedServerValue = normalizeMarkupForTextOrAttribute(serverValue);

        if (normalizedServerValue === normalizedClientValue) {
          return;
        }

        didWarnInvalidHydration = true;
        warningWithoutStack$1(false, 'Prop `%s` did not match. Server: %s Client: %s', propName, JSON.stringify(normalizedServerValue), JSON.stringify(normalizedClientValue));
      };

      warnForExtraAttributes = function (attributeNames) {
        if (didWarnInvalidHydration) {
          return;
        }

        didWarnInvalidHydration = true;
        var names = [];
        attributeNames.forEach(function (name) {
          names.push(name);
        });
        warningWithoutStack$1(false, 'Extra attributes from the server: %s', names);
      };

      warnForInvalidEventListener = function (registrationName, listener) {
        if (listener === false) {
          warning$1(false, 'Expected `%s` listener to be a function, instead got `false`.\n\n' + 'If you used to conditionally omit it with %s={condition && value}, ' + 'pass %s={condition ? value : undefined} instead.', registrationName, registrationName, registrationName);
        } else {
          warning$1(false, 'Expected `%s` listener to be a function, instead got a value of `%s` type.', registrationName, typeof listener);
        }
      }; // Parse the HTML and read it back to normalize the HTML string so that it
      // can be used for comparison.


      normalizeHTML = function (parent, html) {
        // We could have created a separate document here to avoid
        // re-initializing custom elements if they exist. But this breaks
        // how <noscript> is being handled. So we use the same document.
        // See the discussion in https://github.com/facebook/react/pull/11157.
        var testElement = parent.namespaceURI === HTML_NAMESPACE ? parent.ownerDocument.createElement(parent.tagName) : parent.ownerDocument.createElementNS(parent.namespaceURI, parent.tagName);
        testElement.innerHTML = html;
        return testElement.innerHTML;
      };
    }

    function ensureListeningTo(rootContainerElement, registrationName) {
      var isDocumentOrFragment = rootContainerElement.nodeType === DOCUMENT_NODE || rootContainerElement.nodeType === DOCUMENT_FRAGMENT_NODE;
      var doc = isDocumentOrFragment ? rootContainerElement : rootContainerElement.ownerDocument;
      listenTo(registrationName, doc);
    }

    function getOwnerDocumentFromRootContainer(rootContainerElement) {
      return rootContainerElement.nodeType === DOCUMENT_NODE ? rootContainerElement : rootContainerElement.ownerDocument;
    }

    function noop() {}

    function trapClickOnNonInteractiveElement(node) {
      // Mobile Safari does not fire properly bubble click events on
      // non-interactive elements, which means delegated click listeners do not
      // fire. The workaround for this bug involves attaching an empty click
      // listener on the target node.
      // http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
      // Just set it using the onclick property so that we don't have to manage any
      // bookkeeping for it. Not sure if we need to clear it when the listener is
      // removed.
      // TODO: Only do this for the relevant Safaris maybe?
      node.onclick = noop;
    }

    function setInitialDOMProperties(tag, domElement, rootContainerElement, nextProps, isCustomComponentTag) {
      for (var propKey in nextProps) {
        if (!nextProps.hasOwnProperty(propKey)) {
          continue;
        }

        var nextProp = nextProps[propKey];

        if (propKey === STYLE$1) {
          {
            if (nextProp) {
              // Freeze the next style object so that we can assume it won't be
              // mutated. We have already warned for this in the past.
              Object.freeze(nextProp);
            }
          } // Relies on `updateStylesByID` not mutating `styleUpdates`.

          setValueForStyles(domElement, nextProp);
        } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
          var nextHtml = nextProp ? nextProp[HTML] : undefined;

          if (nextHtml != null) {
            setInnerHTML(domElement, nextHtml);
          }
        } else if (propKey === CHILDREN) {
          if (typeof nextProp === 'string') {
            // Avoid setting initial textContent when the text is empty. In IE11 setting
            // textContent on a <textarea> will cause the placeholder to not
            // show within the <textarea> until it has been focused and blurred again.
            // https://github.com/facebook/react/issues/6731#issuecomment-254874553
            var canSetTextContent = tag !== 'textarea' || nextProp !== '';

            if (canSetTextContent) {
              setTextContent(domElement, nextProp);
            }
          } else if (typeof nextProp === 'number') {
            setTextContent(domElement, '' + nextProp);
          }
        } else if (enableFlareAPI && propKey === LISTENERS || propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1) {// Noop
        } else if (propKey === AUTOFOCUS) {// We polyfill it separately on the client during commit.
          // We could have excluded it in the property list instead of
          // adding a special case here, but then it wouldn't be emitted
          // on server rendering (but we *do* want to emit it in SSR).
        } else if (registrationNameModules.hasOwnProperty(propKey)) {
          if (nextProp != null) {
            if (true && typeof nextProp !== 'function') {
              warnForInvalidEventListener(propKey, nextProp);
            }

            ensureListeningTo(rootContainerElement, propKey);
          }
        } else if (nextProp != null) {
          setValueForProperty(domElement, propKey, nextProp, isCustomComponentTag);
        }
      }
    }

    function updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag) {
      // TODO: Handle wasCustomComponentTag
      for (var i = 0; i < updatePayload.length; i += 2) {
        var propKey = updatePayload[i];
        var propValue = updatePayload[i + 1];

        if (propKey === STYLE$1) {
          setValueForStyles(domElement, propValue);
        } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
          setInnerHTML(domElement, propValue);
        } else if (propKey === CHILDREN) {
          setTextContent(domElement, propValue);
        } else {
          setValueForProperty(domElement, propKey, propValue, isCustomComponentTag);
        }
      }
    }

    function createElement(type, props, rootContainerElement, parentNamespace) {
      var isCustomComponentTag; // We create tags in the namespace of their parent container, except HTML
      // tags get no namespace.

      var ownerDocument = getOwnerDocumentFromRootContainer(rootContainerElement);
      var domElement;
      var namespaceURI = parentNamespace;

      if (namespaceURI === HTML_NAMESPACE) {
        namespaceURI = getIntrinsicNamespace(type);
      }

      if (namespaceURI === HTML_NAMESPACE) {
        {
          isCustomComponentTag = isCustomComponent(type, props); // Should this check be gated by parent namespace? Not sure we want to
          // allow <SVG> or <mATH>.

          !(isCustomComponentTag || type === type.toLowerCase()) ? warning$1(false, '<%s /> is using incorrect casing. ' + 'Use PascalCase for React components, ' + 'or lowercase for HTML elements.', type) : void 0;
        }

        if (type === 'script') {
          // Create the script via .innerHTML so its "parser-inserted" flag is
          // set to true and it does not execute
          var div = ownerDocument.createElement('div');
          {
            if (enableTrustedTypesIntegration && !didWarnScriptTags) {
              warning$1(false, 'Encountered a script tag while rendering React component. ' + 'Scripts inside React components are never executed when rendering ' + 'on the client. Consider using template tag instead ' + '(https://developer.mozilla.org/en-US/docs/Web/HTML/Element/template).');
              didWarnScriptTags = true;
            }
          }
          div.innerHTML = '<script><' + '/script>'; // eslint-disable-line
          // This is guaranteed to yield a script element.

          var firstChild = div.firstChild;
          domElement = div.removeChild(firstChild);
        } else if (typeof props.is === 'string') {
          // $FlowIssue `createElement` should be updated for Web Components
          domElement = ownerDocument.createElement(type, {
            is: props.is
          });
        } else {
          // Separate else branch instead of using `props.is || undefined` above because of a Firefox bug.
          // See discussion in https://github.com/facebook/react/pull/6896
          // and discussion in https://bugzilla.mozilla.org/show_bug.cgi?id=1276240
          domElement = ownerDocument.createElement(type); // Normally attributes are assigned in `setInitialDOMProperties`, however the `multiple` and `size`
          // attributes on `select`s needs to be added before `option`s are inserted.
          // This prevents:
          // - a bug where the `select` does not scroll to the correct option because singular
          //  `select` elements automatically pick the first item #13222
          // - a bug where the `select` set the first item as selected despite the `size` attribute #14239
          // See https://github.com/facebook/react/issues/13222
          // and https://github.com/facebook/react/issues/14239

          if (type === 'select') {
            var node = domElement;

            if (props.multiple) {
              node.multiple = true;
            } else if (props.size) {
              // Setting a size greater than 1 causes a select to behave like `multiple=true`, where
              // it is possible that no option is selected.
              //
              // This is only necessary when a select in "single selection mode".
              node.size = props.size;
            }
          }
        }
      } else {
        domElement = ownerDocument.createElementNS(namespaceURI, type);
      }

      {
        if (namespaceURI === HTML_NAMESPACE) {
          if (!isCustomComponentTag && Object.prototype.toString.call(domElement) === '[object HTMLUnknownElement]' && !Object.prototype.hasOwnProperty.call(warnedUnknownTags, type)) {
            warnedUnknownTags[type] = true;
            warning$1(false, 'The tag <%s> is unrecognized in this browser. ' + 'If you meant to render a React component, start its name with ' + 'an uppercase letter.', type);
          }
        }
      }
      return domElement;
    }

    function createTextNode(text, rootContainerElement) {
      return getOwnerDocumentFromRootContainer(rootContainerElement).createTextNode(text);
    }

    function setInitialProperties(domElement, tag, rawProps, rootContainerElement) {
      var isCustomComponentTag = isCustomComponent(tag, rawProps);
      {
        validatePropertiesInDevelopment(tag, rawProps);

        if (isCustomComponentTag && !didWarnShadyDOM && domElement.shadyRoot) {
          warning$1(false, '%s is using shady DOM. Using shady DOM with React can ' + 'cause things to break subtly.', getCurrentFiberOwnerNameInDevOrNull() || 'A component');
          didWarnShadyDOM = true;
        }
      } // TODO: Make sure that we check isMounted before firing any of these events.

      var props;

      switch (tag) {
        case 'iframe':
        case 'object':
        case 'embed':
          trapBubbledEvent(TOP_LOAD, domElement);
          props = rawProps;
          break;

        case 'video':
        case 'audio':
          // Create listener for each media event
          for (var i = 0; i < mediaEventTypes.length; i++) {
            trapBubbledEvent(mediaEventTypes[i], domElement);
          }

          props = rawProps;
          break;

        case 'source':
          trapBubbledEvent(TOP_ERROR, domElement);
          props = rawProps;
          break;

        case 'img':
        case 'image':
        case 'link':
          trapBubbledEvent(TOP_ERROR, domElement);
          trapBubbledEvent(TOP_LOAD, domElement);
          props = rawProps;
          break;

        case 'form':
          trapBubbledEvent(TOP_RESET, domElement);
          trapBubbledEvent(TOP_SUBMIT, domElement);
          props = rawProps;
          break;

        case 'details':
          trapBubbledEvent(TOP_TOGGLE, domElement);
          props = rawProps;
          break;

        case 'input':
          initWrapperState(domElement, rawProps);
          props = getHostProps(domElement, rawProps);
          trapBubbledEvent(TOP_INVALID, domElement); // For controlled components we always need to ensure we're listening
          // to onChange. Even if there is no listener.

          ensureListeningTo(rootContainerElement, 'onChange');
          break;

        case 'option':
          validateProps(domElement, rawProps);
          props = getHostProps$1(domElement, rawProps);
          break;

        case 'select':
          initWrapperState$1(domElement, rawProps);
          props = getHostProps$2(domElement, rawProps);
          trapBubbledEvent(TOP_INVALID, domElement); // For controlled components we always need to ensure we're listening
          // to onChange. Even if there is no listener.

          ensureListeningTo(rootContainerElement, 'onChange');
          break;

        case 'textarea':
          initWrapperState$2(domElement, rawProps);
          props = getHostProps$3(domElement, rawProps);
          trapBubbledEvent(TOP_INVALID, domElement); // For controlled components we always need to ensure we're listening
          // to onChange. Even if there is no listener.

          ensureListeningTo(rootContainerElement, 'onChange');
          break;

        default:
          props = rawProps;
      }

      assertValidProps(tag, props);
      setInitialDOMProperties(tag, domElement, rootContainerElement, props, isCustomComponentTag);

      switch (tag) {
        case 'input':
          // TODO: Make sure we check if this is still unmounted or do any clean
          // up necessary since we never stop tracking anymore.
          track(domElement);
          postMountWrapper(domElement, rawProps, false);
          break;

        case 'textarea':
          // TODO: Make sure we check if this is still unmounted or do any clean
          // up necessary since we never stop tracking anymore.
          track(domElement);
          postMountWrapper$3(domElement, rawProps);
          break;

        case 'option':
          postMountWrapper$1(domElement, rawProps);
          break;

        case 'select':
          postMountWrapper$2(domElement, rawProps);
          break;

        default:
          if (typeof props.onClick === 'function') {
            // TODO: This cast may not be sound for SVG, MathML or custom elements.
            trapClickOnNonInteractiveElement(domElement);
          }

          break;
      }
    } // Calculate the diff between the two objects.


    function diffProperties(domElement, tag, lastRawProps, nextRawProps, rootContainerElement) {
      {
        validatePropertiesInDevelopment(tag, nextRawProps);
      }
      var updatePayload = null;
      var lastProps;
      var nextProps;

      switch (tag) {
        case 'input':
          lastProps = getHostProps(domElement, lastRawProps);
          nextProps = getHostProps(domElement, nextRawProps);
          updatePayload = [];
          break;

        case 'option':
          lastProps = getHostProps$1(domElement, lastRawProps);
          nextProps = getHostProps$1(domElement, nextRawProps);
          updatePayload = [];
          break;

        case 'select':
          lastProps = getHostProps$2(domElement, lastRawProps);
          nextProps = getHostProps$2(domElement, nextRawProps);
          updatePayload = [];
          break;

        case 'textarea':
          lastProps = getHostProps$3(domElement, lastRawProps);
          nextProps = getHostProps$3(domElement, nextRawProps);
          updatePayload = [];
          break;

        default:
          lastProps = lastRawProps;
          nextProps = nextRawProps;

          if (typeof lastProps.onClick !== 'function' && typeof nextProps.onClick === 'function') {
            // TODO: This cast may not be sound for SVG, MathML or custom elements.
            trapClickOnNonInteractiveElement(domElement);
          }

          break;
      }

      assertValidProps(tag, nextProps);
      var propKey;
      var styleName;
      var styleUpdates = null;

      for (propKey in lastProps) {
        if (nextProps.hasOwnProperty(propKey) || !lastProps.hasOwnProperty(propKey) || lastProps[propKey] == null) {
          continue;
        }

        if (propKey === STYLE$1) {
          var lastStyle = lastProps[propKey];

          for (styleName in lastStyle) {
            if (lastStyle.hasOwnProperty(styleName)) {
              if (!styleUpdates) {
                styleUpdates = {};
              }

              styleUpdates[styleName] = '';
            }
          }
        } else if (propKey === DANGEROUSLY_SET_INNER_HTML || propKey === CHILDREN) {// Noop. This is handled by the clear text mechanism.
        } else if (enableFlareAPI && propKey === LISTENERS || propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1) {// Noop
        } else if (propKey === AUTOFOCUS) {// Noop. It doesn't work on updates anyway.
        } else if (registrationNameModules.hasOwnProperty(propKey)) {
          // This is a special case. If any listener updates we need to ensure
          // that the "current" fiber pointer gets updated so we need a commit
          // to update this element.
          if (!updatePayload) {
            updatePayload = [];
          }
        } else {
          // For all other deleted properties we add it to the queue. We use
          // the whitelist in the commit phase instead.
          (updatePayload = updatePayload || []).push(propKey, null);
        }
      }

      for (propKey in nextProps) {
        var nextProp = nextProps[propKey];
        var lastProp = lastProps != null ? lastProps[propKey] : undefined;

        if (!nextProps.hasOwnProperty(propKey) || nextProp === lastProp || nextProp == null && lastProp == null) {
          continue;
        }

        if (propKey === STYLE$1) {
          {
            if (nextProp) {
              // Freeze the next style object so that we can assume it won't be
              // mutated. We have already warned for this in the past.
              Object.freeze(nextProp);
            }
          }

          if (lastProp) {
            // Unset styles on `lastProp` but not on `nextProp`.
            for (styleName in lastProp) {
              if (lastProp.hasOwnProperty(styleName) && (!nextProp || !nextProp.hasOwnProperty(styleName))) {
                if (!styleUpdates) {
                  styleUpdates = {};
                }

                styleUpdates[styleName] = '';
              }
            } // Update styles that changed since `lastProp`.


            for (styleName in nextProp) {
              if (nextProp.hasOwnProperty(styleName) && lastProp[styleName] !== nextProp[styleName]) {
                if (!styleUpdates) {
                  styleUpdates = {};
                }

                styleUpdates[styleName] = nextProp[styleName];
              }
            }
          } else {
            // Relies on `updateStylesByID` not mutating `styleUpdates`.
            if (!styleUpdates) {
              if (!updatePayload) {
                updatePayload = [];
              }

              updatePayload.push(propKey, styleUpdates);
            }

            styleUpdates = nextProp;
          }
        } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
          var nextHtml = nextProp ? nextProp[HTML] : undefined;
          var lastHtml = lastProp ? lastProp[HTML] : undefined;

          if (nextHtml != null) {
            if (lastHtml !== nextHtml) {
              (updatePayload = updatePayload || []).push(propKey, toStringOrTrustedType(nextHtml));
            }
          } else {// TODO: It might be too late to clear this if we have children
            // inserted already.
          }
        } else if (propKey === CHILDREN) {
          if (lastProp !== nextProp && (typeof nextProp === 'string' || typeof nextProp === 'number')) {
            (updatePayload = updatePayload || []).push(propKey, '' + nextProp);
          }
        } else if (enableFlareAPI && propKey === LISTENERS || propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1) {// Noop
        } else if (registrationNameModules.hasOwnProperty(propKey)) {
          if (nextProp != null) {
            // We eagerly listen to this even though we haven't committed yet.
            if (true && typeof nextProp !== 'function') {
              warnForInvalidEventListener(propKey, nextProp);
            }

            ensureListeningTo(rootContainerElement, propKey);
          }

          if (!updatePayload && lastProp !== nextProp) {
            // This is a special case. If any listener updates we need to ensure
            // that the "current" props pointer gets updated so we need a commit
            // to update this element.
            updatePayload = [];
          }
        } else {
          // For any other property we always add it to the queue and then we
          // filter it out using the whitelist during the commit.
          (updatePayload = updatePayload || []).push(propKey, nextProp);
        }
      }

      if (styleUpdates) {
        {
          validateShorthandPropertyCollisionInDev(styleUpdates, nextProps[STYLE$1]);
        }
        (updatePayload = updatePayload || []).push(STYLE$1, styleUpdates);
      }

      return updatePayload;
    } // Apply the diff.


    function updateProperties(domElement, updatePayload, tag, lastRawProps, nextRawProps) {
      // Update checked *before* name.
      // In the middle of an update, it is possible to have multiple checked.
      // When a checked radio tries to change name, browser makes another radio's checked false.
      if (tag === 'input' && nextRawProps.type === 'radio' && nextRawProps.name != null) {
        updateChecked(domElement, nextRawProps);
      }

      var wasCustomComponentTag = isCustomComponent(tag, lastRawProps);
      var isCustomComponentTag = isCustomComponent(tag, nextRawProps); // Apply the diff.

      updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag); // TODO: Ensure that an update gets scheduled if any of the special props
      // changed.

      switch (tag) {
        case 'input':
          // Update the wrapper around inputs *after* updating props. This has to
          // happen after `updateDOMProperties`. Otherwise HTML5 input validations
          // raise warnings and prevent the new value from being assigned.
          updateWrapper(domElement, nextRawProps);
          break;

        case 'textarea':
          updateWrapper$1(domElement, nextRawProps);
          break;

        case 'select':
          // <select> value update needs to occur after <option> children
          // reconciliation
          postUpdateWrapper(domElement, nextRawProps);
          break;
      }
    }

    function getPossibleStandardName(propName) {
      {
        var lowerCasedName = propName.toLowerCase();

        if (!possibleStandardNames.hasOwnProperty(lowerCasedName)) {
          return null;
        }

        return possibleStandardNames[lowerCasedName] || null;
      }
      return null;
    }

    function diffHydratedProperties(domElement, tag, rawProps, parentNamespace, rootContainerElement) {
      var isCustomComponentTag;
      var extraAttributeNames;
      {
        suppressHydrationWarning = rawProps[SUPPRESS_HYDRATION_WARNING$1] === true;
        isCustomComponentTag = isCustomComponent(tag, rawProps);
        validatePropertiesInDevelopment(tag, rawProps);

        if (isCustomComponentTag && !didWarnShadyDOM && domElement.shadyRoot) {
          warning$1(false, '%s is using shady DOM. Using shady DOM with React can ' + 'cause things to break subtly.', getCurrentFiberOwnerNameInDevOrNull() || 'A component');
          didWarnShadyDOM = true;
        }
      } // TODO: Make sure that we check isMounted before firing any of these events.

      switch (tag) {
        case 'iframe':
        case 'object':
        case 'embed':
          trapBubbledEvent(TOP_LOAD, domElement);
          break;

        case 'video':
        case 'audio':
          // Create listener for each media event
          for (var i = 0; i < mediaEventTypes.length; i++) {
            trapBubbledEvent(mediaEventTypes[i], domElement);
          }

          break;

        case 'source':
          trapBubbledEvent(TOP_ERROR, domElement);
          break;

        case 'img':
        case 'image':
        case 'link':
          trapBubbledEvent(TOP_ERROR, domElement);
          trapBubbledEvent(TOP_LOAD, domElement);
          break;

        case 'form':
          trapBubbledEvent(TOP_RESET, domElement);
          trapBubbledEvent(TOP_SUBMIT, domElement);
          break;

        case 'details':
          trapBubbledEvent(TOP_TOGGLE, domElement);
          break;

        case 'input':
          initWrapperState(domElement, rawProps);
          trapBubbledEvent(TOP_INVALID, domElement); // For controlled components we always need to ensure we're listening
          // to onChange. Even if there is no listener.

          ensureListeningTo(rootContainerElement, 'onChange');
          break;

        case 'option':
          validateProps(domElement, rawProps);
          break;

        case 'select':
          initWrapperState$1(domElement, rawProps);
          trapBubbledEvent(TOP_INVALID, domElement); // For controlled components we always need to ensure we're listening
          // to onChange. Even if there is no listener.

          ensureListeningTo(rootContainerElement, 'onChange');
          break;

        case 'textarea':
          initWrapperState$2(domElement, rawProps);
          trapBubbledEvent(TOP_INVALID, domElement); // For controlled components we always need to ensure we're listening
          // to onChange. Even if there is no listener.

          ensureListeningTo(rootContainerElement, 'onChange');
          break;
      }

      assertValidProps(tag, rawProps);
      {
        extraAttributeNames = new Set();
        var attributes = domElement.attributes;

        for (var _i = 0; _i < attributes.length; _i++) {
          var name = attributes[_i].name.toLowerCase();

          switch (name) {
            // Built-in SSR attribute is whitelisted
            case 'data-reactroot':
              break;
            // Controlled attributes are not validated
            // TODO: Only ignore them on controlled tags.

            case 'value':
              break;

            case 'checked':
              break;

            case 'selected':
              break;

            default:
              // Intentionally use the original name.
              // See discussion in https://github.com/facebook/react/pull/10676.
              extraAttributeNames.add(attributes[_i].name);
          }
        }
      }
      var updatePayload = null;

      for (var propKey in rawProps) {
        if (!rawProps.hasOwnProperty(propKey)) {
          continue;
        }

        var nextProp = rawProps[propKey];

        if (propKey === CHILDREN) {
          // For text content children we compare against textContent. This
          // might match additional HTML that is hidden when we read it using
          // textContent. E.g. "foo" will match "f<span>oo</span>" but that still
          // satisfies our requirement. Our requirement is not to produce perfect
          // HTML and attributes. Ideally we should preserve structure but it's
          // ok not to if the visible content is still enough to indicate what
          // even listeners these nodes might be wired up to.
          // TODO: Warn if there is more than a single textNode as a child.
          // TODO: Should we use domElement.firstChild.nodeValue to compare?
          if (typeof nextProp === 'string') {
            if (domElement.textContent !== nextProp) {
              if (true && !suppressHydrationWarning) {
                warnForTextDifference(domElement.textContent, nextProp);
              }

              updatePayload = [CHILDREN, nextProp];
            }
          } else if (typeof nextProp === 'number') {
            if (domElement.textContent !== '' + nextProp) {
              if (true && !suppressHydrationWarning) {
                warnForTextDifference(domElement.textContent, nextProp);
              }

              updatePayload = [CHILDREN, '' + nextProp];
            }
          }
        } else if (registrationNameModules.hasOwnProperty(propKey)) {
          if (nextProp != null) {
            if (true && typeof nextProp !== 'function') {
              warnForInvalidEventListener(propKey, nextProp);
            }

            ensureListeningTo(rootContainerElement, propKey);
          }
        } else if (true && // Convince Flow we've calculated it (it's DEV-only in this method.)
        typeof isCustomComponentTag === 'boolean') {
          // Validate that the properties correspond to their expected values.
          var serverValue = void 0;
          var propertyInfo = getPropertyInfo(propKey);

          if (suppressHydrationWarning) {// Don't bother comparing. We're ignoring all these warnings.
          } else if (enableFlareAPI && propKey === LISTENERS || propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1 || // Controlled attributes are not validated
          // TODO: Only ignore them on controlled tags.
          propKey === 'value' || propKey === 'checked' || propKey === 'selected') {// Noop
          } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
            var serverHTML = domElement.innerHTML;
            var nextHtml = nextProp ? nextProp[HTML] : undefined;
            var expectedHTML = normalizeHTML(domElement, nextHtml != null ? nextHtml : '');

            if (expectedHTML !== serverHTML) {
              warnForPropDifference(propKey, serverHTML, expectedHTML);
            }
          } else if (propKey === STYLE$1) {
            // $FlowFixMe - Should be inferred as not undefined.
            extraAttributeNames.delete(propKey);

            if (canDiffStyleForHydrationWarning) {
              var expectedStyle = createDangerousStringForStyles(nextProp);
              serverValue = domElement.getAttribute('style');

              if (expectedStyle !== serverValue) {
                warnForPropDifference(propKey, serverValue, expectedStyle);
              }
            }
          } else if (isCustomComponentTag) {
            // $FlowFixMe - Should be inferred as not undefined.
            extraAttributeNames.delete(propKey.toLowerCase());
            serverValue = getValueForAttribute(domElement, propKey, nextProp);

            if (nextProp !== serverValue) {
              warnForPropDifference(propKey, serverValue, nextProp);
            }
          } else if (!shouldIgnoreAttribute(propKey, propertyInfo, isCustomComponentTag) && !shouldRemoveAttribute(propKey, nextProp, propertyInfo, isCustomComponentTag)) {
            var isMismatchDueToBadCasing = false;

            if (propertyInfo !== null) {
              // $FlowFixMe - Should be inferred as not undefined.
              extraAttributeNames.delete(propertyInfo.attributeName);
              serverValue = getValueForProperty(domElement, propKey, nextProp, propertyInfo);
            } else {
              var ownNamespace = parentNamespace;

              if (ownNamespace === HTML_NAMESPACE) {
                ownNamespace = getIntrinsicNamespace(tag);
              }

              if (ownNamespace === HTML_NAMESPACE) {
                // $FlowFixMe - Should be inferred as not undefined.
                extraAttributeNames.delete(propKey.toLowerCase());
              } else {
                var standardName = getPossibleStandardName(propKey);

                if (standardName !== null && standardName !== propKey) {
                  // If an SVG prop is supplied with bad casing, it will
                  // be successfully parsed from HTML, but will produce a mismatch
                  // (and would be incorrectly rendered on the client).
                  // However, we already warn about bad casing elsewhere.
                  // So we'll skip the misleading extra mismatch warning in this case.
                  isMismatchDueToBadCasing = true; // $FlowFixMe - Should be inferred as not undefined.

                  extraAttributeNames.delete(standardName);
                } // $FlowFixMe - Should be inferred as not undefined.


                extraAttributeNames.delete(propKey);
              }

              serverValue = getValueForAttribute(domElement, propKey, nextProp);
            }

            if (nextProp !== serverValue && !isMismatchDueToBadCasing) {
              warnForPropDifference(propKey, serverValue, nextProp);
            }
          }
        }
      }

      {
        // $FlowFixMe - Should be inferred as not undefined.
        if (extraAttributeNames.size > 0 && !suppressHydrationWarning) {
          // $FlowFixMe - Should be inferred as not undefined.
          warnForExtraAttributes(extraAttributeNames);
        }
      }

      switch (tag) {
        case 'input':
          // TODO: Make sure we check if this is still unmounted or do any clean
          // up necessary since we never stop tracking anymore.
          track(domElement);
          postMountWrapper(domElement, rawProps, true);
          break;

        case 'textarea':
          // TODO: Make sure we check if this is still unmounted or do any clean
          // up necessary since we never stop tracking anymore.
          track(domElement);
          postMountWrapper$3(domElement, rawProps);
          break;

        case 'select':
        case 'option':
          // For input and textarea we current always set the value property at
          // post mount to force it to diverge from attributes. However, for
          // option and select we don't quite do the same thing and select
          // is not resilient to the DOM state changing so we don't do that here.
          // TODO: Consider not doing this for input and textarea.
          break;

        default:
          if (typeof rawProps.onClick === 'function') {
            // TODO: This cast may not be sound for SVG, MathML or custom elements.
            trapClickOnNonInteractiveElement(domElement);
          }

          break;
      }

      return updatePayload;
    }

    function diffHydratedText(textNode, text) {
      var isDifferent = textNode.nodeValue !== text;
      return isDifferent;
    }

    function warnForUnmatchedText(textNode, text) {
      {
        warnForTextDifference(textNode.nodeValue, text);
      }
    }

    function warnForDeletedHydratableElement(parentNode, child) {
      {
        if (didWarnInvalidHydration) {
          return;
        }

        didWarnInvalidHydration = true;
        warningWithoutStack$1(false, 'Did not expect server HTML to contain a <%s> in <%s>.', child.nodeName.toLowerCase(), parentNode.nodeName.toLowerCase());
      }
    }

    function warnForDeletedHydratableText(parentNode, child) {
      {
        if (didWarnInvalidHydration) {
          return;
        }

        didWarnInvalidHydration = true;
        warningWithoutStack$1(false, 'Did not expect server HTML to contain the text node "%s" in <%s>.', child.nodeValue, parentNode.nodeName.toLowerCase());
      }
    }

    function warnForInsertedHydratedElement(parentNode, tag, props) {
      {
        if (didWarnInvalidHydration) {
          return;
        }

        didWarnInvalidHydration = true;
        warningWithoutStack$1(false, 'Expected server HTML to contain a matching <%s> in <%s>.', tag, parentNode.nodeName.toLowerCase());
      }
    }

    function warnForInsertedHydratedText(parentNode, text) {
      {
        if (text === '') {
          // We expect to insert empty text nodes since they're not represented in
          // the HTML.
          // TODO: Remove this special case if we can just avoid inserting empty
          // text nodes.
          return;
        }

        if (didWarnInvalidHydration) {
          return;
        }

        didWarnInvalidHydration = true;
        warningWithoutStack$1(false, 'Expected server HTML to contain a matching text node for "%s" in <%s>.', text, parentNode.nodeName.toLowerCase());
      }
    }

    function restoreControlledState$$1(domElement, tag, props) {
      switch (tag) {
        case 'input':
          restoreControlledState$1(domElement, props);
          return;

        case 'textarea':
          restoreControlledState$3(domElement, props);
          return;

        case 'select':
          restoreControlledState$2(domElement, props);
          return;
      }
    }

    function listenToEventResponderEventTypes(eventTypes, element) {
      if (enableFlareAPI) {
        // Get the listening Set for this element. We use this to track
        // what events we're listening to.
        var listeningSet = getListeningSetForElement(element); // Go through each target event type of the event responder

        for (var i = 0, length = eventTypes.length; i < length; ++i) {
          var eventType = eventTypes[i];
          var isPassive = !endsWith(eventType, '_active');
          var eventKey = isPassive ? eventType + '_passive' : eventType;
          var targetEventType = isPassive ? eventType : eventType.substring(0, eventType.length - 7);

          if (!listeningSet.has(eventKey)) {
            trapEventForResponderEventSystem(element, targetEventType, isPassive);
            listeningSet.add(eventKey);
          }
        }
      }
    } // We can remove this once the event API is stable and out of a flag


    if (enableFlareAPI) {
      setListenToResponderEventTypes(listenToEventResponderEventTypes);
    }

    function getActiveElement(doc) {
      doc = doc || (typeof document !== 'undefined' ? document : undefined);

      if (typeof doc === 'undefined') {
        return null;
      }

      try {
        return doc.activeElement || doc.body;
      } catch (e) {
        return doc.body;
      }
    }
    /**
     * Given any node return the first leaf node without children.
     *
     * @param {DOMElement|DOMTextNode} node
     * @return {DOMElement|DOMTextNode}
     */


    function getLeafNode(node) {
      while (node && node.firstChild) {
        node = node.firstChild;
      }

      return node;
    }
    /**
     * Get the next sibling within a container. This will walk up the
     * DOM if a node's siblings have been exhausted.
     *
     * @param {DOMElement|DOMTextNode} node
     * @return {?DOMElement|DOMTextNode}
     */


    function getSiblingNode(node) {
      while (node) {
        if (node.nextSibling) {
          return node.nextSibling;
        }

        node = node.parentNode;
      }
    }
    /**
     * Get object describing the nodes which contain characters at offset.
     *
     * @param {DOMElement|DOMTextNode} root
     * @param {number} offset
     * @return {?object}
     */


    function getNodeForCharacterOffset(root, offset) {
      var node = getLeafNode(root);
      var nodeStart = 0;
      var nodeEnd = 0;

      while (node) {
        if (node.nodeType === TEXT_NODE) {
          nodeEnd = nodeStart + node.textContent.length;

          if (nodeStart <= offset && nodeEnd >= offset) {
            return {
              node: node,
              offset: offset - nodeStart
            };
          }

          nodeStart = nodeEnd;
        }

        node = getLeafNode(getSiblingNode(node));
      }
    }
    /**
     * @param {DOMElement} outerNode
     * @return {?object}
     */


    function getOffsets(outerNode) {
      var ownerDocument = outerNode.ownerDocument;
      var win = ownerDocument && ownerDocument.defaultView || window;
      var selection = win.getSelection && win.getSelection();

      if (!selection || selection.rangeCount === 0) {
        return null;
      }

      var anchorNode = selection.anchorNode,
          anchorOffset = selection.anchorOffset,
          focusNode = selection.focusNode,
          focusOffset = selection.focusOffset; // In Firefox, anchorNode and focusNode can be "anonymous divs", e.g. the
      // up/down buttons on an <input type="number">. Anonymous divs do not seem to
      // expose properties, triggering a "Permission denied error" if any of its
      // properties are accessed. The only seemingly possible way to avoid erroring
      // is to access a property that typically works for non-anonymous divs and
      // catch any error that may otherwise arise. See
      // https://bugzilla.mozilla.org/show_bug.cgi?id=208427

      try {
        /* eslint-disable no-unused-expressions */
        anchorNode.nodeType;
        focusNode.nodeType;
        /* eslint-enable no-unused-expressions */
      } catch (e) {
        return null;
      }

      return getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode, focusOffset);
    }
    /**
     * Returns {start, end} where `start` is the character/codepoint index of
     * (anchorNode, anchorOffset) within the textContent of `outerNode`, and
     * `end` is the index of (focusNode, focusOffset).
     *
     * Returns null if you pass in garbage input but we should probably just crash.
     *
     * Exported only for testing.
     */


    function getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode, focusOffset) {
      var length = 0;
      var start = -1;
      var end = -1;
      var indexWithinAnchor = 0;
      var indexWithinFocus = 0;
      var node = outerNode;
      var parentNode = null;

      outer: while (true) {
        var next = null;

        while (true) {
          if (node === anchorNode && (anchorOffset === 0 || node.nodeType === TEXT_NODE)) {
            start = length + anchorOffset;
          }

          if (node === focusNode && (focusOffset === 0 || node.nodeType === TEXT_NODE)) {
            end = length + focusOffset;
          }

          if (node.nodeType === TEXT_NODE) {
            length += node.nodeValue.length;
          }

          if ((next = node.firstChild) === null) {
            break;
          } // Moving from `node` to its first child `next`.


          parentNode = node;
          node = next;
        }

        while (true) {
          if (node === outerNode) {
            // If `outerNode` has children, this is always the second time visiting
            // it. If it has no children, this is still the first loop, and the only
            // valid selection is anchorNode and focusNode both equal to this node
            // and both offsets 0, in which case we will have handled above.
            break outer;
          }

          if (parentNode === anchorNode && ++indexWithinAnchor === anchorOffset) {
            start = length;
          }

          if (parentNode === focusNode && ++indexWithinFocus === focusOffset) {
            end = length;
          }

          if ((next = node.nextSibling) !== null) {
            break;
          }

          node = parentNode;
          parentNode = node.parentNode;
        } // Moving from `node` to its next sibling `next`.


        node = next;
      }

      if (start === -1 || end === -1) {
        // This should never happen. (Would happen if the anchor/focus nodes aren't
        // actually inside the passed-in node.)
        return null;
      }

      return {
        start: start,
        end: end
      };
    }
    /**
     * In modern non-IE browsers, we can support both forward and backward
     * selections.
     *
     * Note: IE10+ supports the Selection object, but it does not support
     * the `extend` method, which means that even in modern IE, it's not possible
     * to programmatically create a backward selection. Thus, for all IE
     * versions, we use the old IE API to create our selections.
     *
     * @param {DOMElement|DOMTextNode} node
     * @param {object} offsets
     */


    function setOffsets(node, offsets) {
      var doc = node.ownerDocument || document;
      var win = doc && doc.defaultView || window; // Edge fails with "Object expected" in some scenarios.
      // (For instance: TinyMCE editor used in a list component that supports pasting to add more,
      // fails when pasting 100+ items)

      if (!win.getSelection) {
        return;
      }

      var selection = win.getSelection();
      var length = node.textContent.length;
      var start = Math.min(offsets.start, length);
      var end = offsets.end === undefined ? start : Math.min(offsets.end, length); // IE 11 uses modern selection, but doesn't support the extend method.
      // Flip backward selections, so we can set with a single range.

      if (!selection.extend && start > end) {
        var temp = end;
        end = start;
        start = temp;
      }

      var startMarker = getNodeForCharacterOffset(node, start);
      var endMarker = getNodeForCharacterOffset(node, end);

      if (startMarker && endMarker) {
        if (selection.rangeCount === 1 && selection.anchorNode === startMarker.node && selection.anchorOffset === startMarker.offset && selection.focusNode === endMarker.node && selection.focusOffset === endMarker.offset) {
          return;
        }

        var range = doc.createRange();
        range.setStart(startMarker.node, startMarker.offset);
        selection.removeAllRanges();

        if (start > end) {
          selection.addRange(range);
          selection.extend(endMarker.node, endMarker.offset);
        } else {
          range.setEnd(endMarker.node, endMarker.offset);
          selection.addRange(range);
        }
      }
    }

    function isTextNode(node) {
      return node && node.nodeType === TEXT_NODE;
    }

    function containsNode(outerNode, innerNode) {
      if (!outerNode || !innerNode) {
        return false;
      } else if (outerNode === innerNode) {
        return true;
      } else if (isTextNode(outerNode)) {
        return false;
      } else if (isTextNode(innerNode)) {
        return containsNode(outerNode, innerNode.parentNode);
      } else if ('contains' in outerNode) {
        return outerNode.contains(innerNode);
      } else if (outerNode.compareDocumentPosition) {
        return !!(outerNode.compareDocumentPosition(innerNode) & 16);
      } else {
        return false;
      }
    }

    function isInDocument(node) {
      return node && node.ownerDocument && containsNode(node.ownerDocument.documentElement, node);
    }

    function isSameOriginFrame(iframe) {
      try {
        // Accessing the contentDocument of a HTMLIframeElement can cause the browser
        // to throw, e.g. if it has a cross-origin src attribute.
        // Safari will show an error in the console when the access results in "Blocked a frame with origin". e.g:
        // iframe.contentDocument.defaultView;
        // A safety way is to access one of the cross origin properties: Window or Location
        // Which might result in "SecurityError" DOM Exception and it is compatible to Safari.
        // https://html.spec.whatwg.org/multipage/browsers.html#integration-with-idl
        return typeof iframe.contentWindow.location.href === 'string';
      } catch (err) {
        return false;
      }
    }

    function getActiveElementDeep() {
      var win = window;
      var element = getActiveElement();

      while (element instanceof win.HTMLIFrameElement) {
        if (isSameOriginFrame(element)) {
          win = element.contentWindow;
        } else {
          return element;
        }

        element = getActiveElement(win.document);
      }

      return element;
    }
    /**
     * @ReactInputSelection: React input selection module. Based on Selection.js,
     * but modified to be suitable for react and has a couple of bug fixes (doesn't
     * assume buttons have range selections allowed).
     * Input selection module for React.
     */

    /**
     * @hasSelectionCapabilities: we get the element types that support selection
     * from https://html.spec.whatwg.org/#do-not-apply, looking at `selectionStart`
     * and `selectionEnd` rows.
     */


    function hasSelectionCapabilities(elem) {
      var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
      return nodeName && (nodeName === 'input' && (elem.type === 'text' || elem.type === 'search' || elem.type === 'tel' || elem.type === 'url' || elem.type === 'password') || nodeName === 'textarea' || elem.contentEditable === 'true');
    }

    function getSelectionInformation() {
      var focusedElem = getActiveElementDeep();
      return {
        focusedElem: focusedElem,
        selectionRange: hasSelectionCapabilities(focusedElem) ? getSelection(focusedElem) : null
      };
    }
    /**
     * @restoreSelection: If any selection information was potentially lost,
     * restore it. This is useful when performing operations that could remove dom
     * nodes and place them back in, resulting in focus being lost.
     */


    function restoreSelection(priorSelectionInformation) {
      var curFocusedElem = getActiveElementDeep();
      var priorFocusedElem = priorSelectionInformation.focusedElem;
      var priorSelectionRange = priorSelectionInformation.selectionRange;

      if (curFocusedElem !== priorFocusedElem && isInDocument(priorFocusedElem)) {
        if (priorSelectionRange !== null && hasSelectionCapabilities(priorFocusedElem)) {
          setSelection(priorFocusedElem, priorSelectionRange);
        } // Focusing a node can change the scroll position, which is undesirable


        var ancestors = [];
        var ancestor = priorFocusedElem;

        while (ancestor = ancestor.parentNode) {
          if (ancestor.nodeType === ELEMENT_NODE) {
            ancestors.push({
              element: ancestor,
              left: ancestor.scrollLeft,
              top: ancestor.scrollTop
            });
          }
        }

        if (typeof priorFocusedElem.focus === 'function') {
          priorFocusedElem.focus();
        }

        for (var i = 0; i < ancestors.length; i++) {
          var info = ancestors[i];
          info.element.scrollLeft = info.left;
          info.element.scrollTop = info.top;
        }
      }
    }
    /**
     * @getSelection: Gets the selection bounds of a focused textarea, input or
     * contentEditable node.
     * -@input: Look up selection bounds of this input
     * -@return {start: selectionStart, end: selectionEnd}
     */


    function getSelection(input) {
      var selection;

      if ('selectionStart' in input) {
        // Modern browser with input or textarea.
        selection = {
          start: input.selectionStart,
          end: input.selectionEnd
        };
      } else {
        // Content editable or old IE textarea.
        selection = getOffsets(input);
      }

      return selection || {
        start: 0,
        end: 0
      };
    }
    /**
     * @setSelection: Sets the selection bounds of a textarea or input and focuses
     * the input.
     * -@input     Set selection bounds of this input or textarea
     * -@offsets   Object of same form that is returned from get*
     */


    function setSelection(input, offsets) {
      var start = offsets.start,
          end = offsets.end;

      if (end === undefined) {
        end = start;
      }

      if ('selectionStart' in input) {
        input.selectionStart = start;
        input.selectionEnd = Math.min(end, input.value.length);
      } else {
        setOffsets(input, offsets);
      }
    }

    var validateDOMNesting = function () {};

    var updatedAncestorInfo = function () {};

    {
      // This validation code was written based on the HTML5 parsing spec:
      // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
      //
      // Note: this does not catch all invalid nesting, nor does it try to (as it's
      // not clear what practical benefit doing so provides); instead, we warn only
      // for cases where the parser will give a parse tree differing from what React
      // intended. For example, <b><div></div></b> is invalid but we don't warn
      // because it still parses correctly; we do warn for other cases like nested
      // <p> tags where the beginning of the second element implicitly closes the
      // first, causing a confusing mess.
      // https://html.spec.whatwg.org/multipage/syntax.html#special
      var specialTags = ['address', 'applet', 'area', 'article', 'aside', 'base', 'basefont', 'bgsound', 'blockquote', 'body', 'br', 'button', 'caption', 'center', 'col', 'colgroup', 'dd', 'details', 'dir', 'div', 'dl', 'dt', 'embed', 'fieldset', 'figcaption', 'figure', 'footer', 'form', 'frame', 'frameset', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'head', 'header', 'hgroup', 'hr', 'html', 'iframe', 'img', 'input', 'isindex', 'li', 'link', 'listing', 'main', 'marquee', 'menu', 'menuitem', 'meta', 'nav', 'noembed', 'noframes', 'noscript', 'object', 'ol', 'p', 'param', 'plaintext', 'pre', 'script', 'section', 'select', 'source', 'style', 'summary', 'table', 'tbody', 'td', 'template', 'textarea', 'tfoot', 'th', 'thead', 'title', 'tr', 'track', 'ul', 'wbr', 'xmp']; // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope

      var inScopeTags = ['applet', 'caption', 'html', 'table', 'td', 'th', 'marquee', 'object', 'template', // https://html.spec.whatwg.org/multipage/syntax.html#html-integration-point
      // TODO: Distinguish by namespace here -- for <title>, including it here
      // errs on the side of fewer warnings
      'foreignObject', 'desc', 'title']; // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-button-scope

      var buttonScopeTags = inScopeTags.concat(['button']); // https://html.spec.whatwg.org/multipage/syntax.html#generate-implied-end-tags

      var impliedEndTags = ['dd', 'dt', 'li', 'option', 'optgroup', 'p', 'rp', 'rt'];
      var emptyAncestorInfo = {
        current: null,
        formTag: null,
        aTagInScope: null,
        buttonTagInScope: null,
        nobrTagInScope: null,
        pTagInButtonScope: null,
        listItemTagAutoclosing: null,
        dlItemTagAutoclosing: null
      };

      updatedAncestorInfo = function (oldInfo, tag) {
        var ancestorInfo = _assign({}, oldInfo || emptyAncestorInfo);

        var info = {
          tag: tag
        };

        if (inScopeTags.indexOf(tag) !== -1) {
          ancestorInfo.aTagInScope = null;
          ancestorInfo.buttonTagInScope = null;
          ancestorInfo.nobrTagInScope = null;
        }

        if (buttonScopeTags.indexOf(tag) !== -1) {
          ancestorInfo.pTagInButtonScope = null;
        } // See rules for 'li', 'dd', 'dt' start tags in
        // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody


        if (specialTags.indexOf(tag) !== -1 && tag !== 'address' && tag !== 'div' && tag !== 'p') {
          ancestorInfo.listItemTagAutoclosing = null;
          ancestorInfo.dlItemTagAutoclosing = null;
        }

        ancestorInfo.current = info;

        if (tag === 'form') {
          ancestorInfo.formTag = info;
        }

        if (tag === 'a') {
          ancestorInfo.aTagInScope = info;
        }

        if (tag === 'button') {
          ancestorInfo.buttonTagInScope = info;
        }

        if (tag === 'nobr') {
          ancestorInfo.nobrTagInScope = info;
        }

        if (tag === 'p') {
          ancestorInfo.pTagInButtonScope = info;
        }

        if (tag === 'li') {
          ancestorInfo.listItemTagAutoclosing = info;
        }

        if (tag === 'dd' || tag === 'dt') {
          ancestorInfo.dlItemTagAutoclosing = info;
        }

        return ancestorInfo;
      };
      /**
       * Returns whether
       */


      var isTagValidWithParent = function (tag, parentTag) {
        // First, let's check if we're in an unusual parsing mode...
        switch (parentTag) {
          // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inselect
          case 'select':
            return tag === 'option' || tag === 'optgroup' || tag === '#text';

          case 'optgroup':
            return tag === 'option' || tag === '#text';
          // Strictly speaking, seeing an <option> doesn't mean we're in a <select>
          // but

          case 'option':
            return tag === '#text';
          // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intd
          // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incaption
          // No special behavior since these rules fall back to "in body" mode for
          // all except special table nodes which cause bad parsing behavior anyway.
          // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intr

          case 'tr':
            return tag === 'th' || tag === 'td' || tag === 'style' || tag === 'script' || tag === 'template';
          // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intbody

          case 'tbody':
          case 'thead':
          case 'tfoot':
            return tag === 'tr' || tag === 'style' || tag === 'script' || tag === 'template';
          // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incolgroup

          case 'colgroup':
            return tag === 'col' || tag === 'template';
          // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intable

          case 'table':
            return tag === 'caption' || tag === 'colgroup' || tag === 'tbody' || tag === 'tfoot' || tag === 'thead' || tag === 'style' || tag === 'script' || tag === 'template';
          // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inhead

          case 'head':
            return tag === 'base' || tag === 'basefont' || tag === 'bgsound' || tag === 'link' || tag === 'meta' || tag === 'title' || tag === 'noscript' || tag === 'noframes' || tag === 'style' || tag === 'script' || tag === 'template';
          // https://html.spec.whatwg.org/multipage/semantics.html#the-html-element

          case 'html':
            return tag === 'head' || tag === 'body' || tag === 'frameset';

          case 'frameset':
            return tag === 'frame';

          case '#document':
            return tag === 'html';
        } // Probably in the "in body" parsing mode, so we outlaw only tag combos
        // where the parsing rules cause implicit opens or closes to be added.
        // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody


        switch (tag) {
          case 'h1':
          case 'h2':
          case 'h3':
          case 'h4':
          case 'h5':
          case 'h6':
            return parentTag !== 'h1' && parentTag !== 'h2' && parentTag !== 'h3' && parentTag !== 'h4' && parentTag !== 'h5' && parentTag !== 'h6';

          case 'rp':
          case 'rt':
            return impliedEndTags.indexOf(parentTag) === -1;

          case 'body':
          case 'caption':
          case 'col':
          case 'colgroup':
          case 'frameset':
          case 'frame':
          case 'head':
          case 'html':
          case 'tbody':
          case 'td':
          case 'tfoot':
          case 'th':
          case 'thead':
          case 'tr':
            // These tags are only valid with a few parents that have special child
            // parsing rules -- if we're down here, then none of those matched and
            // so we allow it only if we don't know what the parent is, as all other
            // cases are invalid.
            return parentTag == null;
        }

        return true;
      };
      /**
       * Returns whether
       */


      var findInvalidAncestorForTag = function (tag, ancestorInfo) {
        switch (tag) {
          case 'address':
          case 'article':
          case 'aside':
          case 'blockquote':
          case 'center':
          case 'details':
          case 'dialog':
          case 'dir':
          case 'div':
          case 'dl':
          case 'fieldset':
          case 'figcaption':
          case 'figure':
          case 'footer':
          case 'header':
          case 'hgroup':
          case 'main':
          case 'menu':
          case 'nav':
          case 'ol':
          case 'p':
          case 'section':
          case 'summary':
          case 'ul':
          case 'pre':
          case 'listing':
          case 'table':
          case 'hr':
          case 'xmp':
          case 'h1':
          case 'h2':
          case 'h3':
          case 'h4':
          case 'h5':
          case 'h6':
            return ancestorInfo.pTagInButtonScope;

          case 'form':
            return ancestorInfo.formTag || ancestorInfo.pTagInButtonScope;

          case 'li':
            return ancestorInfo.listItemTagAutoclosing;

          case 'dd':
          case 'dt':
            return ancestorInfo.dlItemTagAutoclosing;

          case 'button':
            return ancestorInfo.buttonTagInScope;

          case 'a':
            // Spec says something about storing a list of markers, but it sounds
            // equivalent to this check.
            return ancestorInfo.aTagInScope;

          case 'nobr':
            return ancestorInfo.nobrTagInScope;
        }

        return null;
      };

      var didWarn$1 = {};

      validateDOMNesting = function (childTag, childText, ancestorInfo) {
        ancestorInfo = ancestorInfo || emptyAncestorInfo;
        var parentInfo = ancestorInfo.current;
        var parentTag = parentInfo && parentInfo.tag;

        if (childText != null) {
          !(childTag == null) ? warningWithoutStack$1(false, 'validateDOMNesting: when childText is passed, childTag should be null') : void 0;
          childTag = '#text';
        }

        var invalidParent = isTagValidWithParent(childTag, parentTag) ? null : parentInfo;
        var invalidAncestor = invalidParent ? null : findInvalidAncestorForTag(childTag, ancestorInfo);
        var invalidParentOrAncestor = invalidParent || invalidAncestor;

        if (!invalidParentOrAncestor) {
          return;
        }

        var ancestorTag = invalidParentOrAncestor.tag;
        var addendum = getCurrentFiberStackInDev();
        var warnKey = !!invalidParent + '|' + childTag + '|' + ancestorTag + '|' + addendum;

        if (didWarn$1[warnKey]) {
          return;
        }

        didWarn$1[warnKey] = true;
        var tagDisplayName = childTag;
        var whitespaceInfo = '';

        if (childTag === '#text') {
          if (/\S/.test(childText)) {
            tagDisplayName = 'Text nodes';
          } else {
            tagDisplayName = 'Whitespace text nodes';
            whitespaceInfo = " Make sure you don't have any extra whitespace between tags on " + 'each line of your source code.';
          }
        } else {
          tagDisplayName = '<' + childTag + '>';
        }

        if (invalidParent) {
          var info = '';

          if (ancestorTag === 'table' && childTag === 'tr') {
            info += ' Add a <tbody>, <thead> or <tfoot> to your code to match the DOM tree generated by ' + 'the browser.';
          }

          warningWithoutStack$1(false, 'validateDOMNesting(...): %s cannot appear as a child of <%s>.%s%s%s', tagDisplayName, ancestorTag, whitespaceInfo, info, addendum);
        } else {
          warningWithoutStack$1(false, 'validateDOMNesting(...): %s cannot appear as a descendant of ' + '<%s>.%s', tagDisplayName, ancestorTag, addendum);
        }
      };
    } // can re-export everything from this module.

    function shim() {
      (function () {
        {
          {
            throw ReactError(Error("The current renderer does not support persistence. This error is likely caused by a bug in React. Please file an issue."));
          }
        }
      })();
    } // Persistence (when unsupported)


    var supportsPersistence = false;
    var cloneInstance = shim;
    var cloneFundamentalInstance = shim;
    var createContainerChildSet = shim;
    var appendChildToContainerChildSet = shim;
    var finalizeContainerChildren = shim;
    var replaceContainerChildren = shim;
    var cloneHiddenInstance = shim;
    var cloneHiddenTextInstance = shim;
    var SUPPRESS_HYDRATION_WARNING;
    {
      SUPPRESS_HYDRATION_WARNING = 'suppressHydrationWarning';
    }
    var SUSPENSE_START_DATA = '$';
    var SUSPENSE_END_DATA = '/$';
    var SUSPENSE_PENDING_START_DATA = '$?';
    var SUSPENSE_FALLBACK_START_DATA = '$!';
    var STYLE = 'style';
    var eventsEnabled = null;
    var selectionInformation = null;

    function shouldAutoFocusHostComponent(type, props) {
      switch (type) {
        case 'button':
        case 'input':
        case 'select':
        case 'textarea':
          return !!props.autoFocus;
      }

      return false;
    }

    function getRootHostContext(rootContainerInstance) {
      var type;
      var namespace;
      var nodeType = rootContainerInstance.nodeType;

      switch (nodeType) {
        case DOCUMENT_NODE:
        case DOCUMENT_FRAGMENT_NODE:
          {
            type = nodeType === DOCUMENT_NODE ? '#document' : '#fragment';
            var root = rootContainerInstance.documentElement;
            namespace = root ? root.namespaceURI : getChildNamespace(null, '');
            break;
          }

        default:
          {
            var container = nodeType === COMMENT_NODE ? rootContainerInstance.parentNode : rootContainerInstance;
            var ownNamespace = container.namespaceURI || null;
            type = container.tagName;
            namespace = getChildNamespace(ownNamespace, type);
            break;
          }
      }

      {
        var validatedTag = type.toLowerCase();
        var ancestorInfo = updatedAncestorInfo(null, validatedTag);
        return {
          namespace: namespace,
          ancestorInfo: ancestorInfo
        };
      }
      return namespace;
    }

    function getChildHostContext(parentHostContext, type, rootContainerInstance) {
      {
        var parentHostContextDev = parentHostContext;
        var namespace = getChildNamespace(parentHostContextDev.namespace, type);
        var ancestorInfo = updatedAncestorInfo(parentHostContextDev.ancestorInfo, type);
        return {
          namespace: namespace,
          ancestorInfo: ancestorInfo
        };
      }
      var parentNamespace = parentHostContext;
      return getChildNamespace(parentNamespace, type);
    }

    function getPublicInstance(instance) {
      return instance;
    }

    function prepareForCommit(containerInfo) {
      eventsEnabled = isEnabled();
      selectionInformation = getSelectionInformation();
      setEnabled(false);
    }

    function resetAfterCommit(containerInfo) {
      restoreSelection(selectionInformation);
      selectionInformation = null;
      setEnabled(eventsEnabled);
      eventsEnabled = null;
    }

    function createInstance(type, props, rootContainerInstance, hostContext, internalInstanceHandle) {
      var parentNamespace;
      {
        // TODO: take namespace into account when validating.
        var hostContextDev = hostContext;
        validateDOMNesting(type, null, hostContextDev.ancestorInfo);

        if (typeof props.children === 'string' || typeof props.children === 'number') {
          var string = '' + props.children;
          var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type);
          validateDOMNesting(null, string, ownAncestorInfo);
        }

        parentNamespace = hostContextDev.namespace;
      }
      var domElement = createElement(type, props, rootContainerInstance, parentNamespace);
      precacheFiberNode(internalInstanceHandle, domElement);
      updateFiberProps(domElement, props);
      return domElement;
    }

    function appendInitialChild(parentInstance, child) {
      parentInstance.appendChild(child);
    }

    function finalizeInitialChildren(domElement, type, props, rootContainerInstance, hostContext) {
      setInitialProperties(domElement, type, props, rootContainerInstance);
      return shouldAutoFocusHostComponent(type, props);
    }

    function prepareUpdate(domElement, type, oldProps, newProps, rootContainerInstance, hostContext) {
      {
        var hostContextDev = hostContext;

        if (typeof newProps.children !== typeof oldProps.children && (typeof newProps.children === 'string' || typeof newProps.children === 'number')) {
          var string = '' + newProps.children;
          var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type);
          validateDOMNesting(null, string, ownAncestorInfo);
        }
      }
      return diffProperties(domElement, type, oldProps, newProps, rootContainerInstance);
    }

    function shouldSetTextContent(type, props) {
      return type === 'textarea' || type === 'option' || type === 'noscript' || typeof props.children === 'string' || typeof props.children === 'number' || typeof props.dangerouslySetInnerHTML === 'object' && props.dangerouslySetInnerHTML !== null && props.dangerouslySetInnerHTML.__html != null;
    }

    function shouldDeprioritizeSubtree(type, props) {
      return !!props.hidden;
    }

    function createTextInstance(text, rootContainerInstance, hostContext, internalInstanceHandle) {
      {
        var hostContextDev = hostContext;
        validateDOMNesting(null, text, hostContextDev.ancestorInfo);
      }
      var textNode = createTextNode(text, rootContainerInstance);
      precacheFiberNode(internalInstanceHandle, textNode);
      return textNode;
    }

    var isPrimaryRenderer = true;
    var warnsIfNotActing = true; // This initialization code may run even on server environments
    // if a component just imports ReactDOM (e.g. for findDOMNode).
    // Some environments might not have setTimeout or clearTimeout.

    var scheduleTimeout = typeof setTimeout === 'function' ? setTimeout : undefined;
    var cancelTimeout = typeof clearTimeout === 'function' ? clearTimeout : undefined;
    var noTimeout = -1; // -------------------
    //     Mutation
    // -------------------

    var supportsMutation = true;

    function commitMount(domElement, type, newProps, internalInstanceHandle) {
      // Despite the naming that might imply otherwise, this method only
      // fires if there is an `Update` effect scheduled during mounting.
      // This happens if `finalizeInitialChildren` returns `true` (which it
      // does to implement the `autoFocus` attribute on the client). But
      // there are also other cases when this might happen (such as patching
      // up text content during hydration mismatch). So we'll check this again.
      if (shouldAutoFocusHostComponent(type, newProps)) {
        domElement.focus();
      }
    }

    function commitUpdate(domElement, updatePayload, type, oldProps, newProps, internalInstanceHandle) {
      // Update the props handle so that we know which props are the ones with
      // with current event handlers.
      updateFiberProps(domElement, newProps); // Apply the diff to the DOM node.

      updateProperties(domElement, updatePayload, type, oldProps, newProps);
    }

    function resetTextContent(domElement) {
      setTextContent(domElement, '');
    }

    function commitTextUpdate(textInstance, oldText, newText) {
      textInstance.nodeValue = newText;
    }

    function appendChild(parentInstance, child) {
      parentInstance.appendChild(child);
    }

    function appendChildToContainer(container, child) {
      var parentNode;

      if (container.nodeType === COMMENT_NODE) {
        parentNode = container.parentNode;
        parentNode.insertBefore(child, container);
      } else {
        parentNode = container;
        parentNode.appendChild(child);
      } // This container might be used for a portal.
      // If something inside a portal is clicked, that click should bubble
      // through the React tree. However, on Mobile Safari the click would
      // never bubble through the *DOM* tree unless an ancestor with onclick
      // event exists. So we wouldn't see it and dispatch it.
      // This is why we ensure that non React root containers have inline onclick
      // defined.
      // https://github.com/facebook/react/issues/11918


      var reactRootContainer = container._reactRootContainer;

      if ((reactRootContainer === null || reactRootContainer === undefined) && parentNode.onclick === null) {
        // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(parentNode);
      }
    }

    function insertBefore(parentInstance, child, beforeChild) {
      parentInstance.insertBefore(child, beforeChild);
    }

    function insertInContainerBefore(container, child, beforeChild) {
      if (container.nodeType === COMMENT_NODE) {
        container.parentNode.insertBefore(child, beforeChild);
      } else {
        container.insertBefore(child, beforeChild);
      }
    }

    function removeChild(parentInstance, child) {
      parentInstance.removeChild(child);
    }

    function removeChildFromContainer(container, child) {
      if (container.nodeType === COMMENT_NODE) {
        container.parentNode.removeChild(child);
      } else {
        container.removeChild(child);
      }
    }

    function clearSuspenseBoundary(parentInstance, suspenseInstance) {
      var node = suspenseInstance; // Delete all nodes within this suspense boundary.
      // There might be nested nodes so we need to keep track of how
      // deep we are and only break out when we're back on top.

      var depth = 0;

      do {
        var nextNode = node.nextSibling;
        parentInstance.removeChild(node);

        if (nextNode && nextNode.nodeType === COMMENT_NODE) {
          var data = nextNode.data;

          if (data === SUSPENSE_END_DATA) {
            if (depth === 0) {
              parentInstance.removeChild(nextNode); // Retry if any event replaying was blocked on this.

              retryIfBlockedOn(suspenseInstance);
              return;
            } else {
              depth--;
            }
          } else if (data === SUSPENSE_START_DATA || data === SUSPENSE_PENDING_START_DATA || data === SUSPENSE_FALLBACK_START_DATA) {
            depth++;
          }
        }

        node = nextNode;
      } while (node); // TODO: Warn, we didn't find the end comment boundary.
      // Retry if any event replaying was blocked on this.


      retryIfBlockedOn(suspenseInstance);
    }

    function clearSuspenseBoundaryFromContainer(container, suspenseInstance) {
      if (container.nodeType === COMMENT_NODE) {
        clearSuspenseBoundary(container.parentNode, suspenseInstance);
      } else if (container.nodeType === ELEMENT_NODE) {
        clearSuspenseBoundary(container, suspenseInstance);
      } else {} // Document nodes should never contain suspense boundaries.
      // Retry if any event replaying was blocked on this.


      retryIfBlockedOn(container);
    }

    function hideInstance(instance) {
      // TODO: Does this work for all element types? What about MathML? Should we
      // pass host context to this method?
      instance = instance;
      var style = instance.style;

      if (typeof style.setProperty === 'function') {
        style.setProperty('display', 'none', 'important');
      } else {
        style.display = 'none';
      }
    }

    function hideTextInstance(textInstance) {
      textInstance.nodeValue = '';
    }

    function unhideInstance(instance, props) {
      instance = instance;
      var styleProp = props[STYLE];
      var display = styleProp !== undefined && styleProp !== null && styleProp.hasOwnProperty('display') ? styleProp.display : null;
      instance.style.display = dangerousStyleValue('display', display);
    }

    function unhideTextInstance(textInstance, text) {
      textInstance.nodeValue = text;
    } // -------------------
    //     Hydration
    // -------------------


    var supportsHydration = true;

    function canHydrateInstance(instance, type, props) {
      if (instance.nodeType !== ELEMENT_NODE || type.toLowerCase() !== instance.nodeName.toLowerCase()) {
        return null;
      } // This has now been refined to an element node.


      return instance;
    }

    function canHydrateTextInstance(instance, text) {
      if (text === '' || instance.nodeType !== TEXT_NODE) {
        // Empty strings are not parsed by HTML so there won't be a correct match here.
        return null;
      } // This has now been refined to a text node.


      return instance;
    }

    function canHydrateSuspenseInstance(instance) {
      if (instance.nodeType !== COMMENT_NODE) {
        // Empty strings are not parsed by HTML so there won't be a correct match here.
        return null;
      } // This has now been refined to a suspense node.


      return instance;
    }

    function isSuspenseInstancePending(instance) {
      return instance.data === SUSPENSE_PENDING_START_DATA;
    }

    function isSuspenseInstanceFallback(instance) {
      return instance.data === SUSPENSE_FALLBACK_START_DATA;
    }

    function registerSuspenseInstanceRetry(instance, callback) {
      instance._reactRetry = callback;
    }

    function getNextHydratable(node) {
      // Skip non-hydratable nodes.
      for (; node != null; node = node.nextSibling) {
        var nodeType = node.nodeType;

        if (nodeType === ELEMENT_NODE || nodeType === TEXT_NODE) {
          break;
        }

        if (enableSuspenseServerRenderer) {
          if (nodeType === COMMENT_NODE) {
            var nodeData = node.data;

            if (nodeData === SUSPENSE_START_DATA || nodeData === SUSPENSE_FALLBACK_START_DATA || nodeData === SUSPENSE_PENDING_START_DATA) {
              break;
            }
          }
        }
      }

      return node;
    }

    function getNextHydratableSibling(instance) {
      return getNextHydratable(instance.nextSibling);
    }

    function getFirstHydratableChild(parentInstance) {
      return getNextHydratable(parentInstance.firstChild);
    }

    function hydrateInstance(instance, type, props, rootContainerInstance, hostContext, internalInstanceHandle) {
      precacheFiberNode(internalInstanceHandle, instance); // TODO: Possibly defer this until the commit phase where all the events
      // get attached.

      updateFiberProps(instance, props);
      var parentNamespace;
      {
        var hostContextDev = hostContext;
        parentNamespace = hostContextDev.namespace;
      }
      return diffHydratedProperties(instance, type, props, parentNamespace, rootContainerInstance);
    }

    function hydrateTextInstance(textInstance, text, internalInstanceHandle) {
      precacheFiberNode(internalInstanceHandle, textInstance);
      return diffHydratedText(textInstance, text);
    }

    function hydrateSuspenseInstance(suspenseInstance, internalInstanceHandle) {
      precacheFiberNode(internalInstanceHandle, suspenseInstance);
    }

    function getNextHydratableInstanceAfterSuspenseInstance(suspenseInstance) {
      var node = suspenseInstance.nextSibling; // Skip past all nodes within this suspense boundary.
      // There might be nested nodes so we need to keep track of how
      // deep we are and only break out when we're back on top.

      var depth = 0;

      while (node) {
        if (node.nodeType === COMMENT_NODE) {
          var data = node.data;

          if (data === SUSPENSE_END_DATA) {
            if (depth === 0) {
              return getNextHydratableSibling(node);
            } else {
              depth--;
            }
          } else if (data === SUSPENSE_START_DATA || data === SUSPENSE_FALLBACK_START_DATA || data === SUSPENSE_PENDING_START_DATA) {
            depth++;
          }
        }

        node = node.nextSibling;
      } // TODO: Warn, we didn't find the end comment boundary.


      return null;
    } // Returns the SuspenseInstance if this node is a direct child of a
    // SuspenseInstance. I.e. if its previous sibling is a Comment with
    // SUSPENSE_x_START_DATA. Otherwise, null.


    function getParentSuspenseInstance(targetInstance) {
      var node = targetInstance.previousSibling; // Skip past all nodes within this suspense boundary.
      // There might be nested nodes so we need to keep track of how
      // deep we are and only break out when we're back on top.

      var depth = 0;

      while (node) {
        if (node.nodeType === COMMENT_NODE) {
          var data = node.data;

          if (data === SUSPENSE_START_DATA || data === SUSPENSE_FALLBACK_START_DATA || data === SUSPENSE_PENDING_START_DATA) {
            if (depth === 0) {
              return node;
            } else {
              depth--;
            }
          } else if (data === SUSPENSE_END_DATA) {
            depth++;
          }
        }

        node = node.previousSibling;
      }

      return null;
    }

    function commitHydratedContainer(container) {
      // Retry if any event replaying was blocked on this.
      retryIfBlockedOn(container);
    }

    function commitHydratedSuspenseInstance(suspenseInstance) {
      // Retry if any event replaying was blocked on this.
      retryIfBlockedOn(suspenseInstance);
    }

    function didNotMatchHydratedContainerTextInstance(parentContainer, textInstance, text) {
      {
        warnForUnmatchedText(textInstance, text);
      }
    }

    function didNotMatchHydratedTextInstance(parentType, parentProps, parentInstance, textInstance, text) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        warnForUnmatchedText(textInstance, text);
      }
    }

    function didNotHydrateContainerInstance(parentContainer, instance) {
      {
        if (instance.nodeType === ELEMENT_NODE) {
          warnForDeletedHydratableElement(parentContainer, instance);
        } else if (instance.nodeType === COMMENT_NODE) {// TODO: warnForDeletedHydratableSuspenseBoundary
        } else {
          warnForDeletedHydratableText(parentContainer, instance);
        }
      }
    }

    function didNotHydrateInstance(parentType, parentProps, parentInstance, instance) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        if (instance.nodeType === ELEMENT_NODE) {
          warnForDeletedHydratableElement(parentInstance, instance);
        } else if (instance.nodeType === COMMENT_NODE) {// TODO: warnForDeletedHydratableSuspenseBoundary
        } else {
          warnForDeletedHydratableText(parentInstance, instance);
        }
      }
    }

    function didNotFindHydratableContainerInstance(parentContainer, type, props) {
      {
        warnForInsertedHydratedElement(parentContainer, type, props);
      }
    }

    function didNotFindHydratableContainerTextInstance(parentContainer, text) {
      {
        warnForInsertedHydratedText(parentContainer, text);
      }
    }

    function didNotFindHydratableInstance(parentType, parentProps, parentInstance, type, props) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        warnForInsertedHydratedElement(parentInstance, type, props);
      }
    }

    function didNotFindHydratableTextInstance(parentType, parentProps, parentInstance, text) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        warnForInsertedHydratedText(parentInstance, text);
      }
    }

    function didNotFindHydratableSuspenseInstance(parentType, parentProps, parentInstance) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {// TODO: warnForInsertedHydratedSuspense(parentInstance);
      }
    }

    function mountResponderInstance(responder, responderInstance, responderProps, responderState, instance) {
      // Listen to events
      var doc = instance.ownerDocument;
      var _ref = responder,
          rootEventTypes = _ref.rootEventTypes,
          targetEventTypes = _ref.targetEventTypes;

      if (targetEventTypes !== null) {
        listenToEventResponderEventTypes(targetEventTypes, doc);
      }

      if (rootEventTypes !== null) {
        addRootEventTypesForResponderInstance(responderInstance, rootEventTypes);
        listenToEventResponderEventTypes(rootEventTypes, doc);
      }

      mountEventResponder(responder, responderInstance, responderProps, responderState);
      return responderInstance;
    }

    function unmountResponderInstance(responderInstance) {
      if (enableFlareAPI) {
        // TODO stop listening to targetEventTypes
        unmountEventResponder(responderInstance);
      }
    }

    function getFundamentalComponentInstance(fundamentalInstance) {
      if (enableFundamentalAPI) {
        var currentFiber = fundamentalInstance.currentFiber,
            impl = fundamentalInstance.impl,
            props = fundamentalInstance.props,
            state = fundamentalInstance.state;
        var instance = impl.getInstance(null, props, state);
        precacheFiberNode(currentFiber, instance);
        return instance;
      } // Because of the flag above, this gets around the Flow error;


      return null;
    }

    function mountFundamentalComponent(fundamentalInstance) {
      if (enableFundamentalAPI) {
        var impl = fundamentalInstance.impl,
            instance = fundamentalInstance.instance,
            props = fundamentalInstance.props,
            state = fundamentalInstance.state;
        var onMount = impl.onMount;

        if (onMount !== undefined) {
          onMount(null, instance, props, state);
        }
      }
    }

    function shouldUpdateFundamentalComponent(fundamentalInstance) {
      if (enableFundamentalAPI) {
        var impl = fundamentalInstance.impl,
            prevProps = fundamentalInstance.prevProps,
            props = fundamentalInstance.props,
            state = fundamentalInstance.state;
        var shouldUpdate = impl.shouldUpdate;

        if (shouldUpdate !== undefined) {
          return shouldUpdate(null, prevProps, props, state);
        }
      }

      return true;
    }

    function updateFundamentalComponent(fundamentalInstance) {
      if (enableFundamentalAPI) {
        var impl = fundamentalInstance.impl,
            instance = fundamentalInstance.instance,
            prevProps = fundamentalInstance.prevProps,
            props = fundamentalInstance.props,
            state = fundamentalInstance.state;
        var onUpdate = impl.onUpdate;

        if (onUpdate !== undefined) {
          onUpdate(null, instance, prevProps, props, state);
        }
      }
    }

    function unmountFundamentalComponent(fundamentalInstance) {
      if (enableFundamentalAPI) {
        var impl = fundamentalInstance.impl,
            instance = fundamentalInstance.instance,
            props = fundamentalInstance.props,
            state = fundamentalInstance.state;
        var onUnmount = impl.onUnmount;

        if (onUnmount !== undefined) {
          onUnmount(null, instance, props, state);
        }
      }
    }

    var randomKey = Math.random().toString(36).slice(2);
    var internalInstanceKey = '__reactInternalInstance$' + randomKey;
    var internalEventHandlersKey = '__reactEventHandlers$' + randomKey;
    var internalContainerInstanceKey = '__reactContainere$' + randomKey;

    function precacheFiberNode(hostInst, node) {
      node[internalInstanceKey] = hostInst;
    }

    function markContainerAsRoot(hostRoot, node) {
      node[internalContainerInstanceKey] = hostRoot;
    } // Given a DOM node, return the closest HostComponent or HostText fiber ancestor.
    // If the target node is part of a hydrated or not yet rendered subtree, then
    // this may also return a SuspenseComponent or HostRoot to indicate that.
    // Conceptually the HostRoot fiber is a child of the Container node. So if you
    // pass the Container node as the targetNode, you wiill not actually get the
    // HostRoot back. To get to the HostRoot, you need to pass a child of it.
    // The same thing applies to Suspense boundaries.


    function getClosestInstanceFromNode(targetNode) {
      var targetInst = targetNode[internalInstanceKey];

      if (targetInst) {
        // Don't return HostRoot or SuspenseComponent here.
        return targetInst;
      } // If the direct event target isn't a React owned DOM node, we need to look
      // to see if one of its parents is a React owned DOM node.


      var parentNode = targetNode.parentNode;

      while (parentNode) {
        // We'll check if this is a container root that could include
        // React nodes in the future. We need to check this first because
        // if we're a child of a dehydrated container, we need to first
        // find that inner container before moving on to finding the parent
        // instance. Note that we don't check this field on  the targetNode
        // itself because the fibers are conceptually between the container
        // node and the first child. It isn't surrounding the container node.
        // If it's not a container, we check if it's an instance.
        targetInst = parentNode[internalContainerInstanceKey] || parentNode[internalInstanceKey];

        if (targetInst) {
          // Since this wasn't the direct target of the event, we might have
          // stepped past dehydrated DOM nodes to get here. However they could
          // also have been non-React nodes. We need to answer which one.
          // If we the instance doesn't have any children, then there can't be
          // a nested suspense boundary within it. So we can use this as a fast
          // bailout. Most of the time, when people add non-React children to
          // the tree, it is using a ref to a child-less DOM node.
          // Normally we'd only need to check one of the fibers because if it
          // has ever gone from having children to deleting them or vice versa
          // it would have deleted the dehydrated boundary nested inside already.
          // However, since the HostRoot starts out with an alternate it might
          // have one on the alternate so we need to check in case this was a
          // root.
          var alternate = targetInst.alternate;

          if (targetInst.child !== null || alternate !== null && alternate.child !== null) {
            // Next we need to figure out if the node that skipped past is
            // nested within a dehydrated boundary and if so, which one.
            var suspenseInstance = getParentSuspenseInstance(targetNode);

            while (suspenseInstance !== null) {
              // We found a suspense instance. That means that we haven't
              // hydrated it yet. Even though we leave the comments in the
              // DOM after hydrating, and there are boundaries in the DOM
              // that could already be hydrated, we wouldn't have found them
              // through this pass since if the target is hydrated it would
              // have had an internalInstanceKey on it.
              // Let's get the fiber associated with the SuspenseComponent
              // as the deepest instance.
              var targetSuspenseInst = suspenseInstance[internalInstanceKey];

              if (targetSuspenseInst) {
                return targetSuspenseInst;
              } // If we don't find a Fiber on the comment, it might be because
              // we haven't gotten to hydrate it yet. There might still be a
              // parent boundary that hasn't above this one so we need to find
              // the outer most that is known.


              suspenseInstance = getParentSuspenseInstance(suspenseInstance); // If we don't find one, then that should mean that the parent
              // host component also hasn't hydrated yet. We can return it
              // below since it will bail out on the isMounted check later.
            }
          }

          return targetInst;
        }

        targetNode = parentNode;
        parentNode = targetNode.parentNode;
      }

      return null;
    }
    /**
     * Given a DOM node, return the ReactDOMComponent or ReactDOMTextComponent
     * instance, or null if the node was not rendered by this React.
     */


    function getInstanceFromNode$1(node) {
      var inst = node[internalInstanceKey] || node[internalContainerInstanceKey];

      if (inst) {
        if (inst.tag === HostComponent || inst.tag === HostText || inst.tag === SuspenseComponent || inst.tag === HostRoot) {
          return inst;
        } else {
          return null;
        }
      }

      return null;
    }
    /**
     * Given a ReactDOMComponent or ReactDOMTextComponent, return the corresponding
     * DOM node.
     */


    function getNodeFromInstance$1(inst) {
      if (inst.tag === HostComponent || inst.tag === HostText) {
        // In Fiber this, is just the state node right now. We assume it will be
        // a host component or host text.
        return inst.stateNode;
      } // Without this first invariant, passing a non-DOM-component triggers the next
      // invariant for a missing parent, which is super confusing.


      (function () {
        {
          {
            throw ReactError(Error("getNodeFromInstance: Invalid argument."));
          }
        }
      })();
    }

    function getFiberCurrentPropsFromNode$1(node) {
      return node[internalEventHandlersKey] || null;
    }

    function updateFiberProps(node, props) {
      node[internalEventHandlersKey] = props;
    }
    /**
     * These variables store information about text content of a target node,
     * allowing comparison of content before and after a given event.
     *
     * Identify the node where selection currently begins, then observe
     * both its text content and its current position in the DOM. Since the
     * browser may natively replace the target node during composition, we can
     * use its position to find its replacement.
     *
     *
     */


    var root = null;
    var startText = null;
    var fallbackText = null;

    function initialize(nativeEventTarget) {
      root = nativeEventTarget;
      startText = getText();
      return true;
    }

    function reset() {
      root = null;
      startText = null;
      fallbackText = null;
    }

    function getData() {
      if (fallbackText) {
        return fallbackText;
      }

      var start;
      var startValue = startText;
      var startLength = startValue.length;
      var end;
      var endValue = getText();
      var endLength = endValue.length;

      for (start = 0; start < startLength; start++) {
        if (startValue[start] !== endValue[start]) {
          break;
        }
      }

      var minEnd = startLength - start;

      for (end = 1; end <= minEnd; end++) {
        if (startValue[startLength - end] !== endValue[endLength - end]) {
          break;
        }
      }

      var sliceTail = end > 1 ? 1 - end : undefined;
      fallbackText = endValue.slice(start, sliceTail);
      return fallbackText;
    }

    function getText() {
      if ('value' in root) {
        return root.value;
      }

      return root.textContent;
    }
    /**
     * @interface Event
     * @see http://www.w3.org/TR/DOM-Level-3-Events/#events-compositionevents
     */


    var SyntheticCompositionEvent = SyntheticEvent.extend({
      data: null
    });
    /**
     * @interface Event
     * @see http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105
     *      /#events-inputevents
     */

    var SyntheticInputEvent = SyntheticEvent.extend({
      data: null
    });
    var END_KEYCODES = [9, 13, 27, 32]; // Tab, Return, Esc, Space

    var START_KEYCODE = 229;
    var canUseCompositionEvent = canUseDOM && 'CompositionEvent' in window;
    var documentMode = null;

    if (canUseDOM && 'documentMode' in document) {
      documentMode = document.documentMode;
    } // Webkit offers a very useful `textInput` event that can be used to
    // directly represent `beforeInput`. The IE `textinput` event is not as
    // useful, so we don't use it.


    var canUseTextInputEvent = canUseDOM && 'TextEvent' in window && !documentMode; // In IE9+, we have access to composition events, but the data supplied
    // by the native compositionend event may be incorrect. Japanese ideographic
    // spaces, for instance (\u3000) are not recorded correctly.

    var useFallbackCompositionData = canUseDOM && (!canUseCompositionEvent || documentMode && documentMode > 8 && documentMode <= 11);
    var SPACEBAR_CODE = 32;
    var SPACEBAR_CHAR = String.fromCharCode(SPACEBAR_CODE); // Events and their corresponding property names.

    var eventTypes$1 = {
      beforeInput: {
        phasedRegistrationNames: {
          bubbled: 'onBeforeInput',
          captured: 'onBeforeInputCapture'
        },
        dependencies: [TOP_COMPOSITION_END, TOP_KEY_PRESS, TOP_TEXT_INPUT, TOP_PASTE]
      },
      compositionEnd: {
        phasedRegistrationNames: {
          bubbled: 'onCompositionEnd',
          captured: 'onCompositionEndCapture'
        },
        dependencies: [TOP_BLUR, TOP_COMPOSITION_END, TOP_KEY_DOWN, TOP_KEY_PRESS, TOP_KEY_UP, TOP_MOUSE_DOWN]
      },
      compositionStart: {
        phasedRegistrationNames: {
          bubbled: 'onCompositionStart',
          captured: 'onCompositionStartCapture'
        },
        dependencies: [TOP_BLUR, TOP_COMPOSITION_START, TOP_KEY_DOWN, TOP_KEY_PRESS, TOP_KEY_UP, TOP_MOUSE_DOWN]
      },
      compositionUpdate: {
        phasedRegistrationNames: {
          bubbled: 'onCompositionUpdate',
          captured: 'onCompositionUpdateCapture'
        },
        dependencies: [TOP_BLUR, TOP_COMPOSITION_UPDATE, TOP_KEY_DOWN, TOP_KEY_PRESS, TOP_KEY_UP, TOP_MOUSE_DOWN]
      }
    }; // Track whether we've ever handled a keypress on the space key.

    var hasSpaceKeypress = false;
    /**
     * Return whether a native keypress event is assumed to be a command.
     * This is required because Firefox fires `keypress` events for key commands
     * (cut, copy, select-all, etc.) even though no character is inserted.
     */

    function isKeypressCommand(nativeEvent) {
      return (nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) && // ctrlKey && altKey is equivalent to AltGr, and is not a command.
      !(nativeEvent.ctrlKey && nativeEvent.altKey);
    }
    /**
     * Translate native top level events into event types.
     *
     * @param {string} topLevelType
     * @return {object}
     */


    function getCompositionEventType(topLevelType) {
      switch (topLevelType) {
        case TOP_COMPOSITION_START:
          return eventTypes$1.compositionStart;

        case TOP_COMPOSITION_END:
          return eventTypes$1.compositionEnd;

        case TOP_COMPOSITION_UPDATE:
          return eventTypes$1.compositionUpdate;
      }
    }
    /**
     * Does our fallback best-guess model think this event signifies that
     * composition has begun?
     *
     * @param {string} topLevelType
     * @param {object} nativeEvent
     * @return {boolean}
     */


    function isFallbackCompositionStart(topLevelType, nativeEvent) {
      return topLevelType === TOP_KEY_DOWN && nativeEvent.keyCode === START_KEYCODE;
    }
    /**
     * Does our fallback mode think that this event is the end of composition?
     *
     * @param {string} topLevelType
     * @param {object} nativeEvent
     * @return {boolean}
     */


    function isFallbackCompositionEnd(topLevelType, nativeEvent) {
      switch (topLevelType) {
        case TOP_KEY_UP:
          // Command keys insert or clear IME input.
          return END_KEYCODES.indexOf(nativeEvent.keyCode) !== -1;

        case TOP_KEY_DOWN:
          // Expect IME keyCode on each keydown. If we get any other
          // code we must have exited earlier.
          return nativeEvent.keyCode !== START_KEYCODE;

        case TOP_KEY_PRESS:
        case TOP_MOUSE_DOWN:
        case TOP_BLUR:
          // Events are not possible without cancelling IME.
          return true;

        default:
          return false;
      }
    }
    /**
     * Google Input Tools provides composition data via a CustomEvent,
     * with the `data` property populated in the `detail` object. If this
     * is available on the event object, use it. If not, this is a plain
     * composition event and we have nothing special to extract.
     *
     * @param {object} nativeEvent
     * @return {?string}
     */


    function getDataFromCustomEvent(nativeEvent) {
      var detail = nativeEvent.detail;

      if (typeof detail === 'object' && 'data' in detail) {
        return detail.data;
      }

      return null;
    }
    /**
     * Check if a composition event was triggered by Korean IME.
     * Our fallback mode does not work well with IE's Korean IME,
     * so just use native composition events when Korean IME is used.
     * Although CompositionEvent.locale property is deprecated,
     * it is available in IE, where our fallback mode is enabled.
     *
     * @param {object} nativeEvent
     * @return {boolean}
     */


    function isUsingKoreanIME(nativeEvent) {
      return nativeEvent.locale === 'ko';
    } // Track the current IME composition status, if any.


    var isComposing = false;
    /**
     * @return {?object} A SyntheticCompositionEvent.
     */

    function extractCompositionEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
      var eventType;
      var fallbackData;

      if (canUseCompositionEvent) {
        eventType = getCompositionEventType(topLevelType);
      } else if (!isComposing) {
        if (isFallbackCompositionStart(topLevelType, nativeEvent)) {
          eventType = eventTypes$1.compositionStart;
        }
      } else if (isFallbackCompositionEnd(topLevelType, nativeEvent)) {
        eventType = eventTypes$1.compositionEnd;
      }

      if (!eventType) {
        return null;
      }

      if (useFallbackCompositionData && !isUsingKoreanIME(nativeEvent)) {
        // The current composition is stored statically and must not be
        // overwritten while composition continues.
        if (!isComposing && eventType === eventTypes$1.compositionStart) {
          isComposing = initialize(nativeEventTarget);
        } else if (eventType === eventTypes$1.compositionEnd) {
          if (isComposing) {
            fallbackData = getData();
          }
        }
      }

      var event = SyntheticCompositionEvent.getPooled(eventType, targetInst, nativeEvent, nativeEventTarget);

      if (fallbackData) {
        // Inject data generated from fallback path into the synthetic event.
        // This matches the property of native CompositionEventInterface.
        event.data = fallbackData;
      } else {
        var customData = getDataFromCustomEvent(nativeEvent);

        if (customData !== null) {
          event.data = customData;
        }
      }

      accumulateTwoPhaseDispatches(event);
      return event;
    }
    /**
     * @param {TopLevelType} topLevelType Number from `TopLevelType`.
     * @param {object} nativeEvent Native browser event.
     * @return {?string} The string corresponding to this `beforeInput` event.
     */


    function getNativeBeforeInputChars(topLevelType, nativeEvent) {
      switch (topLevelType) {
        case TOP_COMPOSITION_END:
          return getDataFromCustomEvent(nativeEvent);

        case TOP_KEY_PRESS:
          /**
           * If native `textInput` events are available, our goal is to make
           * use of them. However, there is a special case: the spacebar key.
           * In Webkit, preventing default on a spacebar `textInput` event
           * cancels character insertion, but it *also* causes the browser
           * to fall back to its default spacebar behavior of scrolling the
           * page.
           *
           * Tracking at:
           * https://code.google.com/p/chromium/issues/detail?id=355103
           *
           * To avoid this issue, use the keypress event as if no `textInput`
           * event is available.
           */
          var which = nativeEvent.which;

          if (which !== SPACEBAR_CODE) {
            return null;
          }

          hasSpaceKeypress = true;
          return SPACEBAR_CHAR;

        case TOP_TEXT_INPUT:
          // Record the characters to be added to the DOM.
          var chars = nativeEvent.data; // If it's a spacebar character, assume that we have already handled
          // it at the keypress level and bail immediately. Android Chrome
          // doesn't give us keycodes, so we need to ignore it.

          if (chars === SPACEBAR_CHAR && hasSpaceKeypress) {
            return null;
          }

          return chars;

        default:
          // For other native event types, do nothing.
          return null;
      }
    }
    /**
     * For browsers that do not provide the `textInput` event, extract the
     * appropriate string to use for SyntheticInputEvent.
     *
     * @param {number} topLevelType Number from `TopLevelEventTypes`.
     * @param {object} nativeEvent Native browser event.
     * @return {?string} The fallback string for this `beforeInput` event.
     */


    function getFallbackBeforeInputChars(topLevelType, nativeEvent) {
      // If we are currently composing (IME) and using a fallback to do so,
      // try to extract the composed characters from the fallback object.
      // If composition event is available, we extract a string only at
      // compositionevent, otherwise extract it at fallback events.
      if (isComposing) {
        if (topLevelType === TOP_COMPOSITION_END || !canUseCompositionEvent && isFallbackCompositionEnd(topLevelType, nativeEvent)) {
          var chars = getData();
          reset();
          isComposing = false;
          return chars;
        }

        return null;
      }

      switch (topLevelType) {
        case TOP_PASTE:
          // If a paste event occurs after a keypress, throw out the input
          // chars. Paste events should not lead to BeforeInput events.
          return null;

        case TOP_KEY_PRESS:
          /**
           * As of v27, Firefox may fire keypress events even when no character
           * will be inserted. A few possibilities:
           *
           * - `which` is `0`. Arrow keys, Esc key, etc.
           *
           * - `which` is the pressed key code, but no char is available.
           *   Ex: 'AltGr + d` in Polish. There is no modified character for
           *   this key combination and no character is inserted into the
           *   document, but FF fires the keypress for char code `100` anyway.
           *   No `input` event will occur.
           *
           * - `which` is the pressed key code, but a command combination is
           *   being used. Ex: `Cmd+C`. No character is inserted, and no
           *   `input` event will occur.
           */
          if (!isKeypressCommand(nativeEvent)) {
            // IE fires the `keypress` event when a user types an emoji via
            // Touch keyboard of Windows.  In such a case, the `char` property
            // holds an emoji character like `\uD83D\uDE0A`.  Because its length
            // is 2, the property `which` does not represent an emoji correctly.
            // In such a case, we directly return the `char` property instead of
            // using `which`.
            if (nativeEvent.char && nativeEvent.char.length > 1) {
              return nativeEvent.char;
            } else if (nativeEvent.which) {
              return String.fromCharCode(nativeEvent.which);
            }
          }

          return null;

        case TOP_COMPOSITION_END:
          return useFallbackCompositionData && !isUsingKoreanIME(nativeEvent) ? null : nativeEvent.data;

        default:
          return null;
      }
    }
    /**
     * Extract a SyntheticInputEvent for `beforeInput`, based on either native
     * `textInput` or fallback behavior.
     *
     * @return {?object} A SyntheticInputEvent.
     */


    function extractBeforeInputEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
      var chars;

      if (canUseTextInputEvent) {
        chars = getNativeBeforeInputChars(topLevelType, nativeEvent);
      } else {
        chars = getFallbackBeforeInputChars(topLevelType, nativeEvent);
      } // If no characters are being inserted, no BeforeInput event should
      // be fired.


      if (!chars) {
        return null;
      }

      var event = SyntheticInputEvent.getPooled(eventTypes$1.beforeInput, targetInst, nativeEvent, nativeEventTarget);
      event.data = chars;
      accumulateTwoPhaseDispatches(event);
      return event;
    }
    /**
     * Create an `onBeforeInput` event to match
     * http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105/#events-inputevents.
     *
     * This event plugin is based on the native `textInput` event
     * available in Chrome, Safari, Opera, and IE. This event fires after
     * `onKeyPress` and `onCompositionEnd`, but before `onInput`.
     *
     * `beforeInput` is spec'd but not implemented in any browsers, and
     * the `input` event does not provide any useful information about what has
     * actually been added, contrary to the spec. Thus, `textInput` is the best
     * available event to identify the characters that have actually been inserted
     * into the target node.
     *
     * This plugin is also responsible for emitting `composition` events, thus
     * allowing us to share composition fallback code for both `beforeInput` and
     * `composition` event types.
     */


    var BeforeInputEventPlugin = {
      eventTypes: eventTypes$1,
      extractEvents: function (topLevelType, eventSystemFlags, targetInst, nativeEvent, nativeEventTarget) {
        var composition = extractCompositionEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget);
        var beforeInput = extractBeforeInputEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget);

        if (composition === null) {
          return beforeInput;
        }

        if (beforeInput === null) {
          return composition;
        }

        return [composition, beforeInput];
      }
    };
    /**
     * @see http://www.whatwg.org/specs/web-apps/current-work/multipage/the-input-element.html#input-type-attr-summary
     */

    var supportedInputTypes = {
      color: true,
      date: true,
      datetime: true,
      'datetime-local': true,
      email: true,
      month: true,
      number: true,
      password: true,
      range: true,
      search: true,
      tel: true,
      text: true,
      time: true,
      url: true,
      week: true
    };

    function isTextInputElement(elem) {
      var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();

      if (nodeName === 'input') {
        return !!supportedInputTypes[elem.type];
      }

      if (nodeName === 'textarea') {
        return true;
      }

      return false;
    }

    var eventTypes$2 = {
      change: {
        phasedRegistrationNames: {
          bubbled: 'onChange',
          captured: 'onChangeCapture'
        },
        dependencies: [TOP_BLUR, TOP_CHANGE, TOP_CLICK, TOP_FOCUS, TOP_INPUT, TOP_KEY_DOWN, TOP_KEY_UP, TOP_SELECTION_CHANGE]
      }
    };

    function createAndAccumulateChangeEvent(inst, nativeEvent, target) {
      var event = SyntheticEvent.getPooled(eventTypes$2.change, inst, nativeEvent, target);
      event.type = 'change'; // Flag this event loop as needing state restore.

      enqueueStateRestore(target);
      accumulateTwoPhaseDispatches(event);
      return event;
    }
    /**
     * For IE shims
     */


    var activeElement = null;
    var activeElementInst = null;
    /**
     * SECTION: handle `change` event
     */

    function shouldUseChangeEvent(elem) {
      var nodeName = elem.nodeName && elem.nodeName.toLowerCase();
      return nodeName === 'select' || nodeName === 'input' && elem.type === 'file';
    }

    function manualDispatchChangeEvent(nativeEvent) {
      var event = createAndAccumulateChangeEvent(activeElementInst, nativeEvent, getEventTarget(nativeEvent)); // If change and propertychange bubbled, we'd just bind to it like all the
      // other events and have it go through ReactBrowserEventEmitter. Since it
      // doesn't, we manually listen for the events and so we have to enqueue and
      // process the abstract event manually.
      //
      // Batching is necessary here in order to ensure that all event handlers run
      // before the next rerender (including event handlers attached to ancestor
      // elements instead of directly on the input). Without this, controlled
      // components don't work properly in conjunction with event bubbling because
      // the component is rerendered and the value reverted before all the event
      // handlers can run. See https://github.com/facebook/react/issues/708.

      batchedUpdates(runEventInBatch, event);
    }

    function runEventInBatch(event) {
      runEventsInBatch(event);
    }

    function getInstIfValueChanged(targetInst) {
      var targetNode = getNodeFromInstance$1(targetInst);

      if (updateValueIfChanged(targetNode)) {
        return targetInst;
      }
    }

    function getTargetInstForChangeEvent(topLevelType, targetInst) {
      if (topLevelType === TOP_CHANGE) {
        return targetInst;
      }
    }
    /**
     * SECTION: handle `input` event
     */


    var isInputEventSupported = false;

    if (canUseDOM) {
      // IE9 claims to support the input event but fails to trigger it when
      // deleting text, so we ignore its input events.
      isInputEventSupported = isEventSupported('input') && (!document.documentMode || document.documentMode > 9);
    }
    /**
     * (For IE <=9) Starts tracking propertychange events on the passed-in element
     * and override the value property so that we can distinguish user events from
     * value changes in JS.
     */


    function startWatchingForValueChange(target, targetInst) {
      activeElement = target;
      activeElementInst = targetInst;
      activeElement.attachEvent('onpropertychange', handlePropertyChange);
    }
    /**
     * (For IE <=9) Removes the event listeners from the currently-tracked element,
     * if any exists.
     */


    function stopWatchingForValueChange() {
      if (!activeElement) {
        return;
      }

      activeElement.detachEvent('onpropertychange', handlePropertyChange);
      activeElement = null;
      activeElementInst = null;
    }
    /**
     * (For IE <=9) Handles a propertychange event, sending a `change` event if
     * the value of the active element has changed.
     */


    function handlePropertyChange(nativeEvent) {
      if (nativeEvent.propertyName !== 'value') {
        return;
      }

      if (getInstIfValueChanged(activeElementInst)) {
        manualDispatchChangeEvent(nativeEvent);
      }
    }

    function handleEventsForInputEventPolyfill(topLevelType, target, targetInst) {
      if (topLevelType === TOP_FOCUS) {
        // In IE9, propertychange fires for most input events but is buggy and
        // doesn't fire when text is deleted, but conveniently, selectionchange
        // appears to fire in all of the remaining cases so we catch those and
        // forward the event if the value has changed
        // In either case, we don't want to call the event handler if the value
        // is changed from JS so we redefine a setter for `.value` that updates
        // our activeElementValue variable, allowing us to ignore those changes
        //
        // stopWatching() should be a noop here but we call it just in case we
        // missed a blur event somehow.
        stopWatchingForValueChange();
        startWatchingForValueChange(target, targetInst);
      } else if (topLevelType === TOP_BLUR) {
        stopWatchingForValueChange();
      }
    } // For IE8 and IE9.


    function getTargetInstForInputEventPolyfill(topLevelType, targetInst) {
      if (topLevelType === TOP_SELECTION_CHANGE || topLevelType === TOP_KEY_UP || topLevelType === TOP_KEY_DOWN) {
        // On the selectionchange event, the target is just document which isn't
        // helpful for us so just check activeElement instead.
        //
        // 99% of the time, keydown and keyup aren't necessary. IE8 fails to fire
        // propertychange on the first input event after setting `value` from a
        // script and fires only keydown, keypress, keyup. Catching keyup usually
        // gets it and catching keydown lets us fire an event for the first
        // keystroke if user does a key repeat (it'll be a little delayed: right
        // before the second keystroke). Other input methods (e.g., paste) seem to
        // fire selectionchange normally.
        return getInstIfValueChanged(activeElementInst);
      }
    }
    /**
     * SECTION: handle `click` event
     */


    function shouldUseClickEvent(elem) {
      // Use the `click` event to detect changes to checkbox and radio inputs.
      // This approach works across all browsers, whereas `change` does not fire
      // until `blur` in IE8.
      var nodeName = elem.nodeName;
      return nodeName && nodeName.toLowerCase() === 'input' && (elem.type === 'checkbox' || elem.type === 'radio');
    }

    function getTargetInstForClickEvent(topLevelType, targetInst) {
      if (topLevelType === TOP_CLICK) {
        return getInstIfValueChanged(targetInst);
      }
    }

    function getTargetInstForInputOrChangeEvent(topLevelType, targetInst) {
      if (topLevelType === TOP_INPUT || topLevelType === TOP_CHANGE) {
        return getInstIfValueChanged(targetInst);
      }
    }

    function handleControlledInputBlur(node) {
      var state = node._wrapperState;

      if (!state || !state.controlled || node.type !== 'number') {
        return;
      }

      if (!disableInputAttributeSyncing) {
        // If controlled, assign the value attribute to the current value on blur
        setDefaultValue(node, 'number', node.value);
      }
    }
    /**
     * This plugin creates an `onChange` event that normalizes change events
     * across form elements. This event fires at a time when it's possible to
     * change the element's value without seeing a flicker.
     *
     * Supported elements are:
     * - input (see `isTextInputElement`)
     * - textarea
     * - select
     */


    var ChangeEventPlugin = {
      eventTypes: eventTypes$2,
      _isInputEventSupported: isInputEventSupported,
      extractEvents: function (topLevelType, eventSystemFlags, targetInst, nativeEvent, nativeEventTarget) {
        var targetNode = targetInst ? getNodeFromInstance$1(targetInst) : window;
        var getTargetInstFunc, handleEventFunc;

        if (shouldUseChangeEvent(targetNode)) {
          getTargetInstFunc = getTargetInstForChangeEvent;
        } else if (isTextInputElement(targetNode)) {
          if (isInputEventSupported) {
            getTargetInstFunc = getTargetInstForInputOrChangeEvent;
          } else {
            getTargetInstFunc = getTargetInstForInputEventPolyfill;
            handleEventFunc = handleEventsForInputEventPolyfill;
          }
        } else if (shouldUseClickEvent(targetNode)) {
          getTargetInstFunc = getTargetInstForClickEvent;
        }

        if (getTargetInstFunc) {
          var inst = getTargetInstFunc(topLevelType, targetInst);

          if (inst) {
            var event = createAndAccumulateChangeEvent(inst, nativeEvent, nativeEventTarget);
            return event;
          }
        }

        if (handleEventFunc) {
          handleEventFunc(topLevelType, targetNode, targetInst);
        } // When blurring, set the value attribute for number inputs


        if (topLevelType === TOP_BLUR) {
          handleControlledInputBlur(targetNode);
        }
      }
    };
    /**
     * Module that is injectable into `EventPluginHub`, that specifies a
     * deterministic ordering of `EventPlugin`s. A convenient way to reason about
     * plugins, without having to package every one of them. This is better than
     * having plugins be ordered in the same order that they are injected because
     * that ordering would be influenced by the packaging order.
     * `ResponderEventPlugin` must occur before `SimpleEventPlugin` so that
     * preventing default on events is convenient in `SimpleEventPlugin` handlers.
     */

    var DOMEventPluginOrder = ['ResponderEventPlugin', 'SimpleEventPlugin', 'EnterLeaveEventPlugin', 'ChangeEventPlugin', 'SelectEventPlugin', 'BeforeInputEventPlugin'];
    var eventTypes$3 = {
      mouseEnter: {
        registrationName: 'onMouseEnter',
        dependencies: [TOP_MOUSE_OUT, TOP_MOUSE_OVER]
      },
      mouseLeave: {
        registrationName: 'onMouseLeave',
        dependencies: [TOP_MOUSE_OUT, TOP_MOUSE_OVER]
      },
      pointerEnter: {
        registrationName: 'onPointerEnter',
        dependencies: [TOP_POINTER_OUT, TOP_POINTER_OVER]
      },
      pointerLeave: {
        registrationName: 'onPointerLeave',
        dependencies: [TOP_POINTER_OUT, TOP_POINTER_OVER]
      }
    };
    var EnterLeaveEventPlugin = {
      eventTypes: eventTypes$3,

      /**
       * For almost every interaction we care about, there will be both a top-level
       * `mouseover` and `mouseout` event that occurs. Only use `mouseout` so that
       * we do not extract duplicate events. However, moving the mouse into the
       * browser from outside will not fire a `mouseout` event. In this case, we use
       * the `mouseover` top-level event.
       */
      extractEvents: function (topLevelType, eventSystemFlags, targetInst, nativeEvent, nativeEventTarget) {
        var isOverEvent = topLevelType === TOP_MOUSE_OVER || topLevelType === TOP_POINTER_OVER;
        var isOutEvent = topLevelType === TOP_MOUSE_OUT || topLevelType === TOP_POINTER_OUT;

        if (isOverEvent && (eventSystemFlags & IS_REPLAYED) === 0 && (nativeEvent.relatedTarget || nativeEvent.fromElement)) {
          // If this is an over event with a target, then we've already dispatched
          // the event in the out event of the other target. If this is replayed,
          // then it's because we couldn't dispatch against this target previously
          // so we have to do it now instead.
          return null;
        }

        if (!isOutEvent && !isOverEvent) {
          // Must not be a mouse or pointer in or out - ignoring.
          return null;
        }

        var win;

        if (nativeEventTarget.window === nativeEventTarget) {
          // `nativeEventTarget` is probably a window object.
          win = nativeEventTarget;
        } else {
          // TODO: Figure out why `ownerDocument` is sometimes undefined in IE8.
          var doc = nativeEventTarget.ownerDocument;

          if (doc) {
            win = doc.defaultView || doc.parentWindow;
          } else {
            win = window;
          }
        }

        var from;
        var to;

        if (isOutEvent) {
          from = targetInst;
          var related = nativeEvent.relatedTarget || nativeEvent.toElement;
          to = related ? getClosestInstanceFromNode(related) : null;

          if (to !== null) {
            var nearestMounted = getNearestMountedFiber(to);

            if (to !== nearestMounted || to.tag !== HostComponent && to.tag !== HostText) {
              to = null;
            }
          }
        } else {
          // Moving to a node from outside the window.
          from = null;
          to = targetInst;
        }

        if (from === to) {
          // Nothing pertains to our managed components.
          return null;
        }

        var eventInterface, leaveEventType, enterEventType, eventTypePrefix;

        if (topLevelType === TOP_MOUSE_OUT || topLevelType === TOP_MOUSE_OVER) {
          eventInterface = SyntheticMouseEvent;
          leaveEventType = eventTypes$3.mouseLeave;
          enterEventType = eventTypes$3.mouseEnter;
          eventTypePrefix = 'mouse';
        } else if (topLevelType === TOP_POINTER_OUT || topLevelType === TOP_POINTER_OVER) {
          eventInterface = SyntheticPointerEvent;
          leaveEventType = eventTypes$3.pointerLeave;
          enterEventType = eventTypes$3.pointerEnter;
          eventTypePrefix = 'pointer';
        }

        var fromNode = from == null ? win : getNodeFromInstance$1(from);
        var toNode = to == null ? win : getNodeFromInstance$1(to);
        var leave = eventInterface.getPooled(leaveEventType, from, nativeEvent, nativeEventTarget);
        leave.type = eventTypePrefix + 'leave';
        leave.target = fromNode;
        leave.relatedTarget = toNode;
        var enter = eventInterface.getPooled(enterEventType, to, nativeEvent, nativeEventTarget);
        enter.type = eventTypePrefix + 'enter';
        enter.target = toNode;
        enter.relatedTarget = fromNode;
        accumulateEnterLeaveDispatches(leave, enter, from, to);
        return [leave, enter];
      }
    };
    /**
     * inlined Object.is polyfill to avoid requiring consumers ship their own
     * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
     */

    function is(x, y) {
      return x === y && (x !== 0 || 1 / x === 1 / y) || x !== x && y !== y // eslint-disable-line no-self-compare
      ;
    }

    var is$1 = typeof Object.is === 'function' ? Object.is : is;
    var hasOwnProperty$2 = Object.prototype.hasOwnProperty;
    /**
     * Performs equality by iterating through keys on an object and returning false
     * when any key has values which are not strictly equal between the arguments.
     * Returns true when the values of all keys are strictly equal.
     */

    function shallowEqual(objA, objB) {
      if (is$1(objA, objB)) {
        return true;
      }

      if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) {
        return false;
      }

      var keysA = Object.keys(objA);
      var keysB = Object.keys(objB);

      if (keysA.length !== keysB.length) {
        return false;
      } // Test for A's keys different from B.


      for (var i = 0; i < keysA.length; i++) {
        if (!hasOwnProperty$2.call(objB, keysA[i]) || !is$1(objA[keysA[i]], objB[keysA[i]])) {
          return false;
        }
      }

      return true;
    }

    var skipSelectionChangeEvent = canUseDOM && 'documentMode' in document && document.documentMode <= 11;
    var eventTypes$4 = {
      select: {
        phasedRegistrationNames: {
          bubbled: 'onSelect',
          captured: 'onSelectCapture'
        },
        dependencies: [TOP_BLUR, TOP_CONTEXT_MENU, TOP_DRAG_END, TOP_FOCUS, TOP_KEY_DOWN, TOP_KEY_UP, TOP_MOUSE_DOWN, TOP_MOUSE_UP, TOP_SELECTION_CHANGE]
      }
    };
    var activeElement$1 = null;
    var activeElementInst$1 = null;
    var lastSelection = null;
    var mouseDown = false;
    /**
     * Get an object which is a unique representation of the current selection.
     *
     * The return value will not be consistent across nodes or browsers, but
     * two identical selections on the same node will return identical objects.
     *
     * @param {DOMElement} node
     * @return {object}
     */

    function getSelection$1(node) {
      if ('selectionStart' in node && hasSelectionCapabilities(node)) {
        return {
          start: node.selectionStart,
          end: node.selectionEnd
        };
      } else {
        var win = node.ownerDocument && node.ownerDocument.defaultView || window;
        var selection = win.getSelection();
        return {
          anchorNode: selection.anchorNode,
          anchorOffset: selection.anchorOffset,
          focusNode: selection.focusNode,
          focusOffset: selection.focusOffset
        };
      }
    }
    /**
     * Get document associated with the event target.
     *
     * @param {object} nativeEventTarget
     * @return {Document}
     */


    function getEventTargetDocument(eventTarget) {
      return eventTarget.window === eventTarget ? eventTarget.document : eventTarget.nodeType === DOCUMENT_NODE ? eventTarget : eventTarget.ownerDocument;
    }
    /**
     * Poll selection to see whether it's changed.
     *
     * @param {object} nativeEvent
     * @param {object} nativeEventTarget
     * @return {?SyntheticEvent}
     */


    function constructSelectEvent(nativeEvent, nativeEventTarget) {
      // Ensure we have the right element, and that the user is not dragging a
      // selection (this matches native `select` event behavior). In HTML5, select
      // fires only on input and textarea thus if there's no focused element we
      // won't dispatch.
      var doc = getEventTargetDocument(nativeEventTarget);

      if (mouseDown || activeElement$1 == null || activeElement$1 !== getActiveElement(doc)) {
        return null;
      } // Only fire when selection has actually changed.


      var currentSelection = getSelection$1(activeElement$1);

      if (!lastSelection || !shallowEqual(lastSelection, currentSelection)) {
        lastSelection = currentSelection;
        var syntheticEvent = SyntheticEvent.getPooled(eventTypes$4.select, activeElementInst$1, nativeEvent, nativeEventTarget);
        syntheticEvent.type = 'select';
        syntheticEvent.target = activeElement$1;
        accumulateTwoPhaseDispatches(syntheticEvent);
        return syntheticEvent;
      }

      return null;
    }
    /**
     * This plugin creates an `onSelect` event that normalizes select events
     * across form elements.
     *
     * Supported elements are:
     * - input (see `isTextInputElement`)
     * - textarea
     * - contentEditable
     *
     * This differs from native browser implementations in the following ways:
     * - Fires on contentEditable fields as well as inputs.
     * - Fires for collapsed selection.
     * - Fires after user input.
     */


    var SelectEventPlugin = {
      eventTypes: eventTypes$4,
      extractEvents: function (topLevelType, eventSystemFlags, targetInst, nativeEvent, nativeEventTarget) {
        var doc = getEventTargetDocument(nativeEventTarget); // Track whether all listeners exists for this plugin. If none exist, we do
        // not extract events. See #3639.

        if (!doc || !isListeningToAllDependencies('onSelect', doc)) {
          return null;
        }

        var targetNode = targetInst ? getNodeFromInstance$1(targetInst) : window;

        switch (topLevelType) {
          // Track the input node that has focus.
          case TOP_FOCUS:
            if (isTextInputElement(targetNode) || targetNode.contentEditable === 'true') {
              activeElement$1 = targetNode;
              activeElementInst$1 = targetInst;
              lastSelection = null;
            }

            break;

          case TOP_BLUR:
            activeElement$1 = null;
            activeElementInst$1 = null;
            lastSelection = null;
            break;
          // Don't fire the event while the user is dragging. This matches the
          // semantics of the native select event.

          case TOP_MOUSE_DOWN:
            mouseDown = true;
            break;

          case TOP_CONTEXT_MENU:
          case TOP_MOUSE_UP:
          case TOP_DRAG_END:
            mouseDown = false;
            return constructSelectEvent(nativeEvent, nativeEventTarget);
          // Chrome and IE fire non-standard event when selection is changed (and
          // sometimes when it hasn't). IE's event fires out of order with respect
          // to key and input events on deletion, so we discard it.
          //
          // Firefox doesn't support selectionchange, so check selection status
          // after each key entry. The selection changes after keydown and before
          // keyup, but we check on keydown as well in the case of holding down a
          // key, when multiple keydown events are fired but only one keyup is.
          // This is also our approach for IE handling, for the reason above.

          case TOP_SELECTION_CHANGE:
            if (skipSelectionChangeEvent) {
              break;
            }

          // falls through

          case TOP_KEY_DOWN:
          case TOP_KEY_UP:
            return constructSelectEvent(nativeEvent, nativeEventTarget);
        }

        return null;
      }
    };
    /**
     * Inject modules for resolving DOM hierarchy and plugin ordering.
     */

    injection.injectEventPluginOrder(DOMEventPluginOrder);
    setComponentTree(getFiberCurrentPropsFromNode$1, getInstanceFromNode$1, getNodeFromInstance$1);
    /**
     * Some important event plugins included by default (without having to require
     * them).
     */

    injection.injectEventPluginsByName({
      SimpleEventPlugin: SimpleEventPlugin,
      EnterLeaveEventPlugin: EnterLeaveEventPlugin,
      ChangeEventPlugin: ChangeEventPlugin,
      SelectEventPlugin: SelectEventPlugin,
      BeforeInputEventPlugin: BeforeInputEventPlugin
    }); // Prefix measurements so that it's possible to filter them.
    // Longer prefixes are hard to read in DevTools.

    var reactEmoji = "\u269B";
    var warningEmoji = "\u26D4";
    var supportsUserTiming = typeof performance !== 'undefined' && typeof performance.mark === 'function' && typeof performance.clearMarks === 'function' && typeof performance.measure === 'function' && typeof performance.clearMeasures === 'function'; // Keep track of current fiber so that we know the path to unwind on pause.
    // TODO: this looks the same as nextUnitOfWork in scheduler. Can we unify them?

    var currentFiber = null; // If we're in the middle of user code, which fiber and method is it?
    // Reusing `currentFiber` would be confusing for this because user code fiber
    // can change during commit phase too, but we don't need to unwind it (since
    // lifecycles in the commit phase don't resemble a tree).

    var currentPhase = null;
    var currentPhaseFiber = null; // Did lifecycle hook schedule an update? This is often a performance problem,
    // so we will keep track of it, and include it in the report.
    // Track commits caused by cascading updates.

    var isCommitting = false;
    var hasScheduledUpdateInCurrentCommit = false;
    var hasScheduledUpdateInCurrentPhase = false;
    var commitCountInCurrentWorkLoop = 0;
    var effectCountInCurrentCommit = 0; // to avoid stretch the commit phase with measurement overhead.

    var labelsInCurrentCommit = new Set();

    var formatMarkName = function (markName) {
      return reactEmoji + " " + markName;
    };

    var formatLabel = function (label, warning) {
      var prefix = warning ? warningEmoji + " " : reactEmoji + " ";
      var suffix = warning ? " Warning: " + warning : '';
      return "" + prefix + label + suffix;
    };

    var beginMark = function (markName) {
      performance.mark(formatMarkName(markName));
    };

    var clearMark = function (markName) {
      performance.clearMarks(formatMarkName(markName));
    };

    var endMark = function (label, markName, warning) {
      var formattedMarkName = formatMarkName(markName);
      var formattedLabel = formatLabel(label, warning);

      try {
        performance.measure(formattedLabel, formattedMarkName);
      } catch (err) {} // If previous mark was missing for some reason, this will throw.
      // This could only happen if React crashed in an unexpected place earlier.
      // Don't pile on with more errors.
      // Clear marks immediately to avoid growing buffer.


      performance.clearMarks(formattedMarkName);
      performance.clearMeasures(formattedLabel);
    };

    var getFiberMarkName = function (label, debugID) {
      return label + " (#" + debugID + ")";
    };

    var getFiberLabel = function (componentName, isMounted, phase) {
      if (phase === null) {
        // These are composite component total time measurements.
        return componentName + " [" + (isMounted ? 'update' : 'mount') + "]";
      } else {
        // Composite component methods.
        return componentName + "." + phase;
      }
    };

    var beginFiberMark = function (fiber, phase) {
      var componentName = getComponentName(fiber.type) || 'Unknown';
      var debugID = fiber._debugID;
      var isMounted = fiber.alternate !== null;
      var label = getFiberLabel(componentName, isMounted, phase);

      if (isCommitting && labelsInCurrentCommit.has(label)) {
        // During the commit phase, we don't show duplicate labels because
        // there is a fixed overhead for every measurement, and we don't
        // want to stretch the commit phase beyond necessary.
        return false;
      }

      labelsInCurrentCommit.add(label);
      var markName = getFiberMarkName(label, debugID);
      beginMark(markName);
      return true;
    };

    var clearFiberMark = function (fiber, phase) {
      var componentName = getComponentName(fiber.type) || 'Unknown';
      var debugID = fiber._debugID;
      var isMounted = fiber.alternate !== null;
      var label = getFiberLabel(componentName, isMounted, phase);
      var markName = getFiberMarkName(label, debugID);
      clearMark(markName);
    };

    var endFiberMark = function (fiber, phase, warning) {
      var componentName = getComponentName(fiber.type) || 'Unknown';
      var debugID = fiber._debugID;
      var isMounted = fiber.alternate !== null;
      var label = getFiberLabel(componentName, isMounted, phase);
      var markName = getFiberMarkName(label, debugID);
      endMark(label, markName, warning);
    };

    var shouldIgnoreFiber = function (fiber) {
      // Host components should be skipped in the timeline.
      // We could check typeof fiber.type, but does this work with RN?
      switch (fiber.tag) {
        case HostRoot:
        case HostComponent:
        case HostText:
        case HostPortal:
        case Fragment:
        case ContextProvider:
        case ContextConsumer:
        case Mode:
          return true;

        default:
          return false;
      }
    };

    var clearPendingPhaseMeasurement = function () {
      if (currentPhase !== null && currentPhaseFiber !== null) {
        clearFiberMark(currentPhaseFiber, currentPhase);
      }

      currentPhaseFiber = null;
      currentPhase = null;
      hasScheduledUpdateInCurrentPhase = false;
    };

    var pauseTimers = function () {
      // Stops all currently active measurements so that they can be resumed
      // if we continue in a later deferred loop from the same unit of work.
      var fiber = currentFiber;

      while (fiber) {
        if (fiber._debugIsCurrentlyTiming) {
          endFiberMark(fiber, null, null);
        }

        fiber = fiber.return;
      }
    };

    var resumeTimersRecursively = function (fiber) {
      if (fiber.return !== null) {
        resumeTimersRecursively(fiber.return);
      }

      if (fiber._debugIsCurrentlyTiming) {
        beginFiberMark(fiber, null);
      }
    };

    var resumeTimers = function () {
      // Resumes all measurements that were active during the last deferred loop.
      if (currentFiber !== null) {
        resumeTimersRecursively(currentFiber);
      }
    };

    function recordEffect() {
      if (enableUserTimingAPI) {
        effectCountInCurrentCommit++;
      }
    }

    function recordScheduleUpdate() {
      if (enableUserTimingAPI) {
        if (isCommitting) {
          hasScheduledUpdateInCurrentCommit = true;
        }

        if (currentPhase !== null && currentPhase !== 'componentWillMount' && currentPhase !== 'componentWillReceiveProps') {
          hasScheduledUpdateInCurrentPhase = true;
        }
      }
    }

    function startWorkTimer(fiber) {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
          return;
        } // If we pause, this is the fiber to unwind from.


        currentFiber = fiber;

        if (!beginFiberMark(fiber, null)) {
          return;
        }

        fiber._debugIsCurrentlyTiming = true;
      }
    }

    function cancelWorkTimer(fiber) {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
          return;
        } // Remember we shouldn't complete measurement for this fiber.
        // Otherwise flamechart will be deep even for small updates.


        fiber._debugIsCurrentlyTiming = false;
        clearFiberMark(fiber, null);
      }
    }

    function stopWorkTimer(fiber) {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
          return;
        } // If we pause, its parent is the fiber to unwind from.


        currentFiber = fiber.return;

        if (!fiber._debugIsCurrentlyTiming) {
          return;
        }

        fiber._debugIsCurrentlyTiming = false;
        endFiberMark(fiber, null, null);
      }
    }

    function stopFailedWorkTimer(fiber) {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
          return;
        } // If we pause, its parent is the fiber to unwind from.


        currentFiber = fiber.return;

        if (!fiber._debugIsCurrentlyTiming) {
          return;
        }

        fiber._debugIsCurrentlyTiming = false;
        var warning = fiber.tag === SuspenseComponent ? 'Rendering was suspended' : 'An error was thrown inside this error boundary';
        endFiberMark(fiber, null, warning);
      }
    }

    function startPhaseTimer(fiber, phase) {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming) {
          return;
        }

        clearPendingPhaseMeasurement();

        if (!beginFiberMark(fiber, phase)) {
          return;
        }

        currentPhaseFiber = fiber;
        currentPhase = phase;
      }
    }

    function stopPhaseTimer() {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming) {
          return;
        }

        if (currentPhase !== null && currentPhaseFiber !== null) {
          var warning = hasScheduledUpdateInCurrentPhase ? 'Scheduled a cascading update' : null;
          endFiberMark(currentPhaseFiber, currentPhase, warning);
        }

        currentPhase = null;
        currentPhaseFiber = null;
      }
    }

    function startWorkLoopTimer(nextUnitOfWork) {
      if (enableUserTimingAPI) {
        currentFiber = nextUnitOfWork;

        if (!supportsUserTiming) {
          return;
        }

        commitCountInCurrentWorkLoop = 0; // This is top level call.
        // Any other measurements are performed within.

        beginMark('(React Tree Reconciliation)'); // Resume any measurements that were in progress during the last loop.

        resumeTimers();
      }
    }

    function stopWorkLoopTimer(interruptedBy, didCompleteRoot) {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming) {
          return;
        }

        var warning = null;

        if (interruptedBy !== null) {
          if (interruptedBy.tag === HostRoot) {
            warning = 'A top-level update interrupted the previous render';
          } else {
            var componentName = getComponentName(interruptedBy.type) || 'Unknown';
            warning = "An update to " + componentName + " interrupted the previous render";
          }
        } else if (commitCountInCurrentWorkLoop > 1) {
          warning = 'There were cascading updates';
        }

        commitCountInCurrentWorkLoop = 0;
        var label = didCompleteRoot ? '(React Tree Reconciliation: Completed Root)' : '(React Tree Reconciliation: Yielded)'; // Pause any measurements until the next loop.

        pauseTimers();
        endMark(label, '(React Tree Reconciliation)', warning);
      }
    }

    function startCommitTimer() {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming) {
          return;
        }

        isCommitting = true;
        hasScheduledUpdateInCurrentCommit = false;
        labelsInCurrentCommit.clear();
        beginMark('(Committing Changes)');
      }
    }

    function stopCommitTimer() {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming) {
          return;
        }

        var warning = null;

        if (hasScheduledUpdateInCurrentCommit) {
          warning = 'Lifecycle hook scheduled a cascading update';
        } else if (commitCountInCurrentWorkLoop > 0) {
          warning = 'Caused by a cascading update in earlier commit';
        }

        hasScheduledUpdateInCurrentCommit = false;
        commitCountInCurrentWorkLoop++;
        isCommitting = false;
        labelsInCurrentCommit.clear();
        endMark('(Committing Changes)', '(Committing Changes)', warning);
      }
    }

    function startCommitSnapshotEffectsTimer() {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming) {
          return;
        }

        effectCountInCurrentCommit = 0;
        beginMark('(Committing Snapshot Effects)');
      }
    }

    function stopCommitSnapshotEffectsTimer() {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming) {
          return;
        }

        var count = effectCountInCurrentCommit;
        effectCountInCurrentCommit = 0;
        endMark("(Committing Snapshot Effects: " + count + " Total)", '(Committing Snapshot Effects)', null);
      }
    }

    function startCommitHostEffectsTimer() {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming) {
          return;
        }

        effectCountInCurrentCommit = 0;
        beginMark('(Committing Host Effects)');
      }
    }

    function stopCommitHostEffectsTimer() {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming) {
          return;
        }

        var count = effectCountInCurrentCommit;
        effectCountInCurrentCommit = 0;
        endMark("(Committing Host Effects: " + count + " Total)", '(Committing Host Effects)', null);
      }
    }

    function startCommitLifeCyclesTimer() {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming) {
          return;
        }

        effectCountInCurrentCommit = 0;
        beginMark('(Calling Lifecycle Methods)');
      }
    }

    function stopCommitLifeCyclesTimer() {
      if (enableUserTimingAPI) {
        if (!supportsUserTiming) {
          return;
        }

        var count = effectCountInCurrentCommit;
        effectCountInCurrentCommit = 0;
        endMark("(Calling Lifecycle Methods: " + count + " Total)", '(Calling Lifecycle Methods)', null);
      }
    }

    var valueStack = [];
    var fiberStack;
    {
      fiberStack = [];
    }
    var index = -1;

    function createCursor(defaultValue) {
      return {
        current: defaultValue
      };
    }

    function pop(cursor, fiber) {
      if (index < 0) {
        {
          warningWithoutStack$1(false, 'Unexpected pop.');
        }
        return;
      }

      {
        if (fiber !== fiberStack[index]) {
          warningWithoutStack$1(false, 'Unexpected Fiber popped.');
        }
      }
      cursor.current = valueStack[index];
      valueStack[index] = null;
      {
        fiberStack[index] = null;
      }
      index--;
    }

    function push(cursor, value, fiber) {
      index++;
      valueStack[index] = cursor.current;
      {
        fiberStack[index] = fiber;
      }
      cursor.current = value;
    }

    var warnedAboutMissingGetChildContext;
    {
      warnedAboutMissingGetChildContext = {};
    }
    var emptyContextObject = {};
    {
      Object.freeze(emptyContextObject);
    } // A cursor to the current merged context object on the stack.

    var contextStackCursor = createCursor(emptyContextObject); // A cursor to a boolean indicating whether the context has changed.

    var didPerformWorkStackCursor = createCursor(false); // Keep track of the previous context object that was on the stack.
    // We use this to get access to the parent context after we have already
    // pushed the next context provider, and now need to merge their contexts.

    var previousContext = emptyContextObject;

    function getUnmaskedContext(workInProgress, Component, didPushOwnContextIfProvider) {
      if (disableLegacyContext) {
        return emptyContextObject;
      } else {
        if (didPushOwnContextIfProvider && isContextProvider(Component)) {
          // If the fiber is a context provider itself, when we read its context
          // we may have already pushed its own child context on the stack. A context
          // provider should not "see" its own child context. Therefore we read the
          // previous (parent) context instead for a context provider.
          return previousContext;
        }

        return contextStackCursor.current;
      }
    }

    function cacheContext(workInProgress, unmaskedContext, maskedContext) {
      if (disableLegacyContext) {
        return;
      } else {
        var instance = workInProgress.stateNode;
        instance.__reactInternalMemoizedUnmaskedChildContext = unmaskedContext;
        instance.__reactInternalMemoizedMaskedChildContext = maskedContext;
      }
    }

    function getMaskedContext(workInProgress, unmaskedContext) {
      if (disableLegacyContext) {
        return emptyContextObject;
      } else {
        var type = workInProgress.type;
        var contextTypes = type.contextTypes;

        if (!contextTypes) {
          return emptyContextObject;
        } // Avoid recreating masked context unless unmasked context has changed.
        // Failing to do this will result in unnecessary calls to componentWillReceiveProps.
        // This may trigger infinite loops if componentWillReceiveProps calls setState.


        var instance = workInProgress.stateNode;

        if (instance && instance.__reactInternalMemoizedUnmaskedChildContext === unmaskedContext) {
          return instance.__reactInternalMemoizedMaskedChildContext;
        }

        var context = {};

        for (var key in contextTypes) {
          context[key] = unmaskedContext[key];
        }

        {
          var name = getComponentName(type) || 'Unknown';
          checkPropTypes(contextTypes, context, 'context', name, getCurrentFiberStackInDev);
        } // Cache unmasked context so we can avoid recreating masked context unless necessary.
        // Context is created before the class component is instantiated so check for instance.

        if (instance) {
          cacheContext(workInProgress, unmaskedContext, context);
        }

        return context;
      }
    }

    function hasContextChanged() {
      if (disableLegacyContext) {
        return false;
      } else {
        return didPerformWorkStackCursor.current;
      }
    }

    function isContextProvider(type) {
      if (disableLegacyContext) {
        return false;
      } else {
        var childContextTypes = type.childContextTypes;
        return childContextTypes !== null && childContextTypes !== undefined;
      }
    }

    function popContext(fiber) {
      if (disableLegacyContext) {
        return;
      } else {
        pop(didPerformWorkStackCursor, fiber);
        pop(contextStackCursor, fiber);
      }
    }

    function popTopLevelContextObject(fiber) {
      if (disableLegacyContext) {
        return;
      } else {
        pop(didPerformWorkStackCursor, fiber);
        pop(contextStackCursor, fiber);
      }
    }

    function pushTopLevelContextObject(fiber, context, didChange) {
      if (disableLegacyContext) {
        return;
      } else {
        (function () {
          if (!(contextStackCursor.current === emptyContextObject)) {
            {
              throw ReactError(Error("Unexpected context found on stack. This error is likely caused by a bug in React. Please file an issue."));
            }
          }
        })();

        push(contextStackCursor, context, fiber);
        push(didPerformWorkStackCursor, didChange, fiber);
      }
    }

    function processChildContext(fiber, type, parentContext) {
      if (disableLegacyContext) {
        return parentContext;
      } else {
        var instance = fiber.stateNode;
        var childContextTypes = type.childContextTypes; // TODO (bvaughn) Replace this behavior with an invariant() in the future.
        // It has only been added in Fiber to match the (unintentional) behavior in Stack.

        if (typeof instance.getChildContext !== 'function') {
          {
            var componentName = getComponentName(type) || 'Unknown';

            if (!warnedAboutMissingGetChildContext[componentName]) {
              warnedAboutMissingGetChildContext[componentName] = true;
              warningWithoutStack$1(false, '%s.childContextTypes is specified but there is no getChildContext() method ' + 'on the instance. You can either define getChildContext() on %s or remove ' + 'childContextTypes from it.', componentName, componentName);
            }
          }
          return parentContext;
        }

        var childContext;
        {
          setCurrentPhase('getChildContext');
        }
        startPhaseTimer(fiber, 'getChildContext');
        childContext = instance.getChildContext();
        stopPhaseTimer();
        {
          setCurrentPhase(null);
        }

        for (var contextKey in childContext) {
          (function () {
            if (!(contextKey in childContextTypes)) {
              {
                throw ReactError(Error((getComponentName(type) || 'Unknown') + ".getChildContext(): key \"" + contextKey + "\" is not defined in childContextTypes."));
              }
            }
          })();
        }

        {
          var name = getComponentName(type) || 'Unknown';
          checkPropTypes(childContextTypes, childContext, 'child context', name, // In practice, there is one case in which we won't get a stack. It's when
          // somebody calls unstable_renderSubtreeIntoContainer() and we process
          // context from the parent component instance. The stack will be missing
          // because it's outside of the reconciliation, and so the pointer has not
          // been set. This is rare and doesn't matter. We'll also remove that API.
          getCurrentFiberStackInDev);
        }
        return _assign({}, parentContext, {}, childContext);
      }
    }

    function pushContextProvider(workInProgress) {
      if (disableLegacyContext) {
        return false;
      } else {
        var instance = workInProgress.stateNode; // We push the context as early as possible to ensure stack integrity.
        // If the instance does not exist yet, we will push null at first,
        // and replace it on the stack later when invalidating the context.

        var memoizedMergedChildContext = instance && instance.__reactInternalMemoizedMergedChildContext || emptyContextObject; // Remember the parent context so we can merge with it later.
        // Inherit the parent's did-perform-work value to avoid inadvertently blocking updates.

        previousContext = contextStackCursor.current;
        push(contextStackCursor, memoizedMergedChildContext, workInProgress);
        push(didPerformWorkStackCursor, didPerformWorkStackCursor.current, workInProgress);
        return true;
      }
    }

    function invalidateContextProvider(workInProgress, type, didChange) {
      if (disableLegacyContext) {
        return;
      } else {
        var instance = workInProgress.stateNode;

        (function () {
          if (!instance) {
            {
              throw ReactError(Error("Expected to have an instance by this point. This error is likely caused by a bug in React. Please file an issue."));
            }
          }
        })();

        if (didChange) {
          // Merge parent and own context.
          // Skip this if we're not updating due to sCU.
          // This avoids unnecessarily recomputing memoized values.
          var mergedContext = processChildContext(workInProgress, type, previousContext);
          instance.__reactInternalMemoizedMergedChildContext = mergedContext; // Replace the old (or empty) context with the new one.
          // It is important to unwind the context in the reverse order.

          pop(didPerformWorkStackCursor, workInProgress);
          pop(contextStackCursor, workInProgress); // Now push the new context and mark that it has changed.

          push(contextStackCursor, mergedContext, workInProgress);
          push(didPerformWorkStackCursor, didChange, workInProgress);
        } else {
          pop(didPerformWorkStackCursor, workInProgress);
          push(didPerformWorkStackCursor, didChange, workInProgress);
        }
      }
    }

    function findCurrentUnmaskedContext(fiber) {
      if (disableLegacyContext) {
        return emptyContextObject;
      } else {
        // Currently this is only used with renderSubtreeIntoContainer; not sure if it
        // makes sense elsewhere
        (function () {
          if (!(isFiberMounted(fiber) && fiber.tag === ClassComponent)) {
            {
              throw ReactError(Error("Expected subtree parent to be a mounted class component. This error is likely caused by a bug in React. Please file an issue."));
            }
          }
        })();

        var node = fiber;

        do {
          switch (node.tag) {
            case HostRoot:
              return node.stateNode.context;

            case ClassComponent:
              {
                var Component = node.type;

                if (isContextProvider(Component)) {
                  return node.stateNode.__reactInternalMemoizedMergedChildContext;
                }

                break;
              }
          }

          node = node.return;
        } while (node !== null);

        (function () {
          {
            {
              throw ReactError(Error("Found unexpected detached subtree parent. This error is likely caused by a bug in React. Please file an issue."));
            }
          }
        })();
      }
    }

    var LegacyRoot = 0;
    var BatchedRoot = 1;
    var ConcurrentRoot = 2; // Intentionally not named imports because Rollup would use dynamic dispatch for
    // CommonJS interop named imports.

    var Scheduler_runWithPriority = Scheduler.unstable_runWithPriority;
    var Scheduler_scheduleCallback = Scheduler.unstable_scheduleCallback;
    var Scheduler_cancelCallback = Scheduler.unstable_cancelCallback;
    var Scheduler_shouldYield = Scheduler.unstable_shouldYield;
    var Scheduler_requestPaint = Scheduler.unstable_requestPaint;
    var Scheduler_now = Scheduler.unstable_now;
    var Scheduler_getCurrentPriorityLevel = Scheduler.unstable_getCurrentPriorityLevel;
    var Scheduler_ImmediatePriority = Scheduler.unstable_ImmediatePriority;
    var Scheduler_UserBlockingPriority = Scheduler.unstable_UserBlockingPriority;
    var Scheduler_NormalPriority = Scheduler.unstable_NormalPriority;
    var Scheduler_LowPriority = Scheduler.unstable_LowPriority;
    var Scheduler_IdlePriority = Scheduler.unstable_IdlePriority;

    if (enableSchedulerTracing) {
      // Provide explicit error message when production+profiling bundle of e.g.
      // react-dom is used with production (non-profiling) bundle of
      // scheduler/tracing
      (function () {
        if (!(tracing.__interactionsRef != null && tracing.__interactionsRef.current != null)) {
          {
            throw ReactError(Error("It is not supported to run the profiling version of a renderer (for example, `react-dom/profiling`) without also replacing the `scheduler/tracing` module with `scheduler/tracing-profiling`. Your bundler might have a setting for aliasing both modules. Learn more at http://fb.me/react-profiling"));
          }
        }
      })();
    }

    var fakeCallbackNode = {}; // Except for NoPriority, these correspond to Scheduler priorities. We use
    // ascending numbers so we can compare them like numbers. They start at 90 to
    // avoid clashing with Scheduler's priorities.

    var ImmediatePriority = 99;
    var UserBlockingPriority$2 = 98;
    var NormalPriority = 97;
    var LowPriority = 96;
    var IdlePriority = 95; // NoPriority is the absence of priority. Also React-only.

    var NoPriority = 90;
    var shouldYield = Scheduler_shouldYield;
    var requestPaint = // Fall back gracefully if we're running an older version of Scheduler.
    Scheduler_requestPaint !== undefined ? Scheduler_requestPaint : function () {};
    var syncQueue = null;
    var immediateQueueCallbackNode = null;
    var isFlushingSyncQueue = false;
    var initialTimeMs = Scheduler_now(); // If the initial timestamp is reasonably small, use Scheduler's `now` directly.
    // This will be the case for modern browsers that support `performance.now`. In
    // older browsers, Scheduler falls back to `Date.now`, which returns a Unix
    // timestamp. In that case, subtract the module initialization time to simulate
    // the behavior of performance.now and keep our times small enough to fit
    // within 32 bits.
    // TODO: Consider lifting this into Scheduler.

    var now = initialTimeMs < 10000 ? Scheduler_now : function () {
      return Scheduler_now() - initialTimeMs;
    };

    function getCurrentPriorityLevel() {
      switch (Scheduler_getCurrentPriorityLevel()) {
        case Scheduler_ImmediatePriority:
          return ImmediatePriority;

        case Scheduler_UserBlockingPriority:
          return UserBlockingPriority$2;

        case Scheduler_NormalPriority:
          return NormalPriority;

        case Scheduler_LowPriority:
          return LowPriority;

        case Scheduler_IdlePriority:
          return IdlePriority;

        default:
          (function () {
            {
              {
                throw ReactError(Error("Unknown priority level."));
              }
            }
          })();

      }
    }

    function reactPriorityToSchedulerPriority(reactPriorityLevel) {
      switch (reactPriorityLevel) {
        case ImmediatePriority:
          return Scheduler_ImmediatePriority;

        case UserBlockingPriority$2:
          return Scheduler_UserBlockingPriority;

        case NormalPriority:
          return Scheduler_NormalPriority;

        case LowPriority:
          return Scheduler_LowPriority;

        case IdlePriority:
          return Scheduler_IdlePriority;

        default:
          (function () {
            {
              {
                throw ReactError(Error("Unknown priority level."));
              }
            }
          })();

      }
    }

    function runWithPriority$2(reactPriorityLevel, fn) {
      var priorityLevel = reactPriorityToSchedulerPriority(reactPriorityLevel);
      return Scheduler_runWithPriority(priorityLevel, fn);
    }

    function scheduleCallback(reactPriorityLevel, callback, options) {
      var priorityLevel = reactPriorityToSchedulerPriority(reactPriorityLevel);
      return Scheduler_scheduleCallback(priorityLevel, callback, options);
    }

    function scheduleSyncCallback(callback) {
      // Push this callback into an internal queue. We'll flush these either in
      // the next tick, or earlier if something calls `flushSyncCallbackQueue`.
      if (syncQueue === null) {
        syncQueue = [callback]; // Flush the queue in the next tick, at the earliest.

        immediateQueueCallbackNode = Scheduler_scheduleCallback(Scheduler_ImmediatePriority, flushSyncCallbackQueueImpl);
      } else {
        // Push onto existing queue. Don't need to schedule a callback because
        // we already scheduled one when we created the queue.
        syncQueue.push(callback);
      }

      return fakeCallbackNode;
    }

    function cancelCallback(callbackNode) {
      if (callbackNode !== fakeCallbackNode) {
        Scheduler_cancelCallback(callbackNode);
      }
    }

    function flushSyncCallbackQueue() {
      if (immediateQueueCallbackNode !== null) {
        var node = immediateQueueCallbackNode;
        immediateQueueCallbackNode = null;
        Scheduler_cancelCallback(node);
      }

      flushSyncCallbackQueueImpl();
    }

    function flushSyncCallbackQueueImpl() {
      if (!isFlushingSyncQueue && syncQueue !== null) {
        // Prevent re-entrancy.
        isFlushingSyncQueue = true;
        var i = 0;

        try {
          var _isSync = true;
          var queue = syncQueue;
          runWithPriority$2(ImmediatePriority, function () {
            for (; i < queue.length; i++) {
              var callback = queue[i];

              do {
                callback = callback(_isSync);
              } while (callback !== null);
            }
          });
          syncQueue = null;
        } catch (error) {
          // If something throws, leave the remaining callbacks on the queue.
          if (syncQueue !== null) {
            syncQueue = syncQueue.slice(i + 1);
          } // Resume flushing in the next tick


          Scheduler_scheduleCallback(Scheduler_ImmediatePriority, flushSyncCallbackQueue);
          throw error;
        } finally {
          isFlushingSyncQueue = false;
        }
      }
    }

    var NoMode = 0;
    var StrictMode = 1; // TODO: Remove BatchedMode and ConcurrentMode by reading from the root
    // tag instead

    var BatchedMode = 2;
    var ConcurrentMode = 4;
    var ProfileMode = 8; // Max 31 bit integer. The max integer size in V8 for 32-bit systems.
    // Math.pow(2, 30) - 1
    // 0b111111111111111111111111111111

    var MAX_SIGNED_31_BIT_INT = 1073741823;
    var NoWork = 0; // TODO: Think of a better name for Never. The key difference with Idle is that
    // Never work can be committed in an inconsistent state without tearing the UI.
    // The main example is offscreen content, like a hidden subtree. So one possible
    // name is Offscreen. However, it also includes dehydrated Suspense boundaries,
    // which are inconsistent in the sense that they haven't finished yet, but
    // aren't visibly inconsistent because the server rendered HTML matches what the
    // hydrated tree would look like.

    var Never = 1; // Idle is slightly higher priority than Never. It must completely finish in
    // order to be consistent.

    var Idle = 2;
    var Sync = MAX_SIGNED_31_BIT_INT;
    var Batched = Sync - 1;
    var UNIT_SIZE = 10;
    var MAGIC_NUMBER_OFFSET = Batched - 1; // 1 unit of expiration time represents 10ms.

    function msToExpirationTime(ms) {
      // Always add an offset so that we don't clash with the magic number for NoWork.
      return MAGIC_NUMBER_OFFSET - (ms / UNIT_SIZE | 0);
    }

    function expirationTimeToMs(expirationTime) {
      return (MAGIC_NUMBER_OFFSET - expirationTime) * UNIT_SIZE;
    }

    function ceiling(num, precision) {
      return ((num / precision | 0) + 1) * precision;
    }

    function computeExpirationBucket(currentTime, expirationInMs, bucketSizeMs) {
      return MAGIC_NUMBER_OFFSET - ceiling(MAGIC_NUMBER_OFFSET - currentTime + expirationInMs / UNIT_SIZE, bucketSizeMs / UNIT_SIZE);
    } // TODO: This corresponds to Scheduler's NormalPriority, not LowPriority. Update
    // the names to reflect.


    var LOW_PRIORITY_EXPIRATION = 5000;
    var LOW_PRIORITY_BATCH_SIZE = 250;

    function computeAsyncExpiration(currentTime) {
      return computeExpirationBucket(currentTime, LOW_PRIORITY_EXPIRATION, LOW_PRIORITY_BATCH_SIZE);
    }

    function computeSuspenseExpiration(currentTime, timeoutMs) {
      // TODO: Should we warn if timeoutMs is lower than the normal pri expiration time?
      return computeExpirationBucket(currentTime, timeoutMs, LOW_PRIORITY_BATCH_SIZE);
    } // We intentionally set a higher expiration time for interactive updates in
    // dev than in production.
    //
    // If the main thread is being blocked so long that you hit the expiration,
    // it's a problem that could be solved with better scheduling.
    //
    // People will be more likely to notice this and fix it with the long
    // expiration time in development.
    //
    // In production we opt for better UX at the risk of masking scheduling
    // problems, by expiring fast.


    var HIGH_PRIORITY_EXPIRATION = 500;
    var HIGH_PRIORITY_BATCH_SIZE = 100;

    function computeInteractiveExpiration(currentTime) {
      return computeExpirationBucket(currentTime, HIGH_PRIORITY_EXPIRATION, HIGH_PRIORITY_BATCH_SIZE);
    }

    function inferPriorityFromExpirationTime(currentTime, expirationTime) {
      if (expirationTime === Sync) {
        return ImmediatePriority;
      }

      if (expirationTime === Never || expirationTime === Idle) {
        return IdlePriority;
      }

      var msUntil = expirationTimeToMs(expirationTime) - expirationTimeToMs(currentTime);

      if (msUntil <= 0) {
        return ImmediatePriority;
      }

      if (msUntil <= HIGH_PRIORITY_EXPIRATION + HIGH_PRIORITY_BATCH_SIZE) {
        return UserBlockingPriority$2;
      }

      if (msUntil <= LOW_PRIORITY_EXPIRATION + LOW_PRIORITY_BATCH_SIZE) {
        return NormalPriority;
      } // TODO: Handle LowPriority
      // Assume anything lower has idle priority


      return IdlePriority;
    }
    /**
     * Forked from fbjs/warning:
     * https://github.com/facebook/fbjs/blob/e66ba20ad5be433eb54423f2b097d829324d9de6/packages/fbjs/src/__forks__/warning.js
     *
     * Only change is we use console.warn instead of console.error,
     * and do nothing when 'console' is not supported.
     * This really simplifies the code.
     * ---
     * Similar to invariant but only logs a warning if the condition is not met.
     * This can be used to log issues in development environments in critical
     * paths. Removing the logging code for production environments will keep the
     * same logic and follow the same code paths.
     */


    var lowPriorityWarningWithoutStack = function () {};

    {
      var printWarning = function (format) {
        for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
          args[_key - 1] = arguments[_key];
        }

        var argIndex = 0;
        var message = 'Warning: ' + format.replace(/%s/g, function () {
          return args[argIndex++];
        });

        if (typeof console !== 'undefined') {
          console.warn(message);
        }

        try {
          // --- Welcome to debugging React ---
          // This error was thrown as a convenience so that you can use this stack
          // to find the callsite that caused this warning to fire.
          throw new Error(message);
        } catch (x) {}
      };

      lowPriorityWarningWithoutStack = function (condition, format) {
        if (format === undefined) {
          throw new Error('`lowPriorityWarningWithoutStack(condition, format, ...args)` requires a warning ' + 'message argument');
        }

        if (!condition) {
          for (var _len2 = arguments.length, args = new Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
            args[_key2 - 2] = arguments[_key2];
          }

          printWarning.apply(void 0, [format].concat(args));
        }
      };
    }
    var lowPriorityWarningWithoutStack$1 = lowPriorityWarningWithoutStack;
    var ReactStrictModeWarnings = {
      recordUnsafeLifecycleWarnings: function (fiber, instance) {},
      flushPendingUnsafeLifecycleWarnings: function () {},
      recordLegacyContextWarning: function (fiber, instance) {},
      flushLegacyContextWarning: function () {},
      discardPendingWarnings: function () {}
    };
    {
      var findStrictRoot = function (fiber) {
        var maybeStrictRoot = null;
        var node = fiber;

        while (node !== null) {
          if (node.mode & StrictMode) {
            maybeStrictRoot = node;
          }

          node = node.return;
        }

        return maybeStrictRoot;
      };

      var setToSortedString = function (set) {
        var array = [];
        set.forEach(function (value) {
          array.push(value);
        });
        return array.sort().join(', ');
      };

      var pendingComponentWillMountWarnings = [];
      var pendingUNSAFE_ComponentWillMountWarnings = [];
      var pendingComponentWillReceivePropsWarnings = [];
      var pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
      var pendingComponentWillUpdateWarnings = [];
      var pendingUNSAFE_ComponentWillUpdateWarnings = []; // Tracks components we have already warned about.

      var didWarnAboutUnsafeLifecycles = new Set();

      ReactStrictModeWarnings.recordUnsafeLifecycleWarnings = function (fiber, instance) {
        // Dedup strategy: Warn once per component.
        if (didWarnAboutUnsafeLifecycles.has(fiber.type)) {
          return;
        }

        if (typeof instance.componentWillMount === 'function' && // Don't warn about react-lifecycles-compat polyfilled components.
        instance.componentWillMount.__suppressDeprecationWarning !== true) {
          pendingComponentWillMountWarnings.push(fiber);
        }

        if (fiber.mode & StrictMode && typeof instance.UNSAFE_componentWillMount === 'function') {
          pendingUNSAFE_ComponentWillMountWarnings.push(fiber);
        }

        if (typeof instance.componentWillReceiveProps === 'function' && instance.componentWillReceiveProps.__suppressDeprecationWarning !== true) {
          pendingComponentWillReceivePropsWarnings.push(fiber);
        }

        if (fiber.mode & StrictMode && typeof instance.UNSAFE_componentWillReceiveProps === 'function') {
          pendingUNSAFE_ComponentWillReceivePropsWarnings.push(fiber);
        }

        if (typeof instance.componentWillUpdate === 'function' && instance.componentWillUpdate.__suppressDeprecationWarning !== true) {
          pendingComponentWillUpdateWarnings.push(fiber);
        }

        if (fiber.mode & StrictMode && typeof instance.UNSAFE_componentWillUpdate === 'function') {
          pendingUNSAFE_ComponentWillUpdateWarnings.push(fiber);
        }
      };

      ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings = function () {
        // We do an initial pass to gather component names
        var componentWillMountUniqueNames = new Set();

        if (pendingComponentWillMountWarnings.length > 0) {
          pendingComponentWillMountWarnings.forEach(function (fiber) {
            componentWillMountUniqueNames.add(getComponentName(fiber.type) || 'Component');
            didWarnAboutUnsafeLifecycles.add(fiber.type);
          });
          pendingComponentWillMountWarnings = [];
        }

        var UNSAFE_componentWillMountUniqueNames = new Set();

        if (pendingUNSAFE_ComponentWillMountWarnings.length > 0) {
          pendingUNSAFE_ComponentWillMountWarnings.forEach(function (fiber) {
            UNSAFE_componentWillMountUniqueNames.add(getComponentName(fiber.type) || 'Component');
            didWarnAboutUnsafeLifecycles.add(fiber.type);
          });
          pendingUNSAFE_ComponentWillMountWarnings = [];
        }

        var componentWillReceivePropsUniqueNames = new Set();

        if (pendingComponentWillReceivePropsWarnings.length > 0) {
          pendingComponentWillReceivePropsWarnings.forEach(function (fiber) {
            componentWillReceivePropsUniqueNames.add(getComponentName(fiber.type) || 'Component');
            didWarnAboutUnsafeLifecycles.add(fiber.type);
          });
          pendingComponentWillReceivePropsWarnings = [];
        }

        var UNSAFE_componentWillReceivePropsUniqueNames = new Set();

        if (pendingUNSAFE_ComponentWillReceivePropsWarnings.length > 0) {
          pendingUNSAFE_ComponentWillReceivePropsWarnings.forEach(function (fiber) {
            UNSAFE_componentWillReceivePropsUniqueNames.add(getComponentName(fiber.type) || 'Component');
            didWarnAboutUnsafeLifecycles.add(fiber.type);
          });
          pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
        }

        var componentWillUpdateUniqueNames = new Set();

        if (pendingComponentWillUpdateWarnings.length > 0) {
          pendingComponentWillUpdateWarnings.forEach(function (fiber) {
            componentWillUpdateUniqueNames.add(getComponentName(fiber.type) || 'Component');
            didWarnAboutUnsafeLifecycles.add(fiber.type);
          });
          pendingComponentWillUpdateWarnings = [];
        }

        var UNSAFE_componentWillUpdateUniqueNames = new Set();

        if (pendingUNSAFE_ComponentWillUpdateWarnings.length > 0) {
          pendingUNSAFE_ComponentWillUpdateWarnings.forEach(function (fiber) {
            UNSAFE_componentWillUpdateUniqueNames.add(getComponentName(fiber.type) || 'Component');
            didWarnAboutUnsafeLifecycles.add(fiber.type);
          });
          pendingUNSAFE_ComponentWillUpdateWarnings = [];
        } // Finally, we flush all the warnings
        // UNSAFE_ ones before the deprecated ones, since they'll be 'louder'


        if (UNSAFE_componentWillMountUniqueNames.size > 0) {
          var sortedNames = setToSortedString(UNSAFE_componentWillMountUniqueNames);
          warningWithoutStack$1(false, 'Using UNSAFE_componentWillMount in strict mode is not recommended and may indicate bugs in your code. ' + 'See https://fb.me/react-unsafe-component-lifecycles for details.\n\n' + '* Move code with side effects to componentDidMount, and set initial state in the constructor.\n' + '\nPlease update the following components: %s', sortedNames);
        }

        if (UNSAFE_componentWillReceivePropsUniqueNames.size > 0) {
          var _sortedNames = setToSortedString(UNSAFE_componentWillReceivePropsUniqueNames);

          warningWithoutStack$1(false, 'Using UNSAFE_componentWillReceiveProps in strict mode is not recommended ' + 'and may indicate bugs in your code. ' + 'See https://fb.me/react-unsafe-component-lifecycles for details.\n\n' + '* Move data fetching code or side effects to componentDidUpdate.\n' + "* If you're updating state whenever props change, " + 'refactor your code to use memoization techniques or move it to ' + 'static getDerivedStateFromProps. Learn more at: https://fb.me/react-derived-state\n' + '\nPlease update the following components: %s', _sortedNames);
        }

        if (UNSAFE_componentWillUpdateUniqueNames.size > 0) {
          var _sortedNames2 = setToSortedString(UNSAFE_componentWillUpdateUniqueNames);

          warningWithoutStack$1(false, 'Using UNSAFE_componentWillUpdate in strict mode is not recommended ' + 'and may indicate bugs in your code. ' + 'See https://fb.me/react-unsafe-component-lifecycles for details.\n\n' + '* Move data fetching code or side effects to componentDidUpdate.\n' + '\nPlease update the following components: %s', _sortedNames2);
        }

        if (componentWillMountUniqueNames.size > 0) {
          var _sortedNames3 = setToSortedString(componentWillMountUniqueNames);

          lowPriorityWarningWithoutStack$1(false, 'componentWillMount has been renamed, and is not recommended for use. ' + 'See https://fb.me/react-unsafe-component-lifecycles for details.\n\n' + '* Move code with side effects to componentDidMount, and set initial state in the constructor.\n' + '* Rename componentWillMount to UNSAFE_componentWillMount to suppress ' + 'this warning in non-strict mode. In React 17.x, only the UNSAFE_ name will work. ' + 'To rename all deprecated lifecycles to their new names, you can run ' + '`npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n' + '\nPlease update the following components: %s', _sortedNames3);
        }

        if (componentWillReceivePropsUniqueNames.size > 0) {
          var _sortedNames4 = setToSortedString(componentWillReceivePropsUniqueNames);

          lowPriorityWarningWithoutStack$1(false, 'componentWillReceiveProps has been renamed, and is not recommended for use. ' + 'See https://fb.me/react-unsafe-component-lifecycles for details.\n\n' + '* Move data fetching code or side effects to componentDidUpdate.\n' + "* If you're updating state whenever props change, refactor your " + 'code to use memoization techniques or move it to ' + 'static getDerivedStateFromProps. Learn more at: https://fb.me/react-derived-state\n' + '* Rename componentWillReceiveProps to UNSAFE_componentWillReceiveProps to suppress ' + 'this warning in non-strict mode. In React 17.x, only the UNSAFE_ name will work. ' + 'To rename all deprecated lifecycles to their new names, you can run ' + '`npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n' + '\nPlease update the following components: %s', _sortedNames4);
        }

        if (componentWillUpdateUniqueNames.size > 0) {
          var _sortedNames5 = setToSortedString(componentWillUpdateUniqueNames);

          lowPriorityWarningWithoutStack$1(false, 'componentWillUpdate has been renamed, and is not recommended for use. ' + 'See https://fb.me/react-unsafe-component-lifecycles for details.\n\n' + '* Move data fetching code or side effects to componentDidUpdate.\n' + '* Rename componentWillUpdate to UNSAFE_componentWillUpdate to suppress ' + 'this warning in non-strict mode. In React 17.x, only the UNSAFE_ name will work. ' + 'To rename all deprecated lifecycles to their new names, you can run ' + '`npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n' + '\nPlease update the following components: %s', _sortedNames5);
        }
      };

      var pendingLegacyContextWarning = new Map(); // Tracks components we have already warned about.

      var didWarnAboutLegacyContext = new Set();

      ReactStrictModeWarnings.recordLegacyContextWarning = function (fiber, instance) {
        var strictRoot = findStrictRoot(fiber);

        if (strictRoot === null) {
          warningWithoutStack$1(false, 'Expected to find a StrictMode component in a strict mode tree. ' + 'This error is likely caused by a bug in React. Please file an issue.');
          return;
        } // Dedup strategy: Warn once per component.


        if (didWarnAboutLegacyContext.has(fiber.type)) {
          return;
        }

        var warningsForRoot = pendingLegacyContextWarning.get(strictRoot);

        if (fiber.type.contextTypes != null || fiber.type.childContextTypes != null || instance !== null && typeof instance.getChildContext === 'function') {
          if (warningsForRoot === undefined) {
            warningsForRoot = [];
            pendingLegacyContextWarning.set(strictRoot, warningsForRoot);
          }

          warningsForRoot.push(fiber);
        }
      };

      ReactStrictModeWarnings.flushLegacyContextWarning = function () {
        pendingLegacyContextWarning.forEach(function (fiberArray, strictRoot) {
          var uniqueNames = new Set();
          fiberArray.forEach(function (fiber) {
            uniqueNames.add(getComponentName(fiber.type) || 'Component');
            didWarnAboutLegacyContext.add(fiber.type);
          });
          var sortedNames = setToSortedString(uniqueNames);
          var strictRootComponentStack = getStackByFiberInDevAndProd(strictRoot);
          warningWithoutStack$1(false, 'Legacy context API has been detected within a strict-mode tree.' + '\n\nThe old API will be supported in all 16.x releases, but applications ' + 'using it should migrate to the new version.' + '\n\nPlease update the following components: %s' + '\n\nLearn more about this warning here: https://fb.me/react-legacy-context' + '%s', sortedNames, strictRootComponentStack);
        });
      };

      ReactStrictModeWarnings.discardPendingWarnings = function () {
        pendingComponentWillMountWarnings = [];
        pendingUNSAFE_ComponentWillMountWarnings = [];
        pendingComponentWillReceivePropsWarnings = [];
        pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
        pendingComponentWillUpdateWarnings = [];
        pendingUNSAFE_ComponentWillUpdateWarnings = [];
        pendingLegacyContextWarning = new Map();
      };
    }
    var resolveFamily = null; // $FlowFixMe Flow gets confused by a WeakSet feature check below.

    var failedBoundaries = null;

    var setRefreshHandler = function (handler) {
      {
        resolveFamily = handler;
      }
    };

    function resolveFunctionForHotReloading(type) {
      {
        if (resolveFamily === null) {
          // Hot reloading is disabled.
          return type;
        }

        var family = resolveFamily(type);

        if (family === undefined) {
          return type;
        } // Use the latest known implementation.


        return family.current;
      }
    }

    function resolveClassForHotReloading(type) {
      // No implementation differences.
      return resolveFunctionForHotReloading(type);
    }

    function resolveForwardRefForHotReloading(type) {
      {
        if (resolveFamily === null) {
          // Hot reloading is disabled.
          return type;
        }

        var family = resolveFamily(type);

        if (family === undefined) {
          // Check if we're dealing with a real forwardRef. Don't want to crash early.
          if (type !== null && type !== undefined && typeof type.render === 'function') {
            // ForwardRef is special because its resolved .type is an object,
            // but it's possible that we only have its inner render function in the map.
            // If that inner render function is different, we'll build a new forwardRef type.
            var currentRender = resolveFunctionForHotReloading(type.render);

            if (type.render !== currentRender) {
              var syntheticType = {
                $$typeof: REACT_FORWARD_REF_TYPE,
                render: currentRender
              };

              if (type.displayName !== undefined) {
                syntheticType.displayName = type.displayName;
              }

              return syntheticType;
            }
          }

          return type;
        } // Use the latest known implementation.


        return family.current;
      }
    }

    function isCompatibleFamilyForHotReloading(fiber, element) {
      {
        if (resolveFamily === null) {
          // Hot reloading is disabled.
          return false;
        }

        var prevType = fiber.elementType;
        var nextType = element.type; // If we got here, we know types aren't === equal.

        var needsCompareFamilies = false;
        var $$typeofNextType = typeof nextType === 'object' && nextType !== null ? nextType.$$typeof : null;

        switch (fiber.tag) {
          case ClassComponent:
            {
              if (typeof nextType === 'function') {
                needsCompareFamilies = true;
              }

              break;
            }

          case FunctionComponent:
            {
              if (typeof nextType === 'function') {
                needsCompareFamilies = true;
              } else if ($$typeofNextType === REACT_LAZY_TYPE) {
                // We don't know the inner type yet.
                // We're going to assume that the lazy inner type is stable,
                // and so it is sufficient to avoid reconciling it away.
                // We're not going to unwrap or actually use the new lazy type.
                needsCompareFamilies = true;
              }

              break;
            }

          case ForwardRef:
            {
              if ($$typeofNextType === REACT_FORWARD_REF_TYPE) {
                needsCompareFamilies = true;
              } else if ($$typeofNextType === REACT_LAZY_TYPE) {
                needsCompareFamilies = true;
              }

              break;
            }

          case MemoComponent:
          case SimpleMemoComponent:
            {
              if ($$typeofNextType === REACT_MEMO_TYPE) {
                // TODO: if it was but can no longer be simple,
                // we shouldn't set this.
                needsCompareFamilies = true;
              } else if ($$typeofNextType === REACT_LAZY_TYPE) {
                needsCompareFamilies = true;
              }

              break;
            }

          default:
            return false;
        } // Check if both types have a family and it's the same one.


        if (needsCompareFamilies) {
          // Note: memo() and forwardRef() we'll compare outer rather than inner type.
          // This means both of them need to be registered to preserve state.
          // If we unwrapped and compared the inner types for wrappers instead,
          // then we would risk falsely saying two separate memo(Foo)
          // calls are equivalent because they wrap the same Foo function.
          var prevFamily = resolveFamily(prevType);

          if (prevFamily !== undefined && prevFamily === resolveFamily(nextType)) {
            return true;
          }
        }

        return false;
      }
    }

    function markFailedErrorBoundaryForHotReloading(fiber) {
      {
        if (resolveFamily === null) {
          // Hot reloading is disabled.
          return;
        }

        if (typeof WeakSet !== 'function') {
          return;
        }

        if (failedBoundaries === null) {
          failedBoundaries = new WeakSet();
        }

        failedBoundaries.add(fiber);
      }
    }

    var scheduleRefresh = function (root, update) {
      {
        if (resolveFamily === null) {
          // Hot reloading is disabled.
          return;
        }

        var staleFamilies = update.staleFamilies,
            updatedFamilies = update.updatedFamilies;
        flushPassiveEffects();
        flushSync(function () {
          scheduleFibersWithFamiliesRecursively(root.current, updatedFamilies, staleFamilies);
        });
      }
    };

    var scheduleRoot = function (root, element) {
      {
        if (root.context !== emptyContextObject) {
          // Super edge case: root has a legacy _renderSubtree context
          // but we don't know the parentComponent so we can't pass it.
          // Just ignore. We'll delete this with _renderSubtree code path later.
          return;
        }

        flushPassiveEffects();
        updateContainerAtExpirationTime(element, root, null, Sync, null);
      }
    };

    function scheduleFibersWithFamiliesRecursively(fiber, updatedFamilies, staleFamilies) {
      {
        var alternate = fiber.alternate,
            child = fiber.child,
            sibling = fiber.sibling,
            tag = fiber.tag,
            type = fiber.type;
        var candidateType = null;

        switch (tag) {
          case FunctionComponent:
          case SimpleMemoComponent:
          case ClassComponent:
            candidateType = type;
            break;

          case ForwardRef:
            candidateType = type.render;
            break;

          default:
            break;
        }

        if (resolveFamily === null) {
          throw new Error('Expected resolveFamily to be set during hot reload.');
        }

        var needsRender = false;
        var needsRemount = false;

        if (candidateType !== null) {
          var family = resolveFamily(candidateType);

          if (family !== undefined) {
            if (staleFamilies.has(family)) {
              needsRemount = true;
            } else if (updatedFamilies.has(family)) {
              if (tag === ClassComponent) {
                needsRemount = true;
              } else {
                needsRender = true;
              }
            }
          }
        }

        if (failedBoundaries !== null) {
          if (failedBoundaries.has(fiber) || alternate !== null && failedBoundaries.has(alternate)) {
            needsRemount = true;
          }
        }

        if (needsRemount) {
          fiber._debugNeedsRemount = true;
        }

        if (needsRemount || needsRender) {
          scheduleWork(fiber, Sync);
        }

        if (child !== null && !needsRemount) {
          scheduleFibersWithFamiliesRecursively(child, updatedFamilies, staleFamilies);
        }

        if (sibling !== null) {
          scheduleFibersWithFamiliesRecursively(sibling, updatedFamilies, staleFamilies);
        }
      }
    }

    var findHostInstancesForRefresh = function (root, families) {
      {
        var hostInstances = new Set();
        var types = new Set(families.map(function (family) {
          return family.current;
        }));
        findHostInstancesForMatchingFibersRecursively(root.current, types, hostInstances);
        return hostInstances;
      }
    };

    function findHostInstancesForMatchingFibersRecursively(fiber, types, hostInstances) {
      {
        var child = fiber.child,
            sibling = fiber.sibling,
            tag = fiber.tag,
            type = fiber.type;
        var candidateType = null;

        switch (tag) {
          case FunctionComponent:
          case SimpleMemoComponent:
          case ClassComponent:
            candidateType = type;
            break;

          case ForwardRef:
            candidateType = type.render;
            break;

          default:
            break;
        }

        var didMatch = false;

        if (candidateType !== null) {
          if (types.has(candidateType)) {
            didMatch = true;
          }
        }

        if (didMatch) {
          // We have a match. This only drills down to the closest host components.
          // There's no need to search deeper because for the purpose of giving
          // visual feedback, "flashing" outermost parent rectangles is sufficient.
          findHostInstancesForFiberShallowly(fiber, hostInstances);
        } else {
          // If there's no match, maybe there will be one further down in the child tree.
          if (child !== null) {
            findHostInstancesForMatchingFibersRecursively(child, types, hostInstances);
          }
        }

        if (sibling !== null) {
          findHostInstancesForMatchingFibersRecursively(sibling, types, hostInstances);
        }
      }
    }

    function findHostInstancesForFiberShallowly(fiber, hostInstances) {
      {
        var foundHostInstances = findChildHostInstancesForFiberShallowly(fiber, hostInstances);

        if (foundHostInstances) {
          return;
        } // If we didn't find any host children, fallback to closest host parent.


        var node = fiber;

        while (true) {
          switch (node.tag) {
            case HostComponent:
              hostInstances.add(node.stateNode);
              return;

            case HostPortal:
              hostInstances.add(node.stateNode.containerInfo);
              return;

            case HostRoot:
              hostInstances.add(node.stateNode.containerInfo);
              return;
          }

          if (node.return === null) {
            throw new Error('Expected to reach root first.');
          }

          node = node.return;
        }
      }
    }

    function findChildHostInstancesForFiberShallowly(fiber, hostInstances) {
      {
        var node = fiber;
        var foundHostInstances = false;

        while (true) {
          if (node.tag === HostComponent) {
            // We got a match.
            foundHostInstances = true;
            hostInstances.add(node.stateNode); // There may still be more, so keep searching.
          } else if (node.child !== null) {
            node.child.return = node;
            node = node.child;
            continue;
          }

          if (node === fiber) {
            return foundHostInstances;
          }

          while (node.sibling === null) {
            if (node.return === null || node.return === fiber) {
              return foundHostInstances;
            }

            node = node.return;
          }

          node.sibling.return = node.return;
          node = node.sibling;
        }
      }
      return false;
    }

    function resolveDefaultProps(Component, baseProps) {
      if (Component && Component.defaultProps) {
        // Resolve default props. Taken from ReactElement
        var props = _assign({}, baseProps);

        var defaultProps = Component.defaultProps;

        for (var propName in defaultProps) {
          if (props[propName] === undefined) {
            props[propName] = defaultProps[propName];
          }
        }

        return props;
      }

      return baseProps;
    }

    function readLazyComponentType(lazyComponent) {
      initializeLazyComponentType(lazyComponent);

      if (lazyComponent._status !== Resolved) {
        throw lazyComponent._result;
      }

      return lazyComponent._result;
    }

    var valueCursor = createCursor(null);
    var rendererSigil;
    {
      // Use this to detect multiple renderers using the same context
      rendererSigil = {};
    }
    var currentlyRenderingFiber = null;
    var lastContextDependency = null;
    var lastContextWithAllBitsObserved = null;
    var isDisallowedContextReadInDEV = false;

    function resetContextDependencies() {
      // This is called right before React yields execution, to ensure `readContext`
      // cannot be called outside the render phase.
      currentlyRenderingFiber = null;
      lastContextDependency = null;
      lastContextWithAllBitsObserved = null;
      {
        isDisallowedContextReadInDEV = false;
      }
    }

    function enterDisallowedContextReadInDEV() {
      {
        isDisallowedContextReadInDEV = true;
      }
    }

    function exitDisallowedContextReadInDEV() {
      {
        isDisallowedContextReadInDEV = false;
      }
    }

    function pushProvider(providerFiber, nextValue) {
      var context = providerFiber.type._context;

      if (isPrimaryRenderer) {
        push(valueCursor, context._currentValue, providerFiber);
        context._currentValue = nextValue;
        {
          !(context._currentRenderer === undefined || context._currentRenderer === null || context._currentRenderer === rendererSigil) ? warningWithoutStack$1(false, 'Detected multiple renderers concurrently rendering the ' + 'same context provider. This is currently unsupported.') : void 0;
          context._currentRenderer = rendererSigil;
        }
      } else {
        push(valueCursor, context._currentValue2, providerFiber);
        context._currentValue2 = nextValue;
        {
          !(context._currentRenderer2 === undefined || context._currentRenderer2 === null || context._currentRenderer2 === rendererSigil) ? warningWithoutStack$1(false, 'Detected multiple renderers concurrently rendering the ' + 'same context provider. This is currently unsupported.') : void 0;
          context._currentRenderer2 = rendererSigil;
        }
      }
    }

    function popProvider(providerFiber) {
      var currentValue = valueCursor.current;
      pop(valueCursor, providerFiber);
      var context = providerFiber.type._context;

      if (isPrimaryRenderer) {
        context._currentValue = currentValue;
      } else {
        context._currentValue2 = currentValue;
      }
    }

    function calculateChangedBits(context, newValue, oldValue) {
      if (is$1(oldValue, newValue)) {
        // No change
        return 0;
      } else {
        var changedBits = typeof context._calculateChangedBits === 'function' ? context._calculateChangedBits(oldValue, newValue) : MAX_SIGNED_31_BIT_INT;
        {
          !((changedBits & MAX_SIGNED_31_BIT_INT) === changedBits) ? warning$1(false, 'calculateChangedBits: Expected the return value to be a ' + '31-bit integer. Instead received: %s', changedBits) : void 0;
        }
        return changedBits | 0;
      }
    }

    function scheduleWorkOnParentPath(parent, renderExpirationTime) {
      // Update the child expiration time of all the ancestors, including
      // the alternates.
      var node = parent;

      while (node !== null) {
        var alternate = node.alternate;

        if (node.childExpirationTime < renderExpirationTime) {
          node.childExpirationTime = renderExpirationTime;

          if (alternate !== null && alternate.childExpirationTime < renderExpirationTime) {
            alternate.childExpirationTime = renderExpirationTime;
          }
        } else if (alternate !== null && alternate.childExpirationTime < renderExpirationTime) {
          alternate.childExpirationTime = renderExpirationTime;
        } else {
          // Neither alternate was updated, which means the rest of the
          // ancestor path already has sufficient priority.
          break;
        }

        node = node.return;
      }
    }

    function propagateContextChange(workInProgress, context, changedBits, renderExpirationTime) {
      var fiber = workInProgress.child;

      if (fiber !== null) {
        // Set the return pointer of the child to the work-in-progress fiber.
        fiber.return = workInProgress;
      }

      while (fiber !== null) {
        var nextFiber = void 0; // Visit this fiber.

        var list = fiber.dependencies;

        if (list !== null) {
          nextFiber = fiber.child;
          var dependency = list.firstContext;

          while (dependency !== null) {
            // Check if the context matches.
            if (dependency.context === context && (dependency.observedBits & changedBits) !== 0) {
              // Match! Schedule an update on this fiber.
              if (fiber.tag === ClassComponent) {
                // Schedule a force update on the work-in-progress.
                var update = createUpdate(renderExpirationTime, null);
                update.tag = ForceUpdate; // TODO: Because we don't have a work-in-progress, this will add the
                // update to the current fiber, too, which means it will persist even if
                // this render is thrown away. Since it's a race condition, not sure it's
                // worth fixing.

                enqueueUpdate(fiber, update);
              }

              if (fiber.expirationTime < renderExpirationTime) {
                fiber.expirationTime = renderExpirationTime;
              }

              var alternate = fiber.alternate;

              if (alternate !== null && alternate.expirationTime < renderExpirationTime) {
                alternate.expirationTime = renderExpirationTime;
              }

              scheduleWorkOnParentPath(fiber.return, renderExpirationTime); // Mark the expiration time on the list, too.

              if (list.expirationTime < renderExpirationTime) {
                list.expirationTime = renderExpirationTime;
              } // Since we already found a match, we can stop traversing the
              // dependency list.


              break;
            }

            dependency = dependency.next;
          }
        } else if (fiber.tag === ContextProvider) {
          // Don't scan deeper if this is a matching provider
          nextFiber = fiber.type === workInProgress.type ? null : fiber.child;
        } else if (enableSuspenseServerRenderer && fiber.tag === DehydratedFragment) {
          // If a dehydrated suspense bounudary is in this subtree, we don't know
          // if it will have any context consumers in it. The best we can do is
          // mark it as having updates.
          var parentSuspense = fiber.return;

          (function () {
            if (!(parentSuspense !== null)) {
              {
                throw ReactError(Error("We just came from a parent so we must have had a parent. This is a bug in React."));
              }
            }
          })();

          if (parentSuspense.expirationTime < renderExpirationTime) {
            parentSuspense.expirationTime = renderExpirationTime;
          }

          var _alternate = parentSuspense.alternate;

          if (_alternate !== null && _alternate.expirationTime < renderExpirationTime) {
            _alternate.expirationTime = renderExpirationTime;
          } // This is intentionally passing this fiber as the parent
          // because we want to schedule this fiber as having work
          // on its children. We'll use the childExpirationTime on
          // this fiber to indicate that a context has changed.


          scheduleWorkOnParentPath(parentSuspense, renderExpirationTime);
          nextFiber = fiber.sibling;
        } else {
          // Traverse down.
          nextFiber = fiber.child;
        }

        if (nextFiber !== null) {
          // Set the return pointer of the child to the work-in-progress fiber.
          nextFiber.return = fiber;
        } else {
          // No child. Traverse to next sibling.
          nextFiber = fiber;

          while (nextFiber !== null) {
            if (nextFiber === workInProgress) {
              // We're back to the root of this subtree. Exit.
              nextFiber = null;
              break;
            }

            var sibling = nextFiber.sibling;

            if (sibling !== null) {
              // Set the return pointer of the sibling to the work-in-progress fiber.
              sibling.return = nextFiber.return;
              nextFiber = sibling;
              break;
            } // No more siblings. Traverse up.


            nextFiber = nextFiber.return;
          }
        }

        fiber = nextFiber;
      }
    }

    function prepareToReadContext(workInProgress, renderExpirationTime) {
      currentlyRenderingFiber = workInProgress;
      lastContextDependency = null;
      lastContextWithAllBitsObserved = null;
      var dependencies = workInProgress.dependencies;

      if (dependencies !== null) {
        var firstContext = dependencies.firstContext;

        if (firstContext !== null) {
          if (dependencies.expirationTime >= renderExpirationTime) {
            // Context list has a pending update. Mark that this fiber performed work.
            markWorkInProgressReceivedUpdate();
          } // Reset the work-in-progress list


          dependencies.firstContext = null;
        }
      }
    }

    function readContext(context, observedBits) {
      {
        // This warning would fire if you read context inside a Hook like useMemo.
        // Unlike the class check below, it's not enforced in production for perf.
        !!isDisallowedContextReadInDEV ? warning$1(false, 'Context can only be read while React is rendering. ' + 'In classes, you can read it in the render method or getDerivedStateFromProps. ' + 'In function components, you can read it directly in the function body, but not ' + 'inside Hooks like useReducer() or useMemo().') : void 0;
      }

      if (lastContextWithAllBitsObserved === context) {// Nothing to do. We already observe everything in this context.
      } else if (observedBits === false || observedBits === 0) {// Do not observe any updates.
      } else {
        var resolvedObservedBits; // Avoid deopting on observable arguments or heterogeneous types.

        if (typeof observedBits !== 'number' || observedBits === MAX_SIGNED_31_BIT_INT) {
          // Observe all updates.
          lastContextWithAllBitsObserved = context;
          resolvedObservedBits = MAX_SIGNED_31_BIT_INT;
        } else {
          resolvedObservedBits = observedBits;
        }

        var contextItem = {
          context: context,
          observedBits: resolvedObservedBits,
          next: null
        };

        if (lastContextDependency === null) {
          (function () {
            if (!(currentlyRenderingFiber !== null)) {
              {
                throw ReactError(Error("Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo()."));
              }
            }
          })(); // This is the first dependency for this component. Create a new list.


          lastContextDependency = contextItem;
          currentlyRenderingFiber.dependencies = {
            expirationTime: NoWork,
            firstContext: contextItem,
            responders: null
          };
        } else {
          // Append a new context item.
          lastContextDependency = lastContextDependency.next = contextItem;
        }
      }

      return isPrimaryRenderer ? context._currentValue : context._currentValue2;
    } // UpdateQueue is a linked list of prioritized updates.
    //
    // Like fibers, update queues come in pairs: a current queue, which represents
    // the visible state of the screen, and a work-in-progress queue, which can be
    // mutated and processed asynchronously before it is committed — a form of
    // double buffering. If a work-in-progress render is discarded before finishing,
    // we create a new work-in-progress by cloning the current queue.
    //
    // Both queues share a persistent, singly-linked list structure. To schedule an
    // update, we append it to the end of both queues. Each queue maintains a
    // pointer to first update in the persistent list that hasn't been processed.
    // The work-in-progress pointer always has a position equal to or greater than
    // the current queue, since we always work on that one. The current queue's
    // pointer is only updated during the commit phase, when we swap in the
    // work-in-progress.
    //
    // For example:
    //
    //   Current pointer:           A - B - C - D - E - F
    //   Work-in-progress pointer:              D - E - F
    //                                          ^
    //                                          The work-in-progress queue has
    //                                          processed more updates than current.
    //
    // The reason we append to both queues is because otherwise we might drop
    // updates without ever processing them. For example, if we only add updates to
    // the work-in-progress queue, some updates could be lost whenever a work-in
    // -progress render restarts by cloning from current. Similarly, if we only add
    // updates to the current queue, the updates will be lost whenever an already
    // in-progress queue commits and swaps with the current queue. However, by
    // adding to both queues, we guarantee that the update will be part of the next
    // work-in-progress. (And because the work-in-progress queue becomes the
    // current queue once it commits, there's no danger of applying the same
    // update twice.)
    //
    // Prioritization
    // --------------
    //
    // Updates are not sorted by priority, but by insertion; new updates are always
    // appended to the end of the list.
    //
    // The priority is still important, though. When processing the update queue
    // during the render phase, only the updates with sufficient priority are
    // included in the result. If we skip an update because it has insufficient
    // priority, it remains in the queue to be processed later, during a lower
    // priority render. Crucially, all updates subsequent to a skipped update also
    // remain in the queue *regardless of their priority*. That means high priority
    // updates are sometimes processed twice, at two separate priorities. We also
    // keep track of a base state, that represents the state before the first
    // update in the queue is applied.
    //
    // For example:
    //
    //   Given a base state of '', and the following queue of updates
    //
    //     A1 - B2 - C1 - D2
    //
    //   where the number indicates the priority, and the update is applied to the
    //   previous state by appending a letter, React will process these updates as
    //   two separate renders, one per distinct priority level:
    //
    //   First render, at priority 1:
    //     Base state: ''
    //     Updates: [A1, C1]
    //     Result state: 'AC'
    //
    //   Second render, at priority 2:
    //     Base state: 'A'            <-  The base state does not include C1,
    //                                    because B2 was skipped.
    //     Updates: [B2, C1, D2]      <-  C1 was rebased on top of B2
    //     Result state: 'ABCD'
    //
    // Because we process updates in insertion order, and rebase high priority
    // updates when preceding updates are skipped, the final result is deterministic
    // regardless of priority. Intermediate state may vary according to system
    // resources, but the final state is always the same.


    var UpdateState = 0;
    var ReplaceState = 1;
    var ForceUpdate = 2;
    var CaptureUpdate = 3; // Global state that is reset at the beginning of calling `processUpdateQueue`.
    // It should only be read right after calling `processUpdateQueue`, via
    // `checkHasForceUpdateAfterProcessing`.

    var hasForceUpdate = false;
    var didWarnUpdateInsideUpdate;
    var currentlyProcessingQueue;
    {
      didWarnUpdateInsideUpdate = false;
      currentlyProcessingQueue = null;
    }

    function createUpdateQueue(baseState) {
      var queue = {
        baseState: baseState,
        firstUpdate: null,
        lastUpdate: null,
        firstCapturedUpdate: null,
        lastCapturedUpdate: null,
        firstEffect: null,
        lastEffect: null,
        firstCapturedEffect: null,
        lastCapturedEffect: null
      };
      return queue;
    }

    function cloneUpdateQueue(currentQueue) {
      var queue = {
        baseState: currentQueue.baseState,
        firstUpdate: currentQueue.firstUpdate,
        lastUpdate: currentQueue.lastUpdate,
        // TODO: With resuming, if we bail out and resuse the child tree, we should
        // keep these effects.
        firstCapturedUpdate: null,
        lastCapturedUpdate: null,
        firstEffect: null,
        lastEffect: null,
        firstCapturedEffect: null,
        lastCapturedEffect: null
      };
      return queue;
    }

    function createUpdate(expirationTime, suspenseConfig) {
      var update = {
        expirationTime: expirationTime,
        suspenseConfig: suspenseConfig,
        tag: UpdateState,
        payload: null,
        callback: null,
        next: null,
        nextEffect: null
      };
      {
        update.priority = getCurrentPriorityLevel();
      }
      return update;
    }

    function appendUpdateToQueue(queue, update) {
      // Append the update to the end of the list.
      if (queue.lastUpdate === null) {
        // Queue is empty
        queue.firstUpdate = queue.lastUpdate = update;
      } else {
        queue.lastUpdate.next = update;
        queue.lastUpdate = update;
      }
    }

    function enqueueUpdate(fiber, update) {
      // Update queues are created lazily.
      var alternate = fiber.alternate;
      var queue1;
      var queue2;

      if (alternate === null) {
        // There's only one fiber.
        queue1 = fiber.updateQueue;
        queue2 = null;

        if (queue1 === null) {
          queue1 = fiber.updateQueue = createUpdateQueue(fiber.memoizedState);
        }
      } else {
        // There are two owners.
        queue1 = fiber.updateQueue;
        queue2 = alternate.updateQueue;

        if (queue1 === null) {
          if (queue2 === null) {
            // Neither fiber has an update queue. Create new ones.
            queue1 = fiber.updateQueue = createUpdateQueue(fiber.memoizedState);
            queue2 = alternate.updateQueue = createUpdateQueue(alternate.memoizedState);
          } else {
            // Only one fiber has an update queue. Clone to create a new one.
            queue1 = fiber.updateQueue = cloneUpdateQueue(queue2);
          }
        } else {
          if (queue2 === null) {
            // Only one fiber has an update queue. Clone to create a new one.
            queue2 = alternate.updateQueue = cloneUpdateQueue(queue1);
          } else {// Both owners have an update queue.
          }
        }
      }

      if (queue2 === null || queue1 === queue2) {
        // There's only a single queue.
        appendUpdateToQueue(queue1, update);
      } else {
        // There are two queues. We need to append the update to both queues,
        // while accounting for the persistent structure of the list — we don't
        // want the same update to be added multiple times.
        if (queue1.lastUpdate === null || queue2.lastUpdate === null) {
          // One of the queues is not empty. We must add the update to both queues.
          appendUpdateToQueue(queue1, update);
          appendUpdateToQueue(queue2, update);
        } else {
          // Both queues are non-empty. The last update is the same in both lists,
          // because of structural sharing. So, only append to one of the lists.
          appendUpdateToQueue(queue1, update); // But we still need to update the `lastUpdate` pointer of queue2.

          queue2.lastUpdate = update;
        }
      }

      {
        if (fiber.tag === ClassComponent && (currentlyProcessingQueue === queue1 || queue2 !== null && currentlyProcessingQueue === queue2) && !didWarnUpdateInsideUpdate) {
          warningWithoutStack$1(false, 'An update (setState, replaceState, or forceUpdate) was scheduled ' + 'from inside an update function. Update functions should be pure, ' + 'with zero side-effects. Consider using componentDidUpdate or a ' + 'callback.');
          didWarnUpdateInsideUpdate = true;
        }
      }
    }

    function enqueueCapturedUpdate(workInProgress, update) {
      // Captured updates go into a separate list, and only on the work-in-
      // progress queue.
      var workInProgressQueue = workInProgress.updateQueue;

      if (workInProgressQueue === null) {
        workInProgressQueue = workInProgress.updateQueue = createUpdateQueue(workInProgress.memoizedState);
      } else {
        // TODO: I put this here rather than createWorkInProgress so that we don't
        // clone the queue unnecessarily. There's probably a better way to
        // structure this.
        workInProgressQueue = ensureWorkInProgressQueueIsAClone(workInProgress, workInProgressQueue);
      } // Append the update to the end of the list.


      if (workInProgressQueue.lastCapturedUpdate === null) {
        // This is the first render phase update
        workInProgressQueue.firstCapturedUpdate = workInProgressQueue.lastCapturedUpdate = update;
      } else {
        workInProgressQueue.lastCapturedUpdate.next = update;
        workInProgressQueue.lastCapturedUpdate = update;
      }
    }

    function ensureWorkInProgressQueueIsAClone(workInProgress, queue) {
      var current = workInProgress.alternate;

      if (current !== null) {
        // If the work-in-progress queue is equal to the current queue,
        // we need to clone it first.
        if (queue === current.updateQueue) {
          queue = workInProgress.updateQueue = cloneUpdateQueue(queue);
        }
      }

      return queue;
    }

    function getStateFromUpdate(workInProgress, queue, update, prevState, nextProps, instance) {
      switch (update.tag) {
        case ReplaceState:
          {
            var payload = update.payload;

            if (typeof payload === 'function') {
              // Updater function
              {
                enterDisallowedContextReadInDEV();

                if (debugRenderPhaseSideEffects || debugRenderPhaseSideEffectsForStrictMode && workInProgress.mode & StrictMode) {
                  payload.call(instance, prevState, nextProps);
                }
              }
              var nextState = payload.call(instance, prevState, nextProps);
              {
                exitDisallowedContextReadInDEV();
              }
              return nextState;
            } // State object


            return payload;
          }

        case CaptureUpdate:
          {
            workInProgress.effectTag = workInProgress.effectTag & ~ShouldCapture | DidCapture;
          }
        // Intentional fallthrough

        case UpdateState:
          {
            var _payload = update.payload;
            var partialState;

            if (typeof _payload === 'function') {
              // Updater function
              {
                enterDisallowedContextReadInDEV();

                if (debugRenderPhaseSideEffects || debugRenderPhaseSideEffectsForStrictMode && workInProgress.mode & StrictMode) {
                  _payload.call(instance, prevState, nextProps);
                }
              }
              partialState = _payload.call(instance, prevState, nextProps);
              {
                exitDisallowedContextReadInDEV();
              }
            } else {
              // Partial state object
              partialState = _payload;
            }

            if (partialState === null || partialState === undefined) {
              // Null and undefined are treated as no-ops.
              return prevState;
            } // Merge the partial state and the previous state.


            return _assign({}, prevState, partialState);
          }

        case ForceUpdate:
          {
            hasForceUpdate = true;
            return prevState;
          }
      }

      return prevState;
    }

    function processUpdateQueue(workInProgress, queue, props, instance, renderExpirationTime) {
      hasForceUpdate = false;
      queue = ensureWorkInProgressQueueIsAClone(workInProgress, queue);
      {
        currentlyProcessingQueue = queue;
      } // These values may change as we process the queue.

      var newBaseState = queue.baseState;
      var newFirstUpdate = null;
      var newExpirationTime = NoWork; // Iterate through the list of updates to compute the result.

      var update = queue.firstUpdate;
      var resultState = newBaseState;

      while (update !== null) {
        var updateExpirationTime = update.expirationTime;

        if (updateExpirationTime < renderExpirationTime) {
          // This update does not have sufficient priority. Skip it.
          if (newFirstUpdate === null) {
            // This is the first skipped update. It will be the first update in
            // the new list.
            newFirstUpdate = update; // Since this is the first update that was skipped, the current result
            // is the new base state.

            newBaseState = resultState;
          } // Since this update will remain in the list, update the remaining
          // expiration time.


          if (newExpirationTime < updateExpirationTime) {
            newExpirationTime = updateExpirationTime;
          }
        } else {
          // This update does have sufficient priority.
          // Mark the event time of this update as relevant to this render pass.
          // TODO: This should ideally use the true event time of this update rather than
          // its priority which is a derived and not reverseable value.
          // TODO: We should skip this update if it was already committed but currently
          // we have no way of detecting the difference between a committed and suspended
          // update here.
          markRenderEventTimeAndConfig(updateExpirationTime, update.suspenseConfig); // Process it and compute a new result.

          resultState = getStateFromUpdate(workInProgress, queue, update, resultState, props, instance);
          var callback = update.callback;

          if (callback !== null) {
            workInProgress.effectTag |= Callback; // Set this to null, in case it was mutated during an aborted render.

            update.nextEffect = null;

            if (queue.lastEffect === null) {
              queue.firstEffect = queue.lastEffect = update;
            } else {
              queue.lastEffect.nextEffect = update;
              queue.lastEffect = update;
            }
          }
        } // Continue to the next update.


        update = update.next;
      } // Separately, iterate though the list of captured updates.


      var newFirstCapturedUpdate = null;
      update = queue.firstCapturedUpdate;

      while (update !== null) {
        var _updateExpirationTime = update.expirationTime;

        if (_updateExpirationTime < renderExpirationTime) {
          // This update does not have sufficient priority. Skip it.
          if (newFirstCapturedUpdate === null) {
            // This is the first skipped captured update. It will be the first
            // update in the new list.
            newFirstCapturedUpdate = update; // If this is the first update that was skipped, the current result is
            // the new base state.

            if (newFirstUpdate === null) {
              newBaseState = resultState;
            }
          } // Since this update will remain in the list, update the remaining
          // expiration time.


          if (newExpirationTime < _updateExpirationTime) {
            newExpirationTime = _updateExpirationTime;
          }
        } else {
          // This update does have sufficient priority. Process it and compute
          // a new result.
          resultState = getStateFromUpdate(workInProgress, queue, update, resultState, props, instance);
          var _callback = update.callback;

          if (_callback !== null) {
            workInProgress.effectTag |= Callback; // Set this to null, in case it was mutated during an aborted render.

            update.nextEffect = null;

            if (queue.lastCapturedEffect === null) {
              queue.firstCapturedEffect = queue.lastCapturedEffect = update;
            } else {
              queue.lastCapturedEffect.nextEffect = update;
              queue.lastCapturedEffect = update;
            }
          }
        }

        update = update.next;
      }

      if (newFirstUpdate === null) {
        queue.lastUpdate = null;
      }

      if (newFirstCapturedUpdate === null) {
        queue.lastCapturedUpdate = null;
      } else {
        workInProgress.effectTag |= Callback;
      }

      if (newFirstUpdate === null && newFirstCapturedUpdate === null) {
        // We processed every update, without skipping. That means the new base
        // state is the same as the result state.
        newBaseState = resultState;
      }

      queue.baseState = newBaseState;
      queue.firstUpdate = newFirstUpdate;
      queue.firstCapturedUpdate = newFirstCapturedUpdate; // Set the remaining expiration time to be whatever is remaining in the queue.
      // This should be fine because the only two other things that contribute to
      // expiration time are props and context. We're already in the middle of the
      // begin phase by the time we start processing the queue, so we've already
      // dealt with the props. Context in components that specify
      // shouldComponentUpdate is tricky; but we'll have to account for
      // that regardless.

      markUnprocessedUpdateTime(newExpirationTime);
      workInProgress.expirationTime = newExpirationTime;
      workInProgress.memoizedState = resultState;
      {
        currentlyProcessingQueue = null;
      }
    }

    function callCallback(callback, context) {
      (function () {
        if (!(typeof callback === 'function')) {
          {
            throw ReactError(Error("Invalid argument passed as callback. Expected a function. Instead received: " + callback));
          }
        }
      })();

      callback.call(context);
    }

    function resetHasForceUpdateBeforeProcessing() {
      hasForceUpdate = false;
    }

    function checkHasForceUpdateAfterProcessing() {
      return hasForceUpdate;
    }

    function commitUpdateQueue(finishedWork, finishedQueue, instance, renderExpirationTime) {
      // If the finished render included captured updates, and there are still
      // lower priority updates left over, we need to keep the captured updates
      // in the queue so that they are rebased and not dropped once we process the
      // queue again at the lower priority.
      if (finishedQueue.firstCapturedUpdate !== null) {
        // Join the captured update list to the end of the normal list.
        if (finishedQueue.lastUpdate !== null) {
          finishedQueue.lastUpdate.next = finishedQueue.firstCapturedUpdate;
          finishedQueue.lastUpdate = finishedQueue.lastCapturedUpdate;
        } // Clear the list of captured updates.


        finishedQueue.firstCapturedUpdate = finishedQueue.lastCapturedUpdate = null;
      } // Commit the effects


      commitUpdateEffects(finishedQueue.firstEffect, instance);
      finishedQueue.firstEffect = finishedQueue.lastEffect = null;
      commitUpdateEffects(finishedQueue.firstCapturedEffect, instance);
      finishedQueue.firstCapturedEffect = finishedQueue.lastCapturedEffect = null;
    }

    function commitUpdateEffects(effect, instance) {
      while (effect !== null) {
        var callback = effect.callback;

        if (callback !== null) {
          effect.callback = null;
          callCallback(callback, instance);
        }

        effect = effect.nextEffect;
      }
    }

    var ReactCurrentBatchConfig = ReactSharedInternals.ReactCurrentBatchConfig;

    function requestCurrentSuspenseConfig() {
      return ReactCurrentBatchConfig.suspense;
    }

    var fakeInternalInstance = {};
    var isArray$1 = Array.isArray; // React.Component uses a shared frozen object by default.
    // We'll use it to determine whether we need to initialize legacy refs.

    var emptyRefsObject = new React.Component().refs;
    var didWarnAboutStateAssignmentForComponent;
    var didWarnAboutUninitializedState;
    var didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate;
    var didWarnAboutLegacyLifecyclesAndDerivedState;
    var didWarnAboutUndefinedDerivedState;
    var warnOnUndefinedDerivedState;
    var warnOnInvalidCallback$1;
    var didWarnAboutDirectlyAssigningPropsToState;
    var didWarnAboutContextTypeAndContextTypes;
    var didWarnAboutInvalidateContextType;
    {
      didWarnAboutStateAssignmentForComponent = new Set();
      didWarnAboutUninitializedState = new Set();
      didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate = new Set();
      didWarnAboutLegacyLifecyclesAndDerivedState = new Set();
      didWarnAboutDirectlyAssigningPropsToState = new Set();
      didWarnAboutUndefinedDerivedState = new Set();
      didWarnAboutContextTypeAndContextTypes = new Set();
      didWarnAboutInvalidateContextType = new Set();
      var didWarnOnInvalidCallback = new Set();

      warnOnInvalidCallback$1 = function (callback, callerName) {
        if (callback === null || typeof callback === 'function') {
          return;
        }

        var key = callerName + "_" + callback;

        if (!didWarnOnInvalidCallback.has(key)) {
          didWarnOnInvalidCallback.add(key);
          warningWithoutStack$1(false, '%s(...): Expected the last optional `callback` argument to be a ' + 'function. Instead received: %s.', callerName, callback);
        }
      };

      warnOnUndefinedDerivedState = function (type, partialState) {
        if (partialState === undefined) {
          var componentName = getComponentName(type) || 'Component';

          if (!didWarnAboutUndefinedDerivedState.has(componentName)) {
            didWarnAboutUndefinedDerivedState.add(componentName);
            warningWithoutStack$1(false, '%s.getDerivedStateFromProps(): A valid state object (or null) must be returned. ' + 'You have returned undefined.', componentName);
          }
        }
      }; // This is so gross but it's at least non-critical and can be removed if
      // it causes problems. This is meant to give a nicer error message for
      // ReactDOM15.unstable_renderSubtreeIntoContainer(reactDOM16Component,
      // ...)) which otherwise throws a "_processChildContext is not a function"
      // exception.


      Object.defineProperty(fakeInternalInstance, '_processChildContext', {
        enumerable: false,
        value: function () {
          (function () {
            {
              {
                throw ReactError(Error("_processChildContext is not available in React 16+. This likely means you have multiple copies of React and are attempting to nest a React 15 tree inside a React 16 tree using unstable_renderSubtreeIntoContainer, which isn't supported. Try to make sure you have only one copy of React (and ideally, switch to ReactDOM.createPortal)."));
              }
            }
          })();
        }
      });
      Object.freeze(fakeInternalInstance);
    }

    function applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, nextProps) {
      var prevState = workInProgress.memoizedState;
      {
        if (debugRenderPhaseSideEffects || debugRenderPhaseSideEffectsForStrictMode && workInProgress.mode & StrictMode) {
          // Invoke the function an extra time to help detect side-effects.
          getDerivedStateFromProps(nextProps, prevState);
        }
      }
      var partialState = getDerivedStateFromProps(nextProps, prevState);
      {
        warnOnUndefinedDerivedState(ctor, partialState);
      } // Merge the partial state and the previous state.

      var memoizedState = partialState === null || partialState === undefined ? prevState : _assign({}, prevState, partialState);
      workInProgress.memoizedState = memoizedState; // Once the update queue is empty, persist the derived state onto the
      // base state.

      var updateQueue = workInProgress.updateQueue;

      if (updateQueue !== null && workInProgress.expirationTime === NoWork) {
        updateQueue.baseState = memoizedState;
      }
    }

    var classComponentUpdater = {
      isMounted: isMounted,
      enqueueSetState: function (inst, payload, callback) {
        var fiber = get(inst);
        var currentTime = requestCurrentTime();
        var suspenseConfig = requestCurrentSuspenseConfig();
        var expirationTime = computeExpirationForFiber(currentTime, fiber, suspenseConfig);
        var update = createUpdate(expirationTime, suspenseConfig);
        update.payload = payload;

        if (callback !== undefined && callback !== null) {
          {
            warnOnInvalidCallback$1(callback, 'setState');
          }
          update.callback = callback;
        }

        enqueueUpdate(fiber, update);
        scheduleWork(fiber, expirationTime);
      },
      enqueueReplaceState: function (inst, payload, callback) {
        var fiber = get(inst);
        var currentTime = requestCurrentTime();
        var suspenseConfig = requestCurrentSuspenseConfig();
        var expirationTime = computeExpirationForFiber(currentTime, fiber, suspenseConfig);
        var update = createUpdate(expirationTime, suspenseConfig);
        update.tag = ReplaceState;
        update.payload = payload;

        if (callback !== undefined && callback !== null) {
          {
            warnOnInvalidCallback$1(callback, 'replaceState');
          }
          update.callback = callback;
        }

        enqueueUpdate(fiber, update);
        scheduleWork(fiber, expirationTime);
      },
      enqueueForceUpdate: function (inst, callback) {
        var fiber = get(inst);
        var currentTime = requestCurrentTime();
        var suspenseConfig = requestCurrentSuspenseConfig();
        var expirationTime = computeExpirationForFiber(currentTime, fiber, suspenseConfig);
        var update = createUpdate(expirationTime, suspenseConfig);
        update.tag = ForceUpdate;

        if (callback !== undefined && callback !== null) {
          {
            warnOnInvalidCallback$1(callback, 'forceUpdate');
          }
          update.callback = callback;
        }

        enqueueUpdate(fiber, update);
        scheduleWork(fiber, expirationTime);
      }
    };

    function checkShouldComponentUpdate(workInProgress, ctor, oldProps, newProps, oldState, newState, nextContext) {
      var instance = workInProgress.stateNode;

      if (typeof instance.shouldComponentUpdate === 'function') {
        startPhaseTimer(workInProgress, 'shouldComponentUpdate');
        var shouldUpdate = instance.shouldComponentUpdate(newProps, newState, nextContext);
        stopPhaseTimer();
        {
          !(shouldUpdate !== undefined) ? warningWithoutStack$1(false, '%s.shouldComponentUpdate(): Returned undefined instead of a ' + 'boolean value. Make sure to return true or false.', getComponentName(ctor) || 'Component') : void 0;
        }
        return shouldUpdate;
      }

      if (ctor.prototype && ctor.prototype.isPureReactComponent) {
        return !shallowEqual(oldProps, newProps) || !shallowEqual(oldState, newState);
      }

      return true;
    }

    function checkClassInstance(workInProgress, ctor, newProps) {
      var instance = workInProgress.stateNode;
      {
        var name = getComponentName(ctor) || 'Component';
        var renderPresent = instance.render;

        if (!renderPresent) {
          if (ctor.prototype && typeof ctor.prototype.render === 'function') {
            warningWithoutStack$1(false, '%s(...): No `render` method found on the returned component ' + 'instance: did you accidentally return an object from the constructor?', name);
          } else {
            warningWithoutStack$1(false, '%s(...): No `render` method found on the returned component ' + 'instance: you may have forgotten to define `render`.', name);
          }
        }

        var noGetInitialStateOnES6 = !instance.getInitialState || instance.getInitialState.isReactClassApproved || instance.state;
        !noGetInitialStateOnES6 ? warningWithoutStack$1(false, 'getInitialState was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Did you mean to define a state property instead?', name) : void 0;
        var noGetDefaultPropsOnES6 = !instance.getDefaultProps || instance.getDefaultProps.isReactClassApproved;
        !noGetDefaultPropsOnES6 ? warningWithoutStack$1(false, 'getDefaultProps was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Use a static property to define defaultProps instead.', name) : void 0;
        var noInstancePropTypes = !instance.propTypes;
        !noInstancePropTypes ? warningWithoutStack$1(false, 'propTypes was defined as an instance property on %s. Use a static ' + 'property to define propTypes instead.', name) : void 0;
        var noInstanceContextType = !instance.contextType;
        !noInstanceContextType ? warningWithoutStack$1(false, 'contextType was defined as an instance property on %s. Use a static ' + 'property to define contextType instead.', name) : void 0;

        if (disableLegacyContext) {
          if (ctor.childContextTypes) {
            warningWithoutStack$1(false, '%s uses the legacy childContextTypes API which is no longer supported. ' + 'Use React.createContext() instead.', name);
          }

          if (ctor.contextTypes) {
            warningWithoutStack$1(false, '%s uses the legacy contextTypes API which is no longer supported. ' + 'Use React.createContext() with static contextType instead.', name);
          }
        } else {
          var noInstanceContextTypes = !instance.contextTypes;
          !noInstanceContextTypes ? warningWithoutStack$1(false, 'contextTypes was defined as an instance property on %s. Use a static ' + 'property to define contextTypes instead.', name) : void 0;

          if (ctor.contextType && ctor.contextTypes && !didWarnAboutContextTypeAndContextTypes.has(ctor)) {
            didWarnAboutContextTypeAndContextTypes.add(ctor);
            warningWithoutStack$1(false, '%s declares both contextTypes and contextType static properties. ' + 'The legacy contextTypes property will be ignored.', name);
          }
        }

        var noComponentShouldUpdate = typeof instance.componentShouldUpdate !== 'function';
        !noComponentShouldUpdate ? warningWithoutStack$1(false, '%s has a method called ' + 'componentShouldUpdate(). Did you mean shouldComponentUpdate()? ' + 'The name is phrased as a question because the function is ' + 'expected to return a value.', name) : void 0;

        if (ctor.prototype && ctor.prototype.isPureReactComponent && typeof instance.shouldComponentUpdate !== 'undefined') {
          warningWithoutStack$1(false, '%s has a method called shouldComponentUpdate(). ' + 'shouldComponentUpdate should not be used when extending React.PureComponent. ' + 'Please extend React.Component if shouldComponentUpdate is used.', getComponentName(ctor) || 'A pure component');
        }

        var noComponentDidUnmount = typeof instance.componentDidUnmount !== 'function';
        !noComponentDidUnmount ? warningWithoutStack$1(false, '%s has a method called ' + 'componentDidUnmount(). But there is no such lifecycle method. ' + 'Did you mean componentWillUnmount()?', name) : void 0;
        var noComponentDidReceiveProps = typeof instance.componentDidReceiveProps !== 'function';
        !noComponentDidReceiveProps ? warningWithoutStack$1(false, '%s has a method called ' + 'componentDidReceiveProps(). But there is no such lifecycle method. ' + 'If you meant to update the state in response to changing props, ' + 'use componentWillReceiveProps(). If you meant to fetch data or ' + 'run side-effects or mutations after React has updated the UI, use componentDidUpdate().', name) : void 0;
        var noComponentWillRecieveProps = typeof instance.componentWillRecieveProps !== 'function';
        !noComponentWillRecieveProps ? warningWithoutStack$1(false, '%s has a method called ' + 'componentWillRecieveProps(). Did you mean componentWillReceiveProps()?', name) : void 0;
        var noUnsafeComponentWillRecieveProps = typeof instance.UNSAFE_componentWillRecieveProps !== 'function';
        !noUnsafeComponentWillRecieveProps ? warningWithoutStack$1(false, '%s has a method called ' + 'UNSAFE_componentWillRecieveProps(). Did you mean UNSAFE_componentWillReceiveProps()?', name) : void 0;
        var hasMutatedProps = instance.props !== newProps;
        !(instance.props === undefined || !hasMutatedProps) ? warningWithoutStack$1(false, '%s(...): When calling super() in `%s`, make sure to pass ' + "up the same props that your component's constructor was passed.", name, name) : void 0;
        var noInstanceDefaultProps = !instance.defaultProps;
        !noInstanceDefaultProps ? warningWithoutStack$1(false, 'Setting defaultProps as an instance property on %s is not supported and will be ignored.' + ' Instead, define defaultProps as a static property on %s.', name, name) : void 0;

        if (typeof instance.getSnapshotBeforeUpdate === 'function' && typeof instance.componentDidUpdate !== 'function' && !didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.has(ctor)) {
          didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.add(ctor);
          warningWithoutStack$1(false, '%s: getSnapshotBeforeUpdate() should be used with componentDidUpdate(). ' + 'This component defines getSnapshotBeforeUpdate() only.', getComponentName(ctor));
        }

        var noInstanceGetDerivedStateFromProps = typeof instance.getDerivedStateFromProps !== 'function';
        !noInstanceGetDerivedStateFromProps ? warningWithoutStack$1(false, '%s: getDerivedStateFromProps() is defined as an instance method ' + 'and will be ignored. Instead, declare it as a static method.', name) : void 0;
        var noInstanceGetDerivedStateFromCatch = typeof instance.getDerivedStateFromError !== 'function';
        !noInstanceGetDerivedStateFromCatch ? warningWithoutStack$1(false, '%s: getDerivedStateFromError() is defined as an instance method ' + 'and will be ignored. Instead, declare it as a static method.', name) : void 0;
        var noStaticGetSnapshotBeforeUpdate = typeof ctor.getSnapshotBeforeUpdate !== 'function';
        !noStaticGetSnapshotBeforeUpdate ? warningWithoutStack$1(false, '%s: getSnapshotBeforeUpdate() is defined as a static method ' + 'and will be ignored. Instead, declare it as an instance method.', name) : void 0;
        var _state = instance.state;

        if (_state && (typeof _state !== 'object' || isArray$1(_state))) {
          warningWithoutStack$1(false, '%s.state: must be set to an object or null', name);
        }

        if (typeof instance.getChildContext === 'function') {
          !(typeof ctor.childContextTypes === 'object') ? warningWithoutStack$1(false, '%s.getChildContext(): childContextTypes must be defined in order to ' + 'use getChildContext().', name) : void 0;
        }
      }
    }

    function adoptClassInstance(workInProgress, instance) {
      instance.updater = classComponentUpdater;
      workInProgress.stateNode = instance; // The instance needs access to the fiber so that it can schedule updates

      set(instance, workInProgress);
      {
        instance._reactInternalInstance = fakeInternalInstance;
      }
    }

    function constructClassInstance(workInProgress, ctor, props, renderExpirationTime) {
      var isLegacyContextConsumer = false;
      var unmaskedContext = emptyContextObject;
      var context = emptyContextObject;
      var contextType = ctor.contextType;
      {
        if ('contextType' in ctor) {
          var isValid = // Allow null for conditional declaration
          contextType === null || contextType !== undefined && contextType.$$typeof === REACT_CONTEXT_TYPE && contextType._context === undefined; // Not a <Context.Consumer>

          if (!isValid && !didWarnAboutInvalidateContextType.has(ctor)) {
            didWarnAboutInvalidateContextType.add(ctor);
            var addendum = '';

            if (contextType === undefined) {
              addendum = ' However, it is set to undefined. ' + 'This can be caused by a typo or by mixing up named and default imports. ' + 'This can also happen due to a circular dependency, so ' + 'try moving the createContext() call to a separate file.';
            } else if (typeof contextType !== 'object') {
              addendum = ' However, it is set to a ' + typeof contextType + '.';
            } else if (contextType.$$typeof === REACT_PROVIDER_TYPE) {
              addendum = ' Did you accidentally pass the Context.Provider instead?';
            } else if (contextType._context !== undefined) {
              // <Context.Consumer>
              addendum = ' Did you accidentally pass the Context.Consumer instead?';
            } else {
              addendum = ' However, it is set to an object with keys {' + Object.keys(contextType).join(', ') + '}.';
            }

            warningWithoutStack$1(false, '%s defines an invalid contextType. ' + 'contextType should point to the Context object returned by React.createContext().%s', getComponentName(ctor) || 'Component', addendum);
          }
        }
      }

      if (typeof contextType === 'object' && contextType !== null) {
        context = readContext(contextType);
      } else if (!disableLegacyContext) {
        unmaskedContext = getUnmaskedContext(workInProgress, ctor, true);
        var contextTypes = ctor.contextTypes;
        isLegacyContextConsumer = contextTypes !== null && contextTypes !== undefined;
        context = isLegacyContextConsumer ? getMaskedContext(workInProgress, unmaskedContext) : emptyContextObject;
      } // Instantiate twice to help detect side-effects.


      {
        if (debugRenderPhaseSideEffects || debugRenderPhaseSideEffectsForStrictMode && workInProgress.mode & StrictMode) {
          new ctor(props, context); // eslint-disable-line no-new
        }
      }
      var instance = new ctor(props, context);
      var state = workInProgress.memoizedState = instance.state !== null && instance.state !== undefined ? instance.state : null;
      adoptClassInstance(workInProgress, instance);
      {
        if (typeof ctor.getDerivedStateFromProps === 'function' && state === null) {
          var componentName = getComponentName(ctor) || 'Component';

          if (!didWarnAboutUninitializedState.has(componentName)) {
            didWarnAboutUninitializedState.add(componentName);
            warningWithoutStack$1(false, '`%s` uses `getDerivedStateFromProps` but its initial state is ' + '%s. This is not recommended. Instead, define the initial state by ' + 'assigning an object to `this.state` in the constructor of `%s`. ' + 'This ensures that `getDerivedStateFromProps` arguments have a consistent shape.', componentName, instance.state === null ? 'null' : 'undefined', componentName);
          }
        } // If new component APIs are defined, "unsafe" lifecycles won't be called.
        // Warn about these lifecycles if they are present.
        // Don't warn about react-lifecycles-compat polyfilled methods though.


        if (typeof ctor.getDerivedStateFromProps === 'function' || typeof instance.getSnapshotBeforeUpdate === 'function') {
          var foundWillMountName = null;
          var foundWillReceivePropsName = null;
          var foundWillUpdateName = null;

          if (typeof instance.componentWillMount === 'function' && instance.componentWillMount.__suppressDeprecationWarning !== true) {
            foundWillMountName = 'componentWillMount';
          } else if (typeof instance.UNSAFE_componentWillMount === 'function') {
            foundWillMountName = 'UNSAFE_componentWillMount';
          }

          if (typeof instance.componentWillReceiveProps === 'function' && instance.componentWillReceiveProps.__suppressDeprecationWarning !== true) {
            foundWillReceivePropsName = 'componentWillReceiveProps';
          } else if (typeof instance.UNSAFE_componentWillReceiveProps === 'function') {
            foundWillReceivePropsName = 'UNSAFE_componentWillReceiveProps';
          }

          if (typeof instance.componentWillUpdate === 'function' && instance.componentWillUpdate.__suppressDeprecationWarning !== true) {
            foundWillUpdateName = 'componentWillUpdate';
          } else if (typeof instance.UNSAFE_componentWillUpdate === 'function') {
            foundWillUpdateName = 'UNSAFE_componentWillUpdate';
          }

          if (foundWillMountName !== null || foundWillReceivePropsName !== null || foundWillUpdateName !== null) {
            var _componentName = getComponentName(ctor) || 'Component';

            var newApiName = typeof ctor.getDerivedStateFromProps === 'function' ? 'getDerivedStateFromProps()' : 'getSnapshotBeforeUpdate()';

            if (!didWarnAboutLegacyLifecyclesAndDerivedState.has(_componentName)) {
              didWarnAboutLegacyLifecyclesAndDerivedState.add(_componentName);
              warningWithoutStack$1(false, 'Unsafe legacy lifecycles will not be called for components using new component APIs.\n\n' + '%s uses %s but also contains the following legacy lifecycles:%s%s%s\n\n' + 'The above lifecycles should be removed. Learn more about this warning here:\n' + 'https://fb.me/react-unsafe-component-lifecycles', _componentName, newApiName, foundWillMountName !== null ? "\n  " + foundWillMountName : '', foundWillReceivePropsName !== null ? "\n  " + foundWillReceivePropsName : '', foundWillUpdateName !== null ? "\n  " + foundWillUpdateName : '');
            }
          }
        }
      } // Cache unmasked context so we can avoid recreating masked context unless necessary.
      // ReactFiberContext usually updates this cache but can't for newly-created instances.

      if (isLegacyContextConsumer) {
        cacheContext(workInProgress, unmaskedContext, context);
      }

      return instance;
    }

    function callComponentWillMount(workInProgress, instance) {
      startPhaseTimer(workInProgress, 'componentWillMount');
      var oldState = instance.state;

      if (typeof instance.componentWillMount === 'function') {
        instance.componentWillMount();
      }

      if (typeof instance.UNSAFE_componentWillMount === 'function') {
        instance.UNSAFE_componentWillMount();
      }

      stopPhaseTimer();

      if (oldState !== instance.state) {
        {
          warningWithoutStack$1(false, '%s.componentWillMount(): Assigning directly to this.state is ' + "deprecated (except inside a component's " + 'constructor). Use setState instead.', getComponentName(workInProgress.type) || 'Component');
        }
        classComponentUpdater.enqueueReplaceState(instance, instance.state, null);
      }
    }

    function callComponentWillReceiveProps(workInProgress, instance, newProps, nextContext) {
      var oldState = instance.state;
      startPhaseTimer(workInProgress, 'componentWillReceiveProps');

      if (typeof instance.componentWillReceiveProps === 'function') {
        instance.componentWillReceiveProps(newProps, nextContext);
      }

      if (typeof instance.UNSAFE_componentWillReceiveProps === 'function') {
        instance.UNSAFE_componentWillReceiveProps(newProps, nextContext);
      }

      stopPhaseTimer();

      if (instance.state !== oldState) {
        {
          var componentName = getComponentName(workInProgress.type) || 'Component';

          if (!didWarnAboutStateAssignmentForComponent.has(componentName)) {
            didWarnAboutStateAssignmentForComponent.add(componentName);
            warningWithoutStack$1(false, '%s.componentWillReceiveProps(): Assigning directly to ' + "this.state is deprecated (except inside a component's " + 'constructor). Use setState instead.', componentName);
          }
        }
        classComponentUpdater.enqueueReplaceState(instance, instance.state, null);
      }
    } // Invokes the mount life-cycles on a previously never rendered instance.


    function mountClassInstance(workInProgress, ctor, newProps, renderExpirationTime) {
      {
        checkClassInstance(workInProgress, ctor, newProps);
      }
      var instance = workInProgress.stateNode;
      instance.props = newProps;
      instance.state = workInProgress.memoizedState;
      instance.refs = emptyRefsObject;
      var contextType = ctor.contextType;

      if (typeof contextType === 'object' && contextType !== null) {
        instance.context = readContext(contextType);
      } else if (disableLegacyContext) {
        instance.context = emptyContextObject;
      } else {
        var unmaskedContext = getUnmaskedContext(workInProgress, ctor, true);
        instance.context = getMaskedContext(workInProgress, unmaskedContext);
      }

      {
        if (instance.state === newProps) {
          var componentName = getComponentName(ctor) || 'Component';

          if (!didWarnAboutDirectlyAssigningPropsToState.has(componentName)) {
            didWarnAboutDirectlyAssigningPropsToState.add(componentName);
            warningWithoutStack$1(false, '%s: It is not recommended to assign props directly to state ' + "because updates to props won't be reflected in state. " + 'In most cases, it is better to use props directly.', componentName);
          }
        }

        if (workInProgress.mode & StrictMode) {
          ReactStrictModeWarnings.recordLegacyContextWarning(workInProgress, instance);
        }

        if (warnAboutDeprecatedLifecycles) {
          ReactStrictModeWarnings.recordUnsafeLifecycleWarnings(workInProgress, instance);
        }
      }
      var updateQueue = workInProgress.updateQueue;

      if (updateQueue !== null) {
        processUpdateQueue(workInProgress, updateQueue, newProps, instance, renderExpirationTime);
        instance.state = workInProgress.memoizedState;
      }

      var getDerivedStateFromProps = ctor.getDerivedStateFromProps;

      if (typeof getDerivedStateFromProps === 'function') {
        applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, newProps);
        instance.state = workInProgress.memoizedState;
      } // In order to support react-lifecycles-compat polyfilled components,
      // Unsafe lifecycles should not be invoked for components using the new APIs.


      if (typeof ctor.getDerivedStateFromProps !== 'function' && typeof instance.getSnapshotBeforeUpdate !== 'function' && (typeof instance.UNSAFE_componentWillMount === 'function' || typeof instance.componentWillMount === 'function')) {
        callComponentWillMount(workInProgress, instance); // If we had additional state updates during this life-cycle, let's
        // process them now.

        updateQueue = workInProgress.updateQueue;

        if (updateQueue !== null) {
          processUpdateQueue(workInProgress, updateQueue, newProps, instance, renderExpirationTime);
          instance.state = workInProgress.memoizedState;
        }
      }

      if (typeof instance.componentDidMount === 'function') {
        workInProgress.effectTag |= Update;
      }
    }

    function resumeMountClassInstance(workInProgress, ctor, newProps, renderExpirationTime) {
      var instance = workInProgress.stateNode;
      var oldProps = workInProgress.memoizedProps;
      instance.props = oldProps;
      var oldContext = instance.context;
      var contextType = ctor.contextType;
      var nextContext = emptyContextObject;

      if (typeof contextType === 'object' && contextType !== null) {
        nextContext = readContext(contextType);
      } else if (!disableLegacyContext) {
        var nextLegacyUnmaskedContext = getUnmaskedContext(workInProgress, ctor, true);
        nextContext = getMaskedContext(workInProgress, nextLegacyUnmaskedContext);
      }

      var getDerivedStateFromProps = ctor.getDerivedStateFromProps;
      var hasNewLifecycles = typeof getDerivedStateFromProps === 'function' || typeof instance.getSnapshotBeforeUpdate === 'function'; // Note: During these life-cycles, instance.props/instance.state are what
      // ever the previously attempted to render - not the "current". However,
      // during componentDidUpdate we pass the "current" props.
      // In order to support react-lifecycles-compat polyfilled components,
      // Unsafe lifecycles should not be invoked for components using the new APIs.

      if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillReceiveProps === 'function' || typeof instance.componentWillReceiveProps === 'function')) {
        if (oldProps !== newProps || oldContext !== nextContext) {
          callComponentWillReceiveProps(workInProgress, instance, newProps, nextContext);
        }
      }

      resetHasForceUpdateBeforeProcessing();
      var oldState = workInProgress.memoizedState;
      var newState = instance.state = oldState;
      var updateQueue = workInProgress.updateQueue;

      if (updateQueue !== null) {
        processUpdateQueue(workInProgress, updateQueue, newProps, instance, renderExpirationTime);
        newState = workInProgress.memoizedState;
      }

      if (oldProps === newProps && oldState === newState && !hasContextChanged() && !checkHasForceUpdateAfterProcessing()) {
        // If an update was already in progress, we should schedule an Update
        // effect even though we're bailing out, so that cWU/cDU are called.
        if (typeof instance.componentDidMount === 'function') {
          workInProgress.effectTag |= Update;
        }

        return false;
      }

      if (typeof getDerivedStateFromProps === 'function') {
        applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, newProps);
        newState = workInProgress.memoizedState;
      }

      var shouldUpdate = checkHasForceUpdateAfterProcessing() || checkShouldComponentUpdate(workInProgress, ctor, oldProps, newProps, oldState, newState, nextContext);

      if (shouldUpdate) {
        // In order to support react-lifecycles-compat polyfilled components,
        // Unsafe lifecycles should not be invoked for components using the new APIs.
        if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillMount === 'function' || typeof instance.componentWillMount === 'function')) {
          startPhaseTimer(workInProgress, 'componentWillMount');

          if (typeof instance.componentWillMount === 'function') {
            instance.componentWillMount();
          }

          if (typeof instance.UNSAFE_componentWillMount === 'function') {
            instance.UNSAFE_componentWillMount();
          }

          stopPhaseTimer();
        }

        if (typeof instance.componentDidMount === 'function') {
          workInProgress.effectTag |= Update;
        }
      } else {
        // If an update was already in progress, we should schedule an Update
        // effect even though we're bailing out, so that cWU/cDU are called.
        if (typeof instance.componentDidMount === 'function') {
          workInProgress.effectTag |= Update;
        } // If shouldComponentUpdate returned false, we should still update the
        // memoized state to indicate that this work can be reused.


        workInProgress.memoizedProps = newProps;
        workInProgress.memoizedState = newState;
      } // Update the existing instance's state, props, and context pointers even
      // if shouldComponentUpdate returns false.


      instance.props = newProps;
      instance.state = newState;
      instance.context = nextContext;
      return shouldUpdate;
    } // Invokes the update life-cycles and returns false if it shouldn't rerender.


    function updateClassInstance(current, workInProgress, ctor, newProps, renderExpirationTime) {
      var instance = workInProgress.stateNode;
      var oldProps = workInProgress.memoizedProps;
      instance.props = workInProgress.type === workInProgress.elementType ? oldProps : resolveDefaultProps(workInProgress.type, oldProps);
      var oldContext = instance.context;
      var contextType = ctor.contextType;
      var nextContext = emptyContextObject;

      if (typeof contextType === 'object' && contextType !== null) {
        nextContext = readContext(contextType);
      } else if (!disableLegacyContext) {
        var nextUnmaskedContext = getUnmaskedContext(workInProgress, ctor, true);
        nextContext = getMaskedContext(workInProgress, nextUnmaskedContext);
      }

      var getDerivedStateFromProps = ctor.getDerivedStateFromProps;
      var hasNewLifecycles = typeof getDerivedStateFromProps === 'function' || typeof instance.getSnapshotBeforeUpdate === 'function'; // Note: During these life-cycles, instance.props/instance.state are what
      // ever the previously attempted to render - not the "current". However,
      // during componentDidUpdate we pass the "current" props.
      // In order to support react-lifecycles-compat polyfilled components,
      // Unsafe lifecycles should not be invoked for components using the new APIs.

      if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillReceiveProps === 'function' || typeof instance.componentWillReceiveProps === 'function')) {
        if (oldProps !== newProps || oldContext !== nextContext) {
          callComponentWillReceiveProps(workInProgress, instance, newProps, nextContext);
        }
      }

      resetHasForceUpdateBeforeProcessing();
      var oldState = workInProgress.memoizedState;
      var newState = instance.state = oldState;
      var updateQueue = workInProgress.updateQueue;

      if (updateQueue !== null) {
        processUpdateQueue(workInProgress, updateQueue, newProps, instance, renderExpirationTime);
        newState = workInProgress.memoizedState;
      }

      if (oldProps === newProps && oldState === newState && !hasContextChanged() && !checkHasForceUpdateAfterProcessing()) {
        // If an update was already in progress, we should schedule an Update
        // effect even though we're bailing out, so that cWU/cDU are called.
        if (typeof instance.componentDidUpdate === 'function') {
          if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
            workInProgress.effectTag |= Update;
          }
        }

        if (typeof instance.getSnapshotBeforeUpdate === 'function') {
          if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
            workInProgress.effectTag |= Snapshot;
          }
        }

        return false;
      }

      if (typeof getDerivedStateFromProps === 'function') {
        applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, newProps);
        newState = workInProgress.memoizedState;
      }

      var shouldUpdate = checkHasForceUpdateAfterProcessing() || checkShouldComponentUpdate(workInProgress, ctor, oldProps, newProps, oldState, newState, nextContext);

      if (shouldUpdate) {
        // In order to support react-lifecycles-compat polyfilled components,
        // Unsafe lifecycles should not be invoked for components using the new APIs.
        if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillUpdate === 'function' || typeof instance.componentWillUpdate === 'function')) {
          startPhaseTimer(workInProgress, 'componentWillUpdate');

          if (typeof instance.componentWillUpdate === 'function') {
            instance.componentWillUpdate(newProps, newState, nextContext);
          }

          if (typeof instance.UNSAFE_componentWillUpdate === 'function') {
            instance.UNSAFE_componentWillUpdate(newProps, newState, nextContext);
          }

          stopPhaseTimer();
        }

        if (typeof instance.componentDidUpdate === 'function') {
          workInProgress.effectTag |= Update;
        }

        if (typeof instance.getSnapshotBeforeUpdate === 'function') {
          workInProgress.effectTag |= Snapshot;
        }
      } else {
        // If an update was already in progress, we should schedule an Update
        // effect even though we're bailing out, so that cWU/cDU are called.
        if (typeof instance.componentDidUpdate === 'function') {
          if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
            workInProgress.effectTag |= Update;
          }
        }

        if (typeof instance.getSnapshotBeforeUpdate === 'function') {
          if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
            workInProgress.effectTag |= Snapshot;
          }
        } // If shouldComponentUpdate returned false, we should still update the
        // memoized props/state to indicate that this work can be reused.


        workInProgress.memoizedProps = newProps;
        workInProgress.memoizedState = newState;
      } // Update the existing instance's state, props, and context pointers even
      // if shouldComponentUpdate returns false.


      instance.props = newProps;
      instance.state = newState;
      instance.context = nextContext;
      return shouldUpdate;
    }

    var didWarnAboutMaps;
    var didWarnAboutGenerators;
    var didWarnAboutStringRefs;
    var ownerHasKeyUseWarning;
    var ownerHasFunctionTypeWarning;

    var warnForMissingKey = function (child) {};

    {
      didWarnAboutMaps = false;
      didWarnAboutGenerators = false;
      didWarnAboutStringRefs = {};
      /**
       * Warn if there's no key explicitly set on dynamic arrays of children or
       * object keys are not valid. This allows us to keep track of children between
       * updates.
       */

      ownerHasKeyUseWarning = {};
      ownerHasFunctionTypeWarning = {};

      warnForMissingKey = function (child) {
        if (child === null || typeof child !== 'object') {
          return;
        }

        if (!child._store || child._store.validated || child.key != null) {
          return;
        }

        (function () {
          if (!(typeof child._store === 'object')) {
            {
              throw ReactError(Error("React Component in warnForMissingKey should have a _store. This error is likely caused by a bug in React. Please file an issue."));
            }
          }
        })();

        child._store.validated = true;
        var currentComponentErrorInfo = 'Each child in a list should have a unique ' + '"key" prop. See https://fb.me/react-warning-keys for ' + 'more information.' + getCurrentFiberStackInDev();

        if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
          return;
        }

        ownerHasKeyUseWarning[currentComponentErrorInfo] = true;
        warning$1(false, 'Each child in a list should have a unique ' + '"key" prop. See https://fb.me/react-warning-keys for ' + 'more information.');
      };
    }
    var isArray = Array.isArray;

    function coerceRef(returnFiber, current$$1, element) {
      var mixedRef = element.ref;

      if (mixedRef !== null && typeof mixedRef !== 'function' && typeof mixedRef !== 'object') {
        {
          // TODO: Clean this up once we turn on the string ref warning for
          // everyone, because the strict mode case will no longer be relevant
          if (returnFiber.mode & StrictMode || warnAboutStringRefs) {
            var componentName = getComponentName(returnFiber.type) || 'Component';

            if (!didWarnAboutStringRefs[componentName]) {
              if (warnAboutStringRefs) {
                warningWithoutStack$1(false, 'Component "%s" contains the string ref "%s". Support for string refs ' + 'will be removed in a future major release. We recommend using ' + 'useRef() or createRef() instead. ' + 'Learn more about using refs safely here: ' + 'https://fb.me/react-strict-mode-string-ref%s', componentName, mixedRef, getStackByFiberInDevAndProd(returnFiber));
              } else {
                warningWithoutStack$1(false, 'A string ref, "%s", has been found within a strict mode tree. ' + 'String refs are a source of potential bugs and should be avoided. ' + 'We recommend using useRef() or createRef() instead. ' + 'Learn more about using refs safely here: ' + 'https://fb.me/react-strict-mode-string-ref%s', mixedRef, getStackByFiberInDevAndProd(returnFiber));
              }

              didWarnAboutStringRefs[componentName] = true;
            }
          }
        }

        if (element._owner) {
          var owner = element._owner;
          var inst;

          if (owner) {
            var ownerFiber = owner;

            (function () {
              if (!(ownerFiber.tag === ClassComponent)) {
                {
                  throw ReactError(Error("Function components cannot have refs. Did you mean to use React.forwardRef()?"));
                }
              }
            })();

            inst = ownerFiber.stateNode;
          }

          (function () {
            if (!inst) {
              {
                throw ReactError(Error("Missing owner for string ref " + mixedRef + ". This error is likely caused by a bug in React. Please file an issue."));
              }
            }
          })();

          var stringRef = '' + mixedRef; // Check if previous string ref matches new string ref

          if (current$$1 !== null && current$$1.ref !== null && typeof current$$1.ref === 'function' && current$$1.ref._stringRef === stringRef) {
            return current$$1.ref;
          }

          var ref = function (value) {
            var refs = inst.refs;

            if (refs === emptyRefsObject) {
              // This is a lazy pooled frozen object, so we need to initialize.
              refs = inst.refs = {};
            }

            if (value === null) {
              delete refs[stringRef];
            } else {
              refs[stringRef] = value;
            }
          };

          ref._stringRef = stringRef;
          return ref;
        } else {
          (function () {
            if (!(typeof mixedRef === 'string')) {
              {
                throw ReactError(Error("Expected ref to be a function, a string, an object returned by React.createRef(), or null."));
              }
            }
          })();

          (function () {
            if (!element._owner) {
              {
                throw ReactError(Error("Element ref was specified as a string (" + mixedRef + ") but no owner was set. This could happen for one of the following reasons:\n1. You may be adding a ref to a function component\n2. You may be adding a ref to a component that was not created inside a component's render method\n3. You have multiple copies of React loaded\nSee https://fb.me/react-refs-must-have-owner for more information."));
              }
            }
          })();
        }
      }

      return mixedRef;
    }

    function throwOnInvalidObjectType(returnFiber, newChild) {
      if (returnFiber.type !== 'textarea') {
        var addendum = '';
        {
          addendum = ' If you meant to render a collection of children, use an array ' + 'instead.' + getCurrentFiberStackInDev();
        }

        (function () {
          {
            {
              throw ReactError(Error("Objects are not valid as a React child (found: " + (Object.prototype.toString.call(newChild) === '[object Object]' ? 'object with keys {' + Object.keys(newChild).join(', ') + '}' : newChild) + ")." + addendum));
            }
          }
        })();
      }
    }

    function warnOnFunctionType() {
      var currentComponentErrorInfo = 'Functions are not valid as a React child. This may happen if ' + 'you return a Component instead of <Component /> from render. ' + 'Or maybe you meant to call this function rather than return it.' + getCurrentFiberStackInDev();

      if (ownerHasFunctionTypeWarning[currentComponentErrorInfo]) {
        return;
      }

      ownerHasFunctionTypeWarning[currentComponentErrorInfo] = true;
      warning$1(false, 'Functions are not valid as a React child. This may happen if ' + 'you return a Component instead of <Component /> from render. ' + 'Or maybe you meant to call this function rather than return it.');
    } // This wrapper function exists because I expect to clone the code in each path
    // to be able to optimize each path individually by branching early. This needs
    // a compiler or we can do it manually. Helpers that don't need this branching
    // live outside of this function.


    function ChildReconciler(shouldTrackSideEffects) {
      function deleteChild(returnFiber, childToDelete) {
        if (!shouldTrackSideEffects) {
          // Noop.
          return;
        } // Deletions are added in reversed order so we add it to the front.
        // At this point, the return fiber's effect list is empty except for
        // deletions, so we can just append the deletion to the list. The remaining
        // effects aren't added until the complete phase. Once we implement
        // resuming, this may not be true.


        var last = returnFiber.lastEffect;

        if (last !== null) {
          last.nextEffect = childToDelete;
          returnFiber.lastEffect = childToDelete;
        } else {
          returnFiber.firstEffect = returnFiber.lastEffect = childToDelete;
        }

        childToDelete.nextEffect = null;
        childToDelete.effectTag = Deletion;
      }

      function deleteRemainingChildren(returnFiber, currentFirstChild) {
        if (!shouldTrackSideEffects) {
          // Noop.
          return null;
        } // TODO: For the shouldClone case, this could be micro-optimized a bit by
        // assuming that after the first child we've already added everything.


        var childToDelete = currentFirstChild;

        while (childToDelete !== null) {
          deleteChild(returnFiber, childToDelete);
          childToDelete = childToDelete.sibling;
        }

        return null;
      }

      function mapRemainingChildren(returnFiber, currentFirstChild) {
        // Add the remaining children to a temporary map so that we can find them by
        // keys quickly. Implicit (null) keys get added to this set with their index
        // instead.
        var existingChildren = new Map();
        var existingChild = currentFirstChild;

        while (existingChild !== null) {
          if (existingChild.key !== null) {
            existingChildren.set(existingChild.key, existingChild);
          } else {
            existingChildren.set(existingChild.index, existingChild);
          }

          existingChild = existingChild.sibling;
        }

        return existingChildren;
      }

      function useFiber(fiber, pendingProps, expirationTime) {
        // We currently set sibling to null and index to 0 here because it is easy
        // to forget to do before returning it. E.g. for the single child case.
        var clone = createWorkInProgress(fiber, pendingProps, expirationTime);
        clone.index = 0;
        clone.sibling = null;
        return clone;
      }

      function placeChild(newFiber, lastPlacedIndex, newIndex) {
        newFiber.index = newIndex;

        if (!shouldTrackSideEffects) {
          // Noop.
          return lastPlacedIndex;
        }

        var current$$1 = newFiber.alternate;

        if (current$$1 !== null) {
          var oldIndex = current$$1.index;

          if (oldIndex < lastPlacedIndex) {
            // This is a move.
            newFiber.effectTag = Placement;
            return lastPlacedIndex;
          } else {
            // This item can stay in place.
            return oldIndex;
          }
        } else {
          // This is an insertion.
          newFiber.effectTag = Placement;
          return lastPlacedIndex;
        }
      }

      function placeSingleChild(newFiber) {
        // This is simpler for the single child case. We only need to do a
        // placement for inserting new children.
        if (shouldTrackSideEffects && newFiber.alternate === null) {
          newFiber.effectTag = Placement;
        }

        return newFiber;
      }

      function updateTextNode(returnFiber, current$$1, textContent, expirationTime) {
        if (current$$1 === null || current$$1.tag !== HostText) {
          // Insert
          var created = createFiberFromText(textContent, returnFiber.mode, expirationTime);
          created.return = returnFiber;
          return created;
        } else {
          // Update
          var existing = useFiber(current$$1, textContent, expirationTime);
          existing.return = returnFiber;
          return existing;
        }
      }

      function updateElement(returnFiber, current$$1, element, expirationTime) {
        if (current$$1 !== null && (current$$1.elementType === element.type || // Keep this check inline so it only runs on the false path:
        isCompatibleFamilyForHotReloading(current$$1, element))) {
          // Move based on index
          var existing = useFiber(current$$1, element.props, expirationTime);
          existing.ref = coerceRef(returnFiber, current$$1, element);
          existing.return = returnFiber;
          {
            existing._debugSource = element._source;
            existing._debugOwner = element._owner;
          }
          return existing;
        } else {
          // Insert
          var created = createFiberFromElement(element, returnFiber.mode, expirationTime);
          created.ref = coerceRef(returnFiber, current$$1, element);
          created.return = returnFiber;
          return created;
        }
      }

      function updatePortal(returnFiber, current$$1, portal, expirationTime) {
        if (current$$1 === null || current$$1.tag !== HostPortal || current$$1.stateNode.containerInfo !== portal.containerInfo || current$$1.stateNode.implementation !== portal.implementation) {
          // Insert
          var created = createFiberFromPortal(portal, returnFiber.mode, expirationTime);
          created.return = returnFiber;
          return created;
        } else {
          // Update
          var existing = useFiber(current$$1, portal.children || [], expirationTime);
          existing.return = returnFiber;
          return existing;
        }
      }

      function updateFragment(returnFiber, current$$1, fragment, expirationTime, key) {
        if (current$$1 === null || current$$1.tag !== Fragment) {
          // Insert
          var created = createFiberFromFragment(fragment, returnFiber.mode, expirationTime, key);
          created.return = returnFiber;
          return created;
        } else {
          // Update
          var existing = useFiber(current$$1, fragment, expirationTime);
          existing.return = returnFiber;
          return existing;
        }
      }

      function createChild(returnFiber, newChild, expirationTime) {
        if (typeof newChild === 'string' || typeof newChild === 'number') {
          // Text nodes don't have keys. If the previous node is implicitly keyed
          // we can continue to replace it without aborting even if it is not a text
          // node.
          var created = createFiberFromText('' + newChild, returnFiber.mode, expirationTime);
          created.return = returnFiber;
          return created;
        }

        if (typeof newChild === 'object' && newChild !== null) {
          switch (newChild.$$typeof) {
            case REACT_ELEMENT_TYPE:
              {
                var _created = createFiberFromElement(newChild, returnFiber.mode, expirationTime);

                _created.ref = coerceRef(returnFiber, null, newChild);
                _created.return = returnFiber;
                return _created;
              }

            case REACT_PORTAL_TYPE:
              {
                var _created2 = createFiberFromPortal(newChild, returnFiber.mode, expirationTime);

                _created2.return = returnFiber;
                return _created2;
              }
          }

          if (isArray(newChild) || getIteratorFn(newChild)) {
            var _created3 = createFiberFromFragment(newChild, returnFiber.mode, expirationTime, null);

            _created3.return = returnFiber;
            return _created3;
          }

          throwOnInvalidObjectType(returnFiber, newChild);
        }

        {
          if (typeof newChild === 'function') {
            warnOnFunctionType();
          }
        }
        return null;
      }

      function updateSlot(returnFiber, oldFiber, newChild, expirationTime) {
        // Update the fiber if the keys match, otherwise return null.
        var key = oldFiber !== null ? oldFiber.key : null;

        if (typeof newChild === 'string' || typeof newChild === 'number') {
          // Text nodes don't have keys. If the previous node is implicitly keyed
          // we can continue to replace it without aborting even if it is not a text
          // node.
          if (key !== null) {
            return null;
          }

          return updateTextNode(returnFiber, oldFiber, '' + newChild, expirationTime);
        }

        if (typeof newChild === 'object' && newChild !== null) {
          switch (newChild.$$typeof) {
            case REACT_ELEMENT_TYPE:
              {
                if (newChild.key === key) {
                  if (newChild.type === REACT_FRAGMENT_TYPE) {
                    return updateFragment(returnFiber, oldFiber, newChild.props.children, expirationTime, key);
                  }

                  return updateElement(returnFiber, oldFiber, newChild, expirationTime);
                } else {
                  return null;
                }
              }

            case REACT_PORTAL_TYPE:
              {
                if (newChild.key === key) {
                  return updatePortal(returnFiber, oldFiber, newChild, expirationTime);
                } else {
                  return null;
                }
              }
          }

          if (isArray(newChild) || getIteratorFn(newChild)) {
            if (key !== null) {
              return null;
            }

            return updateFragment(returnFiber, oldFiber, newChild, expirationTime, null);
          }

          throwOnInvalidObjectType(returnFiber, newChild);
        }

        {
          if (typeof newChild === 'function') {
            warnOnFunctionType();
          }
        }
        return null;
      }

      function updateFromMap(existingChildren, returnFiber, newIdx, newChild, expirationTime) {
        if (typeof newChild === 'string' || typeof newChild === 'number') {
          // Text nodes don't have keys, so we neither have to check the old nor
          // new node for the key. If both are text nodes, they match.
          var matchedFiber = existingChildren.get(newIdx) || null;
          return updateTextNode(returnFiber, matchedFiber, '' + newChild, expirationTime);
        }

        if (typeof newChild === 'object' && newChild !== null) {
          switch (newChild.$$typeof) {
            case REACT_ELEMENT_TYPE:
              {
                var _matchedFiber = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;

                if (newChild.type === REACT_FRAGMENT_TYPE) {
                  return updateFragment(returnFiber, _matchedFiber, newChild.props.children, expirationTime, newChild.key);
                }

                return updateElement(returnFiber, _matchedFiber, newChild, expirationTime);
              }

            case REACT_PORTAL_TYPE:
              {
                var _matchedFiber2 = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;

                return updatePortal(returnFiber, _matchedFiber2, newChild, expirationTime);
              }
          }

          if (isArray(newChild) || getIteratorFn(newChild)) {
            var _matchedFiber3 = existingChildren.get(newIdx) || null;

            return updateFragment(returnFiber, _matchedFiber3, newChild, expirationTime, null);
          }

          throwOnInvalidObjectType(returnFiber, newChild);
        }

        {
          if (typeof newChild === 'function') {
            warnOnFunctionType();
          }
        }
        return null;
      }
      /**
       * Warns if there is a duplicate or missing key
       */


      function warnOnInvalidKey(child, knownKeys) {
        {
          if (typeof child !== 'object' || child === null) {
            return knownKeys;
          }

          switch (child.$$typeof) {
            case REACT_ELEMENT_TYPE:
            case REACT_PORTAL_TYPE:
              warnForMissingKey(child);
              var key = child.key;

              if (typeof key !== 'string') {
                break;
              }

              if (knownKeys === null) {
                knownKeys = new Set();
                knownKeys.add(key);
                break;
              }

              if (!knownKeys.has(key)) {
                knownKeys.add(key);
                break;
              }

              warning$1(false, 'Encountered two children with the same key, `%s`. ' + 'Keys should be unique so that components maintain their identity ' + 'across updates. Non-unique keys may cause children to be ' + 'duplicated and/or omitted — the behavior is unsupported and ' + 'could change in a future version.', key);
              break;

            default:
              break;
          }
        }
        return knownKeys;
      }

      function reconcileChildrenArray(returnFiber, currentFirstChild, newChildren, expirationTime) {
        // This algorithm can't optimize by searching from both ends since we
        // don't have backpointers on fibers. I'm trying to see how far we can get
        // with that model. If it ends up not being worth the tradeoffs, we can
        // add it later.
        // Even with a two ended optimization, we'd want to optimize for the case
        // where there are few changes and brute force the comparison instead of
        // going for the Map. It'd like to explore hitting that path first in
        // forward-only mode and only go for the Map once we notice that we need
        // lots of look ahead. This doesn't handle reversal as well as two ended
        // search but that's unusual. Besides, for the two ended optimization to
        // work on Iterables, we'd need to copy the whole set.
        // In this first iteration, we'll just live with hitting the bad case
        // (adding everything to a Map) in for every insert/move.
        // If you change this code, also update reconcileChildrenIterator() which
        // uses the same algorithm.
        {
          // First, validate keys.
          var knownKeys = null;

          for (var i = 0; i < newChildren.length; i++) {
            var child = newChildren[i];
            knownKeys = warnOnInvalidKey(child, knownKeys);
          }
        }
        var resultingFirstChild = null;
        var previousNewFiber = null;
        var oldFiber = currentFirstChild;
        var lastPlacedIndex = 0;
        var newIdx = 0;
        var nextOldFiber = null;

        for (; oldFiber !== null && newIdx < newChildren.length; newIdx++) {
          if (oldFiber.index > newIdx) {
            nextOldFiber = oldFiber;
            oldFiber = null;
          } else {
            nextOldFiber = oldFiber.sibling;
          }

          var newFiber = updateSlot(returnFiber, oldFiber, newChildren[newIdx], expirationTime);

          if (newFiber === null) {
            // TODO: This breaks on empty slots like null children. That's
            // unfortunate because it triggers the slow path all the time. We need
            // a better way to communicate whether this was a miss or null,
            // boolean, undefined, etc.
            if (oldFiber === null) {
              oldFiber = nextOldFiber;
            }

            break;
          }

          if (shouldTrackSideEffects) {
            if (oldFiber && newFiber.alternate === null) {
              // We matched the slot, but we didn't reuse the existing fiber, so we
              // need to delete the existing child.
              deleteChild(returnFiber, oldFiber);
            }
          }

          lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);

          if (previousNewFiber === null) {
            // TODO: Move out of the loop. This only happens for the first run.
            resultingFirstChild = newFiber;
          } else {
            // TODO: Defer siblings if we're not at the right index for this slot.
            // I.e. if we had null values before, then we want to defer this
            // for each null value. However, we also don't want to call updateSlot
            // with the previous one.
            previousNewFiber.sibling = newFiber;
          }

          previousNewFiber = newFiber;
          oldFiber = nextOldFiber;
        }

        if (newIdx === newChildren.length) {
          // We've reached the end of the new children. We can delete the rest.
          deleteRemainingChildren(returnFiber, oldFiber);
          return resultingFirstChild;
        }

        if (oldFiber === null) {
          // If we don't have any more existing children we can choose a fast path
          // since the rest will all be insertions.
          for (; newIdx < newChildren.length; newIdx++) {
            var _newFiber = createChild(returnFiber, newChildren[newIdx], expirationTime);

            if (_newFiber === null) {
              continue;
            }

            lastPlacedIndex = placeChild(_newFiber, lastPlacedIndex, newIdx);

            if (previousNewFiber === null) {
              // TODO: Move out of the loop. This only happens for the first run.
              resultingFirstChild = _newFiber;
            } else {
              previousNewFiber.sibling = _newFiber;
            }

            previousNewFiber = _newFiber;
          }

          return resultingFirstChild;
        } // Add all children to a key map for quick lookups.


        var existingChildren = mapRemainingChildren(returnFiber, oldFiber); // Keep scanning and use the map to restore deleted items as moves.

        for (; newIdx < newChildren.length; newIdx++) {
          var _newFiber2 = updateFromMap(existingChildren, returnFiber, newIdx, newChildren[newIdx], expirationTime);

          if (_newFiber2 !== null) {
            if (shouldTrackSideEffects) {
              if (_newFiber2.alternate !== null) {
                // The new fiber is a work in progress, but if there exists a
                // current, that means that we reused the fiber. We need to delete
                // it from the child list so that we don't add it to the deletion
                // list.
                existingChildren.delete(_newFiber2.key === null ? newIdx : _newFiber2.key);
              }
            }

            lastPlacedIndex = placeChild(_newFiber2, lastPlacedIndex, newIdx);

            if (previousNewFiber === null) {
              resultingFirstChild = _newFiber2;
            } else {
              previousNewFiber.sibling = _newFiber2;
            }

            previousNewFiber = _newFiber2;
          }
        }

        if (shouldTrackSideEffects) {
          // Any existing children that weren't consumed above were deleted. We need
          // to add them to the deletion list.
          existingChildren.forEach(function (child) {
            return deleteChild(returnFiber, child);
          });
        }

        return resultingFirstChild;
      }

      function reconcileChildrenIterator(returnFiber, currentFirstChild, newChildrenIterable, expirationTime) {
        // This is the same implementation as reconcileChildrenArray(),
        // but using the iterator instead.
        var iteratorFn = getIteratorFn(newChildrenIterable);

        (function () {
          if (!(typeof iteratorFn === 'function')) {
            {
              throw ReactError(Error("An object is not an iterable. This error is likely caused by a bug in React. Please file an issue."));
            }
          }
        })();

        {
          // We don't support rendering Generators because it's a mutation.
          // See https://github.com/facebook/react/issues/12995
          if (typeof Symbol === 'function' && // $FlowFixMe Flow doesn't know about toStringTag
          newChildrenIterable[Symbol.toStringTag] === 'Generator') {
            !didWarnAboutGenerators ? warning$1(false, 'Using Generators as children is unsupported and will likely yield ' + 'unexpected results because enumerating a generator mutates it. ' + 'You may convert it to an array with `Array.from()` or the ' + '`[...spread]` operator before rendering. Keep in mind ' + 'you might need to polyfill these features for older browsers.') : void 0;
            didWarnAboutGenerators = true;
          } // Warn about using Maps as children


          if (newChildrenIterable.entries === iteratorFn) {
            !didWarnAboutMaps ? warning$1(false, 'Using Maps as children is unsupported and will likely yield ' + 'unexpected results. Convert it to a sequence/iterable of keyed ' + 'ReactElements instead.') : void 0;
            didWarnAboutMaps = true;
          } // First, validate keys.
          // We'll get a different iterator later for the main pass.


          var _newChildren = iteratorFn.call(newChildrenIterable);

          if (_newChildren) {
            var knownKeys = null;

            var _step = _newChildren.next();

            for (; !_step.done; _step = _newChildren.next()) {
              var child = _step.value;
              knownKeys = warnOnInvalidKey(child, knownKeys);
            }
          }
        }
        var newChildren = iteratorFn.call(newChildrenIterable);

        (function () {
          if (!(newChildren != null)) {
            {
              throw ReactError(Error("An iterable object provided no iterator."));
            }
          }
        })();

        var resultingFirstChild = null;
        var previousNewFiber = null;
        var oldFiber = currentFirstChild;
        var lastPlacedIndex = 0;
        var newIdx = 0;
        var nextOldFiber = null;
        var step = newChildren.next();

        for (; oldFiber !== null && !step.done; newIdx++, step = newChildren.next()) {
          if (oldFiber.index > newIdx) {
            nextOldFiber = oldFiber;
            oldFiber = null;
          } else {
            nextOldFiber = oldFiber.sibling;
          }

          var newFiber = updateSlot(returnFiber, oldFiber, step.value, expirationTime);

          if (newFiber === null) {
            // TODO: This breaks on empty slots like null children. That's
            // unfortunate because it triggers the slow path all the time. We need
            // a better way to communicate whether this was a miss or null,
            // boolean, undefined, etc.
            if (oldFiber === null) {
              oldFiber = nextOldFiber;
            }

            break;
          }

          if (shouldTrackSideEffects) {
            if (oldFiber && newFiber.alternate === null) {
              // We matched the slot, but we didn't reuse the existing fiber, so we
              // need to delete the existing child.
              deleteChild(returnFiber, oldFiber);
            }
          }

          lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);

          if (previousNewFiber === null) {
            // TODO: Move out of the loop. This only happens for the first run.
            resultingFirstChild = newFiber;
          } else {
            // TODO: Defer siblings if we're not at the right index for this slot.
            // I.e. if we had null values before, then we want to defer this
            // for each null value. However, we also don't want to call updateSlot
            // with the previous one.
            previousNewFiber.sibling = newFiber;
          }

          previousNewFiber = newFiber;
          oldFiber = nextOldFiber;
        }

        if (step.done) {
          // We've reached the end of the new children. We can delete the rest.
          deleteRemainingChildren(returnFiber, oldFiber);
          return resultingFirstChild;
        }

        if (oldFiber === null) {
          // If we don't have any more existing children we can choose a fast path
          // since the rest will all be insertions.
          for (; !step.done; newIdx++, step = newChildren.next()) {
            var _newFiber3 = createChild(returnFiber, step.value, expirationTime);

            if (_newFiber3 === null) {
              continue;
            }

            lastPlacedIndex = placeChild(_newFiber3, lastPlacedIndex, newIdx);

            if (previousNewFiber === null) {
              // TODO: Move out of the loop. This only happens for the first run.
              resultingFirstChild = _newFiber3;
            } else {
              previousNewFiber.sibling = _newFiber3;
            }

            previousNewFiber = _newFiber3;
          }

          return resultingFirstChild;
        } // Add all children to a key map for quick lookups.


        var existingChildren = mapRemainingChildren(returnFiber, oldFiber); // Keep scanning and use the map to restore deleted items as moves.

        for (; !step.done; newIdx++, step = newChildren.next()) {
          var _newFiber4 = updateFromMap(existingChildren, returnFiber, newIdx, step.value, expirationTime);

          if (_newFiber4 !== null) {
            if (shouldTrackSideEffects) {
              if (_newFiber4.alternate !== null) {
                // The new fiber is a work in progress, but if there exists a
                // current, that means that we reused the fiber. We need to delete
                // it from the child list so that we don't add it to the deletion
                // list.
                existingChildren.delete(_newFiber4.key === null ? newIdx : _newFiber4.key);
              }
            }

            lastPlacedIndex = placeChild(_newFiber4, lastPlacedIndex, newIdx);

            if (previousNewFiber === null) {
              resultingFirstChild = _newFiber4;
            } else {
              previousNewFiber.sibling = _newFiber4;
            }

            previousNewFiber = _newFiber4;
          }
        }

        if (shouldTrackSideEffects) {
          // Any existing children that weren't consumed above were deleted. We need
          // to add them to the deletion list.
          existingChildren.forEach(function (child) {
            return deleteChild(returnFiber, child);
          });
        }

        return resultingFirstChild;
      }

      function reconcileSingleTextNode(returnFiber, currentFirstChild, textContent, expirationTime) {
        // There's no need to check for keys on text nodes since we don't have a
        // way to define them.
        if (currentFirstChild !== null && currentFirstChild.tag === HostText) {
          // We already have an existing node so let's just update it and delete
          // the rest.
          deleteRemainingChildren(returnFiber, currentFirstChild.sibling);
          var existing = useFiber(currentFirstChild, textContent, expirationTime);
          existing.return = returnFiber;
          return existing;
        } // The existing first child is not a text node so we need to create one
        // and delete the existing ones.


        deleteRemainingChildren(returnFiber, currentFirstChild);
        var created = createFiberFromText(textContent, returnFiber.mode, expirationTime);
        created.return = returnFiber;
        return created;
      }

      function reconcileSingleElement(returnFiber, currentFirstChild, element, expirationTime) {
        var key = element.key;
        var child = currentFirstChild;

        while (child !== null) {
          // TODO: If key === null and child.key === null, then this only applies to
          // the first item in the list.
          if (child.key === key) {
            if (child.tag === Fragment ? element.type === REACT_FRAGMENT_TYPE : child.elementType === element.type || // Keep this check inline so it only runs on the false path:
            isCompatibleFamilyForHotReloading(child, element)) {
              deleteRemainingChildren(returnFiber, child.sibling);
              var existing = useFiber(child, element.type === REACT_FRAGMENT_TYPE ? element.props.children : element.props, expirationTime);
              existing.ref = coerceRef(returnFiber, child, element);
              existing.return = returnFiber;
              {
                existing._debugSource = element._source;
                existing._debugOwner = element._owner;
              }
              return existing;
            } else {
              deleteRemainingChildren(returnFiber, child);
              break;
            }
          } else {
            deleteChild(returnFiber, child);
          }

          child = child.sibling;
        }

        if (element.type === REACT_FRAGMENT_TYPE) {
          var created = createFiberFromFragment(element.props.children, returnFiber.mode, expirationTime, element.key);
          created.return = returnFiber;
          return created;
        } else {
          var _created4 = createFiberFromElement(element, returnFiber.mode, expirationTime);

          _created4.ref = coerceRef(returnFiber, currentFirstChild, element);
          _created4.return = returnFiber;
          return _created4;
        }
      }

      function reconcileSinglePortal(returnFiber, currentFirstChild, portal, expirationTime) {
        var key = portal.key;
        var child = currentFirstChild;

        while (child !== null) {
          // TODO: If key === null and child.key === null, then this only applies to
          // the first item in the list.
          if (child.key === key) {
            if (child.tag === HostPortal && child.stateNode.containerInfo === portal.containerInfo && child.stateNode.implementation === portal.implementation) {
              deleteRemainingChildren(returnFiber, child.sibling);
              var existing = useFiber(child, portal.children || [], expirationTime);
              existing.return = returnFiber;
              return existing;
            } else {
              deleteRemainingChildren(returnFiber, child);
              break;
            }
          } else {
            deleteChild(returnFiber, child);
          }

          child = child.sibling;
        }

        var created = createFiberFromPortal(portal, returnFiber.mode, expirationTime);
        created.return = returnFiber;
        return created;
      } // This API will tag the children with the side-effect of the reconciliation
      // itself. They will be added to the side-effect list as we pass through the
      // children and the parent.


      function reconcileChildFibers(returnFiber, currentFirstChild, newChild, expirationTime) {
        // This function is not recursive.
        // If the top level item is an array, we treat it as a set of children,
        // not as a fragment. Nested arrays on the other hand will be treated as
        // fragment nodes. Recursion happens at the normal flow.
        // Handle top level unkeyed fragments as if they were arrays.
        // This leads to an ambiguity between <>{[...]}</> and <>...</>.
        // We treat the ambiguous cases above the same.
        var isUnkeyedTopLevelFragment = typeof newChild === 'object' && newChild !== null && newChild.type === REACT_FRAGMENT_TYPE && newChild.key === null;

        if (isUnkeyedTopLevelFragment) {
          newChild = newChild.props.children;
        } // Handle object types


        var isObject = typeof newChild === 'object' && newChild !== null;

        if (isObject) {
          switch (newChild.$$typeof) {
            case REACT_ELEMENT_TYPE:
              return placeSingleChild(reconcileSingleElement(returnFiber, currentFirstChild, newChild, expirationTime));

            case REACT_PORTAL_TYPE:
              return placeSingleChild(reconcileSinglePortal(returnFiber, currentFirstChild, newChild, expirationTime));
          }
        }

        if (typeof newChild === 'string' || typeof newChild === 'number') {
          return placeSingleChild(reconcileSingleTextNode(returnFiber, currentFirstChild, '' + newChild, expirationTime));
        }

        if (isArray(newChild)) {
          return reconcileChildrenArray(returnFiber, currentFirstChild, newChild, expirationTime);
        }

        if (getIteratorFn(newChild)) {
          return reconcileChildrenIterator(returnFiber, currentFirstChild, newChild, expirationTime);
        }

        if (isObject) {
          throwOnInvalidObjectType(returnFiber, newChild);
        }

        {
          if (typeof newChild === 'function') {
            warnOnFunctionType();
          }
        }

        if (typeof newChild === 'undefined' && !isUnkeyedTopLevelFragment) {
          // If the new child is undefined, and the return fiber is a composite
          // component, throw an error. If Fiber return types are disabled,
          // we already threw above.
          switch (returnFiber.tag) {
            case ClassComponent:
              {
                {
                  var instance = returnFiber.stateNode;

                  if (instance.render._isMockFunction) {
                    // We allow auto-mocks to proceed as if they're returning null.
                    break;
                  }
                }
              }
            // Intentionally fall through to the next case, which handles both
            // functions and classes
            // eslint-disable-next-lined no-fallthrough

            case FunctionComponent:
              {
                var Component = returnFiber.type;

                (function () {
                  {
                    {
                      throw ReactError(Error((Component.displayName || Component.name || 'Component') + "(...): Nothing was returned from render. This usually means a return statement is missing. Or, to render nothing, return null."));
                    }
                  }
                })();
              }
          }
        } // Remaining cases are all treated as empty.


        return deleteRemainingChildren(returnFiber, currentFirstChild);
      }

      return reconcileChildFibers;
    }

    var reconcileChildFibers = ChildReconciler(true);
    var mountChildFibers = ChildReconciler(false);

    function cloneChildFibers(current$$1, workInProgress) {
      (function () {
        if (!(current$$1 === null || workInProgress.child === current$$1.child)) {
          {
            throw ReactError(Error("Resuming work not yet implemented."));
          }
        }
      })();

      if (workInProgress.child === null) {
        return;
      }

      var currentChild = workInProgress.child;
      var newChild = createWorkInProgress(currentChild, currentChild.pendingProps, currentChild.expirationTime);
      workInProgress.child = newChild;
      newChild.return = workInProgress;

      while (currentChild.sibling !== null) {
        currentChild = currentChild.sibling;
        newChild = newChild.sibling = createWorkInProgress(currentChild, currentChild.pendingProps, currentChild.expirationTime);
        newChild.return = workInProgress;
      }

      newChild.sibling = null;
    } // Reset a workInProgress child set to prepare it for a second pass.


    function resetChildFibers(workInProgress, renderExpirationTime) {
      var child = workInProgress.child;

      while (child !== null) {
        resetWorkInProgress(child, renderExpirationTime);
        child = child.sibling;
      }
    }

    var NO_CONTEXT = {};
    var contextStackCursor$1 = createCursor(NO_CONTEXT);
    var contextFiberStackCursor = createCursor(NO_CONTEXT);
    var rootInstanceStackCursor = createCursor(NO_CONTEXT);

    function requiredContext(c) {
      (function () {
        if (!(c !== NO_CONTEXT)) {
          {
            throw ReactError(Error("Expected host context to exist. This error is likely caused by a bug in React. Please file an issue."));
          }
        }
      })();

      return c;
    }

    function getRootHostContainer() {
      var rootInstance = requiredContext(rootInstanceStackCursor.current);
      return rootInstance;
    }

    function pushHostContainer(fiber, nextRootInstance) {
      // Push current root instance onto the stack;
      // This allows us to reset root when portals are popped.
      push(rootInstanceStackCursor, nextRootInstance, fiber); // Track the context and the Fiber that provided it.
      // This enables us to pop only Fibers that provide unique contexts.

      push(contextFiberStackCursor, fiber, fiber); // Finally, we need to push the host context to the stack.
      // However, we can't just call getRootHostContext() and push it because
      // we'd have a different number of entries on the stack depending on
      // whether getRootHostContext() throws somewhere in renderer code or not.
      // So we push an empty value first. This lets us safely unwind on errors.

      push(contextStackCursor$1, NO_CONTEXT, fiber);
      var nextRootContext = getRootHostContext(nextRootInstance); // Now that we know this function doesn't throw, replace it.

      pop(contextStackCursor$1, fiber);
      push(contextStackCursor$1, nextRootContext, fiber);
    }

    function popHostContainer(fiber) {
      pop(contextStackCursor$1, fiber);
      pop(contextFiberStackCursor, fiber);
      pop(rootInstanceStackCursor, fiber);
    }

    function getHostContext() {
      var context = requiredContext(contextStackCursor$1.current);
      return context;
    }

    function pushHostContext(fiber) {
      var rootInstance = requiredContext(rootInstanceStackCursor.current);
      var context = requiredContext(contextStackCursor$1.current);
      var nextContext = getChildHostContext(context, fiber.type, rootInstance); // Don't push this Fiber's context unless it's unique.

      if (context === nextContext) {
        return;
      } // Track the context and the Fiber that provided it.
      // This enables us to pop only Fibers that provide unique contexts.


      push(contextFiberStackCursor, fiber, fiber);
      push(contextStackCursor$1, nextContext, fiber);
    }

    function popHostContext(fiber) {
      // Do not pop unless this Fiber provided the current context.
      // pushHostContext() only pushes Fibers that provide unique contexts.
      if (contextFiberStackCursor.current !== fiber) {
        return;
      }

      pop(contextStackCursor$1, fiber);
      pop(contextFiberStackCursor, fiber);
    }

    var DefaultSuspenseContext = 0; // The Suspense Context is split into two parts. The lower bits is
    // inherited deeply down the subtree. The upper bits only affect
    // this immediate suspense boundary and gets reset each new
    // boundary or suspense list.

    var SubtreeSuspenseContextMask = 1; // Subtree Flags:
    // InvisibleParentSuspenseContext indicates that one of our parent Suspense
    // boundaries is not currently showing visible main content.
    // Either because it is already showing a fallback or is not mounted at all.
    // We can use this to determine if it is desirable to trigger a fallback at
    // the parent. If not, then we might need to trigger undesirable boundaries
    // and/or suspend the commit to avoid hiding the parent content.

    var InvisibleParentSuspenseContext = 1; // Shallow Flags:
    // ForceSuspenseFallback can be used by SuspenseList to force newly added
    // items into their fallback state during one of the render passes.

    var ForceSuspenseFallback = 2;
    var suspenseStackCursor = createCursor(DefaultSuspenseContext);

    function hasSuspenseContext(parentContext, flag) {
      return (parentContext & flag) !== 0;
    }

    function setDefaultShallowSuspenseContext(parentContext) {
      return parentContext & SubtreeSuspenseContextMask;
    }

    function setShallowSuspenseContext(parentContext, shallowContext) {
      return parentContext & SubtreeSuspenseContextMask | shallowContext;
    }

    function addSubtreeSuspenseContext(parentContext, subtreeContext) {
      return parentContext | subtreeContext;
    }

    function pushSuspenseContext(fiber, newContext) {
      push(suspenseStackCursor, newContext, fiber);
    }

    function popSuspenseContext(fiber) {
      pop(suspenseStackCursor, fiber);
    }

    function shouldCaptureSuspense(workInProgress, hasInvisibleParent) {
      // If it was the primary children that just suspended, capture and render the
      // fallback. Otherwise, don't capture and bubble to the next boundary.
      var nextState = workInProgress.memoizedState;

      if (nextState !== null) {
        if (nextState.dehydrated !== null) {
          // A dehydrated boundary always captures.
          return true;
        }

        return false;
      }

      var props = workInProgress.memoizedProps; // In order to capture, the Suspense component must have a fallback prop.

      if (props.fallback === undefined) {
        return false;
      } // Regular boundaries always capture.


      if (props.unstable_avoidThisFallback !== true) {
        return true;
      } // If it's a boundary we should avoid, then we prefer to bubble up to the
      // parent boundary if it is currently invisible.


      if (hasInvisibleParent) {
        return false;
      } // If the parent is not able to handle it, we must handle it.


      return true;
    }

    function findFirstSuspended(row) {
      var node = row;

      while (node !== null) {
        if (node.tag === SuspenseComponent) {
          var state = node.memoizedState;

          if (state !== null) {
            var dehydrated = state.dehydrated;

            if (dehydrated === null || isSuspenseInstancePending(dehydrated) || isSuspenseInstanceFallback(dehydrated)) {
              return node;
            }
          }
        } else if (node.tag === SuspenseListComponent && // revealOrder undefined can't be trusted because it don't
        // keep track of whether it suspended or not.
        node.memoizedProps.revealOrder !== undefined) {
          var didSuspend = (node.effectTag & DidCapture) !== NoEffect;

          if (didSuspend) {
            return node;
          }
        } else if (node.child !== null) {
          node.child.return = node;
          node = node.child;
          continue;
        }

        if (node === row) {
          return null;
        }

        while (node.sibling === null) {
          if (node.return === null || node.return === row) {
            return null;
          }

          node = node.return;
        }

        node.sibling.return = node.return;
        node = node.sibling;
      }

      return null;
    }

    var emptyObject = {};
    var isArray$2 = Array.isArray;

    function createResponderInstance(responder, responderProps, responderState, fiber) {
      return {
        fiber: fiber,
        props: responderProps,
        responder: responder,
        rootEventTypes: null,
        state: responderState
      };
    }

    function mountEventResponder$1(responder, responderProps, fiber, respondersMap, rootContainerInstance) {
      var responderState = emptyObject;
      var getInitialState = responder.getInitialState;

      if (getInitialState !== null) {
        responderState = getInitialState(responderProps);
      }

      var responderInstance = createResponderInstance(responder, responderProps, responderState, fiber);

      if (!rootContainerInstance) {
        var node = fiber;

        while (node !== null) {
          var tag = node.tag;

          if (tag === HostComponent) {
            rootContainerInstance = node.stateNode;
            break;
          } else if (tag === HostRoot) {
            rootContainerInstance = node.stateNode.containerInfo;
            break;
          }

          node = node.return;
        }
      }

      mountResponderInstance(responder, responderInstance, responderProps, responderState, rootContainerInstance);
      respondersMap.set(responder, responderInstance);
    }

    function updateEventListener(listener, fiber, visistedResponders, respondersMap, rootContainerInstance) {
      var responder;
      var props;

      if (listener) {
        responder = listener.responder;
        props = listener.props;
      }

      (function () {
        if (!(responder && responder.$$typeof === REACT_RESPONDER_TYPE)) {
          {
            throw ReactError(Error("An invalid value was used as an event listener. Expect one or many event listeners created via React.unstable_useResponder()."));
          }
        }
      })();

      var listenerProps = props;

      if (visistedResponders.has(responder)) {
        // show warning
        {
          warning$1(false, 'Duplicate event responder "%s" found in event listeners. ' + 'Event listeners passed to elements cannot use the same event responder more than once.', responder.displayName);
        }
        return;
      }

      visistedResponders.add(responder);
      var responderInstance = respondersMap.get(responder);

      if (responderInstance === undefined) {
        // Mount (happens in either complete or commit phase)
        mountEventResponder$1(responder, listenerProps, fiber, respondersMap, rootContainerInstance);
      } else {
        // Update (happens during commit phase only)
        responderInstance.props = listenerProps;
        responderInstance.fiber = fiber;
      }
    }

    function updateEventListeners(listeners, fiber, rootContainerInstance) {
      var visistedResponders = new Set();
      var dependencies = fiber.dependencies;

      if (listeners != null) {
        if (dependencies === null) {
          dependencies = fiber.dependencies = {
            expirationTime: NoWork,
            firstContext: null,
            responders: new Map()
          };
        }

        var respondersMap = dependencies.responders;

        if (respondersMap === null) {
          respondersMap = new Map();
        }

        if (isArray$2(listeners)) {
          for (var i = 0, length = listeners.length; i < length; i++) {
            var listener = listeners[i];
            updateEventListener(listener, fiber, visistedResponders, respondersMap, rootContainerInstance);
          }
        } else {
          updateEventListener(listeners, fiber, visistedResponders, respondersMap, rootContainerInstance);
        }
      }

      if (dependencies !== null) {
        var _respondersMap = dependencies.responders;

        if (_respondersMap !== null) {
          // Unmount
          var mountedResponders = Array.from(_respondersMap.keys());

          for (var _i = 0, _length = mountedResponders.length; _i < _length; _i++) {
            var mountedResponder = mountedResponders[_i];

            if (!visistedResponders.has(mountedResponder)) {
              var responderInstance = _respondersMap.get(mountedResponder);

              unmountResponderInstance(responderInstance);

              _respondersMap.delete(mountedResponder);
            }
          }
        }
      }
    }

    function createResponderListener(responder, props) {
      var eventResponderListener = {
        responder: responder,
        props: props
      };
      {
        Object.freeze(eventResponderListener);
      }
      return eventResponderListener;
    }

    var NoEffect$1 =
    /*             */
    0;
    var UnmountSnapshot =
    /*      */
    2;
    var UnmountMutation =
    /*      */
    4;
    var MountMutation =
    /*        */
    8;
    var UnmountLayout =
    /*        */
    16;
    var MountLayout =
    /*          */
    32;
    var MountPassive =
    /*         */
    64;
    var UnmountPassive =
    /*       */
    128;
    var ReactCurrentDispatcher$1 = ReactSharedInternals.ReactCurrentDispatcher;
    var didWarnAboutMismatchedHooksForComponent;
    {
      didWarnAboutMismatchedHooksForComponent = new Set();
    } // These are set right before calling the component.

    var renderExpirationTime$1 = NoWork; // The work-in-progress fiber. I've named it differently to distinguish it from
    // the work-in-progress hook.

    var currentlyRenderingFiber$1 = null; // Hooks are stored as a linked list on the fiber's memoizedState field. The
    // current hook list is the list that belongs to the current fiber. The
    // work-in-progress hook list is a new list that will be added to the
    // work-in-progress fiber.

    var currentHook = null;
    var nextCurrentHook = null;
    var firstWorkInProgressHook = null;
    var workInProgressHook = null;
    var nextWorkInProgressHook = null;
    var remainingExpirationTime = NoWork;
    var componentUpdateQueue = null;
    var sideEffectTag = 0; // Updates scheduled during render will trigger an immediate re-render at the
    // end of the current pass. We can't store these updates on the normal queue,
    // because if the work is aborted, they should be discarded. Because this is
    // a relatively rare case, we also don't want to add an additional field to
    // either the hook or queue object types. So we store them in a lazily create
    // map of queue -> render-phase updates, which are discarded once the component
    // completes without re-rendering.
    // Whether an update was scheduled during the currently executing render pass.

    var didScheduleRenderPhaseUpdate = false; // Lazily created map of render-phase updates

    var renderPhaseUpdates = null; // Counter to prevent infinite loops.

    var numberOfReRenders = 0;
    var RE_RENDER_LIMIT = 25; // In DEV, this is the name of the currently executing primitive hook

    var currentHookNameInDev = null; // In DEV, this list ensures that hooks are called in the same order between renders.
    // The list stores the order of hooks used during the initial render (mount).
    // Subsequent renders (updates) reference this list.

    var hookTypesDev = null;
    var hookTypesUpdateIndexDev = -1; // In DEV, this tracks whether currently rendering component needs to ignore
    // the dependencies for Hooks that need them (e.g. useEffect or useMemo).
    // When true, such Hooks will always be "remounted". Only used during hot reload.

    var ignorePreviousDependencies = false;

    function mountHookTypesDev() {
      {
        var hookName = currentHookNameInDev;

        if (hookTypesDev === null) {
          hookTypesDev = [hookName];
        } else {
          hookTypesDev.push(hookName);
        }
      }
    }

    function updateHookTypesDev() {
      {
        var hookName = currentHookNameInDev;

        if (hookTypesDev !== null) {
          hookTypesUpdateIndexDev++;

          if (hookTypesDev[hookTypesUpdateIndexDev] !== hookName) {
            warnOnHookMismatchInDev(hookName);
          }
        }
      }
    }

    function checkDepsAreArrayDev(deps) {
      {
        if (deps !== undefined && deps !== null && !Array.isArray(deps)) {
          // Verify deps, but only on mount to avoid extra checks.
          // It's unlikely their type would change as usually you define them inline.
          warning$1(false, '%s received a final argument that is not an array (instead, received `%s`). When ' + 'specified, the final argument must be an array.', currentHookNameInDev, typeof deps);
        }
      }
    }

    function warnOnHookMismatchInDev(currentHookName) {
      {
        var componentName = getComponentName(currentlyRenderingFiber$1.type);

        if (!didWarnAboutMismatchedHooksForComponent.has(componentName)) {
          didWarnAboutMismatchedHooksForComponent.add(componentName);

          if (hookTypesDev !== null) {
            var table = '';
            var secondColumnStart = 30;

            for (var i = 0; i <= hookTypesUpdateIndexDev; i++) {
              var oldHookName = hookTypesDev[i];
              var newHookName = i === hookTypesUpdateIndexDev ? currentHookName : oldHookName;
              var row = i + 1 + ". " + oldHookName; // Extra space so second column lines up
              // lol @ IE not supporting String#repeat

              while (row.length < secondColumnStart) {
                row += ' ';
              }

              row += newHookName + '\n';
              table += row;
            }

            warning$1(false, 'React has detected a change in the order of Hooks called by %s. ' + 'This will lead to bugs and errors if not fixed. ' + 'For more information, read the Rules of Hooks: https://fb.me/rules-of-hooks\n\n' + '   Previous render            Next render\n' + '   ------------------------------------------------------\n' + '%s' + '   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n', componentName, table);
          }
        }
      }
    }

    function throwInvalidHookError() {
      (function () {
        {
          {
            throw ReactError(Error("Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://fb.me/react-invalid-hook-call for tips about how to debug and fix this problem."));
          }
        }
      })();
    }

    function areHookInputsEqual(nextDeps, prevDeps) {
      {
        if (ignorePreviousDependencies) {
          // Only true when this component is being hot reloaded.
          return false;
        }
      }

      if (prevDeps === null) {
        {
          warning$1(false, '%s received a final argument during this render, but not during ' + 'the previous render. Even though the final argument is optional, ' + 'its type cannot change between renders.', currentHookNameInDev);
        }
        return false;
      }

      {
        // Don't bother comparing lengths in prod because these arrays should be
        // passed inline.
        if (nextDeps.length !== prevDeps.length) {
          warning$1(false, 'The final argument passed to %s changed size between renders. The ' + 'order and size of this array must remain constant.\n\n' + 'Previous: %s\n' + 'Incoming: %s', currentHookNameInDev, "[" + prevDeps.join(', ') + "]", "[" + nextDeps.join(', ') + "]");
        }
      }

      for (var i = 0; i < prevDeps.length && i < nextDeps.length; i++) {
        if (is$1(nextDeps[i], prevDeps[i])) {
          continue;
        }

        return false;
      }

      return true;
    }

    function renderWithHooks(current, workInProgress, Component, props, refOrContext, nextRenderExpirationTime) {
      renderExpirationTime$1 = nextRenderExpirationTime;
      currentlyRenderingFiber$1 = workInProgress;
      nextCurrentHook = current !== null ? current.memoizedState : null;
      {
        hookTypesDev = current !== null ? current._debugHookTypes : null;
        hookTypesUpdateIndexDev = -1; // Used for hot reloading:

        ignorePreviousDependencies = current !== null && current.type !== workInProgress.type;
      } // The following should have already been reset
      // currentHook = null;
      // workInProgressHook = null;
      // remainingExpirationTime = NoWork;
      // componentUpdateQueue = null;
      // didScheduleRenderPhaseUpdate = false;
      // renderPhaseUpdates = null;
      // numberOfReRenders = 0;
      // sideEffectTag = 0;
      // TODO Warn if no hooks are used at all during mount, then some are used during update.
      // Currently we will identify the update render as a mount because nextCurrentHook === null.
      // This is tricky because it's valid for certain types of components (e.g. React.lazy)
      // Using nextCurrentHook to differentiate between mount/update only works if at least one stateful hook is used.
      // Non-stateful hooks (e.g. context) don't get added to memoizedState,
      // so nextCurrentHook would be null during updates and mounts.

      {
        if (nextCurrentHook !== null) {
          ReactCurrentDispatcher$1.current = HooksDispatcherOnUpdateInDEV;
        } else if (hookTypesDev !== null) {
          // This dispatcher handles an edge case where a component is updating,
          // but no stateful hooks have been used.
          // We want to match the production code behavior (which will use HooksDispatcherOnMount),
          // but with the extra DEV validation to ensure hooks ordering hasn't changed.
          // This dispatcher does that.
          ReactCurrentDispatcher$1.current = HooksDispatcherOnMountWithHookTypesInDEV;
        } else {
          ReactCurrentDispatcher$1.current = HooksDispatcherOnMountInDEV;
        }
      }
      var children = Component(props, refOrContext);

      if (didScheduleRenderPhaseUpdate) {
        do {
          didScheduleRenderPhaseUpdate = false;
          numberOfReRenders += 1;
          {
            // Even when hot reloading, allow dependencies to stabilize
            // after first render to prevent infinite render phase updates.
            ignorePreviousDependencies = false;
          } // Start over from the beginning of the list

          nextCurrentHook = current !== null ? current.memoizedState : null;
          nextWorkInProgressHook = firstWorkInProgressHook;
          currentHook = null;
          workInProgressHook = null;
          componentUpdateQueue = null;
          {
            // Also validate hook order for cascading updates.
            hookTypesUpdateIndexDev = -1;
          }
          ReactCurrentDispatcher$1.current = HooksDispatcherOnUpdateInDEV;
          children = Component(props, refOrContext);
        } while (didScheduleRenderPhaseUpdate);

        renderPhaseUpdates = null;
        numberOfReRenders = 0;
      } // We can assume the previous dispatcher is always this one, since we set it
      // at the beginning of the render phase and there's no re-entrancy.


      ReactCurrentDispatcher$1.current = ContextOnlyDispatcher;
      var renderedWork = currentlyRenderingFiber$1;
      renderedWork.memoizedState = firstWorkInProgressHook;
      renderedWork.expirationTime = remainingExpirationTime;
      renderedWork.updateQueue = componentUpdateQueue;
      renderedWork.effectTag |= sideEffectTag;
      {
        renderedWork._debugHookTypes = hookTypesDev;
      } // This check uses currentHook so that it works the same in DEV and prod bundles.
      // hookTypesDev could catch more cases (e.g. context) but only in DEV bundles.

      var didRenderTooFewHooks = currentHook !== null && currentHook.next !== null;
      renderExpirationTime$1 = NoWork;
      currentlyRenderingFiber$1 = null;
      currentHook = null;
      nextCurrentHook = null;
      firstWorkInProgressHook = null;
      workInProgressHook = null;
      nextWorkInProgressHook = null;
      {
        currentHookNameInDev = null;
        hookTypesDev = null;
        hookTypesUpdateIndexDev = -1;
      }
      remainingExpirationTime = NoWork;
      componentUpdateQueue = null;
      sideEffectTag = 0; // These were reset above
      // didScheduleRenderPhaseUpdate = false;
      // renderPhaseUpdates = null;
      // numberOfReRenders = 0;

      (function () {
        if (!!didRenderTooFewHooks) {
          {
            throw ReactError(Error("Rendered fewer hooks than expected. This may be caused by an accidental early return statement."));
          }
        }
      })();

      return children;
    }

    function bailoutHooks(current, workInProgress, expirationTime) {
      workInProgress.updateQueue = current.updateQueue;
      workInProgress.effectTag &= ~(Passive | Update);

      if (current.expirationTime <= expirationTime) {
        current.expirationTime = NoWork;
      }
    }

    function resetHooks() {
      // We can assume the previous dispatcher is always this one, since we set it
      // at the beginning of the render phase and there's no re-entrancy.
      ReactCurrentDispatcher$1.current = ContextOnlyDispatcher; // This is used to reset the state of this module when a component throws.
      // It's also called inside mountIndeterminateComponent if we determine the
      // component is a module-style component.

      renderExpirationTime$1 = NoWork;
      currentlyRenderingFiber$1 = null;
      currentHook = null;
      nextCurrentHook = null;
      firstWorkInProgressHook = null;
      workInProgressHook = null;
      nextWorkInProgressHook = null;
      {
        hookTypesDev = null;
        hookTypesUpdateIndexDev = -1;
        currentHookNameInDev = null;
      }
      remainingExpirationTime = NoWork;
      componentUpdateQueue = null;
      sideEffectTag = 0;
      didScheduleRenderPhaseUpdate = false;
      renderPhaseUpdates = null;
      numberOfReRenders = 0;
    }

    function mountWorkInProgressHook() {
      var hook = {
        memoizedState: null,
        baseState: null,
        queue: null,
        baseUpdate: null,
        next: null
      };

      if (workInProgressHook === null) {
        // This is the first hook in the list
        firstWorkInProgressHook = workInProgressHook = hook;
      } else {
        // Append to the end of the list
        workInProgressHook = workInProgressHook.next = hook;
      }

      return workInProgressHook;
    }

    function updateWorkInProgressHook() {
      // This function is used both for updates and for re-renders triggered by a
      // render phase update. It assumes there is either a current hook we can
      // clone, or a work-in-progress hook from a previous render pass that we can
      // use as a base. When we reach the end of the base list, we must switch to
      // the dispatcher used for mounts.
      if (nextWorkInProgressHook !== null) {
        // There's already a work-in-progress. Reuse it.
        workInProgressHook = nextWorkInProgressHook;
        nextWorkInProgressHook = workInProgressHook.next;
        currentHook = nextCurrentHook;
        nextCurrentHook = currentHook !== null ? currentHook.next : null;
      } else {
        // Clone from the current hook.
        (function () {
          if (!(nextCurrentHook !== null)) {
            {
              throw ReactError(Error("Rendered more hooks than during the previous render."));
            }
          }
        })();

        currentHook = nextCurrentHook;
        var newHook = {
          memoizedState: currentHook.memoizedState,
          baseState: currentHook.baseState,
          queue: currentHook.queue,
          baseUpdate: currentHook.baseUpdate,
          next: null
        };

        if (workInProgressHook === null) {
          // This is the first hook in the list.
          workInProgressHook = firstWorkInProgressHook = newHook;
        } else {
          // Append to the end of the list.
          workInProgressHook = workInProgressHook.next = newHook;
        }

        nextCurrentHook = currentHook.next;
      }

      return workInProgressHook;
    }

    function createFunctionComponentUpdateQueue() {
      return {
        lastEffect: null
      };
    }

    function basicStateReducer(state, action) {
      return typeof action === 'function' ? action(state) : action;
    }

    function mountReducer(reducer, initialArg, init) {
      var hook = mountWorkInProgressHook();
      var initialState;

      if (init !== undefined) {
        initialState = init(initialArg);
      } else {
        initialState = initialArg;
      }

      hook.memoizedState = hook.baseState = initialState;
      var queue = hook.queue = {
        last: null,
        dispatch: null,
        lastRenderedReducer: reducer,
        lastRenderedState: initialState
      };
      var dispatch = queue.dispatch = dispatchAction.bind(null, // Flow doesn't know this is non-null, but we do.
      currentlyRenderingFiber$1, queue);
      return [hook.memoizedState, dispatch];
    }

    function updateReducer(reducer, initialArg, init) {
      var hook = updateWorkInProgressHook();
      var queue = hook.queue;

      (function () {
        if (!(queue !== null)) {
          {
            throw ReactError(Error("Should have a queue. This is likely a bug in React. Please file an issue."));
          }
        }
      })();

      queue.lastRenderedReducer = reducer;

      if (numberOfReRenders > 0) {
        // This is a re-render. Apply the new render phase updates to the previous
        // work-in-progress hook.
        var _dispatch = queue.dispatch;

        if (renderPhaseUpdates !== null) {
          // Render phase updates are stored in a map of queue -> linked list
          var firstRenderPhaseUpdate = renderPhaseUpdates.get(queue);

          if (firstRenderPhaseUpdate !== undefined) {
            renderPhaseUpdates.delete(queue);
            var newState = hook.memoizedState;
            var update = firstRenderPhaseUpdate;

            do {
              // Process this render phase update. We don't have to check the
              // priority because it will always be the same as the current
              // render's.
              var action = update.action;
              newState = reducer(newState, action);
              update = update.next;
            } while (update !== null); // Mark that the fiber performed work, but only if the new state is
            // different from the current state.


            if (!is$1(newState, hook.memoizedState)) {
              markWorkInProgressReceivedUpdate();
            }

            hook.memoizedState = newState; // Don't persist the state accumulated from the render phase updates to
            // the base state unless the queue is empty.
            // TODO: Not sure if this is the desired semantics, but it's what we
            // do for gDSFP. I can't remember why.

            if (hook.baseUpdate === queue.last) {
              hook.baseState = newState;
            }

            queue.lastRenderedState = newState;
            return [newState, _dispatch];
          }
        }

        return [hook.memoizedState, _dispatch];
      } // The last update in the entire queue


      var last = queue.last; // The last update that is part of the base state.

      var baseUpdate = hook.baseUpdate;
      var baseState = hook.baseState; // Find the first unprocessed update.

      var first;

      if (baseUpdate !== null) {
        if (last !== null) {
          // For the first update, the queue is a circular linked list where
          // `queue.last.next = queue.first`. Once the first update commits, and
          // the `baseUpdate` is no longer empty, we can unravel the list.
          last.next = null;
        }

        first = baseUpdate.next;
      } else {
        first = last !== null ? last.next : null;
      }

      if (first !== null) {
        var _newState = baseState;
        var newBaseState = null;
        var newBaseUpdate = null;
        var prevUpdate = baseUpdate;
        var _update = first;
        var didSkip = false;

        do {
          var updateExpirationTime = _update.expirationTime;

          if (updateExpirationTime < renderExpirationTime$1) {
            // Priority is insufficient. Skip this update. If this is the first
            // skipped update, the previous update/state is the new base
            // update/state.
            if (!didSkip) {
              didSkip = true;
              newBaseUpdate = prevUpdate;
              newBaseState = _newState;
            } // Update the remaining priority in the queue.


            if (updateExpirationTime > remainingExpirationTime) {
              remainingExpirationTime = updateExpirationTime;
              markUnprocessedUpdateTime(remainingExpirationTime);
            }
          } else {
            // This update does have sufficient priority.
            // Mark the event time of this update as relevant to this render pass.
            // TODO: This should ideally use the true event time of this update rather than
            // its priority which is a derived and not reverseable value.
            // TODO: We should skip this update if it was already committed but currently
            // we have no way of detecting the difference between a committed and suspended
            // update here.
            markRenderEventTimeAndConfig(updateExpirationTime, _update.suspenseConfig); // Process this update.

            if (_update.eagerReducer === reducer) {
              // If this update was processed eagerly, and its reducer matches the
              // current reducer, we can use the eagerly computed state.
              _newState = _update.eagerState;
            } else {
              var _action = _update.action;
              _newState = reducer(_newState, _action);
            }
          }

          prevUpdate = _update;
          _update = _update.next;
        } while (_update !== null && _update !== first);

        if (!didSkip) {
          newBaseUpdate = prevUpdate;
          newBaseState = _newState;
        } // Mark that the fiber performed work, but only if the new state is
        // different from the current state.


        if (!is$1(_newState, hook.memoizedState)) {
          markWorkInProgressReceivedUpdate();
        }

        hook.memoizedState = _newState;
        hook.baseUpdate = newBaseUpdate;
        hook.baseState = newBaseState;
        queue.lastRenderedState = _newState;
      }

      var dispatch = queue.dispatch;
      return [hook.memoizedState, dispatch];
    }

    function mountState(initialState) {
      var hook = mountWorkInProgressHook();

      if (typeof initialState === 'function') {
        initialState = initialState();
      }

      hook.memoizedState = hook.baseState = initialState;
      var queue = hook.queue = {
        last: null,
        dispatch: null,
        lastRenderedReducer: basicStateReducer,
        lastRenderedState: initialState
      };
      var dispatch = queue.dispatch = dispatchAction.bind(null, // Flow doesn't know this is non-null, but we do.
      currentlyRenderingFiber$1, queue);
      return [hook.memoizedState, dispatch];
    }

    function updateState(initialState) {
      return updateReducer(basicStateReducer, initialState);
    }

    function pushEffect(tag, create, destroy, deps) {
      var effect = {
        tag: tag,
        create: create,
        destroy: destroy,
        deps: deps,
        // Circular
        next: null
      };

      if (componentUpdateQueue === null) {
        componentUpdateQueue = createFunctionComponentUpdateQueue();
        componentUpdateQueue.lastEffect = effect.next = effect;
      } else {
        var lastEffect = componentUpdateQueue.lastEffect;

        if (lastEffect === null) {
          componentUpdateQueue.lastEffect = effect.next = effect;
        } else {
          var firstEffect = lastEffect.next;
          lastEffect.next = effect;
          effect.next = firstEffect;
          componentUpdateQueue.lastEffect = effect;
        }
      }

      return effect;
    }

    function mountRef(initialValue) {
      var hook = mountWorkInProgressHook();
      var ref = {
        current: initialValue
      };
      {
        Object.seal(ref);
      }
      hook.memoizedState = ref;
      return ref;
    }

    function updateRef(initialValue) {
      var hook = updateWorkInProgressHook();
      return hook.memoizedState;
    }

    function mountEffectImpl(fiberEffectTag, hookEffectTag, create, deps) {
      var hook = mountWorkInProgressHook();
      var nextDeps = deps === undefined ? null : deps;
      sideEffectTag |= fiberEffectTag;
      hook.memoizedState = pushEffect(hookEffectTag, create, undefined, nextDeps);
    }

    function updateEffectImpl(fiberEffectTag, hookEffectTag, create, deps) {
      var hook = updateWorkInProgressHook();
      var nextDeps = deps === undefined ? null : deps;
      var destroy = undefined;

      if (currentHook !== null) {
        var prevEffect = currentHook.memoizedState;
        destroy = prevEffect.destroy;

        if (nextDeps !== null) {
          var prevDeps = prevEffect.deps;

          if (areHookInputsEqual(nextDeps, prevDeps)) {
            pushEffect(NoEffect$1, create, destroy, nextDeps);
            return;
          }
        }
      }

      sideEffectTag |= fiberEffectTag;
      hook.memoizedState = pushEffect(hookEffectTag, create, destroy, nextDeps);
    }

    function mountEffect(create, deps) {
      {
        // $FlowExpectedError - jest isn't a global, and isn't recognized outside of tests
        if ('undefined' !== typeof jest) {
          warnIfNotCurrentlyActingEffectsInDEV(currentlyRenderingFiber$1);
        }
      }
      return mountEffectImpl(Update | Passive, UnmountPassive | MountPassive, create, deps);
    }

    function updateEffect(create, deps) {
      {
        // $FlowExpectedError - jest isn't a global, and isn't recognized outside of tests
        if ('undefined' !== typeof jest) {
          warnIfNotCurrentlyActingEffectsInDEV(currentlyRenderingFiber$1);
        }
      }
      return updateEffectImpl(Update | Passive, UnmountPassive | MountPassive, create, deps);
    }

    function mountLayoutEffect(create, deps) {
      return mountEffectImpl(Update, UnmountMutation | MountLayout, create, deps);
    }

    function updateLayoutEffect(create, deps) {
      return updateEffectImpl(Update, UnmountMutation | MountLayout, create, deps);
    }

    function imperativeHandleEffect(create, ref) {
      if (typeof ref === 'function') {
        var refCallback = ref;

        var _inst = create();

        refCallback(_inst);
        return function () {
          refCallback(null);
        };
      } else if (ref !== null && ref !== undefined) {
        var refObject = ref;
        {
          !refObject.hasOwnProperty('current') ? warning$1(false, 'Expected useImperativeHandle() first argument to either be a ' + 'ref callback or React.createRef() object. Instead received: %s.', 'an object with keys {' + Object.keys(refObject).join(', ') + '}') : void 0;
        }

        var _inst2 = create();

        refObject.current = _inst2;
        return function () {
          refObject.current = null;
        };
      }
    }

    function mountImperativeHandle(ref, create, deps) {
      {
        !(typeof create === 'function') ? warning$1(false, 'Expected useImperativeHandle() second argument to be a function ' + 'that creates a handle. Instead received: %s.', create !== null ? typeof create : 'null') : void 0;
      } // TODO: If deps are provided, should we skip comparing the ref itself?

      var effectDeps = deps !== null && deps !== undefined ? deps.concat([ref]) : null;
      return mountEffectImpl(Update, UnmountMutation | MountLayout, imperativeHandleEffect.bind(null, create, ref), effectDeps);
    }

    function updateImperativeHandle(ref, create, deps) {
      {
        !(typeof create === 'function') ? warning$1(false, 'Expected useImperativeHandle() second argument to be a function ' + 'that creates a handle. Instead received: %s.', create !== null ? typeof create : 'null') : void 0;
      } // TODO: If deps are provided, should we skip comparing the ref itself?

      var effectDeps = deps !== null && deps !== undefined ? deps.concat([ref]) : null;
      return updateEffectImpl(Update, UnmountMutation | MountLayout, imperativeHandleEffect.bind(null, create, ref), effectDeps);
    }

    function mountDebugValue(value, formatterFn) {// This hook is normally a no-op.
      // The react-debug-hooks package injects its own implementation
      // so that e.g. DevTools can display custom hook values.
    }

    var updateDebugValue = mountDebugValue;

    function mountCallback(callback, deps) {
      var hook = mountWorkInProgressHook();
      var nextDeps = deps === undefined ? null : deps;
      hook.memoizedState = [callback, nextDeps];
      return callback;
    }

    function updateCallback(callback, deps) {
      var hook = updateWorkInProgressHook();
      var nextDeps = deps === undefined ? null : deps;
      var prevState = hook.memoizedState;

      if (prevState !== null) {
        if (nextDeps !== null) {
          var prevDeps = prevState[1];

          if (areHookInputsEqual(nextDeps, prevDeps)) {
            return prevState[0];
          }
        }
      }

      hook.memoizedState = [callback, nextDeps];
      return callback;
    }

    function mountMemo(nextCreate, deps) {
      var hook = mountWorkInProgressHook();
      var nextDeps = deps === undefined ? null : deps;
      var nextValue = nextCreate();
      hook.memoizedState = [nextValue, nextDeps];
      return nextValue;
    }

    function updateMemo(nextCreate, deps) {
      var hook = updateWorkInProgressHook();
      var nextDeps = deps === undefined ? null : deps;
      var prevState = hook.memoizedState;

      if (prevState !== null) {
        // Assume these are defined. If they're not, areHookInputsEqual will warn.
        if (nextDeps !== null) {
          var prevDeps = prevState[1];

          if (areHookInputsEqual(nextDeps, prevDeps)) {
            return prevState[0];
          }
        }
      }

      var nextValue = nextCreate();
      hook.memoizedState = [nextValue, nextDeps];
      return nextValue;
    }

    function dispatchAction(fiber, queue, action) {
      (function () {
        if (!(numberOfReRenders < RE_RENDER_LIMIT)) {
          {
            throw ReactError(Error("Too many re-renders. React limits the number of renders to prevent an infinite loop."));
          }
        }
      })();

      {
        !(typeof arguments[3] !== 'function') ? warning$1(false, "State updates from the useState() and useReducer() Hooks don't support the " + 'second callback argument. To execute a side effect after ' + 'rendering, declare it in the component body with useEffect().') : void 0;
      }
      var alternate = fiber.alternate;

      if (fiber === currentlyRenderingFiber$1 || alternate !== null && alternate === currentlyRenderingFiber$1) {
        // This is a render phase update. Stash it in a lazily-created map of
        // queue -> linked list of updates. After this render pass, we'll restart
        // and apply the stashed updates on top of the work-in-progress hook.
        didScheduleRenderPhaseUpdate = true;
        var update = {
          expirationTime: renderExpirationTime$1,
          suspenseConfig: null,
          action: action,
          eagerReducer: null,
          eagerState: null,
          next: null
        };
        {
          update.priority = getCurrentPriorityLevel();
        }

        if (renderPhaseUpdates === null) {
          renderPhaseUpdates = new Map();
        }

        var firstRenderPhaseUpdate = renderPhaseUpdates.get(queue);

        if (firstRenderPhaseUpdate === undefined) {
          renderPhaseUpdates.set(queue, update);
        } else {
          // Append the update to the end of the list.
          var lastRenderPhaseUpdate = firstRenderPhaseUpdate;

          while (lastRenderPhaseUpdate.next !== null) {
            lastRenderPhaseUpdate = lastRenderPhaseUpdate.next;
          }

          lastRenderPhaseUpdate.next = update;
        }
      } else {
        var currentTime = requestCurrentTime();
        var suspenseConfig = requestCurrentSuspenseConfig();
        var expirationTime = computeExpirationForFiber(currentTime, fiber, suspenseConfig);
        var _update2 = {
          expirationTime: expirationTime,
          suspenseConfig: suspenseConfig,
          action: action,
          eagerReducer: null,
          eagerState: null,
          next: null
        };
        {
          _update2.priority = getCurrentPriorityLevel();
        } // Append the update to the end of the list.

        var last = queue.last;

        if (last === null) {
          // This is the first update. Create a circular list.
          _update2.next = _update2;
        } else {
          var first = last.next;

          if (first !== null) {
            // Still circular.
            _update2.next = first;
          }

          last.next = _update2;
        }

        queue.last = _update2;

        if (fiber.expirationTime === NoWork && (alternate === null || alternate.expirationTime === NoWork)) {
          // The queue is currently empty, which means we can eagerly compute the
          // next state before entering the render phase. If the new state is the
          // same as the current state, we may be able to bail out entirely.
          var lastRenderedReducer = queue.lastRenderedReducer;

          if (lastRenderedReducer !== null) {
            var prevDispatcher;
            {
              prevDispatcher = ReactCurrentDispatcher$1.current;
              ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
            }

            try {
              var currentState = queue.lastRenderedState;
              var eagerState = lastRenderedReducer(currentState, action); // Stash the eagerly computed state, and the reducer used to compute
              // it, on the update object. If the reducer hasn't changed by the
              // time we enter the render phase, then the eager state can be used
              // without calling the reducer again.

              _update2.eagerReducer = lastRenderedReducer;
              _update2.eagerState = eagerState;

              if (is$1(eagerState, currentState)) {
                // Fast path. We can bail out without scheduling React to re-render.
                // It's still possible that we'll need to rebase this update later,
                // if the component re-renders for a different reason and by that
                // time the reducer has changed.
                return;
              }
            } catch (error) {// Suppress the error. It will throw again in the render phase.
            } finally {
              {
                ReactCurrentDispatcher$1.current = prevDispatcher;
              }
            }
          }
        }

        {
          // $FlowExpectedError - jest isn't a global, and isn't recognized outside of tests
          if ('undefined' !== typeof jest) {
            warnIfNotScopedWithMatchingAct(fiber);
            warnIfNotCurrentlyActingUpdatesInDev(fiber);
          }
        }
        scheduleWork(fiber, expirationTime);
      }
    }

    var ContextOnlyDispatcher = {
      readContext: readContext,
      useCallback: throwInvalidHookError,
      useContext: throwInvalidHookError,
      useEffect: throwInvalidHookError,
      useImperativeHandle: throwInvalidHookError,
      useLayoutEffect: throwInvalidHookError,
      useMemo: throwInvalidHookError,
      useReducer: throwInvalidHookError,
      useRef: throwInvalidHookError,
      useState: throwInvalidHookError,
      useDebugValue: throwInvalidHookError,
      useResponder: throwInvalidHookError
    };
    var HooksDispatcherOnMountInDEV = null;
    var HooksDispatcherOnMountWithHookTypesInDEV = null;
    var HooksDispatcherOnUpdateInDEV = null;
    var InvalidNestedHooksDispatcherOnMountInDEV = null;
    var InvalidNestedHooksDispatcherOnUpdateInDEV = null;
    {
      var warnInvalidContextAccess = function () {
        warning$1(false, 'Context can only be read while React is rendering. ' + 'In classes, you can read it in the render method or getDerivedStateFromProps. ' + 'In function components, you can read it directly in the function body, but not ' + 'inside Hooks like useReducer() or useMemo().');
      };

      var warnInvalidHookAccess = function () {
        warning$1(false, 'Do not call Hooks inside useEffect(...), useMemo(...), or other built-in Hooks. ' + 'You can only call Hooks at the top level of your React function. ' + 'For more information, see ' + 'https://fb.me/rules-of-hooks');
      };

      HooksDispatcherOnMountInDEV = {
        readContext: function (context, observedBits) {
          return readContext(context, observedBits);
        },
        useCallback: function (callback, deps) {
          currentHookNameInDev = 'useCallback';
          mountHookTypesDev();
          checkDepsAreArrayDev(deps);
          return mountCallback(callback, deps);
        },
        useContext: function (context, observedBits) {
          currentHookNameInDev = 'useContext';
          mountHookTypesDev();
          return readContext(context, observedBits);
        },
        useEffect: function (create, deps) {
          currentHookNameInDev = 'useEffect';
          mountHookTypesDev();
          checkDepsAreArrayDev(deps);
          return mountEffect(create, deps);
        },
        useImperativeHandle: function (ref, create, deps) {
          currentHookNameInDev = 'useImperativeHandle';
          mountHookTypesDev();
          checkDepsAreArrayDev(deps);
          return mountImperativeHandle(ref, create, deps);
        },
        useLayoutEffect: function (create, deps) {
          currentHookNameInDev = 'useLayoutEffect';
          mountHookTypesDev();
          checkDepsAreArrayDev(deps);
          return mountLayoutEffect(create, deps);
        },
        useMemo: function (create, deps) {
          currentHookNameInDev = 'useMemo';
          mountHookTypesDev();
          checkDepsAreArrayDev(deps);
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;

          try {
            return mountMemo(create, deps);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useReducer: function (reducer, initialArg, init) {
          currentHookNameInDev = 'useReducer';
          mountHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;

          try {
            return mountReducer(reducer, initialArg, init);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useRef: function (initialValue) {
          currentHookNameInDev = 'useRef';
          mountHookTypesDev();
          return mountRef(initialValue);
        },
        useState: function (initialState) {
          currentHookNameInDev = 'useState';
          mountHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;

          try {
            return mountState(initialState);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useDebugValue: function (value, formatterFn) {
          currentHookNameInDev = 'useDebugValue';
          mountHookTypesDev();
          return mountDebugValue(value, formatterFn);
        },
        useResponder: function (responder, props) {
          currentHookNameInDev = 'useResponder';
          mountHookTypesDev();
          return createResponderListener(responder, props);
        }
      };
      HooksDispatcherOnMountWithHookTypesInDEV = {
        readContext: function (context, observedBits) {
          return readContext(context, observedBits);
        },
        useCallback: function (callback, deps) {
          currentHookNameInDev = 'useCallback';
          updateHookTypesDev();
          return mountCallback(callback, deps);
        },
        useContext: function (context, observedBits) {
          currentHookNameInDev = 'useContext';
          updateHookTypesDev();
          return readContext(context, observedBits);
        },
        useEffect: function (create, deps) {
          currentHookNameInDev = 'useEffect';
          updateHookTypesDev();
          return mountEffect(create, deps);
        },
        useImperativeHandle: function (ref, create, deps) {
          currentHookNameInDev = 'useImperativeHandle';
          updateHookTypesDev();
          return mountImperativeHandle(ref, create, deps);
        },
        useLayoutEffect: function (create, deps) {
          currentHookNameInDev = 'useLayoutEffect';
          updateHookTypesDev();
          return mountLayoutEffect(create, deps);
        },
        useMemo: function (create, deps) {
          currentHookNameInDev = 'useMemo';
          updateHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;

          try {
            return mountMemo(create, deps);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useReducer: function (reducer, initialArg, init) {
          currentHookNameInDev = 'useReducer';
          updateHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;

          try {
            return mountReducer(reducer, initialArg, init);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useRef: function (initialValue) {
          currentHookNameInDev = 'useRef';
          updateHookTypesDev();
          return mountRef(initialValue);
        },
        useState: function (initialState) {
          currentHookNameInDev = 'useState';
          updateHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;

          try {
            return mountState(initialState);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useDebugValue: function (value, formatterFn) {
          currentHookNameInDev = 'useDebugValue';
          updateHookTypesDev();
          return mountDebugValue(value, formatterFn);
        },
        useResponder: function (responder, props) {
          currentHookNameInDev = 'useResponder';
          updateHookTypesDev();
          return createResponderListener(responder, props);
        }
      };
      HooksDispatcherOnUpdateInDEV = {
        readContext: function (context, observedBits) {
          return readContext(context, observedBits);
        },
        useCallback: function (callback, deps) {
          currentHookNameInDev = 'useCallback';
          updateHookTypesDev();
          return updateCallback(callback, deps);
        },
        useContext: function (context, observedBits) {
          currentHookNameInDev = 'useContext';
          updateHookTypesDev();
          return readContext(context, observedBits);
        },
        useEffect: function (create, deps) {
          currentHookNameInDev = 'useEffect';
          updateHookTypesDev();
          return updateEffect(create, deps);
        },
        useImperativeHandle: function (ref, create, deps) {
          currentHookNameInDev = 'useImperativeHandle';
          updateHookTypesDev();
          return updateImperativeHandle(ref, create, deps);
        },
        useLayoutEffect: function (create, deps) {
          currentHookNameInDev = 'useLayoutEffect';
          updateHookTypesDev();
          return updateLayoutEffect(create, deps);
        },
        useMemo: function (create, deps) {
          currentHookNameInDev = 'useMemo';
          updateHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

          try {
            return updateMemo(create, deps);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useReducer: function (reducer, initialArg, init) {
          currentHookNameInDev = 'useReducer';
          updateHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

          try {
            return updateReducer(reducer, initialArg, init);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useRef: function (initialValue) {
          currentHookNameInDev = 'useRef';
          updateHookTypesDev();
          return updateRef(initialValue);
        },
        useState: function (initialState) {
          currentHookNameInDev = 'useState';
          updateHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

          try {
            return updateState(initialState);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useDebugValue: function (value, formatterFn) {
          currentHookNameInDev = 'useDebugValue';
          updateHookTypesDev();
          return updateDebugValue(value, formatterFn);
        },
        useResponder: function (responder, props) {
          currentHookNameInDev = 'useResponder';
          updateHookTypesDev();
          return createResponderListener(responder, props);
        }
      };
      InvalidNestedHooksDispatcherOnMountInDEV = {
        readContext: function (context, observedBits) {
          warnInvalidContextAccess();
          return readContext(context, observedBits);
        },
        useCallback: function (callback, deps) {
          currentHookNameInDev = 'useCallback';
          warnInvalidHookAccess();
          mountHookTypesDev();
          return mountCallback(callback, deps);
        },
        useContext: function (context, observedBits) {
          currentHookNameInDev = 'useContext';
          warnInvalidHookAccess();
          mountHookTypesDev();
          return readContext(context, observedBits);
        },
        useEffect: function (create, deps) {
          currentHookNameInDev = 'useEffect';
          warnInvalidHookAccess();
          mountHookTypesDev();
          return mountEffect(create, deps);
        },
        useImperativeHandle: function (ref, create, deps) {
          currentHookNameInDev = 'useImperativeHandle';
          warnInvalidHookAccess();
          mountHookTypesDev();
          return mountImperativeHandle(ref, create, deps);
        },
        useLayoutEffect: function (create, deps) {
          currentHookNameInDev = 'useLayoutEffect';
          warnInvalidHookAccess();
          mountHookTypesDev();
          return mountLayoutEffect(create, deps);
        },
        useMemo: function (create, deps) {
          currentHookNameInDev = 'useMemo';
          warnInvalidHookAccess();
          mountHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;

          try {
            return mountMemo(create, deps);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useReducer: function (reducer, initialArg, init) {
          currentHookNameInDev = 'useReducer';
          warnInvalidHookAccess();
          mountHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;

          try {
            return mountReducer(reducer, initialArg, init);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useRef: function (initialValue) {
          currentHookNameInDev = 'useRef';
          warnInvalidHookAccess();
          mountHookTypesDev();
          return mountRef(initialValue);
        },
        useState: function (initialState) {
          currentHookNameInDev = 'useState';
          warnInvalidHookAccess();
          mountHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;

          try {
            return mountState(initialState);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useDebugValue: function (value, formatterFn) {
          currentHookNameInDev = 'useDebugValue';
          warnInvalidHookAccess();
          mountHookTypesDev();
          return mountDebugValue(value, formatterFn);
        },
        useResponder: function (responder, props) {
          currentHookNameInDev = 'useResponder';
          warnInvalidHookAccess();
          mountHookTypesDev();
          return createResponderListener(responder, props);
        }
      };
      InvalidNestedHooksDispatcherOnUpdateInDEV = {
        readContext: function (context, observedBits) {
          warnInvalidContextAccess();
          return readContext(context, observedBits);
        },
        useCallback: function (callback, deps) {
          currentHookNameInDev = 'useCallback';
          warnInvalidHookAccess();
          updateHookTypesDev();
          return updateCallback(callback, deps);
        },
        useContext: function (context, observedBits) {
          currentHookNameInDev = 'useContext';
          warnInvalidHookAccess();
          updateHookTypesDev();
          return readContext(context, observedBits);
        },
        useEffect: function (create, deps) {
          currentHookNameInDev = 'useEffect';
          warnInvalidHookAccess();
          updateHookTypesDev();
          return updateEffect(create, deps);
        },
        useImperativeHandle: function (ref, create, deps) {
          currentHookNameInDev = 'useImperativeHandle';
          warnInvalidHookAccess();
          updateHookTypesDev();
          return updateImperativeHandle(ref, create, deps);
        },
        useLayoutEffect: function (create, deps) {
          currentHookNameInDev = 'useLayoutEffect';
          warnInvalidHookAccess();
          updateHookTypesDev();
          return updateLayoutEffect(create, deps);
        },
        useMemo: function (create, deps) {
          currentHookNameInDev = 'useMemo';
          warnInvalidHookAccess();
          updateHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

          try {
            return updateMemo(create, deps);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useReducer: function (reducer, initialArg, init) {
          currentHookNameInDev = 'useReducer';
          warnInvalidHookAccess();
          updateHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

          try {
            return updateReducer(reducer, initialArg, init);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useRef: function (initialValue) {
          currentHookNameInDev = 'useRef';
          warnInvalidHookAccess();
          updateHookTypesDev();
          return updateRef(initialValue);
        },
        useState: function (initialState) {
          currentHookNameInDev = 'useState';
          warnInvalidHookAccess();
          updateHookTypesDev();
          var prevDispatcher = ReactCurrentDispatcher$1.current;
          ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

          try {
            return updateState(initialState);
          } finally {
            ReactCurrentDispatcher$1.current = prevDispatcher;
          }
        },
        useDebugValue: function (value, formatterFn) {
          currentHookNameInDev = 'useDebugValue';
          warnInvalidHookAccess();
          updateHookTypesDev();
          return updateDebugValue(value, formatterFn);
        },
        useResponder: function (responder, props) {
          currentHookNameInDev = 'useResponder';
          warnInvalidHookAccess();
          updateHookTypesDev();
          return createResponderListener(responder, props);
        }
      };
    } // CommonJS interop named imports.

    var now$1 = Scheduler.unstable_now;
    var commitTime = 0;
    var profilerStartTime = -1;

    function getCommitTime() {
      return commitTime;
    }

    function recordCommitTime() {
      if (!enableProfilerTimer) {
        return;
      }

      commitTime = now$1();
    }

    function startProfilerTimer(fiber) {
      if (!enableProfilerTimer) {
        return;
      }

      profilerStartTime = now$1();

      if (fiber.actualStartTime < 0) {
        fiber.actualStartTime = now$1();
      }
    }

    function stopProfilerTimerIfRunning(fiber) {
      if (!enableProfilerTimer) {
        return;
      }

      profilerStartTime = -1;
    }

    function stopProfilerTimerIfRunningAndRecordDelta(fiber, overrideBaseTime) {
      if (!enableProfilerTimer) {
        return;
      }

      if (profilerStartTime >= 0) {
        var elapsedTime = now$1() - profilerStartTime;
        fiber.actualDuration += elapsedTime;

        if (overrideBaseTime) {
          fiber.selfBaseDuration = elapsedTime;
        }

        profilerStartTime = -1;
      }
    } // This may have been an insertion or a hydration.


    var hydrationParentFiber = null;
    var nextHydratableInstance = null;
    var isHydrating = false;

    function warnIfHydrating() {
      {
        !!isHydrating ? warning$1(false, 'We should not be hydrating here. This is a bug in React. Please file a bug.') : void 0;
      }
    }

    function enterHydrationState(fiber) {
      if (!supportsHydration) {
        return false;
      }

      var parentInstance = fiber.stateNode.containerInfo;
      nextHydratableInstance = getFirstHydratableChild(parentInstance);
      hydrationParentFiber = fiber;
      isHydrating = true;
      return true;
    }

    function reenterHydrationStateFromDehydratedSuspenseInstance(fiber, suspenseInstance) {
      if (!supportsHydration) {
        return false;
      }

      nextHydratableInstance = getNextHydratableSibling(suspenseInstance);
      popToNextHostParent(fiber);
      isHydrating = true;
      return true;
    }

    function deleteHydratableInstance(returnFiber, instance) {
      {
        switch (returnFiber.tag) {
          case HostRoot:
            didNotHydrateContainerInstance(returnFiber.stateNode.containerInfo, instance);
            break;

          case HostComponent:
            didNotHydrateInstance(returnFiber.type, returnFiber.memoizedProps, returnFiber.stateNode, instance);
            break;
        }
      }
      var childToDelete = createFiberFromHostInstanceForDeletion();
      childToDelete.stateNode = instance;
      childToDelete.return = returnFiber;
      childToDelete.effectTag = Deletion; // This might seem like it belongs on progressedFirstDeletion. However,
      // these children are not part of the reconciliation list of children.
      // Even if we abort and rereconcile the children, that will try to hydrate
      // again and the nodes are still in the host tree so these will be
      // recreated.

      if (returnFiber.lastEffect !== null) {
        returnFiber.lastEffect.nextEffect = childToDelete;
        returnFiber.lastEffect = childToDelete;
      } else {
        returnFiber.firstEffect = returnFiber.lastEffect = childToDelete;
      }
    }

    function insertNonHydratedInstance(returnFiber, fiber) {
      fiber.effectTag = fiber.effectTag & ~Hydrating | Placement;
      {
        switch (returnFiber.tag) {
          case HostRoot:
            {
              var parentContainer = returnFiber.stateNode.containerInfo;

              switch (fiber.tag) {
                case HostComponent:
                  var type = fiber.type;
                  var props = fiber.pendingProps;
                  didNotFindHydratableContainerInstance(parentContainer, type, props);
                  break;

                case HostText:
                  var text = fiber.pendingProps;
                  didNotFindHydratableContainerTextInstance(parentContainer, text);
                  break;

                case SuspenseComponent:
                  break;
              }

              break;
            }

          case HostComponent:
            {
              var parentType = returnFiber.type;
              var parentProps = returnFiber.memoizedProps;
              var parentInstance = returnFiber.stateNode;

              switch (fiber.tag) {
                case HostComponent:
                  var _type = fiber.type;
                  var _props = fiber.pendingProps;
                  didNotFindHydratableInstance(parentType, parentProps, parentInstance, _type, _props);
                  break;

                case HostText:
                  var _text = fiber.pendingProps;
                  didNotFindHydratableTextInstance(parentType, parentProps, parentInstance, _text);
                  break;

                case SuspenseComponent:
                  didNotFindHydratableSuspenseInstance(parentType, parentProps, parentInstance);
                  break;
              }

              break;
            }

          default:
            return;
        }
      }
    }

    function tryHydrate(fiber, nextInstance) {
      switch (fiber.tag) {
        case HostComponent:
          {
            var type = fiber.type;
            var props = fiber.pendingProps;
            var instance = canHydrateInstance(nextInstance, type, props);

            if (instance !== null) {
              fiber.stateNode = instance;
              return true;
            }

            return false;
          }

        case HostText:
          {
            var text = fiber.pendingProps;
            var textInstance = canHydrateTextInstance(nextInstance, text);

            if (textInstance !== null) {
              fiber.stateNode = textInstance;
              return true;
            }

            return false;
          }

        case SuspenseComponent:
          {
            if (enableSuspenseServerRenderer) {
              var suspenseInstance = canHydrateSuspenseInstance(nextInstance);

              if (suspenseInstance !== null) {
                var suspenseState = {
                  dehydrated: suspenseInstance,
                  retryTime: Never
                };
                fiber.memoizedState = suspenseState; // Store the dehydrated fragment as a child fiber.
                // This simplifies the code for getHostSibling and deleting nodes,
                // since it doesn't have to consider all Suspense boundaries and
                // check if they're dehydrated ones or not.

                var dehydratedFragment = createFiberFromDehydratedFragment(suspenseInstance);
                dehydratedFragment.return = fiber;
                fiber.child = dehydratedFragment;
                return true;
              }
            }

            return false;
          }

        default:
          return false;
      }
    }

    function tryToClaimNextHydratableInstance(fiber) {
      if (!isHydrating) {
        return;
      }

      var nextInstance = nextHydratableInstance;

      if (!nextInstance) {
        // Nothing to hydrate. Make it an insertion.
        insertNonHydratedInstance(hydrationParentFiber, fiber);
        isHydrating = false;
        hydrationParentFiber = fiber;
        return;
      }

      var firstAttemptedInstance = nextInstance;

      if (!tryHydrate(fiber, nextInstance)) {
        // If we can't hydrate this instance let's try the next one.
        // We use this as a heuristic. It's based on intuition and not data so it
        // might be flawed or unnecessary.
        nextInstance = getNextHydratableSibling(firstAttemptedInstance);

        if (!nextInstance || !tryHydrate(fiber, nextInstance)) {
          // Nothing to hydrate. Make it an insertion.
          insertNonHydratedInstance(hydrationParentFiber, fiber);
          isHydrating = false;
          hydrationParentFiber = fiber;
          return;
        } // We matched the next one, we'll now assume that the first one was
        // superfluous and we'll delete it. Since we can't eagerly delete it
        // we'll have to schedule a deletion. To do that, this node needs a dummy
        // fiber associated with it.


        deleteHydratableInstance(hydrationParentFiber, firstAttemptedInstance);
      }

      hydrationParentFiber = fiber;
      nextHydratableInstance = getFirstHydratableChild(nextInstance);
    }

    function prepareToHydrateHostInstance(fiber, rootContainerInstance, hostContext) {
      if (!supportsHydration) {
        (function () {
          {
            {
              throw ReactError(Error("Expected prepareToHydrateHostInstance() to never be called. This error is likely caused by a bug in React. Please file an issue."));
            }
          }
        })();
      }

      var instance = fiber.stateNode;
      var updatePayload = hydrateInstance(instance, fiber.type, fiber.memoizedProps, rootContainerInstance, hostContext, fiber); // TODO: Type this specific to this type of component.

      fiber.updateQueue = updatePayload; // If the update payload indicates that there is a change or if there
      // is a new ref we mark this as an update.

      if (updatePayload !== null) {
        return true;
      }

      return false;
    }

    function prepareToHydrateHostTextInstance(fiber) {
      if (!supportsHydration) {
        (function () {
          {
            {
              throw ReactError(Error("Expected prepareToHydrateHostTextInstance() to never be called. This error is likely caused by a bug in React. Please file an issue."));
            }
          }
        })();
      }

      var textInstance = fiber.stateNode;
      var textContent = fiber.memoizedProps;
      var shouldUpdate = hydrateTextInstance(textInstance, textContent, fiber);
      {
        if (shouldUpdate) {
          // We assume that prepareToHydrateHostTextInstance is called in a context where the
          // hydration parent is the parent host component of this host text.
          var returnFiber = hydrationParentFiber;

          if (returnFiber !== null) {
            switch (returnFiber.tag) {
              case HostRoot:
                {
                  var parentContainer = returnFiber.stateNode.containerInfo;
                  didNotMatchHydratedContainerTextInstance(parentContainer, textInstance, textContent);
                  break;
                }

              case HostComponent:
                {
                  var parentType = returnFiber.type;
                  var parentProps = returnFiber.memoizedProps;
                  var parentInstance = returnFiber.stateNode;
                  didNotMatchHydratedTextInstance(parentType, parentProps, parentInstance, textInstance, textContent);
                  break;
                }
            }
          }
        }
      }
      return shouldUpdate;
    }

    function prepareToHydrateHostSuspenseInstance(fiber) {
      if (!supportsHydration) {
        (function () {
          {
            {
              throw ReactError(Error("Expected prepareToHydrateHostSuspenseInstance() to never be called. This error is likely caused by a bug in React. Please file an issue."));
            }
          }
        })();
      }

      var suspenseState = fiber.memoizedState;
      var suspenseInstance = suspenseState !== null ? suspenseState.dehydrated : null;

      (function () {
        if (!suspenseInstance) {
          {
            throw ReactError(Error("Expected to have a hydrated suspense instance. This error is likely caused by a bug in React. Please file an issue."));
          }
        }
      })();

      hydrateSuspenseInstance(suspenseInstance, fiber);
    }

    function skipPastDehydratedSuspenseInstance(fiber) {
      if (!supportsHydration) {
        (function () {
          {
            {
              throw ReactError(Error("Expected skipPastDehydratedSuspenseInstance() to never be called. This error is likely caused by a bug in React. Please file an issue."));
            }
          }
        })();
      }

      var suspenseState = fiber.memoizedState;
      var suspenseInstance = suspenseState !== null ? suspenseState.dehydrated : null;

      if (suspenseInstance === null) {
        // This Suspense boundary was hydrated without a match.
        return nextHydratableInstance;
      }

      return getNextHydratableInstanceAfterSuspenseInstance(suspenseInstance);
    }

    function popToNextHostParent(fiber) {
      var parent = fiber.return;

      while (parent !== null && parent.tag !== HostComponent && parent.tag !== HostRoot && parent.tag !== SuspenseComponent) {
        parent = parent.return;
      }

      hydrationParentFiber = parent;
    }

    function popHydrationState(fiber) {
      if (!supportsHydration) {
        return false;
      }

      if (fiber !== hydrationParentFiber) {
        // We're deeper than the current hydration context, inside an inserted
        // tree.
        return false;
      }

      if (!isHydrating) {
        // If we're not currently hydrating but we're in a hydration context, then
        // we were an insertion and now need to pop up reenter hydration of our
        // siblings.
        popToNextHostParent(fiber);
        isHydrating = true;
        return false;
      }

      var type = fiber.type; // If we have any remaining hydratable nodes, we need to delete them now.
      // We only do this deeper than head and body since they tend to have random
      // other nodes in them. We also ignore components with pure text content in
      // side of them.
      // TODO: Better heuristic.

      if (fiber.tag !== HostComponent || type !== 'head' && type !== 'body' && !shouldSetTextContent(type, fiber.memoizedProps)) {
        var nextInstance = nextHydratableInstance;

        while (nextInstance) {
          deleteHydratableInstance(fiber, nextInstance);
          nextInstance = getNextHydratableSibling(nextInstance);
        }
      }

      popToNextHostParent(fiber);

      if (fiber.tag === SuspenseComponent) {
        nextHydratableInstance = skipPastDehydratedSuspenseInstance(fiber);
      } else {
        nextHydratableInstance = hydrationParentFiber ? getNextHydratableSibling(fiber.stateNode) : null;
      }

      return true;
    }

    function resetHydrationState() {
      if (!supportsHydration) {
        return;
      }

      hydrationParentFiber = null;
      nextHydratableInstance = null;
      isHydrating = false;
    }

    var ReactCurrentOwner$3 = ReactSharedInternals.ReactCurrentOwner;
    var didReceiveUpdate = false;
    var didWarnAboutBadClass;
    var didWarnAboutModulePatternComponent;
    var didWarnAboutContextTypeOnFunctionComponent;
    var didWarnAboutGetDerivedStateOnFunctionComponent;
    var didWarnAboutFunctionRefs;
    var didWarnAboutReassigningProps;
    var didWarnAboutMaxDuration;
    var didWarnAboutRevealOrder;
    var didWarnAboutTailOptions;
    var didWarnAboutDefaultPropsOnFunctionComponent;
    {
      didWarnAboutBadClass = {};
      didWarnAboutModulePatternComponent = {};
      didWarnAboutContextTypeOnFunctionComponent = {};
      didWarnAboutGetDerivedStateOnFunctionComponent = {};
      didWarnAboutFunctionRefs = {};
      didWarnAboutReassigningProps = false;
      didWarnAboutMaxDuration = false;
      didWarnAboutRevealOrder = {};
      didWarnAboutTailOptions = {};
      didWarnAboutDefaultPropsOnFunctionComponent = {};
    }

    function reconcileChildren(current$$1, workInProgress, nextChildren, renderExpirationTime) {
      if (current$$1 === null) {
        // If this is a fresh new component that hasn't been rendered yet, we
        // won't update its child set by applying minimal side-effects. Instead,
        // we will add them all to the child before it gets rendered. That means
        // we can optimize this reconciliation pass by not tracking side-effects.
        workInProgress.child = mountChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
      } else {
        // If the current child is the same as the work in progress, it means that
        // we haven't yet started any work on these children. Therefore, we use
        // the clone algorithm to create a copy of all the current children.
        // If we had any progressed work already, that is invalid at this point so
        // let's throw it out.
        workInProgress.child = reconcileChildFibers(workInProgress, current$$1.child, nextChildren, renderExpirationTime);
      }
    }

    function forceUnmountCurrentAndReconcile(current$$1, workInProgress, nextChildren, renderExpirationTime) {
      // This function is fork of reconcileChildren. It's used in cases where we
      // want to reconcile without matching against the existing set. This has the
      // effect of all current children being unmounted; even if the type and key
      // are the same, the old child is unmounted and a new child is created.
      //
      // To do this, we're going to go through the reconcile algorithm twice. In
      // the first pass, we schedule a deletion for all the current children by
      // passing null.
      workInProgress.child = reconcileChildFibers(workInProgress, current$$1.child, null, renderExpirationTime); // In the second pass, we mount the new children. The trick here is that we
      // pass null in place of where we usually pass the current child set. This has
      // the effect of remounting all children regardless of whether their their
      // identity matches.

      workInProgress.child = reconcileChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
    }

    function updateForwardRef(current$$1, workInProgress, Component, nextProps, renderExpirationTime) {
      // TODO: current can be non-null here even if the component
      // hasn't yet mounted. This happens after the first render suspends.
      // We'll need to figure out if this is fine or can cause issues.
      {
        if (workInProgress.type !== workInProgress.elementType) {
          // Lazy component props can't be validated in createElement
          // because they're only guaranteed to be resolved here.
          var innerPropTypes = Component.propTypes;

          if (innerPropTypes) {
            checkPropTypes(innerPropTypes, nextProps, // Resolved props
            'prop', getComponentName(Component), getCurrentFiberStackInDev);
          }
        }
      }
      var render = Component.render;
      var ref = workInProgress.ref; // The rest is a fork of updateFunctionComponent

      var nextChildren;
      prepareToReadContext(workInProgress, renderExpirationTime);
      {
        ReactCurrentOwner$3.current = workInProgress;
        setCurrentPhase('render');
        nextChildren = renderWithHooks(current$$1, workInProgress, render, nextProps, ref, renderExpirationTime);

        if (debugRenderPhaseSideEffects || debugRenderPhaseSideEffectsForStrictMode && workInProgress.mode & StrictMode) {
          // Only double-render components with Hooks
          if (workInProgress.memoizedState !== null) {
            nextChildren = renderWithHooks(current$$1, workInProgress, render, nextProps, ref, renderExpirationTime);
          }
        }

        setCurrentPhase(null);
      }

      if (current$$1 !== null && !didReceiveUpdate) {
        bailoutHooks(current$$1, workInProgress, renderExpirationTime);
        return bailoutOnAlreadyFinishedWork(current$$1, workInProgress, renderExpirationTime);
      } // React DevTools reads this flag.


      workInProgress.effectTag |= PerformedWork;
      reconcileChildren(current$$1, workInProgress, nextChildren, renderExpirationTime);
      return workInProgress.child;
    }

    function updateMemoComponent(current$$1, workInProgress, Component, nextProps, updateExpirationTime, renderExpirationTime) {
      if (current$$1 === null) {
        var type = Component.type;

        if (isSimpleFunctionComponent(type) && Component.compare === null && // SimpleMemoComponent codepath doesn't resolve outer props either.
        Component.defaultProps === undefined) {
          var resolvedType = type;
          {
            resolvedType = resolveFunctionForHotReloading(type);
          } // If this is a plain function component without default props,
          // and with only the default shallow comparison, we upgrade it
          // to a SimpleMemoComponent to allow fast path updates.

          workInProgress.tag = SimpleMemoComponent;
          workInProgress.type = resolvedType;
          {
            validateFunctionComponentInDev(workInProgress, type);
          }
          return updateSimpleMemoComponent(current$$1, workInProgress, resolvedType, nextProps, updateExpirationTime, renderExpirationTime);
        }

        {
          var innerPropTypes = type.propTypes;

          if (innerPropTypes) {
            // Inner memo component props aren't currently validated in createElement.
            // We could move it there, but we'd still need this for lazy code path.
            checkPropTypes(innerPropTypes, nextProps, // Resolved props
            'prop', getComponentName(type), getCurrentFiberStackInDev);
          }
        }
        var child = createFiberFromTypeAndProps(Component.type, null, nextProps, null, workInProgress.mode, renderExpirationTime);
        child.ref = workInProgress.ref;
        child.return = workInProgress;
        workInProgress.child = child;
        return child;
      }

      {
        var _type = Component.type;
        var _innerPropTypes = _type.propTypes;

        if (_innerPropTypes) {
          // Inner memo component props aren't currently validated in createElement.
          // We could move it there, but we'd still need this for lazy code path.
          checkPropTypes(_innerPropTypes, nextProps, // Resolved props
          'prop', getComponentName(_type), getCurrentFiberStackInDev);
        }
      }
      var currentChild = current$$1.child; // This is always exactly one child

      if (updateExpirationTime < renderExpirationTime) {
        // This will be the props with resolved defaultProps,
        // unlike current.memoizedProps which will be the unresolved ones.
        var prevProps = currentChild.memoizedProps; // Default to shallow comparison

        var compare = Component.compare;
        compare = compare !== null ? compare : shallowEqual;

        if (compare(prevProps, nextProps) && current$$1.ref === workInProgress.ref) {
          return bailoutOnAlreadyFinishedWork(current$$1, workInProgress, renderExpirationTime);
        }
      } // React DevTools reads this flag.


      workInProgress.effectTag |= PerformedWork;
      var newChild = createWorkInProgress(currentChild, nextProps, renderExpirationTime);
      newChild.ref = workInProgress.ref;
      newChild.return = workInProgress;
      workInProgress.child = newChild;
      return newChild;
    }

    function updateSimpleMemoComponent(current$$1, workInProgress, Component, nextProps, updateExpirationTime, renderExpirationTime) {
      // TODO: current can be non-null here even if the component
      // hasn't yet mounted. This happens when the inner render suspends.
      // We'll need to figure out if this is fine or can cause issues.
      {
        if (workInProgress.type !== workInProgress.elementType) {
          // Lazy component props can't be validated in createElement
          // because they're only guaranteed to be resolved here.
          var outerMemoType = workInProgress.elementType;

          if (outerMemoType.$$typeof === REACT_LAZY_TYPE) {
            // We warn when you define propTypes on lazy()
            // so let's just skip over it to find memo() outer wrapper.
            // Inner props for memo are validated later.
            outerMemoType = refineResolvedLazyComponent(outerMemoType);
          }

          var outerPropTypes = outerMemoType && outerMemoType.propTypes;

          if (outerPropTypes) {
            checkPropTypes(outerPropTypes, nextProps, // Resolved (SimpleMemoComponent has no defaultProps)
            'prop', getComponentName(outerMemoType), getCurrentFiberStackInDev);
          } // Inner propTypes will be validated in the function component path.

        }
      }

      if (current$$1 !== null) {
        var prevProps = current$$1.memoizedProps;

        if (shallowEqual(prevProps, nextProps) && current$$1.ref === workInProgress.ref && // Prevent bailout if the implementation changed due to hot reload:
        workInProgress.type === current$$1.type) {
          didReceiveUpdate = false;

          if (updateExpirationTime < renderExpirationTime) {
            return bailoutOnAlreadyFinishedWork(current$$1, workInProgress, renderExpirationTime);
          }
        }
      }

      return updateFunctionComponent(current$$1, workInProgress, Component, nextProps, renderExpirationTime);
    }

    function updateFragment(current$$1, workInProgress, renderExpirationTime) {
      var nextChildren = workInProgress.pendingProps;
      reconcileChildren(current$$1, workInProgress, nextChildren, renderExpirationTime);
      return workInProgress.child;
    }

    function updateMode(current$$1, workInProgress, renderExpirationTime) {
      var nextChildren = workInProgress.pendingProps.children;
      reconcileChildren(current$$1, workInProgress, nextChildren, renderExpirationTime);
      return workInProgress.child;
    }

    function updateProfiler(current$$1, workInProgress, renderExpirationTime) {
      if (enableProfilerTimer) {
        workInProgress.effectTag |= Update;
      }

      var nextProps = workInProgress.pendingProps;
      var nextChildren = nextProps.children;
      reconcileChildren(current$$1, workInProgress, nextChildren, renderExpirationTime);
      return workInProgress.child;
    }

    function markRef(current$$1, workInProgress) {
      var ref = workInProgress.ref;

      if (current$$1 === null && ref !== null || current$$1 !== null && current$$1.ref !== ref) {
        // Schedule a Ref effect
        workInProgress.effectTag |= Ref;
      }
    }

    function updateFunctionComponent(current$$1, workInProgress, Component, nextProps, renderExpirationTime) {
      {
        if (workInProgress.type !== workInProgress.elementType) {
          // Lazy component props can't be validated in createElement
          // because they're only guaranteed to be resolved here.
          var innerPropTypes = Component.propTypes;

          if (innerPropTypes) {
            checkPropTypes(innerPropTypes, nextProps, // Resolved props
            'prop', getComponentName(Component), getCurrentFiberStackInDev);
          }
        }
      }
      var context;

      if (!disableLegacyContext) {
        var unmaskedContext = getUnmaskedContext(workInProgress, Component, true);
        context = getMaskedContext(workInProgress, unmaskedContext);
      }

      var nextChildren;
      prepareToReadContext(workInProgress, renderExpirationTime);
      {
        ReactCurrentOwner$3.current = workInProgress;
        setCurrentPhase('render');
        nextChildren = renderWithHooks(current$$1, workInProgress, Component, nextProps, context, renderExpirationTime);

        if (debugRenderPhaseSideEffects || debugRenderPhaseSideEffectsForStrictMode && workInProgress.mode & StrictMode) {
          // Only double-render components with Hooks
          if (workInProgress.memoizedState !== null) {
            nextChildren = renderWithHooks(current$$1, workInProgress, Component, nextProps, context, renderExpirationTime);
          }
        }

        setCurrentPhase(null);
      }

      if (current$$1 !== null && !didReceiveUpdate) {
        bailoutHooks(current$$1, workInProgress, renderExpirationTime);
        return bailoutOnAlreadyFinishedWork(current$$1, workInProgress, renderExpirationTime);
      } // React DevTools reads this flag.


      workInProgress.effectTag |= PerformedWork;
      reconcileChildren(current$$1, workInProgress, nextChildren, renderExpirationTime);
      return workInProgress.child;
    }

    function updateClassComponent(current$$1, workInProgress, Component, nextProps, renderExpirationTime) {
      {
        if (workInProgress.type !== workInProgress.elementType) {
          // Lazy component props can't be validated in createElement
          // because they're only guaranteed to be resolved here.
          var innerPropTypes = Component.propTypes;

          if (innerPropTypes) {
            checkPropTypes(innerPropTypes, nextProps, // Resolved props
            'prop', getComponentName(Component), getCurrentFiberStackInDev);
          }
        }
      } // Push context providers early to prevent context stack mismatches.
      // During mounting we don't know the child context yet as the instance doesn't exist.
      // We will invalidate the child context in finishClassComponent() right after rendering.

      var hasContext;

      if (isContextProvider(Component)) {
        hasContext = true;
        pushContextProvider(workInProgress);
      } else {
        hasContext = false;
      }

      prepareToReadContext(workInProgress, renderExpirationTime);
      var instance = workInProgress.stateNode;
      var shouldUpdate;

      if (instance === null) {
        if (current$$1 !== null) {
          // An class component without an instance only mounts if it suspended
          // inside a non- concurrent tree, in an inconsistent state. We want to
          // tree it like a new mount, even though an empty version of it already
          // committed. Disconnect the alternate pointers.
          current$$1.alternate = null;
          workInProgress.alternate = null; // Since this is conceptually a new fiber, schedule a Placement effect

          workInProgress.effectTag |= Placement;
        } // In the initial pass we might need to construct the instance.


        constructClassInstance(workInProgress, Component, nextProps, renderExpirationTime);
        mountClassInstance(workInProgress, Component, nextProps, renderExpirationTime);
        shouldUpdate = true;
      } else if (current$$1 === null) {
        // In a resume, we'll already have an instance we can reuse.
        shouldUpdate = resumeMountClassInstance(workInProgress, Component, nextProps, renderExpirationTime);
      } else {
        shouldUpdate = updateClassInstance(current$$1, workInProgress, Component, nextProps, renderExpirationTime);
      }

      var nextUnitOfWork = finishClassComponent(current$$1, workInProgress, Component, shouldUpdate, hasContext, renderExpirationTime);
      {
        var inst = workInProgress.stateNode;

        if (inst.props !== nextProps) {
          !didWarnAboutReassigningProps ? warning$1(false, 'It looks like %s is reassigning its own `this.props` while rendering. ' + 'This is not supported and can lead to confusing bugs.', getComponentName(workInProgress.type) || 'a component') : void 0;
          didWarnAboutReassigningProps = true;
        }
      }
      return nextUnitOfWork;
    }

    function finishClassComponent(current$$1, workInProgress, Component, shouldUpdate, hasContext, renderExpirationTime) {
      // Refs should update even if shouldComponentUpdate returns false
      markRef(current$$1, workInProgress);
      var didCaptureError = (workInProgress.effectTag & DidCapture) !== NoEffect;

      if (!shouldUpdate && !didCaptureError) {
        // Context providers should defer to sCU for rendering
        if (hasContext) {
          invalidateContextProvider(workInProgress, Component, false);
        }

        return bailoutOnAlreadyFinishedWork(current$$1, workInProgress, renderExpirationTime);
      }

      var instance = workInProgress.stateNode; // Rerender

      ReactCurrentOwner$3.current = workInProgress;
      var nextChildren;

      if (didCaptureError && typeof Component.getDerivedStateFromError !== 'function') {
        // If we captured an error, but getDerivedStateFrom catch is not defined,
        // unmount all the children. componentDidCatch will schedule an update to
        // re-render a fallback. This is temporary until we migrate everyone to
        // the new API.
        // TODO: Warn in a future release.
        nextChildren = null;

        if (enableProfilerTimer) {
          stopProfilerTimerIfRunning(workInProgress);
        }
      } else {
        {
          setCurrentPhase('render');
          nextChildren = instance.render();

          if (debugRenderPhaseSideEffects || debugRenderPhaseSideEffectsForStrictMode && workInProgress.mode & StrictMode) {
            instance.render();
          }

          setCurrentPhase(null);
        }
      } // React DevTools reads this flag.


      workInProgress.effectTag |= PerformedWork;

      if (current$$1 !== null && didCaptureError) {
        // If we're recovering from an error, reconcile without reusing any of
        // the existing children. Conceptually, the normal children and the children
        // that are shown on error are two different sets, so we shouldn't reuse
        // normal children even if their identities match.
        forceUnmountCurrentAndReconcile(current$$1, workInProgress, nextChildren, renderExpirationTime);
      } else {
        reconcileChildren(current$$1, workInProgress, nextChildren, renderExpirationTime);
      } // Memoize state using the values we just used to render.
      // TODO: Restructure so we never read values from the instance.


      workInProgress.memoizedState = instance.state; // The context might have changed so we need to recalculate it.

      if (hasContext) {
        invalidateContextProvider(workInProgress, Component, true);
      }

      return workInProgress.child;
    }

    function pushHostRootContext(workInProgress) {
      var root = workInProgress.stateNode;

      if (root.pendingContext) {
        pushTopLevelContextObject(workInProgress, root.pendingContext, root.pendingContext !== root.context);
      } else if (root.context) {
        // Should always be set
        pushTopLevelContextObject(workInProgress, root.context, false);
      }

      pushHostContainer(workInProgress, root.containerInfo);
    }

    function updateHostRoot(current$$1, workInProgress, renderExpirationTime) {
      pushHostRootContext(workInProgress);
      var updateQueue = workInProgress.updateQueue;

      (function () {
        if (!(updateQueue !== null)) {
          {
            throw ReactError(Error("If the root does not have an updateQueue, we should have already bailed out. This error is likely caused by a bug in React. Please file an issue."));
          }
        }
      })();

      var nextProps = workInProgress.pendingProps;
      var prevState = workInProgress.memoizedState;
      var prevChildren = prevState !== null ? prevState.element : null;
      processUpdateQueue(workInProgress, updateQueue, nextProps, null, renderExpirationTime);
      var nextState = workInProgress.memoizedState; // Caution: React DevTools currently depends on this property
      // being called "element".

      var nextChildren = nextState.element;

      if (nextChildren === prevChildren) {
        // If the state is the same as before, that's a bailout because we had
        // no work that expires at this time.
        resetHydrationState();
        return bailoutOnAlreadyFinishedWork(current$$1, workInProgress, renderExpirationTime);
      }

      var root = workInProgress.stateNode;

      if (root.hydrate && enterHydrationState(workInProgress)) {
        // If we don't have any current children this might be the first pass.
        // We always try to hydrate. If this isn't a hydration pass there won't
        // be any children to hydrate which is effectively the same thing as
        // not hydrating.
        var child = mountChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
        workInProgress.child = child;
        var node = child;

        while (node) {
          // Mark each child as hydrating. This is a fast path to know whether this
          // tree is part of a hydrating tree. This is used to determine if a child
          // node has fully mounted yet, and for scheduling event replaying.
          // Conceptually this is similar to Placement in that a new subtree is
          // inserted into the React tree here. It just happens to not need DOM
          // mutations because it already exists.
          node.effectTag = node.effectTag & ~Placement | Hydrating;
          node = node.sibling;
        }
      } else {
        // Otherwise reset hydration state in case we aborted and resumed another
        // root.
        reconcileChildren(current$$1, workInProgress, nextChildren, renderExpirationTime);
        resetHydrationState();
      }

      return workInProgress.child;
    }

    function updateHostComponent(current$$1, workInProgress, renderExpirationTime) {
      pushHostContext(workInProgress);

      if (current$$1 === null) {
        tryToClaimNextHydratableInstance(workInProgress);
      }

      var type = workInProgress.type;
      var nextProps = workInProgress.pendingProps;
      var prevProps = current$$1 !== null ? current$$1.memoizedProps : null;
      var nextChildren = nextProps.children;
      var isDirectTextChild = shouldSetTextContent(type, nextProps);

      if (isDirectTextChild) {
        // We special case a direct text child of a host node. This is a common
        // case. We won't handle it as a reified child. We will instead handle
        // this in the host environment that also have access to this prop. That
        // avoids allocating another HostText fiber and traversing it.
        nextChildren = null;
      } else if (prevProps !== null && shouldSetTextContent(type, prevProps)) {
        // If we're switching from a direct text child to a normal child, or to
        // empty, we need to schedule the text content to be reset.
        workInProgress.effectTag |= ContentReset;
      }

      markRef(current$$1, workInProgress); // Check the host config to see if the children are offscreen/hidden.

      if (workInProgress.mode & ConcurrentMode && renderExpirationTime !== Never && shouldDeprioritizeSubtree(type, nextProps)) {
        if (enableSchedulerTracing) {
          markSpawnedWork(Never);
        } // Schedule this fiber to re-render at offscreen priority. Then bailout.


        workInProgress.expirationTime = workInProgress.childExpirationTime = Never;
        return null;
      }

      reconcileChildren(current$$1, workInProgress, nextChildren, renderExpirationTime);
      return workInProgress.child;
    }

    function updateHostText(current$$1, workInProgress) {
      if (current$$1 === null) {
        tryToClaimNextHydratableInstance(workInProgress);
      } // Nothing to do here. This is terminal. We'll do the completion step
      // immediately after.


      return null;
    }

    function mountLazyComponent(_current, workInProgress, elementType, updateExpirationTime, renderExpirationTime) {
      if (_current !== null) {
        // An lazy component only mounts if it suspended inside a non-
        // concurrent tree, in an inconsistent state. We want to treat it like
        // a new mount, even though an empty version of it already committed.
        // Disconnect the alternate pointers.
        _current.alternate = null;
        workInProgress.alternate = null; // Since this is conceptually a new fiber, schedule a Placement effect

        workInProgress.effectTag |= Placement;
      }

      var props = workInProgress.pendingProps; // We can't start a User Timing measurement with correct label yet.
      // Cancel and resume right after we know the tag.

      cancelWorkTimer(workInProgress);
      var Component = readLazyComponentType(elementType); // Store the unwrapped component in the type.

      workInProgress.type = Component;
      var resolvedTag = workInProgress.tag = resolveLazyComponentTag(Component);
      startWorkTimer(workInProgress);
      var resolvedProps = resolveDefaultProps(Component, props);
      var child;

      switch (resolvedTag) {
        case FunctionComponent:
          {
            {
              validateFunctionComponentInDev(workInProgress, Component);
              workInProgress.type = Component = resolveFunctionForHotReloading(Component);
            }
            child = updateFunctionComponent(null, workInProgress, Component, resolvedProps, renderExpirationTime);
            break;
          }

        case ClassComponent:
          {
            {
              workInProgress.type = Component = resolveClassForHotReloading(Component);
            }
            child = updateClassComponent(null, workInProgress, Component, resolvedProps, renderExpirationTime);
            break;
          }

        case ForwardRef:
          {
            {
              workInProgress.type = Component = resolveForwardRefForHotReloading(Component);
            }
            child = updateForwardRef(null, workInProgress, Component, resolvedProps, renderExpirationTime);
            break;
          }

        case MemoComponent:
          {
            {
              if (workInProgress.type !== workInProgress.elementType) {
                var outerPropTypes = Component.propTypes;

                if (outerPropTypes) {
                  checkPropTypes(outerPropTypes, resolvedProps, // Resolved for outer only
                  'prop', getComponentName(Component), getCurrentFiberStackInDev);
                }
              }
            }
            child = updateMemoComponent(null, workInProgress, Component, resolveDefaultProps(Component.type, resolvedProps), // The inner type can have defaults too
            updateExpirationTime, renderExpirationTime);
            break;
          }

        default:
          {
            var hint = '';
            {
              if (Component !== null && typeof Component === 'object' && Component.$$typeof === REACT_LAZY_TYPE) {
                hint = ' Did you wrap a component in React.lazy() more than once?';
              }
            } // This message intentionally doesn't mention ForwardRef or MemoComponent
            // because the fact that it's a separate type of work is an
            // implementation detail.

            (function () {
              {
                {
                  throw ReactError(Error("Element type is invalid. Received a promise that resolves to: " + Component + ". Lazy element type must resolve to a class or function." + hint));
                }
              }
            })();
          }
      }

      return child;
    }

    function mountIncompleteClassComponent(_current, workInProgress, Component, nextProps, renderExpirationTime) {
      if (_current !== null) {
        // An incomplete component only mounts if it suspended inside a non-
        // concurrent tree, in an inconsistent state. We want to treat it like
        // a new mount, even though an empty version of it already committed.
        // Disconnect the alternate pointers.
        _current.alternate = null;
        workInProgress.alternate = null; // Since this is conceptually a new fiber, schedule a Placement effect

        workInProgress.effectTag |= Placement;
      } // Promote the fiber to a class and try rendering again.


      workInProgress.tag = ClassComponent; // The rest of this function is a fork of `updateClassComponent`
      // Push context providers early to prevent context stack mismatches.
      // During mounting we don't know the child context yet as the instance doesn't exist.
      // We will invalidate the child context in finishClassComponent() right after rendering.

      var hasContext;

      if (isContextProvider(Component)) {
        hasContext = true;
        pushContextProvider(workInProgress);
      } else {
        hasContext = false;
      }

      prepareToReadContext(workInProgress, renderExpirationTime);
      constructClassInstance(workInProgress, Component, nextProps, renderExpirationTime);
      mountClassInstance(workInProgress, Component, nextProps, renderExpirationTime);
      return finishClassComponent(null, workInProgress, Component, true, hasContext, renderExpirationTime);
    }

    function mountIndeterminateComponent(_current, workInProgress, Component, renderExpirationTime) {
      if (_current !== null) {
        // An indeterminate component only mounts if it suspended inside a non-
        // concurrent tree, in an inconsistent state. We want to treat it like
        // a new mount, even though an empty version of it already committed.
        // Disconnect the alternate pointers.
        _current.alternate = null;
        workInProgress.alternate = null; // Since this is conceptually a new fiber, schedule a Placement effect

        workInProgress.effectTag |= Placement;
      }

      var props = workInProgress.pendingProps;
      var context;

      if (!disableLegacyContext) {
        var unmaskedContext = getUnmaskedContext(workInProgress, Component, false);
        context = getMaskedContext(workInProgress, unmaskedContext);
      }

      prepareToReadContext(workInProgress, renderExpirationTime);
      var value;
      {
        if (Component.prototype && typeof Component.prototype.render === 'function') {
          var componentName = getComponentName(Component) || 'Unknown';

          if (!didWarnAboutBadClass[componentName]) {
            warningWithoutStack$1(false, "The <%s /> component appears to have a render method, but doesn't extend React.Component. " + 'This is likely to cause errors. Change %s to extend React.Component instead.', componentName, componentName);
            didWarnAboutBadClass[componentName] = true;
          }
        }

        if (workInProgress.mode & StrictMode) {
          ReactStrictModeWarnings.recordLegacyContextWarning(workInProgress, null);
        }

        ReactCurrentOwner$3.current = workInProgress;
        value = renderWithHooks(null, workInProgress, Component, props, context, renderExpirationTime);
      } // React DevTools reads this flag.

      workInProgress.effectTag |= PerformedWork;

      if (typeof value === 'object' && value !== null && typeof value.render === 'function' && value.$$typeof === undefined) {
        {
          var _componentName = getComponentName(Component) || 'Unknown';

          if (!didWarnAboutModulePatternComponent[_componentName]) {
            warningWithoutStack$1(false, 'The <%s /> component appears to be a function component that returns a class instance. ' + 'Change %s to a class that extends React.Component instead. ' + "If you can't use a class try assigning the prototype on the function as a workaround. " + "`%s.prototype = React.Component.prototype`. Don't use an arrow function since it " + 'cannot be called with `new` by React.', _componentName, _componentName, _componentName);
            didWarnAboutModulePatternComponent[_componentName] = true;
          }
        } // Proceed under the assumption that this is a class instance

        workInProgress.tag = ClassComponent; // Throw out any hooks that were used.

        resetHooks(); // Push context providers early to prevent context stack mismatches.
        // During mounting we don't know the child context yet as the instance doesn't exist.
        // We will invalidate the child context in finishClassComponent() right after rendering.

        var hasContext = false;

        if (isContextProvider(Component)) {
          hasContext = true;
          pushContextProvider(workInProgress);
        } else {
          hasContext = false;
        }

        workInProgress.memoizedState = value.state !== null && value.state !== undefined ? value.state : null;
        var getDerivedStateFromProps = Component.getDerivedStateFromProps;

        if (typeof getDerivedStateFromProps === 'function') {
          applyDerivedStateFromProps(workInProgress, Component, getDerivedStateFromProps, props);
        }

        adoptClassInstance(workInProgress, value);
        mountClassInstance(workInProgress, Component, props, renderExpirationTime);
        return finishClassComponent(null, workInProgress, Component, true, hasContext, renderExpirationTime);
      } else {
        // Proceed under the assumption that this is a function component
        workInProgress.tag = FunctionComponent;
        {
          if (disableLegacyContext && Component.contextTypes) {
            warningWithoutStack$1(false, '%s uses the legacy contextTypes API which is no longer supported. ' + 'Use React.createContext() with React.useContext() instead.', getComponentName(Component) || 'Unknown');
          }

          if (debugRenderPhaseSideEffects || debugRenderPhaseSideEffectsForStrictMode && workInProgress.mode & StrictMode) {
            // Only double-render components with Hooks
            if (workInProgress.memoizedState !== null) {
              value = renderWithHooks(null, workInProgress, Component, props, context, renderExpirationTime);
            }
          }
        }
        reconcileChildren(null, workInProgress, value, renderExpirationTime);
        {
          validateFunctionComponentInDev(workInProgress, Component);
        }
        return workInProgress.child;
      }
    }

    function validateFunctionComponentInDev(workInProgress, Component) {
      if (Component) {
        !!Component.childContextTypes ? warningWithoutStack$1(false, '%s(...): childContextTypes cannot be defined on a function component.', Component.displayName || Component.name || 'Component') : void 0;
      }

      if (workInProgress.ref !== null) {
        var info = '';
        var ownerName = getCurrentFiberOwnerNameInDevOrNull();

        if (ownerName) {
          info += '\n\nCheck the render method of `' + ownerName + '`.';
        }

        var warningKey = ownerName || workInProgress._debugID || '';
        var debugSource = workInProgress._debugSource;

        if (debugSource) {
          warningKey = debugSource.fileName + ':' + debugSource.lineNumber;
        }

        if (!didWarnAboutFunctionRefs[warningKey]) {
          didWarnAboutFunctionRefs[warningKey] = true;
          warning$1(false, 'Function components cannot be given refs. ' + 'Attempts to access this ref will fail. ' + 'Did you mean to use React.forwardRef()?%s', info);
        }
      }

      if (warnAboutDefaultPropsOnFunctionComponents && Component.defaultProps !== undefined) {
        var componentName = getComponentName(Component) || 'Unknown';

        if (!didWarnAboutDefaultPropsOnFunctionComponent[componentName]) {
          warningWithoutStack$1(false, '%s: Support for defaultProps will be removed from function components ' + 'in a future major release. Use JavaScript default parameters instead.', componentName);
          didWarnAboutDefaultPropsOnFunctionComponent[componentName] = true;
        }
      }

      if (typeof Component.getDerivedStateFromProps === 'function') {
        var _componentName2 = getComponentName(Component) || 'Unknown';

        if (!didWarnAboutGetDerivedStateOnFunctionComponent[_componentName2]) {
          warningWithoutStack$1(false, '%s: Function components do not support getDerivedStateFromProps.', _componentName2);
          didWarnAboutGetDerivedStateOnFunctionComponent[_componentName2] = true;
        }
      }

      if (typeof Component.contextType === 'object' && Component.contextType !== null) {
        var _componentName3 = getComponentName(Component) || 'Unknown';

        if (!didWarnAboutContextTypeOnFunctionComponent[_componentName3]) {
          warningWithoutStack$1(false, '%s: Function components do not support contextType.', _componentName3);
          didWarnAboutContextTypeOnFunctionComponent[_componentName3] = true;
        }
      }
    }

    var SUSPENDED_MARKER = {
      dehydrated: null,
      retryTime: Never
    };

    function shouldRemainOnFallback(suspenseContext, current$$1, workInProgress) {
      // If the context is telling us that we should show a fallback, and we're not
      // already showing content, then we should show the fallback instead.
      return hasSuspenseContext(suspenseContext, ForceSuspenseFallback) && (current$$1 === null || current$$1.memoizedState !== null);
    }

    function updateSuspenseComponent(current$$1, workInProgress, renderExpirationTime) {
      var mode = workInProgress.mode;
      var nextProps = workInProgress.pendingProps; // This is used by DevTools to force a boundary to suspend.

      {
        if (shouldSuspend(workInProgress)) {
          workInProgress.effectTag |= DidCapture;
        }
      }
      var suspenseContext = suspenseStackCursor.current;
      var nextDidTimeout = false;
      var didSuspend = (workInProgress.effectTag & DidCapture) !== NoEffect;

      if (didSuspend || shouldRemainOnFallback(suspenseContext, current$$1, workInProgress)) {
        // Something in this boundary's subtree already suspended. Switch to
        // rendering the fallback children.
        nextDidTimeout = true;
        workInProgress.effectTag &= ~DidCapture;
      } else {
        // Attempting the main content
        if (current$$1 === null || current$$1.memoizedState !== null) {
          // This is a new mount or this boundary is already showing a fallback state.
          // Mark this subtree context as having at least one invisible parent that could
          // handle the fallback state.
          // Boundaries without fallbacks or should be avoided are not considered since
          // they cannot handle preferred fallback states.
          if (nextProps.fallback !== undefined && nextProps.unstable_avoidThisFallback !== true) {
            suspenseContext = addSubtreeSuspenseContext(suspenseContext, InvisibleParentSuspenseContext);
          }
        }
      }

      suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
      pushSuspenseContext(workInProgress, suspenseContext);
      {
        if ('maxDuration' in nextProps) {
          if (!didWarnAboutMaxDuration) {
            didWarnAboutMaxDuration = true;
            warning$1(false, 'maxDuration has been removed from React. ' + 'Remove the maxDuration prop.');
          }
        }
      } // This next part is a bit confusing. If the children timeout, we switch to
      // showing the fallback children in place of the "primary" children.
      // However, we don't want to delete the primary children because then their
      // state will be lost (both the React state and the host state, e.g.
      // uncontrolled form inputs). Instead we keep them mounted and hide them.
      // Both the fallback children AND the primary children are rendered at the
      // same time. Once the primary children are un-suspended, we can delete
      // the fallback children — don't need to preserve their state.
      //
      // The two sets of children are siblings in the host environment, but
      // semantically, for purposes of reconciliation, they are two separate sets.
      // So we store them using two fragment fibers.
      //
      // However, we want to avoid allocating extra fibers for every placeholder.
      // They're only necessary when the children time out, because that's the
      // only time when both sets are mounted.
      //
      // So, the extra fragment fibers are only used if the children time out.
      // Otherwise, we render the primary children directly. This requires some
      // custom reconciliation logic to preserve the state of the primary
      // children. It's essentially a very basic form of re-parenting.

      if (current$$1 === null) {
        if (enableSuspenseServerRenderer) {
          // If we're currently hydrating, try to hydrate this boundary.
          // But only if this has a fallback.
          if (nextProps.fallback !== undefined) {
            tryToClaimNextHydratableInstance(workInProgress); // This could've been a dehydrated suspense component.

            var suspenseState = workInProgress.memoizedState;

            if (suspenseState !== null) {
              var dehydrated = suspenseState.dehydrated;

              if (dehydrated !== null) {
                return mountDehydratedSuspenseComponent(workInProgress, dehydrated, renderExpirationTime);
              }
            }
          }
        } // This is the initial mount. This branch is pretty simple because there's
        // no previous state that needs to be preserved.


        if (nextDidTimeout) {
          // Mount separate fragments for primary and fallback children.
          var nextFallbackChildren = nextProps.fallback;
          var primaryChildFragment = createFiberFromFragment(null, mode, NoWork, null);
          primaryChildFragment.return = workInProgress;

          if ((workInProgress.mode & BatchedMode) === NoMode) {
            // Outside of batched mode, we commit the effects from the
            // partially completed, timed-out tree, too.
            var progressedState = workInProgress.memoizedState;
            var progressedPrimaryChild = progressedState !== null ? workInProgress.child.child : workInProgress.child;
            primaryChildFragment.child = progressedPrimaryChild;
            var progressedChild = progressedPrimaryChild;

            while (progressedChild !== null) {
              progressedChild.return = primaryChildFragment;
              progressedChild = progressedChild.sibling;
            }
          }

          var fallbackChildFragment = createFiberFromFragment(nextFallbackChildren, mode, renderExpirationTime, null);
          fallbackChildFragment.return = workInProgress;
          primaryChildFragment.sibling = fallbackChildFragment; // Skip the primary children, and continue working on the
          // fallback children.

          workInProgress.memoizedState = SUSPENDED_MARKER;
          workInProgress.child = primaryChildFragment;
          return fallbackChildFragment;
        } else {
          // Mount the primary children without an intermediate fragment fiber.
          var nextPrimaryChildren = nextProps.children;
          workInProgress.memoizedState = null;
          return workInProgress.child = mountChildFibers(workInProgress, null, nextPrimaryChildren, renderExpirationTime);
        }
      } else {
        // This is an update. This branch is more complicated because we need to
        // ensure the state of the primary children is preserved.
        var prevState = current$$1.memoizedState;

        if (prevState !== null) {
          if (enableSuspenseServerRenderer) {
            var _dehydrated = prevState.dehydrated;

            if (_dehydrated !== null) {
              if (!didSuspend) {
                return updateDehydratedSuspenseComponent(current$$1, workInProgress, _dehydrated, prevState, renderExpirationTime);
              } else if (workInProgress.memoizedState !== null) {
                // Something suspended and we should still be in dehydrated mode.
                // Leave the existing child in place.
                workInProgress.child = current$$1.child; // The dehydrated completion pass expects this flag to be there
                // but the normal suspense pass doesn't.

                workInProgress.effectTag |= DidCapture;
                return null;
              } else {
                // Suspended but we should no longer be in dehydrated mode.
                // Therefore we now have to render the fallback. Wrap the children
                // in a fragment fiber to keep them separate from the fallback
                // children.
                var _nextFallbackChildren = nextProps.fallback;

                var _primaryChildFragment = createFiberFromFragment( // It shouldn't matter what the pending props are because we aren't
                // going to render this fragment.
                null, mode, NoWork, null);

                _primaryChildFragment.return = workInProgress; // This is always null since we never want the previous child
                // that we're not going to hydrate.

                _primaryChildFragment.child = null;

                if ((workInProgress.mode & BatchedMode) === NoMode) {
                  // Outside of batched mode, we commit the effects from the
                  // partially completed, timed-out tree, too.
                  var _progressedChild = _primaryChildFragment.child = workInProgress.child;

                  while (_progressedChild !== null) {
                    _progressedChild.return = _primaryChildFragment;
                    _progressedChild = _progressedChild.sibling;
                  }
                } else {
                  // We will have dropped the effect list which contains the deletion.
                  // We need to reconcile to delete the current child.
                  reconcileChildFibers(workInProgress, current$$1.child, null, renderExpirationTime);
                } // Because primaryChildFragment is a new fiber that we're inserting as the
                // parent of a new tree, we need to set its treeBaseDuration.


                if (enableProfilerTimer && workInProgress.mode & ProfileMode) {
                  // treeBaseDuration is the sum of all the child tree base durations.
                  var treeBaseDuration = 0;
                  var hiddenChild = _primaryChildFragment.child;

                  while (hiddenChild !== null) {
                    treeBaseDuration += hiddenChild.treeBaseDuration;
                    hiddenChild = hiddenChild.sibling;
                  }

                  _primaryChildFragment.treeBaseDuration = treeBaseDuration;
                } // Create a fragment from the fallback children, too.


                var _fallbackChildFragment = createFiberFromFragment(_nextFallbackChildren, mode, renderExpirationTime, null);

                _fallbackChildFragment.return = workInProgress;
                _primaryChildFragment.sibling = _fallbackChildFragment;
                _fallbackChildFragment.effectTag |= Placement;
                _primaryChildFragment.childExpirationTime = NoWork;
                workInProgress.memoizedState = SUSPENDED_MARKER;
                workInProgress.child = _primaryChildFragment; // Skip the primary children, and continue working on the
                // fallback children.

                return _fallbackChildFragment;
              }
            }
          } // The current tree already timed out. That means each child set is
          // wrapped in a fragment fiber.


          var currentPrimaryChildFragment = current$$1.child;
          var currentFallbackChildFragment = currentPrimaryChildFragment.sibling;

          if (nextDidTimeout) {
            // Still timed out. Reuse the current primary children by cloning
            // its fragment. We're going to skip over these entirely.
            var _nextFallbackChildren2 = nextProps.fallback;

            var _primaryChildFragment2 = createWorkInProgress(currentPrimaryChildFragment, currentPrimaryChildFragment.pendingProps, NoWork);

            _primaryChildFragment2.return = workInProgress;

            if ((workInProgress.mode & BatchedMode) === NoMode) {
              // Outside of batched mode, we commit the effects from the
              // partially completed, timed-out tree, too.
              var _progressedState = workInProgress.memoizedState;

              var _progressedPrimaryChild = _progressedState !== null ? workInProgress.child.child : workInProgress.child;

              if (_progressedPrimaryChild !== currentPrimaryChildFragment.child) {
                _primaryChildFragment2.child = _progressedPrimaryChild;
                var _progressedChild2 = _progressedPrimaryChild;

                while (_progressedChild2 !== null) {
                  _progressedChild2.return = _primaryChildFragment2;
                  _progressedChild2 = _progressedChild2.sibling;
                }
              }
            } // Because primaryChildFragment is a new fiber that we're inserting as the
            // parent of a new tree, we need to set its treeBaseDuration.


            if (enableProfilerTimer && workInProgress.mode & ProfileMode) {
              // treeBaseDuration is the sum of all the child tree base durations.
              var _treeBaseDuration = 0;
              var _hiddenChild = _primaryChildFragment2.child;

              while (_hiddenChild !== null) {
                _treeBaseDuration += _hiddenChild.treeBaseDuration;
                _hiddenChild = _hiddenChild.sibling;
              }

              _primaryChildFragment2.treeBaseDuration = _treeBaseDuration;
            } // Clone the fallback child fragment, too. These we'll continue
            // working on.


            var _fallbackChildFragment2 = createWorkInProgress(currentFallbackChildFragment, _nextFallbackChildren2, currentFallbackChildFragment.expirationTime);

            _fallbackChildFragment2.return = workInProgress;
            _primaryChildFragment2.sibling = _fallbackChildFragment2;
            _primaryChildFragment2.childExpirationTime = NoWork; // Skip the primary children, and continue working on the
            // fallback children.

            workInProgress.memoizedState = SUSPENDED_MARKER;
            workInProgress.child = _primaryChildFragment2;
            return _fallbackChildFragment2;
          } else {
            // No longer suspended. Switch back to showing the primary children,
            // and remove the intermediate fragment fiber.
            var _nextPrimaryChildren = nextProps.children;
            var currentPrimaryChild = currentPrimaryChildFragment.child;
            var primaryChild = reconcileChildFibers(workInProgress, currentPrimaryChild, _nextPrimaryChildren, renderExpirationTime); // If this render doesn't suspend, we need to delete the fallback
            // children. Wait until the complete phase, after we've confirmed the
            // fallback is no longer needed.
            // TODO: Would it be better to store the fallback fragment on
            // the stateNode?
            // Continue rendering the children, like we normally do.

            workInProgress.memoizedState = null;
            return workInProgress.child = primaryChild;
          }
        } else {
          // The current tree has not already timed out. That means the primary
          // children are not wrapped in a fragment fiber.
          var _currentPrimaryChild = current$$1.child;

          if (nextDidTimeout) {
            // Timed out. Wrap the children in a fragment fiber to keep them
            // separate from the fallback children.
            var _nextFallbackChildren3 = nextProps.fallback;

            var _primaryChildFragment3 = createFiberFromFragment( // It shouldn't matter what the pending props are because we aren't
            // going to render this fragment.
            null, mode, NoWork, null);

            _primaryChildFragment3.return = workInProgress;
            _primaryChildFragment3.child = _currentPrimaryChild;

            if (_currentPrimaryChild !== null) {
              _currentPrimaryChild.return = _primaryChildFragment3;
            } // Even though we're creating a new fiber, there are no new children,
            // because we're reusing an already mounted tree. So we don't need to
            // schedule a placement.
            // primaryChildFragment.effectTag |= Placement;


            if ((workInProgress.mode & BatchedMode) === NoMode) {
              // Outside of batched mode, we commit the effects from the
              // partially completed, timed-out tree, too.
              var _progressedState2 = workInProgress.memoizedState;

              var _progressedPrimaryChild2 = _progressedState2 !== null ? workInProgress.child.child : workInProgress.child;

              _primaryChildFragment3.child = _progressedPrimaryChild2;
              var _progressedChild3 = _progressedPrimaryChild2;

              while (_progressedChild3 !== null) {
                _progressedChild3.return = _primaryChildFragment3;
                _progressedChild3 = _progressedChild3.sibling;
              }
            } // Because primaryChildFragment is a new fiber that we're inserting as the
            // parent of a new tree, we need to set its treeBaseDuration.


            if (enableProfilerTimer && workInProgress.mode & ProfileMode) {
              // treeBaseDuration is the sum of all the child tree base durations.
              var _treeBaseDuration2 = 0;
              var _hiddenChild2 = _primaryChildFragment3.child;

              while (_hiddenChild2 !== null) {
                _treeBaseDuration2 += _hiddenChild2.treeBaseDuration;
                _hiddenChild2 = _hiddenChild2.sibling;
              }

              _primaryChildFragment3.treeBaseDuration = _treeBaseDuration2;
            } // Create a fragment from the fallback children, too.


            var _fallbackChildFragment3 = createFiberFromFragment(_nextFallbackChildren3, mode, renderExpirationTime, null);

            _fallbackChildFragment3.return = workInProgress;
            _primaryChildFragment3.sibling = _fallbackChildFragment3;
            _fallbackChildFragment3.effectTag |= Placement;
            _primaryChildFragment3.childExpirationTime = NoWork; // Skip the primary children, and continue working on the
            // fallback children.

            workInProgress.memoizedState = SUSPENDED_MARKER;
            workInProgress.child = _primaryChildFragment3;
            return _fallbackChildFragment3;
          } else {
            // Still haven't timed out.  Continue rendering the children, like we
            // normally do.
            workInProgress.memoizedState = null;
            var _nextPrimaryChildren2 = nextProps.children;
            return workInProgress.child = reconcileChildFibers(workInProgress, _currentPrimaryChild, _nextPrimaryChildren2, renderExpirationTime);
          }
        }
      }
    }

    function retrySuspenseComponentWithoutHydrating(current$$1, workInProgress, renderExpirationTime) {
      // We're now not suspended nor dehydrated.
      workInProgress.memoizedState = null; // Retry with the full children.

      var nextProps = workInProgress.pendingProps;
      var nextChildren = nextProps.children; // This will ensure that the children get Placement effects and
      // that the old child gets a Deletion effect.
      // We could also call forceUnmountCurrentAndReconcile.

      reconcileChildren(current$$1, workInProgress, nextChildren, renderExpirationTime);
      return workInProgress.child;
    }

    function mountDehydratedSuspenseComponent(workInProgress, suspenseInstance, renderExpirationTime) {
      // During the first pass, we'll bail out and not drill into the children.
      // Instead, we'll leave the content in place and try to hydrate it later.
      if ((workInProgress.mode & BatchedMode) === NoMode) {
        {
          warning$1(false, 'Cannot hydrate Suspense in legacy mode. Switch from ' + 'ReactDOM.hydrate(element, container) to ' + 'ReactDOM.unstable_createSyncRoot(container, { hydrate: true })' + '.render(element) or remove the Suspense components from ' + 'the server rendered components.');
        }
        workInProgress.expirationTime = Sync;
      } else if (isSuspenseInstanceFallback(suspenseInstance)) {
        // This is a client-only boundary. Since we won't get any content from the server
        // for this, we need to schedule that at a higher priority based on when it would
        // have timed out. In theory we could render it in this pass but it would have the
        // wrong priority associated with it and will prevent hydration of parent path.
        // Instead, we'll leave work left on it to render it in a separate commit.
        // TODO This time should be the time at which the server rendered response that is
        // a parent to this boundary was displayed. However, since we currently don't have
        // a protocol to transfer that time, we'll just estimate it by using the current
        // time. This will mean that Suspense timeouts are slightly shifted to later than
        // they should be.
        var serverDisplayTime = requestCurrentTime(); // Schedule a normal pri update to render this content.

        var newExpirationTime = computeAsyncExpiration(serverDisplayTime);

        if (enableSchedulerTracing) {
          markSpawnedWork(newExpirationTime);
        }

        workInProgress.expirationTime = newExpirationTime;
      } else {
        // We'll continue hydrating the rest at offscreen priority since we'll already
        // be showing the right content coming from the server, it is no rush.
        workInProgress.expirationTime = Never;

        if (enableSchedulerTracing) {
          markSpawnedWork(Never);
        }
      }

      return null;
    }

    function updateDehydratedSuspenseComponent(current$$1, workInProgress, suspenseInstance, suspenseState, renderExpirationTime) {
      // We should never be hydrating at this point because it is the first pass,
      // but after we've already committed once.
      warnIfHydrating();

      if ((workInProgress.mode & BatchedMode) === NoMode) {
        return retrySuspenseComponentWithoutHydrating(current$$1, workInProgress, renderExpirationTime);
      }

      if (isSuspenseInstanceFallback(suspenseInstance)) {
        // This boundary is in a permanent fallback state. In this case, we'll never
        // get an update and we'll never be able to hydrate the final content. Let's just try the
        // client side render instead.
        return retrySuspenseComponentWithoutHydrating(current$$1, workInProgress, renderExpirationTime);
      } // We use childExpirationTime to indicate that a child might depend on context, so if
      // any context has changed, we need to treat is as if the input might have changed.


      var hasContextChanged$$1 = current$$1.childExpirationTime >= renderExpirationTime;

      if (didReceiveUpdate || hasContextChanged$$1) {
        // This boundary has changed since the first render. This means that we are now unable to
        // hydrate it. We might still be able to hydrate it using an earlier expiration time, if
        // we are rendering at lower expiration than sync.
        if (renderExpirationTime < Sync) {
          if (suspenseState.retryTime <= renderExpirationTime) {
            // This render is even higher pri than we've seen before, let's try again
            // at even higher pri.
            var attemptHydrationAtExpirationTime = renderExpirationTime + 1;
            suspenseState.retryTime = attemptHydrationAtExpirationTime;
            scheduleWork(current$$1, attemptHydrationAtExpirationTime); // TODO: Early abort this render.
          } else {// We have already tried to ping at a higher priority than we're rendering with
              // so if we got here, we must have failed to hydrate at those levels. We must
              // now give up. Instead, we're going to delete the whole subtree and instead inject
              // a new real Suspense boundary to take its place, which may render content
              // or fallback. This might suspend for a while and if it does we might still have
              // an opportunity to hydrate before this pass commits.
            }
        } // If we have scheduled higher pri work above, this will probably just abort the render
        // since we now have higher priority work, but in case it doesn't, we need to prepare to
        // render something, if we time out. Even if that requires us to delete everything and
        // skip hydration.
        // Delay having to do this as long as the suspense timeout allows us.


        renderDidSuspendDelayIfPossible();
        return retrySuspenseComponentWithoutHydrating(current$$1, workInProgress, renderExpirationTime);
      } else if (isSuspenseInstancePending(suspenseInstance)) {
        // This component is still pending more data from the server, so we can't hydrate its
        // content. We treat it as if this component suspended itself. It might seem as if
        // we could just try to render it client-side instead. However, this will perform a
        // lot of unnecessary work and is unlikely to complete since it often will suspend
        // on missing data anyway. Additionally, the server might be able to render more
        // than we can on the client yet. In that case we'd end up with more fallback states
        // on the client than if we just leave it alone. If the server times out or errors
        // these should update this boundary to the permanent Fallback state instead.
        // Mark it as having captured (i.e. suspended).
        workInProgress.effectTag |= DidCapture; // Leave the child in place. I.e. the dehydrated fragment.

        workInProgress.child = current$$1.child; // Register a callback to retry this boundary once the server has sent the result.

        registerSuspenseInstanceRetry(suspenseInstance, retryDehydratedSuspenseBoundary.bind(null, current$$1));
        return null;
      } else {
        // This is the first attempt.
        reenterHydrationStateFromDehydratedSuspenseInstance(workInProgress, suspenseInstance);
        var nextProps = workInProgress.pendingProps;
        var nextChildren = nextProps.children;
        var child = mountChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
        var node = child;

        while (node) {
          // Mark each child as hydrating. This is a fast path to know whether this
          // tree is part of a hydrating tree. This is used to determine if a child
          // node has fully mounted yet, and for scheduling event replaying.
          // Conceptually this is similar to Placement in that a new subtree is
          // inserted into the React tree here. It just happens to not need DOM
          // mutations because it already exists.
          node.effectTag |= Hydrating;
          node = node.sibling;
        }

        workInProgress.child = child;
        return workInProgress.child;
      }
    }

    function propagateSuspenseContextChange(workInProgress, firstChild, renderExpirationTime) {
      // Mark any Suspense boundaries with fallbacks as having work to do.
      // If they were previously forced into fallbacks, they may now be able
      // to unblock.
      var node = firstChild;

      while (node !== null) {
        if (node.tag === SuspenseComponent) {
          var state = node.memoizedState;

          if (state !== null) {
            if (node.expirationTime < renderExpirationTime) {
              node.expirationTime = renderExpirationTime;
            }

            var alternate = node.alternate;

            if (alternate !== null && alternate.expirationTime < renderExpirationTime) {
              alternate.expirationTime = renderExpirationTime;
            }

            scheduleWorkOnParentPath(node.return, renderExpirationTime);
          }
        } else if (node.child !== null) {
          node.child.return = node;
          node = node.child;
          continue;
        }

        if (node === workInProgress) {
          return;
        }

        while (node.sibling === null) {
          if (node.return === null || node.return === workInProgress) {
            return;
          }

          node = node.return;
        }

        node.sibling.return = node.return;
        node = node.sibling;
      }
    }

    function findLastContentRow(firstChild) {
      // This is going to find the last row among these children that is already
      // showing content on the screen, as opposed to being in fallback state or
      // new. If a row has multiple Suspense boundaries, any of them being in the
      // fallback state, counts as the whole row being in a fallback state.
      // Note that the "rows" will be workInProgress, but any nested children
      // will still be current since we haven't rendered them yet. The mounted
      // order may not be the same as the new order. We use the new order.
      var row = firstChild;
      var lastContentRow = null;

      while (row !== null) {
        var currentRow = row.alternate; // New rows can't be content rows.

        if (currentRow !== null && findFirstSuspended(currentRow) === null) {
          lastContentRow = row;
        }

        row = row.sibling;
      }

      return lastContentRow;
    }

    function validateRevealOrder(revealOrder) {
      {
        if (revealOrder !== undefined && revealOrder !== 'forwards' && revealOrder !== 'backwards' && revealOrder !== 'together' && !didWarnAboutRevealOrder[revealOrder]) {
          didWarnAboutRevealOrder[revealOrder] = true;

          if (typeof revealOrder === 'string') {
            switch (revealOrder.toLowerCase()) {
              case 'together':
              case 'forwards':
              case 'backwards':
                {
                  warning$1(false, '"%s" is not a valid value for revealOrder on <SuspenseList />. ' + 'Use lowercase "%s" instead.', revealOrder, revealOrder.toLowerCase());
                  break;
                }

              case 'forward':
              case 'backward':
                {
                  warning$1(false, '"%s" is not a valid value for revealOrder on <SuspenseList />. ' + 'React uses the -s suffix in the spelling. Use "%ss" instead.', revealOrder, revealOrder.toLowerCase());
                  break;
                }

              default:
                warning$1(false, '"%s" is not a supported revealOrder on <SuspenseList />. ' + 'Did you mean "together", "forwards" or "backwards"?', revealOrder);
                break;
            }
          } else {
            warning$1(false, '%s is not a supported value for revealOrder on <SuspenseList />. ' + 'Did you mean "together", "forwards" or "backwards"?', revealOrder);
          }
        }
      }
    }

    function validateTailOptions(tailMode, revealOrder) {
      {
        if (tailMode !== undefined && !didWarnAboutTailOptions[tailMode]) {
          if (tailMode !== 'collapsed' && tailMode !== 'hidden') {
            didWarnAboutTailOptions[tailMode] = true;
            warning$1(false, '"%s" is not a supported value for tail on <SuspenseList />. ' + 'Did you mean "collapsed" or "hidden"?', tailMode);
          } else if (revealOrder !== 'forwards' && revealOrder !== 'backwards') {
            didWarnAboutTailOptions[tailMode] = true;
            warning$1(false, '<SuspenseList tail="%s" /> is only valid if revealOrder is ' + '"forwards" or "backwards". ' + 'Did you mean to specify revealOrder="forwards"?', tailMode);
          }
        }
      }
    }

    function validateSuspenseListNestedChild(childSlot, index) {
      {
        var isArray = Array.isArray(childSlot);
        var isIterable = !isArray && typeof getIteratorFn(childSlot) === 'function';

        if (isArray || isIterable) {
          var type = isArray ? 'array' : 'iterable';
          warning$1(false, 'A nested %s was passed to row #%s in <SuspenseList />. Wrap it in ' + 'an additional SuspenseList to configure its revealOrder: ' + '<SuspenseList revealOrder=...> ... ' + '<SuspenseList revealOrder=...>{%s}</SuspenseList> ... ' + '</SuspenseList>', type, index, type);
          return false;
        }
      }
      return true;
    }

    function validateSuspenseListChildren(children, revealOrder) {
      {
        if ((revealOrder === 'forwards' || revealOrder === 'backwards') && children !== undefined && children !== null && children !== false) {
          if (Array.isArray(children)) {
            for (var i = 0; i < children.length; i++) {
              if (!validateSuspenseListNestedChild(children[i], i)) {
                return;
              }
            }
          } else {
            var iteratorFn = getIteratorFn(children);

            if (typeof iteratorFn === 'function') {
              var childrenIterator = iteratorFn.call(children);

              if (childrenIterator) {
                var step = childrenIterator.next();
                var _i = 0;

                for (; !step.done; step = childrenIterator.next()) {
                  if (!validateSuspenseListNestedChild(step.value, _i)) {
                    return;
                  }

                  _i++;
                }
              }
            } else {
              warning$1(false, 'A single row was passed to a <SuspenseList revealOrder="%s" />. ' + 'This is not useful since it needs multiple rows. ' + 'Did you mean to pass multiple children or an array?', revealOrder);
            }
          }
        }
      }
    }

    function initSuspenseListRenderState(workInProgress, isBackwards, tail, lastContentRow, tailMode) {
      var renderState = workInProgress.memoizedState;

      if (renderState === null) {
        workInProgress.memoizedState = {
          isBackwards: isBackwards,
          rendering: null,
          last: lastContentRow,
          tail: tail,
          tailExpiration: 0,
          tailMode: tailMode
        };
      } else {
        // We can reuse the existing object from previous renders.
        renderState.isBackwards = isBackwards;
        renderState.rendering = null;
        renderState.last = lastContentRow;
        renderState.tail = tail;
        renderState.tailExpiration = 0;
        renderState.tailMode = tailMode;
      }
    } // This can end up rendering this component multiple passes.
    // The first pass splits the children fibers into two sets. A head and tail.
    // We first render the head. If anything is in fallback state, we do another
    // pass through beginWork to rerender all children (including the tail) with
    // the force suspend context. If the first render didn't have anything in
    // in fallback state. Then we render each row in the tail one-by-one.
    // That happens in the completeWork phase without going back to beginWork.


    function updateSuspenseListComponent(current$$1, workInProgress, renderExpirationTime) {
      var nextProps = workInProgress.pendingProps;
      var revealOrder = nextProps.revealOrder;
      var tailMode = nextProps.tail;
      var newChildren = nextProps.children;
      validateRevealOrder(revealOrder);
      validateTailOptions(tailMode, revealOrder);
      validateSuspenseListChildren(newChildren, revealOrder);
      reconcileChildren(current$$1, workInProgress, newChildren, renderExpirationTime);
      var suspenseContext = suspenseStackCursor.current;
      var shouldForceFallback = hasSuspenseContext(suspenseContext, ForceSuspenseFallback);

      if (shouldForceFallback) {
        suspenseContext = setShallowSuspenseContext(suspenseContext, ForceSuspenseFallback);
        workInProgress.effectTag |= DidCapture;
      } else {
        var didSuspendBefore = current$$1 !== null && (current$$1.effectTag & DidCapture) !== NoEffect;

        if (didSuspendBefore) {
          // If we previously forced a fallback, we need to schedule work
          // on any nested boundaries to let them know to try to render
          // again. This is the same as context updating.
          propagateSuspenseContextChange(workInProgress, workInProgress.child, renderExpirationTime);
        }

        suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
      }

      pushSuspenseContext(workInProgress, suspenseContext);

      if ((workInProgress.mode & BatchedMode) === NoMode) {
        // Outside of batched mode, SuspenseList doesn't work so we just
        // use make it a noop by treating it as the default revealOrder.
        workInProgress.memoizedState = null;
      } else {
        switch (revealOrder) {
          case 'forwards':
            {
              var lastContentRow = findLastContentRow(workInProgress.child);
              var tail;

              if (lastContentRow === null) {
                // The whole list is part of the tail.
                // TODO: We could fast path by just rendering the tail now.
                tail = workInProgress.child;
                workInProgress.child = null;
              } else {
                // Disconnect the tail rows after the content row.
                // We're going to render them separately later.
                tail = lastContentRow.sibling;
                lastContentRow.sibling = null;
              }

              initSuspenseListRenderState(workInProgress, false, // isBackwards
              tail, lastContentRow, tailMode);
              break;
            }

          case 'backwards':
            {
              // We're going to find the first row that has existing content.
              // At the same time we're going to reverse the list of everything
              // we pass in the meantime. That's going to be our tail in reverse
              // order.
              var _tail = null;
              var row = workInProgress.child;
              workInProgress.child = null;

              while (row !== null) {
                var currentRow = row.alternate; // New rows can't be content rows.

                if (currentRow !== null && findFirstSuspended(currentRow) === null) {
                  // This is the beginning of the main content.
                  workInProgress.child = row;
                  break;
                }

                var nextRow = row.sibling;
                row.sibling = _tail;
                _tail = row;
                row = nextRow;
              } // TODO: If workInProgress.child is null, we can continue on the tail immediately.


              initSuspenseListRenderState(workInProgress, true, // isBackwards
              _tail, null, // last
              tailMode);
              break;
            }

          case 'together':
            {
              initSuspenseListRenderState(workInProgress, false, // isBackwards
              null, // tail
              null, // last
              undefined);
              break;
            }

          default:
            {
              // The default reveal order is the same as not having
              // a boundary.
              workInProgress.memoizedState = null;
            }
        }
      }

      return workInProgress.child;
    }

    function updatePortalComponent(current$$1, workInProgress, renderExpirationTime) {
      pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
      var nextChildren = workInProgress.pendingProps;

      if (current$$1 === null) {
        // Portals are special because we don't append the children during mount
        // but at commit. Therefore we need to track insertions which the normal
        // flow doesn't do during mount. This doesn't happen at the root because
        // the root always starts with a "current" with a null child.
        // TODO: Consider unifying this with how the root works.
        workInProgress.child = reconcileChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
      } else {
        reconcileChildren(current$$1, workInProgress, nextChildren, renderExpirationTime);
      }

      return workInProgress.child;
    }

    function updateContextProvider(current$$1, workInProgress, renderExpirationTime) {
      var providerType = workInProgress.type;
      var context = providerType._context;
      var newProps = workInProgress.pendingProps;
      var oldProps = workInProgress.memoizedProps;
      var newValue = newProps.value;
      {
        var providerPropTypes = workInProgress.type.propTypes;

        if (providerPropTypes) {
          checkPropTypes(providerPropTypes, newProps, 'prop', 'Context.Provider', getCurrentFiberStackInDev);
        }
      }
      pushProvider(workInProgress, newValue);

      if (oldProps !== null) {
        var oldValue = oldProps.value;
        var changedBits = calculateChangedBits(context, newValue, oldValue);

        if (changedBits === 0) {
          // No change. Bailout early if children are the same.
          if (oldProps.children === newProps.children && !hasContextChanged()) {
            return bailoutOnAlreadyFinishedWork(current$$1, workInProgress, renderExpirationTime);
          }
        } else {
          // The context value changed. Search for matching consumers and schedule
          // them to update.
          propagateContextChange(workInProgress, context, changedBits, renderExpirationTime);
        }
      }

      var newChildren = newProps.children;
      reconcileChildren(current$$1, workInProgress, newChildren, renderExpirationTime);
      return workInProgress.child;
    }

    var hasWarnedAboutUsingContextAsConsumer = false;

    function updateContextConsumer(current$$1, workInProgress, renderExpirationTime) {
      var context = workInProgress.type; // The logic below for Context differs depending on PROD or DEV mode. In
      // DEV mode, we create a separate object for Context.Consumer that acts
      // like a proxy to Context. This proxy object adds unnecessary code in PROD
      // so we use the old behaviour (Context.Consumer references Context) to
      // reduce size and overhead. The separate object references context via
      // a property called "_context", which also gives us the ability to check
      // in DEV mode if this property exists or not and warn if it does not.

      {
        if (context._context === undefined) {
          // This may be because it's a Context (rather than a Consumer).
          // Or it may be because it's older React where they're the same thing.
          // We only want to warn if we're sure it's a new React.
          if (context !== context.Consumer) {
            if (!hasWarnedAboutUsingContextAsConsumer) {
              hasWarnedAboutUsingContextAsConsumer = true;
              warning$1(false, 'Rendering <Context> directly is not supported and will be removed in ' + 'a future major release. Did you mean to render <Context.Consumer> instead?');
            }
          }
        } else {
          context = context._context;
        }
      }
      var newProps = workInProgress.pendingProps;
      var render = newProps.children;
      {
        !(typeof render === 'function') ? warningWithoutStack$1(false, 'A context consumer was rendered with multiple children, or a child ' + "that isn't a function. A context consumer expects a single child " + 'that is a function. If you did pass a function, make sure there ' + 'is no trailing or leading whitespace around it.') : void 0;
      }
      prepareToReadContext(workInProgress, renderExpirationTime);
      var newValue = readContext(context, newProps.unstable_observedBits);
      var newChildren;
      {
        ReactCurrentOwner$3.current = workInProgress;
        setCurrentPhase('render');
        newChildren = render(newValue);
        setCurrentPhase(null);
      } // React DevTools reads this flag.

      workInProgress.effectTag |= PerformedWork;
      reconcileChildren(current$$1, workInProgress, newChildren, renderExpirationTime);
      return workInProgress.child;
    }

    function updateFundamentalComponent$1(current$$1, workInProgress, renderExpirationTime) {
      var fundamentalImpl = workInProgress.type.impl;

      if (fundamentalImpl.reconcileChildren === false) {
        return null;
      }

      var nextProps = workInProgress.pendingProps;
      var nextChildren = nextProps.children;
      reconcileChildren(current$$1, workInProgress, nextChildren, renderExpirationTime);
      return workInProgress.child;
    }

    function updateScopeComponent(current$$1, workInProgress, renderExpirationTime) {
      var nextProps = workInProgress.pendingProps;
      var nextChildren = nextProps.children;
      reconcileChildren(current$$1, workInProgress, nextChildren, renderExpirationTime);
      return workInProgress.child;
    }

    function markWorkInProgressReceivedUpdate() {
      didReceiveUpdate = true;
    }

    function bailoutOnAlreadyFinishedWork(current$$1, workInProgress, renderExpirationTime) {
      cancelWorkTimer(workInProgress);

      if (current$$1 !== null) {
        // Reuse previous dependencies
        workInProgress.dependencies = current$$1.dependencies;
      }

      if (enableProfilerTimer) {
        // Don't update "base" render times for bailouts.
        stopProfilerTimerIfRunning(workInProgress);
      }

      var updateExpirationTime = workInProgress.expirationTime;

      if (updateExpirationTime !== NoWork) {
        markUnprocessedUpdateTime(updateExpirationTime);
      } // Check if the children have any pending work.


      var childExpirationTime = workInProgress.childExpirationTime;

      if (childExpirationTime < renderExpirationTime) {
        // The children don't have any work either. We can skip them.
        // TODO: Once we add back resuming, we should check if the children are
        // a work-in-progress set. If so, we need to transfer their effects.
        return null;
      } else {
        // This fiber doesn't have work, but its subtree does. Clone the child
        // fibers and continue.
        cloneChildFibers(current$$1, workInProgress);
        return workInProgress.child;
      }
    }

    function remountFiber(current$$1, oldWorkInProgress, newWorkInProgress) {
      {
        var returnFiber = oldWorkInProgress.return;

        if (returnFiber === null) {
          throw new Error('Cannot swap the root fiber.');
        } // Disconnect from the old current.
        // It will get deleted.


        current$$1.alternate = null;
        oldWorkInProgress.alternate = null; // Connect to the new tree.

        newWorkInProgress.index = oldWorkInProgress.index;
        newWorkInProgress.sibling = oldWorkInProgress.sibling;
        newWorkInProgress.return = oldWorkInProgress.return;
        newWorkInProgress.ref = oldWorkInProgress.ref; // Replace the child/sibling pointers above it.

        if (oldWorkInProgress === returnFiber.child) {
          returnFiber.child = newWorkInProgress;
        } else {
          var prevSibling = returnFiber.child;

          if (prevSibling === null) {
            throw new Error('Expected parent to have a child.');
          }

          while (prevSibling.sibling !== oldWorkInProgress) {
            prevSibling = prevSibling.sibling;

            if (prevSibling === null) {
              throw new Error('Expected to find the previous sibling.');
            }
          }

          prevSibling.sibling = newWorkInProgress;
        } // Delete the old fiber and place the new one.
        // Since the old fiber is disconnected, we have to schedule it manually.


        var last = returnFiber.lastEffect;

        if (last !== null) {
          last.nextEffect = current$$1;
          returnFiber.lastEffect = current$$1;
        } else {
          returnFiber.firstEffect = returnFiber.lastEffect = current$$1;
        }

        current$$1.nextEffect = null;
        current$$1.effectTag = Deletion;
        newWorkInProgress.effectTag |= Placement; // Restart work from the new fiber.

        return newWorkInProgress;
      }
    }

    function beginWork$1(current$$1, workInProgress, renderExpirationTime) {
      var updateExpirationTime = workInProgress.expirationTime;
      {
        if (workInProgress._debugNeedsRemount && current$$1 !== null) {
          // This will restart the begin phase with a new fiber.
          return remountFiber(current$$1, workInProgress, createFiberFromTypeAndProps(workInProgress.type, workInProgress.key, workInProgress.pendingProps, workInProgress._debugOwner || null, workInProgress.mode, workInProgress.expirationTime));
        }
      }

      if (current$$1 !== null) {
        var oldProps = current$$1.memoizedProps;
        var newProps = workInProgress.pendingProps;

        if (oldProps !== newProps || hasContextChanged() || // Force a re-render if the implementation changed due to hot reload:
        workInProgress.type !== current$$1.type) {
          // If props or context changed, mark the fiber as having performed work.
          // This may be unset if the props are determined to be equal later (memo).
          didReceiveUpdate = true;
        } else if (updateExpirationTime < renderExpirationTime) {
          didReceiveUpdate = false; // This fiber does not have any pending work. Bailout without entering
          // the begin phase. There's still some bookkeeping we that needs to be done
          // in this optimized path, mostly pushing stuff onto the stack.

          switch (workInProgress.tag) {
            case HostRoot:
              pushHostRootContext(workInProgress);
              resetHydrationState();
              break;

            case HostComponent:
              pushHostContext(workInProgress);

              if (workInProgress.mode & ConcurrentMode && renderExpirationTime !== Never && shouldDeprioritizeSubtree(workInProgress.type, newProps)) {
                if (enableSchedulerTracing) {
                  markSpawnedWork(Never);
                } // Schedule this fiber to re-render at offscreen priority. Then bailout.


                workInProgress.expirationTime = workInProgress.childExpirationTime = Never;
                return null;
              }

              break;

            case ClassComponent:
              {
                var Component = workInProgress.type;

                if (isContextProvider(Component)) {
                  pushContextProvider(workInProgress);
                }

                break;
              }

            case HostPortal:
              pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
              break;

            case ContextProvider:
              {
                var newValue = workInProgress.memoizedProps.value;
                pushProvider(workInProgress, newValue);
                break;
              }

            case Profiler:
              if (enableProfilerTimer) {
                workInProgress.effectTag |= Update;
              }

              break;

            case SuspenseComponent:
              {
                var state = workInProgress.memoizedState;

                if (state !== null) {
                  if (enableSuspenseServerRenderer) {
                    if (state.dehydrated !== null) {
                      pushSuspenseContext(workInProgress, setDefaultShallowSuspenseContext(suspenseStackCursor.current)); // We know that this component will suspend again because if it has
                      // been unsuspended it has committed as a resolved Suspense component.
                      // If it needs to be retried, it should have work scheduled on it.

                      workInProgress.effectTag |= DidCapture;
                      break;
                    }
                  } // If this boundary is currently timed out, we need to decide
                  // whether to retry the primary children, or to skip over it and
                  // go straight to the fallback. Check the priority of the primary
                  // child fragment.


                  var primaryChildFragment = workInProgress.child;
                  var primaryChildExpirationTime = primaryChildFragment.childExpirationTime;

                  if (primaryChildExpirationTime !== NoWork && primaryChildExpirationTime >= renderExpirationTime) {
                    // The primary children have pending work. Use the normal path
                    // to attempt to render the primary children again.
                    return updateSuspenseComponent(current$$1, workInProgress, renderExpirationTime);
                  } else {
                    pushSuspenseContext(workInProgress, setDefaultShallowSuspenseContext(suspenseStackCursor.current)); // The primary children do not have pending work with sufficient
                    // priority. Bailout.

                    var child = bailoutOnAlreadyFinishedWork(current$$1, workInProgress, renderExpirationTime);

                    if (child !== null) {
                      // The fallback children have pending work. Skip over the
                      // primary children and work on the fallback.
                      return child.sibling;
                    } else {
                      return null;
                    }
                  }
                } else {
                  pushSuspenseContext(workInProgress, setDefaultShallowSuspenseContext(suspenseStackCursor.current));
                }

                break;
              }

            case SuspenseListComponent:
              {
                var didSuspendBefore = (current$$1.effectTag & DidCapture) !== NoEffect;
                var hasChildWork = workInProgress.childExpirationTime >= renderExpirationTime;

                if (didSuspendBefore) {
                  if (hasChildWork) {
                    // If something was in fallback state last time, and we have all the
                    // same children then we're still in progressive loading state.
                    // Something might get unblocked by state updates or retries in the
                    // tree which will affect the tail. So we need to use the normal
                    // path to compute the correct tail.
                    return updateSuspenseListComponent(current$$1, workInProgress, renderExpirationTime);
                  } // If none of the children had any work, that means that none of
                  // them got retried so they'll still be blocked in the same way
                  // as before. We can fast bail out.


                  workInProgress.effectTag |= DidCapture;
                } // If nothing suspended before and we're rendering the same children,
                // then the tail doesn't matter. Anything new that suspends will work
                // in the "together" mode, so we can continue from the state we had.


                var renderState = workInProgress.memoizedState;

                if (renderState !== null) {
                  // Reset to the "together" mode in case we've started a different
                  // update in the past but didn't complete it.
                  renderState.rendering = null;
                  renderState.tail = null;
                }

                pushSuspenseContext(workInProgress, suspenseStackCursor.current);

                if (hasChildWork) {
                  break;
                } else {
                  // If none of the children had any work, that means that none of
                  // them got retried so they'll still be blocked in the same way
                  // as before. We can fast bail out.
                  return null;
                }
              }
          }

          return bailoutOnAlreadyFinishedWork(current$$1, workInProgress, renderExpirationTime);
        } else {
          // An update was scheduled on this fiber, but there are no new props
          // nor legacy context. Set this to false. If an update queue or context
          // consumer produces a changed value, it will set this to true. Otherwise,
          // the component will assume the children have not changed and bail out.
          didReceiveUpdate = false;
        }
      } else {
        didReceiveUpdate = false;
      } // Before entering the begin phase, clear the expiration time.


      workInProgress.expirationTime = NoWork;

      switch (workInProgress.tag) {
        case IndeterminateComponent:
          {
            return mountIndeterminateComponent(current$$1, workInProgress, workInProgress.type, renderExpirationTime);
          }

        case LazyComponent:
          {
            var elementType = workInProgress.elementType;
            return mountLazyComponent(current$$1, workInProgress, elementType, updateExpirationTime, renderExpirationTime);
          }

        case FunctionComponent:
          {
            var _Component = workInProgress.type;
            var unresolvedProps = workInProgress.pendingProps;
            var resolvedProps = workInProgress.elementType === _Component ? unresolvedProps : resolveDefaultProps(_Component, unresolvedProps);
            return updateFunctionComponent(current$$1, workInProgress, _Component, resolvedProps, renderExpirationTime);
          }

        case ClassComponent:
          {
            var _Component2 = workInProgress.type;
            var _unresolvedProps = workInProgress.pendingProps;

            var _resolvedProps = workInProgress.elementType === _Component2 ? _unresolvedProps : resolveDefaultProps(_Component2, _unresolvedProps);

            return updateClassComponent(current$$1, workInProgress, _Component2, _resolvedProps, renderExpirationTime);
          }

        case HostRoot:
          return updateHostRoot(current$$1, workInProgress, renderExpirationTime);

        case HostComponent:
          return updateHostComponent(current$$1, workInProgress, renderExpirationTime);

        case HostText:
          return updateHostText(current$$1, workInProgress);

        case SuspenseComponent:
          return updateSuspenseComponent(current$$1, workInProgress, renderExpirationTime);

        case HostPortal:
          return updatePortalComponent(current$$1, workInProgress, renderExpirationTime);

        case ForwardRef:
          {
            var type = workInProgress.type;
            var _unresolvedProps2 = workInProgress.pendingProps;

            var _resolvedProps2 = workInProgress.elementType === type ? _unresolvedProps2 : resolveDefaultProps(type, _unresolvedProps2);

            return updateForwardRef(current$$1, workInProgress, type, _resolvedProps2, renderExpirationTime);
          }

        case Fragment:
          return updateFragment(current$$1, workInProgress, renderExpirationTime);

        case Mode:
          return updateMode(current$$1, workInProgress, renderExpirationTime);

        case Profiler:
          return updateProfiler(current$$1, workInProgress, renderExpirationTime);

        case ContextProvider:
          return updateContextProvider(current$$1, workInProgress, renderExpirationTime);

        case ContextConsumer:
          return updateContextConsumer(current$$1, workInProgress, renderExpirationTime);

        case MemoComponent:
          {
            var _type2 = workInProgress.type;
            var _unresolvedProps3 = workInProgress.pendingProps; // Resolve outer props first, then resolve inner props.

            var _resolvedProps3 = resolveDefaultProps(_type2, _unresolvedProps3);

            {
              if (workInProgress.type !== workInProgress.elementType) {
                var outerPropTypes = _type2.propTypes;

                if (outerPropTypes) {
                  checkPropTypes(outerPropTypes, _resolvedProps3, // Resolved for outer only
                  'prop', getComponentName(_type2), getCurrentFiberStackInDev);
                }
              }
            }
            _resolvedProps3 = resolveDefaultProps(_type2.type, _resolvedProps3);
            return updateMemoComponent(current$$1, workInProgress, _type2, _resolvedProps3, updateExpirationTime, renderExpirationTime);
          }

        case SimpleMemoComponent:
          {
            return updateSimpleMemoComponent(current$$1, workInProgress, workInProgress.type, workInProgress.pendingProps, updateExpirationTime, renderExpirationTime);
          }

        case IncompleteClassComponent:
          {
            var _Component3 = workInProgress.type;
            var _unresolvedProps4 = workInProgress.pendingProps;

            var _resolvedProps4 = workInProgress.elementType === _Component3 ? _unresolvedProps4 : resolveDefaultProps(_Component3, _unresolvedProps4);

            return mountIncompleteClassComponent(current$$1, workInProgress, _Component3, _resolvedProps4, renderExpirationTime);
          }

        case SuspenseListComponent:
          {
            return updateSuspenseListComponent(current$$1, workInProgress, renderExpirationTime);
          }

        case FundamentalComponent:
          {
            if (enableFundamentalAPI) {
              return updateFundamentalComponent$1(current$$1, workInProgress, renderExpirationTime);
            }

            break;
          }

        case ScopeComponent:
          {
            if (enableScopeAPI) {
              return updateScopeComponent(current$$1, workInProgress, renderExpirationTime);
            }

            break;
          }
      }

      (function () {
        {
          {
            throw ReactError(Error("Unknown unit of work tag (" + workInProgress.tag + "). This error is likely caused by a bug in React. Please file an issue."));
          }
        }
      })();
    }

    function createFundamentalStateInstance(currentFiber, props, impl, state) {
      return {
        currentFiber: currentFiber,
        impl: impl,
        instance: null,
        prevProps: null,
        props: props,
        state: state
      };
    }

    function isFiberSuspenseAndTimedOut(fiber) {
      return fiber.tag === SuspenseComponent && fiber.memoizedState !== null;
    }

    function getSuspenseFallbackChild(fiber) {
      return fiber.child.sibling.child;
    }

    function collectScopedNodes(node, fn, scopedNodes) {
      if (enableScopeAPI) {
        if (node.tag === HostComponent) {
          var _type = node.type,
              memoizedProps = node.memoizedProps;

          if (fn(_type, memoizedProps) === true) {
            scopedNodes.push(getPublicInstance(node.stateNode));
          }
        }

        var child = node.child;

        if (isFiberSuspenseAndTimedOut(node)) {
          child = getSuspenseFallbackChild(node);
        }

        if (child !== null) {
          collectScopedNodesFromChildren(child, fn, scopedNodes);
        }
      }
    }

    function collectScopedNodesFromChildren(startingChild, fn, scopedNodes) {
      var child = startingChild;

      while (child !== null) {
        collectScopedNodes(child, fn, scopedNodes);
        child = child.sibling;
      }
    }

    function collectNearestScopeMethods(node, scope, childrenScopes) {
      if (isValidScopeNode(node, scope)) {
        childrenScopes.push(node.stateNode.methods);
      } else {
        var child = node.child;

        if (isFiberSuspenseAndTimedOut(node)) {
          child = getSuspenseFallbackChild(node);
        }

        if (child !== null) {
          collectNearestChildScopeMethods(child, scope, childrenScopes);
        }
      }
    }

    function collectNearestChildScopeMethods(startingChild, scope, childrenScopes) {
      var child = startingChild;

      while (child !== null) {
        collectNearestScopeMethods(child, scope, childrenScopes);
        child = child.sibling;
      }
    }

    function isValidScopeNode(node, scope) {
      return node.tag === ScopeComponent && node.type === scope;
    }

    function createScopeMethods(scope, instance) {
      var fn = scope.fn;
      return {
        getChildren: function () {
          var currentFiber = instance.fiber;
          var child = currentFiber.child;
          var childrenScopes = [];

          if (child !== null) {
            collectNearestChildScopeMethods(child, scope, childrenScopes);
          }

          return childrenScopes.length === 0 ? null : childrenScopes;
        },
        getChildrenFromRoot: function () {
          var currentFiber = instance.fiber;
          var node = currentFiber;

          while (node !== null) {
            var parent = node.return;

            if (parent === null) {
              break;
            }

            node = parent;

            if (node.tag === ScopeComponent && node.type === scope) {
              break;
            }
          }

          var childrenScopes = [];
          collectNearestChildScopeMethods(node.child, scope, childrenScopes);
          return childrenScopes.length === 0 ? null : childrenScopes;
        },
        getParent: function () {
          var node = instance.fiber.return;

          while (node !== null) {
            if (node.tag === ScopeComponent && node.type === scope) {
              return node.stateNode.methods;
            }

            node = node.return;
          }

          return null;
        },
        getProps: function () {
          var currentFiber = instance.fiber;
          return currentFiber.memoizedProps;
        },
        getScopedNodes: function () {
          var currentFiber = instance.fiber;
          var child = currentFiber.child;
          var scopedNodes = [];

          if (child !== null) {
            collectScopedNodesFromChildren(child, fn, scopedNodes);
          }

          return scopedNodes.length === 0 ? null : scopedNodes;
        }
      };
    }

    function markUpdate(workInProgress) {
      // Tag the fiber with an update effect. This turns a Placement into
      // a PlacementAndUpdate.
      workInProgress.effectTag |= Update;
    }

    function markRef$1(workInProgress) {
      workInProgress.effectTag |= Ref;
    }

    var appendAllChildren;
    var updateHostContainer;
    var updateHostComponent$1;
    var updateHostText$1;

    if (supportsMutation) {
      // Mutation mode
      appendAllChildren = function (parent, workInProgress, needsVisibilityToggle, isHidden) {
        // We only have the top Fiber that was created but we need recurse down its
        // children to find all the terminal nodes.
        var node = workInProgress.child;

        while (node !== null) {
          if (node.tag === HostComponent || node.tag === HostText) {
            appendInitialChild(parent, node.stateNode);
          } else if (enableFundamentalAPI && node.tag === FundamentalComponent) {
            appendInitialChild(parent, node.stateNode.instance);
          } else if (node.tag === HostPortal) {// If we have a portal child, then we don't want to traverse
            // down its children. Instead, we'll get insertions from each child in
            // the portal directly.
          } else if (node.child !== null) {
            node.child.return = node;
            node = node.child;
            continue;
          }

          if (node === workInProgress) {
            return;
          }

          while (node.sibling === null) {
            if (node.return === null || node.return === workInProgress) {
              return;
            }

            node = node.return;
          }

          node.sibling.return = node.return;
          node = node.sibling;
        }
      };

      updateHostContainer = function (workInProgress) {// Noop
      };

      updateHostComponent$1 = function (current, workInProgress, type, newProps, rootContainerInstance) {
        // If we have an alternate, that means this is an update and we need to
        // schedule a side-effect to do the updates.
        var oldProps = current.memoizedProps;

        if (oldProps === newProps) {
          // In mutation mode, this is sufficient for a bailout because
          // we won't touch this node even if children changed.
          return;
        } // If we get updated because one of our children updated, we don't
        // have newProps so we'll have to reuse them.
        // TODO: Split the update API as separate for the props vs. children.
        // Even better would be if children weren't special cased at all tho.


        var instance = workInProgress.stateNode;
        var currentHostContext = getHostContext(); // TODO: Experiencing an error where oldProps is null. Suggests a host
        // component is hitting the resume path. Figure out why. Possibly
        // related to `hidden`.

        var updatePayload = prepareUpdate(instance, type, oldProps, newProps, rootContainerInstance, currentHostContext); // TODO: Type this specific to this type of component.

        workInProgress.updateQueue = updatePayload; // If the update payload indicates that there is a change or if there
        // is a new ref we mark this as an update. All the work is done in commitWork.

        if (updatePayload) {
          markUpdate(workInProgress);
        }
      };

      updateHostText$1 = function (current, workInProgress, oldText, newText) {
        // If the text differs, mark it as an update. All the work in done in commitWork.
        if (oldText !== newText) {
          markUpdate(workInProgress);
        }
      };
    } else if (supportsPersistence) {
      // Persistent host tree mode
      appendAllChildren = function (parent, workInProgress, needsVisibilityToggle, isHidden) {
        // We only have the top Fiber that was created but we need recurse down its
        // children to find all the terminal nodes.
        var node = workInProgress.child;

        while (node !== null) {
          // eslint-disable-next-line no-labels
          branches: if (node.tag === HostComponent) {
            var instance = node.stateNode;

            if (needsVisibilityToggle && isHidden) {
              // This child is inside a timed out tree. Hide it.
              var props = node.memoizedProps;
              var type = node.type;
              instance = cloneHiddenInstance(instance, type, props, node);
            }

            appendInitialChild(parent, instance);
          } else if (node.tag === HostText) {
            var _instance = node.stateNode;

            if (needsVisibilityToggle && isHidden) {
              // This child is inside a timed out tree. Hide it.
              var text = node.memoizedProps;
              _instance = cloneHiddenTextInstance(_instance, text, node);
            }

            appendInitialChild(parent, _instance);
          } else if (enableFundamentalAPI && node.tag === FundamentalComponent) {
            var _instance2 = node.stateNode.instance;

            if (needsVisibilityToggle && isHidden) {
              // This child is inside a timed out tree. Hide it.
              var _props = node.memoizedProps;
              var _type = node.type;
              _instance2 = cloneHiddenInstance(_instance2, _type, _props, node);
            }

            appendInitialChild(parent, _instance2);
          } else if (node.tag === HostPortal) {// If we have a portal child, then we don't want to traverse
            // down its children. Instead, we'll get insertions from each child in
            // the portal directly.
          } else if (node.tag === SuspenseComponent) {
            if ((node.effectTag & Update) !== NoEffect) {
              // Need to toggle the visibility of the primary children.
              var newIsHidden = node.memoizedState !== null;

              if (newIsHidden) {
                var primaryChildParent = node.child;

                if (primaryChildParent !== null) {
                  if (primaryChildParent.child !== null) {
                    primaryChildParent.child.return = primaryChildParent;
                    appendAllChildren(parent, primaryChildParent, true, newIsHidden);
                  }

                  var fallbackChildParent = primaryChildParent.sibling;

                  if (fallbackChildParent !== null) {
                    fallbackChildParent.return = node;
                    node = fallbackChildParent;
                    continue;
                  }
                }
              }
            }

            if (node.child !== null) {
              // Continue traversing like normal
              node.child.return = node;
              node = node.child;
              continue;
            }
          } else if (node.child !== null) {
            node.child.return = node;
            node = node.child;
            continue;
          } // $FlowFixMe This is correct but Flow is confused by the labeled break.


          node = node;

          if (node === workInProgress) {
            return;
          }

          while (node.sibling === null) {
            if (node.return === null || node.return === workInProgress) {
              return;
            }

            node = node.return;
          }

          node.sibling.return = node.return;
          node = node.sibling;
        }
      }; // An unfortunate fork of appendAllChildren because we have two different parent types.


      var appendAllChildrenToContainer = function (containerChildSet, workInProgress, needsVisibilityToggle, isHidden) {
        // We only have the top Fiber that was created but we need recurse down its
        // children to find all the terminal nodes.
        var node = workInProgress.child;

        while (node !== null) {
          // eslint-disable-next-line no-labels
          branches: if (node.tag === HostComponent) {
            var instance = node.stateNode;

            if (needsVisibilityToggle && isHidden) {
              // This child is inside a timed out tree. Hide it.
              var props = node.memoizedProps;
              var type = node.type;
              instance = cloneHiddenInstance(instance, type, props, node);
            }

            appendChildToContainerChildSet(containerChildSet, instance);
          } else if (node.tag === HostText) {
            var _instance3 = node.stateNode;

            if (needsVisibilityToggle && isHidden) {
              // This child is inside a timed out tree. Hide it.
              var text = node.memoizedProps;
              _instance3 = cloneHiddenTextInstance(_instance3, text, node);
            }

            appendChildToContainerChildSet(containerChildSet, _instance3);
          } else if (enableFundamentalAPI && node.tag === FundamentalComponent) {
            var _instance4 = node.stateNode.instance;

            if (needsVisibilityToggle && isHidden) {
              // This child is inside a timed out tree. Hide it.
              var _props2 = node.memoizedProps;
              var _type2 = node.type;
              _instance4 = cloneHiddenInstance(_instance4, _type2, _props2, node);
            }

            appendChildToContainerChildSet(containerChildSet, _instance4);
          } else if (node.tag === HostPortal) {// If we have a portal child, then we don't want to traverse
            // down its children. Instead, we'll get insertions from each child in
            // the portal directly.
          } else if (node.tag === SuspenseComponent) {
            if ((node.effectTag & Update) !== NoEffect) {
              // Need to toggle the visibility of the primary children.
              var newIsHidden = node.memoizedState !== null;

              if (newIsHidden) {
                var primaryChildParent = node.child;

                if (primaryChildParent !== null) {
                  if (primaryChildParent.child !== null) {
                    primaryChildParent.child.return = primaryChildParent;
                    appendAllChildrenToContainer(containerChildSet, primaryChildParent, true, newIsHidden);
                  }

                  var fallbackChildParent = primaryChildParent.sibling;

                  if (fallbackChildParent !== null) {
                    fallbackChildParent.return = node;
                    node = fallbackChildParent;
                    continue;
                  }
                }
              }
            }

            if (node.child !== null) {
              // Continue traversing like normal
              node.child.return = node;
              node = node.child;
              continue;
            }
          } else if (node.child !== null) {
            node.child.return = node;
            node = node.child;
            continue;
          } // $FlowFixMe This is correct but Flow is confused by the labeled break.


          node = node;

          if (node === workInProgress) {
            return;
          }

          while (node.sibling === null) {
            if (node.return === null || node.return === workInProgress) {
              return;
            }

            node = node.return;
          }

          node.sibling.return = node.return;
          node = node.sibling;
        }
      };

      updateHostContainer = function (workInProgress) {
        var portalOrRoot = workInProgress.stateNode;
        var childrenUnchanged = workInProgress.firstEffect === null;

        if (childrenUnchanged) {// No changes, just reuse the existing instance.
        } else {
          var container = portalOrRoot.containerInfo;
          var newChildSet = createContainerChildSet(container); // If children might have changed, we have to add them all to the set.

          appendAllChildrenToContainer(newChildSet, workInProgress, false, false);
          portalOrRoot.pendingChildren = newChildSet; // Schedule an update on the container to swap out the container.

          markUpdate(workInProgress);
          finalizeContainerChildren(container, newChildSet);
        }
      };

      updateHostComponent$1 = function (current, workInProgress, type, newProps, rootContainerInstance) {
        var currentInstance = current.stateNode;
        var oldProps = current.memoizedProps; // If there are no effects associated with this node, then none of our children had any updates.
        // This guarantees that we can reuse all of them.

        var childrenUnchanged = workInProgress.firstEffect === null;

        if (childrenUnchanged && oldProps === newProps) {
          // No changes, just reuse the existing instance.
          // Note that this might release a previous clone.
          workInProgress.stateNode = currentInstance;
          return;
        }

        var recyclableInstance = workInProgress.stateNode;
        var currentHostContext = getHostContext();
        var updatePayload = null;

        if (oldProps !== newProps) {
          updatePayload = prepareUpdate(recyclableInstance, type, oldProps, newProps, rootContainerInstance, currentHostContext);
        }

        if (childrenUnchanged && updatePayload === null) {
          // No changes, just reuse the existing instance.
          // Note that this might release a previous clone.
          workInProgress.stateNode = currentInstance;
          return;
        }

        var newInstance = cloneInstance(currentInstance, updatePayload, type, oldProps, newProps, workInProgress, childrenUnchanged, recyclableInstance);

        if (finalizeInitialChildren(newInstance, type, newProps, rootContainerInstance, currentHostContext)) {
          markUpdate(workInProgress);
        }

        workInProgress.stateNode = newInstance;

        if (childrenUnchanged) {
          // If there are no other effects in this tree, we need to flag this node as having one.
          // Even though we're not going to use it for anything.
          // Otherwise parents won't know that there are new children to propagate upwards.
          markUpdate(workInProgress);
        } else {
          // If children might have changed, we have to add them all to the set.
          appendAllChildren(newInstance, workInProgress, false, false);
        }
      };

      updateHostText$1 = function (current, workInProgress, oldText, newText) {
        if (oldText !== newText) {
          // If the text content differs, we'll create a new text instance for it.
          var rootContainerInstance = getRootHostContainer();
          var currentHostContext = getHostContext();
          workInProgress.stateNode = createTextInstance(newText, rootContainerInstance, currentHostContext, workInProgress); // We'll have to mark it as having an effect, even though we won't use the effect for anything.
          // This lets the parents know that at least one of their children has changed.

          markUpdate(workInProgress);
        }
      };
    } else {
      // No host operations
      updateHostContainer = function (workInProgress) {// Noop
      };

      updateHostComponent$1 = function (current, workInProgress, type, newProps, rootContainerInstance) {// Noop
      };

      updateHostText$1 = function (current, workInProgress, oldText, newText) {// Noop
      };
    }

    function cutOffTailIfNeeded(renderState, hasRenderedATailFallback) {
      switch (renderState.tailMode) {
        case 'hidden':
          {
            // Any insertions at the end of the tail list after this point
            // should be invisible. If there are already mounted boundaries
            // anything before them are not considered for collapsing.
            // Therefore we need to go through the whole tail to find if
            // there are any.
            var tailNode = renderState.tail;
            var lastTailNode = null;

            while (tailNode !== null) {
              if (tailNode.alternate !== null) {
                lastTailNode = tailNode;
              }

              tailNode = tailNode.sibling;
            } // Next we're simply going to delete all insertions after the
            // last rendered item.


            if (lastTailNode === null) {
              // All remaining items in the tail are insertions.
              renderState.tail = null;
            } else {
              // Detach the insertion after the last node that was already
              // inserted.
              lastTailNode.sibling = null;
            }

            break;
          }

        case 'collapsed':
          {
            // Any insertions at the end of the tail list after this point
            // should be invisible. If there are already mounted boundaries
            // anything before them are not considered for collapsing.
            // Therefore we need to go through the whole tail to find if
            // there are any.
            var _tailNode = renderState.tail;
            var _lastTailNode = null;

            while (_tailNode !== null) {
              if (_tailNode.alternate !== null) {
                _lastTailNode = _tailNode;
              }

              _tailNode = _tailNode.sibling;
            } // Next we're simply going to delete all insertions after the
            // last rendered item.


            if (_lastTailNode === null) {
              // All remaining items in the tail are insertions.
              if (!hasRenderedATailFallback && renderState.tail !== null) {
                // We suspended during the head. We want to show at least one
                // row at the tail. So we'll keep on and cut off the rest.
                renderState.tail.sibling = null;
              } else {
                renderState.tail = null;
              }
            } else {
              // Detach the insertion after the last node that was already
              // inserted.
              _lastTailNode.sibling = null;
            }

            break;
          }
      }
    }

    function completeWork(current, workInProgress, renderExpirationTime) {
      var newProps = workInProgress.pendingProps;

      switch (workInProgress.tag) {
        case IndeterminateComponent:
          break;

        case LazyComponent:
          break;

        case SimpleMemoComponent:
        case FunctionComponent:
          break;

        case ClassComponent:
          {
            var Component = workInProgress.type;

            if (isContextProvider(Component)) {
              popContext(workInProgress);
            }

            break;
          }

        case HostRoot:
          {
            popHostContainer(workInProgress);
            popTopLevelContextObject(workInProgress);
            var fiberRoot = workInProgress.stateNode;

            if (fiberRoot.pendingContext) {
              fiberRoot.context = fiberRoot.pendingContext;
              fiberRoot.pendingContext = null;
            }

            if (current === null || current.child === null) {
              // If we hydrated, pop so that we can delete any remaining children
              // that weren't hydrated.
              var wasHydrated = popHydrationState(workInProgress);

              if (wasHydrated) {
                // If we hydrated, then we'll need to schedule an update for
                // the commit side-effects on the root.
                markUpdate(workInProgress);
              }
            }

            updateHostContainer(workInProgress);
            break;
          }

        case HostComponent:
          {
            popHostContext(workInProgress);
            var rootContainerInstance = getRootHostContainer();
            var type = workInProgress.type;

            if (current !== null && workInProgress.stateNode != null) {
              updateHostComponent$1(current, workInProgress, type, newProps, rootContainerInstance);

              if (enableFlareAPI) {
                var prevListeners = current.memoizedProps.listeners;
                var nextListeners = newProps.listeners;

                if (prevListeners !== nextListeners) {
                  markUpdate(workInProgress);
                }
              }

              if (current.ref !== workInProgress.ref) {
                markRef$1(workInProgress);
              }
            } else {
              if (!newProps) {
                (function () {
                  if (!(workInProgress.stateNode !== null)) {
                    {
                      throw ReactError(Error("We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue."));
                    }
                  }
                })(); // This can happen when we abort work.


                break;
              }

              var currentHostContext = getHostContext(); // TODO: Move createInstance to beginWork and keep it on a context
              // "stack" as the parent. Then append children as we go in beginWork
              // or completeWork depending on we want to add then top->down or
              // bottom->up. Top->down is faster in IE11.

              var _wasHydrated = popHydrationState(workInProgress);

              if (_wasHydrated) {
                // TODO: Move this and createInstance step into the beginPhase
                // to consolidate.
                if (prepareToHydrateHostInstance(workInProgress, rootContainerInstance, currentHostContext)) {
                  // If changes to the hydrated node needs to be applied at the
                  // commit-phase we mark this as such.
                  markUpdate(workInProgress);
                }

                if (enableFlareAPI) {
                  var listeners = newProps.listeners;

                  if (listeners != null) {
                    updateEventListeners(listeners, workInProgress, rootContainerInstance);
                  }
                }
              } else {
                var instance = createInstance(type, newProps, rootContainerInstance, currentHostContext, workInProgress);
                appendAllChildren(instance, workInProgress, false, false); // This needs to be set before we mount Flare event listeners

                workInProgress.stateNode = instance;

                if (enableFlareAPI) {
                  var _listeners = newProps.listeners;

                  if (_listeners != null) {
                    updateEventListeners(_listeners, workInProgress, rootContainerInstance);
                  }
                } // Certain renderers require commit-time effects for initial mount.
                // (eg DOM renderer supports auto-focus for certain elements).
                // Make sure such renderers get scheduled for later work.


                if (finalizeInitialChildren(instance, type, newProps, rootContainerInstance, currentHostContext)) {
                  markUpdate(workInProgress);
                }
              }

              if (workInProgress.ref !== null) {
                // If there is a ref on a host node we need to schedule a callback
                markRef$1(workInProgress);
              }
            }

            break;
          }

        case HostText:
          {
            var newText = newProps;

            if (current && workInProgress.stateNode != null) {
              var oldText = current.memoizedProps; // If we have an alternate, that means this is an update and we need
              // to schedule a side-effect to do the updates.

              updateHostText$1(current, workInProgress, oldText, newText);
            } else {
              if (typeof newText !== 'string') {
                (function () {
                  if (!(workInProgress.stateNode !== null)) {
                    {
                      throw ReactError(Error("We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue."));
                    }
                  }
                })(); // This can happen when we abort work.

              }

              var _rootContainerInstance = getRootHostContainer();

              var _currentHostContext = getHostContext();

              var _wasHydrated2 = popHydrationState(workInProgress);

              if (_wasHydrated2) {
                if (prepareToHydrateHostTextInstance(workInProgress)) {
                  markUpdate(workInProgress);
                }
              } else {
                workInProgress.stateNode = createTextInstance(newText, _rootContainerInstance, _currentHostContext, workInProgress);
              }
            }

            break;
          }

        case ForwardRef:
          break;

        case SuspenseComponent:
          {
            popSuspenseContext(workInProgress);
            var nextState = workInProgress.memoizedState;

            if (enableSuspenseServerRenderer) {
              if (nextState !== null && nextState.dehydrated !== null) {
                if (current === null) {
                  var _wasHydrated3 = popHydrationState(workInProgress);

                  (function () {
                    if (!_wasHydrated3) {
                      {
                        throw ReactError(Error("A dehydrated suspense component was completed without a hydrated node. This is probably a bug in React."));
                      }
                    }
                  })();

                  prepareToHydrateHostSuspenseInstance(workInProgress);

                  if (enableSchedulerTracing) {
                    markSpawnedWork(Never);
                  }

                  return null;
                } else {
                  // We should never have been in a hydration state if we didn't have a current.
                  // However, in some of those paths, we might have reentered a hydration state
                  // and then we might be inside a hydration state. In that case, we'll need to
                  // exit out of it.
                  resetHydrationState();

                  if ((workInProgress.effectTag & DidCapture) === NoEffect) {
                    // This boundary did not suspend so it's now hydrated and unsuspended.
                    workInProgress.memoizedState = null;
                  } // If nothing suspended, we need to schedule an effect to mark this boundary
                  // as having hydrated so events know that they're free be invoked.
                  // It's also a signal to replay events and the suspense callback.
                  // If something suspended, schedule an effect to attach retry listeners.
                  // So we might as well always mark this.


                  workInProgress.effectTag |= Update;
                  return null;
                }
              }
            }

            if ((workInProgress.effectTag & DidCapture) !== NoEffect) {
              // Something suspended. Re-render with the fallback children.
              workInProgress.expirationTime = renderExpirationTime; // Do not reset the effect list.

              return workInProgress;
            }

            var nextDidTimeout = nextState !== null;
            var prevDidTimeout = false;

            if (current === null) {
              // In cases where we didn't find a suitable hydration boundary we never
              // put this in dehydrated mode, but we still need to pop the hydration
              // state since we might be inside the insertion tree.
              popHydrationState(workInProgress);
            } else {
              var prevState = current.memoizedState;
              prevDidTimeout = prevState !== null;

              if (!nextDidTimeout && prevState !== null) {
                // We just switched from the fallback to the normal children.
                // Delete the fallback.
                // TODO: Would it be better to store the fallback fragment on
                // the stateNode during the begin phase?
                var currentFallbackChild = current.child.sibling;

                if (currentFallbackChild !== null) {
                  // Deletions go at the beginning of the return fiber's effect list
                  var first = workInProgress.firstEffect;

                  if (first !== null) {
                    workInProgress.firstEffect = currentFallbackChild;
                    currentFallbackChild.nextEffect = first;
                  } else {
                    workInProgress.firstEffect = workInProgress.lastEffect = currentFallbackChild;
                    currentFallbackChild.nextEffect = null;
                  }

                  currentFallbackChild.effectTag = Deletion;
                }
              }
            }

            if (nextDidTimeout && !prevDidTimeout) {
              // If this subtreee is running in batched mode we can suspend,
              // otherwise we won't suspend.
              // TODO: This will still suspend a synchronous tree if anything
              // in the concurrent tree already suspended during this render.
              // This is a known bug.
              if ((workInProgress.mode & BatchedMode) !== NoMode) {
                // TODO: Move this back to throwException because this is too late
                // if this is a large tree which is common for initial loads. We
                // don't know if we should restart a render or not until we get
                // this marker, and this is too late.
                // If this render already had a ping or lower pri updates,
                // and this is the first time we know we're going to suspend we
                // should be able to immediately restart from within throwException.
                var hasInvisibleChildContext = current === null && workInProgress.memoizedProps.unstable_avoidThisFallback !== true;

                if (hasInvisibleChildContext || hasSuspenseContext(suspenseStackCursor.current, InvisibleParentSuspenseContext)) {
                  // If this was in an invisible tree or a new render, then showing
                  // this boundary is ok.
                  renderDidSuspend();
                } else {
                  // Otherwise, we're going to have to hide content so we should
                  // suspend for longer if possible.
                  renderDidSuspendDelayIfPossible();
                }
              }
            }

            if (supportsPersistence) {
              // TODO: Only schedule updates if not prevDidTimeout.
              if (nextDidTimeout) {
                // If this boundary just timed out, schedule an effect to attach a
                // retry listener to the proimse. This flag is also used to hide the
                // primary children.
                workInProgress.effectTag |= Update;
              }
            }

            if (supportsMutation) {
              // TODO: Only schedule updates if these values are non equal, i.e. it changed.
              if (nextDidTimeout || prevDidTimeout) {
                // If this boundary just timed out, schedule an effect to attach a
                // retry listener to the proimse. This flag is also used to hide the
                // primary children. In mutation mode, we also need the flag to
                // *unhide* children that were previously hidden, so check if the
                // is currently timed out, too.
                workInProgress.effectTag |= Update;
              }
            }

            if (enableSuspenseCallback && workInProgress.updateQueue !== null && workInProgress.memoizedProps.suspenseCallback != null) {
              // Always notify the callback
              workInProgress.effectTag |= Update;
            }

            break;
          }

        case Fragment:
          break;

        case Mode:
          break;

        case Profiler:
          break;

        case HostPortal:
          popHostContainer(workInProgress);
          updateHostContainer(workInProgress);
          break;

        case ContextProvider:
          // Pop provider fiber
          popProvider(workInProgress);
          break;

        case ContextConsumer:
          break;

        case MemoComponent:
          break;

        case IncompleteClassComponent:
          {
            // Same as class component case. I put it down here so that the tags are
            // sequential to ensure this switch is compiled to a jump table.
            var _Component = workInProgress.type;

            if (isContextProvider(_Component)) {
              popContext(workInProgress);
            }

            break;
          }

        case SuspenseListComponent:
          {
            popSuspenseContext(workInProgress);
            var renderState = workInProgress.memoizedState;

            if (renderState === null) {
              // We're running in the default, "independent" mode. We don't do anything
              // in this mode.
              break;
            }

            var didSuspendAlready = (workInProgress.effectTag & DidCapture) !== NoEffect;
            var renderedTail = renderState.rendering;

            if (renderedTail === null) {
              // We just rendered the head.
              if (!didSuspendAlready) {
                // This is the first pass. We need to figure out if anything is still
                // suspended in the rendered set.
                // If new content unsuspended, but there's still some content that
                // didn't. Then we need to do a second pass that forces everything
                // to keep showing their fallbacks.
                // We might be suspended if something in this render pass suspended, or
                // something in the previous committed pass suspended. Otherwise,
                // there's no chance so we can skip the expensive call to
                // findFirstSuspended.
                var cannotBeSuspended = renderHasNotSuspendedYet() && (current === null || (current.effectTag & DidCapture) === NoEffect);

                if (!cannotBeSuspended) {
                  var row = workInProgress.child;

                  while (row !== null) {
                    var suspended = findFirstSuspended(row);

                    if (suspended !== null) {
                      didSuspendAlready = true;
                      workInProgress.effectTag |= DidCapture;
                      cutOffTailIfNeeded(renderState, false); // If this is a newly suspended tree, it might not get committed as
                      // part of the second pass. In that case nothing will subscribe to
                      // its thennables. Instead, we'll transfer its thennables to the
                      // SuspenseList so that it can retry if they resolve.
                      // There might be multiple of these in the list but since we're
                      // going to wait for all of them anyway, it doesn't really matter
                      // which ones gets to ping. In theory we could get clever and keep
                      // track of how many dependencies remain but it gets tricky because
                      // in the meantime, we can add/remove/change items and dependencies.
                      // We might bail out of the loop before finding any but that
                      // doesn't matter since that means that the other boundaries that
                      // we did find already has their listeners attached.

                      var newThennables = suspended.updateQueue;

                      if (newThennables !== null) {
                        workInProgress.updateQueue = newThennables;
                        workInProgress.effectTag |= Update;
                      } // Rerender the whole list, but this time, we'll force fallbacks
                      // to stay in place.
                      // Reset the effect list before doing the second pass since that's now invalid.


                      workInProgress.firstEffect = workInProgress.lastEffect = null; // Reset the child fibers to their original state.

                      resetChildFibers(workInProgress, renderExpirationTime); // Set up the Suspense Context to force suspense and immediately
                      // rerender the children.

                      pushSuspenseContext(workInProgress, setShallowSuspenseContext(suspenseStackCursor.current, ForceSuspenseFallback));
                      return workInProgress.child;
                    }

                    row = row.sibling;
                  }
                }
              } else {
                cutOffTailIfNeeded(renderState, false);
              } // Next we're going to render the tail.

            } else {
              // Append the rendered row to the child list.
              if (!didSuspendAlready) {
                var _suspended = findFirstSuspended(renderedTail);

                if (_suspended !== null) {
                  workInProgress.effectTag |= DidCapture;
                  didSuspendAlready = true;
                  cutOffTailIfNeeded(renderState, true); // This might have been modified.

                  if (renderState.tail === null && renderState.tailMode === 'hidden') {
                    // We need to delete the row we just rendered.
                    // Ensure we transfer the update queue to the parent.
                    var _newThennables = _suspended.updateQueue;

                    if (_newThennables !== null) {
                      workInProgress.updateQueue = _newThennables;
                      workInProgress.effectTag |= Update;
                    } // Reset the effect list to what it w as before we rendered this
                    // child. The nested children have already appended themselves.


                    var lastEffect = workInProgress.lastEffect = renderState.lastEffect; // Remove any effects that were appended after this point.

                    if (lastEffect !== null) {
                      lastEffect.nextEffect = null;
                    } // We're done.


                    return null;
                  }
                } else if (now() > renderState.tailExpiration && renderExpirationTime > Never) {
                  // We have now passed our CPU deadline and we'll just give up further
                  // attempts to render the main content and only render fallbacks.
                  // The assumption is that this is usually faster.
                  workInProgress.effectTag |= DidCapture;
                  didSuspendAlready = true;
                  cutOffTailIfNeeded(renderState, false); // Since nothing actually suspended, there will nothing to ping this
                  // to get it started back up to attempt the next item. If we can show
                  // them, then they really have the same priority as this render.
                  // So we'll pick it back up the very next render pass once we've had
                  // an opportunity to yield for paint.

                  var nextPriority = renderExpirationTime - 1;
                  workInProgress.expirationTime = workInProgress.childExpirationTime = nextPriority;

                  if (enableSchedulerTracing) {
                    markSpawnedWork(nextPriority);
                  }
                }
              }

              if (renderState.isBackwards) {
                // The effect list of the backwards tail will have been added
                // to the end. This breaks the guarantee that life-cycles fire in
                // sibling order but that isn't a strong guarantee promised by React.
                // Especially since these might also just pop in during future commits.
                // Append to the beginning of the list.
                renderedTail.sibling = workInProgress.child;
                workInProgress.child = renderedTail;
              } else {
                var previousSibling = renderState.last;

                if (previousSibling !== null) {
                  previousSibling.sibling = renderedTail;
                } else {
                  workInProgress.child = renderedTail;
                }

                renderState.last = renderedTail;
              }
            }

            if (renderState.tail !== null) {
              // We still have tail rows to render.
              if (renderState.tailExpiration === 0) {
                // Heuristic for how long we're willing to spend rendering rows
                // until we just give up and show what we have so far.
                var TAIL_EXPIRATION_TIMEOUT_MS = 500;
                renderState.tailExpiration = now() + TAIL_EXPIRATION_TIMEOUT_MS;
              } // Pop a row.


              var next = renderState.tail;
              renderState.rendering = next;
              renderState.tail = next.sibling;
              renderState.lastEffect = workInProgress.lastEffect;
              next.sibling = null; // Restore the context.
              // TODO: We can probably just avoid popping it instead and only
              // setting it the first time we go from not suspended to suspended.

              var suspenseContext = suspenseStackCursor.current;

              if (didSuspendAlready) {
                suspenseContext = setShallowSuspenseContext(suspenseContext, ForceSuspenseFallback);
              } else {
                suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
              }

              pushSuspenseContext(workInProgress, suspenseContext); // Do a pass over the next row.

              return next;
            }

            break;
          }

        case FundamentalComponent:
          {
            if (enableFundamentalAPI) {
              var fundamentalImpl = workInProgress.type.impl;
              var fundamentalInstance = workInProgress.stateNode;

              if (fundamentalInstance === null) {
                var getInitialState = fundamentalImpl.getInitialState;
                var fundamentalState;

                if (getInitialState !== undefined) {
                  fundamentalState = getInitialState(newProps);
                }

                fundamentalInstance = workInProgress.stateNode = createFundamentalStateInstance(workInProgress, newProps, fundamentalImpl, fundamentalState || {});

                var _instance5 = getFundamentalComponentInstance(fundamentalInstance);

                fundamentalInstance.instance = _instance5;

                if (fundamentalImpl.reconcileChildren === false) {
                  return null;
                }

                appendAllChildren(_instance5, workInProgress, false, false);
                mountFundamentalComponent(fundamentalInstance);
              } else {
                // We fire update in commit phase
                var prevProps = fundamentalInstance.props;
                fundamentalInstance.prevProps = prevProps;
                fundamentalInstance.props = newProps;
                fundamentalInstance.currentFiber = workInProgress;

                if (supportsPersistence) {
                  var _instance6 = cloneFundamentalInstance(fundamentalInstance);

                  fundamentalInstance.instance = _instance6;
                  appendAllChildren(_instance6, workInProgress, false, false);
                }

                var shouldUpdate = shouldUpdateFundamentalComponent(fundamentalInstance);

                if (shouldUpdate) {
                  markUpdate(workInProgress);
                }
              }
            }

            break;
          }

        case ScopeComponent:
          {
            if (enableScopeAPI) {
              if (current === null) {
                var _type3 = workInProgress.type;
                var scopeInstance = {
                  fiber: workInProgress,
                  methods: null
                };
                workInProgress.stateNode = scopeInstance;
                scopeInstance.methods = createScopeMethods(_type3, scopeInstance);

                if (enableFlareAPI) {
                  var _listeners2 = newProps.listeners;

                  if (_listeners2 != null) {
                    var _rootContainerInstance2 = getRootHostContainer();

                    updateEventListeners(_listeners2, workInProgress, _rootContainerInstance2);
                  }
                }

                if (workInProgress.ref !== null) {
                  markRef$1(workInProgress);
                  markUpdate(workInProgress);
                }
              } else {
                if (enableFlareAPI) {
                  var _prevListeners = current.memoizedProps.listeners;
                  var _nextListeners = newProps.listeners;

                  if (_prevListeners !== _nextListeners || workInProgress.ref !== null) {
                    markUpdate(workInProgress);
                  }
                } else {
                  if (workInProgress.ref !== null) {
                    markUpdate(workInProgress);
                  }
                }

                if (current.ref !== workInProgress.ref) {
                  markRef$1(workInProgress);
                }
              }
            }

            break;
          }

        default:
          (function () {
            {
              {
                throw ReactError(Error("Unknown unit of work tag (" + workInProgress.tag + "). This error is likely caused by a bug in React. Please file an issue."));
              }
            }
          })();

      }

      return null;
    }

    function unwindWork(workInProgress, renderExpirationTime) {
      switch (workInProgress.tag) {
        case ClassComponent:
          {
            var Component = workInProgress.type;

            if (isContextProvider(Component)) {
              popContext(workInProgress);
            }

            var effectTag = workInProgress.effectTag;

            if (effectTag & ShouldCapture) {
              workInProgress.effectTag = effectTag & ~ShouldCapture | DidCapture;
              return workInProgress;
            }

            return null;
          }

        case HostRoot:
          {
            popHostContainer(workInProgress);
            popTopLevelContextObject(workInProgress);
            var _effectTag = workInProgress.effectTag;

            (function () {
              if (!((_effectTag & DidCapture) === NoEffect)) {
                {
                  throw ReactError(Error("The root failed to unmount after an error. This is likely a bug in React. Please file an issue."));
                }
              }
            })();

            workInProgress.effectTag = _effectTag & ~ShouldCapture | DidCapture;
            return workInProgress;
          }

        case HostComponent:
          {
            // TODO: popHydrationState
            popHostContext(workInProgress);
            return null;
          }

        case SuspenseComponent:
          {
            popSuspenseContext(workInProgress);

            if (enableSuspenseServerRenderer) {
              var suspenseState = workInProgress.memoizedState;

              if (suspenseState !== null && suspenseState.dehydrated !== null) {
                (function () {
                  if (!(workInProgress.alternate !== null)) {
                    {
                      throw ReactError(Error("Threw in newly mounted dehydrated component. This is likely a bug in React. Please file an issue."));
                    }
                  }
                })();

                resetHydrationState();
              }
            }

            var _effectTag2 = workInProgress.effectTag;

            if (_effectTag2 & ShouldCapture) {
              workInProgress.effectTag = _effectTag2 & ~ShouldCapture | DidCapture; // Captured a suspense effect. Re-render the boundary.

              return workInProgress;
            }

            return null;
          }

        case SuspenseListComponent:
          {
            popSuspenseContext(workInProgress); // SuspenseList doesn't actually catch anything. It should've been
            // caught by a nested boundary. If not, it should bubble through.

            return null;
          }

        case HostPortal:
          popHostContainer(workInProgress);
          return null;

        case ContextProvider:
          popProvider(workInProgress);
          return null;

        default:
          return null;
      }
    }

    function unwindInterruptedWork(interruptedWork) {
      switch (interruptedWork.tag) {
        case ClassComponent:
          {
            var childContextTypes = interruptedWork.type.childContextTypes;

            if (childContextTypes !== null && childContextTypes !== undefined) {
              popContext(interruptedWork);
            }

            break;
          }

        case HostRoot:
          {
            popHostContainer(interruptedWork);
            popTopLevelContextObject(interruptedWork);
            break;
          }

        case HostComponent:
          {
            popHostContext(interruptedWork);
            break;
          }

        case HostPortal:
          popHostContainer(interruptedWork);
          break;

        case SuspenseComponent:
          popSuspenseContext(interruptedWork);
          break;

        case SuspenseListComponent:
          popSuspenseContext(interruptedWork);
          break;

        case ContextProvider:
          popProvider(interruptedWork);
          break;

        default:
          break;
      }
    }

    function createCapturedValue(value, source) {
      // If the value is an error, call this function immediately after it is thrown
      // so the stack is accurate.
      return {
        value: value,
        source: source,
        stack: getStackByFiberInDevAndProd(source)
      };
    } // This module is forked in different environments.
    // By default, return `true` to log errors to the console.
    // Forks can return `false` if this isn't desirable.


    function showErrorDialog(capturedError) {
      return true;
    }

    function logCapturedError(capturedError) {
      var logError = showErrorDialog(capturedError); // Allow injected showErrorDialog() to prevent default console.error logging.
      // This enables renderers like ReactNative to better manage redbox behavior.

      if (logError === false) {
        return;
      }

      var error = capturedError.error;
      {
        var componentName = capturedError.componentName,
            componentStack = capturedError.componentStack,
            errorBoundaryName = capturedError.errorBoundaryName,
            errorBoundaryFound = capturedError.errorBoundaryFound,
            willRetry = capturedError.willRetry; // Browsers support silencing uncaught errors by calling
        // `preventDefault()` in window `error` handler.
        // We record this information as an expando on the error.

        if (error != null && error._suppressLogging) {
          if (errorBoundaryFound && willRetry) {
            // The error is recoverable and was silenced.
            // Ignore it and don't print the stack addendum.
            // This is handy for testing error boundaries without noise.
            return;
          } // The error is fatal. Since the silencing might have
          // been accidental, we'll surface it anyway.
          // However, the browser would have silenced the original error
          // so we'll print it first, and then print the stack addendum.


          console.error(error); // For a more detailed description of this block, see:
          // https://github.com/facebook/react/pull/13384
        }

        var componentNameMessage = componentName ? "The above error occurred in the <" + componentName + "> component:" : 'The above error occurred in one of your React components:';
        var errorBoundaryMessage; // errorBoundaryFound check is sufficient; errorBoundaryName check is to satisfy Flow.

        if (errorBoundaryFound && errorBoundaryName) {
          if (willRetry) {
            errorBoundaryMessage = "React will try to recreate this component tree from scratch " + ("using the error boundary you provided, " + errorBoundaryName + ".");
          } else {
            errorBoundaryMessage = "This error was initially handled by the error boundary " + errorBoundaryName + ".\n" + "Recreating the tree from scratch failed so React will unmount the tree.";
          }
        } else {
          errorBoundaryMessage = 'Consider adding an error boundary to your tree to customize error handling behavior.\n' + 'Visit https://fb.me/react-error-boundaries to learn more about error boundaries.';
        }

        var combinedMessage = "" + componentNameMessage + componentStack + "\n\n" + ("" + errorBoundaryMessage); // In development, we provide our own message with just the component stack.
        // We don't include the original error message and JS stack because the browser
        // has already printed it. Even if the application swallows the error, it is still
        // displayed by the browser thanks to the DEV-only fake event trick in ReactErrorUtils.

        console.error(combinedMessage);
      }
    }

    var didWarnAboutUndefinedSnapshotBeforeUpdate = null;
    {
      didWarnAboutUndefinedSnapshotBeforeUpdate = new Set();
    }
    var PossiblyWeakSet = typeof WeakSet === 'function' ? WeakSet : Set;

    function logError(boundary, errorInfo) {
      var source = errorInfo.source;
      var stack = errorInfo.stack;

      if (stack === null && source !== null) {
        stack = getStackByFiberInDevAndProd(source);
      }

      var capturedError = {
        componentName: source !== null ? getComponentName(source.type) : null,
        componentStack: stack !== null ? stack : '',
        error: errorInfo.value,
        errorBoundary: null,
        errorBoundaryName: null,
        errorBoundaryFound: false,
        willRetry: false
      };

      if (boundary !== null && boundary.tag === ClassComponent) {
        capturedError.errorBoundary = boundary.stateNode;
        capturedError.errorBoundaryName = getComponentName(boundary.type);
        capturedError.errorBoundaryFound = true;
        capturedError.willRetry = true;
      }

      try {
        logCapturedError(capturedError);
      } catch (e) {
        // This method must not throw, or React internal state will get messed up.
        // If console.error is overridden, or logCapturedError() shows a dialog that throws,
        // we want to report this error outside of the normal stack as a last resort.
        // https://github.com/facebook/react/issues/13188
        setTimeout(function () {
          throw e;
        });
      }
    }

    var callComponentWillUnmountWithTimer = function (current$$1, instance) {
      startPhaseTimer(current$$1, 'componentWillUnmount');
      instance.props = current$$1.memoizedProps;
      instance.state = current$$1.memoizedState;
      instance.componentWillUnmount();
      stopPhaseTimer();
    }; // Capture errors so they don't interrupt unmounting.


    function safelyCallComponentWillUnmount(current$$1, instance) {
      {
        invokeGuardedCallback(null, callComponentWillUnmountWithTimer, null, current$$1, instance);

        if (hasCaughtError()) {
          var unmountError = clearCaughtError();
          captureCommitPhaseError(current$$1, unmountError);
        }
      }
    }

    function safelyDetachRef(current$$1) {
      var ref = current$$1.ref;

      if (ref !== null) {
        if (typeof ref === 'function') {
          {
            invokeGuardedCallback(null, ref, null, null);

            if (hasCaughtError()) {
              var refError = clearCaughtError();
              captureCommitPhaseError(current$$1, refError);
            }
          }
        } else {
          ref.current = null;
        }
      }
    }

    function safelyCallDestroy(current$$1, destroy) {
      {
        invokeGuardedCallback(null, destroy, null);

        if (hasCaughtError()) {
          var error = clearCaughtError();
          captureCommitPhaseError(current$$1, error);
        }
      }
    }

    function commitBeforeMutationLifeCycles(current$$1, finishedWork) {
      switch (finishedWork.tag) {
        case FunctionComponent:
        case ForwardRef:
        case SimpleMemoComponent:
          {
            commitHookEffectList(UnmountSnapshot, NoEffect$1, finishedWork);
            return;
          }

        case ClassComponent:
          {
            if (finishedWork.effectTag & Snapshot) {
              if (current$$1 !== null) {
                var prevProps = current$$1.memoizedProps;
                var prevState = current$$1.memoizedState;
                startPhaseTimer(finishedWork, 'getSnapshotBeforeUpdate');
                var instance = finishedWork.stateNode; // We could update instance props and state here,
                // but instead we rely on them being set during last render.
                // TODO: revisit this when we implement resuming.

                {
                  if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                    !(instance.props === finishedWork.memoizedProps) ? warning$1(false, 'Expected %s props to match memoized props before ' + 'getSnapshotBeforeUpdate. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance') : void 0;
                    !(instance.state === finishedWork.memoizedState) ? warning$1(false, 'Expected %s state to match memoized state before ' + 'getSnapshotBeforeUpdate. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance') : void 0;
                  }
                }
                var snapshot = instance.getSnapshotBeforeUpdate(finishedWork.elementType === finishedWork.type ? prevProps : resolveDefaultProps(finishedWork.type, prevProps), prevState);
                {
                  var didWarnSet = didWarnAboutUndefinedSnapshotBeforeUpdate;

                  if (snapshot === undefined && !didWarnSet.has(finishedWork.type)) {
                    didWarnSet.add(finishedWork.type);
                    warningWithoutStack$1(false, '%s.getSnapshotBeforeUpdate(): A snapshot value (or null) ' + 'must be returned. You have returned undefined.', getComponentName(finishedWork.type));
                  }
                }
                instance.__reactInternalSnapshotBeforeUpdate = snapshot;
                stopPhaseTimer();
              }
            }

            return;
          }

        case HostRoot:
        case HostComponent:
        case HostText:
        case HostPortal:
        case IncompleteClassComponent:
          // Nothing to do for these component types
          return;

        default:
          {
            (function () {
              {
                {
                  throw ReactError(Error("This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue."));
                }
              }
            })();
          }
      }
    }

    function commitHookEffectList(unmountTag, mountTag, finishedWork) {
      var updateQueue = finishedWork.updateQueue;
      var lastEffect = updateQueue !== null ? updateQueue.lastEffect : null;

      if (lastEffect !== null) {
        var firstEffect = lastEffect.next;
        var effect = firstEffect;

        do {
          if ((effect.tag & unmountTag) !== NoEffect$1) {
            // Unmount
            var destroy = effect.destroy;
            effect.destroy = undefined;

            if (destroy !== undefined) {
              destroy();
            }
          }

          if ((effect.tag & mountTag) !== NoEffect$1) {
            // Mount
            var create = effect.create;
            effect.destroy = create();
            {
              var _destroy = effect.destroy;

              if (_destroy !== undefined && typeof _destroy !== 'function') {
                var addendum = void 0;

                if (_destroy === null) {
                  addendum = ' You returned null. If your effect does not require clean ' + 'up, return undefined (or nothing).';
                } else if (typeof _destroy.then === 'function') {
                  addendum = '\n\nIt looks like you wrote useEffect(async () => ...) or returned a Promise. ' + 'Instead, write the async function inside your effect ' + 'and call it immediately:\n\n' + 'useEffect(() => {\n' + '  async function fetchData() {\n' + '    // You can await here\n' + '    const response = await MyAPI.getData(someId);\n' + '    // ...\n' + '  }\n' + '  fetchData();\n' + "}, [someId]); // Or [] if effect doesn't need props or state\n\n" + 'Learn more about data fetching with Hooks: https://fb.me/react-hooks-data-fetching';
                } else {
                  addendum = ' You returned: ' + _destroy;
                }

                warningWithoutStack$1(false, 'An effect function must not return anything besides a function, ' + 'which is used for clean-up.%s%s', addendum, getStackByFiberInDevAndProd(finishedWork));
              }
            }
          }

          effect = effect.next;
        } while (effect !== firstEffect);
      }
    }

    function commitPassiveHookEffects(finishedWork) {
      if ((finishedWork.effectTag & Passive) !== NoEffect) {
        switch (finishedWork.tag) {
          case FunctionComponent:
          case ForwardRef:
          case SimpleMemoComponent:
            {
              commitHookEffectList(UnmountPassive, NoEffect$1, finishedWork);
              commitHookEffectList(NoEffect$1, MountPassive, finishedWork);
              break;
            }

          default:
            break;
        }
      }
    }

    function commitLifeCycles(finishedRoot, current$$1, finishedWork, committedExpirationTime) {
      switch (finishedWork.tag) {
        case FunctionComponent:
        case ForwardRef:
        case SimpleMemoComponent:
          {
            commitHookEffectList(UnmountLayout, MountLayout, finishedWork);
            break;
          }

        case ClassComponent:
          {
            var instance = finishedWork.stateNode;

            if (finishedWork.effectTag & Update) {
              if (current$$1 === null) {
                startPhaseTimer(finishedWork, 'componentDidMount'); // We could update instance props and state here,
                // but instead we rely on them being set during last render.
                // TODO: revisit this when we implement resuming.

                {
                  if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                    !(instance.props === finishedWork.memoizedProps) ? warning$1(false, 'Expected %s props to match memoized props before ' + 'componentDidMount. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance') : void 0;
                    !(instance.state === finishedWork.memoizedState) ? warning$1(false, 'Expected %s state to match memoized state before ' + 'componentDidMount. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance') : void 0;
                  }
                }
                instance.componentDidMount();
                stopPhaseTimer();
              } else {
                var prevProps = finishedWork.elementType === finishedWork.type ? current$$1.memoizedProps : resolveDefaultProps(finishedWork.type, current$$1.memoizedProps);
                var prevState = current$$1.memoizedState;
                startPhaseTimer(finishedWork, 'componentDidUpdate'); // We could update instance props and state here,
                // but instead we rely on them being set during last render.
                // TODO: revisit this when we implement resuming.

                {
                  if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                    !(instance.props === finishedWork.memoizedProps) ? warning$1(false, 'Expected %s props to match memoized props before ' + 'componentDidUpdate. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance') : void 0;
                    !(instance.state === finishedWork.memoizedState) ? warning$1(false, 'Expected %s state to match memoized state before ' + 'componentDidUpdate. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance') : void 0;
                  }
                }
                instance.componentDidUpdate(prevProps, prevState, instance.__reactInternalSnapshotBeforeUpdate);
                stopPhaseTimer();
              }
            }

            var updateQueue = finishedWork.updateQueue;

            if (updateQueue !== null) {
              {
                if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                  !(instance.props === finishedWork.memoizedProps) ? warning$1(false, 'Expected %s props to match memoized props before ' + 'processing the update queue. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance') : void 0;
                  !(instance.state === finishedWork.memoizedState) ? warning$1(false, 'Expected %s state to match memoized state before ' + 'processing the update queue. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance') : void 0;
                }
              } // We could update instance props and state here,
              // but instead we rely on them being set during last render.
              // TODO: revisit this when we implement resuming.

              commitUpdateQueue(finishedWork, updateQueue, instance, committedExpirationTime);
            }

            return;
          }

        case HostRoot:
          {
            var _updateQueue = finishedWork.updateQueue;

            if (_updateQueue !== null) {
              var _instance = null;

              if (finishedWork.child !== null) {
                switch (finishedWork.child.tag) {
                  case HostComponent:
                    _instance = getPublicInstance(finishedWork.child.stateNode);
                    break;

                  case ClassComponent:
                    _instance = finishedWork.child.stateNode;
                    break;
                }
              }

              commitUpdateQueue(finishedWork, _updateQueue, _instance, committedExpirationTime);
            }

            return;
          }

        case HostComponent:
          {
            var _instance2 = finishedWork.stateNode; // Renderers may schedule work to be done after host components are mounted
            // (eg DOM renderer may schedule auto-focus for inputs and form controls).
            // These effects should only be committed when components are first mounted,
            // aka when there is no current/alternate.

            if (current$$1 === null && finishedWork.effectTag & Update) {
              var type = finishedWork.type;
              var props = finishedWork.memoizedProps;
              commitMount(_instance2, type, props, finishedWork);
            }

            return;
          }

        case HostText:
          {
            // We have no life-cycles associated with text.
            return;
          }

        case HostPortal:
          {
            // We have no life-cycles associated with portals.
            return;
          }

        case Profiler:
          {
            if (enableProfilerTimer) {
              var onRender = finishedWork.memoizedProps.onRender;

              if (typeof onRender === 'function') {
                if (enableSchedulerTracing) {
                  onRender(finishedWork.memoizedProps.id, current$$1 === null ? 'mount' : 'update', finishedWork.actualDuration, finishedWork.treeBaseDuration, finishedWork.actualStartTime, getCommitTime(), finishedRoot.memoizedInteractions);
                } else {
                  onRender(finishedWork.memoizedProps.id, current$$1 === null ? 'mount' : 'update', finishedWork.actualDuration, finishedWork.treeBaseDuration, finishedWork.actualStartTime, getCommitTime());
                }
              }
            }

            return;
          }

        case SuspenseComponent:
          {
            commitSuspenseHydrationCallbacks(finishedRoot, finishedWork);
            return;
          }

        case SuspenseListComponent:
        case IncompleteClassComponent:
        case FundamentalComponent:
        case ScopeComponent:
          return;

        default:
          {
            (function () {
              {
                {
                  throw ReactError(Error("This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue."));
                }
              }
            })();
          }
      }
    }

    function hideOrUnhideAllChildren(finishedWork, isHidden) {
      if (supportsMutation) {
        // We only have the top Fiber that was inserted but we need to recurse down its
        // children to find all the terminal nodes.
        var node = finishedWork;

        while (true) {
          if (node.tag === HostComponent) {
            var instance = node.stateNode;

            if (isHidden) {
              hideInstance(instance);
            } else {
              unhideInstance(node.stateNode, node.memoizedProps);
            }
          } else if (node.tag === HostText) {
            var _instance3 = node.stateNode;

            if (isHidden) {
              hideTextInstance(_instance3);
            } else {
              unhideTextInstance(_instance3, node.memoizedProps);
            }
          } else if (node.tag === SuspenseComponent && node.memoizedState !== null && node.memoizedState.dehydrated === null) {
            // Found a nested Suspense component that timed out. Skip over the
            // primary child fragment, which should remain hidden.
            var fallbackChildFragment = node.child.sibling;
            fallbackChildFragment.return = node;
            node = fallbackChildFragment;
            continue;
          } else if (node.child !== null) {
            node.child.return = node;
            node = node.child;
            continue;
          }

          if (node === finishedWork) {
            return;
          }

          while (node.sibling === null) {
            if (node.return === null || node.return === finishedWork) {
              return;
            }

            node = node.return;
          }

          node.sibling.return = node.return;
          node = node.sibling;
        }
      }
    }

    function commitAttachRef(finishedWork) {
      var ref = finishedWork.ref;

      if (ref !== null) {
        var instance = finishedWork.stateNode;
        var instanceToUse;

        switch (finishedWork.tag) {
          case HostComponent:
            instanceToUse = getPublicInstance(instance);
            break;

          default:
            instanceToUse = instance;
        } // Moved outside to ensure DCE works with this flag


        if (enableScopeAPI && finishedWork.tag === ScopeComponent) {
          instanceToUse = instance.methods;
        }

        if (typeof ref === 'function') {
          ref(instanceToUse);
        } else {
          {
            if (!ref.hasOwnProperty('current')) {
              warningWithoutStack$1(false, 'Unexpected ref object provided for %s. ' + 'Use either a ref-setter function or React.createRef().%s', getComponentName(finishedWork.type), getStackByFiberInDevAndProd(finishedWork));
            }
          }
          ref.current = instanceToUse;
        }
      }
    }

    function commitDetachRef(current$$1) {
      var currentRef = current$$1.ref;

      if (currentRef !== null) {
        if (typeof currentRef === 'function') {
          currentRef(null);
        } else {
          currentRef.current = null;
        }
      }
    } // User-originating errors (lifecycles and refs) should not interrupt
    // deletion, so don't let them throw. Host-originating errors should
    // interrupt deletion, so it's okay


    function commitUnmount(finishedRoot, current$$1, renderPriorityLevel) {
      onCommitUnmount(current$$1);

      switch (current$$1.tag) {
        case FunctionComponent:
        case ForwardRef:
        case MemoComponent:
        case SimpleMemoComponent:
          {
            var updateQueue = current$$1.updateQueue;

            if (updateQueue !== null) {
              var lastEffect = updateQueue.lastEffect;

              if (lastEffect !== null) {
                var firstEffect = lastEffect.next; // When the owner fiber is deleted, the destroy function of a passive
                // effect hook is called during the synchronous commit phase. This is
                // a concession to implementation complexity. Calling it in the
                // passive effect phase (like they usually are, when dependencies
                // change during an update) would require either traversing the
                // children of the deleted fiber again, or including unmount effects
                // as part of the fiber effect list.
                //
                // Because this is during the sync commit phase, we need to change
                // the priority.
                //
                // TODO: Reconsider this implementation trade off.

                var priorityLevel = renderPriorityLevel > NormalPriority ? NormalPriority : renderPriorityLevel;
                runWithPriority$2(priorityLevel, function () {
                  var effect = firstEffect;

                  do {
                    var destroy = effect.destroy;

                    if (destroy !== undefined) {
                      safelyCallDestroy(current$$1, destroy);
                    }

                    effect = effect.next;
                  } while (effect !== firstEffect);
                });
              }
            }

            break;
          }

        case ClassComponent:
          {
            safelyDetachRef(current$$1);
            var instance = current$$1.stateNode;

            if (typeof instance.componentWillUnmount === 'function') {
              safelyCallComponentWillUnmount(current$$1, instance);
            }

            return;
          }

        case HostComponent:
          {
            if (enableFlareAPI) {
              var dependencies = current$$1.dependencies;

              if (dependencies !== null) {
                var respondersMap = dependencies.responders;

                if (respondersMap !== null) {
                  var responderInstances = Array.from(respondersMap.values());

                  for (var i = 0, length = responderInstances.length; i < length; i++) {
                    var responderInstance = responderInstances[i];
                    unmountResponderInstance(responderInstance);
                  }

                  dependencies.responders = null;
                }
              }
            }

            safelyDetachRef(current$$1);
            return;
          }

        case HostPortal:
          {
            // TODO: this is recursive.
            // We are also not using this parent because
            // the portal will get pushed immediately.
            if (supportsMutation) {
              unmountHostComponents(finishedRoot, current$$1, renderPriorityLevel);
            } else if (supportsPersistence) {
              emptyPortalContainer(current$$1);
            }

            return;
          }

        case FundamentalComponent:
          {
            if (enableFundamentalAPI) {
              var fundamentalInstance = current$$1.stateNode;

              if (fundamentalInstance !== null) {
                unmountFundamentalComponent(fundamentalInstance);
                current$$1.stateNode = null;
              }
            }

            return;
          }

        case DehydratedFragment:
          {
            if (enableSuspenseCallback) {
              var hydrationCallbacks = finishedRoot.hydrationCallbacks;

              if (hydrationCallbacks !== null) {
                var onDeleted = hydrationCallbacks.onDeleted;

                if (onDeleted) {
                  onDeleted(current$$1.stateNode);
                }
              }
            }

            return;
          }

        case ScopeComponent:
          {
            if (enableScopeAPI) {
              safelyDetachRef(current$$1);
            }
          }
      }
    }

    function commitNestedUnmounts(finishedRoot, root, renderPriorityLevel) {
      // While we're inside a removed host node we don't want to call
      // removeChild on the inner nodes because they're removed by the top
      // call anyway. We also want to call componentWillUnmount on all
      // composites before this host node is removed from the tree. Therefore
      // we do an inner loop while we're still inside the host node.
      var node = root;

      while (true) {
        commitUnmount(finishedRoot, node, renderPriorityLevel); // Visit children because they may contain more composite or host nodes.
        // Skip portals because commitUnmount() currently visits them recursively.

        if (node.child !== null && ( // If we use mutation we drill down into portals using commitUnmount above.
        // If we don't use mutation we drill down into portals here instead.
        !supportsMutation || node.tag !== HostPortal)) {
          node.child.return = node;
          node = node.child;
          continue;
        }

        if (node === root) {
          return;
        }

        while (node.sibling === null) {
          if (node.return === null || node.return === root) {
            return;
          }

          node = node.return;
        }

        node.sibling.return = node.return;
        node = node.sibling;
      }
    }

    function detachFiber(current$$1) {
      var alternate = current$$1.alternate; // Cut off the return pointers to disconnect it from the tree. Ideally, we
      // should clear the child pointer of the parent alternate to let this
      // get GC:ed but we don't know which for sure which parent is the current
      // one so we'll settle for GC:ing the subtree of this child. This child
      // itself will be GC:ed when the parent updates the next time.

      current$$1.return = null;
      current$$1.child = null;
      current$$1.memoizedState = null;
      current$$1.updateQueue = null;
      current$$1.dependencies = null;
      current$$1.alternate = null;
      current$$1.firstEffect = null;
      current$$1.lastEffect = null;
      current$$1.pendingProps = null;
      current$$1.memoizedProps = null;

      if (alternate !== null) {
        detachFiber(alternate);
      }
    }

    function emptyPortalContainer(current$$1) {
      if (!supportsPersistence) {
        return;
      }

      var portal = current$$1.stateNode;
      var containerInfo = portal.containerInfo;
      var emptyChildSet = createContainerChildSet(containerInfo);
      replaceContainerChildren(containerInfo, emptyChildSet);
    }

    function commitContainer(finishedWork) {
      if (!supportsPersistence) {
        return;
      }

      switch (finishedWork.tag) {
        case ClassComponent:
        case HostComponent:
        case HostText:
        case FundamentalComponent:
          {
            return;
          }

        case HostRoot:
        case HostPortal:
          {
            var portalOrRoot = finishedWork.stateNode;
            var containerInfo = portalOrRoot.containerInfo,
                pendingChildren = portalOrRoot.pendingChildren;
            replaceContainerChildren(containerInfo, pendingChildren);
            return;
          }

        default:
          {
            (function () {
              {
                {
                  throw ReactError(Error("This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue."));
                }
              }
            })();
          }
      }
    }

    function getHostParentFiber(fiber) {
      var parent = fiber.return;

      while (parent !== null) {
        if (isHostParent(parent)) {
          return parent;
        }

        parent = parent.return;
      }

      (function () {
        {
          {
            throw ReactError(Error("Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue."));
          }
        }
      })();
    }

    function isHostParent(fiber) {
      return fiber.tag === HostComponent || fiber.tag === HostRoot || fiber.tag === HostPortal;
    }

    function getHostSibling(fiber) {
      // We're going to search forward into the tree until we find a sibling host
      // node. Unfortunately, if multiple insertions are done in a row we have to
      // search past them. This leads to exponential search for the next sibling.
      // TODO: Find a more efficient way to do this.
      var node = fiber;

      siblings: while (true) {
        // If we didn't find anything, let's try the next sibling.
        while (node.sibling === null) {
          if (node.return === null || isHostParent(node.return)) {
            // If we pop out of the root or hit the parent the fiber we are the
            // last sibling.
            return null;
          }

          node = node.return;
        }

        node.sibling.return = node.return;
        node = node.sibling;

        while (node.tag !== HostComponent && node.tag !== HostText && node.tag !== DehydratedFragment) {
          // If it is not host node and, we might have a host node inside it.
          // Try to search down until we find one.
          if (node.effectTag & Placement) {
            // If we don't have a child, try the siblings instead.
            continue siblings;
          } // If we don't have a child, try the siblings instead.
          // We also skip portals because they are not part of this host tree.


          if (node.child === null || node.tag === HostPortal) {
            continue siblings;
          } else {
            node.child.return = node;
            node = node.child;
          }
        } // Check if this host node is stable or about to be placed.


        if (!(node.effectTag & Placement)) {
          // Found it!
          return node.stateNode;
        }
      }
    }

    function commitPlacement(finishedWork) {
      if (!supportsMutation) {
        return;
      } // Recursively insert all host nodes into the parent.


      var parentFiber = getHostParentFiber(finishedWork); // Note: these two variables *must* always be updated together.

      var parent;
      var isContainer;
      var parentStateNode = parentFiber.stateNode;

      switch (parentFiber.tag) {
        case HostComponent:
          parent = parentStateNode;
          isContainer = false;
          break;

        case HostRoot:
          parent = parentStateNode.containerInfo;
          isContainer = true;
          break;

        case HostPortal:
          parent = parentStateNode.containerInfo;
          isContainer = true;
          break;

        case FundamentalComponent:
          if (enableFundamentalAPI) {
            parent = parentStateNode.instance;
            isContainer = false;
          }

        // eslint-disable-next-line-no-fallthrough

        default:
          (function () {
            {
              {
                throw ReactError(Error("Invalid host parent fiber. This error is likely caused by a bug in React. Please file an issue."));
              }
            }
          })();

      }

      if (parentFiber.effectTag & ContentReset) {
        // Reset the text content of the parent before doing any insertions
        resetTextContent(parent); // Clear ContentReset from the effect tag

        parentFiber.effectTag &= ~ContentReset;
      }

      var before = getHostSibling(finishedWork); // We only have the top Fiber that was inserted but we need to recurse down its
      // children to find all the terminal nodes.

      var node = finishedWork;

      while (true) {
        var isHost = node.tag === HostComponent || node.tag === HostText;

        if (isHost || enableFundamentalAPI && node.tag === FundamentalComponent) {
          var stateNode = isHost ? node.stateNode : node.stateNode.instance;

          if (before) {
            if (isContainer) {
              insertInContainerBefore(parent, stateNode, before);
            } else {
              insertBefore(parent, stateNode, before);
            }
          } else {
            if (isContainer) {
              appendChildToContainer(parent, stateNode);
            } else {
              appendChild(parent, stateNode);
            }
          }
        } else if (node.tag === HostPortal) {// If the insertion itself is a portal, then we don't want to traverse
          // down its children. Instead, we'll get insertions from each child in
          // the portal directly.
        } else if (node.child !== null) {
          node.child.return = node;
          node = node.child;
          continue;
        }

        if (node === finishedWork) {
          return;
        }

        while (node.sibling === null) {
          if (node.return === null || node.return === finishedWork) {
            return;
          }

          node = node.return;
        }

        node.sibling.return = node.return;
        node = node.sibling;
      }
    }

    function unmountHostComponents(finishedRoot, current$$1, renderPriorityLevel) {
      // We only have the top Fiber that was deleted but we need to recurse down its
      // children to find all the terminal nodes.
      var node = current$$1; // Each iteration, currentParent is populated with node's host parent if not
      // currentParentIsValid.

      var currentParentIsValid = false; // Note: these two variables *must* always be updated together.

      var currentParent;
      var currentParentIsContainer;

      while (true) {
        if (!currentParentIsValid) {
          var parent = node.return;

          findParent: while (true) {
            (function () {
              if (!(parent !== null)) {
                {
                  throw ReactError(Error("Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue."));
                }
              }
            })();

            var parentStateNode = parent.stateNode;

            switch (parent.tag) {
              case HostComponent:
                currentParent = parentStateNode;
                currentParentIsContainer = false;
                break findParent;

              case HostRoot:
                currentParent = parentStateNode.containerInfo;
                currentParentIsContainer = true;
                break findParent;

              case HostPortal:
                currentParent = parentStateNode.containerInfo;
                currentParentIsContainer = true;
                break findParent;

              case FundamentalComponent:
                if (enableFundamentalAPI) {
                  currentParent = parentStateNode.instance;
                  currentParentIsContainer = false;
                }

            }

            parent = parent.return;
          }

          currentParentIsValid = true;
        }

        if (node.tag === HostComponent || node.tag === HostText) {
          commitNestedUnmounts(finishedRoot, node, renderPriorityLevel); // After all the children have unmounted, it is now safe to remove the
          // node from the tree.

          if (currentParentIsContainer) {
            removeChildFromContainer(currentParent, node.stateNode);
          } else {
            removeChild(currentParent, node.stateNode);
          } // Don't visit children because we already visited them.

        } else if (enableFundamentalAPI && node.tag === FundamentalComponent) {
          var fundamentalNode = node.stateNode.instance;
          commitNestedUnmounts(finishedRoot, node, renderPriorityLevel); // After all the children have unmounted, it is now safe to remove the
          // node from the tree.

          if (currentParentIsContainer) {
            removeChildFromContainer(currentParent, fundamentalNode);
          } else {
            removeChild(currentParent, fundamentalNode);
          }
        } else if (enableSuspenseServerRenderer && node.tag === DehydratedFragment) {
          if (enableSuspenseCallback) {
            var hydrationCallbacks = finishedRoot.hydrationCallbacks;

            if (hydrationCallbacks !== null) {
              var onDeleted = hydrationCallbacks.onDeleted;

              if (onDeleted) {
                onDeleted(node.stateNode);
              }
            }
          } // Delete the dehydrated suspense boundary and all of its content.


          if (currentParentIsContainer) {
            clearSuspenseBoundaryFromContainer(currentParent, node.stateNode);
          } else {
            clearSuspenseBoundary(currentParent, node.stateNode);
          }
        } else if (node.tag === HostPortal) {
          if (node.child !== null) {
            // When we go into a portal, it becomes the parent to remove from.
            // We will reassign it back when we pop the portal on the way up.
            currentParent = node.stateNode.containerInfo;
            currentParentIsContainer = true; // Visit children because portals might contain host components.

            node.child.return = node;
            node = node.child;
            continue;
          }
        } else {
          commitUnmount(finishedRoot, node, renderPriorityLevel); // Visit children because we may find more host components below.

          if (node.child !== null) {
            node.child.return = node;
            node = node.child;
            continue;
          }
        }

        if (node === current$$1) {
          return;
        }

        while (node.sibling === null) {
          if (node.return === null || node.return === current$$1) {
            return;
          }

          node = node.return;

          if (node.tag === HostPortal) {
            // When we go out of the portal, we need to restore the parent.
            // Since we don't keep a stack of them, we will search for it.
            currentParentIsValid = false;
          }
        }

        node.sibling.return = node.return;
        node = node.sibling;
      }
    }

    function commitDeletion(finishedRoot, current$$1, renderPriorityLevel) {
      if (supportsMutation) {
        // Recursively delete all host nodes from the parent.
        // Detach refs and call componentWillUnmount() on the whole subtree.
        unmountHostComponents(finishedRoot, current$$1, renderPriorityLevel);
      } else {
        // Detach refs and call componentWillUnmount() on the whole subtree.
        commitNestedUnmounts(finishedRoot, current$$1, renderPriorityLevel);
      }

      detachFiber(current$$1);
    }

    function commitWork(current$$1, finishedWork) {
      if (!supportsMutation) {
        switch (finishedWork.tag) {
          case FunctionComponent:
          case ForwardRef:
          case MemoComponent:
          case SimpleMemoComponent:
            {
              // Note: We currently never use MountMutation, but useLayout uses
              // UnmountMutation.
              commitHookEffectList(UnmountMutation, MountMutation, finishedWork);
              return;
            }

          case Profiler:
            {
              return;
            }

          case SuspenseComponent:
            {
              commitSuspenseComponent(finishedWork);
              attachSuspenseRetryListeners(finishedWork);
              return;
            }

          case SuspenseListComponent:
            {
              attachSuspenseRetryListeners(finishedWork);
              return;
            }

          case HostRoot:
            {
              if (supportsHydration) {
                var root = finishedWork.stateNode;

                if (root.hydrate) {
                  // We've just hydrated. No need to hydrate again.
                  root.hydrate = false;
                  commitHydratedContainer(root.containerInfo);
                }
              }

              break;
            }
        }

        commitContainer(finishedWork);
        return;
      }

      switch (finishedWork.tag) {
        case FunctionComponent:
        case ForwardRef:
        case MemoComponent:
        case SimpleMemoComponent:
          {
            // Note: We currently never use MountMutation, but useLayout uses
            // UnmountMutation.
            commitHookEffectList(UnmountMutation, MountMutation, finishedWork);
            return;
          }

        case ClassComponent:
          {
            return;
          }

        case HostComponent:
          {
            var instance = finishedWork.stateNode;

            if (instance != null) {
              // Commit the work prepared earlier.
              var newProps = finishedWork.memoizedProps; // For hydration we reuse the update path but we treat the oldProps
              // as the newProps. The updatePayload will contain the real change in
              // this case.

              var oldProps = current$$1 !== null ? current$$1.memoizedProps : newProps;
              var type = finishedWork.type; // TODO: Type the updateQueue to be specific to host components.

              var updatePayload = finishedWork.updateQueue;
              finishedWork.updateQueue = null;

              if (updatePayload !== null) {
                commitUpdate(instance, updatePayload, type, oldProps, newProps, finishedWork);
              }

              if (enableFlareAPI) {
                var prevListeners = oldProps.listeners;
                var nextListeners = newProps.listeners;

                if (prevListeners !== nextListeners) {
                  updateEventListeners(nextListeners, finishedWork, null);
                }
              }
            }

            return;
          }

        case HostText:
          {
            (function () {
              if (!(finishedWork.stateNode !== null)) {
                {
                  throw ReactError(Error("This should have a text node initialized. This error is likely caused by a bug in React. Please file an issue."));
                }
              }
            })();

            var textInstance = finishedWork.stateNode;
            var newText = finishedWork.memoizedProps; // For hydration we reuse the update path but we treat the oldProps
            // as the newProps. The updatePayload will contain the real change in
            // this case.

            var oldText = current$$1 !== null ? current$$1.memoizedProps : newText;
            commitTextUpdate(textInstance, oldText, newText);
            return;
          }

        case HostRoot:
          {
            if (supportsHydration) {
              var _root = finishedWork.stateNode;

              if (_root.hydrate) {
                // We've just hydrated. No need to hydrate again.
                _root.hydrate = false;
                commitHydratedContainer(_root.containerInfo);
              }
            }

            return;
          }

        case Profiler:
          {
            return;
          }

        case SuspenseComponent:
          {
            commitSuspenseComponent(finishedWork);
            attachSuspenseRetryListeners(finishedWork);
            return;
          }

        case SuspenseListComponent:
          {
            attachSuspenseRetryListeners(finishedWork);
            return;
          }

        case IncompleteClassComponent:
          {
            return;
          }

        case FundamentalComponent:
          {
            if (enableFundamentalAPI) {
              var fundamentalInstance = finishedWork.stateNode;
              updateFundamentalComponent(fundamentalInstance);
            }

            return;
          }

        case ScopeComponent:
          {
            if (enableScopeAPI) {
              var scopeInstance = finishedWork.stateNode;
              scopeInstance.fiber = finishedWork;

              if (enableFlareAPI) {
                var _newProps = finishedWork.memoizedProps;

                var _oldProps = current$$1 !== null ? current$$1.memoizedProps : _newProps;

                var _prevListeners = _oldProps.listeners;
                var _nextListeners = _newProps.listeners;

                if (_prevListeners !== _nextListeners) {
                  updateEventListeners(_nextListeners, finishedWork, null);
                }
              }
            }

            return;
          }

        default:
          {
            (function () {
              {
                {
                  throw ReactError(Error("This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue."));
                }
              }
            })();
          }
      }
    }

    function commitSuspenseComponent(finishedWork) {
      var newState = finishedWork.memoizedState;
      var newDidTimeout;
      var primaryChildParent = finishedWork;

      if (newState === null) {
        newDidTimeout = false;
      } else {
        newDidTimeout = true;
        primaryChildParent = finishedWork.child;
        markCommitTimeOfFallback();
      }

      if (supportsMutation && primaryChildParent !== null) {
        hideOrUnhideAllChildren(primaryChildParent, newDidTimeout);
      }

      if (enableSuspenseCallback && newState !== null) {
        var suspenseCallback = finishedWork.memoizedProps.suspenseCallback;

        if (typeof suspenseCallback === 'function') {
          var thenables = finishedWork.updateQueue;

          if (thenables !== null) {
            suspenseCallback(new Set(thenables));
          }
        } else {
          if (suspenseCallback !== undefined) {
            warning$1(false, 'Unexpected type for suspenseCallback.');
          }
        }
      }
    }

    function commitSuspenseHydrationCallbacks(finishedRoot, finishedWork) {
      if (!supportsHydration) {
        return;
      }

      var newState = finishedWork.memoizedState;

      if (newState === null) {
        var current$$1 = finishedWork.alternate;

        if (current$$1 !== null) {
          var prevState = current$$1.memoizedState;

          if (prevState !== null) {
            var suspenseInstance = prevState.dehydrated;

            if (suspenseInstance !== null) {
              commitHydratedSuspenseInstance(suspenseInstance);

              if (enableSuspenseCallback) {
                var hydrationCallbacks = finishedRoot.hydrationCallbacks;

                if (hydrationCallbacks !== null) {
                  var onHydrated = hydrationCallbacks.onHydrated;

                  if (onHydrated) {
                    onHydrated(suspenseInstance);
                  }
                }
              }
            }
          }
        }
      }
    }

    function attachSuspenseRetryListeners(finishedWork) {
      // If this boundary just timed out, then it will have a set of thenables.
      // For each thenable, attach a listener so that when it resolves, React
      // attempts to re-render the boundary in the primary (pre-timeout) state.
      var thenables = finishedWork.updateQueue;

      if (thenables !== null) {
        finishedWork.updateQueue = null;
        var retryCache = finishedWork.stateNode;

        if (retryCache === null) {
          retryCache = finishedWork.stateNode = new PossiblyWeakSet();
        }

        thenables.forEach(function (thenable) {
          // Memoize using the boundary fiber to prevent redundant listeners.
          var retry = resolveRetryThenable.bind(null, finishedWork, thenable);

          if (!retryCache.has(thenable)) {
            if (enableSchedulerTracing) {
              if (thenable.__reactDoNotTraceInteractions !== true) {
                retry = tracing.unstable_wrap(retry);
              }
            }

            retryCache.add(thenable);
            thenable.then(retry, retry);
          }
        });
      }
    }

    function commitResetTextContent(current$$1) {
      if (!supportsMutation) {
        return;
      }

      resetTextContent(current$$1.stateNode);
    }

    var PossiblyWeakMap$1 = typeof WeakMap === 'function' ? WeakMap : Map;

    function createRootErrorUpdate(fiber, errorInfo, expirationTime) {
      var update = createUpdate(expirationTime, null); // Unmount the root by rendering null.

      update.tag = CaptureUpdate; // Caution: React DevTools currently depends on this property
      // being called "element".

      update.payload = {
        element: null
      };
      var error = errorInfo.value;

      update.callback = function () {
        onUncaughtError(error);
        logError(fiber, errorInfo);
      };

      return update;
    }

    function createClassErrorUpdate(fiber, errorInfo, expirationTime) {
      var update = createUpdate(expirationTime, null);
      update.tag = CaptureUpdate;
      var getDerivedStateFromError = fiber.type.getDerivedStateFromError;

      if (typeof getDerivedStateFromError === 'function') {
        var error = errorInfo.value;

        update.payload = function () {
          logError(fiber, errorInfo);
          return getDerivedStateFromError(error);
        };
      }

      var inst = fiber.stateNode;

      if (inst !== null && typeof inst.componentDidCatch === 'function') {
        update.callback = function callback() {
          {
            markFailedErrorBoundaryForHotReloading(fiber);
          }

          if (typeof getDerivedStateFromError !== 'function') {
            // To preserve the preexisting retry behavior of error boundaries,
            // we keep track of which ones already failed during this batch.
            // This gets reset before we yield back to the browser.
            // TODO: Warn in strict mode if getDerivedStateFromError is
            // not defined.
            markLegacyErrorBoundaryAsFailed(this); // Only log here if componentDidCatch is the only error boundary method defined

            logError(fiber, errorInfo);
          }

          var error = errorInfo.value;
          var stack = errorInfo.stack;
          this.componentDidCatch(error, {
            componentStack: stack !== null ? stack : ''
          });
          {
            if (typeof getDerivedStateFromError !== 'function') {
              // If componentDidCatch is the only error boundary method defined,
              // then it needs to call setState to recover from errors.
              // If no state update is scheduled then the boundary will swallow the error.
              !(fiber.expirationTime === Sync) ? warningWithoutStack$1(false, '%s: Error boundaries should implement getDerivedStateFromError(). ' + 'In that method, return a state update to display an error message or fallback UI.', getComponentName(fiber.type) || 'Unknown') : void 0;
            }
          }
        };
      } else {
        update.callback = function () {
          markFailedErrorBoundaryForHotReloading(fiber);
        };
      }

      return update;
    }

    function attachPingListener(root, renderExpirationTime, thenable) {
      // Attach a listener to the promise to "ping" the root and retry. But
      // only if one does not already exist for the current render expiration
      // time (which acts like a "thread ID" here).
      var pingCache = root.pingCache;
      var threadIDs;

      if (pingCache === null) {
        pingCache = root.pingCache = new PossiblyWeakMap$1();
        threadIDs = new Set();
        pingCache.set(thenable, threadIDs);
      } else {
        threadIDs = pingCache.get(thenable);

        if (threadIDs === undefined) {
          threadIDs = new Set();
          pingCache.set(thenable, threadIDs);
        }
      }

      if (!threadIDs.has(renderExpirationTime)) {
        // Memoize using the thread ID to prevent redundant listeners.
        threadIDs.add(renderExpirationTime);
        var ping = pingSuspendedRoot.bind(null, root, thenable, renderExpirationTime);
        thenable.then(ping, ping);
      }
    }

    function throwException(root, returnFiber, sourceFiber, value, renderExpirationTime) {
      // The source fiber did not complete.
      sourceFiber.effectTag |= Incomplete; // Its effect list is no longer valid.

      sourceFiber.firstEffect = sourceFiber.lastEffect = null;

      if (value !== null && typeof value === 'object' && typeof value.then === 'function') {
        // This is a thenable.
        var thenable = value;
        checkForWrongSuspensePriorityInDEV(sourceFiber);
        var hasInvisibleParentBoundary = hasSuspenseContext(suspenseStackCursor.current, InvisibleParentSuspenseContext); // Schedule the nearest Suspense to re-render the timed out view.

        var _workInProgress = returnFiber;

        do {
          if (_workInProgress.tag === SuspenseComponent && shouldCaptureSuspense(_workInProgress, hasInvisibleParentBoundary)) {
            // Found the nearest boundary.
            // Stash the promise on the boundary fiber. If the boundary times out, we'll
            // attach another listener to flip the boundary back to its normal state.
            var thenables = _workInProgress.updateQueue;

            if (thenables === null) {
              var updateQueue = new Set();
              updateQueue.add(thenable);
              _workInProgress.updateQueue = updateQueue;
            } else {
              thenables.add(thenable);
            } // If the boundary is outside of batched mode, we should *not*
            // suspend the commit. Pretend as if the suspended component rendered
            // null and keep rendering. In the commit phase, we'll schedule a
            // subsequent synchronous update to re-render the Suspense.
            //
            // Note: It doesn't matter whether the component that suspended was
            // inside a batched mode tree. If the Suspense is outside of it, we
            // should *not* suspend the commit.


            if ((_workInProgress.mode & BatchedMode) === NoMode) {
              _workInProgress.effectTag |= DidCapture; // We're going to commit this fiber even though it didn't complete.
              // But we shouldn't call any lifecycle methods or callbacks. Remove
              // all lifecycle effect tags.

              sourceFiber.effectTag &= ~(LifecycleEffectMask | Incomplete);

              if (sourceFiber.tag === ClassComponent) {
                var currentSourceFiber = sourceFiber.alternate;

                if (currentSourceFiber === null) {
                  // This is a new mount. Change the tag so it's not mistaken for a
                  // completed class component. For example, we should not call
                  // componentWillUnmount if it is deleted.
                  sourceFiber.tag = IncompleteClassComponent;
                } else {
                  // When we try rendering again, we should not reuse the current fiber,
                  // since it's known to be in an inconsistent state. Use a force update to
                  // prevent a bail out.
                  var update = createUpdate(Sync, null);
                  update.tag = ForceUpdate;
                  enqueueUpdate(sourceFiber, update);
                }
              } // The source fiber did not complete. Mark it with Sync priority to
              // indicate that it still has pending work.


              sourceFiber.expirationTime = Sync; // Exit without suspending.

              return;
            } // Confirmed that the boundary is in a concurrent mode tree. Continue
            // with the normal suspend path.
            //
            // After this we'll use a set of heuristics to determine whether this
            // render pass will run to completion or restart or "suspend" the commit.
            // The actual logic for this is spread out in different places.
            //
            // This first principle is that if we're going to suspend when we complete
            // a root, then we should also restart if we get an update or ping that
            // might unsuspend it, and vice versa. The only reason to suspend is
            // because you think you might want to restart before committing. However,
            // it doesn't make sense to restart only while in the period we're suspended.
            //
            // Restarting too aggressively is also not good because it starves out any
            // intermediate loading state. So we use heuristics to determine when.
            // Suspense Heuristics
            //
            // If nothing threw a Promise or all the same fallbacks are already showing,
            // then don't suspend/restart.
            //
            // If this is an initial render of a new tree of Suspense boundaries and
            // those trigger a fallback, then don't suspend/restart. We want to ensure
            // that we can show the initial loading state as quickly as possible.
            //
            // If we hit a "Delayed" case, such as when we'd switch from content back into
            // a fallback, then we should always suspend/restart. SuspenseConfig applies to
            // this case. If none is defined, JND is used instead.
            //
            // If we're already showing a fallback and it gets "retried", allowing us to show
            // another level, but there's still an inner boundary that would show a fallback,
            // then we suspend/restart for 500ms since the last time we showed a fallback
            // anywhere in the tree. This effectively throttles progressive loading into a
            // consistent train of commits. This also gives us an opportunity to restart to
            // get to the completed state slightly earlier.
            //
            // If there's ambiguity due to batching it's resolved in preference of:
            // 1) "delayed", 2) "initial render", 3) "retry".
            //
            // We want to ensure that a "busy" state doesn't get force committed. We want to
            // ensure that new initial loading states can commit as soon as possible.


            attachPingListener(root, renderExpirationTime, thenable);
            _workInProgress.effectTag |= ShouldCapture;
            _workInProgress.expirationTime = renderExpirationTime;
            return;
          } // This boundary already captured during this render. Continue to the next
          // boundary.


          _workInProgress = _workInProgress.return;
        } while (_workInProgress !== null); // No boundary was found. Fallthrough to error mode.
        // TODO: Use invariant so the message is stripped in prod?


        value = new Error((getComponentName(sourceFiber.type) || 'A React component') + ' suspended while rendering, but no fallback UI was specified.\n' + '\n' + 'Add a <Suspense fallback=...> component higher in the tree to ' + 'provide a loading indicator or placeholder to display.' + getStackByFiberInDevAndProd(sourceFiber));
      } // We didn't find a boundary that could handle this type of exception. Start
      // over and traverse parent path again, this time treating the exception
      // as an error.


      renderDidError();
      value = createCapturedValue(value, sourceFiber);
      var workInProgress = returnFiber;

      do {
        switch (workInProgress.tag) {
          case HostRoot:
            {
              var _errorInfo = value;
              workInProgress.effectTag |= ShouldCapture;
              workInProgress.expirationTime = renderExpirationTime;

              var _update = createRootErrorUpdate(workInProgress, _errorInfo, renderExpirationTime);

              enqueueCapturedUpdate(workInProgress, _update);
              return;
            }

          case ClassComponent:
            // Capture and retry
            var errorInfo = value;
            var ctor = workInProgress.type;
            var instance = workInProgress.stateNode;

            if ((workInProgress.effectTag & DidCapture) === NoEffect && (typeof ctor.getDerivedStateFromError === 'function' || instance !== null && typeof instance.componentDidCatch === 'function' && !isAlreadyFailedLegacyErrorBoundary(instance))) {
              workInProgress.effectTag |= ShouldCapture;
              workInProgress.expirationTime = renderExpirationTime; // Schedule the error boundary to re-render using updated state

              var _update2 = createClassErrorUpdate(workInProgress, errorInfo, renderExpirationTime);

              enqueueCapturedUpdate(workInProgress, _update2);
              return;
            }

            break;

          default:
            break;
        }

        workInProgress = workInProgress.return;
      } while (workInProgress !== null);
    }

    var ceil = Math.ceil;
    var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher;
    var ReactCurrentOwner$2 = ReactSharedInternals.ReactCurrentOwner;
    var IsSomeRendererActing = ReactSharedInternals.IsSomeRendererActing;
    var NoContext =
    /*                    */
    0;
    var BatchedContext =
    /*               */
    1;
    var EventContext =
    /*                 */
    2;
    var DiscreteEventContext =
    /*         */
    4;
    var LegacyUnbatchedContext =
    /*       */
    8;
    var RenderContext =
    /*                */
    16;
    var CommitContext =
    /*                */
    32;
    var RootIncomplete = 0;
    var RootFatalErrored = 1;
    var RootErrored = 2;
    var RootSuspended = 3;
    var RootSuspendedWithDelay = 4;
    var RootCompleted = 5;
    var RootLocked = 6; // Describes where we are in the React execution stack

    var executionContext = NoContext; // The root we're working on

    var workInProgressRoot = null; // The fiber we're working on

    var workInProgress = null; // The expiration time we're rendering

    var renderExpirationTime = NoWork; // Whether to root completed, errored, suspended, etc.

    var workInProgressRootExitStatus = RootIncomplete; // A fatal error, if one is thrown

    var workInProgressRootFatalError = null; // Most recent event time among processed updates during this render.
    // This is conceptually a time stamp but expressed in terms of an ExpirationTime
    // because we deal mostly with expiration times in the hot path, so this avoids
    // the conversion happening in the hot path.

    var workInProgressRootLatestProcessedExpirationTime = Sync;
    var workInProgressRootLatestSuspenseTimeout = Sync;
    var workInProgressRootCanSuspendUsingConfig = null; // The work left over by components that were visited during this render. Only
    // includes unprocessed updates, not work in bailed out children.

    var workInProgressRootNextUnprocessedUpdateTime = NoWork; // If we're pinged while rendering we don't always restart immediately.
    // This flag determines if it might be worthwhile to restart if an opportunity
    // happens latere.

    var workInProgressRootHasPendingPing = false; // The most recent time we committed a fallback. This lets us ensure a train
    // model where we don't commit new loading states in too quick succession.

    var globalMostRecentFallbackTime = 0;
    var FALLBACK_THROTTLE_MS = 500;
    var nextEffect = null;
    var hasUncaughtError = false;
    var firstUncaughtError = null;
    var legacyErrorBoundariesThatAlreadyFailed = null;
    var rootDoesHavePassiveEffects = false;
    var rootWithPendingPassiveEffects = null;
    var pendingPassiveEffectsRenderPriority = NoPriority;
    var pendingPassiveEffectsExpirationTime = NoWork;
    var rootsWithPendingDiscreteUpdates = null; // Use these to prevent an infinite loop of nested updates

    var NESTED_UPDATE_LIMIT = 50;
    var nestedUpdateCount = 0;
    var rootWithNestedUpdates = null;
    var NESTED_PASSIVE_UPDATE_LIMIT = 50;
    var nestedPassiveUpdateCount = 0;
    var interruptedBy = null; // Marks the need to reschedule pending interactions at these expiration times
    // during the commit phase. This enables them to be traced across components
    // that spawn new work during render. E.g. hidden boundaries, suspended SSR
    // hydration or SuspenseList.

    var spawnedWorkDuringRender = null; // Expiration times are computed by adding to the current time (the start
    // time). However, if two updates are scheduled within the same event, we
    // should treat their start times as simultaneous, even if the actual clock
    // time has advanced between the first and second call.
    // In other words, because expiration times determine how updates are batched,
    // we want all updates of like priority that occur within the same event to
    // receive the same expiration time. Otherwise we get tearing.

    var currentEventTime = NoWork;

    function requestCurrentTime() {
      if ((executionContext & (RenderContext | CommitContext)) !== NoContext) {
        // We're inside React, so it's fine to read the actual time.
        return msToExpirationTime(now());
      } // We're not inside React, so we may be in the middle of a browser event.


      if (currentEventTime !== NoWork) {
        // Use the same start time for all updates until we enter React again.
        return currentEventTime;
      } // This is the first update since React yielded. Compute a new start time.


      currentEventTime = msToExpirationTime(now());
      return currentEventTime;
    }

    function computeExpirationForFiber(currentTime, fiber, suspenseConfig) {
      var mode = fiber.mode;

      if ((mode & BatchedMode) === NoMode) {
        return Sync;
      }

      var priorityLevel = getCurrentPriorityLevel();

      if ((mode & ConcurrentMode) === NoMode) {
        return priorityLevel === ImmediatePriority ? Sync : Batched;
      }

      if ((executionContext & RenderContext) !== NoContext) {
        // Use whatever time we're already rendering
        // TODO: Should there be a way to opt out, like with `runWithPriority`?
        return renderExpirationTime;
      }

      var expirationTime;

      if (suspenseConfig !== null) {
        // Compute an expiration time based on the Suspense timeout.
        expirationTime = computeSuspenseExpiration(currentTime, suspenseConfig.timeoutMs | 0 || LOW_PRIORITY_EXPIRATION);
      } else {
        // Compute an expiration time based on the Scheduler priority.
        switch (priorityLevel) {
          case ImmediatePriority:
            expirationTime = Sync;
            break;

          case UserBlockingPriority$2:
            // TODO: Rename this to computeUserBlockingExpiration
            expirationTime = computeInteractiveExpiration(currentTime);
            break;

          case NormalPriority:
          case LowPriority:
            // TODO: Handle LowPriority
            // TODO: Rename this to... something better.
            expirationTime = computeAsyncExpiration(currentTime);
            break;

          case IdlePriority:
            expirationTime = Idle;
            break;

          default:
            (function () {
              {
                {
                  throw ReactError(Error("Expected a valid priority level"));
                }
              }
            })();

        }
      } // If we're in the middle of rendering a tree, do not update at the same
      // expiration time that is already rendering.
      // TODO: We shouldn't have to do this if the update is on a different root.
      // Refactor computeExpirationForFiber + scheduleUpdate so we have access to
      // the root when we check for this condition.


      if (workInProgressRoot !== null && expirationTime === renderExpirationTime) {
        // This is a trick to move this update into a separate batch
        expirationTime -= 1;
      }

      return expirationTime;
    }

    var lastUniqueAsyncExpiration = NoWork;

    function computeUniqueAsyncExpiration() {
      var currentTime = requestCurrentTime();
      var result = computeAsyncExpiration(currentTime);

      if (result <= lastUniqueAsyncExpiration) {
        // Since we assume the current time monotonically increases, we only hit
        // this branch when computeUniqueAsyncExpiration is fired multiple times
        // within a 200ms window (or whatever the async bucket size is).
        result -= 1;
      }

      lastUniqueAsyncExpiration = result;
      return result;
    }

    function scheduleUpdateOnFiber(fiber, expirationTime) {
      checkForNestedUpdates();
      warnAboutInvalidUpdatesOnClassComponentsInDEV(fiber);
      var root = markUpdateTimeFromFiberToRoot(fiber, expirationTime);

      if (root === null) {
        warnAboutUpdateOnUnmountedFiberInDEV(fiber);
        return;
      }

      checkForInterruption(fiber, expirationTime);
      recordScheduleUpdate(); // TODO: computeExpirationForFiber also reads the priority. Pass the
      // priority as an argument to that function and this one.

      var priorityLevel = getCurrentPriorityLevel();

      if (expirationTime === Sync) {
        if ( // Check if we're inside unbatchedUpdates
        (executionContext & LegacyUnbatchedContext) !== NoContext && // Check if we're not already rendering
        (executionContext & (RenderContext | CommitContext)) === NoContext) {
          // Register pending interactions on the root to avoid losing traced interaction data.
          schedulePendingInteractions(root, expirationTime); // This is a legacy edge case. The initial mount of a ReactDOM.render-ed
          // root inside of batchedUpdates should be synchronous, but layout updates
          // should be deferred until the end of the batch.

          performSyncWorkOnRoot(root);
        } else {
          ensureRootIsScheduled(root);
          schedulePendingInteractions(root, expirationTime);

          if (executionContext === NoContext) {
            // Flush the synchronous work now, unless we're already working or inside
            // a batch. This is intentionally inside scheduleUpdateOnFiber instead of
            // scheduleCallbackForFiber to preserve the ability to schedule a callback
            // without immediately flushing it. We only do this for user-initiated
            // updates, to preserve historical behavior of sync mode.
            flushSyncCallbackQueue();
          }
        }
      } else {
        ensureRootIsScheduled(root);
        schedulePendingInteractions(root, expirationTime);
      }

      if ((executionContext & DiscreteEventContext) !== NoContext && ( // Only updates at user-blocking priority or greater are considered
      // discrete, even inside a discrete event.
      priorityLevel === UserBlockingPriority$2 || priorityLevel === ImmediatePriority)) {
        // This is the result of a discrete event. Track the lowest priority
        // discrete update per root so we can flush them early, if needed.
        if (rootsWithPendingDiscreteUpdates === null) {
          rootsWithPendingDiscreteUpdates = new Map([[root, expirationTime]]);
        } else {
          var lastDiscreteTime = rootsWithPendingDiscreteUpdates.get(root);

          if (lastDiscreteTime === undefined || lastDiscreteTime > expirationTime) {
            rootsWithPendingDiscreteUpdates.set(root, expirationTime);
          }
        }
      }
    }

    var scheduleWork = scheduleUpdateOnFiber; // This is split into a separate function so we can mark a fiber with pending
    // work without treating it as a typical update that originates from an event;
    // e.g. retrying a Suspense boundary isn't an update, but it does schedule work
    // on a fiber.

    function markUpdateTimeFromFiberToRoot(fiber, expirationTime) {
      // Update the source fiber's expiration time
      if (fiber.expirationTime < expirationTime) {
        fiber.expirationTime = expirationTime;
      }

      var alternate = fiber.alternate;

      if (alternate !== null && alternate.expirationTime < expirationTime) {
        alternate.expirationTime = expirationTime;
      } // Walk the parent path to the root and update the child expiration time.


      var node = fiber.return;
      var root = null;

      if (node === null && fiber.tag === HostRoot) {
        root = fiber.stateNode;
      } else {
        while (node !== null) {
          alternate = node.alternate;

          if (node.childExpirationTime < expirationTime) {
            node.childExpirationTime = expirationTime;

            if (alternate !== null && alternate.childExpirationTime < expirationTime) {
              alternate.childExpirationTime = expirationTime;
            }
          } else if (alternate !== null && alternate.childExpirationTime < expirationTime) {
            alternate.childExpirationTime = expirationTime;
          }

          if (node.return === null && node.tag === HostRoot) {
            root = node.stateNode;
            break;
          }

          node = node.return;
        }
      }

      if (root !== null) {
        if (workInProgressRoot === root) {
          // Received an update to a tree that's in the middle of rendering. Mark
          // that's unprocessed work on this root.
          markUnprocessedUpdateTime(expirationTime);

          if (workInProgressRootExitStatus === RootSuspendedWithDelay) {
            // The root already suspended with a delay, which means this render
            // definitely won't finish. Since we have a new update, let's mark it as
            // suspended now, right before marking the incoming update. This has the
            // effect of interrupting the current render and switching to the update.
            // TODO: This happens to work when receiving an update during the render
            // phase, because of the trick inside computeExpirationForFiber to
            // subtract 1 from `renderExpirationTime` to move it into a
            // separate bucket. But we should probably model it with an exception,
            // using the same mechanism we use to force hydration of a subtree.
            // TODO: This does not account for low pri updates that were already
            // scheduled before the root started rendering. Need to track the next
            // pending expiration time (perhaps by backtracking the return path) and
            // then trigger a restart in the `renderDidSuspendDelayIfPossible` path.
            markRootSuspendedAtTime(root, renderExpirationTime);
          }
        } // Mark that the root has a pending update.


        markRootUpdatedAtTime(root, expirationTime);
      }

      return root;
    }

    function getNextRootExpirationTimeToWorkOn(root) {
      // Determines the next expiration time that the root should render, taking
      // into account levels that may be suspended, or levels that may have
      // received a ping.
      var lastExpiredTime = root.lastExpiredTime;

      if (lastExpiredTime !== NoWork) {
        return lastExpiredTime;
      } // "Pending" refers to any update that hasn't committed yet, including if it
      // suspended. The "suspended" range is therefore a subset.


      var firstPendingTime = root.firstPendingTime;

      if (!isRootSuspendedAtTime(root, firstPendingTime)) {
        // The highest priority pending time is not suspended. Let's work on that.
        return firstPendingTime;
      } // If the first pending time is suspended, check if there's a lower priority
      // pending level that we know about. Or check if we received a ping. Work
      // on whichever is higher priority.


      var lastPingedTime = root.lastPingedTime;
      var nextKnownPendingLevel = root.nextKnownPendingLevel;
      return lastPingedTime > nextKnownPendingLevel ? lastPingedTime : nextKnownPendingLevel;
    } // Use this function to schedule a task for a root. There's only one task per
    // root; if a task was already scheduled, we'll check to make sure the
    // expiration time of the existing task is the same as the expiration time of
    // the next level that the root has work on. This function is called on every
    // update, and right before exiting a task.


    function ensureRootIsScheduled(root) {
      var lastExpiredTime = root.lastExpiredTime;

      if (lastExpiredTime !== NoWork) {
        // Special case: Expired work should flush synchronously.
        root.callbackExpirationTime = Sync;
        root.callbackPriority = ImmediatePriority;
        root.callbackNode = scheduleSyncCallback(performSyncWorkOnRoot.bind(null, root));
        return;
      }

      var expirationTime = getNextRootExpirationTimeToWorkOn(root);
      var existingCallbackNode = root.callbackNode;

      if (expirationTime === NoWork) {
        // There's nothing to work on.
        if (existingCallbackNode !== null) {
          root.callbackNode = null;
          root.callbackExpirationTime = NoWork;
          root.callbackPriority = NoPriority;
        }

        return;
      } // TODO: If this is an update, we already read the current time. Pass the
      // time as an argument.


      var currentTime = requestCurrentTime();
      var priorityLevel = inferPriorityFromExpirationTime(currentTime, expirationTime); // If there's an existing render task, confirm it has the correct priority and
      // expiration time. Otherwise, we'll cancel it and schedule a new one.

      if (existingCallbackNode !== null) {
        var existingCallbackPriority = root.callbackPriority;
        var existingCallbackExpirationTime = root.callbackExpirationTime;

        if ( // Callback must have the exact same expiration time.
        existingCallbackExpirationTime === expirationTime && // Callback must have greater or equal priority.
        existingCallbackPriority >= priorityLevel) {
          // Existing callback is sufficient.
          return;
        } // Need to schedule a new task.
        // TODO: Instead of scheduling a new task, we should be able to change the
        // priority of the existing one.


        cancelCallback(existingCallbackNode);
      }

      root.callbackExpirationTime = expirationTime;
      root.callbackPriority = priorityLevel;
      var callbackNode;

      if (expirationTime === Sync) {
        // Sync React callbacks are scheduled on a special internal queue
        callbackNode = scheduleSyncCallback(performSyncWorkOnRoot.bind(null, root));
      } else if (disableSchedulerTimeoutBasedOnReactExpirationTime) {
        callbackNode = scheduleCallback(priorityLevel, performConcurrentWorkOnRoot.bind(null, root));
      } else {
        callbackNode = scheduleCallback(priorityLevel, performConcurrentWorkOnRoot.bind(null, root), // Compute a task timeout based on the expiration time. This also affects
        // ordering because tasks are processed in timeout order.
        {
          timeout: expirationTimeToMs(expirationTime) - now()
        });
      }

      root.callbackNode = callbackNode;
    } // This is the entry point for every concurrent task, i.e. anything that
    // goes through Scheduler.


    function performConcurrentWorkOnRoot(root, didTimeout) {
      // Since we know we're in a React event, we can clear the current
      // event time. The next update will compute a new event time.
      currentEventTime = NoWork;

      if (didTimeout) {
        // The render task took too long to complete. Mark the current time as
        // expired to synchronously render all expired work in a single batch.
        var currentTime = requestCurrentTime();
        markRootExpiredAtTime(root, currentTime); // This will schedule a synchronous callback.

        ensureRootIsScheduled(root);
        return null;
      } // Determine the next expiration time to work on, using the fields stored
      // on the root.


      var expirationTime = getNextRootExpirationTimeToWorkOn(root);

      if (expirationTime !== NoWork) {
        var originalCallbackNode = root.callbackNode;

        (function () {
          if (!((executionContext & (RenderContext | CommitContext)) === NoContext)) {
            {
              throw ReactError(Error("Should not already be working."));
            }
          }
        })();

        flushPassiveEffects(); // If the root or expiration time have changed, throw out the existing stack
        // and prepare a fresh one. Otherwise we'll continue where we left off.

        if (root !== workInProgressRoot || expirationTime !== renderExpirationTime) {
          prepareFreshStack(root, expirationTime);
          startWorkOnPendingInteractions(root, expirationTime);
        } // If we have a work-in-progress fiber, it means there's still work to do
        // in this root.


        if (workInProgress !== null) {
          var prevExecutionContext = executionContext;
          executionContext |= RenderContext;
          var prevDispatcher = pushDispatcher(root);
          var prevInteractions = pushInteractions(root);
          startWorkLoopTimer(workInProgress);

          do {
            try {
              workLoopConcurrent();
              break;
            } catch (thrownValue) {
              handleError(root, thrownValue);
            }
          } while (true);

          resetContextDependencies();
          executionContext = prevExecutionContext;
          popDispatcher(prevDispatcher);

          if (enableSchedulerTracing) {
            popInteractions(prevInteractions);
          }

          if (workInProgressRootExitStatus === RootFatalErrored) {
            var fatalError = workInProgressRootFatalError;
            stopInterruptedWorkLoopTimer();
            prepareFreshStack(root, expirationTime);
            markRootSuspendedAtTime(root, expirationTime);
            ensureRootIsScheduled(root);
            throw fatalError;
          }

          if (workInProgress !== null) {
            // There's still work left over. Exit without committing.
            stopInterruptedWorkLoopTimer();
          } else {
            // We now have a consistent tree. The next step is either to commit it,
            // or, if something suspended, wait to commit it after a timeout.
            stopFinishedWorkLoopTimer();
            var finishedWork = root.finishedWork = root.current.alternate;
            root.finishedExpirationTime = expirationTime;
            resolveLocksOnRoot(root, expirationTime);
            finishConcurrentRender(root, finishedWork, workInProgressRootExitStatus, expirationTime);
          }

          ensureRootIsScheduled(root);

          if (root.callbackNode === originalCallbackNode) {
            // The task node scheduled for this root is the same one that's
            // currently executed. Need to return a continuation.
            return performConcurrentWorkOnRoot.bind(null, root);
          }
        }
      }

      return null;
    }

    function finishConcurrentRender(root, finishedWork, exitStatus, expirationTime) {
      // Set this to null to indicate there's no in-progress render.
      workInProgressRoot = null;

      switch (exitStatus) {
        case RootIncomplete:
        case RootFatalErrored:
          {
            (function () {
              {
                {
                  throw ReactError(Error("Root did not complete. This is a bug in React."));
                }
              }
            })();
          }
        // Flow knows about invariant, so it complains if I add a break
        // statement, but eslint doesn't know about invariant, so it complains
        // if I do. eslint-disable-next-line no-fallthrough

        case RootErrored:
          {
            if (expirationTime !== Idle) {
              // If this was an async render, the error may have happened due to
              // a mutation in a concurrent event. Try rendering one more time,
              // synchronously, to see if the error goes away. If there are
              // lower priority updates, let's include those, too, in case they
              // fix the inconsistency. Render at Idle to include all updates.
              markRootExpiredAtTime(root, Idle);
              break;
            } // Commit the root in its errored state.


            commitRoot(root);
            break;
          }

        case RootSuspended:
          {
            markRootSuspendedAtTime(root, expirationTime);
            var lastSuspendedTime = root.lastSuspendedTime;

            if (expirationTime === lastSuspendedTime) {
              root.nextKnownPendingLevel = getRemainingExpirationTime(finishedWork);
            }

            flushSuspensePriorityWarningInDEV(); // We have an acceptable loading state. We need to figure out if we
            // should immediately commit it or wait a bit.
            // If we have processed new updates during this render, we may now
            // have a new loading state ready. We want to ensure that we commit
            // that as soon as possible.

            var hasNotProcessedNewUpdates = workInProgressRootLatestProcessedExpirationTime === Sync;

            if (hasNotProcessedNewUpdates && // do not delay if we're inside an act() scope
            !(true && flushSuspenseFallbacksInTests && IsThisRendererActing.current)) {
              // If we have not processed any new updates during this pass, then
              // this is either a retry of an existing fallback state or a
              // hidden tree. Hidden trees shouldn't be batched with other work
              // and after that's fixed it can only be a retry. We're going to
              // throttle committing retries so that we don't show too many
              // loading states too quickly.
              var msUntilTimeout = globalMostRecentFallbackTime + FALLBACK_THROTTLE_MS - now(); // Don't bother with a very short suspense time.

              if (msUntilTimeout > 10) {
                if (workInProgressRootHasPendingPing) {
                  var lastPingedTime = root.lastPingedTime;

                  if (lastPingedTime === NoWork || lastPingedTime >= expirationTime) {
                    // This render was pinged but we didn't get to restart
                    // earlier so try restarting now instead.
                    root.lastPingedTime = expirationTime;
                    prepareFreshStack(root, expirationTime);
                    break;
                  }
                }

                var nextTime = getNextRootExpirationTimeToWorkOn(root);

                if (nextTime !== NoWork && nextTime !== expirationTime) {
                  // There's additional work on this root.
                  break;
                }

                if (lastSuspendedTime !== NoWork && lastSuspendedTime !== expirationTime) {
                  // We should prefer to render the fallback of at the last
                  // suspended level. Ping the last suspended level to try
                  // rendering it again.
                  root.lastPingedTime = lastSuspendedTime;
                  break;
                } // The render is suspended, it hasn't timed out, and there's no
                // lower priority work to do. Instead of committing the fallback
                // immediately, wait for more data to arrive.


                root.timeoutHandle = scheduleTimeout(commitRoot.bind(null, root), msUntilTimeout);
                break;
              }
            } // The work expired. Commit immediately.


            commitRoot(root);
            break;
          }

        case RootSuspendedWithDelay:
          {
            markRootSuspendedAtTime(root, expirationTime);
            var _lastSuspendedTime = root.lastSuspendedTime;

            if (expirationTime === _lastSuspendedTime) {
              root.nextKnownPendingLevel = getRemainingExpirationTime(finishedWork);
            }

            flushSuspensePriorityWarningInDEV();

            if ( // do not delay if we're inside an act() scope
            !(true && flushSuspenseFallbacksInTests && IsThisRendererActing.current)) {
              // We're suspended in a state that should be avoided. We'll try to
              // avoid committing it for as long as the timeouts let us.
              if (workInProgressRootHasPendingPing) {
                var _lastPingedTime = root.lastPingedTime;

                if (_lastPingedTime === NoWork || _lastPingedTime >= expirationTime) {
                  // This render was pinged but we didn't get to restart earlier
                  // so try restarting now instead.
                  root.lastPingedTime = expirationTime;
                  prepareFreshStack(root, expirationTime);
                  break;
                }
              }

              var _nextTime = getNextRootExpirationTimeToWorkOn(root);

              if (_nextTime !== NoWork && _nextTime !== expirationTime) {
                // There's additional work on this root.
                break;
              }

              if (_lastSuspendedTime !== NoWork && _lastSuspendedTime !== expirationTime) {
                // We should prefer to render the fallback of at the last
                // suspended level. Ping the last suspended level to try
                // rendering it again.
                root.lastPingedTime = _lastSuspendedTime;
                break;
              }

              var _msUntilTimeout;

              if (workInProgressRootLatestSuspenseTimeout !== Sync) {
                // We have processed a suspense config whose expiration time we
                // can use as the timeout.
                _msUntilTimeout = expirationTimeToMs(workInProgressRootLatestSuspenseTimeout) - now();
              } else if (workInProgressRootLatestProcessedExpirationTime === Sync) {
                // This should never normally happen because only new updates
                // cause delayed states, so we should have processed something.
                // However, this could also happen in an offscreen tree.
                _msUntilTimeout = 0;
              } else {
                // If we don't have a suspense config, we're going to use a
                // heuristic to determine how long we can suspend.
                var eventTimeMs = inferTimeFromExpirationTime(workInProgressRootLatestProcessedExpirationTime);
                var currentTimeMs = now();
                var timeUntilExpirationMs = expirationTimeToMs(expirationTime) - currentTimeMs;
                var timeElapsed = currentTimeMs - eventTimeMs;

                if (timeElapsed < 0) {
                  // We get this wrong some time since we estimate the time.
                  timeElapsed = 0;
                }

                _msUntilTimeout = jnd(timeElapsed) - timeElapsed; // Clamp the timeout to the expiration time. TODO: Once the
                // event time is exact instead of inferred from expiration time
                // we don't need this.

                if (timeUntilExpirationMs < _msUntilTimeout) {
                  _msUntilTimeout = timeUntilExpirationMs;
                }
              } // Don't bother with a very short suspense time.


              if (_msUntilTimeout > 10) {
                // The render is suspended, it hasn't timed out, and there's no
                // lower priority work to do. Instead of committing the fallback
                // immediately, wait for more data to arrive.
                root.timeoutHandle = scheduleTimeout(commitRoot.bind(null, root), _msUntilTimeout);
                break;
              }
            } // The work expired. Commit immediately.


            commitRoot(root);
            break;
          }

        case RootCompleted:
          {
            // The work completed. Ready to commit.
            if ( // do not delay if we're inside an act() scope
            !(true && flushSuspenseFallbacksInTests && IsThisRendererActing.current) && workInProgressRootLatestProcessedExpirationTime !== Sync && workInProgressRootCanSuspendUsingConfig !== null) {
              // If we have exceeded the minimum loading delay, which probably
              // means we have shown a spinner already, we might have to suspend
              // a bit longer to ensure that the spinner is shown for
              // enough time.
              var _msUntilTimeout2 = computeMsUntilSuspenseLoadingDelay(workInProgressRootLatestProcessedExpirationTime, expirationTime, workInProgressRootCanSuspendUsingConfig);

              if (_msUntilTimeout2 > 10) {
                markRootSuspendedAtTime(root, expirationTime);
                root.timeoutHandle = scheduleTimeout(commitRoot.bind(null, root), _msUntilTimeout2);
                break;
              }
            }

            commitRoot(root);
            break;
          }

        case RootLocked:
          {
            // This root has a lock that prevents it from committing. Exit. If
            // we begin work on the root again, without any intervening updates,
            // it will finish without doing additional work.
            markRootSuspendedAtTime(root, expirationTime);
            break;
          }

        default:
          {
            (function () {
              {
                {
                  throw ReactError(Error("Unknown root exit status."));
                }
              }
            })();
          }
      }
    } // This is the entry point for synchronous tasks that don't go
    // through Scheduler


    function performSyncWorkOnRoot(root) {
      // Check if there's expired work on this root. Otherwise, render at Sync.
      var lastExpiredTime = root.lastExpiredTime;
      var expirationTime = lastExpiredTime !== NoWork ? lastExpiredTime : Sync;

      if (root.finishedExpirationTime === expirationTime) {
        // There's already a pending commit at this expiration time.
        // TODO: This is poorly factored. This case only exists for the
        // batch.commit() API.
        commitRoot(root);
      } else {
        (function () {
          if (!((executionContext & (RenderContext | CommitContext)) === NoContext)) {
            {
              throw ReactError(Error("Should not already be working."));
            }
          }
        })();

        flushPassiveEffects(); // If the root or expiration time have changed, throw out the existing stack
        // and prepare a fresh one. Otherwise we'll continue where we left off.

        if (root !== workInProgressRoot || expirationTime !== renderExpirationTime) {
          prepareFreshStack(root, expirationTime);
          startWorkOnPendingInteractions(root, expirationTime);
        } // If we have a work-in-progress fiber, it means there's still work to do
        // in this root.


        if (workInProgress !== null) {
          var prevExecutionContext = executionContext;
          executionContext |= RenderContext;
          var prevDispatcher = pushDispatcher(root);
          var prevInteractions = pushInteractions(root);
          startWorkLoopTimer(workInProgress);

          do {
            try {
              workLoopSync();
              break;
            } catch (thrownValue) {
              handleError(root, thrownValue);
            }
          } while (true);

          resetContextDependencies();
          executionContext = prevExecutionContext;
          popDispatcher(prevDispatcher);

          if (enableSchedulerTracing) {
            popInteractions(prevInteractions);
          }

          if (workInProgressRootExitStatus === RootFatalErrored) {
            var fatalError = workInProgressRootFatalError;
            stopInterruptedWorkLoopTimer();
            prepareFreshStack(root, expirationTime);
            markRootSuspendedAtTime(root, expirationTime);
            ensureRootIsScheduled(root);
            throw fatalError;
          }

          if (workInProgress !== null) {
            // This is a sync render, so we should have finished the whole tree.
            (function () {
              {
                {
                  throw ReactError(Error("Cannot commit an incomplete root. This error is likely caused by a bug in React. Please file an issue."));
                }
              }
            })();
          } else {
            // We now have a consistent tree. Because this is a sync render, we
            // will commit it even if something suspended. The only exception is
            // if the root is locked (using the unstable_createBatch API).
            stopFinishedWorkLoopTimer();
            root.finishedWork = root.current.alternate;
            root.finishedExpirationTime = expirationTime;
            resolveLocksOnRoot(root, expirationTime);
            finishSyncRender(root, workInProgressRootExitStatus, expirationTime);
          } // Before exiting, make sure there's a callback scheduled for the next
          // pending level.


          ensureRootIsScheduled(root);
        }
      }

      return null;
    }

    function finishSyncRender(root, exitStatus, expirationTime) {
      if (exitStatus === RootLocked) {
        // This root has a lock that prevents it from committing. Exit. If we
        // begin work on the root again, without any intervening updates, it
        // will finish without doing additional work.
        markRootSuspendedAtTime(root, expirationTime);
      } else {
        // Set this to null to indicate there's no in-progress render.
        workInProgressRoot = null;
        {
          if (exitStatus === RootSuspended || exitStatus === RootSuspendedWithDelay) {
            flushSuspensePriorityWarningInDEV();
          }
        }
        commitRoot(root);
      }
    }

    function flushRoot(root, expirationTime) {
      if ((executionContext & (RenderContext | CommitContext)) !== NoContext) {
        (function () {
          {
            {
              throw ReactError(Error("work.commit(): Cannot commit while already rendering. This likely means you attempted to commit from inside a lifecycle method."));
            }
          }
        })();
      }

      markRootExpiredAtTime(root, expirationTime);
      ensureRootIsScheduled(root);
      flushSyncCallbackQueue();
    }

    function flushDiscreteUpdates() {
      // TODO: Should be able to flush inside batchedUpdates, but not inside `act`.
      // However, `act` uses `batchedUpdates`, so there's no way to distinguish
      // those two cases. Need to fix this before exposing flushDiscreteUpdates
      // as a public API.
      if ((executionContext & (BatchedContext | RenderContext | CommitContext)) !== NoContext) {
        if (true && (executionContext & RenderContext) !== NoContext) {
          warning$1(false, 'unstable_flushDiscreteUpdates: Cannot flush updates when React is ' + 'already rendering.');
        } // We're already rendering, so we can't synchronously flush pending work.
        // This is probably a nested event dispatch triggered by a lifecycle/effect,
        // like `el.focus()`. Exit.


        return;
      }

      flushPendingDiscreteUpdates(); // If the discrete updates scheduled passive effects, flush them now so that
      // they fire before the next serial event.

      flushPassiveEffects();
    }

    function resolveLocksOnRoot(root, expirationTime) {
      var firstBatch = root.firstBatch;

      if (firstBatch !== null && firstBatch._defer && firstBatch._expirationTime >= expirationTime) {
        scheduleCallback(NormalPriority, function () {
          firstBatch._onComplete();

          return null;
        });
        workInProgressRootExitStatus = RootLocked;
      }
    }

    function flushPendingDiscreteUpdates() {
      if (rootsWithPendingDiscreteUpdates !== null) {
        // For each root with pending discrete updates, schedule a callback to
        // immediately flush them.
        var roots = rootsWithPendingDiscreteUpdates;
        rootsWithPendingDiscreteUpdates = null;
        roots.forEach(function (expirationTime, root) {
          markRootExpiredAtTime(root, expirationTime);
          ensureRootIsScheduled(root);
        }); // Now flush the immediate queue.

        flushSyncCallbackQueue();
      }
    }

    function batchedUpdates$1(fn, a) {
      var prevExecutionContext = executionContext;
      executionContext |= BatchedContext;

      try {
        return fn(a);
      } finally {
        executionContext = prevExecutionContext;

        if (executionContext === NoContext) {
          // Flush the immediate callbacks that were scheduled during this batch
          flushSyncCallbackQueue();
        }
      }
    }

    function batchedEventUpdates$1(fn, a) {
      var prevExecutionContext = executionContext;
      executionContext |= EventContext;

      try {
        return fn(a);
      } finally {
        executionContext = prevExecutionContext;

        if (executionContext === NoContext) {
          // Flush the immediate callbacks that were scheduled during this batch
          flushSyncCallbackQueue();
        }
      }
    }

    function discreteUpdates$1(fn, a, b, c) {
      var prevExecutionContext = executionContext;
      executionContext |= DiscreteEventContext;

      try {
        // Should this
        return runWithPriority$2(UserBlockingPriority$2, fn.bind(null, a, b, c));
      } finally {
        executionContext = prevExecutionContext;

        if (executionContext === NoContext) {
          // Flush the immediate callbacks that were scheduled during this batch
          flushSyncCallbackQueue();
        }
      }
    }

    function unbatchedUpdates(fn, a) {
      var prevExecutionContext = executionContext;
      executionContext &= ~BatchedContext;
      executionContext |= LegacyUnbatchedContext;

      try {
        return fn(a);
      } finally {
        executionContext = prevExecutionContext;

        if (executionContext === NoContext) {
          // Flush the immediate callbacks that were scheduled during this batch
          flushSyncCallbackQueue();
        }
      }
    }

    function flushSync(fn, a) {
      if ((executionContext & (RenderContext | CommitContext)) !== NoContext) {
        (function () {
          {
            {
              throw ReactError(Error("flushSync was called from inside a lifecycle method. It cannot be called when React is already rendering."));
            }
          }
        })();
      }

      var prevExecutionContext = executionContext;
      executionContext |= BatchedContext;

      try {
        return runWithPriority$2(ImmediatePriority, fn.bind(null, a));
      } finally {
        executionContext = prevExecutionContext; // Flush the immediate callbacks that were scheduled during this batch.
        // Note that this will happen even if batchedUpdates is higher up
        // the stack.

        flushSyncCallbackQueue();
      }
    }

    function flushControlled(fn) {
      var prevExecutionContext = executionContext;
      executionContext |= BatchedContext;

      try {
        runWithPriority$2(ImmediatePriority, fn);
      } finally {
        executionContext = prevExecutionContext;

        if (executionContext === NoContext) {
          // Flush the immediate callbacks that were scheduled during this batch
          flushSyncCallbackQueue();
        }
      }
    }

    function prepareFreshStack(root, expirationTime) {
      root.finishedWork = null;
      root.finishedExpirationTime = NoWork;
      var timeoutHandle = root.timeoutHandle;

      if (timeoutHandle !== noTimeout) {
        // The root previous suspended and scheduled a timeout to commit a fallback
        // state. Now that we have additional work, cancel the timeout.
        root.timeoutHandle = noTimeout; // $FlowFixMe Complains noTimeout is not a TimeoutID, despite the check above

        cancelTimeout(timeoutHandle);
      }

      if (workInProgress !== null) {
        var interruptedWork = workInProgress.return;

        while (interruptedWork !== null) {
          unwindInterruptedWork(interruptedWork);
          interruptedWork = interruptedWork.return;
        }
      }

      workInProgressRoot = root;
      workInProgress = createWorkInProgress(root.current, null, expirationTime);
      renderExpirationTime = expirationTime;
      workInProgressRootExitStatus = RootIncomplete;
      workInProgressRootFatalError = null;
      workInProgressRootLatestProcessedExpirationTime = Sync;
      workInProgressRootLatestSuspenseTimeout = Sync;
      workInProgressRootCanSuspendUsingConfig = null;
      workInProgressRootNextUnprocessedUpdateTime = NoWork;
      workInProgressRootHasPendingPing = false;

      if (enableSchedulerTracing) {
        spawnedWorkDuringRender = null;
      }

      {
        ReactStrictModeWarnings.discardPendingWarnings();
        componentsThatTriggeredHighPriSuspend = null;
      }
    }

    function handleError(root, thrownValue) {
      do {
        try {
          // Reset module-level state that was set during the render phase.
          resetContextDependencies();
          resetHooks();

          if (workInProgress === null || workInProgress.return === null) {
            // Expected to be working on a non-root fiber. This is a fatal error
            // because there's no ancestor that can handle it; the root is
            // supposed to capture all errors that weren't caught by an error
            // boundary.
            workInProgressRootExitStatus = RootFatalErrored;
            workInProgressRootFatalError = thrownValue;
            return null;
          }

          if (enableProfilerTimer && workInProgress.mode & ProfileMode) {
            // Record the time spent rendering before an error was thrown. This
            // avoids inaccurate Profiler durations in the case of a
            // suspended render.
            stopProfilerTimerIfRunningAndRecordDelta(workInProgress, true);
          }

          throwException(root, workInProgress.return, workInProgress, thrownValue, renderExpirationTime);
          workInProgress = completeUnitOfWork(workInProgress);
        } catch (yetAnotherThrownValue) {
          // Something in the return path also threw.
          thrownValue = yetAnotherThrownValue;
          continue;
        } // Return to the normal work loop.


        return;
      } while (true);
    }

    function pushDispatcher(root) {
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = ContextOnlyDispatcher;

      if (prevDispatcher === null) {
        // The React isomorphic package does not include a default dispatcher.
        // Instead the first renderer will lazily attach one, in order to give
        // nicer error messages.
        return ContextOnlyDispatcher;
      } else {
        return prevDispatcher;
      }
    }

    function popDispatcher(prevDispatcher) {
      ReactCurrentDispatcher.current = prevDispatcher;
    }

    function pushInteractions(root) {
      if (enableSchedulerTracing) {
        var prevInteractions = tracing.__interactionsRef.current;
        tracing.__interactionsRef.current = root.memoizedInteractions;
        return prevInteractions;
      }

      return null;
    }

    function popInteractions(prevInteractions) {
      if (enableSchedulerTracing) {
        tracing.__interactionsRef.current = prevInteractions;
      }
    }

    function markCommitTimeOfFallback() {
      globalMostRecentFallbackTime = now();
    }

    function markRenderEventTimeAndConfig(expirationTime, suspenseConfig) {
      if (expirationTime < workInProgressRootLatestProcessedExpirationTime && expirationTime > Idle) {
        workInProgressRootLatestProcessedExpirationTime = expirationTime;
      }

      if (suspenseConfig !== null) {
        if (expirationTime < workInProgressRootLatestSuspenseTimeout && expirationTime > Idle) {
          workInProgressRootLatestSuspenseTimeout = expirationTime; // Most of the time we only have one config and getting wrong is not bad.

          workInProgressRootCanSuspendUsingConfig = suspenseConfig;
        }
      }
    }

    function markUnprocessedUpdateTime(expirationTime) {
      if (expirationTime > workInProgressRootNextUnprocessedUpdateTime) {
        workInProgressRootNextUnprocessedUpdateTime = expirationTime;
      }
    }

    function renderDidSuspend() {
      if (workInProgressRootExitStatus === RootIncomplete) {
        workInProgressRootExitStatus = RootSuspended;
      }
    }

    function renderDidSuspendDelayIfPossible() {
      if (workInProgressRootExitStatus === RootIncomplete || workInProgressRootExitStatus === RootSuspended) {
        workInProgressRootExitStatus = RootSuspendedWithDelay;
      } // Check if there's a lower priority update somewhere else in the tree.


      if (workInProgressRootNextUnprocessedUpdateTime !== NoWork && workInProgressRoot !== null) {
        // Mark the current render as suspended, and then mark that there's a
        // pending update.
        // TODO: This should immediately interrupt the current render, instead
        // of waiting until the next time we yield.
        markRootSuspendedAtTime(workInProgressRoot, renderExpirationTime);
        markRootUpdatedAtTime(workInProgressRoot, workInProgressRootNextUnprocessedUpdateTime);
      }
    }

    function renderDidError() {
      if (workInProgressRootExitStatus !== RootCompleted) {
        workInProgressRootExitStatus = RootErrored;
      }
    } // Called during render to determine if anything has suspended.
    // Returns false if we're not sure.


    function renderHasNotSuspendedYet() {
      // If something errored or completed, we can't really be sure,
      // so those are false.
      return workInProgressRootExitStatus === RootIncomplete;
    }

    function inferTimeFromExpirationTime(expirationTime) {
      // We don't know exactly when the update was scheduled, but we can infer an
      // approximate start time from the expiration time.
      var earliestExpirationTimeMs = expirationTimeToMs(expirationTime);
      return earliestExpirationTimeMs - LOW_PRIORITY_EXPIRATION;
    }

    function inferTimeFromExpirationTimeWithSuspenseConfig(expirationTime, suspenseConfig) {
      // We don't know exactly when the update was scheduled, but we can infer an
      // approximate start time from the expiration time by subtracting the timeout
      // that was added to the event time.
      var earliestExpirationTimeMs = expirationTimeToMs(expirationTime);
      return earliestExpirationTimeMs - (suspenseConfig.timeoutMs | 0 || LOW_PRIORITY_EXPIRATION);
    } // The work loop is an extremely hot path. Tell Closure not to inline it.

    /** @noinline */


    function workLoopSync() {
      // Already timed out, so perform work without checking if we need to yield.
      while (workInProgress !== null) {
        workInProgress = performUnitOfWork(workInProgress);
      }
    }
    /** @noinline */


    function workLoopConcurrent() {
      // Perform work until Scheduler asks us to yield
      while (workInProgress !== null && !shouldYield()) {
        workInProgress = performUnitOfWork(workInProgress);
      }
    }

    function performUnitOfWork(unitOfWork) {
      // The current, flushed, state of this fiber is the alternate. Ideally
      // nothing should rely on this, but relying on it here means that we don't
      // need an additional field on the work in progress.
      var current$$1 = unitOfWork.alternate;
      startWorkTimer(unitOfWork);
      setCurrentFiber(unitOfWork);
      var next;

      if (enableProfilerTimer && (unitOfWork.mode & ProfileMode) !== NoMode) {
        startProfilerTimer(unitOfWork);
        next = beginWork$$1(current$$1, unitOfWork, renderExpirationTime);
        stopProfilerTimerIfRunningAndRecordDelta(unitOfWork, true);
      } else {
        next = beginWork$$1(current$$1, unitOfWork, renderExpirationTime);
      }

      resetCurrentFiber();
      unitOfWork.memoizedProps = unitOfWork.pendingProps;

      if (next === null) {
        // If this doesn't spawn new work, complete the current work.
        next = completeUnitOfWork(unitOfWork);
      }

      ReactCurrentOwner$2.current = null;
      return next;
    }

    function completeUnitOfWork(unitOfWork) {
      // Attempt to complete the current unit of work, then move to the next
      // sibling. If there are no more siblings, return to the parent fiber.
      workInProgress = unitOfWork;

      do {
        // The current, flushed, state of this fiber is the alternate. Ideally
        // nothing should rely on this, but relying on it here means that we don't
        // need an additional field on the work in progress.
        var current$$1 = workInProgress.alternate;
        var returnFiber = workInProgress.return; // Check if the work completed or if something threw.

        if ((workInProgress.effectTag & Incomplete) === NoEffect) {
          setCurrentFiber(workInProgress);
          var next = void 0;

          if (!enableProfilerTimer || (workInProgress.mode & ProfileMode) === NoMode) {
            next = completeWork(current$$1, workInProgress, renderExpirationTime);
          } else {
            startProfilerTimer(workInProgress);
            next = completeWork(current$$1, workInProgress, renderExpirationTime); // Update render duration assuming we didn't error.

            stopProfilerTimerIfRunningAndRecordDelta(workInProgress, false);
          }

          stopWorkTimer(workInProgress);
          resetCurrentFiber();
          resetChildExpirationTime(workInProgress);

          if (next !== null) {
            // Completing this fiber spawned new work. Work on that next.
            return next;
          }

          if (returnFiber !== null && // Do not append effects to parents if a sibling failed to complete
          (returnFiber.effectTag & Incomplete) === NoEffect) {
            // Append all the effects of the subtree and this fiber onto the effect
            // list of the parent. The completion order of the children affects the
            // side-effect order.
            if (returnFiber.firstEffect === null) {
              returnFiber.firstEffect = workInProgress.firstEffect;
            }

            if (workInProgress.lastEffect !== null) {
              if (returnFiber.lastEffect !== null) {
                returnFiber.lastEffect.nextEffect = workInProgress.firstEffect;
              }

              returnFiber.lastEffect = workInProgress.lastEffect;
            } // If this fiber had side-effects, we append it AFTER the children's
            // side-effects. We can perform certain side-effects earlier if needed,
            // by doing multiple passes over the effect list. We don't want to
            // schedule our own side-effect on our own list because if end up
            // reusing children we'll schedule this effect onto itself since we're
            // at the end.


            var effectTag = workInProgress.effectTag; // Skip both NoWork and PerformedWork tags when creating the effect
            // list. PerformedWork effect is read by React DevTools but shouldn't be
            // committed.

            if (effectTag > PerformedWork) {
              if (returnFiber.lastEffect !== null) {
                returnFiber.lastEffect.nextEffect = workInProgress;
              } else {
                returnFiber.firstEffect = workInProgress;
              }

              returnFiber.lastEffect = workInProgress;
            }
          }
        } else {
          // This fiber did not complete because something threw. Pop values off
          // the stack without entering the complete phase. If this is a boundary,
          // capture values if possible.
          var _next = unwindWork(workInProgress, renderExpirationTime); // Because this fiber did not complete, don't reset its expiration time.


          if (enableProfilerTimer && (workInProgress.mode & ProfileMode) !== NoMode) {
            // Record the render duration for the fiber that errored.
            stopProfilerTimerIfRunningAndRecordDelta(workInProgress, false); // Include the time spent working on failed children before continuing.

            var actualDuration = workInProgress.actualDuration;
            var child = workInProgress.child;

            while (child !== null) {
              actualDuration += child.actualDuration;
              child = child.sibling;
            }

            workInProgress.actualDuration = actualDuration;
          }

          if (_next !== null) {
            // If completing this work spawned new work, do that next. We'll come
            // back here again.
            // Since we're restarting, remove anything that is not a host effect
            // from the effect tag.
            // TODO: The name stopFailedWorkTimer is misleading because Suspense
            // also captures and restarts.
            stopFailedWorkTimer(workInProgress);
            _next.effectTag &= HostEffectMask;
            return _next;
          }

          stopWorkTimer(workInProgress);

          if (returnFiber !== null) {
            // Mark the parent fiber as incomplete and clear its effect list.
            returnFiber.firstEffect = returnFiber.lastEffect = null;
            returnFiber.effectTag |= Incomplete;
          }
        }

        var siblingFiber = workInProgress.sibling;

        if (siblingFiber !== null) {
          // If there is more work to do in this returnFiber, do that next.
          return siblingFiber;
        } // Otherwise, return to the parent


        workInProgress = returnFiber;
      } while (workInProgress !== null); // We've reached the root.


      if (workInProgressRootExitStatus === RootIncomplete) {
        workInProgressRootExitStatus = RootCompleted;
      }

      return null;
    }

    function getRemainingExpirationTime(fiber) {
      var updateExpirationTime = fiber.expirationTime;
      var childExpirationTime = fiber.childExpirationTime;
      return updateExpirationTime > childExpirationTime ? updateExpirationTime : childExpirationTime;
    }

    function resetChildExpirationTime(completedWork) {
      if (renderExpirationTime !== Never && completedWork.childExpirationTime === Never) {
        // The children of this component are hidden. Don't bubble their
        // expiration times.
        return;
      }

      var newChildExpirationTime = NoWork; // Bubble up the earliest expiration time.

      if (enableProfilerTimer && (completedWork.mode & ProfileMode) !== NoMode) {
        // In profiling mode, resetChildExpirationTime is also used to reset
        // profiler durations.
        var actualDuration = completedWork.actualDuration;
        var treeBaseDuration = completedWork.selfBaseDuration; // When a fiber is cloned, its actualDuration is reset to 0. This value will
        // only be updated if work is done on the fiber (i.e. it doesn't bailout).
        // When work is done, it should bubble to the parent's actualDuration. If
        // the fiber has not been cloned though, (meaning no work was done), then
        // this value will reflect the amount of time spent working on a previous
        // render. In that case it should not bubble. We determine whether it was
        // cloned by comparing the child pointer.

        var shouldBubbleActualDurations = completedWork.alternate === null || completedWork.child !== completedWork.alternate.child;
        var child = completedWork.child;

        while (child !== null) {
          var childUpdateExpirationTime = child.expirationTime;
          var childChildExpirationTime = child.childExpirationTime;

          if (childUpdateExpirationTime > newChildExpirationTime) {
            newChildExpirationTime = childUpdateExpirationTime;
          }

          if (childChildExpirationTime > newChildExpirationTime) {
            newChildExpirationTime = childChildExpirationTime;
          }

          if (shouldBubbleActualDurations) {
            actualDuration += child.actualDuration;
          }

          treeBaseDuration += child.treeBaseDuration;
          child = child.sibling;
        }

        completedWork.actualDuration = actualDuration;
        completedWork.treeBaseDuration = treeBaseDuration;
      } else {
        var _child = completedWork.child;

        while (_child !== null) {
          var _childUpdateExpirationTime = _child.expirationTime;
          var _childChildExpirationTime = _child.childExpirationTime;

          if (_childUpdateExpirationTime > newChildExpirationTime) {
            newChildExpirationTime = _childUpdateExpirationTime;
          }

          if (_childChildExpirationTime > newChildExpirationTime) {
            newChildExpirationTime = _childChildExpirationTime;
          }

          _child = _child.sibling;
        }
      }

      completedWork.childExpirationTime = newChildExpirationTime;
    }

    function commitRoot(root) {
      var renderPriorityLevel = getCurrentPriorityLevel();
      runWithPriority$2(ImmediatePriority, commitRootImpl.bind(null, root, renderPriorityLevel));
      return null;
    }

    function commitRootImpl(root, renderPriorityLevel) {
      flushPassiveEffects();
      flushRenderPhaseStrictModeWarningsInDEV();

      (function () {
        if (!((executionContext & (RenderContext | CommitContext)) === NoContext)) {
          {
            throw ReactError(Error("Should not already be working."));
          }
        }
      })();

      var finishedWork = root.finishedWork;
      var expirationTime = root.finishedExpirationTime;

      if (finishedWork === null) {
        return null;
      }

      root.finishedWork = null;
      root.finishedExpirationTime = NoWork;

      (function () {
        if (!(finishedWork !== root.current)) {
          {
            throw ReactError(Error("Cannot commit the same tree as before. This error is likely caused by a bug in React. Please file an issue."));
          }
        }
      })(); // commitRoot never returns a continuation; it always finishes synchronously.
      // So we can clear these now to allow a new callback to be scheduled.


      root.callbackNode = null;
      root.callbackExpirationTime = NoWork;
      root.callbackPriority = NoPriority;
      root.nextKnownPendingLevel = NoWork;
      startCommitTimer(); // Update the first and last pending times on this root. The new first
      // pending time is whatever is left on the root fiber.

      var remainingExpirationTimeBeforeCommit = getRemainingExpirationTime(finishedWork);
      markRootFinishedAtTime(root, expirationTime, remainingExpirationTimeBeforeCommit);

      if (root === workInProgressRoot) {
        // We can reset these now that they are finished.
        workInProgressRoot = null;
        workInProgress = null;
        renderExpirationTime = NoWork;
      } else {} // This indicates that the last root we worked on is not the same one that
      // we're committing now. This most commonly happens when a suspended root
      // times out.
      // Get the list of effects.


      var firstEffect;

      if (finishedWork.effectTag > PerformedWork) {
        // A fiber's effect list consists only of its children, not itself. So if
        // the root has an effect, we need to add it to the end of the list. The
        // resulting list is the set that would belong to the root's parent, if it
        // had one; that is, all the effects in the tree including the root.
        if (finishedWork.lastEffect !== null) {
          finishedWork.lastEffect.nextEffect = finishedWork;
          firstEffect = finishedWork.firstEffect;
        } else {
          firstEffect = finishedWork;
        }
      } else {
        // There is no effect on the root.
        firstEffect = finishedWork.firstEffect;
      }

      if (firstEffect !== null) {
        var prevExecutionContext = executionContext;
        executionContext |= CommitContext;
        var prevInteractions = pushInteractions(root); // Reset this to null before calling lifecycles

        ReactCurrentOwner$2.current = null; // The commit phase is broken into several sub-phases. We do a separate pass
        // of the effect list for each phase: all mutation effects come before all
        // layout effects, and so on.
        // The first phase a "before mutation" phase. We use this phase to read the
        // state of the host tree right before we mutate it. This is where
        // getSnapshotBeforeUpdate is called.

        startCommitSnapshotEffectsTimer();
        prepareForCommit(root.containerInfo);
        nextEffect = firstEffect;

        do {
          {
            invokeGuardedCallback(null, commitBeforeMutationEffects, null);

            if (hasCaughtError()) {
              (function () {
                if (!(nextEffect !== null)) {
                  {
                    throw ReactError(Error("Should be working on an effect."));
                  }
                }
              })();

              var error = clearCaughtError();
              captureCommitPhaseError(nextEffect, error);
              nextEffect = nextEffect.nextEffect;
            }
          }
        } while (nextEffect !== null);

        stopCommitSnapshotEffectsTimer();

        if (enableProfilerTimer) {
          // Mark the current commit time to be shared by all Profilers in this
          // batch. This enables them to be grouped later.
          recordCommitTime();
        } // The next phase is the mutation phase, where we mutate the host tree.


        startCommitHostEffectsTimer();
        nextEffect = firstEffect;

        do {
          {
            invokeGuardedCallback(null, commitMutationEffects, null, root, renderPriorityLevel);

            if (hasCaughtError()) {
              (function () {
                if (!(nextEffect !== null)) {
                  {
                    throw ReactError(Error("Should be working on an effect."));
                  }
                }
              })();

              var _error = clearCaughtError();

              captureCommitPhaseError(nextEffect, _error);
              nextEffect = nextEffect.nextEffect;
            }
          }
        } while (nextEffect !== null);

        stopCommitHostEffectsTimer();
        resetAfterCommit(root.containerInfo); // The work-in-progress tree is now the current tree. This must come after
        // the mutation phase, so that the previous tree is still current during
        // componentWillUnmount, but before the layout phase, so that the finished
        // work is current during componentDidMount/Update.

        root.current = finishedWork; // The next phase is the layout phase, where we call effects that read
        // the host tree after it's been mutated. The idiomatic use case for this is
        // layout, but class component lifecycles also fire here for legacy reasons.

        startCommitLifeCyclesTimer();
        nextEffect = firstEffect;

        do {
          {
            invokeGuardedCallback(null, commitLayoutEffects, null, root, expirationTime);

            if (hasCaughtError()) {
              (function () {
                if (!(nextEffect !== null)) {
                  {
                    throw ReactError(Error("Should be working on an effect."));
                  }
                }
              })();

              var _error2 = clearCaughtError();

              captureCommitPhaseError(nextEffect, _error2);
              nextEffect = nextEffect.nextEffect;
            }
          }
        } while (nextEffect !== null);

        stopCommitLifeCyclesTimer();
        nextEffect = null; // Tell Scheduler to yield at the end of the frame, so the browser has an
        // opportunity to paint.

        requestPaint();

        if (enableSchedulerTracing) {
          popInteractions(prevInteractions);
        }

        executionContext = prevExecutionContext;
      } else {
        // No effects.
        root.current = finishedWork; // Measure these anyway so the flamegraph explicitly shows that there were
        // no effects.
        // TODO: Maybe there's a better way to report this.

        startCommitSnapshotEffectsTimer();
        stopCommitSnapshotEffectsTimer();

        if (enableProfilerTimer) {
          recordCommitTime();
        }

        startCommitHostEffectsTimer();
        stopCommitHostEffectsTimer();
        startCommitLifeCyclesTimer();
        stopCommitLifeCyclesTimer();
      }

      stopCommitTimer();
      var rootDidHavePassiveEffects = rootDoesHavePassiveEffects;

      if (rootDoesHavePassiveEffects) {
        // This commit has passive effects. Stash a reference to them. But don't
        // schedule a callback until after flushing layout work.
        rootDoesHavePassiveEffects = false;
        rootWithPendingPassiveEffects = root;
        pendingPassiveEffectsExpirationTime = expirationTime;
        pendingPassiveEffectsRenderPriority = renderPriorityLevel;
      } else {
        // We are done with the effect chain at this point so let's clear the
        // nextEffect pointers to assist with GC. If we have passive effects, we'll
        // clear this in flushPassiveEffects.
        nextEffect = firstEffect;

        while (nextEffect !== null) {
          var nextNextEffect = nextEffect.nextEffect;
          nextEffect.nextEffect = null;
          nextEffect = nextNextEffect;
        }
      } // Check if there's remaining work on this root


      var remainingExpirationTime = root.firstPendingTime;

      if (remainingExpirationTime !== NoWork) {
        if (enableSchedulerTracing) {
          if (spawnedWorkDuringRender !== null) {
            var expirationTimes = spawnedWorkDuringRender;
            spawnedWorkDuringRender = null;

            for (var i = 0; i < expirationTimes.length; i++) {
              scheduleInteractions(root, expirationTimes[i], root.memoizedInteractions);
            }
          }

          schedulePendingInteractions(root, remainingExpirationTime);
        }
      } else {
        // If there's no remaining work, we can clear the set of already failed
        // error boundaries.
        legacyErrorBoundariesThatAlreadyFailed = null;
      }

      if (enableSchedulerTracing) {
        if (!rootDidHavePassiveEffects) {
          // If there are no passive effects, then we can complete the pending interactions.
          // Otherwise, we'll wait until after the passive effects are flushed.
          // Wait to do this until after remaining work has been scheduled,
          // so that we don't prematurely signal complete for interactions when there's e.g. hidden work.
          finishPendingInteractions(root, expirationTime);
        }
      }

      if (remainingExpirationTime === Sync) {
        // Count the number of times the root synchronously re-renders without
        // finishing. If there are too many, it indicates an infinite update loop.
        if (root === rootWithNestedUpdates) {
          nestedUpdateCount++;
        } else {
          nestedUpdateCount = 0;
          rootWithNestedUpdates = root;
        }
      } else {
        nestedUpdateCount = 0;
      }

      onCommitRoot(finishedWork.stateNode, expirationTime); // Always call this before exiting `commitRoot`, to ensure that any
      // additional work on this root is scheduled.

      ensureRootIsScheduled(root);

      if (hasUncaughtError) {
        hasUncaughtError = false;
        var _error3 = firstUncaughtError;
        firstUncaughtError = null;
        throw _error3;
      }

      if ((executionContext & LegacyUnbatchedContext) !== NoContext) {
        // This is a legacy edge case. We just committed the initial mount of
        // a ReactDOM.render-ed root inside of batchedUpdates. The commit fired
        // synchronously, but layout updates should be deferred until the end
        // of the batch.
        return null;
      } // If layout work was scheduled, flush it now.


      flushSyncCallbackQueue();
      return null;
    }

    function commitBeforeMutationEffects() {
      while (nextEffect !== null) {
        var effectTag = nextEffect.effectTag;

        if ((effectTag & Snapshot) !== NoEffect) {
          setCurrentFiber(nextEffect);
          recordEffect();
          var current$$1 = nextEffect.alternate;
          commitBeforeMutationLifeCycles(current$$1, nextEffect);
          resetCurrentFiber();
        }

        if ((effectTag & Passive) !== NoEffect) {
          // If there are passive effects, schedule a callback to flush at
          // the earliest opportunity.
          if (!rootDoesHavePassiveEffects) {
            rootDoesHavePassiveEffects = true;
            scheduleCallback(NormalPriority, function () {
              flushPassiveEffects();
              return null;
            });
          }
        }

        nextEffect = nextEffect.nextEffect;
      }
    }

    function commitMutationEffects(root, renderPriorityLevel) {
      // TODO: Should probably move the bulk of this function to commitWork.
      while (nextEffect !== null) {
        setCurrentFiber(nextEffect);
        var effectTag = nextEffect.effectTag;

        if (effectTag & ContentReset) {
          commitResetTextContent(nextEffect);
        }

        if (effectTag & Ref) {
          var current$$1 = nextEffect.alternate;

          if (current$$1 !== null) {
            commitDetachRef(current$$1);
          }
        } // The following switch statement is only concerned about placement,
        // updates, and deletions. To avoid needing to add a case for every possible
        // bitmap value, we remove the secondary effects from the effect tag and
        // switch on that value.


        var primaryEffectTag = effectTag & (Placement | Update | Deletion | Hydrating);

        switch (primaryEffectTag) {
          case Placement:
            {
              commitPlacement(nextEffect); // Clear the "placement" from effect tag so that we know that this is
              // inserted, before any life-cycles like componentDidMount gets called.
              // TODO: findDOMNode doesn't rely on this any more but isMounted does
              // and isMounted is deprecated anyway so we should be able to kill this.

              nextEffect.effectTag &= ~Placement;
              break;
            }

          case PlacementAndUpdate:
            {
              // Placement
              commitPlacement(nextEffect); // Clear the "placement" from effect tag so that we know that this is
              // inserted, before any life-cycles like componentDidMount gets called.

              nextEffect.effectTag &= ~Placement; // Update

              var _current = nextEffect.alternate;
              commitWork(_current, nextEffect);
              break;
            }

          case Hydrating:
            {
              nextEffect.effectTag &= ~Hydrating;
              break;
            }

          case HydratingAndUpdate:
            {
              nextEffect.effectTag &= ~Hydrating; // Update

              var _current2 = nextEffect.alternate;
              commitWork(_current2, nextEffect);
              break;
            }

          case Update:
            {
              var _current3 = nextEffect.alternate;
              commitWork(_current3, nextEffect);
              break;
            }

          case Deletion:
            {
              commitDeletion(root, nextEffect, renderPriorityLevel);
              break;
            }
        } // TODO: Only record a mutation effect if primaryEffectTag is non-zero.


        recordEffect();
        resetCurrentFiber();
        nextEffect = nextEffect.nextEffect;
      }
    }

    function commitLayoutEffects(root, committedExpirationTime) {
      // TODO: Should probably move the bulk of this function to commitWork.
      while (nextEffect !== null) {
        setCurrentFiber(nextEffect);
        var effectTag = nextEffect.effectTag;

        if (effectTag & (Update | Callback)) {
          recordEffect();
          var current$$1 = nextEffect.alternate;
          commitLifeCycles(root, current$$1, nextEffect, committedExpirationTime);
        }

        if (effectTag & Ref) {
          recordEffect();
          commitAttachRef(nextEffect);
        }

        resetCurrentFiber();
        nextEffect = nextEffect.nextEffect;
      }
    }

    function flushPassiveEffects() {
      if (pendingPassiveEffectsRenderPriority !== NoPriority) {
        var priorityLevel = pendingPassiveEffectsRenderPriority > NormalPriority ? NormalPriority : pendingPassiveEffectsRenderPriority;
        pendingPassiveEffectsRenderPriority = NoPriority;
        return runWithPriority$2(priorityLevel, flushPassiveEffectsImpl);
      }
    }

    function flushPassiveEffectsImpl() {
      if (rootWithPendingPassiveEffects === null) {
        return false;
      }

      var root = rootWithPendingPassiveEffects;
      var expirationTime = pendingPassiveEffectsExpirationTime;
      rootWithPendingPassiveEffects = null;
      pendingPassiveEffectsExpirationTime = NoWork;

      (function () {
        if (!((executionContext & (RenderContext | CommitContext)) === NoContext)) {
          {
            throw ReactError(Error("Cannot flush passive effects while already rendering."));
          }
        }
      })();

      var prevExecutionContext = executionContext;
      executionContext |= CommitContext;
      var prevInteractions = pushInteractions(root); // Note: This currently assumes there are no passive effects on the root
      // fiber, because the root is not part of its own effect list. This could
      // change in the future.

      var effect = root.current.firstEffect;

      while (effect !== null) {
        {
          setCurrentFiber(effect);
          invokeGuardedCallback(null, commitPassiveHookEffects, null, effect);

          if (hasCaughtError()) {
            (function () {
              if (!(effect !== null)) {
                {
                  throw ReactError(Error("Should be working on an effect."));
                }
              }
            })();

            var error = clearCaughtError();
            captureCommitPhaseError(effect, error);
          }

          resetCurrentFiber();
        }
        var nextNextEffect = effect.nextEffect; // Remove nextEffect pointer to assist GC

        effect.nextEffect = null;
        effect = nextNextEffect;
      }

      if (enableSchedulerTracing) {
        popInteractions(prevInteractions);
        finishPendingInteractions(root, expirationTime);
      }

      executionContext = prevExecutionContext;
      flushSyncCallbackQueue(); // If additional passive effects were scheduled, increment a counter. If this
      // exceeds the limit, we'll fire a warning.

      nestedPassiveUpdateCount = rootWithPendingPassiveEffects === null ? 0 : nestedPassiveUpdateCount + 1;
      return true;
    }

    function isAlreadyFailedLegacyErrorBoundary(instance) {
      return legacyErrorBoundariesThatAlreadyFailed !== null && legacyErrorBoundariesThatAlreadyFailed.has(instance);
    }

    function markLegacyErrorBoundaryAsFailed(instance) {
      if (legacyErrorBoundariesThatAlreadyFailed === null) {
        legacyErrorBoundariesThatAlreadyFailed = new Set([instance]);
      } else {
        legacyErrorBoundariesThatAlreadyFailed.add(instance);
      }
    }

    function prepareToThrowUncaughtError(error) {
      if (!hasUncaughtError) {
        hasUncaughtError = true;
        firstUncaughtError = error;
      }
    }

    var onUncaughtError = prepareToThrowUncaughtError;

    function captureCommitPhaseErrorOnRoot(rootFiber, sourceFiber, error) {
      var errorInfo = createCapturedValue(error, sourceFiber);
      var update = createRootErrorUpdate(rootFiber, errorInfo, Sync);
      enqueueUpdate(rootFiber, update);
      var root = markUpdateTimeFromFiberToRoot(rootFiber, Sync);

      if (root !== null) {
        ensureRootIsScheduled(root);
        schedulePendingInteractions(root, Sync);
      }
    }

    function captureCommitPhaseError(sourceFiber, error) {
      if (sourceFiber.tag === HostRoot) {
        // Error was thrown at the root. There is no parent, so the root
        // itself should capture it.
        captureCommitPhaseErrorOnRoot(sourceFiber, sourceFiber, error);
        return;
      }

      var fiber = sourceFiber.return;

      while (fiber !== null) {
        if (fiber.tag === HostRoot) {
          captureCommitPhaseErrorOnRoot(fiber, sourceFiber, error);
          return;
        } else if (fiber.tag === ClassComponent) {
          var ctor = fiber.type;
          var instance = fiber.stateNode;

          if (typeof ctor.getDerivedStateFromError === 'function' || typeof instance.componentDidCatch === 'function' && !isAlreadyFailedLegacyErrorBoundary(instance)) {
            var errorInfo = createCapturedValue(error, sourceFiber);
            var update = createClassErrorUpdate(fiber, errorInfo, // TODO: This is always sync
            Sync);
            enqueueUpdate(fiber, update);
            var root = markUpdateTimeFromFiberToRoot(fiber, Sync);

            if (root !== null) {
              ensureRootIsScheduled(root);
              schedulePendingInteractions(root, Sync);
            }

            return;
          }
        }

        fiber = fiber.return;
      }
    }

    function pingSuspendedRoot(root, thenable, suspendedTime) {
      var pingCache = root.pingCache;

      if (pingCache !== null) {
        // The thenable resolved, so we no longer need to memoize, because it will
        // never be thrown again.
        pingCache.delete(thenable);
      }

      if (workInProgressRoot === root && renderExpirationTime === suspendedTime) {
        // Received a ping at the same priority level at which we're currently
        // rendering. We might want to restart this render. This should mirror
        // the logic of whether or not a root suspends once it completes.
        // TODO: If we're rendering sync either due to Sync, Batched or expired,
        // we should probably never restart.
        // If we're suspended with delay, we'll always suspend so we can always
        // restart. If we're suspended without any updates, it might be a retry.
        // If it's early in the retry we can restart. We can't know for sure
        // whether we'll eventually process an update during this render pass,
        // but it's somewhat unlikely that we get to a ping before that, since
        // getting to the root most update is usually very fast.
        if (workInProgressRootExitStatus === RootSuspendedWithDelay || workInProgressRootExitStatus === RootSuspended && workInProgressRootLatestProcessedExpirationTime === Sync && now() - globalMostRecentFallbackTime < FALLBACK_THROTTLE_MS) {
          // Restart from the root. Don't need to schedule a ping because
          // we're already working on this tree.
          prepareFreshStack(root, renderExpirationTime);
        } else {
          // Even though we can't restart right now, we might get an
          // opportunity later. So we mark this render as having a ping.
          workInProgressRootHasPendingPing = true;
        }

        return;
      }

      if (!isRootSuspendedAtTime(root, suspendedTime)) {
        // The root is no longer suspended at this time.
        return;
      }

      var lastPingedTime = root.lastPingedTime;

      if (lastPingedTime !== NoWork && lastPingedTime < suspendedTime) {
        // There's already a lower priority ping scheduled.
        return;
      } // Mark the time at which this ping was scheduled.


      root.lastPingedTime = suspendedTime;

      if (root.finishedExpirationTime === suspendedTime) {
        // If there's a pending fallback waiting to commit, throw it away.
        root.finishedExpirationTime = NoWork;
        root.finishedWork = null;
      }

      ensureRootIsScheduled(root);
      schedulePendingInteractions(root, suspendedTime);
    }

    function retryTimedOutBoundary(boundaryFiber, retryTime) {
      // The boundary fiber (a Suspense component or SuspenseList component)
      // previously was rendered in its fallback state. One of the promises that
      // suspended it has resolved, which means at least part of the tree was
      // likely unblocked. Try rendering again, at a new expiration time.
      if (retryTime === Never) {
        var suspenseConfig = null; // Retries don't carry over the already committed update.

        var currentTime = requestCurrentTime();
        retryTime = computeExpirationForFiber(currentTime, boundaryFiber, suspenseConfig);
      } // TODO: Special case idle priority?


      var root = markUpdateTimeFromFiberToRoot(boundaryFiber, retryTime);

      if (root !== null) {
        ensureRootIsScheduled(root);
        schedulePendingInteractions(root, retryTime);
      }
    }

    function retryDehydratedSuspenseBoundary(boundaryFiber) {
      var suspenseState = boundaryFiber.memoizedState;
      var retryTime = Never;

      if (suspenseState !== null) {
        retryTime = suspenseState.retryTime;
      }

      retryTimedOutBoundary(boundaryFiber, retryTime);
    }

    function resolveRetryThenable(boundaryFiber, thenable) {
      var retryTime = Never; // Default

      var retryCache;

      if (enableSuspenseServerRenderer) {
        switch (boundaryFiber.tag) {
          case SuspenseComponent:
            retryCache = boundaryFiber.stateNode;
            var suspenseState = boundaryFiber.memoizedState;

            if (suspenseState !== null) {
              retryTime = suspenseState.retryTime;
            }

            break;

          case SuspenseListComponent:
            retryCache = boundaryFiber.stateNode;
            break;

          default:
            (function () {
              {
                {
                  throw ReactError(Error("Pinged unknown suspense boundary type. This is probably a bug in React."));
                }
              }
            })();

        }
      } else {
        retryCache = boundaryFiber.stateNode;
      }

      if (retryCache !== null) {
        // The thenable resolved, so we no longer need to memoize, because it will
        // never be thrown again.
        retryCache.delete(thenable);
      }

      retryTimedOutBoundary(boundaryFiber, retryTime);
    } // Computes the next Just Noticeable Difference (JND) boundary.
    // The theory is that a person can't tell the difference between small differences in time.
    // Therefore, if we wait a bit longer than necessary that won't translate to a noticeable
    // difference in the experience. However, waiting for longer might mean that we can avoid
    // showing an intermediate loading state. The longer we have already waited, the harder it
    // is to tell small differences in time. Therefore, the longer we've already waited,
    // the longer we can wait additionally. At some point we have to give up though.
    // We pick a train model where the next boundary commits at a consistent schedule.
    // These particular numbers are vague estimates. We expect to adjust them based on research.


    function jnd(timeElapsed) {
      return timeElapsed < 120 ? 120 : timeElapsed < 480 ? 480 : timeElapsed < 1080 ? 1080 : timeElapsed < 1920 ? 1920 : timeElapsed < 3000 ? 3000 : timeElapsed < 4320 ? 4320 : ceil(timeElapsed / 1960) * 1960;
    }

    function computeMsUntilSuspenseLoadingDelay(mostRecentEventTime, committedExpirationTime, suspenseConfig) {
      var busyMinDurationMs = suspenseConfig.busyMinDurationMs | 0;

      if (busyMinDurationMs <= 0) {
        return 0;
      }

      var busyDelayMs = suspenseConfig.busyDelayMs | 0; // Compute the time until this render pass would expire.

      var currentTimeMs = now();
      var eventTimeMs = inferTimeFromExpirationTimeWithSuspenseConfig(mostRecentEventTime, suspenseConfig);
      var timeElapsed = currentTimeMs - eventTimeMs;

      if (timeElapsed <= busyDelayMs) {
        // If we haven't yet waited longer than the initial delay, we don't
        // have to wait any additional time.
        return 0;
      }

      var msUntilTimeout = busyDelayMs + busyMinDurationMs - timeElapsed; // This is the value that is passed to `setTimeout`.

      return msUntilTimeout;
    }

    function checkForNestedUpdates() {
      if (nestedUpdateCount > NESTED_UPDATE_LIMIT) {
        nestedUpdateCount = 0;
        rootWithNestedUpdates = null;

        (function () {
          {
            {
              throw ReactError(Error("Maximum update depth exceeded. This can happen when a component repeatedly calls setState inside componentWillUpdate or componentDidUpdate. React limits the number of nested updates to prevent infinite loops."));
            }
          }
        })();
      }

      {
        if (nestedPassiveUpdateCount > NESTED_PASSIVE_UPDATE_LIMIT) {
          nestedPassiveUpdateCount = 0;
          warning$1(false, 'Maximum update depth exceeded. This can happen when a component ' + "calls setState inside useEffect, but useEffect either doesn't " + 'have a dependency array, or one of the dependencies changes on ' + 'every render.');
        }
      }
    }

    function flushRenderPhaseStrictModeWarningsInDEV() {
      {
        ReactStrictModeWarnings.flushLegacyContextWarning();

        if (warnAboutDeprecatedLifecycles) {
          ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings();
        }
      }
    }

    function stopFinishedWorkLoopTimer() {
      var didCompleteRoot = true;
      stopWorkLoopTimer(interruptedBy, didCompleteRoot);
      interruptedBy = null;
    }

    function stopInterruptedWorkLoopTimer() {
      // TODO: Track which fiber caused the interruption.
      var didCompleteRoot = false;
      stopWorkLoopTimer(interruptedBy, didCompleteRoot);
      interruptedBy = null;
    }

    function checkForInterruption(fiberThatReceivedUpdate, updateExpirationTime) {
      if (enableUserTimingAPI && workInProgressRoot !== null && updateExpirationTime > renderExpirationTime) {
        interruptedBy = fiberThatReceivedUpdate;
      }
    }

    var didWarnStateUpdateForUnmountedComponent = null;

    function warnAboutUpdateOnUnmountedFiberInDEV(fiber) {
      {
        var tag = fiber.tag;

        if (tag !== HostRoot && tag !== ClassComponent && tag !== FunctionComponent && tag !== ForwardRef && tag !== MemoComponent && tag !== SimpleMemoComponent) {
          // Only warn for user-defined components, not internal ones like Suspense.
          return;
        } // We show the whole stack but dedupe on the top component's name because
        // the problematic code almost always lies inside that component.


        var componentName = getComponentName(fiber.type) || 'ReactComponent';

        if (didWarnStateUpdateForUnmountedComponent !== null) {
          if (didWarnStateUpdateForUnmountedComponent.has(componentName)) {
            return;
          }

          didWarnStateUpdateForUnmountedComponent.add(componentName);
        } else {
          didWarnStateUpdateForUnmountedComponent = new Set([componentName]);
        }

        warningWithoutStack$1(false, "Can't perform a React state update on an unmounted component. This " + 'is a no-op, but it indicates a memory leak in your application. To ' + 'fix, cancel all subscriptions and asynchronous tasks in %s.%s', tag === ClassComponent ? 'the componentWillUnmount method' : 'a useEffect cleanup function', getStackByFiberInDevAndProd(fiber));
      }
    }

    var beginWork$$1;

    if (true && replayFailedUnitOfWorkWithInvokeGuardedCallback) {
      var dummyFiber = null;

      beginWork$$1 = function (current$$1, unitOfWork, expirationTime) {
        // If a component throws an error, we replay it again in a synchronously
        // dispatched event, so that the debugger will treat it as an uncaught
        // error See ReactErrorUtils for more information.
        // Before entering the begin phase, copy the work-in-progress onto a dummy
        // fiber. If beginWork throws, we'll use this to reset the state.
        var originalWorkInProgressCopy = assignFiberPropertiesInDEV(dummyFiber, unitOfWork);

        try {
          return beginWork$1(current$$1, unitOfWork, expirationTime);
        } catch (originalError) {
          if (originalError !== null && typeof originalError === 'object' && typeof originalError.then === 'function') {
            // Don't replay promises. Treat everything else like an error.
            throw originalError;
          } // Keep this code in sync with renderRoot; any changes here must have
          // corresponding changes there.


          resetContextDependencies();
          resetHooks(); // Unwind the failed stack frame

          unwindInterruptedWork(unitOfWork); // Restore the original properties of the fiber.

          assignFiberPropertiesInDEV(unitOfWork, originalWorkInProgressCopy);

          if (enableProfilerTimer && unitOfWork.mode & ProfileMode) {
            // Reset the profiler timer.
            startProfilerTimer(unitOfWork);
          } // Run beginWork again.


          invokeGuardedCallback(null, beginWork$1, null, current$$1, unitOfWork, expirationTime);

          if (hasCaughtError()) {
            var replayError = clearCaughtError(); // `invokeGuardedCallback` sometimes sets an expando `_suppressLogging`.
            // Rethrow this error instead of the original one.

            throw replayError;
          } else {
            // This branch is reachable if the render phase is impure.
            throw originalError;
          }
        }
      };
    } else {
      beginWork$$1 = beginWork$1;
    }

    var didWarnAboutUpdateInRender = false;
    var didWarnAboutUpdateInGetChildContext = false;

    function warnAboutInvalidUpdatesOnClassComponentsInDEV(fiber) {
      {
        if (fiber.tag === ClassComponent) {
          switch (phase) {
            case 'getChildContext':
              if (didWarnAboutUpdateInGetChildContext) {
                return;
              }

              warningWithoutStack$1(false, 'setState(...): Cannot call setState() inside getChildContext()');
              didWarnAboutUpdateInGetChildContext = true;
              break;

            case 'render':
              if (didWarnAboutUpdateInRender) {
                return;
              }

              warningWithoutStack$1(false, 'Cannot update during an existing state transition (such as ' + 'within `render`). Render methods should be a pure function of ' + 'props and state.');
              didWarnAboutUpdateInRender = true;
              break;
          }
        }
      }
    } // a 'shared' variable that changes when act() opens/closes in tests.


    var IsThisRendererActing = {
      current: false
    };

    function warnIfNotScopedWithMatchingAct(fiber) {
      {
        if (warnsIfNotActing === true && IsSomeRendererActing.current === true && IsThisRendererActing.current !== true) {
          warningWithoutStack$1(false, "It looks like you're using the wrong act() around your test interactions.\n" + 'Be sure to use the matching version of act() corresponding to your renderer:\n\n' + '// for react-dom:\n' + "import {act} from 'react-dom/test-utils';\n" + '// ...\n' + 'act(() => ...);\n\n' + '// for react-test-renderer:\n' + "import TestRenderer from 'react-test-renderer';\n" + 'const {act} = TestRenderer;\n' + '// ...\n' + 'act(() => ...);' + '%s', getStackByFiberInDevAndProd(fiber));
        }
      }
    }

    function warnIfNotCurrentlyActingEffectsInDEV(fiber) {
      {
        if (warnsIfNotActing === true && (fiber.mode & StrictMode) !== NoMode && IsSomeRendererActing.current === false && IsThisRendererActing.current === false) {
          warningWithoutStack$1(false, 'An update to %s ran an effect, but was not wrapped in act(...).\n\n' + 'When testing, code that causes React state updates should be ' + 'wrapped into act(...):\n\n' + 'act(() => {\n' + '  /* fire events that update state */\n' + '});\n' + '/* assert on the output */\n\n' + "This ensures that you're testing the behavior the user would see " + 'in the browser.' + ' Learn more at https://fb.me/react-wrap-tests-with-act' + '%s', getComponentName(fiber.type), getStackByFiberInDevAndProd(fiber));
        }
      }
    }

    function warnIfNotCurrentlyActingUpdatesInDEV(fiber) {
      {
        if (warnsIfNotActing === true && executionContext === NoContext && IsSomeRendererActing.current === false && IsThisRendererActing.current === false) {
          warningWithoutStack$1(false, 'An update to %s inside a test was not wrapped in act(...).\n\n' + 'When testing, code that causes React state updates should be ' + 'wrapped into act(...):\n\n' + 'act(() => {\n' + '  /* fire events that update state */\n' + '});\n' + '/* assert on the output */\n\n' + "This ensures that you're testing the behavior the user would see " + 'in the browser.' + ' Learn more at https://fb.me/react-wrap-tests-with-act' + '%s', getComponentName(fiber.type), getStackByFiberInDevAndProd(fiber));
        }
      }
    }

    var warnIfNotCurrentlyActingUpdatesInDev = warnIfNotCurrentlyActingUpdatesInDEV; // In tests, we want to enforce a mocked scheduler.

    var didWarnAboutUnmockedScheduler = false; // TODO Before we release concurrent mode, revisit this and decide whether a mocked
    // scheduler is the actual recommendation. The alternative could be a testing build,
    // a new lib, or whatever; we dunno just yet. This message is for early adopters
    // to get their tests right.

    function warnIfUnmockedScheduler(fiber) {
      {
        if (didWarnAboutUnmockedScheduler === false && Scheduler.unstable_flushAllWithoutAsserting === undefined) {
          if (fiber.mode & BatchedMode || fiber.mode & ConcurrentMode) {
            didWarnAboutUnmockedScheduler = true;
            warningWithoutStack$1(false, 'In Concurrent or Sync modes, the "scheduler" module needs to be mocked ' + 'to guarantee consistent behaviour across tests and browsers. ' + 'For example, with jest: \n' + "jest.mock('scheduler', () => require('scheduler/unstable_mock'));\n\n" + 'For more info, visit https://fb.me/react-mock-scheduler');
          } else if (warnAboutUnmockedScheduler === true) {
            didWarnAboutUnmockedScheduler = true;
            warningWithoutStack$1(false, 'Starting from React v17, the "scheduler" module will need to be mocked ' + 'to guarantee consistent behaviour across tests and browsers. ' + 'For example, with jest: \n' + "jest.mock('scheduler', () => require('scheduler/unstable_mock'));\n\n" + 'For more info, visit https://fb.me/react-mock-scheduler');
          }
        }
      }
    }

    var componentsThatTriggeredHighPriSuspend = null;

    function checkForWrongSuspensePriorityInDEV(sourceFiber) {
      {
        var currentPriorityLevel = getCurrentPriorityLevel();

        if ((sourceFiber.mode & ConcurrentMode) !== NoEffect && (currentPriorityLevel === UserBlockingPriority$2 || currentPriorityLevel === ImmediatePriority)) {
          var workInProgressNode = sourceFiber;

          while (workInProgressNode !== null) {
            // Add the component that triggered the suspense
            var current$$1 = workInProgressNode.alternate;

            if (current$$1 !== null) {
              // TODO: warn component that triggers the high priority
              // suspend is the HostRoot
              switch (workInProgressNode.tag) {
                case ClassComponent:
                  // Loop through the component's update queue and see whether the component
                  // has triggered any high priority updates
                  var updateQueue = current$$1.updateQueue;

                  if (updateQueue !== null) {
                    var update = updateQueue.firstUpdate;

                    while (update !== null) {
                      var priorityLevel = update.priority;

                      if (priorityLevel === UserBlockingPriority$2 || priorityLevel === ImmediatePriority) {
                        if (componentsThatTriggeredHighPriSuspend === null) {
                          componentsThatTriggeredHighPriSuspend = new Set([getComponentName(workInProgressNode.type)]);
                        } else {
                          componentsThatTriggeredHighPriSuspend.add(getComponentName(workInProgressNode.type));
                        }

                        break;
                      }

                      update = update.next;
                    }
                  }

                  break;

                case FunctionComponent:
                case ForwardRef:
                case SimpleMemoComponent:
                  if (workInProgressNode.memoizedState !== null && workInProgressNode.memoizedState.baseUpdate !== null) {
                    var _update = workInProgressNode.memoizedState.baseUpdate; // Loop through the functional component's memoized state to see whether
                    // the component has triggered any high pri updates

                    while (_update !== null) {
                      var priority = _update.priority;

                      if (priority === UserBlockingPriority$2 || priority === ImmediatePriority) {
                        if (componentsThatTriggeredHighPriSuspend === null) {
                          componentsThatTriggeredHighPriSuspend = new Set([getComponentName(workInProgressNode.type)]);
                        } else {
                          componentsThatTriggeredHighPriSuspend.add(getComponentName(workInProgressNode.type));
                        }

                        break;
                      }

                      if (_update.next === workInProgressNode.memoizedState.baseUpdate) {
                        break;
                      }

                      _update = _update.next;
                    }
                  }

                  break;

                default:
                  break;
              }
            }

            workInProgressNode = workInProgressNode.return;
          }
        }
      }
    }

    function flushSuspensePriorityWarningInDEV() {
      {
        if (componentsThatTriggeredHighPriSuspend !== null) {
          var componentNames = [];
          componentsThatTriggeredHighPriSuspend.forEach(function (name) {
            return componentNames.push(name);
          });
          componentsThatTriggeredHighPriSuspend = null;

          if (componentNames.length > 0) {
            warningWithoutStack$1(false, '%s triggered a user-blocking update that suspended.' + '\n\n' + 'The fix is to split the update into multiple parts: a user-blocking ' + 'update to provide immediate feedback, and another update that ' + 'triggers the bulk of the changes.' + '\n\n' + 'Refer to the documentation for useSuspenseTransition to learn how ' + 'to implement this pattern.', // TODO: Add link to React docs with more information, once it exists
            componentNames.sort().join(', '));
          }
        }
      }
    }

    function computeThreadID(root, expirationTime) {
      // Interaction threads are unique per root and expiration time.
      return expirationTime * 1000 + root.interactionThreadID;
    }

    function markSpawnedWork(expirationTime) {
      if (!enableSchedulerTracing) {
        return;
      }

      if (spawnedWorkDuringRender === null) {
        spawnedWorkDuringRender = [expirationTime];
      } else {
        spawnedWorkDuringRender.push(expirationTime);
      }
    }

    function scheduleInteractions(root, expirationTime, interactions) {
      if (!enableSchedulerTracing) {
        return;
      }

      if (interactions.size > 0) {
        var pendingInteractionMap = root.pendingInteractionMap;
        var pendingInteractions = pendingInteractionMap.get(expirationTime);

        if (pendingInteractions != null) {
          interactions.forEach(function (interaction) {
            if (!pendingInteractions.has(interaction)) {
              // Update the pending async work count for previously unscheduled interaction.
              interaction.__count++;
            }

            pendingInteractions.add(interaction);
          });
        } else {
          pendingInteractionMap.set(expirationTime, new Set(interactions)); // Update the pending async work count for the current interactions.

          interactions.forEach(function (interaction) {
            interaction.__count++;
          });
        }

        var subscriber = tracing.__subscriberRef.current;

        if (subscriber !== null) {
          var threadID = computeThreadID(root, expirationTime);
          subscriber.onWorkScheduled(interactions, threadID);
        }
      }
    }

    function schedulePendingInteractions(root, expirationTime) {
      // This is called when work is scheduled on a root.
      // It associates the current interactions with the newly-scheduled expiration.
      // They will be restored when that expiration is later committed.
      if (!enableSchedulerTracing) {
        return;
      }

      scheduleInteractions(root, expirationTime, tracing.__interactionsRef.current);
    }

    function startWorkOnPendingInteractions(root, expirationTime) {
      // This is called when new work is started on a root.
      if (!enableSchedulerTracing) {
        return;
      } // Determine which interactions this batch of work currently includes, So that
      // we can accurately attribute time spent working on it, And so that cascading
      // work triggered during the render phase will be associated with it.


      var interactions = new Set();
      root.pendingInteractionMap.forEach(function (scheduledInteractions, scheduledExpirationTime) {
        if (scheduledExpirationTime >= expirationTime) {
          scheduledInteractions.forEach(function (interaction) {
            return interactions.add(interaction);
          });
        }
      }); // Store the current set of interactions on the FiberRoot for a few reasons:
      // We can re-use it in hot functions like renderRoot() without having to
      // recalculate it. We will also use it in commitWork() to pass to any Profiler
      // onRender() hooks. This also provides DevTools with a way to access it when
      // the onCommitRoot() hook is called.

      root.memoizedInteractions = interactions;

      if (interactions.size > 0) {
        var subscriber = tracing.__subscriberRef.current;

        if (subscriber !== null) {
          var threadID = computeThreadID(root, expirationTime);

          try {
            subscriber.onWorkStarted(interactions, threadID);
          } catch (error) {
            // If the subscriber throws, rethrow it in a separate task
            scheduleCallback(ImmediatePriority, function () {
              throw error;
            });
          }
        }
      }
    }

    function finishPendingInteractions(root, committedExpirationTime) {
      if (!enableSchedulerTracing) {
        return;
      }

      var earliestRemainingTimeAfterCommit = root.firstPendingTime;
      var subscriber;

      try {
        subscriber = tracing.__subscriberRef.current;

        if (subscriber !== null && root.memoizedInteractions.size > 0) {
          var threadID = computeThreadID(root, committedExpirationTime);
          subscriber.onWorkStopped(root.memoizedInteractions, threadID);
        }
      } catch (error) {
        // If the subscriber throws, rethrow it in a separate task
        scheduleCallback(ImmediatePriority, function () {
          throw error;
        });
      } finally {
        // Clear completed interactions from the pending Map.
        // Unless the render was suspended or cascading work was scheduled,
        // In which case– leave pending interactions until the subsequent render.
        var pendingInteractionMap = root.pendingInteractionMap;
        pendingInteractionMap.forEach(function (scheduledInteractions, scheduledExpirationTime) {
          // Only decrement the pending interaction count if we're done.
          // If there's still work at the current priority,
          // That indicates that we are waiting for suspense data.
          if (scheduledExpirationTime > earliestRemainingTimeAfterCommit) {
            pendingInteractionMap.delete(scheduledExpirationTime);
            scheduledInteractions.forEach(function (interaction) {
              interaction.__count--;

              if (subscriber !== null && interaction.__count === 0) {
                try {
                  subscriber.onInteractionScheduledWorkCompleted(interaction);
                } catch (error) {
                  // If the subscriber throws, rethrow it in a separate task
                  scheduleCallback(ImmediatePriority, function () {
                    throw error;
                  });
                }
              }
            });
          }
        });
      }
    }

    var onCommitFiberRoot = null;
    var onCommitFiberUnmount = null;
    var hasLoggedError = false;
    var isDevToolsPresent = typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== 'undefined';

    function injectInternals(internals) {
      if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined') {
        // No DevTools
        return false;
      }

      var hook = __REACT_DEVTOOLS_GLOBAL_HOOK__;

      if (hook.isDisabled) {
        // This isn't a real property on the hook, but it can be set to opt out
        // of DevTools integration and associated warnings and logs.
        // https://github.com/facebook/react/issues/3877
        return true;
      }

      if (!hook.supportsFiber) {
        {
          warningWithoutStack$1(false, 'The installed version of React DevTools is too old and will not work ' + 'with the current version of React. Please update React DevTools. ' + 'https://fb.me/react-devtools');
        } // DevTools exists, even though it doesn't support Fiber.

        return true;
      }

      try {
        var rendererID = hook.inject(internals); // We have successfully injected, so now it is safe to set up hooks.

        onCommitFiberRoot = function (root, expirationTime) {
          try {
            var didError = (root.current.effectTag & DidCapture) === DidCapture;

            if (enableProfilerTimer) {
              var currentTime = requestCurrentTime();
              var priorityLevel = inferPriorityFromExpirationTime(currentTime, expirationTime);
              hook.onCommitFiberRoot(rendererID, root, priorityLevel, didError);
            } else {
              hook.onCommitFiberRoot(rendererID, root, undefined, didError);
            }
          } catch (err) {
            if (true && !hasLoggedError) {
              hasLoggedError = true;
              warningWithoutStack$1(false, 'React DevTools encountered an error: %s', err);
            }
          }
        };

        onCommitFiberUnmount = function (fiber) {
          try {
            hook.onCommitFiberUnmount(rendererID, fiber);
          } catch (err) {
            if (true && !hasLoggedError) {
              hasLoggedError = true;
              warningWithoutStack$1(false, 'React DevTools encountered an error: %s', err);
            }
          }
        };
      } catch (err) {
        // Catch all errors because it is unsafe to throw during initialization.
        {
          warningWithoutStack$1(false, 'React DevTools encountered an error: %s.', err);
        }
      } // DevTools exists


      return true;
    }

    function onCommitRoot(root, expirationTime) {
      if (typeof onCommitFiberRoot === 'function') {
        onCommitFiberRoot(root, expirationTime);
      }
    }

    function onCommitUnmount(fiber) {
      if (typeof onCommitFiberUnmount === 'function') {
        onCommitFiberUnmount(fiber);
      }
    }

    var hasBadMapPolyfill;
    {
      hasBadMapPolyfill = false;

      try {
        var nonExtensibleObject = Object.preventExtensions({});
        var testMap = new Map([[nonExtensibleObject, null]]);
        var testSet = new Set([nonExtensibleObject]); // This is necessary for Rollup to not consider these unused.
        // https://github.com/rollup/rollup/issues/1771
        // TODO: we can remove these if Rollup fixes the bug.

        testMap.set(0, 0);
        testSet.add(0);
      } catch (e) {
        // TODO: Consider warning about bad polyfills
        hasBadMapPolyfill = true;
      }
    }
    var debugCounter = 1;

    function FiberNode(tag, pendingProps, key, mode) {
      // Instance
      this.tag = tag;
      this.key = key;
      this.elementType = null;
      this.type = null;
      this.stateNode = null; // Fiber

      this.return = null;
      this.child = null;
      this.sibling = null;
      this.index = 0;
      this.ref = null;
      this.pendingProps = pendingProps;
      this.memoizedProps = null;
      this.updateQueue = null;
      this.memoizedState = null;
      this.dependencies = null;
      this.mode = mode; // Effects

      this.effectTag = NoEffect;
      this.nextEffect = null;
      this.firstEffect = null;
      this.lastEffect = null;
      this.expirationTime = NoWork;
      this.childExpirationTime = NoWork;
      this.alternate = null;

      if (enableProfilerTimer) {
        // Note: The following is done to avoid a v8 performance cliff.
        //
        // Initializing the fields below to smis and later updating them with
        // double values will cause Fibers to end up having separate shapes.
        // This behavior/bug has something to do with Object.preventExtension().
        // Fortunately this only impacts DEV builds.
        // Unfortunately it makes React unusably slow for some applications.
        // To work around this, initialize the fields below with doubles.
        //
        // Learn more about this here:
        // https://github.com/facebook/react/issues/14365
        // https://bugs.chromium.org/p/v8/issues/detail?id=8538
        this.actualDuration = Number.NaN;
        this.actualStartTime = Number.NaN;
        this.selfBaseDuration = Number.NaN;
        this.treeBaseDuration = Number.NaN; // It's okay to replace the initial doubles with smis after initialization.
        // This won't trigger the performance cliff mentioned above,
        // and it simplifies other profiler code (including DevTools).

        this.actualDuration = 0;
        this.actualStartTime = -1;
        this.selfBaseDuration = 0;
        this.treeBaseDuration = 0;
      } // This is normally DEV-only except www when it adds listeners.
      // TODO: remove the User Timing integration in favor of Root Events.


      if (enableUserTimingAPI) {
        this._debugID = debugCounter++;
        this._debugIsCurrentlyTiming = false;
      }

      {
        this._debugSource = null;
        this._debugOwner = null;
        this._debugNeedsRemount = false;
        this._debugHookTypes = null;

        if (!hasBadMapPolyfill && typeof Object.preventExtensions === 'function') {
          Object.preventExtensions(this);
        }
      }
    } // This is a constructor function, rather than a POJO constructor, still
    // please ensure we do the following:
    // 1) Nobody should add any instance methods on this. Instance methods can be
    //    more difficult to predict when they get optimized and they are almost
    //    never inlined properly in static compilers.
    // 2) Nobody should rely on `instanceof Fiber` for type testing. We should
    //    always know when it is a fiber.
    // 3) We might want to experiment with using numeric keys since they are easier
    //    to optimize in a non-JIT environment.
    // 4) We can easily go from a constructor to a createFiber object literal if that
    //    is faster.
    // 5) It should be easy to port this to a C struct and keep a C implementation
    //    compatible.


    var createFiber = function (tag, pendingProps, key, mode) {
      // $FlowFixMe: the shapes are exact here but Flow doesn't like constructors
      return new FiberNode(tag, pendingProps, key, mode);
    };

    function shouldConstruct(Component) {
      var prototype = Component.prototype;
      return !!(prototype && prototype.isReactComponent);
    }

    function isSimpleFunctionComponent(type) {
      return typeof type === 'function' && !shouldConstruct(type) && type.defaultProps === undefined;
    }

    function resolveLazyComponentTag(Component) {
      if (typeof Component === 'function') {
        return shouldConstruct(Component) ? ClassComponent : FunctionComponent;
      } else if (Component !== undefined && Component !== null) {
        var $$typeof = Component.$$typeof;

        if ($$typeof === REACT_FORWARD_REF_TYPE) {
          return ForwardRef;
        }

        if ($$typeof === REACT_MEMO_TYPE) {
          return MemoComponent;
        }
      }

      return IndeterminateComponent;
    } // This is used to create an alternate fiber to do work on.


    function createWorkInProgress(current, pendingProps, expirationTime) {
      var workInProgress = current.alternate;

      if (workInProgress === null) {
        // We use a double buffering pooling technique because we know that we'll
        // only ever need at most two versions of a tree. We pool the "other" unused
        // node that we're free to reuse. This is lazily created to avoid allocating
        // extra objects for things that are never updated. It also allow us to
        // reclaim the extra memory if needed.
        workInProgress = createFiber(current.tag, pendingProps, current.key, current.mode);
        workInProgress.elementType = current.elementType;
        workInProgress.type = current.type;
        workInProgress.stateNode = current.stateNode;
        {
          // DEV-only fields
          workInProgress._debugID = current._debugID;
          workInProgress._debugSource = current._debugSource;
          workInProgress._debugOwner = current._debugOwner;
          workInProgress._debugHookTypes = current._debugHookTypes;
        }
        workInProgress.alternate = current;
        current.alternate = workInProgress;
      } else {
        workInProgress.pendingProps = pendingProps; // We already have an alternate.
        // Reset the effect tag.

        workInProgress.effectTag = NoEffect; // The effect list is no longer valid.

        workInProgress.nextEffect = null;
        workInProgress.firstEffect = null;
        workInProgress.lastEffect = null;

        if (enableProfilerTimer) {
          // We intentionally reset, rather than copy, actualDuration & actualStartTime.
          // This prevents time from endlessly accumulating in new commits.
          // This has the downside of resetting values for different priority renders,
          // But works for yielding (the common case) and should support resuming.
          workInProgress.actualDuration = 0;
          workInProgress.actualStartTime = -1;
        }
      }

      workInProgress.childExpirationTime = current.childExpirationTime;
      workInProgress.expirationTime = current.expirationTime;
      workInProgress.child = current.child;
      workInProgress.memoizedProps = current.memoizedProps;
      workInProgress.memoizedState = current.memoizedState;
      workInProgress.updateQueue = current.updateQueue; // Clone the dependencies object. This is mutated during the render phase, so
      // it cannot be shared with the current fiber.

      var currentDependencies = current.dependencies;
      workInProgress.dependencies = currentDependencies === null ? null : {
        expirationTime: currentDependencies.expirationTime,
        firstContext: currentDependencies.firstContext,
        responders: currentDependencies.responders
      }; // These will be overridden during the parent's reconciliation

      workInProgress.sibling = current.sibling;
      workInProgress.index = current.index;
      workInProgress.ref = current.ref;

      if (enableProfilerTimer) {
        workInProgress.selfBaseDuration = current.selfBaseDuration;
        workInProgress.treeBaseDuration = current.treeBaseDuration;
      }

      {
        workInProgress._debugNeedsRemount = current._debugNeedsRemount;

        switch (workInProgress.tag) {
          case IndeterminateComponent:
          case FunctionComponent:
          case SimpleMemoComponent:
            workInProgress.type = resolveFunctionForHotReloading(current.type);
            break;

          case ClassComponent:
            workInProgress.type = resolveClassForHotReloading(current.type);
            break;

          case ForwardRef:
            workInProgress.type = resolveForwardRefForHotReloading(current.type);
            break;

          default:
            break;
        }
      }
      return workInProgress;
    } // Used to reuse a Fiber for a second pass.


    function resetWorkInProgress(workInProgress, renderExpirationTime) {
      // This resets the Fiber to what createFiber or createWorkInProgress would
      // have set the values to before during the first pass. Ideally this wouldn't
      // be necessary but unfortunately many code paths reads from the workInProgress
      // when they should be reading from current and writing to workInProgress.
      // We assume pendingProps, index, key, ref, return are still untouched to
      // avoid doing another reconciliation.
      // Reset the effect tag but keep any Placement tags, since that's something
      // that child fiber is setting, not the reconciliation.
      workInProgress.effectTag &= Placement; // The effect list is no longer valid.

      workInProgress.nextEffect = null;
      workInProgress.firstEffect = null;
      workInProgress.lastEffect = null;
      var current = workInProgress.alternate;

      if (current === null) {
        // Reset to createFiber's initial values.
        workInProgress.childExpirationTime = NoWork;
        workInProgress.expirationTime = renderExpirationTime;
        workInProgress.child = null;
        workInProgress.memoizedProps = null;
        workInProgress.memoizedState = null;
        workInProgress.updateQueue = null;
        workInProgress.dependencies = null;

        if (enableProfilerTimer) {
          // Note: We don't reset the actualTime counts. It's useful to accumulate
          // actual time across multiple render passes.
          workInProgress.selfBaseDuration = 0;
          workInProgress.treeBaseDuration = 0;
        }
      } else {
        // Reset to the cloned values that createWorkInProgress would've.
        workInProgress.childExpirationTime = current.childExpirationTime;
        workInProgress.expirationTime = current.expirationTime;
        workInProgress.child = current.child;
        workInProgress.memoizedProps = current.memoizedProps;
        workInProgress.memoizedState = current.memoizedState;
        workInProgress.updateQueue = current.updateQueue; // Clone the dependencies object. This is mutated during the render phase, so
        // it cannot be shared with the current fiber.

        var currentDependencies = current.dependencies;
        workInProgress.dependencies = currentDependencies === null ? null : {
          expirationTime: currentDependencies.expirationTime,
          firstContext: currentDependencies.firstContext,
          responders: currentDependencies.responders
        };

        if (enableProfilerTimer) {
          // Note: We don't reset the actualTime counts. It's useful to accumulate
          // actual time across multiple render passes.
          workInProgress.selfBaseDuration = current.selfBaseDuration;
          workInProgress.treeBaseDuration = current.treeBaseDuration;
        }
      }

      return workInProgress;
    }

    function createHostRootFiber(tag) {
      var mode;

      if (tag === ConcurrentRoot) {
        mode = ConcurrentMode | BatchedMode | StrictMode;
      } else if (tag === BatchedRoot) {
        mode = BatchedMode | StrictMode;
      } else {
        mode = NoMode;
      }

      if (enableProfilerTimer && isDevToolsPresent) {
        // Always collect profile timings when DevTools are present.
        // This enables DevTools to start capturing timing at any point–
        // Without some nodes in the tree having empty base times.
        mode |= ProfileMode;
      }

      return createFiber(HostRoot, null, null, mode);
    }

    function createFiberFromTypeAndProps(type, // React$ElementType
    key, pendingProps, owner, mode, expirationTime) {
      var fiber;
      var fiberTag = IndeterminateComponent; // The resolved type is set if we know what the final type will be. I.e. it's not lazy.

      var resolvedType = type;

      if (typeof type === 'function') {
        if (shouldConstruct(type)) {
          fiberTag = ClassComponent;
          {
            resolvedType = resolveClassForHotReloading(resolvedType);
          }
        } else {
          {
            resolvedType = resolveFunctionForHotReloading(resolvedType);
          }
        }
      } else if (typeof type === 'string') {
        fiberTag = HostComponent;
      } else {
        getTag: switch (type) {
          case REACT_FRAGMENT_TYPE:
            return createFiberFromFragment(pendingProps.children, mode, expirationTime, key);

          case REACT_CONCURRENT_MODE_TYPE:
            fiberTag = Mode;
            mode |= ConcurrentMode | BatchedMode | StrictMode;
            break;

          case REACT_STRICT_MODE_TYPE:
            fiberTag = Mode;
            mode |= StrictMode;
            break;

          case REACT_PROFILER_TYPE:
            return createFiberFromProfiler(pendingProps, mode, expirationTime, key);

          case REACT_SUSPENSE_TYPE:
            return createFiberFromSuspense(pendingProps, mode, expirationTime, key);

          case REACT_SUSPENSE_LIST_TYPE:
            return createFiberFromSuspenseList(pendingProps, mode, expirationTime, key);

          default:
            {
              if (typeof type === 'object' && type !== null) {
                switch (type.$$typeof) {
                  case REACT_PROVIDER_TYPE:
                    fiberTag = ContextProvider;
                    break getTag;

                  case REACT_CONTEXT_TYPE:
                    // This is a consumer
                    fiberTag = ContextConsumer;
                    break getTag;

                  case REACT_FORWARD_REF_TYPE:
                    fiberTag = ForwardRef;
                    {
                      resolvedType = resolveForwardRefForHotReloading(resolvedType);
                    }
                    break getTag;

                  case REACT_MEMO_TYPE:
                    fiberTag = MemoComponent;
                    break getTag;

                  case REACT_LAZY_TYPE:
                    fiberTag = LazyComponent;
                    resolvedType = null;
                    break getTag;

                  case REACT_FUNDAMENTAL_TYPE:
                    if (enableFundamentalAPI) {
                      return createFiberFromFundamental(type, pendingProps, mode, expirationTime, key);
                    }

                    break;

                  case REACT_SCOPE_TYPE:
                    if (enableScopeAPI) {
                      return createFiberFromScope(type, pendingProps, mode, expirationTime, key);
                    }

                }
              }

              var info = '';
              {
                if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {
                  info += ' You likely forgot to export your component from the file ' + "it's defined in, or you might have mixed up default and " + 'named imports.';
                }

                var ownerName = owner ? getComponentName(owner.type) : null;

                if (ownerName) {
                  info += '\n\nCheck the render method of `' + ownerName + '`.';
                }
              }

              (function () {
                {
                  {
                    throw ReactError(Error("Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) but got: " + (type == null ? type : typeof type) + "." + info));
                  }
                }
              })();
            }
        }
      }

      fiber = createFiber(fiberTag, pendingProps, key, mode);
      fiber.elementType = type;
      fiber.type = resolvedType;
      fiber.expirationTime = expirationTime;
      return fiber;
    }

    function createFiberFromElement(element, mode, expirationTime) {
      var owner = null;
      {
        owner = element._owner;
      }
      var type = element.type;
      var key = element.key;
      var pendingProps = element.props;
      var fiber = createFiberFromTypeAndProps(type, key, pendingProps, owner, mode, expirationTime);
      {
        fiber._debugSource = element._source;
        fiber._debugOwner = element._owner;
      }
      return fiber;
    }

    function createFiberFromFragment(elements, mode, expirationTime, key) {
      var fiber = createFiber(Fragment, elements, key, mode);
      fiber.expirationTime = expirationTime;
      return fiber;
    }

    function createFiberFromFundamental(fundamentalComponent, pendingProps, mode, expirationTime, key) {
      var fiber = createFiber(FundamentalComponent, pendingProps, key, mode);
      fiber.elementType = fundamentalComponent;
      fiber.type = fundamentalComponent;
      fiber.expirationTime = expirationTime;
      return fiber;
    }

    function createFiberFromScope(scope, pendingProps, mode, expirationTime, key) {
      var fiber = createFiber(ScopeComponent, pendingProps, key, mode);
      fiber.type = scope;
      fiber.elementType = scope;
      fiber.expirationTime = expirationTime;
      return fiber;
    }

    function createFiberFromProfiler(pendingProps, mode, expirationTime, key) {
      {
        if (typeof pendingProps.id !== 'string' || typeof pendingProps.onRender !== 'function') {
          warningWithoutStack$1(false, 'Profiler must specify an "id" string and "onRender" function as props');
        }
      }
      var fiber = createFiber(Profiler, pendingProps, key, mode | ProfileMode); // TODO: The Profiler fiber shouldn't have a type. It has a tag.

      fiber.elementType = REACT_PROFILER_TYPE;
      fiber.type = REACT_PROFILER_TYPE;
      fiber.expirationTime = expirationTime;
      return fiber;
    }

    function createFiberFromSuspense(pendingProps, mode, expirationTime, key) {
      var fiber = createFiber(SuspenseComponent, pendingProps, key, mode); // TODO: The SuspenseComponent fiber shouldn't have a type. It has a tag.
      // This needs to be fixed in getComponentName so that it relies on the tag
      // instead.

      fiber.type = REACT_SUSPENSE_TYPE;
      fiber.elementType = REACT_SUSPENSE_TYPE;
      fiber.expirationTime = expirationTime;
      return fiber;
    }

    function createFiberFromSuspenseList(pendingProps, mode, expirationTime, key) {
      var fiber = createFiber(SuspenseListComponent, pendingProps, key, mode);
      {
        // TODO: The SuspenseListComponent fiber shouldn't have a type. It has a tag.
        // This needs to be fixed in getComponentName so that it relies on the tag
        // instead.
        fiber.type = REACT_SUSPENSE_LIST_TYPE;
      }
      fiber.elementType = REACT_SUSPENSE_LIST_TYPE;
      fiber.expirationTime = expirationTime;
      return fiber;
    }

    function createFiberFromText(content, mode, expirationTime) {
      var fiber = createFiber(HostText, content, null, mode);
      fiber.expirationTime = expirationTime;
      return fiber;
    }

    function createFiberFromHostInstanceForDeletion() {
      var fiber = createFiber(HostComponent, null, null, NoMode); // TODO: These should not need a type.

      fiber.elementType = 'DELETED';
      fiber.type = 'DELETED';
      return fiber;
    }

    function createFiberFromDehydratedFragment(dehydratedNode) {
      var fiber = createFiber(DehydratedFragment, null, null, NoMode);
      fiber.stateNode = dehydratedNode;
      return fiber;
    }

    function createFiberFromPortal(portal, mode, expirationTime) {
      var pendingProps = portal.children !== null ? portal.children : [];
      var fiber = createFiber(HostPortal, pendingProps, portal.key, mode);
      fiber.expirationTime = expirationTime;
      fiber.stateNode = {
        containerInfo: portal.containerInfo,
        pendingChildren: null,
        // Used by persistent updates
        implementation: portal.implementation
      };
      return fiber;
    } // Used for stashing WIP properties to replay failed work in DEV.


    function assignFiberPropertiesInDEV(target, source) {
      if (target === null) {
        // This Fiber's initial properties will always be overwritten.
        // We only use a Fiber to ensure the same hidden class so DEV isn't slow.
        target = createFiber(IndeterminateComponent, null, null, NoMode);
      } // This is intentionally written as a list of all properties.
      // We tried to use Object.assign() instead but this is called in
      // the hottest path, and Object.assign() was too slow:
      // https://github.com/facebook/react/issues/12502
      // This code is DEV-only so size is not a concern.


      target.tag = source.tag;
      target.key = source.key;
      target.elementType = source.elementType;
      target.type = source.type;
      target.stateNode = source.stateNode;
      target.return = source.return;
      target.child = source.child;
      target.sibling = source.sibling;
      target.index = source.index;
      target.ref = source.ref;
      target.pendingProps = source.pendingProps;
      target.memoizedProps = source.memoizedProps;
      target.updateQueue = source.updateQueue;
      target.memoizedState = source.memoizedState;
      target.dependencies = source.dependencies;
      target.mode = source.mode;
      target.effectTag = source.effectTag;
      target.nextEffect = source.nextEffect;
      target.firstEffect = source.firstEffect;
      target.lastEffect = source.lastEffect;
      target.expirationTime = source.expirationTime;
      target.childExpirationTime = source.childExpirationTime;
      target.alternate = source.alternate;

      if (enableProfilerTimer) {
        target.actualDuration = source.actualDuration;
        target.actualStartTime = source.actualStartTime;
        target.selfBaseDuration = source.selfBaseDuration;
        target.treeBaseDuration = source.treeBaseDuration;
      }

      target._debugID = source._debugID;
      target._debugSource = source._debugSource;
      target._debugOwner = source._debugOwner;
      target._debugIsCurrentlyTiming = source._debugIsCurrentlyTiming;
      target._debugNeedsRemount = source._debugNeedsRemount;
      target._debugHookTypes = source._debugHookTypes;
      return target;
    }

    function FiberRootNode(containerInfo, tag, hydrate) {
      this.tag = tag;
      this.current = null;
      this.containerInfo = containerInfo;
      this.pendingChildren = null;
      this.pingCache = null;
      this.finishedExpirationTime = NoWork;
      this.finishedWork = null;
      this.timeoutHandle = noTimeout;
      this.context = null;
      this.pendingContext = null;
      this.hydrate = hydrate;
      this.firstBatch = null;
      this.callbackNode = null;
      this.callbackPriority = NoPriority;
      this.firstPendingTime = NoWork;
      this.firstSuspendedTime = NoWork;
      this.lastSuspendedTime = NoWork;
      this.nextKnownPendingLevel = NoWork;
      this.lastPingedTime = NoWork;
      this.lastExpiredTime = NoWork;

      if (enableSchedulerTracing) {
        this.interactionThreadID = tracing.unstable_getThreadID();
        this.memoizedInteractions = new Set();
        this.pendingInteractionMap = new Map();
      }

      if (enableSuspenseCallback) {
        this.hydrationCallbacks = null;
      }
    }

    function createFiberRoot(containerInfo, tag, hydrate, hydrationCallbacks) {
      var root = new FiberRootNode(containerInfo, tag, hydrate);

      if (enableSuspenseCallback) {
        root.hydrationCallbacks = hydrationCallbacks;
      } // Cyclic construction. This cheats the type system right now because
      // stateNode is any.


      var uninitializedFiber = createHostRootFiber(tag);
      root.current = uninitializedFiber;
      uninitializedFiber.stateNode = root;
      return root;
    }

    function isRootSuspendedAtTime(root, expirationTime) {
      var firstSuspendedTime = root.firstSuspendedTime;
      var lastSuspendedTime = root.lastSuspendedTime;
      return firstSuspendedTime !== NoWork && firstSuspendedTime >= expirationTime && lastSuspendedTime <= expirationTime;
    }

    function markRootSuspendedAtTime(root, expirationTime) {
      var firstSuspendedTime = root.firstSuspendedTime;
      var lastSuspendedTime = root.lastSuspendedTime;

      if (firstSuspendedTime < expirationTime) {
        root.firstSuspendedTime = expirationTime;
      }

      if (lastSuspendedTime > expirationTime || firstSuspendedTime === NoWork) {
        root.lastSuspendedTime = expirationTime;
      }

      if (expirationTime <= root.lastPingedTime) {
        root.lastPingedTime = NoWork;
      }

      if (expirationTime <= root.lastExpiredTime) {
        root.lastExpiredTime = NoWork;
      }
    }

    function markRootUpdatedAtTime(root, expirationTime) {
      // Update the range of pending times
      var firstPendingTime = root.firstPendingTime;

      if (expirationTime > firstPendingTime) {
        root.firstPendingTime = expirationTime;
      } // Update the range of suspended times. Treat everything lower priority or
      // equal to this update as unsuspended.


      var firstSuspendedTime = root.firstSuspendedTime;

      if (firstSuspendedTime !== NoWork) {
        if (expirationTime >= firstSuspendedTime) {
          // The entire suspended range is now unsuspended.
          root.firstSuspendedTime = root.lastSuspendedTime = root.nextKnownPendingLevel = NoWork;
        } else if (expirationTime >= root.lastSuspendedTime) {
          root.lastSuspendedTime = expirationTime + 1;
        } // This is a pending level. Check if it's higher priority than the next
        // known pending level.


        if (expirationTime > root.nextKnownPendingLevel) {
          root.nextKnownPendingLevel = expirationTime;
        }
      }
    }

    function markRootFinishedAtTime(root, finishedExpirationTime, remainingExpirationTime) {
      // Update the range of pending times
      root.firstPendingTime = remainingExpirationTime; // Update the range of suspended times. Treat everything higher priority or
      // equal to this update as unsuspended.

      if (finishedExpirationTime <= root.lastSuspendedTime) {
        // The entire suspended range is now unsuspended.
        root.firstSuspendedTime = root.lastSuspendedTime = root.nextKnownPendingLevel = NoWork;
      } else if (finishedExpirationTime <= root.firstSuspendedTime) {
        // Part of the suspended range is now unsuspended. Narrow the range to
        // include everything between the unsuspended time (non-inclusive) and the
        // last suspended time.
        root.firstSuspendedTime = finishedExpirationTime - 1;
      }

      if (finishedExpirationTime <= root.lastPingedTime) {
        // Clear the pinged time
        root.lastPingedTime = NoWork;
      }

      if (finishedExpirationTime <= root.lastExpiredTime) {
        // Clear the expired time
        root.lastExpiredTime = NoWork;
      }
    }

    function markRootExpiredAtTime(root, expirationTime) {
      var lastExpiredTime = root.lastExpiredTime;

      if (lastExpiredTime === NoWork || lastExpiredTime > expirationTime) {
        root.lastExpiredTime = expirationTime;
      }
    } // This lets us hook into Fiber to debug what it's doing.
    // See https://github.com/facebook/react/pull/8033.
    // This is not part of the public API, not even for React DevTools.
    // You may only inject a debugTool if you work on React Fiber itself.


    var ReactFiberInstrumentation = {
      debugTool: null
    };
    var ReactFiberInstrumentation_1 = ReactFiberInstrumentation;
    var didWarnAboutNestedUpdates;
    var didWarnAboutFindNodeInStrictMode;
    {
      didWarnAboutNestedUpdates = false;
      didWarnAboutFindNodeInStrictMode = {};
    }

    function getContextForSubtree(parentComponent) {
      if (!parentComponent) {
        return emptyContextObject;
      }

      var fiber = get(parentComponent);
      var parentContext = findCurrentUnmaskedContext(fiber);

      if (fiber.tag === ClassComponent) {
        var Component = fiber.type;

        if (isContextProvider(Component)) {
          return processChildContext(fiber, Component, parentContext);
        }
      }

      return parentContext;
    }

    function scheduleRootUpdate(current$$1, element, expirationTime, suspenseConfig, callback) {
      {
        if (phase === 'render' && current !== null && !didWarnAboutNestedUpdates) {
          didWarnAboutNestedUpdates = true;
          warningWithoutStack$1(false, 'Render methods should be a pure function of props and state; ' + 'triggering nested component updates from render is not allowed. ' + 'If necessary, trigger nested updates in componentDidUpdate.\n\n' + 'Check the render method of %s.', getComponentName(current.type) || 'Unknown');
        }
      }
      var update = createUpdate(expirationTime, suspenseConfig); // Caution: React DevTools currently depends on this property
      // being called "element".

      update.payload = {
        element: element
      };
      callback = callback === undefined ? null : callback;

      if (callback !== null) {
        !(typeof callback === 'function') ? warningWithoutStack$1(false, 'render(...): Expected the last optional `callback` argument to be a ' + 'function. Instead received: %s.', callback) : void 0;
        update.callback = callback;
      }

      enqueueUpdate(current$$1, update);
      scheduleWork(current$$1, expirationTime);
      return expirationTime;
    }

    function updateContainerAtExpirationTime(element, container, parentComponent, expirationTime, suspenseConfig, callback) {
      // TODO: If this is a nested container, this won't be the root.
      var current$$1 = container.current;
      {
        if (ReactFiberInstrumentation_1.debugTool) {
          if (current$$1.alternate === null) {
            ReactFiberInstrumentation_1.debugTool.onMountContainer(container);
          } else if (element === null) {
            ReactFiberInstrumentation_1.debugTool.onUnmountContainer(container);
          } else {
            ReactFiberInstrumentation_1.debugTool.onUpdateContainer(container);
          }
        }
      }
      var context = getContextForSubtree(parentComponent);

      if (container.context === null) {
        container.context = context;
      } else {
        container.pendingContext = context;
      }

      return scheduleRootUpdate(current$$1, element, expirationTime, suspenseConfig, callback);
    }

    function findHostInstance(component) {
      var fiber = get(component);

      if (fiber === undefined) {
        if (typeof component.render === 'function') {
          (function () {
            {
              {
                throw ReactError(Error("Unable to find node on an unmounted component."));
              }
            }
          })();
        } else {
          (function () {
            {
              {
                throw ReactError(Error("Argument appears to not be a ReactComponent. Keys: " + Object.keys(component)));
              }
            }
          })();
        }
      }

      var hostFiber = findCurrentHostFiber(fiber);

      if (hostFiber === null) {
        return null;
      }

      return hostFiber.stateNode;
    }

    function findHostInstanceWithWarning(component, methodName) {
      {
        var fiber = get(component);

        if (fiber === undefined) {
          if (typeof component.render === 'function') {
            (function () {
              {
                {
                  throw ReactError(Error("Unable to find node on an unmounted component."));
                }
              }
            })();
          } else {
            (function () {
              {
                {
                  throw ReactError(Error("Argument appears to not be a ReactComponent. Keys: " + Object.keys(component)));
                }
              }
            })();
          }
        }

        var hostFiber = findCurrentHostFiber(fiber);

        if (hostFiber === null) {
          return null;
        }

        if (hostFiber.mode & StrictMode) {
          var componentName = getComponentName(fiber.type) || 'Component';

          if (!didWarnAboutFindNodeInStrictMode[componentName]) {
            didWarnAboutFindNodeInStrictMode[componentName] = true;

            if (fiber.mode & StrictMode) {
              warningWithoutStack$1(false, '%s is deprecated in StrictMode. ' + '%s was passed an instance of %s which is inside StrictMode. ' + 'Instead, add a ref directly to the element you want to reference. ' + 'Learn more about using refs safely here: ' + 'https://fb.me/react-strict-mode-find-node%s', methodName, methodName, componentName, getStackByFiberInDevAndProd(hostFiber));
            } else {
              warningWithoutStack$1(false, '%s is deprecated in StrictMode. ' + '%s was passed an instance of %s which renders StrictMode children. ' + 'Instead, add a ref directly to the element you want to reference. ' + 'Learn more about using refs safely here: ' + 'https://fb.me/react-strict-mode-find-node%s', methodName, methodName, componentName, getStackByFiberInDevAndProd(hostFiber));
            }
          }
        }

        return hostFiber.stateNode;
      }
      return findHostInstance(component);
    }

    function createContainer(containerInfo, tag, hydrate, hydrationCallbacks) {
      return createFiberRoot(containerInfo, tag, hydrate, hydrationCallbacks);
    }

    function updateContainer(element, container, parentComponent, callback) {
      var current$$1 = container.current;
      var currentTime = requestCurrentTime();
      {
        // $FlowExpectedError - jest isn't a global, and isn't recognized outside of tests
        if ('undefined' !== typeof jest) {
          warnIfUnmockedScheduler(current$$1);
          warnIfNotScopedWithMatchingAct(current$$1);
        }
      }
      var suspenseConfig = requestCurrentSuspenseConfig();
      var expirationTime = computeExpirationForFiber(currentTime, current$$1, suspenseConfig);
      return updateContainerAtExpirationTime(element, container, parentComponent, expirationTime, suspenseConfig, callback);
    }

    function getPublicRootInstance(container) {
      var containerFiber = container.current;

      if (!containerFiber.child) {
        return null;
      }

      switch (containerFiber.child.tag) {
        case HostComponent:
          return getPublicInstance(containerFiber.child.stateNode);

        default:
          return containerFiber.child.stateNode;
      }
    }

    function attemptSynchronousHydration$1(fiber) {
      switch (fiber.tag) {
        case HostRoot:
          var root = fiber.stateNode;

          if (root.hydrate) {
            // Flush the first scheduled "update".
            flushRoot(root, root.firstPendingTime);
          }

          break;

        case SuspenseComponent:
          flushSync(function () {
            return scheduleWork(fiber, Sync);
          });
          break;
      }
    }

    function findHostInstanceWithNoPortals(fiber) {
      var hostFiber = findCurrentHostFiberWithNoPortals(fiber);

      if (hostFiber === null) {
        return null;
      }

      if (hostFiber.tag === FundamentalComponent) {
        return hostFiber.stateNode.instance;
      }

      return hostFiber.stateNode;
    }

    var shouldSuspendImpl = function (fiber) {
      return false;
    };

    function shouldSuspend(fiber) {
      return shouldSuspendImpl(fiber);
    }

    var overrideHookState = null;
    var overrideProps = null;
    var scheduleUpdate = null;
    var setSuspenseHandler = null;
    {
      var copyWithSetImpl = function (obj, path, idx, value) {
        if (idx >= path.length) {
          return value;
        }

        var key = path[idx];
        var updated = Array.isArray(obj) ? obj.slice() : _assign({}, obj); // $FlowFixMe number or string is fine here

        updated[key] = copyWithSetImpl(obj[key], path, idx + 1, value);
        return updated;
      };

      var copyWithSet = function (obj, path, value) {
        return copyWithSetImpl(obj, path, 0, value);
      }; // Support DevTools editable values for useState and useReducer.


      overrideHookState = function (fiber, id, path, value) {
        // For now, the "id" of stateful hooks is just the stateful hook index.
        // This may change in the future with e.g. nested hooks.
        var currentHook = fiber.memoizedState;

        while (currentHook !== null && id > 0) {
          currentHook = currentHook.next;
          id--;
        }

        if (currentHook !== null) {
          var newState = copyWithSet(currentHook.memoizedState, path, value);
          currentHook.memoizedState = newState;
          currentHook.baseState = newState; // We aren't actually adding an update to the queue,
          // because there is no update we can add for useReducer hooks that won't trigger an error.
          // (There's no appropriate action type for DevTools overrides.)
          // As a result though, React will see the scheduled update as a noop and bailout.
          // Shallow cloning props works as a workaround for now to bypass the bailout check.

          fiber.memoizedProps = _assign({}, fiber.memoizedProps);
          scheduleWork(fiber, Sync);
        }
      }; // Support DevTools props for function components, forwardRef, memo, host components, etc.


      overrideProps = function (fiber, path, value) {
        fiber.pendingProps = copyWithSet(fiber.memoizedProps, path, value);

        if (fiber.alternate) {
          fiber.alternate.pendingProps = fiber.pendingProps;
        }

        scheduleWork(fiber, Sync);
      };

      scheduleUpdate = function (fiber) {
        scheduleWork(fiber, Sync);
      };

      setSuspenseHandler = function (newShouldSuspendImpl) {
        shouldSuspendImpl = newShouldSuspendImpl;
      };
    }

    function injectIntoDevTools(devToolsConfig) {
      var findFiberByHostInstance = devToolsConfig.findFiberByHostInstance;
      var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher;
      return injectInternals(_assign({}, devToolsConfig, {
        overrideHookState: overrideHookState,
        overrideProps: overrideProps,
        setSuspenseHandler: setSuspenseHandler,
        scheduleUpdate: scheduleUpdate,
        currentDispatcherRef: ReactCurrentDispatcher,
        findHostInstanceByFiber: function (fiber) {
          var hostFiber = findCurrentHostFiber(fiber);

          if (hostFiber === null) {
            return null;
          }

          return hostFiber.stateNode;
        },
        findFiberByHostInstance: function (instance) {
          if (!findFiberByHostInstance) {
            // Might not be implemented by the renderer.
            return null;
          }

          return findFiberByHostInstance(instance);
        },
        // React Refresh
        findHostInstancesForRefresh: findHostInstancesForRefresh,
        scheduleRefresh: scheduleRefresh,
        scheduleRoot: scheduleRoot,
        setRefreshHandler: setRefreshHandler,
        // Enables DevTools to append owner stacks to error messages in DEV mode.
        getCurrentFiber: function () {
          return current;
        }
      }));
    } // This file intentionally does *not* have the Flow annotation.
    // Don't add it. See `./inline-typed.js` for an explanation.


    function createPortal$1(children, containerInfo, // TODO: figure out the API for cross-renderer implementation.
    implementation) {
      var key = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : null;
      return {
        // This tag allow us to uniquely identify this as a React Portal
        $$typeof: REACT_PORTAL_TYPE,
        key: key == null ? null : '' + key,
        children: children,
        containerInfo: containerInfo,
        implementation: implementation
      };
    } // TODO: this is special because it gets imported during build.


    var ReactVersion = '16.10.1'; // TODO: This type is shared between the reconciler and ReactDOM, but will
    // eventually be lifted out to the renderer.

    setAttemptSynchronousHydration(attemptSynchronousHydration$1);
    var ReactCurrentOwner = ReactSharedInternals.ReactCurrentOwner;
    var topLevelUpdateWarnings;
    var warnOnInvalidCallback;
    var didWarnAboutUnstableCreatePortal = false;
    {
      if (typeof Map !== 'function' || // $FlowIssue Flow incorrectly thinks Map has no prototype
      Map.prototype == null || typeof Map.prototype.forEach !== 'function' || typeof Set !== 'function' || // $FlowIssue Flow incorrectly thinks Set has no prototype
      Set.prototype == null || typeof Set.prototype.clear !== 'function' || typeof Set.prototype.forEach !== 'function') {
        warningWithoutStack$1(false, 'React depends on Map and Set built-in types. Make sure that you load a ' + 'polyfill in older browsers. https://fb.me/react-polyfills');
      }

      topLevelUpdateWarnings = function (container) {
        if (container._reactRootContainer && container.nodeType !== COMMENT_NODE) {
          var hostInstance = findHostInstanceWithNoPortals(container._reactRootContainer._internalRoot.current);

          if (hostInstance) {
            !(hostInstance.parentNode === container) ? warningWithoutStack$1(false, 'render(...): It looks like the React-rendered content of this ' + 'container was removed without using React. This is not ' + 'supported and will cause errors. Instead, call ' + 'ReactDOM.unmountComponentAtNode to empty a container.') : void 0;
          }
        }

        var isRootRenderedBySomeReact = !!container._reactRootContainer;
        var rootEl = getReactRootElementInContainer(container);
        var hasNonRootReactChild = !!(rootEl && getInstanceFromNode$1(rootEl));
        !(!hasNonRootReactChild || isRootRenderedBySomeReact) ? warningWithoutStack$1(false, 'render(...): Replacing React-rendered children with a new root ' + 'component. If you intended to update the children of this node, ' + 'you should instead have the existing children update their state ' + 'and render the new components instead of calling ReactDOM.render.') : void 0;
        !(container.nodeType !== ELEMENT_NODE || !container.tagName || container.tagName.toUpperCase() !== 'BODY') ? warningWithoutStack$1(false, 'render(): Rendering components directly into document.body is ' + 'discouraged, since its children are often manipulated by third-party ' + 'scripts and browser extensions. This may lead to subtle ' + 'reconciliation issues. Try rendering into a container element created ' + 'for your app.') : void 0;
      };

      warnOnInvalidCallback = function (callback, callerName) {
        !(callback === null || typeof callback === 'function') ? warningWithoutStack$1(false, '%s(...): Expected the last optional `callback` argument to be a ' + 'function. Instead received: %s.', callerName, callback) : void 0;
      };
    }
    setRestoreImplementation(restoreControlledState$$1);

    function ReactBatch(root) {
      var expirationTime = computeUniqueAsyncExpiration();
      this._expirationTime = expirationTime;
      this._root = root;
      this._next = null;
      this._callbacks = null;
      this._didComplete = false;
      this._hasChildren = false;
      this._children = null;
      this._defer = true;
    }

    ReactBatch.prototype.render = function (children) {
      var _this = this;

      (function () {
        if (!_this._defer) {
          {
            throw ReactError(Error("batch.render: Cannot render a batch that already committed."));
          }
        }
      })();

      this._hasChildren = true;
      this._children = children;
      var internalRoot = this._root._internalRoot;
      var expirationTime = this._expirationTime;
      var work = new ReactWork();
      updateContainerAtExpirationTime(children, internalRoot, null, expirationTime, null, work._onCommit);
      return work;
    };

    ReactBatch.prototype.then = function (onComplete) {
      if (this._didComplete) {
        onComplete();
        return;
      }

      var callbacks = this._callbacks;

      if (callbacks === null) {
        callbacks = this._callbacks = [];
      }

      callbacks.push(onComplete);
    };

    ReactBatch.prototype.commit = function () {
      var _this2 = this;

      var internalRoot = this._root._internalRoot;
      var firstBatch = internalRoot.firstBatch;

      (function () {
        if (!(_this2._defer && firstBatch !== null)) {
          {
            throw ReactError(Error("batch.commit: Cannot commit a batch multiple times."));
          }
        }
      })();

      if (!this._hasChildren) {
        // This batch is empty. Return.
        this._next = null;
        this._defer = false;
        return;
      }

      var expirationTime = this._expirationTime; // Ensure this is the first batch in the list.

      if (firstBatch !== this) {
        // This batch is not the earliest batch. We need to move it to the front.
        // Update its expiration time to be the expiration time of the earliest
        // batch, so that we can flush it without flushing the other batches.
        if (this._hasChildren) {
          expirationTime = this._expirationTime = firstBatch._expirationTime; // Rendering this batch again ensures its children will be the final state
          // when we flush (updates are processed in insertion order: last
          // update wins).
          // TODO: This forces a restart. Should we print a warning?

          this.render(this._children);
        } // Remove the batch from the list.


        var previous = null;
        var batch = firstBatch;

        while (batch !== this) {
          previous = batch;
          batch = batch._next;
        }

        (function () {
          if (!(previous !== null)) {
            {
              throw ReactError(Error("batch.commit: Cannot commit a batch multiple times."));
            }
          }
        })();

        previous._next = batch._next; // Add it to the front.

        this._next = firstBatch;
        firstBatch = internalRoot.firstBatch = this;
      } // Synchronously flush all the work up to this batch's expiration time.


      this._defer = false;
      flushRoot(internalRoot, expirationTime); // Pop the batch from the list.

      var next = this._next;
      this._next = null;
      firstBatch = internalRoot.firstBatch = next; // Append the next earliest batch's children to the update queue.

      if (firstBatch !== null && firstBatch._hasChildren) {
        firstBatch.render(firstBatch._children);
      }
    };

    ReactBatch.prototype._onComplete = function () {
      if (this._didComplete) {
        return;
      }

      this._didComplete = true;
      var callbacks = this._callbacks;

      if (callbacks === null) {
        return;
      } // TODO: Error handling.


      for (var i = 0; i < callbacks.length; i++) {
        var _callback = callbacks[i];

        _callback();
      }
    };

    function ReactWork() {
      this._callbacks = null;
      this._didCommit = false; // TODO: Avoid need to bind by replacing callbacks in the update queue with
      // list of Work objects.

      this._onCommit = this._onCommit.bind(this);
    }

    ReactWork.prototype.then = function (onCommit) {
      if (this._didCommit) {
        onCommit();
        return;
      }

      var callbacks = this._callbacks;

      if (callbacks === null) {
        callbacks = this._callbacks = [];
      }

      callbacks.push(onCommit);
    };

    ReactWork.prototype._onCommit = function () {
      if (this._didCommit) {
        return;
      }

      this._didCommit = true;
      var callbacks = this._callbacks;

      if (callbacks === null) {
        return;
      } // TODO: Error handling.


      for (var i = 0; i < callbacks.length; i++) {
        var _callback2 = callbacks[i];

        (function () {
          if (!(typeof _callback2 === 'function')) {
            {
              throw ReactError(Error("Invalid argument passed as callback. Expected a function. Instead received: " + _callback2));
            }
          }
        })();

        _callback2();
      }
    };

    function createRootImpl(container, tag, options) {
      // Tag is either LegacyRoot or Concurrent Root
      var hydrate = options != null && options.hydrate === true;
      var hydrationCallbacks = options != null && options.hydrationOptions || null;
      var root = createContainer(container, tag, hydrate, hydrationCallbacks);
      markContainerAsRoot(root.current, container);

      if (hydrate && tag !== LegacyRoot) {
        var doc = container.nodeType === DOCUMENT_NODE ? container : container.ownerDocument;
        eagerlyTrapReplayableEvents(doc);
      }

      return root;
    }

    function ReactSyncRoot(container, tag, options) {
      this._internalRoot = createRootImpl(container, tag, options);
    }

    function ReactRoot(container, options) {
      this._internalRoot = createRootImpl(container, ConcurrentRoot, options);
    }

    ReactRoot.prototype.render = ReactSyncRoot.prototype.render = function (children, callback) {
      var root = this._internalRoot;
      var work = new ReactWork();
      callback = callback === undefined ? null : callback;
      {
        warnOnInvalidCallback(callback, 'render');
      }

      if (callback !== null) {
        work.then(callback);
      }

      updateContainer(children, root, null, work._onCommit);
      return work;
    };

    ReactRoot.prototype.unmount = ReactSyncRoot.prototype.unmount = function (callback) {
      var root = this._internalRoot;
      var work = new ReactWork();
      callback = callback === undefined ? null : callback;
      {
        warnOnInvalidCallback(callback, 'render');
      }

      if (callback !== null) {
        work.then(callback);
      }

      updateContainer(null, root, null, work._onCommit);
      return work;
    }; // Sync roots cannot create batches. Only concurrent ones.


    ReactRoot.prototype.createBatch = function () {
      var batch = new ReactBatch(this);
      var expirationTime = batch._expirationTime;
      var internalRoot = this._internalRoot;
      var firstBatch = internalRoot.firstBatch;

      if (firstBatch === null) {
        internalRoot.firstBatch = batch;
        batch._next = null;
      } else {
        // Insert sorted by expiration time then insertion order
        var insertAfter = null;
        var insertBefore = firstBatch;

        while (insertBefore !== null && insertBefore._expirationTime >= expirationTime) {
          insertAfter = insertBefore;
          insertBefore = insertBefore._next;
        }

        batch._next = insertBefore;

        if (insertAfter !== null) {
          insertAfter._next = batch;
        }
      }

      return batch;
    };
    /**
     * True if the supplied DOM node is a valid node element.
     *
     * @param {?DOMElement} node The candidate DOM node.
     * @return {boolean} True if the DOM is a valid DOM node.
     * @internal
     */


    function isValidContainer(node) {
      return !!(node && (node.nodeType === ELEMENT_NODE || node.nodeType === DOCUMENT_NODE || node.nodeType === DOCUMENT_FRAGMENT_NODE || node.nodeType === COMMENT_NODE && node.nodeValue === ' react-mount-point-unstable '));
    }

    function getReactRootElementInContainer(container) {
      if (!container) {
        return null;
      }

      if (container.nodeType === DOCUMENT_NODE) {
        return container.documentElement;
      } else {
        return container.firstChild;
      }
    }

    function shouldHydrateDueToLegacyHeuristic(container) {
      var rootElement = getReactRootElementInContainer(container);
      return !!(rootElement && rootElement.nodeType === ELEMENT_NODE && rootElement.hasAttribute(ROOT_ATTRIBUTE_NAME));
    }

    setBatchingImplementation(batchedUpdates$1, discreteUpdates$1, flushDiscreteUpdates, batchedEventUpdates$1);
    var warnedAboutHydrateAPI = false;

    function legacyCreateRootFromDOMContainer(container, forceHydrate) {
      var shouldHydrate = forceHydrate || shouldHydrateDueToLegacyHeuristic(container); // First clear any existing content.

      if (!shouldHydrate) {
        var warned = false;
        var rootSibling;

        while (rootSibling = container.lastChild) {
          {
            if (!warned && rootSibling.nodeType === ELEMENT_NODE && rootSibling.hasAttribute(ROOT_ATTRIBUTE_NAME)) {
              warned = true;
              warningWithoutStack$1(false, 'render(): Target node has markup rendered by React, but there ' + 'are unrelated nodes as well. This is most commonly caused by ' + 'white-space inserted around server-rendered markup.');
            }
          }
          container.removeChild(rootSibling);
        }
      }

      {
        if (shouldHydrate && !forceHydrate && !warnedAboutHydrateAPI) {
          warnedAboutHydrateAPI = true;
          lowPriorityWarningWithoutStack$1(false, 'render(): Calling ReactDOM.render() to hydrate server-rendered markup ' + 'will stop working in React v17. Replace the ReactDOM.render() call ' + 'with ReactDOM.hydrate() if you want React to attach to the server HTML.');
        }
      } // Legacy roots are not batched.

      return new ReactSyncRoot(container, LegacyRoot, shouldHydrate ? {
        hydrate: true
      } : undefined);
    }

    function legacyRenderSubtreeIntoContainer(parentComponent, children, container, forceHydrate, callback) {
      {
        topLevelUpdateWarnings(container);
        warnOnInvalidCallback(callback === undefined ? null : callback, 'render');
      } // TODO: Without `any` type, Flow says "Property cannot be accessed on any
      // member of intersection type." Whyyyyyy.

      var root = container._reactRootContainer;
      var fiberRoot;

      if (!root) {
        // Initial mount
        root = container._reactRootContainer = legacyCreateRootFromDOMContainer(container, forceHydrate);
        fiberRoot = root._internalRoot;

        if (typeof callback === 'function') {
          var originalCallback = callback;

          callback = function () {
            var instance = getPublicRootInstance(fiberRoot);
            originalCallback.call(instance);
          };
        } // Initial mount should not be batched.


        unbatchedUpdates(function () {
          updateContainer(children, fiberRoot, parentComponent, callback);
        });
      } else {
        fiberRoot = root._internalRoot;

        if (typeof callback === 'function') {
          var _originalCallback = callback;

          callback = function () {
            var instance = getPublicRootInstance(fiberRoot);

            _originalCallback.call(instance);
          };
        } // Update


        updateContainer(children, fiberRoot, parentComponent, callback);
      }

      return getPublicRootInstance(fiberRoot);
    }

    function createPortal$$1(children, container) {
      var key = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : null;

      (function () {
        if (!isValidContainer(container)) {
          {
            throw ReactError(Error("Target container is not a DOM element."));
          }
        }
      })(); // TODO: pass ReactDOM portal implementation as third argument


      return createPortal$1(children, container, null, key);
    }

    var ReactDOM = {
      createPortal: createPortal$$1,
      findDOMNode: function (componentOrElement) {
        {
          var owner = ReactCurrentOwner.current;

          if (owner !== null && owner.stateNode !== null) {
            var warnedAboutRefsInRender = owner.stateNode._warnedAboutRefsInRender;
            !warnedAboutRefsInRender ? warningWithoutStack$1(false, '%s is accessing findDOMNode inside its render(). ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', getComponentName(owner.type) || 'A component') : void 0;
            owner.stateNode._warnedAboutRefsInRender = true;
          }
        }

        if (componentOrElement == null) {
          return null;
        }

        if (componentOrElement.nodeType === ELEMENT_NODE) {
          return componentOrElement;
        }

        {
          return findHostInstanceWithWarning(componentOrElement, 'findDOMNode');
        }
        return findHostInstance(componentOrElement);
      },
      hydrate: function (element, container, callback) {
        (function () {
          if (!isValidContainer(container)) {
            {
              throw ReactError(Error("Target container is not a DOM element."));
            }
          }
        })();

        {
          !!container._reactHasBeenPassedToCreateRootDEV ? warningWithoutStack$1(false, 'You are calling ReactDOM.hydrate() on a container that was previously ' + 'passed to ReactDOM.%s(). This is not supported. ' + 'Did you mean to call createRoot(container, {hydrate: true}).render(element)?', enableStableConcurrentModeAPIs ? 'createRoot' : 'unstable_createRoot') : void 0;
        } // TODO: throw or warn if we couldn't hydrate?

        return legacyRenderSubtreeIntoContainer(null, element, container, true, callback);
      },
      render: function (element, container, callback) {
        (function () {
          if (!isValidContainer(container)) {
            {
              throw ReactError(Error("Target container is not a DOM element."));
            }
          }
        })();

        {
          !!container._reactHasBeenPassedToCreateRootDEV ? warningWithoutStack$1(false, 'You are calling ReactDOM.render() on a container that was previously ' + 'passed to ReactDOM.%s(). This is not supported. ' + 'Did you mean to call root.render(element)?', enableStableConcurrentModeAPIs ? 'createRoot' : 'unstable_createRoot') : void 0;
        }
        return legacyRenderSubtreeIntoContainer(null, element, container, false, callback);
      },
      unstable_renderSubtreeIntoContainer: function (parentComponent, element, containerNode, callback) {
        (function () {
          if (!isValidContainer(containerNode)) {
            {
              throw ReactError(Error("Target container is not a DOM element."));
            }
          }
        })();

        (function () {
          if (!(parentComponent != null && has(parentComponent))) {
            {
              throw ReactError(Error("parentComponent must be a valid React Component"));
            }
          }
        })();

        return legacyRenderSubtreeIntoContainer(parentComponent, element, containerNode, false, callback);
      },
      unmountComponentAtNode: function (container) {
        (function () {
          if (!isValidContainer(container)) {
            {
              throw ReactError(Error("unmountComponentAtNode(...): Target container is not a DOM element."));
            }
          }
        })();

        {
          !!container._reactHasBeenPassedToCreateRootDEV ? warningWithoutStack$1(false, 'You are calling ReactDOM.unmountComponentAtNode() on a container that was previously ' + 'passed to ReactDOM.%s(). This is not supported. Did you mean to call root.unmount()?', enableStableConcurrentModeAPIs ? 'createRoot' : 'unstable_createRoot') : void 0;
        }

        if (container._reactRootContainer) {
          {
            var rootEl = getReactRootElementInContainer(container);
            var renderedByDifferentReact = rootEl && !getInstanceFromNode$1(rootEl);
            !!renderedByDifferentReact ? warningWithoutStack$1(false, "unmountComponentAtNode(): The node you're attempting to unmount " + 'was rendered by another copy of React.') : void 0;
          } // Unmount should not be batched.

          unbatchedUpdates(function () {
            legacyRenderSubtreeIntoContainer(null, null, container, false, function () {
              container._reactRootContainer = null;
            });
          }); // If you call unmountComponentAtNode twice in quick succession, you'll
          // get `true` twice. That's probably fine?

          return true;
        } else {
          {
            var _rootEl = getReactRootElementInContainer(container);

            var hasNonRootReactChild = !!(_rootEl && getInstanceFromNode$1(_rootEl)); // Check if the container itself is a React root node.

            var isContainerReactRoot = container.nodeType === ELEMENT_NODE && isValidContainer(container.parentNode) && !!container.parentNode._reactRootContainer;
            !!hasNonRootReactChild ? warningWithoutStack$1(false, "unmountComponentAtNode(): The node you're attempting to unmount " + 'was rendered by React and is not a top-level container. %s', isContainerReactRoot ? 'You may have accidentally passed in a React root node instead ' + 'of its container.' : 'Instead, have the parent component update its state and ' + 'rerender in order to remove this component.') : void 0;
          }
          return false;
        }
      },
      // Temporary alias since we already shipped React 16 RC with it.
      // TODO: remove in React 17.
      unstable_createPortal: function () {
        if (!didWarnAboutUnstableCreatePortal) {
          didWarnAboutUnstableCreatePortal = true;
          lowPriorityWarningWithoutStack$1(false, 'The ReactDOM.unstable_createPortal() alias has been deprecated, ' + 'and will be removed in React 17+. Update your code to use ' + 'ReactDOM.createPortal() instead. It has the exact same API, ' + 'but without the "unstable_" prefix.');
        }

        return createPortal$$1.apply(void 0, arguments);
      },
      unstable_batchedUpdates: batchedUpdates$1,
      // TODO remove this legacy method, unstable_discreteUpdates replaces it
      unstable_interactiveUpdates: function (fn, a, b, c) {
        flushDiscreteUpdates();
        return discreteUpdates$1(fn, a, b, c);
      },
      unstable_discreteUpdates: discreteUpdates$1,
      unstable_flushDiscreteUpdates: flushDiscreteUpdates,
      flushSync: flushSync,
      unstable_createRoot: createRoot,
      unstable_createSyncRoot: createSyncRoot,
      unstable_flushControlled: flushControlled,
      __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED: {
        // Keep in sync with ReactDOMUnstableNativeDependencies.js
        // ReactTestUtils.js, and ReactTestUtilsAct.js. This is an array for better minification.
        Events: [getInstanceFromNode$1, getNodeFromInstance$1, getFiberCurrentPropsFromNode$1, injection.injectEventPluginsByName, eventNameDispatchConfigs, accumulateTwoPhaseDispatches, accumulateDirectDispatches, enqueueStateRestore, restoreStateIfNeeded, dispatchEvent, runEventsInBatch, flushPassiveEffects, IsThisRendererActing]
      }
    };

    function createRoot(container, options) {
      var functionName = enableStableConcurrentModeAPIs ? 'createRoot' : 'unstable_createRoot';

      (function () {
        if (!isValidContainer(container)) {
          {
            throw ReactError(Error(functionName + "(...): Target container is not a DOM element."));
          }
        }
      })();

      warnIfReactDOMContainerInDEV(container);
      return new ReactRoot(container, options);
    }

    function createSyncRoot(container, options) {
      var functionName = enableStableConcurrentModeAPIs ? 'createRoot' : 'unstable_createRoot';

      (function () {
        if (!isValidContainer(container)) {
          {
            throw ReactError(Error(functionName + "(...): Target container is not a DOM element."));
          }
        }
      })();

      warnIfReactDOMContainerInDEV(container);
      return new ReactSyncRoot(container, BatchedRoot, options);
    }

    function warnIfReactDOMContainerInDEV(container) {
      {
        !!container._reactRootContainer ? warningWithoutStack$1(false, 'You are calling ReactDOM.%s() on a container that was previously ' + 'passed to ReactDOM.render(). This is not supported.', enableStableConcurrentModeAPIs ? 'createRoot' : 'unstable_createRoot') : void 0;
        container._reactHasBeenPassedToCreateRootDEV = true;
      }
    }

    if (enableStableConcurrentModeAPIs) {
      ReactDOM.createRoot = createRoot;
      ReactDOM.createSyncRoot = createSyncRoot;
    }

    var foundDevTools = injectIntoDevTools({
      findFiberByHostInstance: getClosestInstanceFromNode,
      bundleType: 1,
      version: ReactVersion,
      rendererPackageName: 'react-dom'
    });
    {
      if (!foundDevTools && canUseDOM && window.top === window.self) {
        // If we're in Chrome or Firefox, provide a download link if not installed.
        if (navigator.userAgent.indexOf('Chrome') > -1 && navigator.userAgent.indexOf('Edge') === -1 || navigator.userAgent.indexOf('Firefox') > -1) {
          var protocol = window.location.protocol; // Don't warn in exotic cases like chrome-extension://.

          if (/^(https?|file):$/.test(protocol)) {
            console.info('%cDownload the React DevTools ' + 'for a better development experience: ' + 'https://fb.me/react-devtools' + (protocol === 'file:' ? '\nYou might need to use a local HTTP server (instead of file://): ' + 'https://fb.me/react-devtools-faq' : ''), 'font-weight:bold');
          }
        }
      }
    }
    var ReactDOM$2 = Object.freeze({
      default: ReactDOM
    });
    var ReactDOM$3 = ReactDOM$2 && ReactDOM || ReactDOM$2; // TODO: decide on the top-level export form.
    // This is hacky but makes it work with both Rollup and Jest.

    var reactDom = ReactDOM$3.default || ReactDOM$3;
    module.exports = reactDom;
  })();
}
},{"react":"../node_modules/react/index.js","object-assign":"../node_modules/object-assign/index.js","scheduler":"../node_modules/scheduler/index.js","prop-types/checkPropTypes":"../node_modules/prop-types/checkPropTypes.js","scheduler/tracing":"../node_modules/scheduler/tracing.js"}],"../node_modules/react-dom/index.js":[function(require,module,exports) {
'use strict';

function checkDCE() {
  /* global __REACT_DEVTOOLS_GLOBAL_HOOK__ */
  if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined' || typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== 'function') {
    return;
  }

  if ("development" !== 'production') {
    // This branch is unreachable because this function is only called
    // in production, but the condition is true only in development.
    // Therefore if the branch is still here, dead code elimination wasn't
    // properly applied.
    // Don't change the message. React DevTools relies on it. Also make sure
    // this message doesn't occur elsewhere in this function, or it will cause
    // a false positive.
    throw new Error('^_^');
  }

  try {
    // Verify that the code above has been dead code eliminated (DCE'd).
    __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
  } catch (err) {
    // DevTools shouldn't crash React, no matter what.
    // We should still report in case we break this code.
    console.error(err);
  }
}

if ("development" === 'production') {
  // DCE check should happen before ReactDOM bundle executes so that
  // DevTools can report bad minification during injection.
  checkDCE();
  module.exports = require('./cjs/react-dom.production.min.js');
} else {
  module.exports = require('./cjs/react-dom.development.js');
}
},{"./cjs/react-dom.development.js":"../node_modules/react-dom/cjs/react-dom.development.js"}],"../node_modules/react-is/cjs/react-is.development.js":[function(require,module,exports) {
/** @license React v16.10.1
 * react-is.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
'use strict';

if ("development" !== "production") {
  (function () {
    'use strict';

    Object.defineProperty(exports, '__esModule', {
      value: true
    }); // The Symbol used to tag the ReactElement-like types. If there is no native Symbol
    // nor polyfill, then a plain number is used for performance.

    var hasSymbol = typeof Symbol === 'function' && Symbol.for;
    var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for('react.element') : 0xeac7;
    var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for('react.portal') : 0xeaca;
    var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for('react.fragment') : 0xeacb;
    var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for('react.strict_mode') : 0xeacc;
    var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for('react.profiler') : 0xead2;
    var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for('react.provider') : 0xeacd;
    var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for('react.context') : 0xeace; // TODO: We don't use AsyncMode or ConcurrentMode anymore. They were temporary
    // (unstable) APIs that have been removed. Can we remove the symbols?

    var REACT_ASYNC_MODE_TYPE = hasSymbol ? Symbol.for('react.async_mode') : 0xeacf;
    var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for('react.concurrent_mode') : 0xeacf;
    var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for('react.forward_ref') : 0xead0;
    var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for('react.suspense') : 0xead1;
    var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for('react.suspense_list') : 0xead8;
    var REACT_MEMO_TYPE = hasSymbol ? Symbol.for('react.memo') : 0xead3;
    var REACT_LAZY_TYPE = hasSymbol ? Symbol.for('react.lazy') : 0xead4;
    var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for('react.fundamental') : 0xead5;
    var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for('react.responder') : 0xead6;
    var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for('react.scope') : 0xead7;

    function isValidElementType(type) {
      return typeof type === 'string' || typeof type === 'function' || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
      type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === 'object' && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE);
    }
    /**
     * Forked from fbjs/warning:
     * https://github.com/facebook/fbjs/blob/e66ba20ad5be433eb54423f2b097d829324d9de6/packages/fbjs/src/__forks__/warning.js
     *
     * Only change is we use console.warn instead of console.error,
     * and do nothing when 'console' is not supported.
     * This really simplifies the code.
     * ---
     * Similar to invariant but only logs a warning if the condition is not met.
     * This can be used to log issues in development environments in critical
     * paths. Removing the logging code for production environments will keep the
     * same logic and follow the same code paths.
     */


    var lowPriorityWarningWithoutStack = function () {};

    {
      var printWarning = function (format) {
        for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
          args[_key - 1] = arguments[_key];
        }

        var argIndex = 0;
        var message = 'Warning: ' + format.replace(/%s/g, function () {
          return args[argIndex++];
        });

        if (typeof console !== 'undefined') {
          console.warn(message);
        }

        try {
          // --- Welcome to debugging React ---
          // This error was thrown as a convenience so that you can use this stack
          // to find the callsite that caused this warning to fire.
          throw new Error(message);
        } catch (x) {}
      };

      lowPriorityWarningWithoutStack = function (condition, format) {
        if (format === undefined) {
          throw new Error('`lowPriorityWarningWithoutStack(condition, format, ...args)` requires a warning ' + 'message argument');
        }

        if (!condition) {
          for (var _len2 = arguments.length, args = new Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
            args[_key2 - 2] = arguments[_key2];
          }

          printWarning.apply(void 0, [format].concat(args));
        }
      };
    }
    var lowPriorityWarningWithoutStack$1 = lowPriorityWarningWithoutStack;

    function typeOf(object) {
      if (typeof object === 'object' && object !== null) {
        var $$typeof = object.$$typeof;

        switch ($$typeof) {
          case REACT_ELEMENT_TYPE:
            var type = object.type;

            switch (type) {
              case REACT_ASYNC_MODE_TYPE:
              case REACT_CONCURRENT_MODE_TYPE:
              case REACT_FRAGMENT_TYPE:
              case REACT_PROFILER_TYPE:
              case REACT_STRICT_MODE_TYPE:
              case REACT_SUSPENSE_TYPE:
                return type;

              default:
                var $$typeofType = type && type.$$typeof;

                switch ($$typeofType) {
                  case REACT_CONTEXT_TYPE:
                  case REACT_FORWARD_REF_TYPE:
                  case REACT_PROVIDER_TYPE:
                    return $$typeofType;

                  default:
                    return $$typeof;
                }

            }

          case REACT_LAZY_TYPE:
          case REACT_MEMO_TYPE:
          case REACT_PORTAL_TYPE:
            return $$typeof;
        }
      }

      return undefined;
    } // AsyncMode is deprecated along with isAsyncMode


    var AsyncMode = REACT_ASYNC_MODE_TYPE;
    var ConcurrentMode = REACT_CONCURRENT_MODE_TYPE;
    var ContextConsumer = REACT_CONTEXT_TYPE;
    var ContextProvider = REACT_PROVIDER_TYPE;
    var Element = REACT_ELEMENT_TYPE;
    var ForwardRef = REACT_FORWARD_REF_TYPE;
    var Fragment = REACT_FRAGMENT_TYPE;
    var Lazy = REACT_LAZY_TYPE;
    var Memo = REACT_MEMO_TYPE;
    var Portal = REACT_PORTAL_TYPE;
    var Profiler = REACT_PROFILER_TYPE;
    var StrictMode = REACT_STRICT_MODE_TYPE;
    var Suspense = REACT_SUSPENSE_TYPE;
    var hasWarnedAboutDeprecatedIsAsyncMode = false; // AsyncMode should be deprecated

    function isAsyncMode(object) {
      {
        if (!hasWarnedAboutDeprecatedIsAsyncMode) {
          hasWarnedAboutDeprecatedIsAsyncMode = true;
          lowPriorityWarningWithoutStack$1(false, 'The ReactIs.isAsyncMode() alias has been deprecated, ' + 'and will be removed in React 17+. Update your code to use ' + 'ReactIs.isConcurrentMode() instead. It has the exact same API.');
        }
      }
      return isConcurrentMode(object) || typeOf(object) === REACT_ASYNC_MODE_TYPE;
    }

    function isConcurrentMode(object) {
      return typeOf(object) === REACT_CONCURRENT_MODE_TYPE;
    }

    function isContextConsumer(object) {
      return typeOf(object) === REACT_CONTEXT_TYPE;
    }

    function isContextProvider(object) {
      return typeOf(object) === REACT_PROVIDER_TYPE;
    }

    function isElement(object) {
      return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
    }

    function isForwardRef(object) {
      return typeOf(object) === REACT_FORWARD_REF_TYPE;
    }

    function isFragment(object) {
      return typeOf(object) === REACT_FRAGMENT_TYPE;
    }

    function isLazy(object) {
      return typeOf(object) === REACT_LAZY_TYPE;
    }

    function isMemo(object) {
      return typeOf(object) === REACT_MEMO_TYPE;
    }

    function isPortal(object) {
      return typeOf(object) === REACT_PORTAL_TYPE;
    }

    function isProfiler(object) {
      return typeOf(object) === REACT_PROFILER_TYPE;
    }

    function isStrictMode(object) {
      return typeOf(object) === REACT_STRICT_MODE_TYPE;
    }

    function isSuspense(object) {
      return typeOf(object) === REACT_SUSPENSE_TYPE;
    }

    exports.typeOf = typeOf;
    exports.AsyncMode = AsyncMode;
    exports.ConcurrentMode = ConcurrentMode;
    exports.ContextConsumer = ContextConsumer;
    exports.ContextProvider = ContextProvider;
    exports.Element = Element;
    exports.ForwardRef = ForwardRef;
    exports.Fragment = Fragment;
    exports.Lazy = Lazy;
    exports.Memo = Memo;
    exports.Portal = Portal;
    exports.Profiler = Profiler;
    exports.StrictMode = StrictMode;
    exports.Suspense = Suspense;
    exports.isValidElementType = isValidElementType;
    exports.isAsyncMode = isAsyncMode;
    exports.isConcurrentMode = isConcurrentMode;
    exports.isContextConsumer = isContextConsumer;
    exports.isContextProvider = isContextProvider;
    exports.isElement = isElement;
    exports.isForwardRef = isForwardRef;
    exports.isFragment = isFragment;
    exports.isLazy = isLazy;
    exports.isMemo = isMemo;
    exports.isPortal = isPortal;
    exports.isProfiler = isProfiler;
    exports.isStrictMode = isStrictMode;
    exports.isSuspense = isSuspense;
  })();
}
},{}],"../node_modules/react-is/index.js":[function(require,module,exports) {
'use strict';

if ("development" === 'production') {
  module.exports = require('./cjs/react-is.production.min.js');
} else {
  module.exports = require('./cjs/react-is.development.js');
}
},{"./cjs/react-is.development.js":"../node_modules/react-is/cjs/react-is.development.js"}],"../node_modules/prop-types/factoryWithTypeCheckers.js":[function(require,module,exports) {
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
'use strict';

var ReactIs = require('react-is');

var assign = require('object-assign');

var ReactPropTypesSecret = require('./lib/ReactPropTypesSecret');

var checkPropTypes = require('./checkPropTypes');

var has = Function.call.bind(Object.prototype.hasOwnProperty);

var printWarning = function () {};

if ("development" !== 'production') {
  printWarning = function (text) {
    var message = 'Warning: ' + text;

    if (typeof console !== 'undefined') {
      console.error(message);
    }

    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };
}

function emptyFunctionThatReturnsNull() {
  return null;
}

module.exports = function (isValidElement, throwOnDirectAccess) {
  /* global Symbol */
  var ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
  var FAUX_ITERATOR_SYMBOL = '@@iterator'; // Before Symbol spec.

  /**
   * Returns the iterator method function contained on the iterable object.
   *
   * Be sure to invoke the function with the iterable as context:
   *
   *     var iteratorFn = getIteratorFn(myIterable);
   *     if (iteratorFn) {
   *       var iterator = iteratorFn.call(myIterable);
   *       ...
   *     }
   *
   * @param {?object} maybeIterable
   * @return {?function}
   */

  function getIteratorFn(maybeIterable) {
    var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);

    if (typeof iteratorFn === 'function') {
      return iteratorFn;
    }
  }
  /**
   * Collection of methods that allow declaration and validation of props that are
   * supplied to React components. Example usage:
   *
   *   var Props = require('ReactPropTypes');
   *   var MyArticle = React.createClass({
   *     propTypes: {
   *       // An optional string prop named "description".
   *       description: Props.string,
   *
   *       // A required enum prop named "category".
   *       category: Props.oneOf(['News','Photos']).isRequired,
   *
   *       // A prop named "dialog" that requires an instance of Dialog.
   *       dialog: Props.instanceOf(Dialog).isRequired
   *     },
   *     render: function() { ... }
   *   });
   *
   * A more formal specification of how these methods are used:
   *
   *   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
   *   decl := ReactPropTypes.{type}(.isRequired)?
   *
   * Each and every declaration produces a function with the same signature. This
   * allows the creation of custom validation functions. For example:
   *
   *  var MyLink = React.createClass({
   *    propTypes: {
   *      // An optional string or URI prop named "href".
   *      href: function(props, propName, componentName) {
   *        var propValue = props[propName];
   *        if (propValue != null && typeof propValue !== 'string' &&
   *            !(propValue instanceof URI)) {
   *          return new Error(
   *            'Expected a string or an URI for ' + propName + ' in ' +
   *            componentName
   *          );
   *        }
   *      }
   *    },
   *    render: function() {...}
   *  });
   *
   * @internal
   */


  var ANONYMOUS = '<<anonymous>>'; // Important!
  // Keep this list in sync with production version in `./factoryWithThrowingShims.js`.

  var ReactPropTypes = {
    array: createPrimitiveTypeChecker('array'),
    bool: createPrimitiveTypeChecker('boolean'),
    func: createPrimitiveTypeChecker('function'),
    number: createPrimitiveTypeChecker('number'),
    object: createPrimitiveTypeChecker('object'),
    string: createPrimitiveTypeChecker('string'),
    symbol: createPrimitiveTypeChecker('symbol'),
    any: createAnyTypeChecker(),
    arrayOf: createArrayOfTypeChecker,
    element: createElementTypeChecker(),
    elementType: createElementTypeTypeChecker(),
    instanceOf: createInstanceTypeChecker,
    node: createNodeChecker(),
    objectOf: createObjectOfTypeChecker,
    oneOf: createEnumTypeChecker,
    oneOfType: createUnionTypeChecker,
    shape: createShapeTypeChecker,
    exact: createStrictShapeTypeChecker
  };
  /**
   * inlined Object.is polyfill to avoid requiring consumers ship their own
   * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
   */

  /*eslint-disable no-self-compare*/

  function is(x, y) {
    // SameValue algorithm
    if (x === y) {
      // Steps 1-5, 7-10
      // Steps 6.b-6.e: +0 != -0
      return x !== 0 || 1 / x === 1 / y;
    } else {
      // Step 6.a: NaN == NaN
      return x !== x && y !== y;
    }
  }
  /*eslint-enable no-self-compare*/

  /**
   * We use an Error-like object for backward compatibility as people may call
   * PropTypes directly and inspect their output. However, we don't use real
   * Errors anymore. We don't inspect their stack anyway, and creating them
   * is prohibitively expensive if they are created too often, such as what
   * happens in oneOfType() for any type before the one that matched.
   */


  function PropTypeError(message) {
    this.message = message;
    this.stack = '';
  } // Make `instanceof Error` still work for returned errors.


  PropTypeError.prototype = Error.prototype;

  function createChainableTypeChecker(validate) {
    if ("development" !== 'production') {
      var manualPropTypeCallCache = {};
      var manualPropTypeWarningCount = 0;
    }

    function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
      componentName = componentName || ANONYMOUS;
      propFullName = propFullName || propName;

      if (secret !== ReactPropTypesSecret) {
        if (throwOnDirectAccess) {
          // New behavior only for users of `prop-types` package
          var err = new Error('Calling PropTypes validators directly is not supported by the `prop-types` package. ' + 'Use `PropTypes.checkPropTypes()` to call them. ' + 'Read more at http://fb.me/use-check-prop-types');
          err.name = 'Invariant Violation';
          throw err;
        } else if ("development" !== 'production' && typeof console !== 'undefined') {
          // Old behavior for people using React.PropTypes
          var cacheKey = componentName + ':' + propName;

          if (!manualPropTypeCallCache[cacheKey] && // Avoid spamming the console because they are often not actionable except for lib authors
          manualPropTypeWarningCount < 3) {
            printWarning('You are manually calling a React.PropTypes validation ' + 'function for the `' + propFullName + '` prop on `' + componentName + '`. This is deprecated ' + 'and will throw in the standalone `prop-types` package. ' + 'You may be seeing this warning due to a third-party PropTypes ' + 'library. See https://fb.me/react-warning-dont-call-proptypes ' + 'for details.');
            manualPropTypeCallCache[cacheKey] = true;
            manualPropTypeWarningCount++;
          }
        }
      }

      if (props[propName] == null) {
        if (isRequired) {
          if (props[propName] === null) {
            return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required ' + ('in `' + componentName + '`, but its value is `null`.'));
          }

          return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required in ' + ('`' + componentName + '`, but its value is `undefined`.'));
        }

        return null;
      } else {
        return validate(props, propName, componentName, location, propFullName);
      }
    }

    var chainedCheckType = checkType.bind(null, false);
    chainedCheckType.isRequired = checkType.bind(null, true);
    return chainedCheckType;
  }

  function createPrimitiveTypeChecker(expectedType) {
    function validate(props, propName, componentName, location, propFullName, secret) {
      var propValue = props[propName];
      var propType = getPropType(propValue);

      if (propType !== expectedType) {
        // `propValue` being instance of, say, date/regexp, pass the 'object'
        // check, but we can offer a more precise error message here rather than
        // 'of type `object`'.
        var preciseType = getPreciseType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + preciseType + '` supplied to `' + componentName + '`, expected ') + ('`' + expectedType + '`.'));
      }

      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function createAnyTypeChecker() {
    return createChainableTypeChecker(emptyFunctionThatReturnsNull);
  }

  function createArrayOfTypeChecker(typeChecker) {
    function validate(props, propName, componentName, location, propFullName) {
      if (typeof typeChecker !== 'function') {
        return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside arrayOf.');
      }

      var propValue = props[propName];

      if (!Array.isArray(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an array.'));
      }

      for (var i = 0; i < propValue.length; i++) {
        var error = typeChecker(propValue, i, componentName, location, propFullName + '[' + i + ']', ReactPropTypesSecret);

        if (error instanceof Error) {
          return error;
        }
      }

      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function createElementTypeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];

      if (!isValidElement(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected a single ReactElement.'));
      }

      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function createElementTypeTypeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];

      if (!ReactIs.isValidElementType(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected a single ReactElement type.'));
      }

      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function createInstanceTypeChecker(expectedClass) {
    function validate(props, propName, componentName, location, propFullName) {
      if (!(props[propName] instanceof expectedClass)) {
        var expectedClassName = expectedClass.name || ANONYMOUS;
        var actualClassName = getClassName(props[propName]);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + actualClassName + '` supplied to `' + componentName + '`, expected ') + ('instance of `' + expectedClassName + '`.'));
      }

      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function createEnumTypeChecker(expectedValues) {
    if (!Array.isArray(expectedValues)) {
      if ("development" !== 'production') {
        if (arguments.length > 1) {
          printWarning('Invalid arguments supplied to oneOf, expected an array, got ' + arguments.length + ' arguments. ' + 'A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z]).');
        } else {
          printWarning('Invalid argument supplied to oneOf, expected an array.');
        }
      }

      return emptyFunctionThatReturnsNull;
    }

    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];

      for (var i = 0; i < expectedValues.length; i++) {
        if (is(propValue, expectedValues[i])) {
          return null;
        }
      }

      var valuesString = JSON.stringify(expectedValues, function replacer(key, value) {
        var type = getPreciseType(value);

        if (type === 'symbol') {
          return String(value);
        }

        return value;
      });
      return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of value `' + String(propValue) + '` ' + ('supplied to `' + componentName + '`, expected one of ' + valuesString + '.'));
    }

    return createChainableTypeChecker(validate);
  }

  function createObjectOfTypeChecker(typeChecker) {
    function validate(props, propName, componentName, location, propFullName) {
      if (typeof typeChecker !== 'function') {
        return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside objectOf.');
      }

      var propValue = props[propName];
      var propType = getPropType(propValue);

      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an object.'));
      }

      for (var key in propValue) {
        if (has(propValue, key)) {
          var error = typeChecker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);

          if (error instanceof Error) {
            return error;
          }
        }
      }

      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function createUnionTypeChecker(arrayOfTypeCheckers) {
    if (!Array.isArray(arrayOfTypeCheckers)) {
      "development" !== 'production' ? printWarning('Invalid argument supplied to oneOfType, expected an instance of array.') : void 0;
      return emptyFunctionThatReturnsNull;
    }

    for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
      var checker = arrayOfTypeCheckers[i];

      if (typeof checker !== 'function') {
        printWarning('Invalid argument supplied to oneOfType. Expected an array of check functions, but ' + 'received ' + getPostfixForTypeWarning(checker) + ' at index ' + i + '.');
        return emptyFunctionThatReturnsNull;
      }
    }

    function validate(props, propName, componentName, location, propFullName) {
      for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
        var checker = arrayOfTypeCheckers[i];

        if (checker(props, propName, componentName, location, propFullName, ReactPropTypesSecret) == null) {
          return null;
        }
      }

      return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`.'));
    }

    return createChainableTypeChecker(validate);
  }

  function createNodeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      if (!isNode(props[propName])) {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`, expected a ReactNode.'));
      }

      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function createShapeTypeChecker(shapeTypes) {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      var propType = getPropType(propValue);

      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
      }

      for (var key in shapeTypes) {
        var checker = shapeTypes[key];

        if (!checker) {
          continue;
        }

        var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);

        if (error) {
          return error;
        }
      }

      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function createStrictShapeTypeChecker(shapeTypes) {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      var propType = getPropType(propValue);

      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
      } // We need to check all keys in case some are required but missing from
      // props.


      var allKeys = assign({}, props[propName], shapeTypes);

      for (var key in allKeys) {
        var checker = shapeTypes[key];

        if (!checker) {
          return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` key `' + key + '` supplied to `' + componentName + '`.' + '\nBad object: ' + JSON.stringify(props[propName], null, '  ') + '\nValid keys: ' + JSON.stringify(Object.keys(shapeTypes), null, '  '));
        }

        var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);

        if (error) {
          return error;
        }
      }

      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function isNode(propValue) {
    switch (typeof propValue) {
      case 'number':
      case 'string':
      case 'undefined':
        return true;

      case 'boolean':
        return !propValue;

      case 'object':
        if (Array.isArray(propValue)) {
          return propValue.every(isNode);
        }

        if (propValue === null || isValidElement(propValue)) {
          return true;
        }

        var iteratorFn = getIteratorFn(propValue);

        if (iteratorFn) {
          var iterator = iteratorFn.call(propValue);
          var step;

          if (iteratorFn !== propValue.entries) {
            while (!(step = iterator.next()).done) {
              if (!isNode(step.value)) {
                return false;
              }
            }
          } else {
            // Iterator will provide entry [k,v] tuples rather than values.
            while (!(step = iterator.next()).done) {
              var entry = step.value;

              if (entry) {
                if (!isNode(entry[1])) {
                  return false;
                }
              }
            }
          }
        } else {
          return false;
        }

        return true;

      default:
        return false;
    }
  }

  function isSymbol(propType, propValue) {
    // Native Symbol.
    if (propType === 'symbol') {
      return true;
    } // falsy value can't be a Symbol


    if (!propValue) {
      return false;
    } // 19.4.3.5 Symbol.prototype[@@toStringTag] === 'Symbol'


    if (propValue['@@toStringTag'] === 'Symbol') {
      return true;
    } // Fallback for non-spec compliant Symbols which are polyfilled.


    if (typeof Symbol === 'function' && propValue instanceof Symbol) {
      return true;
    }

    return false;
  } // Equivalent of `typeof` but with special handling for array and regexp.


  function getPropType(propValue) {
    var propType = typeof propValue;

    if (Array.isArray(propValue)) {
      return 'array';
    }

    if (propValue instanceof RegExp) {
      // Old webkits (at least until Android 4.0) return 'function' rather than
      // 'object' for typeof a RegExp. We'll normalize this here so that /bla/
      // passes PropTypes.object.
      return 'object';
    }

    if (isSymbol(propType, propValue)) {
      return 'symbol';
    }

    return propType;
  } // This handles more types than `getPropType`. Only used for error messages.
  // See `createPrimitiveTypeChecker`.


  function getPreciseType(propValue) {
    if (typeof propValue === 'undefined' || propValue === null) {
      return '' + propValue;
    }

    var propType = getPropType(propValue);

    if (propType === 'object') {
      if (propValue instanceof Date) {
        return 'date';
      } else if (propValue instanceof RegExp) {
        return 'regexp';
      }
    }

    return propType;
  } // Returns a string that is postfixed to a warning about an invalid type.
  // For example, "undefined" or "of type array"


  function getPostfixForTypeWarning(value) {
    var type = getPreciseType(value);

    switch (type) {
      case 'array':
      case 'object':
        return 'an ' + type;

      case 'boolean':
      case 'date':
      case 'regexp':
        return 'a ' + type;

      default:
        return type;
    }
  } // Returns class name of the object, if any.


  function getClassName(propValue) {
    if (!propValue.constructor || !propValue.constructor.name) {
      return ANONYMOUS;
    }

    return propValue.constructor.name;
  }

  ReactPropTypes.checkPropTypes = checkPropTypes;
  ReactPropTypes.resetWarningCache = checkPropTypes.resetWarningCache;
  ReactPropTypes.PropTypes = ReactPropTypes;
  return ReactPropTypes;
};
},{"react-is":"../node_modules/react-is/index.js","object-assign":"../node_modules/object-assign/index.js","./lib/ReactPropTypesSecret":"../node_modules/prop-types/lib/ReactPropTypesSecret.js","./checkPropTypes":"../node_modules/prop-types/checkPropTypes.js"}],"../node_modules/prop-types/index.js":[function(require,module,exports) {
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
if ("development" !== 'production') {
  var ReactIs = require('react-is'); // By explicitly using `prop-types` you are opting into new development behavior.
  // http://fb.me/prop-types-in-prod


  var throwOnDirectAccess = true;
  module.exports = require('./factoryWithTypeCheckers')(ReactIs.isElement, throwOnDirectAccess);
} else {
  // By explicitly using `prop-types` you are opting into new production behavior.
  // http://fb.me/prop-types-in-prod
  module.exports = require('./factoryWithThrowingShims')();
}
},{"react-is":"../node_modules/react-is/index.js","./factoryWithTypeCheckers":"../node_modules/prop-types/factoryWithTypeCheckers.js"}],"../node_modules/react-redux/es/components/Context.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = exports.ReactReduxContext = void 0;

var _react = _interopRequireDefault(require("react"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var ReactReduxContext = _react.default.createContext(null);

exports.ReactReduxContext = ReactReduxContext;
var _default = ReactReduxContext;
exports.default = _default;
},{"react":"../node_modules/react/index.js"}],"../node_modules/react-redux/es/utils/batch.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getBatch = exports.setBatch = void 0;

// Default to a dummy "batch" implementation that just runs the callback
function defaultNoopBatch(callback) {
  callback();
}

var batch = defaultNoopBatch; // Allow injecting another batching function later

var setBatch = function setBatch(newBatch) {
  return batch = newBatch;
}; // Supply a getter just to skip dealing with ESM bindings


exports.setBatch = setBatch;

var getBatch = function getBatch() {
  return batch;
};

exports.getBatch = getBatch;
},{}],"../node_modules/react-redux/es/utils/Subscription.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _batch = require("./batch");

// encapsulates the subscription logic for connecting a component to the redux store, as
// well as nesting subscriptions of descendant components, so that we can ensure the
// ancestor components re-render before descendants
var CLEARED = null;
var nullListeners = {
  notify: function notify() {}
};

function createListenerCollection() {
  var batch = (0, _batch.getBatch)(); // the current/next pattern is copied from redux's createStore code.
  // TODO: refactor+expose that code to be reusable here?

  var current = [];
  var next = [];
  return {
    clear: function clear() {
      next = CLEARED;
      current = CLEARED;
    },
    notify: function notify() {
      var listeners = current = next;
      batch(function () {
        for (var i = 0; i < listeners.length; i++) {
          listeners[i]();
        }
      });
    },
    get: function get() {
      return next;
    },
    subscribe: function subscribe(listener) {
      var isSubscribed = true;
      if (next === current) next = current.slice();
      next.push(listener);
      return function unsubscribe() {
        if (!isSubscribed || current === CLEARED) return;
        isSubscribed = false;
        if (next === current) next = current.slice();
        next.splice(next.indexOf(listener), 1);
      };
    }
  };
}

var Subscription =
/*#__PURE__*/
function () {
  function Subscription(store, parentSub) {
    this.store = store;
    this.parentSub = parentSub;
    this.unsubscribe = null;
    this.listeners = nullListeners;
    this.handleChangeWrapper = this.handleChangeWrapper.bind(this);
  }

  var _proto = Subscription.prototype;

  _proto.addNestedSub = function addNestedSub(listener) {
    this.trySubscribe();
    return this.listeners.subscribe(listener);
  };

  _proto.notifyNestedSubs = function notifyNestedSubs() {
    this.listeners.notify();
  };

  _proto.handleChangeWrapper = function handleChangeWrapper() {
    if (this.onStateChange) {
      this.onStateChange();
    }
  };

  _proto.isSubscribed = function isSubscribed() {
    return Boolean(this.unsubscribe);
  };

  _proto.trySubscribe = function trySubscribe() {
    if (!this.unsubscribe) {
      this.unsubscribe = this.parentSub ? this.parentSub.addNestedSub(this.handleChangeWrapper) : this.store.subscribe(this.handleChangeWrapper);
      this.listeners = createListenerCollection();
    }
  };

  _proto.tryUnsubscribe = function tryUnsubscribe() {
    if (this.unsubscribe) {
      this.unsubscribe();
      this.unsubscribe = null;
      this.listeners.clear();
      this.listeners = nullListeners;
    }
  };

  return Subscription;
}();

exports.default = Subscription;
},{"./batch":"../node_modules/react-redux/es/utils/batch.js"}],"../node_modules/react-redux/es/components/Provider.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _react = _interopRequireWildcard(require("react"));

var _propTypes = _interopRequireDefault(require("prop-types"));

var _Context = require("./Context");

var _Subscription = _interopRequireDefault(require("../utils/Subscription"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function () { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; if (obj != null) { var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

function Provider(_ref) {
  var store = _ref.store,
      context = _ref.context,
      children = _ref.children;
  var contextValue = (0, _react.useMemo)(function () {
    var subscription = new _Subscription.default(store);
    subscription.onStateChange = subscription.notifyNestedSubs;
    return {
      store: store,
      subscription: subscription
    };
  }, [store]);
  var previousState = (0, _react.useMemo)(function () {
    return store.getState();
  }, [store]);
  (0, _react.useEffect)(function () {
    var subscription = contextValue.subscription;
    subscription.trySubscribe();

    if (previousState !== store.getState()) {
      subscription.notifyNestedSubs();
    }

    return function () {
      subscription.tryUnsubscribe();
      subscription.onStateChange = null;
    };
  }, [contextValue, previousState]);
  var Context = context || _Context.ReactReduxContext;
  return _react.default.createElement(Context.Provider, {
    value: contextValue
  }, children);
}

Provider.propTypes = {
  store: _propTypes.default.shape({
    subscribe: _propTypes.default.func.isRequired,
    dispatch: _propTypes.default.func.isRequired,
    getState: _propTypes.default.func.isRequired
  }),
  context: _propTypes.default.object,
  children: _propTypes.default.any
};
var _default = Provider;
exports.default = _default;
},{"react":"../node_modules/react/index.js","prop-types":"../node_modules/prop-types/index.js","./Context":"../node_modules/react-redux/es/components/Context.js","../utils/Subscription":"../node_modules/react-redux/es/utils/Subscription.js"}],"../node_modules/@babel/runtime/helpers/esm/extends.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = _extends;

function _extends() {
  exports.default = _extends = Object.assign || function (target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];

      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }

    return target;
  };

  return _extends.apply(this, arguments);
}
},{}],"../node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = _objectWithoutPropertiesLoose;

function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null) return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;

  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0) continue;
    target[key] = source[key];
  }

  return target;
}
},{}],"../node_modules/hoist-non-react-statics/dist/hoist-non-react-statics.cjs.js":[function(require,module,exports) {
'use strict';

/**
 * Copyright 2015, Yahoo! Inc.
 * Copyrights licensed under the New BSD License. See the accompanying LICENSE file for terms.
 */
var ReactIs = require('react-is');
var REACT_STATICS = {
    childContextTypes: true,
    contextType: true,
    contextTypes: true,
    defaultProps: true,
    displayName: true,
    getDefaultProps: true,
    getDerivedStateFromError: true,
    getDerivedStateFromProps: true,
    mixins: true,
    propTypes: true,
    type: true
};

var KNOWN_STATICS = {
    name: true,
    length: true,
    prototype: true,
    caller: true,
    callee: true,
    arguments: true,
    arity: true
};

var FORWARD_REF_STATICS = {
    '$$typeof': true,
    render: true,
    defaultProps: true,
    displayName: true,
    propTypes: true
};

var MEMO_STATICS = {
    '$$typeof': true,
    compare: true,
    defaultProps: true,
    displayName: true,
    propTypes: true,
    type: true
};

var TYPE_STATICS = {};
TYPE_STATICS[ReactIs.ForwardRef] = FORWARD_REF_STATICS;

function getStatics(component) {
    if (ReactIs.isMemo(component)) {
        return MEMO_STATICS;
    }
    return TYPE_STATICS[component['$$typeof']] || REACT_STATICS;
}

var defineProperty = Object.defineProperty;
var getOwnPropertyNames = Object.getOwnPropertyNames;
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
var getPrototypeOf = Object.getPrototypeOf;
var objectPrototype = Object.prototype;

function hoistNonReactStatics(targetComponent, sourceComponent, blacklist) {
    if (typeof sourceComponent !== 'string') {
        // don't hoist over string (html) components

        if (objectPrototype) {
            var inheritedComponent = getPrototypeOf(sourceComponent);
            if (inheritedComponent && inheritedComponent !== objectPrototype) {
                hoistNonReactStatics(targetComponent, inheritedComponent, blacklist);
            }
        }

        var keys = getOwnPropertyNames(sourceComponent);

        if (getOwnPropertySymbols) {
            keys = keys.concat(getOwnPropertySymbols(sourceComponent));
        }

        var targetStatics = getStatics(targetComponent);
        var sourceStatics = getStatics(sourceComponent);

        for (var i = 0; i < keys.length; ++i) {
            var key = keys[i];
            if (!KNOWN_STATICS[key] && !(blacklist && blacklist[key]) && !(sourceStatics && sourceStatics[key]) && !(targetStatics && targetStatics[key])) {
                var descriptor = getOwnPropertyDescriptor(sourceComponent, key);
                try {
                    // Avoid failures from read-only properties
                    defineProperty(targetComponent, key, descriptor);
                } catch (e) {}
            }
        }

        return targetComponent;
    }

    return targetComponent;
}

module.exports = hoistNonReactStatics;

},{"react-is":"../node_modules/react-is/index.js"}],"../node_modules/invariant/browser.js":[function(require,module,exports) {
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
'use strict';
/**
 * Use invariant() to assert state which your program assumes to be true.
 *
 * Provide sprintf-style format (only %s is supported) and arguments
 * to provide information about what broke and what you were
 * expecting.
 *
 * The invariant message will be stripped in production, but the invariant
 * will remain to ensure logic does not differ in production.
 */

var invariant = function (condition, format, a, b, c, d, e, f) {
  if ("development" !== 'production') {
    if (format === undefined) {
      throw new Error('invariant requires an error message argument');
    }
  }

  if (!condition) {
    var error;

    if (format === undefined) {
      error = new Error('Minified exception occurred; use the non-minified dev environment ' + 'for the full error message and additional helpful warnings.');
    } else {
      var args = [a, b, c, d, e, f];
      var argIndex = 0;
      error = new Error(format.replace(/%s/g, function () {
        return args[argIndex++];
      }));
      error.name = 'Invariant Violation';
    }

    error.framesToPop = 1; // we don't care about invariant's own frame

    throw error;
  }
};

module.exports = invariant;
},{}],"../node_modules/react-redux/es/components/connectAdvanced.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = connectAdvanced;

var _extends2 = _interopRequireDefault(require("@babel/runtime/helpers/esm/extends"));

var _objectWithoutPropertiesLoose2 = _interopRequireDefault(require("@babel/runtime/helpers/esm/objectWithoutPropertiesLoose"));

var _hoistNonReactStatics = _interopRequireDefault(require("hoist-non-react-statics"));

var _invariant = _interopRequireDefault(require("invariant"));

var _react = _interopRequireWildcard(require("react"));

var _reactIs = require("react-is");

var _Subscription = _interopRequireDefault(require("../utils/Subscription"));

var _Context = require("./Context");

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function () { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; if (obj != null) { var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// Define some constant arrays just to avoid re-creating these
var EMPTY_ARRAY = [];
var NO_SUBSCRIPTION_ARRAY = [null, null];

var stringifyComponent = function stringifyComponent(Comp) {
  try {
    return JSON.stringify(Comp);
  } catch (err) {
    return String(Comp);
  }
};

function storeStateUpdatesReducer(state, action) {
  var updateCount = state[1];
  return [action.payload, updateCount + 1];
}

var initStateUpdates = function initStateUpdates() {
  return [null, 0];
}; // React currently throws a warning when using useLayoutEffect on the server.
// To get around it, we can conditionally useEffect on the server (no-op) and
// useLayoutEffect in the browser. We need useLayoutEffect because we want
// `connect` to perform sync updates to a ref to save the latest props after
// a render is actually committed to the DOM.


var useIsomorphicLayoutEffect = typeof window !== 'undefined' && typeof window.document !== 'undefined' && typeof window.document.createElement !== 'undefined' ? _react.useLayoutEffect : _react.useEffect;

function connectAdvanced(
/*
  selectorFactory is a func that is responsible for returning the selector function used to
  compute new props from state, props, and dispatch. For example:
     export default connectAdvanced((dispatch, options) => (state, props) => ({
      thing: state.things[props.thingId],
      saveThing: fields => dispatch(actionCreators.saveThing(props.thingId, fields)),
    }))(YourComponent)
   Access to dispatch is provided to the factory so selectorFactories can bind actionCreators
  outside of their selector as an optimization. Options passed to connectAdvanced are passed to
  the selectorFactory, along with displayName and WrappedComponent, as the second argument.
   Note that selectorFactory is responsible for all caching/memoization of inbound and outbound
  props. Do not use connectAdvanced directly without memoizing results between calls to your
  selector, otherwise the Connect component will re-render on every state or props change.
*/
selectorFactory, // options object:
_ref) {
  if (_ref === void 0) {
    _ref = {};
  }

  var _ref2 = _ref,
      _ref2$getDisplayName = _ref2.getDisplayName,
      getDisplayName = _ref2$getDisplayName === void 0 ? function (name) {
    return "ConnectAdvanced(" + name + ")";
  } : _ref2$getDisplayName,
      _ref2$methodName = _ref2.methodName,
      methodName = _ref2$methodName === void 0 ? 'connectAdvanced' : _ref2$methodName,
      _ref2$renderCountProp = _ref2.renderCountProp,
      renderCountProp = _ref2$renderCountProp === void 0 ? undefined : _ref2$renderCountProp,
      _ref2$shouldHandleSta = _ref2.shouldHandleStateChanges,
      shouldHandleStateChanges = _ref2$shouldHandleSta === void 0 ? true : _ref2$shouldHandleSta,
      _ref2$storeKey = _ref2.storeKey,
      storeKey = _ref2$storeKey === void 0 ? 'store' : _ref2$storeKey,
      _ref2$withRef = _ref2.withRef,
      withRef = _ref2$withRef === void 0 ? false : _ref2$withRef,
      _ref2$forwardRef = _ref2.forwardRef,
      forwardRef = _ref2$forwardRef === void 0 ? false : _ref2$forwardRef,
      _ref2$context = _ref2.context,
      context = _ref2$context === void 0 ? _Context.ReactReduxContext : _ref2$context,
      connectOptions = (0, _objectWithoutPropertiesLoose2.default)(_ref2, ["getDisplayName", "methodName", "renderCountProp", "shouldHandleStateChanges", "storeKey", "withRef", "forwardRef", "context"]);
  (0, _invariant.default)(renderCountProp === undefined, "renderCountProp is removed. render counting is built into the latest React Dev Tools profiling extension");
  (0, _invariant.default)(!withRef, 'withRef is removed. To access the wrapped instance, use a ref on the connected component');
  var customStoreWarningMessage = 'To use a custom Redux store for specific components, create a custom React context with ' + "React.createContext(), and pass the context object to React Redux's Provider and specific components" + ' like: <Provider context={MyContext}><ConnectedComponent context={MyContext} /></Provider>. ' + 'You may also pass a {context : MyContext} option to connect';
  (0, _invariant.default)(storeKey === 'store', 'storeKey has been removed and does not do anything. ' + customStoreWarningMessage);
  var Context = context;
  return function wrapWithConnect(WrappedComponent) {
    if ("development" !== 'production') {
      (0, _invariant.default)((0, _reactIs.isValidElementType)(WrappedComponent), "You must pass a component to the function returned by " + (methodName + ". Instead received " + stringifyComponent(WrappedComponent)));
    }

    var wrappedComponentName = WrappedComponent.displayName || WrappedComponent.name || 'Component';
    var displayName = getDisplayName(wrappedComponentName);
    var selectorFactoryOptions = (0, _extends2.default)({}, connectOptions, {
      getDisplayName: getDisplayName,
      methodName: methodName,
      renderCountProp: renderCountProp,
      shouldHandleStateChanges: shouldHandleStateChanges,
      storeKey: storeKey,
      displayName: displayName,
      wrappedComponentName: wrappedComponentName,
      WrappedComponent: WrappedComponent
    });
    var pure = connectOptions.pure;

    function createChildSelector(store) {
      return selectorFactory(store.dispatch, selectorFactoryOptions);
    } // If we aren't running in "pure" mode, we don't want to memoize values.
    // To avoid conditionally calling hooks, we fall back to a tiny wrapper
    // that just executes the given callback immediately.


    var usePureOnlyMemo = pure ? _react.useMemo : function (callback) {
      return callback();
    };

    function ConnectFunction(props) {
      var _useMemo = (0, _react.useMemo)(function () {
        // Distinguish between actual "data" props that were passed to the wrapper component,
        // and values needed to control behavior (forwarded refs, alternate context instances).
        // To maintain the wrapperProps object reference, memoize this destructuring.
        var forwardedRef = props.forwardedRef,
            wrapperProps = (0, _objectWithoutPropertiesLoose2.default)(props, ["forwardedRef"]);
        return [props.context, forwardedRef, wrapperProps];
      }, [props]),
          propsContext = _useMemo[0],
          forwardedRef = _useMemo[1],
          wrapperProps = _useMemo[2];

      var ContextToUse = (0, _react.useMemo)(function () {
        // Users may optionally pass in a custom context instance to use instead of our ReactReduxContext.
        // Memoize the check that determines which context instance we should use.
        return propsContext && propsContext.Consumer && (0, _reactIs.isContextConsumer)(_react.default.createElement(propsContext.Consumer, null)) ? propsContext : Context;
      }, [propsContext, Context]); // Retrieve the store and ancestor subscription via context, if available

      var contextValue = (0, _react.useContext)(ContextToUse); // The store _must_ exist as either a prop or in context

      var didStoreComeFromProps = Boolean(props.store);
      var didStoreComeFromContext = Boolean(contextValue) && Boolean(contextValue.store);
      (0, _invariant.default)(didStoreComeFromProps || didStoreComeFromContext, "Could not find \"store\" in the context of " + ("\"" + displayName + "\". Either wrap the root component in a <Provider>, ") + "or pass a custom React context provider to <Provider> and the corresponding " + ("React context consumer to " + displayName + " in connect options."));
      var store = props.store || contextValue.store;
      var childPropsSelector = (0, _react.useMemo)(function () {
        // The child props selector needs the store reference as an input.
        // Re-create this selector whenever the store changes.
        return createChildSelector(store);
      }, [store]);

      var _useMemo2 = (0, _react.useMemo)(function () {
        if (!shouldHandleStateChanges) return NO_SUBSCRIPTION_ARRAY; // This Subscription's source should match where store came from: props vs. context. A component
        // connected to the store via props shouldn't use subscription from context, or vice versa.

        var subscription = new _Subscription.default(store, didStoreComeFromProps ? null : contextValue.subscription); // `notifyNestedSubs` is duplicated to handle the case where the component is unmounted in
        // the middle of the notification loop, where `subscription` will then be null. This can
        // probably be avoided if Subscription's listeners logic is changed to not call listeners
        // that have been unsubscribed in the  middle of the notification loop.

        var notifyNestedSubs = subscription.notifyNestedSubs.bind(subscription);
        return [subscription, notifyNestedSubs];
      }, [store, didStoreComeFromProps, contextValue]),
          subscription = _useMemo2[0],
          notifyNestedSubs = _useMemo2[1]; // Determine what {store, subscription} value should be put into nested context, if necessary,
      // and memoize that value to avoid unnecessary context updates.


      var overriddenContextValue = (0, _react.useMemo)(function () {
        if (didStoreComeFromProps) {
          // This component is directly subscribed to a store from props.
          // We don't want descendants reading from this store - pass down whatever
          // the existing context value is from the nearest connected ancestor.
          return contextValue;
        } // Otherwise, put this component's subscription instance into context, so that
        // connected descendants won't update until after this component is done


        return (0, _extends2.default)({}, contextValue, {
          subscription: subscription
        });
      }, [didStoreComeFromProps, contextValue, subscription]); // We need to force this wrapper component to re-render whenever a Redux store update
      // causes a change to the calculated child component props (or we caught an error in mapState)

      var _useReducer = (0, _react.useReducer)(storeStateUpdatesReducer, EMPTY_ARRAY, initStateUpdates),
          _useReducer$ = _useReducer[0],
          previousStateUpdateResult = _useReducer$[0],
          forceComponentUpdateDispatch = _useReducer[1]; // Propagate any mapState/mapDispatch errors upwards


      if (previousStateUpdateResult && previousStateUpdateResult.error) {
        throw previousStateUpdateResult.error;
      } // Set up refs to coordinate values between the subscription effect and the render logic


      var lastChildProps = (0, _react.useRef)();
      var lastWrapperProps = (0, _react.useRef)(wrapperProps);
      var childPropsFromStoreUpdate = (0, _react.useRef)();
      var renderIsScheduled = (0, _react.useRef)(false);
      var actualChildProps = usePureOnlyMemo(function () {
        // Tricky logic here:
        // - This render may have been triggered by a Redux store update that produced new child props
        // - However, we may have gotten new wrapper props after that
        // If we have new child props, and the same wrapper props, we know we should use the new child props as-is.
        // But, if we have new wrapper props, those might change the child props, so we have to recalculate things.
        // So, we'll use the child props from store update only if the wrapper props are the same as last time.
        if (childPropsFromStoreUpdate.current && wrapperProps === lastWrapperProps.current) {
          return childPropsFromStoreUpdate.current;
        } // TODO We're reading the store directly in render() here. Bad idea?
        // This will likely cause Bad Things (TM) to happen in Concurrent Mode.
        // Note that we do this because on renders _not_ caused by store updates, we need the latest store state
        // to determine what the child props should be.


        return childPropsSelector(store.getState(), wrapperProps);
      }, [store, previousStateUpdateResult, wrapperProps]); // We need this to execute synchronously every time we re-render. However, React warns
      // about useLayoutEffect in SSR, so we try to detect environment and fall back to
      // just useEffect instead to avoid the warning, since neither will run anyway.

      useIsomorphicLayoutEffect(function () {
        // We want to capture the wrapper props and child props we used for later comparisons
        lastWrapperProps.current = wrapperProps;
        lastChildProps.current = actualChildProps;
        renderIsScheduled.current = false; // If the render was from a store update, clear out that reference and cascade the subscriber update

        if (childPropsFromStoreUpdate.current) {
          childPropsFromStoreUpdate.current = null;
          notifyNestedSubs();
        }
      }); // Our re-subscribe logic only runs when the store/subscription setup changes

      useIsomorphicLayoutEffect(function () {
        // If we're not subscribed to the store, nothing to do here
        if (!shouldHandleStateChanges) return; // Capture values for checking if and when this component unmounts

        var didUnsubscribe = false;
        var lastThrownError = null; // We'll run this callback every time a store subscription update propagates to this component

        var checkForUpdates = function checkForUpdates() {
          if (didUnsubscribe) {
            // Don't run stale listeners.
            // Redux doesn't guarantee unsubscriptions happen until next dispatch.
            return;
          }

          var latestStoreState = store.getState();
          var newChildProps, error;

          try {
            // Actually run the selector with the most recent store state and wrapper props
            // to determine what the child props should be
            newChildProps = childPropsSelector(latestStoreState, lastWrapperProps.current);
          } catch (e) {
            error = e;
            lastThrownError = e;
          }

          if (!error) {
            lastThrownError = null;
          } // If the child props haven't changed, nothing to do here - cascade the subscription update


          if (newChildProps === lastChildProps.current) {
            if (!renderIsScheduled.current) {
              notifyNestedSubs();
            }
          } else {
            // Save references to the new child props.  Note that we track the "child props from store update"
            // as a ref instead of a useState/useReducer because we need a way to determine if that value has
            // been processed.  If this went into useState/useReducer, we couldn't clear out the value without
            // forcing another re-render, which we don't want.
            lastChildProps.current = newChildProps;
            childPropsFromStoreUpdate.current = newChildProps;
            renderIsScheduled.current = true; // If the child props _did_ change (or we caught an error), this wrapper component needs to re-render

            forceComponentUpdateDispatch({
              type: 'STORE_UPDATED',
              payload: {
                latestStoreState: latestStoreState,
                error: error
              }
            });
          }
        }; // Actually subscribe to the nearest connected ancestor (or store)


        subscription.onStateChange = checkForUpdates;
        subscription.trySubscribe(); // Pull data from the store after first render in case the store has
        // changed since we began.

        checkForUpdates();

        var unsubscribeWrapper = function unsubscribeWrapper() {
          didUnsubscribe = true;
          subscription.tryUnsubscribe();
          subscription.onStateChange = null;

          if (lastThrownError) {
            // It's possible that we caught an error due to a bad mapState function, but the
            // parent re-rendered without this component and we're about to unmount.
            // This shouldn't happen as long as we do top-down subscriptions correctly, but
            // if we ever do those wrong, this throw will surface the error in our tests.
            // In that case, throw the error from here so it doesn't get lost.
            throw lastThrownError;
          }
        };

        return unsubscribeWrapper;
      }, [store, subscription, childPropsSelector]); // Now that all that's done, we can finally try to actually render the child component.
      // We memoize the elements for the rendered child component as an optimization.

      var renderedWrappedComponent = (0, _react.useMemo)(function () {
        return _react.default.createElement(WrappedComponent, (0, _extends2.default)({}, actualChildProps, {
          ref: forwardedRef
        }));
      }, [forwardedRef, WrappedComponent, actualChildProps]); // If React sees the exact same element reference as last time, it bails out of re-rendering
      // that child, same as if it was wrapped in React.memo() or returned false from shouldComponentUpdate.

      var renderedChild = (0, _react.useMemo)(function () {
        if (shouldHandleStateChanges) {
          // If this component is subscribed to store updates, we need to pass its own
          // subscription instance down to our descendants. That means rendering the same
          // Context instance, and putting a different value into the context.
          return _react.default.createElement(ContextToUse.Provider, {
            value: overriddenContextValue
          }, renderedWrappedComponent);
        }

        return renderedWrappedComponent;
      }, [ContextToUse, renderedWrappedComponent, overriddenContextValue]);
      return renderedChild;
    } // If we're in "pure" mode, ensure our wrapper component only re-renders when incoming props have changed.


    var Connect = pure ? _react.default.memo(ConnectFunction) : ConnectFunction;
    Connect.WrappedComponent = WrappedComponent;
    Connect.displayName = displayName;

    if (forwardRef) {
      var forwarded = _react.default.forwardRef(function forwardConnectRef(props, ref) {
        return _react.default.createElement(Connect, (0, _extends2.default)({}, props, {
          forwardedRef: ref
        }));
      });

      forwarded.displayName = displayName;
      forwarded.WrappedComponent = WrappedComponent;
      return (0, _hoistNonReactStatics.default)(forwarded, WrappedComponent);
    }

    return (0, _hoistNonReactStatics.default)(Connect, WrappedComponent);
  };
}
},{"@babel/runtime/helpers/esm/extends":"../node_modules/@babel/runtime/helpers/esm/extends.js","@babel/runtime/helpers/esm/objectWithoutPropertiesLoose":"../node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js","hoist-non-react-statics":"../node_modules/hoist-non-react-statics/dist/hoist-non-react-statics.cjs.js","invariant":"../node_modules/invariant/browser.js","react":"../node_modules/react/index.js","react-is":"../node_modules/react-is/index.js","../utils/Subscription":"../node_modules/react-redux/es/utils/Subscription.js","./Context":"../node_modules/react-redux/es/components/Context.js"}],"../node_modules/react-redux/es/utils/shallowEqual.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = shallowEqual;
var hasOwn = Object.prototype.hasOwnProperty;

function is(x, y) {
  if (x === y) {
    return x !== 0 || y !== 0 || 1 / x === 1 / y;
  } else {
    return x !== x && y !== y;
  }
}

function shallowEqual(objA, objB) {
  if (is(objA, objB)) return true;

  if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) {
    return false;
  }

  var keysA = Object.keys(objA);
  var keysB = Object.keys(objB);
  if (keysA.length !== keysB.length) return false;

  for (var i = 0; i < keysA.length; i++) {
    if (!hasOwn.call(objB, keysA[i]) || !is(objA[keysA[i]], objB[keysA[i]])) {
      return false;
    }
  }

  return true;
}
},{}],"../node_modules/symbol-observable/es/ponyfill.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = symbolObservablePonyfill;

function symbolObservablePonyfill(root) {
  var result;
  var Symbol = root.Symbol;

  if (typeof Symbol === 'function') {
    if (Symbol.observable) {
      result = Symbol.observable;
    } else {
      result = Symbol('observable');
      Symbol.observable = result;
    }
  } else {
    result = '@@observable';
  }

  return result;
}

;
},{}],"../node_modules/symbol-observable/es/index.js":[function(require,module,exports) {
var global = arguments[3];
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _ponyfill = _interopRequireDefault(require("./ponyfill.js"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/* global window */
var root;

if (typeof self !== 'undefined') {
  root = self;
} else if (typeof window !== 'undefined') {
  root = window;
} else if (typeof global !== 'undefined') {
  root = global;
} else if (typeof module !== 'undefined') {
  root = module;
} else {
  root = Function('return this')();
}

var result = (0, _ponyfill.default)(root);
var _default = result;
exports.default = _default;
},{"./ponyfill.js":"../node_modules/symbol-observable/es/ponyfill.js"}],"../node_modules/redux/es/redux.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.applyMiddleware = applyMiddleware;
exports.bindActionCreators = bindActionCreators;
exports.combineReducers = combineReducers;
exports.compose = compose;
exports.createStore = createStore;
exports.__DO_NOT_USE__ActionTypes = void 0;

var _symbolObservable = _interopRequireDefault(require("symbol-observable"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * These are private action types reserved by Redux.
 * For any unknown actions, you must return the current state.
 * If the current state is undefined, you must return the initial state.
 * Do not reference these action types directly in your code.
 */
var randomString = function randomString() {
  return Math.random().toString(36).substring(7).split('').join('.');
};

var ActionTypes = {
  INIT: "@@redux/INIT" + randomString(),
  REPLACE: "@@redux/REPLACE" + randomString(),
  PROBE_UNKNOWN_ACTION: function PROBE_UNKNOWN_ACTION() {
    return "@@redux/PROBE_UNKNOWN_ACTION" + randomString();
  }
};
/**
 * @param {any} obj The object to inspect.
 * @returns {boolean} True if the argument appears to be a plain object.
 */

exports.__DO_NOT_USE__ActionTypes = ActionTypes;

function isPlainObject(obj) {
  if (typeof obj !== 'object' || obj === null) return false;
  var proto = obj;

  while (Object.getPrototypeOf(proto) !== null) {
    proto = Object.getPrototypeOf(proto);
  }

  return Object.getPrototypeOf(obj) === proto;
}
/**
 * Creates a Redux store that holds the state tree.
 * The only way to change the data in the store is to call `dispatch()` on it.
 *
 * There should only be a single store in your app. To specify how different
 * parts of the state tree respond to actions, you may combine several reducers
 * into a single reducer function by using `combineReducers`.
 *
 * @param {Function} reducer A function that returns the next state tree, given
 * the current state tree and the action to handle.
 *
 * @param {any} [preloadedState] The initial state. You may optionally specify it
 * to hydrate the state from the server in universal apps, or to restore a
 * previously serialized user session.
 * If you use `combineReducers` to produce the root reducer function, this must be
 * an object with the same shape as `combineReducers` keys.
 *
 * @param {Function} [enhancer] The store enhancer. You may optionally specify it
 * to enhance the store with third-party capabilities such as middleware,
 * time travel, persistence, etc. The only store enhancer that ships with Redux
 * is `applyMiddleware()`.
 *
 * @returns {Store} A Redux store that lets you read the state, dispatch actions
 * and subscribe to changes.
 */


function createStore(reducer, preloadedState, enhancer) {
  var _ref2;

  if (typeof preloadedState === 'function' && typeof enhancer === 'function' || typeof enhancer === 'function' && typeof arguments[3] === 'function') {
    throw new Error('It looks like you are passing several store enhancers to ' + 'createStore(). This is not supported. Instead, compose them ' + 'together to a single function.');
  }

  if (typeof preloadedState === 'function' && typeof enhancer === 'undefined') {
    enhancer = preloadedState;
    preloadedState = undefined;
  }

  if (typeof enhancer !== 'undefined') {
    if (typeof enhancer !== 'function') {
      throw new Error('Expected the enhancer to be a function.');
    }

    return enhancer(createStore)(reducer, preloadedState);
  }

  if (typeof reducer !== 'function') {
    throw new Error('Expected the reducer to be a function.');
  }

  var currentReducer = reducer;
  var currentState = preloadedState;
  var currentListeners = [];
  var nextListeners = currentListeners;
  var isDispatching = false;
  /**
   * This makes a shallow copy of currentListeners so we can use
   * nextListeners as a temporary list while dispatching.
   *
   * This prevents any bugs around consumers calling
   * subscribe/unsubscribe in the middle of a dispatch.
   */

  function ensureCanMutateNextListeners() {
    if (nextListeners === currentListeners) {
      nextListeners = currentListeners.slice();
    }
  }
  /**
   * Reads the state tree managed by the store.
   *
   * @returns {any} The current state tree of your application.
   */


  function getState() {
    if (isDispatching) {
      throw new Error('You may not call store.getState() while the reducer is executing. ' + 'The reducer has already received the state as an argument. ' + 'Pass it down from the top reducer instead of reading it from the store.');
    }

    return currentState;
  }
  /**
   * Adds a change listener. It will be called any time an action is dispatched,
   * and some part of the state tree may potentially have changed. You may then
   * call `getState()` to read the current state tree inside the callback.
   *
   * You may call `dispatch()` from a change listener, with the following
   * caveats:
   *
   * 1. The subscriptions are snapshotted just before every `dispatch()` call.
   * If you subscribe or unsubscribe while the listeners are being invoked, this
   * will not have any effect on the `dispatch()` that is currently in progress.
   * However, the next `dispatch()` call, whether nested or not, will use a more
   * recent snapshot of the subscription list.
   *
   * 2. The listener should not expect to see all state changes, as the state
   * might have been updated multiple times during a nested `dispatch()` before
   * the listener is called. It is, however, guaranteed that all subscribers
   * registered before the `dispatch()` started will be called with the latest
   * state by the time it exits.
   *
   * @param {Function} listener A callback to be invoked on every dispatch.
   * @returns {Function} A function to remove this change listener.
   */


  function subscribe(listener) {
    if (typeof listener !== 'function') {
      throw new Error('Expected the listener to be a function.');
    }

    if (isDispatching) {
      throw new Error('You may not call store.subscribe() while the reducer is executing. ' + 'If you would like to be notified after the store has been updated, subscribe from a ' + 'component and invoke store.getState() in the callback to access the latest state. ' + 'See https://redux.js.org/api-reference/store#subscribe(listener) for more details.');
    }

    var isSubscribed = true;
    ensureCanMutateNextListeners();
    nextListeners.push(listener);
    return function unsubscribe() {
      if (!isSubscribed) {
        return;
      }

      if (isDispatching) {
        throw new Error('You may not unsubscribe from a store listener while the reducer is executing. ' + 'See https://redux.js.org/api-reference/store#subscribe(listener) for more details.');
      }

      isSubscribed = false;
      ensureCanMutateNextListeners();
      var index = nextListeners.indexOf(listener);
      nextListeners.splice(index, 1);
    };
  }
  /**
   * Dispatches an action. It is the only way to trigger a state change.
   *
   * The `reducer` function, used to create the store, will be called with the
   * current state tree and the given `action`. Its return value will
   * be considered the **next** state of the tree, and the change listeners
   * will be notified.
   *
   * The base implementation only supports plain object actions. If you want to
   * dispatch a Promise, an Observable, a thunk, or something else, you need to
   * wrap your store creating function into the corresponding middleware. For
   * example, see the documentation for the `redux-thunk` package. Even the
   * middleware will eventually dispatch plain object actions using this method.
   *
   * @param {Object} action A plain object representing “what changed”. It is
   * a good idea to keep actions serializable so you can record and replay user
   * sessions, or use the time travelling `redux-devtools`. An action must have
   * a `type` property which may not be `undefined`. It is a good idea to use
   * string constants for action types.
   *
   * @returns {Object} For convenience, the same action object you dispatched.
   *
   * Note that, if you use a custom middleware, it may wrap `dispatch()` to
   * return something else (for example, a Promise you can await).
   */


  function dispatch(action) {
    if (!isPlainObject(action)) {
      throw new Error('Actions must be plain objects. ' + 'Use custom middleware for async actions.');
    }

    if (typeof action.type === 'undefined') {
      throw new Error('Actions may not have an undefined "type" property. ' + 'Have you misspelled a constant?');
    }

    if (isDispatching) {
      throw new Error('Reducers may not dispatch actions.');
    }

    try {
      isDispatching = true;
      currentState = currentReducer(currentState, action);
    } finally {
      isDispatching = false;
    }

    var listeners = currentListeners = nextListeners;

    for (var i = 0; i < listeners.length; i++) {
      var listener = listeners[i];
      listener();
    }

    return action;
  }
  /**
   * Replaces the reducer currently used by the store to calculate the state.
   *
   * You might need this if your app implements code splitting and you want to
   * load some of the reducers dynamically. You might also need this if you
   * implement a hot reloading mechanism for Redux.
   *
   * @param {Function} nextReducer The reducer for the store to use instead.
   * @returns {void}
   */


  function replaceReducer(nextReducer) {
    if (typeof nextReducer !== 'function') {
      throw new Error('Expected the nextReducer to be a function.');
    }

    currentReducer = nextReducer; // This action has a similiar effect to ActionTypes.INIT.
    // Any reducers that existed in both the new and old rootReducer
    // will receive the previous state. This effectively populates
    // the new state tree with any relevant data from the old one.

    dispatch({
      type: ActionTypes.REPLACE
    });
  }
  /**
   * Interoperability point for observable/reactive libraries.
   * @returns {observable} A minimal observable of state changes.
   * For more information, see the observable proposal:
   * https://github.com/tc39/proposal-observable
   */


  function observable() {
    var _ref;

    var outerSubscribe = subscribe;
    return _ref = {
      /**
       * The minimal observable subscription method.
       * @param {Object} observer Any object that can be used as an observer.
       * The observer object should have a `next` method.
       * @returns {subscription} An object with an `unsubscribe` method that can
       * be used to unsubscribe the observable from the store, and prevent further
       * emission of values from the observable.
       */
      subscribe: function subscribe(observer) {
        if (typeof observer !== 'object' || observer === null) {
          throw new TypeError('Expected the observer to be an object.');
        }

        function observeState() {
          if (observer.next) {
            observer.next(getState());
          }
        }

        observeState();
        var unsubscribe = outerSubscribe(observeState);
        return {
          unsubscribe: unsubscribe
        };
      }
    }, _ref[_symbolObservable.default] = function () {
      return this;
    }, _ref;
  } // When a store is created, an "INIT" action is dispatched so that every
  // reducer returns their initial state. This effectively populates
  // the initial state tree.


  dispatch({
    type: ActionTypes.INIT
  });
  return _ref2 = {
    dispatch: dispatch,
    subscribe: subscribe,
    getState: getState,
    replaceReducer: replaceReducer
  }, _ref2[_symbolObservable.default] = observable, _ref2;
}
/**
 * Prints a warning in the console if it exists.
 *
 * @param {String} message The warning message.
 * @returns {void}
 */


function warning(message) {
  /* eslint-disable no-console */
  if (typeof console !== 'undefined' && typeof console.error === 'function') {
    console.error(message);
  }
  /* eslint-enable no-console */


  try {
    // This error was thrown as a convenience so that if you enable
    // "break on all exceptions" in your console,
    // it would pause the execution at this line.
    throw new Error(message);
  } catch (e) {} // eslint-disable-line no-empty

}

function getUndefinedStateErrorMessage(key, action) {
  var actionType = action && action.type;
  var actionDescription = actionType && "action \"" + String(actionType) + "\"" || 'an action';
  return "Given " + actionDescription + ", reducer \"" + key + "\" returned undefined. " + "To ignore an action, you must explicitly return the previous state. " + "If you want this reducer to hold no value, you can return null instead of undefined.";
}

function getUnexpectedStateShapeWarningMessage(inputState, reducers, action, unexpectedKeyCache) {
  var reducerKeys = Object.keys(reducers);
  var argumentName = action && action.type === ActionTypes.INIT ? 'preloadedState argument passed to createStore' : 'previous state received by the reducer';

  if (reducerKeys.length === 0) {
    return 'Store does not have a valid reducer. Make sure the argument passed ' + 'to combineReducers is an object whose values are reducers.';
  }

  if (!isPlainObject(inputState)) {
    return "The " + argumentName + " has unexpected type of \"" + {}.toString.call(inputState).match(/\s([a-z|A-Z]+)/)[1] + "\". Expected argument to be an object with the following " + ("keys: \"" + reducerKeys.join('", "') + "\"");
  }

  var unexpectedKeys = Object.keys(inputState).filter(function (key) {
    return !reducers.hasOwnProperty(key) && !unexpectedKeyCache[key];
  });
  unexpectedKeys.forEach(function (key) {
    unexpectedKeyCache[key] = true;
  });
  if (action && action.type === ActionTypes.REPLACE) return;

  if (unexpectedKeys.length > 0) {
    return "Unexpected " + (unexpectedKeys.length > 1 ? 'keys' : 'key') + " " + ("\"" + unexpectedKeys.join('", "') + "\" found in " + argumentName + ". ") + "Expected to find one of the known reducer keys instead: " + ("\"" + reducerKeys.join('", "') + "\". Unexpected keys will be ignored.");
  }
}

function assertReducerShape(reducers) {
  Object.keys(reducers).forEach(function (key) {
    var reducer = reducers[key];
    var initialState = reducer(undefined, {
      type: ActionTypes.INIT
    });

    if (typeof initialState === 'undefined') {
      throw new Error("Reducer \"" + key + "\" returned undefined during initialization. " + "If the state passed to the reducer is undefined, you must " + "explicitly return the initial state. The initial state may " + "not be undefined. If you don't want to set a value for this reducer, " + "you can use null instead of undefined.");
    }

    if (typeof reducer(undefined, {
      type: ActionTypes.PROBE_UNKNOWN_ACTION()
    }) === 'undefined') {
      throw new Error("Reducer \"" + key + "\" returned undefined when probed with a random type. " + ("Don't try to handle " + ActionTypes.INIT + " or other actions in \"redux/*\" ") + "namespace. They are considered private. Instead, you must return the " + "current state for any unknown actions, unless it is undefined, " + "in which case you must return the initial state, regardless of the " + "action type. The initial state may not be undefined, but can be null.");
    }
  });
}
/**
 * Turns an object whose values are different reducer functions, into a single
 * reducer function. It will call every child reducer, and gather their results
 * into a single state object, whose keys correspond to the keys of the passed
 * reducer functions.
 *
 * @param {Object} reducers An object whose values correspond to different
 * reducer functions that need to be combined into one. One handy way to obtain
 * it is to use ES6 `import * as reducers` syntax. The reducers may never return
 * undefined for any action. Instead, they should return their initial state
 * if the state passed to them was undefined, and the current state for any
 * unrecognized action.
 *
 * @returns {Function} A reducer function that invokes every reducer inside the
 * passed object, and builds a state object with the same shape.
 */


function combineReducers(reducers) {
  var reducerKeys = Object.keys(reducers);
  var finalReducers = {};

  for (var i = 0; i < reducerKeys.length; i++) {
    var key = reducerKeys[i];

    if ("development" !== 'production') {
      if (typeof reducers[key] === 'undefined') {
        warning("No reducer provided for key \"" + key + "\"");
      }
    }

    if (typeof reducers[key] === 'function') {
      finalReducers[key] = reducers[key];
    }
  }

  var finalReducerKeys = Object.keys(finalReducers); // This is used to make sure we don't warn about the same
  // keys multiple times.

  var unexpectedKeyCache;

  if ("development" !== 'production') {
    unexpectedKeyCache = {};
  }

  var shapeAssertionError;

  try {
    assertReducerShape(finalReducers);
  } catch (e) {
    shapeAssertionError = e;
  }

  return function combination(state, action) {
    if (state === void 0) {
      state = {};
    }

    if (shapeAssertionError) {
      throw shapeAssertionError;
    }

    if ("development" !== 'production') {
      var warningMessage = getUnexpectedStateShapeWarningMessage(state, finalReducers, action, unexpectedKeyCache);

      if (warningMessage) {
        warning(warningMessage);
      }
    }

    var hasChanged = false;
    var nextState = {};

    for (var _i = 0; _i < finalReducerKeys.length; _i++) {
      var _key = finalReducerKeys[_i];
      var reducer = finalReducers[_key];
      var previousStateForKey = state[_key];
      var nextStateForKey = reducer(previousStateForKey, action);

      if (typeof nextStateForKey === 'undefined') {
        var errorMessage = getUndefinedStateErrorMessage(_key, action);
        throw new Error(errorMessage);
      }

      nextState[_key] = nextStateForKey;
      hasChanged = hasChanged || nextStateForKey !== previousStateForKey;
    }

    return hasChanged ? nextState : state;
  };
}

function bindActionCreator(actionCreator, dispatch) {
  return function () {
    return dispatch(actionCreator.apply(this, arguments));
  };
}
/**
 * Turns an object whose values are action creators, into an object with the
 * same keys, but with every function wrapped into a `dispatch` call so they
 * may be invoked directly. This is just a convenience method, as you can call
 * `store.dispatch(MyActionCreators.doSomething())` yourself just fine.
 *
 * For convenience, you can also pass an action creator as the first argument,
 * and get a dispatch wrapped function in return.
 *
 * @param {Function|Object} actionCreators An object whose values are action
 * creator functions. One handy way to obtain it is to use ES6 `import * as`
 * syntax. You may also pass a single function.
 *
 * @param {Function} dispatch The `dispatch` function available on your Redux
 * store.
 *
 * @returns {Function|Object} The object mimicking the original object, but with
 * every action creator wrapped into the `dispatch` call. If you passed a
 * function as `actionCreators`, the return value will also be a single
 * function.
 */


function bindActionCreators(actionCreators, dispatch) {
  if (typeof actionCreators === 'function') {
    return bindActionCreator(actionCreators, dispatch);
  }

  if (typeof actionCreators !== 'object' || actionCreators === null) {
    throw new Error("bindActionCreators expected an object or a function, instead received " + (actionCreators === null ? 'null' : typeof actionCreators) + ". " + "Did you write \"import ActionCreators from\" instead of \"import * as ActionCreators from\"?");
  }

  var boundActionCreators = {};

  for (var key in actionCreators) {
    var actionCreator = actionCreators[key];

    if (typeof actionCreator === 'function') {
      boundActionCreators[key] = bindActionCreator(actionCreator, dispatch);
    }
  }

  return boundActionCreators;
}

function _defineProperty(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
  } else {
    obj[key] = value;
  }

  return obj;
}

function ownKeys(object, enumerableOnly) {
  var keys = Object.keys(object);

  if (Object.getOwnPropertySymbols) {
    keys.push.apply(keys, Object.getOwnPropertySymbols(object));
  }

  if (enumerableOnly) keys = keys.filter(function (sym) {
    return Object.getOwnPropertyDescriptor(object, sym).enumerable;
  });
  return keys;
}

function _objectSpread2(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i] != null ? arguments[i] : {};

    if (i % 2) {
      ownKeys(source, true).forEach(function (key) {
        _defineProperty(target, key, source[key]);
      });
    } else if (Object.getOwnPropertyDescriptors) {
      Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
    } else {
      ownKeys(source).forEach(function (key) {
        Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
      });
    }
  }

  return target;
}
/**
 * Composes single-argument functions from right to left. The rightmost
 * function can take multiple arguments as it provides the signature for
 * the resulting composite function.
 *
 * @param {...Function} funcs The functions to compose.
 * @returns {Function} A function obtained by composing the argument functions
 * from right to left. For example, compose(f, g, h) is identical to doing
 * (...args) => f(g(h(...args))).
 */


function compose() {
  for (var _len = arguments.length, funcs = new Array(_len), _key = 0; _key < _len; _key++) {
    funcs[_key] = arguments[_key];
  }

  if (funcs.length === 0) {
    return function (arg) {
      return arg;
    };
  }

  if (funcs.length === 1) {
    return funcs[0];
  }

  return funcs.reduce(function (a, b) {
    return function () {
      return a(b.apply(void 0, arguments));
    };
  });
}
/**
 * Creates a store enhancer that applies middleware to the dispatch method
 * of the Redux store. This is handy for a variety of tasks, such as expressing
 * asynchronous actions in a concise manner, or logging every action payload.
 *
 * See `redux-thunk` package as an example of the Redux middleware.
 *
 * Because middleware is potentially asynchronous, this should be the first
 * store enhancer in the composition chain.
 *
 * Note that each middleware will be given the `dispatch` and `getState` functions
 * as named arguments.
 *
 * @param {...Function} middlewares The middleware chain to be applied.
 * @returns {Function} A store enhancer applying the middleware.
 */


function applyMiddleware() {
  for (var _len = arguments.length, middlewares = new Array(_len), _key = 0; _key < _len; _key++) {
    middlewares[_key] = arguments[_key];
  }

  return function (createStore) {
    return function () {
      var store = createStore.apply(void 0, arguments);

      var _dispatch = function dispatch() {
        throw new Error('Dispatching while constructing your middleware is not allowed. ' + 'Other middleware would not be applied to this dispatch.');
      };

      var middlewareAPI = {
        getState: store.getState,
        dispatch: function dispatch() {
          return _dispatch.apply(void 0, arguments);
        }
      };
      var chain = middlewares.map(function (middleware) {
        return middleware(middlewareAPI);
      });
      _dispatch = compose.apply(void 0, chain)(store.dispatch);
      return _objectSpread2({}, store, {
        dispatch: _dispatch
      });
    };
  };
}
/*
 * This is a dummy function to check if the function name has been altered by minification.
 * If the function has been minified and NODE_ENV !== 'production', warn the user.
 */


function isCrushed() {}

if ("development" !== 'production' && typeof isCrushed.name === 'string' && isCrushed.name !== 'isCrushed') {
  warning('You are currently using minified code outside of NODE_ENV === "production". ' + 'This means that you are running a slower development build of Redux. ' + 'You can use loose-envify (https://github.com/zertosh/loose-envify) for browserify ' + 'or setting mode to production in webpack (https://webpack.js.org/concepts/mode/) ' + 'to ensure you have the correct code for your production build.');
}
},{"symbol-observable":"../node_modules/symbol-observable/es/index.js"}],"../node_modules/react-redux/es/utils/isPlainObject.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isPlainObject;

/**
 * @param {any} obj The object to inspect.
 * @returns {boolean} True if the argument appears to be a plain object.
 */
function isPlainObject(obj) {
  if (typeof obj !== 'object' || obj === null) return false;
  var proto = Object.getPrototypeOf(obj);
  if (proto === null) return true;
  var baseProto = proto;

  while (Object.getPrototypeOf(baseProto) !== null) {
    baseProto = Object.getPrototypeOf(baseProto);
  }

  return proto === baseProto;
}
},{}],"../node_modules/react-redux/es/utils/warning.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = warning;

/**
 * Prints a warning in the console if it exists.
 *
 * @param {String} message The warning message.
 * @returns {void}
 */
function warning(message) {
  /* eslint-disable no-console */
  if (typeof console !== 'undefined' && typeof console.error === 'function') {
    console.error(message);
  }
  /* eslint-enable no-console */


  try {
    // This error was thrown as a convenience so that if you enable
    // "break on all exceptions" in your console,
    // it would pause the execution at this line.
    throw new Error(message);
    /* eslint-disable no-empty */
  } catch (e) {}
  /* eslint-enable no-empty */

}
},{}],"../node_modules/react-redux/es/utils/verifyPlainObject.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = verifyPlainObject;

var _isPlainObject = _interopRequireDefault(require("./isPlainObject"));

var _warning = _interopRequireDefault(require("./warning"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function verifyPlainObject(value, displayName, methodName) {
  if (!(0, _isPlainObject.default)(value)) {
    (0, _warning.default)(methodName + "() in " + displayName + " must return a plain object. Instead received " + value + ".");
  }
}
},{"./isPlainObject":"../node_modules/react-redux/es/utils/isPlainObject.js","./warning":"../node_modules/react-redux/es/utils/warning.js"}],"../node_modules/react-redux/es/connect/wrapMapToProps.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.wrapMapToPropsConstant = wrapMapToPropsConstant;
exports.getDependsOnOwnProps = getDependsOnOwnProps;
exports.wrapMapToPropsFunc = wrapMapToPropsFunc;

var _verifyPlainObject = _interopRequireDefault(require("../utils/verifyPlainObject"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function wrapMapToPropsConstant(getConstant) {
  return function initConstantSelector(dispatch, options) {
    var constant = getConstant(dispatch, options);

    function constantSelector() {
      return constant;
    }

    constantSelector.dependsOnOwnProps = false;
    return constantSelector;
  };
} // dependsOnOwnProps is used by createMapToPropsProxy to determine whether to pass props as args
// to the mapToProps function being wrapped. It is also used by makePurePropsSelector to determine
// whether mapToProps needs to be invoked when props have changed.
//
// A length of one signals that mapToProps does not depend on props from the parent component.
// A length of zero is assumed to mean mapToProps is getting args via arguments or ...args and
// therefore not reporting its length accurately..


function getDependsOnOwnProps(mapToProps) {
  return mapToProps.dependsOnOwnProps !== null && mapToProps.dependsOnOwnProps !== undefined ? Boolean(mapToProps.dependsOnOwnProps) : mapToProps.length !== 1;
} // Used by whenMapStateToPropsIsFunction and whenMapDispatchToPropsIsFunction,
// this function wraps mapToProps in a proxy function which does several things:
//
//  * Detects whether the mapToProps function being called depends on props, which
//    is used by selectorFactory to decide if it should reinvoke on props changes.
//
//  * On first call, handles mapToProps if returns another function, and treats that
//    new function as the true mapToProps for subsequent calls.
//
//  * On first call, verifies the first result is a plain object, in order to warn
//    the developer that their mapToProps function is not returning a valid result.
//


function wrapMapToPropsFunc(mapToProps, methodName) {
  return function initProxySelector(dispatch, _ref) {
    var displayName = _ref.displayName;

    var proxy = function mapToPropsProxy(stateOrDispatch, ownProps) {
      return proxy.dependsOnOwnProps ? proxy.mapToProps(stateOrDispatch, ownProps) : proxy.mapToProps(stateOrDispatch);
    }; // allow detectFactoryAndVerify to get ownProps


    proxy.dependsOnOwnProps = true;

    proxy.mapToProps = function detectFactoryAndVerify(stateOrDispatch, ownProps) {
      proxy.mapToProps = mapToProps;
      proxy.dependsOnOwnProps = getDependsOnOwnProps(mapToProps);
      var props = proxy(stateOrDispatch, ownProps);

      if (typeof props === 'function') {
        proxy.mapToProps = props;
        proxy.dependsOnOwnProps = getDependsOnOwnProps(props);
        props = proxy(stateOrDispatch, ownProps);
      }

      if ("development" !== 'production') (0, _verifyPlainObject.default)(props, displayName, methodName);
      return props;
    };

    return proxy;
  };
}
},{"../utils/verifyPlainObject":"../node_modules/react-redux/es/utils/verifyPlainObject.js"}],"../node_modules/react-redux/es/connect/mapDispatchToProps.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.whenMapDispatchToPropsIsFunction = whenMapDispatchToPropsIsFunction;
exports.whenMapDispatchToPropsIsMissing = whenMapDispatchToPropsIsMissing;
exports.whenMapDispatchToPropsIsObject = whenMapDispatchToPropsIsObject;
exports.default = void 0;

var _redux = require("redux");

var _wrapMapToProps = require("./wrapMapToProps");

function whenMapDispatchToPropsIsFunction(mapDispatchToProps) {
  return typeof mapDispatchToProps === 'function' ? (0, _wrapMapToProps.wrapMapToPropsFunc)(mapDispatchToProps, 'mapDispatchToProps') : undefined;
}

function whenMapDispatchToPropsIsMissing(mapDispatchToProps) {
  return !mapDispatchToProps ? (0, _wrapMapToProps.wrapMapToPropsConstant)(function (dispatch) {
    return {
      dispatch: dispatch
    };
  }) : undefined;
}

function whenMapDispatchToPropsIsObject(mapDispatchToProps) {
  return mapDispatchToProps && typeof mapDispatchToProps === 'object' ? (0, _wrapMapToProps.wrapMapToPropsConstant)(function (dispatch) {
    return (0, _redux.bindActionCreators)(mapDispatchToProps, dispatch);
  }) : undefined;
}

var _default = [whenMapDispatchToPropsIsFunction, whenMapDispatchToPropsIsMissing, whenMapDispatchToPropsIsObject];
exports.default = _default;
},{"redux":"../node_modules/redux/es/redux.js","./wrapMapToProps":"../node_modules/react-redux/es/connect/wrapMapToProps.js"}],"../node_modules/react-redux/es/connect/mapStateToProps.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.whenMapStateToPropsIsFunction = whenMapStateToPropsIsFunction;
exports.whenMapStateToPropsIsMissing = whenMapStateToPropsIsMissing;
exports.default = void 0;

var _wrapMapToProps = require("./wrapMapToProps");

function whenMapStateToPropsIsFunction(mapStateToProps) {
  return typeof mapStateToProps === 'function' ? (0, _wrapMapToProps.wrapMapToPropsFunc)(mapStateToProps, 'mapStateToProps') : undefined;
}

function whenMapStateToPropsIsMissing(mapStateToProps) {
  return !mapStateToProps ? (0, _wrapMapToProps.wrapMapToPropsConstant)(function () {
    return {};
  }) : undefined;
}

var _default = [whenMapStateToPropsIsFunction, whenMapStateToPropsIsMissing];
exports.default = _default;
},{"./wrapMapToProps":"../node_modules/react-redux/es/connect/wrapMapToProps.js"}],"../node_modules/react-redux/es/connect/mergeProps.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.defaultMergeProps = defaultMergeProps;
exports.wrapMergePropsFunc = wrapMergePropsFunc;
exports.whenMergePropsIsFunction = whenMergePropsIsFunction;
exports.whenMergePropsIsOmitted = whenMergePropsIsOmitted;
exports.default = void 0;

var _extends2 = _interopRequireDefault(require("@babel/runtime/helpers/esm/extends"));

var _verifyPlainObject = _interopRequireDefault(require("../utils/verifyPlainObject"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function defaultMergeProps(stateProps, dispatchProps, ownProps) {
  return (0, _extends2.default)({}, ownProps, {}, stateProps, {}, dispatchProps);
}

function wrapMergePropsFunc(mergeProps) {
  return function initMergePropsProxy(dispatch, _ref) {
    var displayName = _ref.displayName,
        pure = _ref.pure,
        areMergedPropsEqual = _ref.areMergedPropsEqual;
    var hasRunOnce = false;
    var mergedProps;
    return function mergePropsProxy(stateProps, dispatchProps, ownProps) {
      var nextMergedProps = mergeProps(stateProps, dispatchProps, ownProps);

      if (hasRunOnce) {
        if (!pure || !areMergedPropsEqual(nextMergedProps, mergedProps)) mergedProps = nextMergedProps;
      } else {
        hasRunOnce = true;
        mergedProps = nextMergedProps;
        if ("development" !== 'production') (0, _verifyPlainObject.default)(mergedProps, displayName, 'mergeProps');
      }

      return mergedProps;
    };
  };
}

function whenMergePropsIsFunction(mergeProps) {
  return typeof mergeProps === 'function' ? wrapMergePropsFunc(mergeProps) : undefined;
}

function whenMergePropsIsOmitted(mergeProps) {
  return !mergeProps ? function () {
    return defaultMergeProps;
  } : undefined;
}

var _default = [whenMergePropsIsFunction, whenMergePropsIsOmitted];
exports.default = _default;
},{"@babel/runtime/helpers/esm/extends":"../node_modules/@babel/runtime/helpers/esm/extends.js","../utils/verifyPlainObject":"../node_modules/react-redux/es/utils/verifyPlainObject.js"}],"../node_modules/react-redux/es/connect/verifySubselectors.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = verifySubselectors;

var _warning = _interopRequireDefault(require("../utils/warning"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function verify(selector, methodName, displayName) {
  if (!selector) {
    throw new Error("Unexpected value for " + methodName + " in " + displayName + ".");
  } else if (methodName === 'mapStateToProps' || methodName === 'mapDispatchToProps') {
    if (!Object.prototype.hasOwnProperty.call(selector, 'dependsOnOwnProps')) {
      (0, _warning.default)("The selector for " + methodName + " of " + displayName + " did not specify a value for dependsOnOwnProps.");
    }
  }
}

function verifySubselectors(mapStateToProps, mapDispatchToProps, mergeProps, displayName) {
  verify(mapStateToProps, 'mapStateToProps', displayName);
  verify(mapDispatchToProps, 'mapDispatchToProps', displayName);
  verify(mergeProps, 'mergeProps', displayName);
}
},{"../utils/warning":"../node_modules/react-redux/es/utils/warning.js"}],"../node_modules/react-redux/es/connect/selectorFactory.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.impureFinalPropsSelectorFactory = impureFinalPropsSelectorFactory;
exports.pureFinalPropsSelectorFactory = pureFinalPropsSelectorFactory;
exports.default = finalPropsSelectorFactory;

var _objectWithoutPropertiesLoose2 = _interopRequireDefault(require("@babel/runtime/helpers/esm/objectWithoutPropertiesLoose"));

var _verifySubselectors = _interopRequireDefault(require("./verifySubselectors"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function impureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch) {
  return function impureFinalPropsSelector(state, ownProps) {
    return mergeProps(mapStateToProps(state, ownProps), mapDispatchToProps(dispatch, ownProps), ownProps);
  };
}

function pureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, _ref) {
  var areStatesEqual = _ref.areStatesEqual,
      areOwnPropsEqual = _ref.areOwnPropsEqual,
      areStatePropsEqual = _ref.areStatePropsEqual;
  var hasRunAtLeastOnce = false;
  var state;
  var ownProps;
  var stateProps;
  var dispatchProps;
  var mergedProps;

  function handleFirstCall(firstState, firstOwnProps) {
    state = firstState;
    ownProps = firstOwnProps;
    stateProps = mapStateToProps(state, ownProps);
    dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    hasRunAtLeastOnce = true;
    return mergedProps;
  }

  function handleNewPropsAndNewState() {
    stateProps = mapStateToProps(state, ownProps);
    if (mapDispatchToProps.dependsOnOwnProps) dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }

  function handleNewProps() {
    if (mapStateToProps.dependsOnOwnProps) stateProps = mapStateToProps(state, ownProps);
    if (mapDispatchToProps.dependsOnOwnProps) dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }

  function handleNewState() {
    var nextStateProps = mapStateToProps(state, ownProps);
    var statePropsChanged = !areStatePropsEqual(nextStateProps, stateProps);
    stateProps = nextStateProps;
    if (statePropsChanged) mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }

  function handleSubsequentCalls(nextState, nextOwnProps) {
    var propsChanged = !areOwnPropsEqual(nextOwnProps, ownProps);
    var stateChanged = !areStatesEqual(nextState, state);
    state = nextState;
    ownProps = nextOwnProps;
    if (propsChanged && stateChanged) return handleNewPropsAndNewState();
    if (propsChanged) return handleNewProps();
    if (stateChanged) return handleNewState();
    return mergedProps;
  }

  return function pureFinalPropsSelector(nextState, nextOwnProps) {
    return hasRunAtLeastOnce ? handleSubsequentCalls(nextState, nextOwnProps) : handleFirstCall(nextState, nextOwnProps);
  };
} // TODO: Add more comments
// If pure is true, the selector returned by selectorFactory will memoize its results,
// allowing connectAdvanced's shouldComponentUpdate to return false if final
// props have not changed. If false, the selector will always return a new
// object and shouldComponentUpdate will always return true.


function finalPropsSelectorFactory(dispatch, _ref2) {
  var initMapStateToProps = _ref2.initMapStateToProps,
      initMapDispatchToProps = _ref2.initMapDispatchToProps,
      initMergeProps = _ref2.initMergeProps,
      options = (0, _objectWithoutPropertiesLoose2.default)(_ref2, ["initMapStateToProps", "initMapDispatchToProps", "initMergeProps"]);
  var mapStateToProps = initMapStateToProps(dispatch, options);
  var mapDispatchToProps = initMapDispatchToProps(dispatch, options);
  var mergeProps = initMergeProps(dispatch, options);

  if ("development" !== 'production') {
    (0, _verifySubselectors.default)(mapStateToProps, mapDispatchToProps, mergeProps, options.displayName);
  }

  var selectorFactory = options.pure ? pureFinalPropsSelectorFactory : impureFinalPropsSelectorFactory;
  return selectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, options);
}
},{"@babel/runtime/helpers/esm/objectWithoutPropertiesLoose":"../node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js","./verifySubselectors":"../node_modules/react-redux/es/connect/verifySubselectors.js"}],"../node_modules/react-redux/es/connect/connect.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createConnect = createConnect;
exports.default = void 0;

var _extends2 = _interopRequireDefault(require("@babel/runtime/helpers/esm/extends"));

var _objectWithoutPropertiesLoose2 = _interopRequireDefault(require("@babel/runtime/helpers/esm/objectWithoutPropertiesLoose"));

var _connectAdvanced = _interopRequireDefault(require("../components/connectAdvanced"));

var _shallowEqual = _interopRequireDefault(require("../utils/shallowEqual"));

var _mapDispatchToProps = _interopRequireDefault(require("./mapDispatchToProps"));

var _mapStateToProps = _interopRequireDefault(require("./mapStateToProps"));

var _mergeProps = _interopRequireDefault(require("./mergeProps"));

var _selectorFactory = _interopRequireDefault(require("./selectorFactory"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/*
  connect is a facade over connectAdvanced. It turns its args into a compatible
  selectorFactory, which has the signature:

    (dispatch, options) => (nextState, nextOwnProps) => nextFinalProps
  
  connect passes its args to connectAdvanced as options, which will in turn pass them to
  selectorFactory each time a Connect component instance is instantiated or hot reloaded.

  selectorFactory returns a final props selector from its mapStateToProps,
  mapStateToPropsFactories, mapDispatchToProps, mapDispatchToPropsFactories, mergeProps,
  mergePropsFactories, and pure args.

  The resulting final props selector is called by the Connect component instance whenever
  it receives new props or store state.
 */
function match(arg, factories, name) {
  for (var i = factories.length - 1; i >= 0; i--) {
    var result = factories[i](arg);
    if (result) return result;
  }

  return function (dispatch, options) {
    throw new Error("Invalid value of type " + typeof arg + " for " + name + " argument when connecting component " + options.wrappedComponentName + ".");
  };
}

function strictEqual(a, b) {
  return a === b;
} // createConnect with default args builds the 'official' connect behavior. Calling it with
// different options opens up some testing and extensibility scenarios


function createConnect(_temp) {
  var _ref = _temp === void 0 ? {} : _temp,
      _ref$connectHOC = _ref.connectHOC,
      connectHOC = _ref$connectHOC === void 0 ? _connectAdvanced.default : _ref$connectHOC,
      _ref$mapStateToPropsF = _ref.mapStateToPropsFactories,
      mapStateToPropsFactories = _ref$mapStateToPropsF === void 0 ? _mapStateToProps.default : _ref$mapStateToPropsF,
      _ref$mapDispatchToPro = _ref.mapDispatchToPropsFactories,
      mapDispatchToPropsFactories = _ref$mapDispatchToPro === void 0 ? _mapDispatchToProps.default : _ref$mapDispatchToPro,
      _ref$mergePropsFactor = _ref.mergePropsFactories,
      mergePropsFactories = _ref$mergePropsFactor === void 0 ? _mergeProps.default : _ref$mergePropsFactor,
      _ref$selectorFactory = _ref.selectorFactory,
      selectorFactory = _ref$selectorFactory === void 0 ? _selectorFactory.default : _ref$selectorFactory;

  return function connect(mapStateToProps, mapDispatchToProps, mergeProps, _ref2) {
    if (_ref2 === void 0) {
      _ref2 = {};
    }

    var _ref3 = _ref2,
        _ref3$pure = _ref3.pure,
        pure = _ref3$pure === void 0 ? true : _ref3$pure,
        _ref3$areStatesEqual = _ref3.areStatesEqual,
        areStatesEqual = _ref3$areStatesEqual === void 0 ? strictEqual : _ref3$areStatesEqual,
        _ref3$areOwnPropsEqua = _ref3.areOwnPropsEqual,
        areOwnPropsEqual = _ref3$areOwnPropsEqua === void 0 ? _shallowEqual.default : _ref3$areOwnPropsEqua,
        _ref3$areStatePropsEq = _ref3.areStatePropsEqual,
        areStatePropsEqual = _ref3$areStatePropsEq === void 0 ? _shallowEqual.default : _ref3$areStatePropsEq,
        _ref3$areMergedPropsE = _ref3.areMergedPropsEqual,
        areMergedPropsEqual = _ref3$areMergedPropsE === void 0 ? _shallowEqual.default : _ref3$areMergedPropsE,
        extraOptions = (0, _objectWithoutPropertiesLoose2.default)(_ref3, ["pure", "areStatesEqual", "areOwnPropsEqual", "areStatePropsEqual", "areMergedPropsEqual"]);
    var initMapStateToProps = match(mapStateToProps, mapStateToPropsFactories, 'mapStateToProps');
    var initMapDispatchToProps = match(mapDispatchToProps, mapDispatchToPropsFactories, 'mapDispatchToProps');
    var initMergeProps = match(mergeProps, mergePropsFactories, 'mergeProps');
    return connectHOC(selectorFactory, (0, _extends2.default)({
      // used in error messages
      methodName: 'connect',
      // used to compute Connect's displayName from the wrapped component's displayName.
      getDisplayName: function getDisplayName(name) {
        return "Connect(" + name + ")";
      },
      // if mapStateToProps is falsy, the Connect component doesn't subscribe to store state changes
      shouldHandleStateChanges: Boolean(mapStateToProps),
      // passed through to selectorFactory
      initMapStateToProps: initMapStateToProps,
      initMapDispatchToProps: initMapDispatchToProps,
      initMergeProps: initMergeProps,
      pure: pure,
      areStatesEqual: areStatesEqual,
      areOwnPropsEqual: areOwnPropsEqual,
      areStatePropsEqual: areStatePropsEqual,
      areMergedPropsEqual: areMergedPropsEqual
    }, extraOptions));
  };
}

var _default = createConnect();

exports.default = _default;
},{"@babel/runtime/helpers/esm/extends":"../node_modules/@babel/runtime/helpers/esm/extends.js","@babel/runtime/helpers/esm/objectWithoutPropertiesLoose":"../node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js","../components/connectAdvanced":"../node_modules/react-redux/es/components/connectAdvanced.js","../utils/shallowEqual":"../node_modules/react-redux/es/utils/shallowEqual.js","./mapDispatchToProps":"../node_modules/react-redux/es/connect/mapDispatchToProps.js","./mapStateToProps":"../node_modules/react-redux/es/connect/mapStateToProps.js","./mergeProps":"../node_modules/react-redux/es/connect/mergeProps.js","./selectorFactory":"../node_modules/react-redux/es/connect/selectorFactory.js"}],"../node_modules/react-redux/es/hooks/useReduxContext.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useReduxContext = useReduxContext;

var _react = require("react");

var _invariant = _interopRequireDefault(require("invariant"));

var _Context = require("../components/Context");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * A hook to access the value of the `ReactReduxContext`. This is a low-level
 * hook that you should usually not need to call directly.
 *
 * @returns {any} the value of the `ReactReduxContext`
 *
 * @example
 *
 * import React from 'react'
 * import { useReduxContext } from 'react-redux'
 *
 * export const CounterComponent = ({ value }) => {
 *   const { store } = useReduxContext()
 *   return <div>{store.getState()}</div>
 * }
 */
function useReduxContext() {
  var contextValue = (0, _react.useContext)(_Context.ReactReduxContext);
  (0, _invariant.default)(contextValue, 'could not find react-redux context value; please ensure the component is wrapped in a <Provider>');
  return contextValue;
}
},{"react":"../node_modules/react/index.js","invariant":"../node_modules/invariant/browser.js","../components/Context":"../node_modules/react-redux/es/components/Context.js"}],"../node_modules/react-redux/es/hooks/useStore.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createStoreHook = createStoreHook;
exports.useStore = void 0;

var _react = require("react");

var _Context = require("../components/Context");

var _useReduxContext2 = require("./useReduxContext");

/**
 * Hook factory, which creates a `useStore` hook bound to a given context.
 *
 * @param {Function} [context=ReactReduxContext] Context passed to your `<Provider>`.
 * @returns {Function} A `useStore` hook bound to the specified context.
 */
function createStoreHook(context) {
  if (context === void 0) {
    context = _Context.ReactReduxContext;
  }

  var useReduxContext = context === _Context.ReactReduxContext ? _useReduxContext2.useReduxContext : function () {
    return (0, _react.useContext)(context);
  };
  return function useStore() {
    var _useReduxContext = useReduxContext(),
        store = _useReduxContext.store;

    return store;
  };
}
/**
 * A hook to access the redux store.
 *
 * @returns {any} the redux store
 *
 * @example
 *
 * import React from 'react'
 * import { useStore } from 'react-redux'
 *
 * export const ExampleComponent = () => {
 *   const store = useStore()
 *   return <div>{store.getState()}</div>
 * }
 */


var useStore = createStoreHook();
exports.useStore = useStore;
},{"react":"../node_modules/react/index.js","../components/Context":"../node_modules/react-redux/es/components/Context.js","./useReduxContext":"../node_modules/react-redux/es/hooks/useReduxContext.js"}],"../node_modules/react-redux/es/hooks/useDispatch.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createDispatchHook = createDispatchHook;
exports.useDispatch = void 0;

var _Context = require("../components/Context");

var _useStore = require("./useStore");

/**
 * Hook factory, which creates a `useDispatch` hook bound to a given context.
 *
 * @param {Function} [context=ReactReduxContext] Context passed to your `<Provider>`.
 * @returns {Function} A `useDispatch` hook bound to the specified context.
 */
function createDispatchHook(context) {
  if (context === void 0) {
    context = _Context.ReactReduxContext;
  }

  var useStore = context === _Context.ReactReduxContext ? _useStore.useStore : (0, _useStore.createStoreHook)(context);
  return function useDispatch() {
    var store = useStore();
    return store.dispatch;
  };
}
/**
 * A hook to access the redux `dispatch` function.
 *
 * @returns {any|function} redux store's `dispatch` function
 *
 * @example
 *
 * import React, { useCallback } from 'react'
 * import { useDispatch } from 'react-redux'
 *
 * export const CounterComponent = ({ value }) => {
 *   const dispatch = useDispatch()
 *   const increaseCounter = useCallback(() => dispatch({ type: 'increase-counter' }), [])
 *   return (
 *     <div>
 *       <span>{value}</span>
 *       <button onClick={increaseCounter}>Increase counter</button>
 *     </div>
 *   )
 * }
 */


var useDispatch = createDispatchHook();
exports.useDispatch = useDispatch;
},{"../components/Context":"../node_modules/react-redux/es/components/Context.js","./useStore":"../node_modules/react-redux/es/hooks/useStore.js"}],"../node_modules/react-redux/es/hooks/useSelector.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createSelectorHook = createSelectorHook;
exports.useSelector = void 0;

var _react = require("react");

var _invariant = _interopRequireDefault(require("invariant"));

var _useReduxContext2 = require("./useReduxContext");

var _Subscription = _interopRequireDefault(require("../utils/Subscription"));

var _Context = require("../components/Context");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// React currently throws a warning when using useLayoutEffect on the server.
// To get around it, we can conditionally useEffect on the server (no-op) and
// useLayoutEffect in the browser. We need useLayoutEffect to ensure the store
// subscription callback always has the selector from the latest render commit
// available, otherwise a store update may happen between render and the effect,
// which may cause missed updates; we also must ensure the store subscription
// is created synchronously, otherwise a store update may occur before the
// subscription is created and an inconsistent state may be observed
var useIsomorphicLayoutEffect = typeof window !== 'undefined' ? _react.useLayoutEffect : _react.useEffect;

var refEquality = function refEquality(a, b) {
  return a === b;
};

function useSelectorWithStoreAndSubscription(selector, equalityFn, store, contextSub) {
  var _useReducer = (0, _react.useReducer)(function (s) {
    return s + 1;
  }, 0),
      forceRender = _useReducer[1];

  var subscription = (0, _react.useMemo)(function () {
    return new _Subscription.default(store, contextSub);
  }, [store, contextSub]);
  var latestSubscriptionCallbackError = (0, _react.useRef)();
  var latestSelector = (0, _react.useRef)();
  var latestSelectedState = (0, _react.useRef)();
  var selectedState;

  try {
    if (selector !== latestSelector.current || latestSubscriptionCallbackError.current) {
      selectedState = selector(store.getState());
    } else {
      selectedState = latestSelectedState.current;
    }
  } catch (err) {
    var errorMessage = "An error occured while selecting the store state: " + err.message + ".";

    if (latestSubscriptionCallbackError.current) {
      errorMessage += "\nThe error may be correlated with this previous error:\n" + latestSubscriptionCallbackError.current.stack + "\n\nOriginal stack trace:";
    }

    throw new Error(errorMessage);
  }

  useIsomorphicLayoutEffect(function () {
    latestSelector.current = selector;
    latestSelectedState.current = selectedState;
    latestSubscriptionCallbackError.current = undefined;
  });
  useIsomorphicLayoutEffect(function () {
    function checkForUpdates() {
      try {
        var newSelectedState = latestSelector.current(store.getState());

        if (equalityFn(newSelectedState, latestSelectedState.current)) {
          return;
        }

        latestSelectedState.current = newSelectedState;
      } catch (err) {
        // we ignore all errors here, since when the component
        // is re-rendered, the selectors are called again, and
        // will throw again, if neither props nor store state
        // changed
        latestSubscriptionCallbackError.current = err;
      }

      forceRender({});
    }

    subscription.onStateChange = checkForUpdates;
    subscription.trySubscribe();
    checkForUpdates();
    return function () {
      return subscription.tryUnsubscribe();
    };
  }, [store, subscription]);
  return selectedState;
}
/**
 * Hook factory, which creates a `useSelector` hook bound to a given context.
 *
 * @param {Function} [context=ReactReduxContext] Context passed to your `<Provider>`.
 * @returns {Function} A `useSelector` hook bound to the specified context.
 */


function createSelectorHook(context) {
  if (context === void 0) {
    context = _Context.ReactReduxContext;
  }

  var useReduxContext = context === _Context.ReactReduxContext ? _useReduxContext2.useReduxContext : function () {
    return (0, _react.useContext)(context);
  };
  return function useSelector(selector, equalityFn) {
    if (equalityFn === void 0) {
      equalityFn = refEquality;
    }

    (0, _invariant.default)(selector, "You must pass a selector to useSelectors");

    var _useReduxContext = useReduxContext(),
        store = _useReduxContext.store,
        contextSub = _useReduxContext.subscription;

    return useSelectorWithStoreAndSubscription(selector, equalityFn, store, contextSub);
  };
}
/**
 * A hook to access the redux store's state. This hook takes a selector function
 * as an argument. The selector is called with the store state.
 *
 * This hook takes an optional equality comparison function as the second parameter
 * that allows you to customize the way the selected state is compared to determine
 * whether the component needs to be re-rendered.
 *
 * @param {Function} selector the selector function
 * @param {Function=} equalityFn the function that will be used to determine equality
 *
 * @returns {any} the selected state
 *
 * @example
 *
 * import React from 'react'
 * import { useSelector } from 'react-redux'
 *
 * export const CounterComponent = () => {
 *   const counter = useSelector(state => state.counter)
 *   return <div>{counter}</div>
 * }
 */


var useSelector = createSelectorHook();
exports.useSelector = useSelector;
},{"react":"../node_modules/react/index.js","invariant":"../node_modules/invariant/browser.js","./useReduxContext":"../node_modules/react-redux/es/hooks/useReduxContext.js","../utils/Subscription":"../node_modules/react-redux/es/utils/Subscription.js","../components/Context":"../node_modules/react-redux/es/components/Context.js"}],"../node_modules/react-redux/es/utils/reactBatchedUpdates.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
Object.defineProperty(exports, "unstable_batchedUpdates", {
  enumerable: true,
  get: function () {
    return _reactDom.unstable_batchedUpdates;
  }
});

var _reactDom = require("react-dom");
},{"react-dom":"../node_modules/react-dom/index.js"}],"../node_modules/react-redux/es/index.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
Object.defineProperty(exports, "Provider", {
  enumerable: true,
  get: function () {
    return _Provider.default;
  }
});
Object.defineProperty(exports, "connectAdvanced", {
  enumerable: true,
  get: function () {
    return _connectAdvanced.default;
  }
});
Object.defineProperty(exports, "ReactReduxContext", {
  enumerable: true,
  get: function () {
    return _Context.ReactReduxContext;
  }
});
Object.defineProperty(exports, "connect", {
  enumerable: true,
  get: function () {
    return _connect.default;
  }
});
Object.defineProperty(exports, "useDispatch", {
  enumerable: true,
  get: function () {
    return _useDispatch.useDispatch;
  }
});
Object.defineProperty(exports, "createDispatchHook", {
  enumerable: true,
  get: function () {
    return _useDispatch.createDispatchHook;
  }
});
Object.defineProperty(exports, "useSelector", {
  enumerable: true,
  get: function () {
    return _useSelector.useSelector;
  }
});
Object.defineProperty(exports, "createSelectorHook", {
  enumerable: true,
  get: function () {
    return _useSelector.createSelectorHook;
  }
});
Object.defineProperty(exports, "useStore", {
  enumerable: true,
  get: function () {
    return _useStore.useStore;
  }
});
Object.defineProperty(exports, "createStoreHook", {
  enumerable: true,
  get: function () {
    return _useStore.createStoreHook;
  }
});
Object.defineProperty(exports, "batch", {
  enumerable: true,
  get: function () {
    return _reactBatchedUpdates.unstable_batchedUpdates;
  }
});
Object.defineProperty(exports, "shallowEqual", {
  enumerable: true,
  get: function () {
    return _shallowEqual.default;
  }
});

var _Provider = _interopRequireDefault(require("./components/Provider"));

var _connectAdvanced = _interopRequireDefault(require("./components/connectAdvanced"));

var _Context = require("./components/Context");

var _connect = _interopRequireDefault(require("./connect/connect"));

var _useDispatch = require("./hooks/useDispatch");

var _useSelector = require("./hooks/useSelector");

var _useStore = require("./hooks/useStore");

var _batch = require("./utils/batch");

var _reactBatchedUpdates = require("./utils/reactBatchedUpdates");

var _shallowEqual = _interopRequireDefault(require("./utils/shallowEqual"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

(0, _batch.setBatch)(_reactBatchedUpdates.unstable_batchedUpdates);
},{"./components/Provider":"../node_modules/react-redux/es/components/Provider.js","./components/connectAdvanced":"../node_modules/react-redux/es/components/connectAdvanced.js","./components/Context":"../node_modules/react-redux/es/components/Context.js","./connect/connect":"../node_modules/react-redux/es/connect/connect.js","./hooks/useDispatch":"../node_modules/react-redux/es/hooks/useDispatch.js","./hooks/useSelector":"../node_modules/react-redux/es/hooks/useSelector.js","./hooks/useStore":"../node_modules/react-redux/es/hooks/useStore.js","./utils/batch":"../node_modules/react-redux/es/utils/batch.js","./utils/reactBatchedUpdates":"../node_modules/react-redux/es/utils/reactBatchedUpdates.js","./utils/shallowEqual":"../node_modules/react-redux/es/utils/shallowEqual.js"}],"../node_modules/@redux-saga/symbols/dist/redux-saga-symbols.esm.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.TERMINATE = exports.TASK_CANCEL = exports.TASK = exports.SELF_CANCELLATION = exports.SAGA_LOCATION = exports.SAGA_ACTION = exports.MULTICAST = exports.MATCH = exports.IO = exports.CHANNEL_END_TYPE = exports.CANCEL = void 0;

var createSymbol = function createSymbol(name) {
  return "@@redux-saga/" + name;
};

var CANCEL =
/*#__PURE__*/
createSymbol('CANCEL_PROMISE');
exports.CANCEL = CANCEL;
var CHANNEL_END_TYPE =
/*#__PURE__*/
createSymbol('CHANNEL_END');
exports.CHANNEL_END_TYPE = CHANNEL_END_TYPE;
var IO =
/*#__PURE__*/
createSymbol('IO');
exports.IO = IO;
var MATCH =
/*#__PURE__*/
createSymbol('MATCH');
exports.MATCH = MATCH;
var MULTICAST =
/*#__PURE__*/
createSymbol('MULTICAST');
exports.MULTICAST = MULTICAST;
var SAGA_ACTION =
/*#__PURE__*/
createSymbol('SAGA_ACTION');
exports.SAGA_ACTION = SAGA_ACTION;
var SELF_CANCELLATION =
/*#__PURE__*/
createSymbol('SELF_CANCELLATION');
exports.SELF_CANCELLATION = SELF_CANCELLATION;
var TASK =
/*#__PURE__*/
createSymbol('TASK');
exports.TASK = TASK;
var TASK_CANCEL =
/*#__PURE__*/
createSymbol('TASK_CANCEL');
exports.TASK_CANCEL = TASK_CANCEL;
var TERMINATE =
/*#__PURE__*/
createSymbol('TERMINATE');
exports.TERMINATE = TERMINATE;
var SAGA_LOCATION =
/*#__PURE__*/
createSymbol('LOCATION');
exports.SAGA_LOCATION = SAGA_LOCATION;
},{}],"../node_modules/@redux-saga/is/dist/redux-saga-is.esm.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.undef = exports.task = exports.symbol = exports.stringableFunc = exports.string = exports.sagaAction = exports.promise = exports.pattern = exports.observable = exports.object = exports.number = exports.notUndef = exports.multicast = exports.iterator = exports.iterable = exports.func = exports.effect = exports.channel = exports.buffer = exports.array = void 0;

var _symbols = require("@redux-saga/symbols");

var undef = function undef(v) {
  return v === null || v === undefined;
};

exports.undef = undef;

var notUndef = function notUndef(v) {
  return v !== null && v !== undefined;
};

exports.notUndef = notUndef;

var func = function func(f) {
  return typeof f === 'function';
};

exports.func = func;

var number = function number(n) {
  return typeof n === 'number';
};

exports.number = number;

var string = function string(s) {
  return typeof s === 'string';
};

exports.string = string;
var array = Array.isArray;
exports.array = array;

var object = function object(obj) {
  return obj && !array(obj) && typeof obj === 'object';
};

exports.object = object;

var promise = function promise(p) {
  return p && func(p.then);
};

exports.promise = promise;

var iterator = function iterator(it) {
  return it && func(it.next) && func(it.throw);
};

exports.iterator = iterator;

var iterable = function iterable(it) {
  return it && func(Symbol) ? func(it[Symbol.iterator]) : array(it);
};

exports.iterable = iterable;

var task = function task(t) {
  return t && t[_symbols.TASK];
};

exports.task = task;

var sagaAction = function sagaAction(a) {
  return Boolean(a && a[_symbols.SAGA_ACTION]);
};

exports.sagaAction = sagaAction;

var observable = function observable(ob) {
  return ob && func(ob.subscribe);
};

exports.observable = observable;

var buffer = function buffer(buf) {
  return buf && func(buf.isEmpty) && func(buf.take) && func(buf.put);
};

exports.buffer = buffer;

var pattern = function pattern(pat) {
  return pat && (string(pat) || symbol(pat) || func(pat) || array(pat) && pat.every(pattern));
};

exports.pattern = pattern;

var channel = function channel(ch) {
  return ch && func(ch.take) && func(ch.close);
};

exports.channel = channel;

var stringableFunc = function stringableFunc(f) {
  return func(f) && f.hasOwnProperty('toString');
};

exports.stringableFunc = stringableFunc;

var symbol = function symbol(sym) {
  return Boolean(sym) && typeof Symbol === 'function' && sym.constructor === Symbol && sym !== Symbol.prototype;
};

exports.symbol = symbol;

var multicast = function multicast(ch) {
  return channel(ch) && ch[_symbols.MULTICAST];
};

exports.multicast = multicast;

var effect = function effect(eff) {
  return eff && eff[_symbols.IO];
};

exports.effect = effect;
},{"@redux-saga/symbols":"../node_modules/@redux-saga/symbols/dist/redux-saga-symbols.esm.js"}],"../node_modules/@redux-saga/delay-p/dist/redux-saga-delay-p.esm.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _symbols = require("@redux-saga/symbols");

function delayP(ms, val) {
  if (val === void 0) {
    val = true;
  }

  var timeoutId;
  var promise = new Promise(function (resolve) {
    timeoutId = setTimeout(resolve, ms, val);
  });

  promise[_symbols.CANCEL] = function () {
    clearTimeout(timeoutId);
  };

  return promise;
}

var _default = delayP;
exports.default = _default;
},{"@redux-saga/symbols":"../node_modules/@redux-saga/symbols/dist/redux-saga-symbols.esm.js"}],"../node_modules/@redux-saga/core/dist/io-427945dd.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.$ = apply;
exports.B = logError;
exports.K = take;
exports.L = fork;
exports.M = cancel;
exports.N = call;
exports.O = actionChannel;
exports.V = race;
exports.Y = put;
exports._ = all;
exports.a0 = cps;
exports.a1 = spawn;
exports.a2 = join;
exports.a3 = select;
exports.a4 = cancelled;
exports.a5 = flush;
exports.a6 = getContext;
exports.a7 = setContext;
exports.c = check;
exports.j = getMetaInfo;
exports.l = createAllStyleChildCallbacks;
exports.o = once;
exports.q = makeIterator;
exports.r = remove;
exports.v = getLocation;
exports.z = exports.y = exports.x = exports.w = exports.u = exports.t = exports.s = exports.p = exports.n = exports.m = exports.k = exports.i = exports.h = exports.g = exports.f = exports.e = exports.d = exports.b = exports.a = exports.Z = exports.X = exports.W = exports.U = exports.T = exports.S = exports.R = exports.Q = exports.P = exports.J = exports.I = exports.H = exports.G = exports.F = exports.E = exports.D = exports.C = exports.A = void 0;

var _symbols = require("@redux-saga/symbols");

var _extends2 = _interopRequireDefault(require("@babel/runtime/helpers/esm/extends"));

var _is = require("@redux-saga/is");

var _delayP = _interopRequireDefault(require("@redux-saga/delay-p"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var konst = function konst(v) {
  return function () {
    return v;
  };
};

var kTrue =
/*#__PURE__*/
konst(true);
exports.k = kTrue;

var noop = function noop() {};

exports.t = noop;

if ("development" !== 'production' && typeof Proxy !== 'undefined') {
  exports.t = noop =
  /*#__PURE__*/
  new Proxy(noop, {
    set: function set() {
      throw internalErr('There was an attempt to assign a property to internal `noop` function.');
    }
  });
}

var identity = function identity(v) {
  return v;
};

exports.E = identity;
var hasSymbol = typeof Symbol === 'function';
var asyncIteratorSymbol = hasSymbol && Symbol.asyncIterator ? Symbol.asyncIterator : '@@asyncIterator';
exports.x = asyncIteratorSymbol;

function check(value, predicate, error) {
  if (!predicate(value)) {
    throw new Error(error);
  }
}

var assignWithSymbols = function assignWithSymbols(target, source) {
  (0, _extends2.default)(target, source);

  if (Object.getOwnPropertySymbols) {
    Object.getOwnPropertySymbols(source).forEach(function (s) {
      target[s] = source[s];
    });
  }
};

exports.p = assignWithSymbols;

var flatMap = function flatMap(mapper, arr) {
  var _ref;

  return (_ref = []).concat.apply(_ref, arr.map(mapper));
};

exports.u = flatMap;

function remove(array, item) {
  var index = array.indexOf(item);

  if (index >= 0) {
    array.splice(index, 1);
  }
}

function once(fn) {
  var called = false;
  return function () {
    if (called) {
      return;
    }

    called = true;
    fn();
  };
}

var kThrow = function kThrow(err) {
  throw err;
};

var kReturn = function kReturn(value) {
  return {
    value: value,
    done: true
  };
};

function makeIterator(next, thro, name) {
  if (thro === void 0) {
    thro = kThrow;
  }

  if (name === void 0) {
    name = 'iterator';
  }

  var iterator = {
    meta: {
      name: name
    },
    next: next,
    throw: thro,
    return: kReturn,
    isSagaIterator: true
  };

  if (typeof Symbol !== 'undefined') {
    iterator[Symbol.iterator] = function () {
      return iterator;
    };
  }

  return iterator;
}

function logError(error, _ref2) {
  var sagaStack = _ref2.sagaStack;
  /*eslint-disable no-console*/

  console.error(error);
  console.error(sagaStack);
}

var internalErr = function internalErr(err) {
  return new Error("\n  redux-saga: Error checking hooks detected an inconsistent state. This is likely a bug\n  in redux-saga code and not yours. Thanks for reporting this in the project's github repo.\n  Error: " + err + "\n");
};

exports.i = internalErr;

var createSetContextWarning = function createSetContextWarning(ctx, props) {
  return (ctx ? ctx + '.' : '') + "setContext(props): argument " + props + " is not a plain object";
};

exports.w = createSetContextWarning;
var FROZEN_ACTION_ERROR = "You can't put (a.k.a. dispatch from saga) frozen actions.\nWe have to define a special non-enumerable property on those actions for scheduling purposes.\nOtherwise you wouldn't be able to communicate properly between sagas & other subscribers (action ordering would become far less predictable).\nIf you are using redux and you care about this behaviour (frozen actions),\nthen you might want to switch to freezing actions in a middleware rather than in action creator.\nExample implementation:\n\nconst freezeActions = store => next => action => next(Object.freeze(action))\n"; // creates empty, but not-holey array

var createEmptyArray = function createEmptyArray(n) {
  return Array.apply(null, new Array(n));
};

exports.m = createEmptyArray;

var wrapSagaDispatch = function wrapSagaDispatch(dispatch) {
  return function (action) {
    if ("development" !== 'production') {
      check(action, function (ac) {
        return !Object.isFrozen(ac);
      }, FROZEN_ACTION_ERROR);
    }

    return dispatch(Object.defineProperty(action, _symbols.SAGA_ACTION, {
      value: true
    }));
  };
};

exports.D = wrapSagaDispatch;

var shouldTerminate = function shouldTerminate(res) {
  return res === _symbols.TERMINATE;
};

exports.z = shouldTerminate;

var shouldCancel = function shouldCancel(res) {
  return res === _symbols.TASK_CANCEL;
};

exports.y = shouldCancel;

var shouldComplete = function shouldComplete(res) {
  return shouldTerminate(res) || shouldCancel(res);
};

exports.s = shouldComplete;

function createAllStyleChildCallbacks(shape, parentCallback) {
  var keys = Object.keys(shape);
  var totalCount = keys.length;

  if ("development" !== 'production') {
    check(totalCount, function (c) {
      return c > 0;
    }, 'createAllStyleChildCallbacks: get an empty array or object');
  }

  var completedCount = 0;
  var completed;
  var results = (0, _is.array)(shape) ? createEmptyArray(totalCount) : {};
  var childCallbacks = {};

  function checkEnd() {
    if (completedCount === totalCount) {
      completed = true;
      parentCallback(results);
    }
  }

  keys.forEach(function (key) {
    var chCbAtKey = function chCbAtKey(res, isErr) {
      if (completed) {
        return;
      }

      if (isErr || shouldComplete(res)) {
        parentCallback.cancel();
        parentCallback(res, isErr);
      } else {
        results[key] = res;
        completedCount++;
        checkEnd();
      }
    };

    chCbAtKey.cancel = noop;
    childCallbacks[key] = chCbAtKey;
  });

  parentCallback.cancel = function () {
    if (!completed) {
      completed = true;
      keys.forEach(function (key) {
        return childCallbacks[key].cancel();
      });
    }
  };

  return childCallbacks;
}

function getMetaInfo(fn) {
  return {
    name: fn.name || 'anonymous',
    location: getLocation(fn)
  };
}

function getLocation(instrumented) {
  return instrumented[_symbols.SAGA_LOCATION];
}

var BUFFER_OVERFLOW = "Channel's Buffer overflow!";
var ON_OVERFLOW_THROW = 1;
var ON_OVERFLOW_DROP = 2;
var ON_OVERFLOW_SLIDE = 3;
var ON_OVERFLOW_EXPAND = 4;
var zeroBuffer = {
  isEmpty: kTrue,
  put: noop,
  take: noop
};

function ringBuffer(limit, overflowAction) {
  if (limit === void 0) {
    limit = 10;
  }

  var arr = new Array(limit);
  var length = 0;
  var pushIndex = 0;
  var popIndex = 0;

  var push = function push(it) {
    arr[pushIndex] = it;
    pushIndex = (pushIndex + 1) % limit;
    length++;
  };

  var take = function take() {
    if (length != 0) {
      var it = arr[popIndex];
      arr[popIndex] = null;
      length--;
      popIndex = (popIndex + 1) % limit;
      return it;
    }
  };

  var flush = function flush() {
    var items = [];

    while (length) {
      items.push(take());
    }

    return items;
  };

  return {
    isEmpty: function isEmpty() {
      return length == 0;
    },
    put: function put(it) {
      if (length < limit) {
        push(it);
      } else {
        var doubledLimit;

        switch (overflowAction) {
          case ON_OVERFLOW_THROW:
            throw new Error(BUFFER_OVERFLOW);

          case ON_OVERFLOW_SLIDE:
            arr[pushIndex] = it;
            pushIndex = (pushIndex + 1) % limit;
            popIndex = pushIndex;
            break;

          case ON_OVERFLOW_EXPAND:
            doubledLimit = 2 * limit;
            arr = flush();
            length = arr.length;
            pushIndex = arr.length;
            popIndex = 0;
            arr.length = doubledLimit;
            limit = doubledLimit;
            push(it);
            break;

          default: // DROP

        }
      }
    },
    take: take,
    flush: flush
  };
}

var none = function none() {
  return zeroBuffer;
};

exports.n = none;

var fixed = function fixed(limit) {
  return ringBuffer(limit, ON_OVERFLOW_THROW);
};

var dropping = function dropping(limit) {
  return ringBuffer(limit, ON_OVERFLOW_DROP);
};

var sliding = function sliding(limit) {
  return ringBuffer(limit, ON_OVERFLOW_SLIDE);
};

exports.Q = sliding;

var expanding = function expanding(initialSize) {
  return ringBuffer(initialSize, ON_OVERFLOW_EXPAND);
};

exports.e = expanding;
var buffers =
/*#__PURE__*/
Object.freeze({
  none: none,
  fixed: fixed,
  dropping: dropping,
  sliding: sliding,
  expanding: expanding
});
exports.H = buffers;
var TAKE = 'TAKE';
exports.T = TAKE;
var PUT = 'PUT';
exports.P = PUT;
var ALL = 'ALL';
exports.A = ALL;
var RACE = 'RACE';
exports.R = RACE;
var CALL = 'CALL';
exports.C = CALL;
var CPS = 'CPS';
exports.a = CPS;
var FORK = 'FORK';
exports.F = FORK;
var JOIN = 'JOIN';
exports.J = JOIN;
var CANCEL = 'CANCEL';
exports.b = CANCEL;
var SELECT = 'SELECT';
exports.S = SELECT;
var ACTION_CHANNEL = 'ACTION_CHANNEL';
exports.d = ACTION_CHANNEL;
var CANCELLED = 'CANCELLED';
exports.f = CANCELLED;
var FLUSH = 'FLUSH';
exports.g = FLUSH;
var GET_CONTEXT = 'GET_CONTEXT';
exports.G = GET_CONTEXT;
var SET_CONTEXT = 'SET_CONTEXT';
exports.h = SET_CONTEXT;
var effectTypes =
/*#__PURE__*/
Object.freeze({
  TAKE: TAKE,
  PUT: PUT,
  ALL: ALL,
  RACE: RACE,
  CALL: CALL,
  CPS: CPS,
  FORK: FORK,
  JOIN: JOIN,
  CANCEL: CANCEL,
  SELECT: SELECT,
  ACTION_CHANNEL: ACTION_CHANNEL,
  CANCELLED: CANCELLED,
  FLUSH: FLUSH,
  GET_CONTEXT: GET_CONTEXT,
  SET_CONTEXT: SET_CONTEXT
});
exports.W = effectTypes;
var TEST_HINT = '\n(HINT: if you are getting these errors in tests, consider using createMockTask from @redux-saga/testing-utils)';

var makeEffect = function makeEffect(type, payload) {
  var _ref;

  return _ref = {}, _ref[_symbols.IO] = true, _ref.combinator = false, _ref.type = type, _ref.payload = payload, _ref;
};

var isForkEffect = function isForkEffect(eff) {
  return eff && eff[_symbols.IO] && eff.type === FORK;
};

var detach = function detach(eff) {
  if ("development" !== 'production') {
    check(eff, isForkEffect, 'detach(eff): argument must be a fork effect');
  }

  return makeEffect(FORK, (0, _extends2.default)({}, eff.payload, {
    detached: true
  }));
};

exports.I = detach;

function take(patternOrChannel, multicastPattern) {
  if (patternOrChannel === void 0) {
    patternOrChannel = '*';
  }

  if ("development" !== 'production' && arguments.length) {
    check(arguments[0], _is.notUndef, 'take(patternOrChannel): patternOrChannel is undefined');
  }

  if ((0, _is.pattern)(patternOrChannel)) {
    return makeEffect(TAKE, {
      pattern: patternOrChannel
    });
  }

  if ((0, _is.multicast)(patternOrChannel) && (0, _is.notUndef)(multicastPattern) && (0, _is.pattern)(multicastPattern)) {
    return makeEffect(TAKE, {
      channel: patternOrChannel,
      pattern: multicastPattern
    });
  }

  if ((0, _is.channel)(patternOrChannel)) {
    return makeEffect(TAKE, {
      channel: patternOrChannel
    });
  }

  if ("development" !== 'production') {
    throw new Error("take(patternOrChannel): argument " + patternOrChannel + " is not valid channel or a valid pattern");
  }
}

var takeMaybe = function takeMaybe() {
  var eff = take.apply(void 0, arguments);
  eff.payload.maybe = true;
  return eff;
};

exports.X = takeMaybe;

function put(channel$1, action) {
  if ("development" !== 'production') {
    if (arguments.length > 1) {
      check(channel$1, _is.notUndef, 'put(channel, action): argument channel is undefined');
      check(channel$1, _is.channel, "put(channel, action): argument " + channel$1 + " is not a valid channel");
      check(action, _is.notUndef, 'put(channel, action): argument action is undefined');
    } else {
      check(channel$1, _is.notUndef, 'put(action): argument action is undefined');
    }
  }

  if ((0, _is.undef)(action)) {
    action = channel$1; // `undefined` instead of `null` to make default parameter work

    channel$1 = undefined;
  }

  return makeEffect(PUT, {
    channel: channel$1,
    action: action
  });
}

var putResolve = function putResolve() {
  var eff = put.apply(void 0, arguments);
  eff.payload.resolve = true;
  return eff;
};

exports.Z = putResolve;

function all(effects) {
  var eff = makeEffect(ALL, effects);
  eff.combinator = true;
  return eff;
}

function race(effects) {
  var eff = makeEffect(RACE, effects);
  eff.combinator = true;
  return eff;
} // this match getFnCallDescriptor logic


var validateFnDescriptor = function validateFnDescriptor(effectName, fnDescriptor) {
  check(fnDescriptor, _is.notUndef, effectName + ": argument fn is undefined or null");

  if ((0, _is.func)(fnDescriptor)) {
    return;
  }

  var context = null;
  var fn;

  if ((0, _is.array)(fnDescriptor)) {
    context = fnDescriptor[0];
    fn = fnDescriptor[1];
    check(fn, _is.notUndef, effectName + ": argument of type [context, fn] has undefined or null `fn`");
  } else if ((0, _is.object)(fnDescriptor)) {
    context = fnDescriptor.context;
    fn = fnDescriptor.fn;
    check(fn, _is.notUndef, effectName + ": argument of type {context, fn} has undefined or null `fn`");
  } else {
    check(fnDescriptor, _is.func, effectName + ": argument fn is not function");
    return;
  }

  if (context && (0, _is.string)(fn)) {
    check(context[fn], _is.func, effectName + ": context arguments has no such method - \"" + fn + "\"");
    return;
  }

  check(fn, _is.func, effectName + ": unpacked fn argument (from [context, fn] or {context, fn}) is not a function");
};

function getFnCallDescriptor(fnDescriptor, args) {
  var context = null;
  var fn;

  if ((0, _is.func)(fnDescriptor)) {
    fn = fnDescriptor;
  } else {
    if ((0, _is.array)(fnDescriptor)) {
      context = fnDescriptor[0];
      fn = fnDescriptor[1];
    } else {
      context = fnDescriptor.context;
      fn = fnDescriptor.fn;
    }

    if (context && (0, _is.string)(fn) && (0, _is.func)(context[fn])) {
      fn = context[fn];
    }
  }

  return {
    context: context,
    fn: fn,
    args: args
  };
}

var isNotDelayEffect = function isNotDelayEffect(fn) {
  return fn !== delay;
};

function call(fnDescriptor) {
  for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }

  if ("development" !== 'production') {
    var arg0 = typeof args[0] === 'number' ? args[0] : 'ms';
    check(fnDescriptor, isNotDelayEffect, "instead of writing `yield call(delay, " + arg0 + ")` where delay is an effect from `redux-saga/effects` you should write `yield delay(" + arg0 + ")`");
    validateFnDescriptor('call', fnDescriptor);
  }

  return makeEffect(CALL, getFnCallDescriptor(fnDescriptor, args));
}

function apply(context, fn, args) {
  if (args === void 0) {
    args = [];
  }

  var fnDescriptor = [context, fn];

  if ("development" !== 'production') {
    validateFnDescriptor('apply', fnDescriptor);
  }

  return makeEffect(CALL, getFnCallDescriptor([context, fn], args));
}

function cps(fnDescriptor) {
  if ("development" !== 'production') {
    validateFnDescriptor('cps', fnDescriptor);
  }

  for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
    args[_key2 - 1] = arguments[_key2];
  }

  return makeEffect(CPS, getFnCallDescriptor(fnDescriptor, args));
}

function fork(fnDescriptor) {
  if ("development" !== 'production') {
    validateFnDescriptor('fork', fnDescriptor);
  }

  for (var _len3 = arguments.length, args = new Array(_len3 > 1 ? _len3 - 1 : 0), _key3 = 1; _key3 < _len3; _key3++) {
    args[_key3 - 1] = arguments[_key3];
  }

  return makeEffect(FORK, getFnCallDescriptor(fnDescriptor, args));
}

function spawn(fnDescriptor) {
  if ("development" !== 'production') {
    validateFnDescriptor('spawn', fnDescriptor);
  }

  for (var _len4 = arguments.length, args = new Array(_len4 > 1 ? _len4 - 1 : 0), _key4 = 1; _key4 < _len4; _key4++) {
    args[_key4 - 1] = arguments[_key4];
  }

  return detach(fork.apply(void 0, [fnDescriptor].concat(args)));
}

function join(taskOrTasks) {
  if ("development" !== 'production') {
    if (arguments.length > 1) {
      throw new Error('join(...tasks) is not supported any more. Please use join([...tasks]) to join multiple tasks.');
    }

    if ((0, _is.array)(taskOrTasks)) {
      taskOrTasks.forEach(function (t) {
        check(t, _is.task, "join([...tasks]): argument " + t + " is not a valid Task object " + TEST_HINT);
      });
    } else {
      check(taskOrTasks, _is.task, "join(task): argument " + taskOrTasks + " is not a valid Task object " + TEST_HINT);
    }
  }

  return makeEffect(JOIN, taskOrTasks);
}

function cancel(taskOrTasks) {
  if (taskOrTasks === void 0) {
    taskOrTasks = _symbols.SELF_CANCELLATION;
  }

  if ("development" !== 'production') {
    if (arguments.length > 1) {
      throw new Error('cancel(...tasks) is not supported any more. Please use cancel([...tasks]) to cancel multiple tasks.');
    }

    if ((0, _is.array)(taskOrTasks)) {
      taskOrTasks.forEach(function (t) {
        check(t, _is.task, "cancel([...tasks]): argument " + t + " is not a valid Task object " + TEST_HINT);
      });
    } else if (taskOrTasks !== _symbols.SELF_CANCELLATION && (0, _is.notUndef)(taskOrTasks)) {
      check(taskOrTasks, _is.task, "cancel(task): argument " + taskOrTasks + " is not a valid Task object " + TEST_HINT);
    }
  }

  return makeEffect(CANCEL, taskOrTasks);
}

function select(selector) {
  if (selector === void 0) {
    selector = identity;
  }

  for (var _len5 = arguments.length, args = new Array(_len5 > 1 ? _len5 - 1 : 0), _key5 = 1; _key5 < _len5; _key5++) {
    args[_key5 - 1] = arguments[_key5];
  }

  if ("development" !== 'production' && arguments.length) {
    check(arguments[0], _is.notUndef, 'select(selector, [...]): argument selector is undefined');
    check(selector, _is.func, "select(selector, [...]): argument " + selector + " is not a function");
  }

  return makeEffect(SELECT, {
    selector: selector,
    args: args
  });
}
/**
  channel(pattern, [buffer])    => creates a proxy channel for store actions
**/


function actionChannel(pattern$1, buffer$1) {
  if ("development" !== 'production') {
    check(pattern$1, _is.pattern, 'actionChannel(pattern,...): argument pattern is not valid');

    if (arguments.length > 1) {
      check(buffer$1, _is.notUndef, 'actionChannel(pattern, buffer): argument buffer is undefined');
      check(buffer$1, _is.buffer, "actionChannel(pattern, buffer): argument " + buffer$1 + " is not a valid buffer");
    }
  }

  return makeEffect(ACTION_CHANNEL, {
    pattern: pattern$1,
    buffer: buffer$1
  });
}

function cancelled() {
  return makeEffect(CANCELLED, {});
}

function flush(channel$1) {
  if ("development" !== 'production') {
    check(channel$1, _is.channel, "flush(channel): argument " + channel$1 + " is not valid channel");
  }

  return makeEffect(FLUSH, channel$1);
}

function getContext(prop) {
  if ("development" !== 'production') {
    check(prop, _is.string, "getContext(prop): argument " + prop + " is not a string");
  }

  return makeEffect(GET_CONTEXT, prop);
}

function setContext(props) {
  if ("development" !== 'production') {
    check(props, _is.object, createSetContextWarning(null, props));
  }

  return makeEffect(SET_CONTEXT, props);
}

var delay =
/*#__PURE__*/
call.bind(null, _delayP.default);
exports.U = delay;
},{"@redux-saga/symbols":"../node_modules/@redux-saga/symbols/dist/redux-saga-symbols.esm.js","@babel/runtime/helpers/esm/extends":"../node_modules/@babel/runtime/helpers/esm/extends.js","@redux-saga/is":"../node_modules/@redux-saga/is/dist/redux-saga-is.esm.js","@redux-saga/delay-p":"../node_modules/@redux-saga/delay-p/dist/redux-saga-delay-p.esm.js"}],"../node_modules/@redux-saga/core/dist/redux-saga-effects.esm.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.debounce = debounce;
exports.retry = retry$1;
exports.takeEvery = takeEvery$1;
exports.takeLatest = takeLatest$1;
exports.takeLeading = takeLeading$1;
exports.throttle = throttle$1;
Object.defineProperty(exports, "actionChannel", {
  enumerable: true,
  get: function () {
    return _io427945dd.O;
  }
});
Object.defineProperty(exports, "all", {
  enumerable: true,
  get: function () {
    return _io427945dd._;
  }
});
Object.defineProperty(exports, "apply", {
  enumerable: true,
  get: function () {
    return _io427945dd.$;
  }
});
Object.defineProperty(exports, "call", {
  enumerable: true,
  get: function () {
    return _io427945dd.N;
  }
});
Object.defineProperty(exports, "cancel", {
  enumerable: true,
  get: function () {
    return _io427945dd.M;
  }
});
Object.defineProperty(exports, "cancelled", {
  enumerable: true,
  get: function () {
    return _io427945dd.a4;
  }
});
Object.defineProperty(exports, "cps", {
  enumerable: true,
  get: function () {
    return _io427945dd.a0;
  }
});
Object.defineProperty(exports, "delay", {
  enumerable: true,
  get: function () {
    return _io427945dd.U;
  }
});
Object.defineProperty(exports, "effectTypes", {
  enumerable: true,
  get: function () {
    return _io427945dd.W;
  }
});
Object.defineProperty(exports, "flush", {
  enumerable: true,
  get: function () {
    return _io427945dd.a5;
  }
});
Object.defineProperty(exports, "fork", {
  enumerable: true,
  get: function () {
    return _io427945dd.L;
  }
});
Object.defineProperty(exports, "getContext", {
  enumerable: true,
  get: function () {
    return _io427945dd.a6;
  }
});
Object.defineProperty(exports, "join", {
  enumerable: true,
  get: function () {
    return _io427945dd.a2;
  }
});
Object.defineProperty(exports, "put", {
  enumerable: true,
  get: function () {
    return _io427945dd.Y;
  }
});
Object.defineProperty(exports, "putResolve", {
  enumerable: true,
  get: function () {
    return _io427945dd.Z;
  }
});
Object.defineProperty(exports, "race", {
  enumerable: true,
  get: function () {
    return _io427945dd.V;
  }
});
Object.defineProperty(exports, "select", {
  enumerable: true,
  get: function () {
    return _io427945dd.a3;
  }
});
Object.defineProperty(exports, "setContext", {
  enumerable: true,
  get: function () {
    return _io427945dd.a7;
  }
});
Object.defineProperty(exports, "spawn", {
  enumerable: true,
  get: function () {
    return _io427945dd.a1;
  }
});
Object.defineProperty(exports, "take", {
  enumerable: true,
  get: function () {
    return _io427945dd.K;
  }
});
Object.defineProperty(exports, "takeMaybe", {
  enumerable: true,
  get: function () {
    return _io427945dd.X;
  }
});

require("@redux-saga/symbols");

require("@babel/runtime/helpers/esm/extends");

var _is = require("@redux-saga/is");

var _io427945dd = require("./io-427945dd.js");

require("@redux-saga/delay-p");

var done = function done(value) {
  return {
    done: true,
    value: value
  };
};

var qEnd = {};

function safeName(patternOrChannel) {
  if ((0, _is.channel)(patternOrChannel)) {
    return 'channel';
  }

  if ((0, _is.stringableFunc)(patternOrChannel)) {
    return String(patternOrChannel);
  }

  if ((0, _is.func)(patternOrChannel)) {
    return patternOrChannel.name;
  }

  return String(patternOrChannel);
}

function fsmIterator(fsm, startState, name) {
  var stateUpdater,
      errorState,
      effect,
      nextState = startState;

  function next(arg, error) {
    if (nextState === qEnd) {
      return done(arg);
    }

    if (error && !errorState) {
      nextState = qEnd;
      throw error;
    } else {
      stateUpdater && stateUpdater(arg);
      var currentState = error ? fsm[errorState](error) : fsm[nextState]();
      nextState = currentState.nextState;
      effect = currentState.effect;
      stateUpdater = currentState.stateUpdater;
      errorState = currentState.errorState;
      return nextState === qEnd ? done(arg) : effect;
    }
  }

  return (0, _io427945dd.q)(next, function (error) {
    return next(null, error);
  }, name);
}

function takeEvery(patternOrChannel, worker) {
  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }

  var yTake = {
    done: false,
    value: (0, _io427945dd.K)(patternOrChannel)
  };

  var yFork = function yFork(ac) {
    return {
      done: false,
      value: _io427945dd.L.apply(void 0, [worker].concat(args, [ac]))
    };
  };

  var action,
      setAction = function setAction(ac) {
    return action = ac;
  };

  return fsmIterator({
    q1: function q1() {
      return {
        nextState: 'q2',
        effect: yTake,
        stateUpdater: setAction
      };
    },
    q2: function q2() {
      return {
        nextState: 'q1',
        effect: yFork(action)
      };
    }
  }, 'q1', "takeEvery(" + safeName(patternOrChannel) + ", " + worker.name + ")");
}

function takeLatest(patternOrChannel, worker) {
  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }

  var yTake = {
    done: false,
    value: (0, _io427945dd.K)(patternOrChannel)
  };

  var yFork = function yFork(ac) {
    return {
      done: false,
      value: _io427945dd.L.apply(void 0, [worker].concat(args, [ac]))
    };
  };

  var yCancel = function yCancel(task) {
    return {
      done: false,
      value: (0, _io427945dd.M)(task)
    };
  };

  var task, action;

  var setTask = function setTask(t) {
    return task = t;
  };

  var setAction = function setAction(ac) {
    return action = ac;
  };

  return fsmIterator({
    q1: function q1() {
      return {
        nextState: 'q2',
        effect: yTake,
        stateUpdater: setAction
      };
    },
    q2: function q2() {
      return task ? {
        nextState: 'q3',
        effect: yCancel(task)
      } : {
        nextState: 'q1',
        effect: yFork(action),
        stateUpdater: setTask
      };
    },
    q3: function q3() {
      return {
        nextState: 'q1',
        effect: yFork(action),
        stateUpdater: setTask
      };
    }
  }, 'q1', "takeLatest(" + safeName(patternOrChannel) + ", " + worker.name + ")");
}

function takeLeading(patternOrChannel, worker) {
  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }

  var yTake = {
    done: false,
    value: (0, _io427945dd.K)(patternOrChannel)
  };

  var yCall = function yCall(ac) {
    return {
      done: false,
      value: _io427945dd.N.apply(void 0, [worker].concat(args, [ac]))
    };
  };

  var action;

  var setAction = function setAction(ac) {
    return action = ac;
  };

  return fsmIterator({
    q1: function q1() {
      return {
        nextState: 'q2',
        effect: yTake,
        stateUpdater: setAction
      };
    },
    q2: function q2() {
      return {
        nextState: 'q1',
        effect: yCall(action)
      };
    }
  }, 'q1', "takeLeading(" + safeName(patternOrChannel) + ", " + worker.name + ")");
}

function throttle(delayLength, pattern, worker) {
  for (var _len = arguments.length, args = new Array(_len > 3 ? _len - 3 : 0), _key = 3; _key < _len; _key++) {
    args[_key - 3] = arguments[_key];
  }

  var action, channel;
  var yActionChannel = {
    done: false,
    value: (0, _io427945dd.O)(pattern, (0, _io427945dd.Q)(1))
  };

  var yTake = function yTake() {
    return {
      done: false,
      value: (0, _io427945dd.K)(channel)
    };
  };

  var yFork = function yFork(ac) {
    return {
      done: false,
      value: _io427945dd.L.apply(void 0, [worker].concat(args, [ac]))
    };
  };

  var yDelay = {
    done: false,
    value: (0, _io427945dd.U)(delayLength)
  };

  var setAction = function setAction(ac) {
    return action = ac;
  };

  var setChannel = function setChannel(ch) {
    return channel = ch;
  };

  return fsmIterator({
    q1: function q1() {
      return {
        nextState: 'q2',
        effect: yActionChannel,
        stateUpdater: setChannel
      };
    },
    q2: function q2() {
      return {
        nextState: 'q3',
        effect: yTake(),
        stateUpdater: setAction
      };
    },
    q3: function q3() {
      return {
        nextState: 'q4',
        effect: yFork(action)
      };
    },
    q4: function q4() {
      return {
        nextState: 'q2',
        effect: yDelay
      };
    }
  }, 'q1', "throttle(" + safeName(pattern) + ", " + worker.name + ")");
}

function retry(maxTries, delayLength, fn) {
  var counter = maxTries;

  for (var _len = arguments.length, args = new Array(_len > 3 ? _len - 3 : 0), _key = 3; _key < _len; _key++) {
    args[_key - 3] = arguments[_key];
  }

  var yCall = {
    done: false,
    value: _io427945dd.N.apply(void 0, [fn].concat(args))
  };
  var yDelay = {
    done: false,
    value: (0, _io427945dd.U)(delayLength)
  };
  return fsmIterator({
    q1: function q1() {
      return {
        nextState: 'q2',
        effect: yCall,
        errorState: 'q10'
      };
    },
    q2: function q2() {
      return {
        nextState: qEnd
      };
    },
    q10: function q10(error) {
      counter -= 1;

      if (counter <= 0) {
        throw error;
      }

      return {
        nextState: 'q1',
        effect: yDelay
      };
    }
  }, 'q1', "retry(" + fn.name + ")");
}

function debounceHelper(delayLength, patternOrChannel, worker) {
  for (var _len = arguments.length, args = new Array(_len > 3 ? _len - 3 : 0), _key = 3; _key < _len; _key++) {
    args[_key - 3] = arguments[_key];
  }

  var action, raceOutput;
  var yTake = {
    done: false,
    value: (0, _io427945dd.K)(patternOrChannel)
  };
  var yRace = {
    done: false,
    value: (0, _io427945dd.V)({
      action: (0, _io427945dd.K)(patternOrChannel),
      debounce: (0, _io427945dd.U)(delayLength)
    })
  };

  var yFork = function yFork(ac) {
    return {
      done: false,
      value: _io427945dd.L.apply(void 0, [worker].concat(args, [ac]))
    };
  };

  var yNoop = function yNoop(value) {
    return {
      done: false,
      value: value
    };
  };

  var setAction = function setAction(ac) {
    return action = ac;
  };

  var setRaceOutput = function setRaceOutput(ro) {
    return raceOutput = ro;
  };

  return fsmIterator({
    q1: function q1() {
      return {
        nextState: 'q2',
        effect: yTake,
        stateUpdater: setAction
      };
    },
    q2: function q2() {
      return {
        nextState: 'q3',
        effect: yRace,
        stateUpdater: setRaceOutput
      };
    },
    q3: function q3() {
      return raceOutput.debounce ? {
        nextState: 'q1',
        effect: yFork(action)
      } : {
        nextState: 'q2',
        effect: yNoop(raceOutput.action),
        stateUpdater: setAction
      };
    }
  }, 'q1', "debounce(" + safeName(patternOrChannel) + ", " + worker.name + ")");
}

var validateTakeEffect = function validateTakeEffect(fn, patternOrChannel, worker) {
  (0, _io427945dd.c)(patternOrChannel, _is.notUndef, fn.name + " requires a pattern or channel");
  (0, _io427945dd.c)(worker, _is.notUndef, fn.name + " requires a saga parameter");
};

function takeEvery$1(patternOrChannel, worker) {
  if ("development" !== 'production') {
    validateTakeEffect(takeEvery$1, patternOrChannel, worker);
  }

  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }

  return _io427945dd.L.apply(void 0, [takeEvery, patternOrChannel, worker].concat(args));
}

function takeLatest$1(patternOrChannel, worker) {
  if ("development" !== 'production') {
    validateTakeEffect(takeLatest$1, patternOrChannel, worker);
  }

  for (var _len2 = arguments.length, args = new Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
    args[_key2 - 2] = arguments[_key2];
  }

  return _io427945dd.L.apply(void 0, [takeLatest, patternOrChannel, worker].concat(args));
}

function takeLeading$1(patternOrChannel, worker) {
  if ("development" !== 'production') {
    validateTakeEffect(takeLeading$1, patternOrChannel, worker);
  }

  for (var _len3 = arguments.length, args = new Array(_len3 > 2 ? _len3 - 2 : 0), _key3 = 2; _key3 < _len3; _key3++) {
    args[_key3 - 2] = arguments[_key3];
  }

  return _io427945dd.L.apply(void 0, [takeLeading, patternOrChannel, worker].concat(args));
}

function throttle$1(ms, pattern, worker) {
  if ("development" !== 'production') {
    (0, _io427945dd.c)(pattern, _is.notUndef, 'throttle requires a pattern');
    (0, _io427945dd.c)(worker, _is.notUndef, 'throttle requires a saga parameter');
  }

  for (var _len4 = arguments.length, args = new Array(_len4 > 3 ? _len4 - 3 : 0), _key4 = 3; _key4 < _len4; _key4++) {
    args[_key4 - 3] = arguments[_key4];
  }

  return _io427945dd.L.apply(void 0, [throttle, ms, pattern, worker].concat(args));
}

function retry$1(maxTries, delayLength, worker) {
  for (var _len5 = arguments.length, args = new Array(_len5 > 3 ? _len5 - 3 : 0), _key5 = 3; _key5 < _len5; _key5++) {
    args[_key5 - 3] = arguments[_key5];
  }

  return _io427945dd.N.apply(void 0, [retry, maxTries, delayLength, worker].concat(args));
}

function debounce(delayLength, pattern, worker) {
  for (var _len6 = arguments.length, args = new Array(_len6 > 3 ? _len6 - 3 : 0), _key6 = 3; _key6 < _len6; _key6++) {
    args[_key6 - 3] = arguments[_key6];
  }

  return _io427945dd.L.apply(void 0, [debounceHelper, delayLength, pattern, worker].concat(args));
}
},{"@redux-saga/symbols":"../node_modules/@redux-saga/symbols/dist/redux-saga-symbols.esm.js","@babel/runtime/helpers/esm/extends":"../node_modules/@babel/runtime/helpers/esm/extends.js","@redux-saga/is":"../node_modules/@redux-saga/is/dist/redux-saga-is.esm.js","./io-427945dd.js":"../node_modules/@redux-saga/core/dist/io-427945dd.js","@redux-saga/delay-p":"../node_modules/@redux-saga/delay-p/dist/redux-saga-delay-p.esm.js"}],"../node_modules/redux-saga/dist/redux-saga-effects-npm-proxy.esm.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _effects = require("@redux-saga/core/effects");

Object.keys(_effects).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function () {
      return _effects[key];
    }
  });
});
},{"@redux-saga/core/effects":"../node_modules/@redux-saga/core/dist/redux-saga-effects.esm.js"}],"redux/nameListStore.ts":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getRankedNames = exports.isLoaded = exports.nameListReducer = exports.nameListActions = void 0;

var __assign = void 0 && (void 0).__assign || function () {
  __assign = Object.assign || function (t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];

      for (var p in s) {
        if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
      }
    }

    return t;
  };

  return __assign.apply(this, arguments);
};

var initialState = [];
var nameListActions = {
  importNames: function importNames(names) {
    return {
      type: "nameList/importNames",
      names: names
    };
  },
  upvote: function upvote(id) {
    return {
      type: "nameList/upvote",
      id: id
    };
  },
  downvote: function downvote(id) {
    return {
      type: "nameList/downvote",
      id: id
    };
  }
};
exports.nameListActions = nameListActions;

var nameListReducer = function nameListReducer(state, action) {
  if (state === void 0) {
    state = initialState;
  }

  switch (action.type) {
    case "nameList/importNames":
      return action.names.map(function (name) {
        return {
          value: name,
          upvotes: 0,
          downvotes: 0
        };
      });

    case "nameList/upvote":
      {
        var name = state[action.id];
        return state.map(function (name, id) {
          return __assign(__assign({}, name), {
            upvotes: id === action.id ? name.upvotes + 1 : name.upvotes
          });
        });
      }

    case "nameList/downvote":
      {
        var name = state[action.id];
        return state.map(function (name, id) {
          return __assign(__assign({}, name), {
            downvotes: id === action.id ? name.downvotes + 1 : name.downvotes
          });
        });
      }
  }

  return state;
};

exports.nameListReducer = nameListReducer;

var isLoaded = function isLoaded(state) {
  return state.names.length > 0;
};

exports.isLoaded = isLoaded;

var getRankedNames = function getRankedNames(state) {
  var names = state.names.filter(function (name) {
    return name.upvotes + name.downvotes > 0;
  }).map(function (name) {
    return {
      name: name.value,
      score: name.upvotes / (name.upvotes + name.downvotes)
    };
  });
  names.sort(function (a, b) {
    return b.score - a.score || a.name.localeCompare(b.name);
  });
  return names;
};

exports.getRankedNames = getRankedNames;
},{}],"redux/stackStore.ts":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getCurrentName = exports.getNameIdsInStack = exports.stackReducer = exports.stackStoreActions = void 0;

var __assign = void 0 && (void 0).__assign || function () {
  __assign = Object.assign || function (t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];

      for (var p in s) {
        if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
      }
    }

    return t;
  };

  return __assign.apply(this, arguments);
};

var __spreadArrays = void 0 && (void 0).__spreadArrays || function () {
  for (var s = 0, i = 0, il = arguments.length; i < il; i++) {
    s += arguments[i].length;
  }

  for (var r = Array(s), k = 0, i = 0; i < il; i++) {
    for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++) {
      r[k] = a[j];
    }
  }

  return r;
};

var initialState = {
  currentNameId: undefined,
  remainingNameIds: [],
  allNameIds: []
};
var stackStoreActions = {
  init: function init(nameIds) {
    return {
      type: "stack/init",
      nameIds: nameIds
    };
  },
  pickCard: function pickCard(id) {
    return {
      type: "stack/pickCard",
      id: id
    };
  }
};
exports.stackStoreActions = stackStoreActions;

var stackReducer = function stackReducer(state, action) {
  if (state === void 0) {
    state = initialState;
  }

  switch (action.type) {
    case "stack/init":
      return __assign(__assign({}, state), {
        currentNameId: undefined,
        remainingNameIds: __spreadArrays(action.nameIds),
        allNameIds: __spreadArrays(action.nameIds)
      });

    case "stack/pickCard":
      {
        var remainingIds = state.remainingNameIds.filter(function (id) {
          return id !== action.id;
        });
        return __assign(__assign({}, state), {
          currentNameId: action.id,
          remainingNameIds: remainingIds.length > 0 ? remainingIds : __spreadArrays(state.allNameIds)
        });
      }
  }

  return state;
};

exports.stackReducer = stackReducer;

var getNameIdsInStack = function getNameIdsInStack(state) {
  return state.stack.remainingNameIds;
};

exports.getNameIdsInStack = getNameIdsInStack;

var getCurrentName = function getCurrentName(state) {
  return state.names[state.stack.currentNameId];
};

exports.getCurrentName = getCurrentName;
},{}],"utils/array.ts":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.pickRandomElement = void 0;

var pickRandomElement = function pickRandomElement(getRandomNumber) {
  return function (array) {
    var index = Math.floor(getRandomNumber() * array.length);
    return array[index];
  };
};

exports.pickRandomElement = pickRandomElement;
},{}],"models/SubPage.tsx":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.SubPage = void 0;
var SubPage;
exports.SubPage = SubPage;

(function (SubPage) {
  SubPage[SubPage["Voting"] = 0] = "Voting";
  SubPage[SubPage["Results"] = 1] = "Results";
  SubPage[SubPage["NameList"] = 2] = "NameList";
})(SubPage || (exports.SubPage = SubPage = {}));
},{}],"redux/appStore.ts":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getSubPage = exports.appReducer = exports.appAction = void 0;

var _SubPage = require("../models/SubPage");

var __assign = void 0 && (void 0).__assign || function () {
  __assign = Object.assign || function (t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];

      for (var p in s) {
        if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
      }
    }

    return t;
  };

  return __assign.apply(this, arguments);
};

var initialState = {
  subPage: _SubPage.SubPage.NameList
};
var appAction = {
  selectSubPage: function selectSubPage(subPage) {
    return {
      type: "app/selectSubPage",
      subPage: subPage
    };
  }
};
exports.appAction = appAction;

var appReducer = function appReducer(state, action) {
  if (state === void 0) {
    state = initialState;
  }

  switch (action.type) {
    case "app/selectSubPage":
      return __assign(__assign({}, state), {
        subPage: action.subPage
      });
  }

  return state;
};

exports.appReducer = appReducer;

var getSubPage = function getSubPage(state) {
  return state.app.subPage;
};

exports.getSubPage = getSubPage;
},{"../models/SubPage":"models/SubPage.tsx"}],"redux/sagas.ts":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.importFileSaga = importFileSaga;
exports.importNamesSaga = importNamesSaga;
exports.sagas = sagas;
exports.sagaActions = void 0;

var _effects = require("redux-saga/effects");

var _nameListStore = require("./nameListStore");

var _stackStore = require("./stackStore");

var _array = require("../utils/array");

var _appStore = require("./appStore");

var _SubPage = require("../models/SubPage");

var __generator = void 0 && (void 0).__generator || function (thisArg, body) {
  var _ = {
    label: 0,
    sent: function sent() {
      if (t[0] & 1) throw t[1];
      return t[1];
    },
    trys: [],
    ops: []
  },
      f,
      y,
      t,
      g;
  return g = {
    next: verb(0),
    "throw": verb(1),
    "return": verb(2)
  }, typeof Symbol === "function" && (g[Symbol.iterator] = function () {
    return this;
  }), g;

  function verb(n) {
    return function (v) {
      return step([n, v]);
    };
  }

  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");

    while (_) {
      try {
        if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
        if (y = 0, t) op = [op[0] & 2, t.value];

        switch (op[0]) {
          case 0:
          case 1:
            t = op;
            break;

          case 4:
            _.label++;
            return {
              value: op[1],
              done: false
            };

          case 5:
            _.label++;
            y = op[1];
            op = [0];
            continue;

          case 7:
            op = _.ops.pop();

            _.trys.pop();

            continue;

          default:
            if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
              _ = 0;
              continue;
            }

            if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
              _.label = op[1];
              break;
            }

            if (op[0] === 6 && _.label < t[1]) {
              _.label = t[1];
              t = op;
              break;
            }

            if (t && _.label < t[2]) {
              _.label = t[2];

              _.ops.push(op);

              break;
            }

            if (t[2]) _.ops.pop();

            _.trys.pop();

            continue;
        }

        op = body.call(thisArg, _);
      } catch (e) {
        op = [6, e];
        y = 0;
      } finally {
        f = t = 0;
      }
    }

    if (op[0] & 5) throw op[1];
    return {
      value: op[0] ? op[1] : void 0,
      done: true
    };
  }
};

var ACTION_IMPORT_FILE = "importFile";
var ACTION_IMPORT_NAMES = "importNames";
var ACTION_UPVOTE = "upvote";
var ACTION_DOWNVOTE = "downvote";
var ACTION_NEXT_CARD = "nextCard";
var sagaActions = {
  importFile: function importFile(fileContent) {
    return {
      type: ACTION_IMPORT_FILE,
      fileContent: fileContent
    };
  },
  importNames: function importNames(names) {
    return {
      type: ACTION_IMPORT_NAMES,
      names: names
    };
  },
  upvote: function upvote() {
    return {
      type: ACTION_UPVOTE
    };
  },
  downvote: function downvote() {
    return {
      type: ACTION_DOWNVOTE
    };
  },
  nextCard: function nextCard() {
    return {
      type: ACTION_NEXT_CARD
    };
  }
};
exports.sagaActions = sagaActions;

function importFileSaga(action) {
  var array;
  return __generator(this, function (_a) {
    switch (_a.label) {
      case 0:
        array = action.fileContent.split("\n").map(function (string) {
          return string.replace(/^\s+|\s+$/g, "");
        }).filter(function (string) {
          return string.length > 0;
        });
        return [4
        /*yield*/
        , (0, _effects.put)(sagaActions.importNames(array))];

      case 1:
        _a.sent();

        return [4
        /*yield*/
        , (0, _effects.put)(_appStore.appAction.selectSubPage(_SubPage.SubPage.Voting))];

      case 2:
        _a.sent();

        return [2
        /*return*/
        ];
    }
  });
}

function importNamesSaga(action) {
  return __generator(this, function (_a) {
    switch (_a.label) {
      case 0:
        return [4
        /*yield*/
        , (0, _effects.put)(_nameListStore.nameListActions.importNames(action.names))];

      case 1:
        _a.sent();

        return [4
        /*yield*/
        , (0, _effects.put)(_stackStore.stackStoreActions.init(action.names.map(function (_, i) {
          return i;
        })))];

      case 2:
        _a.sent();

        return [4
        /*yield*/
        , (0, _effects.put)(sagaActions.nextCard())];

      case 3:
        _a.sent();

        return [2
        /*return*/
        ];
    }
  });
}

function nextCardSaga(action) {
  var nameIds, pickedCardId;
  return __generator(this, function (_a) {
    switch (_a.label) {
      case 0:
        return [4
        /*yield*/
        , (0, _effects.select)(_stackStore.getNameIdsInStack)];

      case 1:
        nameIds = _a.sent();
        return [4
        /*yield*/
        , (0, _effects.call)((0, _array.pickRandomElement)(Math.random), nameIds)];

      case 2:
        pickedCardId = _a.sent();
        return [4
        /*yield*/
        , (0, _effects.put)(_stackStore.stackStoreActions.pickCard(pickedCardId))];

      case 3:
        _a.sent();

        return [2
        /*return*/
        ];
    }
  });
}

function voteSaga(voteActionCreator) {
  var id;
  return __generator(this, function (_a) {
    switch (_a.label) {
      case 0:
        return [4
        /*yield*/
        , (0, _effects.select)(function (state) {
          return state.stack.currentNameId;
        })];

      case 1:
        id = _a.sent();
        return [4
        /*yield*/
        , (0, _effects.put)(voteActionCreator(id))];

      case 2:
        _a.sent();

        return [4
        /*yield*/
        , (0, _effects.put)(sagaActions.nextCard())];

      case 3:
        _a.sent();

        return [2
        /*return*/
        ];
    }
  });
}

function upvoteSaga() {
  return __generator(this, function (_a) {
    switch (_a.label) {
      case 0:
        return [4
        /*yield*/
        , voteSaga(_nameListStore.nameListActions.upvote)];

      case 1:
        _a.sent();

        return [2
        /*return*/
        ];
    }
  });
}

function downvoteSaga() {
  return __generator(this, function (_a) {
    switch (_a.label) {
      case 0:
        return [4
        /*yield*/
        , voteSaga(_nameListStore.nameListActions.downvote)];

      case 1:
        _a.sent();

        return [2
        /*return*/
        ];
    }
  });
}

function sagas() {
  return __generator(this, function (_a) {
    switch (_a.label) {
      case 0:
        return [4
        /*yield*/
        , (0, _effects.takeEvery)(ACTION_IMPORT_FILE, importFileSaga)];

      case 1:
        _a.sent();

        return [4
        /*yield*/
        , (0, _effects.takeEvery)(ACTION_IMPORT_NAMES, importNamesSaga)];

      case 2:
        _a.sent();

        return [4
        /*yield*/
        , (0, _effects.takeEvery)(ACTION_NEXT_CARD, nextCardSaga)];

      case 3:
        _a.sent();

        return [4
        /*yield*/
        , (0, _effects.takeEvery)(ACTION_UPVOTE, upvoteSaga)];

      case 4:
        _a.sent();

        return [4
        /*yield*/
        , (0, _effects.takeEvery)(ACTION_DOWNVOTE, downvoteSaga)];

      case 5:
        _a.sent();

        return [2
        /*return*/
        ];
    }
  });
}
},{"redux-saga/effects":"../node_modules/redux-saga/dist/redux-saga-effects-npm-proxy.esm.js","./nameListStore":"redux/nameListStore.ts","./stackStore":"redux/stackStore.ts","../utils/array":"utils/array.ts","./appStore":"redux/appStore.ts","../models/SubPage":"models/SubPage.tsx"}],"../node_modules/parcel-bundler/src/builtins/bundle-url.js":[function(require,module,exports) {
var bundleURL = null;

function getBundleURLCached() {
  if (!bundleURL) {
    bundleURL = getBundleURL();
  }

  return bundleURL;
}

function getBundleURL() {
  // Attempt to find the URL of the current script and use that as the base URL
  try {
    throw new Error();
  } catch (err) {
    var matches = ('' + err.stack).match(/(https?|file|ftp|chrome-extension|moz-extension):\/\/[^)\n]+/g);

    if (matches) {
      return getBaseURL(matches[0]);
    }
  }

  return '/';
}

function getBaseURL(url) {
  return ('' + url).replace(/^((?:https?|file|ftp|chrome-extension|moz-extension):\/\/.+)\/[^/]+$/, '$1') + '/';
}

exports.getBundleURL = getBundleURLCached;
exports.getBaseURL = getBaseURL;
},{}],"../node_modules/parcel-bundler/src/builtins/css-loader.js":[function(require,module,exports) {
var bundle = require('./bundle-url');

function updateLink(link) {
  var newLink = link.cloneNode();

  newLink.onload = function () {
    link.remove();
  };

  newLink.href = link.href.split('?')[0] + '?' + Date.now();
  link.parentNode.insertBefore(newLink, link.nextSibling);
}

var cssTimeout = null;

function reloadCSS() {
  if (cssTimeout) {
    return;
  }

  cssTimeout = setTimeout(function () {
    var links = document.querySelectorAll('link[rel="stylesheet"]');

    for (var i = 0; i < links.length; i++) {
      if (bundle.getBaseURL(links[i].href) === bundle.getBundleURL()) {
        updateLink(links[i]);
      }
    }

    cssTimeout = null;
  }, 50);
}

module.exports = reloadCSS;
},{"./bundle-url":"../node_modules/parcel-bundler/src/builtins/bundle-url.js"}],"voting/NameCard.css":[function(require,module,exports) {
var reloadCSS = require('_css_loader');

module.hot.dispose(reloadCSS);
module.hot.accept(reloadCSS);
module.exports = {
  "cardWrapper": "_cardWrapper_df678",
  "card": "_card_df678",
  "animation-card": "_animation-card_df678",
  "title": "_title_df678",
  "animation-title": "_animation-title_df678"
};
},{"_css_loader":"../node_modules/parcel-bundler/src/builtins/css-loader.js"}],"voting/NameCard.tsx":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.NameCard = void 0;

var React = _interopRequireWildcard(require("react"));

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function () { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; if (obj != null) { var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

var styles = require("./NameCard.css");
/**
 * Distance in pixel the card has to be swiped
 */


var MINIMUM_SWIPE_DISTANCE = 80;

var NameCard = function NameCard(props) {
  var touchPosition = (0, React.useRef)(0);

  var _a = (0, React.useState)(0),
      position = _a[0],
      setPosition = _a[1];

  var _b = (0, React.useState)(false),
      isDragging = _b[0],
      setIsDragging = _b[1];

  var startDragging = function startDragging() {
    setIsDragging(function () {
      return true;
    });
    window.addEventListener("mousemove", drag);
    window.addEventListener("mouseup", stopDragging);
  };

  var stopDragging = function stopDragging() {
    window.removeEventListener("mousemove", drag);
    window.removeEventListener("mouseup", stopDragging);
    setIsDragging(function () {
      return false;
    });
  };

  var drag = function drag(e) {
    setPosition(function (position) {
      return position + e.movementX;
    });
  };

  var startDraggingTouch = function startDraggingTouch(e) {
    touchPosition.current = e.touches[0].clientX;
    setIsDragging(function () {
      return true;
    });
    window.addEventListener("touchmove", dragTouch);
    window.addEventListener("touchend", stopDraggingTouch);
  };

  var dragTouch = function dragTouch(e) {
    var clientX = e.touches[0].clientX;
    setPosition(function (position) {
      return position - touchPosition.current + clientX;
    });
    touchPosition.current = clientX;
  };

  var stopDraggingTouch = function stopDraggingTouch() {
    window.removeEventListener("touchmove", dragTouch);
    window.removeEventListener("touchend", stopDraggingTouch);
    setIsDragging(function () {
      return false;
    });
  };

  var moveBack = function moveBack() {
    if (!isDragging) {
      setPosition(function (position) {
        return Math.sign(position) * Math.max(0, Math.abs(position) - 10);
      });
    }
  };

  (0, React.useEffect)(function () {
    var backwardsMovement = window.setInterval(moveBack, 10);
    return function () {
      window.clearInterval(backwardsMovement);
    };
  });
  /**
   * Remove event listeners on unmount
   */

  (0, React.useEffect)(function () {
    return function () {
      window.removeEventListener("mousemove", drag);
      window.removeEventListener("mouseup", stopDragging);
      window.removeEventListener("touchmove", dragTouch);
      window.removeEventListener("touchend", stopDraggingTouch);
    };
  }, []);
  (0, React.useEffect)(function () {
    if (position > MINIMUM_SWIPE_DISTANCE) {
      props.upvote();
    } else if (position < -MINIMUM_SWIPE_DISTANCE) {
      props.downvote();
    }
  }, [isDragging]);

  var getBackgroundColor = function getBackgroundColor() {
    var offset = position / 5;
    var r = 255 - Math.min(255, offset);
    var g = 255 - Math.min(255, -offset);
    var b = Math.min(r, g);
    return "rgb(" + r + "," + g + "," + b + ")";
  };

  return React.createElement("div", {
    className: styles.cardWrapper
  }, React.createElement("div", {
    className: styles.card,
    onMouseDown: startDragging,
    onTouchStart: startDraggingTouch,
    style: {
      left: position,
      backgroundColor: getBackgroundColor()
    }
  }, React.createElement("div", {
    className: styles.title + " text-5xl"
  }, props.name)));
};

exports.NameCard = NameCard;
},{"react":"../node_modules/react/index.js","./NameCard.css":"voting/NameCard.css"}],"voting/Voting.tsx":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Voting = void 0;

var React = _interopRequireWildcard(require("react"));

var _reactRedux = require("react-redux");

var _sagas = require("../redux/sagas");

var _stackStore = require("../redux/stackStore");

var _NameCard = require("./NameCard");

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function () { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; if (obj != null) { var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

var Voting = function Voting() {
  var _a = (0, React.useState)(0),
      key = _a[0],
      setKey = _a[1];

  var toggleKey = function toggleKey() {
    setKey(function (key) {
      return key ^ 1;
    });
  };

  var dispatch = (0, _reactRedux.useDispatch)();

  var upvote = function upvote() {
    dispatch(_sagas.sagaActions.upvote());
    toggleKey();
  };

  var downvote = function downvote() {
    dispatch(_sagas.sagaActions.downvote());
    toggleKey();
  };

  var currentName = (0, _reactRedux.useSelector)(_stackStore.getCurrentName);

  if (!currentName) {
    return React.createElement(React.Fragment, null, "Loading ...");
  }

  return React.createElement(_NameCard.NameCard, {
    key: key,
    name: currentName.value,
    upvote: upvote,
    downvote: downvote
  });
};

exports.Voting = Voting;
},{"react":"../node_modules/react/index.js","react-redux":"../node_modules/react-redux/es/index.js","../redux/sagas":"redux/sagas.ts","../redux/stackStore":"redux/stackStore.ts","./NameCard":"voting/NameCard.tsx"}],"menu/MenuButton.tsx":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.MenuButton = void 0;

var React = _interopRequireWildcard(require("react"));

var _reactRedux = require("react-redux");

var _appStore = require("../redux/appStore");

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function () { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; if (obj != null) { var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

var MenuButton = function MenuButton(props) {
  var dispatch = (0, _reactRedux.useDispatch)();

  var handleClick = function handleClick() {
    return dispatch(_appStore.appAction.selectSubPage(props.subPage));
  };

  return React.createElement("div", {
    className: "self-center text-center cursor-pointer",
    onClick: handleClick
  }, React.createElement("div", {
    className: "w-full text-gray-700 mb-1"
  }, React.createElement("i", {
    className: "fas fa-" + props.icon + " text-4xl"
  })), React.createElement("div", {
    className: "w-full text-lg font-bold text-gray-700"
  }, props.caption));
};

exports.MenuButton = MenuButton;
},{"react":"../node_modules/react/index.js","react-redux":"../node_modules/react-redux/es/index.js","../redux/appStore":"redux/appStore.ts"}],"menu/Menu.tsx":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Menu = void 0;

var React = _interopRequireWildcard(require("react"));

var _MenuButton = require("./MenuButton");

var _SubPage = require("../models/SubPage");

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function () { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; if (obj != null) { var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

var Menu = function Menu() {
  return React.createElement("div", {
    className: "flex justify-around h-full border-t-4 border-gray-300"
  }, React.createElement(_MenuButton.MenuButton, {
    caption: "Bewerten",
    icon: "heart",
    subPage: _SubPage.SubPage.Voting
  }), React.createElement(_MenuButton.MenuButton, {
    caption: "Resultate",
    icon: "trophy",
    subPage: _SubPage.SubPage.Results
  }), React.createElement(_MenuButton.MenuButton, {
    caption: "Namen",
    icon: "file-alt",
    subPage: _SubPage.SubPage.NameList
  }));
};

exports.Menu = Menu;
},{"react":"../node_modules/react/index.js","./MenuButton":"menu/MenuButton.tsx","../models/SubPage":"models/SubPage.tsx"}],"results/Results.tsx":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Results = void 0;

var _react = _interopRequireDefault(require("react"));

var _reactRedux = require("react-redux");

var _nameListStore = require("../redux/nameListStore");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Results = function Results() {
  var entries = (0, _reactRedux.useSelector)(_nameListStore.getRankedNames);
  return _react.default.createElement("div", {
    className: "px-5 py-2"
  }, entries.length === 0 && _react.default.createElement("div", {
    className: "font-bold w-full text-xl py-3 "
  }, "Es wurden noch keine Namen bewertet"), entries.map(function (_a, i) {
    var name = _a.name,
        score = _a.score;
    return _react.default.createElement("div", {
      key: name,
      className: "w-full text-xl py-3 flex border-t first:border-t-0 border-gray-300"
    }, _react.default.createElement("div", {
      className: "w-10 text-gray-600"
    }, i + 1, "."), _react.default.createElement("div", {
      className: "flex-grow font-bold break-words"
    }, name), _react.default.createElement("div", {
      className: "text-gray-600"
    }, Math.round(score * 100), "%"));
  }));
};

exports.Results = Results;
},{"react":"../node_modules/react/index.js","react-redux":"../node_modules/react-redux/es/index.js","../redux/nameListStore":"redux/nameListStore.ts"}],"nameList/FileDialog.tsx":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.FileDialog = void 0;

var _react = _interopRequireWildcard(require("react"));

var _reactRedux = require("react-redux");

var _sagas = require("../redux/sagas");

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function () { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; if (obj != null) { var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

var __awaiter = void 0 && (void 0).__awaiter || function (thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function (resolve) {
      resolve(value);
    });
  }

  return new (P || (P = Promise))(function (resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }

    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }

    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }

    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};

var __generator = void 0 && (void 0).__generator || function (thisArg, body) {
  var _ = {
    label: 0,
    sent: function sent() {
      if (t[0] & 1) throw t[1];
      return t[1];
    },
    trys: [],
    ops: []
  },
      f,
      y,
      t,
      g;
  return g = {
    next: verb(0),
    "throw": verb(1),
    "return": verb(2)
  }, typeof Symbol === "function" && (g[Symbol.iterator] = function () {
    return this;
  }), g;

  function verb(n) {
    return function (v) {
      return step([n, v]);
    };
  }

  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");

    while (_) {
      try {
        if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
        if (y = 0, t) op = [op[0] & 2, t.value];

        switch (op[0]) {
          case 0:
          case 1:
            t = op;
            break;

          case 4:
            _.label++;
            return {
              value: op[1],
              done: false
            };

          case 5:
            _.label++;
            y = op[1];
            op = [0];
            continue;

          case 7:
            op = _.ops.pop();

            _.trys.pop();

            continue;

          default:
            if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
              _ = 0;
              continue;
            }

            if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
              _.label = op[1];
              break;
            }

            if (op[0] === 6 && _.label < t[1]) {
              _.label = t[1];
              t = op;
              break;
            }

            if (t && _.label < t[2]) {
              _.label = t[2];

              _.ops.push(op);

              break;
            }

            if (t[2]) _.ops.pop();

            _.trys.pop();

            continue;
        }

        op = body.call(thisArg, _);
      } catch (e) {
        op = [6, e];
        y = 0;
      } finally {
        f = t = 0;
      }
    }

    if (op[0] & 5) throw op[1];
    return {
      value: op[0] ? op[1] : void 0,
      done: true
    };
  }
};

var FileDialog = function FileDialog() {
  var fileUpload = (0, _react.useRef)();
  var dispatch = (0, _reactRedux.useDispatch)();

  var importFile = function importFile(e) {
    return __awaiter(void 0, void 0, void 0, function () {
      var fileToLoad, fileReader;
      return __generator(this, function (_a) {
        fileToLoad = fileUpload.current.files[0];
        fileReader = new FileReader();

        fileReader.onload = function (fileLoadedEvent) {
          var text = fileLoadedEvent.target.result;
          dispatch(_sagas.sagaActions.importFile(text));
        };

        fileReader.readAsText(fileToLoad, "UTF-8");
        return [2
        /*return*/
        ];
      });
    });
  };

  return _react.default.createElement("form", {
    className: "p-5"
  }, _react.default.createElement("input", {
    ref: fileUpload,
    type: "file",
    onInput: importFile,
    hidden: true
  }), _react.default.createElement("a", {
    onClick: function onClick() {
      return fileUpload.current.click();
    },
    className: "btn btn-primary text-lg"
  }, "Datei importieren"));
};

exports.FileDialog = FileDialog;
},{"react":"../node_modules/react/index.js","react-redux":"../node_modules/react-redux/es/index.js","../redux/sagas":"redux/sagas.ts"}],"../node_modules/parcel-bundler/src/builtins/bundle-loader.js":[function(require,module,exports) {
var getBundleURL = require('./bundle-url').getBundleURL;

function loadBundlesLazy(bundles) {
  if (!Array.isArray(bundles)) {
    bundles = [bundles];
  }

  var id = bundles[bundles.length - 1];

  try {
    return Promise.resolve(require(id));
  } catch (err) {
    if (err.code === 'MODULE_NOT_FOUND') {
      return new LazyPromise(function (resolve, reject) {
        loadBundles(bundles.slice(0, -1)).then(function () {
          return require(id);
        }).then(resolve, reject);
      });
    }

    throw err;
  }
}

function loadBundles(bundles) {
  return Promise.all(bundles.map(loadBundle));
}

var bundleLoaders = {};

function registerBundleLoader(type, loader) {
  bundleLoaders[type] = loader;
}

module.exports = exports = loadBundlesLazy;
exports.load = loadBundles;
exports.register = registerBundleLoader;
var bundles = {};

function loadBundle(bundle) {
  var id;

  if (Array.isArray(bundle)) {
    id = bundle[1];
    bundle = bundle[0];
  }

  if (bundles[bundle]) {
    return bundles[bundle];
  }

  var type = (bundle.substring(bundle.lastIndexOf('.') + 1, bundle.length) || bundle).toLowerCase();
  var bundleLoader = bundleLoaders[type];

  if (bundleLoader) {
    return bundles[bundle] = bundleLoader(getBundleURL() + bundle).then(function (resolved) {
      if (resolved) {
        module.bundle.register(id, resolved);
      }

      return resolved;
    }).catch(function (e) {
      delete bundles[bundle];
      throw e;
    });
  }
}

function LazyPromise(executor) {
  this.executor = executor;
  this.promise = null;
}

LazyPromise.prototype.then = function (onSuccess, onError) {
  if (this.promise === null) this.promise = new Promise(this.executor);
  return this.promise.then(onSuccess, onError);
};

LazyPromise.prototype.catch = function (onError) {
  if (this.promise === null) this.promise = new Promise(this.executor);
  return this.promise.catch(onError);
};
},{"./bundle-url":"../node_modules/parcel-bundler/src/builtins/bundle-url.js"}],"nameList/Presets.tsx":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Presets = void 0;

var _react = _interopRequireDefault(require("react"));

var _reactRedux = require("react-redux");

var _sagas = require("../redux/sagas");

var _appStore = require("../redux/appStore");

var _SubPage = require("../models/SubPage");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var __awaiter = void 0 && (void 0).__awaiter || function (thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function (resolve) {
      resolve(value);
    });
  }

  return new (P || (P = Promise))(function (resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }

    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }

    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }

    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};

var __generator = void 0 && (void 0).__generator || function (thisArg, body) {
  var _ = {
    label: 0,
    sent: function sent() {
      if (t[0] & 1) throw t[1];
      return t[1];
    },
    trys: [],
    ops: []
  },
      f,
      y,
      t,
      g;
  return g = {
    next: verb(0),
    "throw": verb(1),
    "return": verb(2)
  }, typeof Symbol === "function" && (g[Symbol.iterator] = function () {
    return this;
  }), g;

  function verb(n) {
    return function (v) {
      return step([n, v]);
    };
  }

  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");

    while (_) {
      try {
        if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
        if (y = 0, t) op = [op[0] & 2, t.value];

        switch (op[0]) {
          case 0:
          case 1:
            t = op;
            break;

          case 4:
            _.label++;
            return {
              value: op[1],
              done: false
            };

          case 5:
            _.label++;
            y = op[1];
            op = [0];
            continue;

          case 7:
            op = _.ops.pop();

            _.trys.pop();

            continue;

          default:
            if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
              _ = 0;
              continue;
            }

            if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
              _.label = op[1];
              break;
            }

            if (op[0] === 6 && _.label < t[1]) {
              _.label = t[1];
              t = op;
              break;
            }

            if (t && _.label < t[2]) {
              _.label = t[2];

              _.ops.push(op);

              break;
            }

            if (t[2]) _.ops.pop();

            _.trys.pop();

            continue;
        }

        op = body.call(thisArg, _);
      } catch (e) {
        op = [6, e];
        y = 0;
      } finally {
        f = t = 0;
      }
    }

    if (op[0] & 5) throw op[1];
    return {
      value: op[0] ? op[1] : void 0,
      done: true
    };
  }
};

var Presets = function Presets() {
  var dispatch = (0, _reactRedux.useDispatch)();
  var entries = [{
    name: "Männliche Vornamen Deutschschweiz, 2018",
    file: require("_bundle_loader")(require.resolve("../data/ch-de-2018-m"))
  }, {
    name: "Weibliche Vornamen Deutschschweiz, 2018",
    file: require("_bundle_loader")(require.resolve("../data/ch-de-2018-f"))
  }];

  var loadList = function loadList(file) {
    return __awaiter(void 0, void 0, void 0, function () {
      var names;
      return __generator(this, function (_a) {
        switch (_a.label) {
          case 0:
            return [4
            /*yield*/
            , file];

          case 1:
            names = _a.sent().names;
            dispatch(_sagas.sagaActions.importNames(names));
            dispatch(_appStore.appAction.selectSubPage(_SubPage.SubPage.Voting));
            return [2
            /*return*/
            ];
        }
      });
    });
  };

  return _react.default.createElement("div", {
    className: "px-5 py-2"
  }, entries.map(function (_a, i) {
    var name = _a.name,
        file = _a.file;
    return _react.default.createElement("div", {
      key: i,
      className: "w-full text-xl py-3 flex border-t first:border-t-0 border-gray-300 cursor-pointer break-words",
      onClick: function onClick() {
        return loadList(file);
      }
    }, name);
  }));
};

exports.Presets = Presets;
},{"react":"../node_modules/react/index.js","react-redux":"../node_modules/react-redux/es/index.js","../redux/sagas":"redux/sagas.ts","../redux/appStore":"redux/appStore.ts","../models/SubPage":"models/SubPage.tsx","_bundle_loader":"../node_modules/parcel-bundler/src/builtins/bundle-loader.js","../data/ch-de-2018-m":[["ch-de-2018-m.33a34914.js","data/ch-de-2018-m.ts"],"ch-de-2018-m.33a34914.js.map","data/ch-de-2018-m.ts"],"../data/ch-de-2018-f":[["ch-de-2018-f.31f8f6bb.js","data/ch-de-2018-f.ts"],"ch-de-2018-f.31f8f6bb.js.map","data/ch-de-2018-f.ts"]}],"nameList/NameList.tsx":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.NameList = void 0;

var _react = _interopRequireDefault(require("react"));

var _FileDialog = require("./FileDialog");

var _Presets = require("./Presets");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var NameList = function NameList() {
  return _react.default.createElement(_react.default.Fragment, null, _react.default.createElement(_FileDialog.FileDialog, null), _react.default.createElement(_Presets.Presets, null));
};

exports.NameList = NameList;
},{"react":"../node_modules/react/index.js","./FileDialog":"nameList/FileDialog.tsx","./Presets":"nameList/Presets.tsx"}],"App.tsx":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.App = void 0;

var React = _interopRequireWildcard(require("react"));

var _reactRedux = require("react-redux");

var _Voting = require("./voting/Voting");

var _Menu = require("./menu/Menu");

var _Results = require("./results/Results");

var _SubPage = require("./models/SubPage");

var _appStore = require("./redux/appStore");

var _NameList = require("./nameList/NameList");

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function () { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; if (obj != null) { var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

var App = function App() {
  var subPage = (0, _reactRedux.useSelector)(_appStore.getSubPage);

  var renderSubPage = function renderSubPage() {
    switch (subPage) {
      case _SubPage.SubPage.Voting:
        return React.createElement(_Voting.Voting, null);

      case _SubPage.SubPage.Results:
        return React.createElement(_Results.Results, null);

      case _SubPage.SubPage.NameList:
        return React.createElement(_NameList.NameList, null);
    }
  };

  return React.createElement("div", {
    className: "flex flex-col h-full"
  }, React.createElement("div", {
    className: "flex-grow overflow-y-scroll overflow-x-hidden"
  }, renderSubPage()), React.createElement("div", {
    className: "flex-shrink-0 h-24 w-full bg-gray-100"
  }, React.createElement(_Menu.Menu, null)));
};

exports.App = App;
},{"react":"../node_modules/react/index.js","react-redux":"../node_modules/react-redux/es/index.js","./voting/Voting":"voting/Voting.tsx","./menu/Menu":"menu/Menu.tsx","./results/Results":"results/Results.tsx","./models/SubPage":"models/SubPage.tsx","./redux/appStore":"redux/appStore.ts","./nameList/NameList":"nameList/NameList.tsx"}],"../node_modules/@redux-saga/deferred/dist/redux-saga-deferred.esm.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.arrayOfDeferred = arrayOfDeferred;
exports.default = void 0;

function deferred() {
  var def = {};
  def.promise = new Promise(function (resolve, reject) {
    def.resolve = resolve;
    def.reject = reject;
  });
  return def;
}

function arrayOfDeferred(length) {
  var arr = [];

  for (var i = 0; i < length; i++) {
    arr.push(deferred());
  }

  return arr;
}

var _default = deferred;
exports.default = _default;
},{}],"../node_modules/@redux-saga/core/dist/redux-saga-core.esm.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.channel = channel;
exports.eventChannel = eventChannel;
exports.multicastChannel = multicastChannel;
exports.runSaga = runSaga;
exports.stdChannel = stdChannel;
Object.defineProperty(exports, "CANCEL", {
  enumerable: true,
  get: function () {
    return _symbols.CANCEL;
  }
});
Object.defineProperty(exports, "SAGA_LOCATION", {
  enumerable: true,
  get: function () {
    return _symbols.SAGA_LOCATION;
  }
});
Object.defineProperty(exports, "buffers", {
  enumerable: true,
  get: function () {
    return _io427945dd.H;
  }
});
Object.defineProperty(exports, "detach", {
  enumerable: true,
  get: function () {
    return _io427945dd.I;
  }
});
exports.isEnd = exports.END = exports.default = void 0;

var _symbols = require("@redux-saga/symbols");

var _extends2 = _interopRequireDefault(require("@babel/runtime/helpers/esm/extends"));

var _objectWithoutPropertiesLoose2 = _interopRequireDefault(require("@babel/runtime/helpers/esm/objectWithoutPropertiesLoose"));

var _is = require("@redux-saga/is");

var _io427945dd = require("./io-427945dd.js");

var _redux = require("redux");

var _deferred = _interopRequireDefault(require("@redux-saga/deferred"));

require("@redux-saga/delay-p");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var queue = [];
/**
  Variable to hold a counting semaphore
  - Incrementing adds a lock and puts the scheduler in a `suspended` state (if it's not
    already suspended)
  - Decrementing releases a lock. Zero locks puts the scheduler in a `released` state. This
    triggers flushing the queued tasks.
**/

var semaphore = 0;
/**
  Executes a task 'atomically'. Tasks scheduled during this execution will be queued
  and flushed after this task has finished (assuming the scheduler endup in a released
  state).
**/

function exec(task) {
  try {
    suspend();
    task();
  } finally {
    release();
  }
}
/**
  Executes or queues a task depending on the state of the scheduler (`suspended` or `released`)
**/


function asap(task) {
  queue.push(task);

  if (!semaphore) {
    suspend();
    flush();
  }
}
/**
 * Puts the scheduler in a `suspended` state and executes a task immediately.
 */


function immediately(task) {
  try {
    suspend();
    return task();
  } finally {
    flush();
  }
}
/**
  Puts the scheduler in a `suspended` state. Scheduled tasks will be queued until the
  scheduler is released.
**/


function suspend() {
  semaphore++;
}
/**
  Puts the scheduler in a `released` state.
**/


function release() {
  semaphore--;
}
/**
  Releases the current lock. Executes all queued tasks if the scheduler is in the released state.
**/


function flush() {
  release();
  var task;

  while (!semaphore && (task = queue.shift()) !== undefined) {
    exec(task);
  }
}

var array = function array(patterns) {
  return function (input) {
    return patterns.some(function (p) {
      return matcher(p)(input);
    });
  };
};

var predicate = function predicate(_predicate) {
  return function (input) {
    return _predicate(input);
  };
};

var string = function string(pattern) {
  return function (input) {
    return input.type === String(pattern);
  };
};

var symbol = function symbol(pattern) {
  return function (input) {
    return input.type === pattern;
  };
};

var wildcard = function wildcard() {
  return _io427945dd.k;
};

function matcher(pattern) {
  // prettier-ignore
  var matcherCreator = pattern === '*' ? wildcard : (0, _is.string)(pattern) ? string : (0, _is.array)(pattern) ? array : (0, _is.stringableFunc)(pattern) ? string : (0, _is.func)(pattern) ? predicate : (0, _is.symbol)(pattern) ? symbol : null;

  if (matcherCreator === null) {
    throw new Error("invalid pattern: " + pattern);
  }

  return matcherCreator(pattern);
}

var END = {
  type: _symbols.CHANNEL_END_TYPE
};
exports.END = END;

var isEnd = function isEnd(a) {
  return a && a.type === _symbols.CHANNEL_END_TYPE;
};

exports.isEnd = isEnd;
var CLOSED_CHANNEL_WITH_TAKERS = 'Cannot have a closed channel with pending takers';
var INVALID_BUFFER = 'invalid buffer passed to channel factory function';
var UNDEFINED_INPUT_ERROR = "Saga or channel was provided with an undefined action\nHints:\n  - check that your Action Creator returns a non-undefined value\n  - if the Saga was started using runSaga, check that your subscribe source provides the action to its listeners";

function channel(buffer$1) {
  if (buffer$1 === void 0) {
    buffer$1 = (0, _io427945dd.e)();
  }

  var closed = false;
  var takers = [];

  if ("development" !== 'production') {
    (0, _io427945dd.c)(buffer$1, _is.buffer, INVALID_BUFFER);
  }

  function checkForbiddenStates() {
    if (closed && takers.length) {
      throw (0, _io427945dd.i)(CLOSED_CHANNEL_WITH_TAKERS);
    }

    if (takers.length && !buffer$1.isEmpty()) {
      throw (0, _io427945dd.i)('Cannot have pending takers with non empty buffer');
    }
  }

  function put(input) {
    if ("development" !== 'production') {
      checkForbiddenStates();
      (0, _io427945dd.c)(input, _is.notUndef, UNDEFINED_INPUT_ERROR);
    }

    if (closed) {
      return;
    }

    if (takers.length === 0) {
      return buffer$1.put(input);
    }

    var cb = takers.shift();
    cb(input);
  }

  function take(cb) {
    if ("development" !== 'production') {
      checkForbiddenStates();
      (0, _io427945dd.c)(cb, _is.func, "channel.take's callback must be a function");
    }

    if (closed && buffer$1.isEmpty()) {
      cb(END);
    } else if (!buffer$1.isEmpty()) {
      cb(buffer$1.take());
    } else {
      takers.push(cb);

      cb.cancel = function () {
        (0, _io427945dd.r)(takers, cb);
      };
    }
  }

  function flush(cb) {
    if ("development" !== 'production') {
      checkForbiddenStates();
      (0, _io427945dd.c)(cb, _is.func, "channel.flush' callback must be a function");
    }

    if (closed && buffer$1.isEmpty()) {
      cb(END);
      return;
    }

    cb(buffer$1.flush());
  }

  function close() {
    if ("development" !== 'production') {
      checkForbiddenStates();
    }

    if (closed) {
      return;
    }

    closed = true;
    var arr = takers;
    takers = [];

    for (var i = 0, len = arr.length; i < len; i++) {
      var taker = arr[i];
      taker(END);
    }
  }

  return {
    take: take,
    put: put,
    flush: flush,
    close: close
  };
}

function eventChannel(subscribe, buffer) {
  if (buffer === void 0) {
    buffer = (0, _io427945dd.n)();
  }

  var closed = false;
  var unsubscribe;
  var chan = channel(buffer);

  var close = function close() {
    if (closed) {
      return;
    }

    closed = true;

    if ((0, _is.func)(unsubscribe)) {
      unsubscribe();
    }

    chan.close();
  };

  unsubscribe = subscribe(function (input) {
    if (isEnd(input)) {
      close();
      return;
    }

    chan.put(input);
  });

  if ("development" !== 'production') {
    (0, _io427945dd.c)(unsubscribe, _is.func, 'in eventChannel: subscribe should return a function to unsubscribe');
  }

  unsubscribe = (0, _io427945dd.o)(unsubscribe);

  if (closed) {
    unsubscribe();
  }

  return {
    take: chan.take,
    flush: chan.flush,
    close: close
  };
}

function multicastChannel() {
  var _ref;

  var closed = false;
  var currentTakers = [];
  var nextTakers = currentTakers;

  function checkForbiddenStates() {
    if (closed && nextTakers.length) {
      throw (0, _io427945dd.i)(CLOSED_CHANNEL_WITH_TAKERS);
    }
  }

  var ensureCanMutateNextTakers = function ensureCanMutateNextTakers() {
    if (nextTakers !== currentTakers) {
      return;
    }

    nextTakers = currentTakers.slice();
  };

  var close = function close() {
    if ("development" !== 'production') {
      checkForbiddenStates();
    }

    closed = true;
    var takers = currentTakers = nextTakers;
    nextTakers = [];
    takers.forEach(function (taker) {
      taker(END);
    });
  };

  return _ref = {}, _ref[_symbols.MULTICAST] = true, _ref.put = function put(input) {
    if ("development" !== 'production') {
      checkForbiddenStates();
      (0, _io427945dd.c)(input, _is.notUndef, UNDEFINED_INPUT_ERROR);
    }

    if (closed) {
      return;
    }

    if (isEnd(input)) {
      close();
      return;
    }

    var takers = currentTakers = nextTakers;

    for (var i = 0, len = takers.length; i < len; i++) {
      var taker = takers[i];

      if (taker[_symbols.MATCH](input)) {
        taker.cancel();
        taker(input);
      }
    }
  }, _ref.take = function take(cb, matcher) {
    if (matcher === void 0) {
      matcher = wildcard;
    }

    if ("development" !== 'production') {
      checkForbiddenStates();
    }

    if (closed) {
      cb(END);
      return;
    }

    cb[_symbols.MATCH] = matcher;
    ensureCanMutateNextTakers();
    nextTakers.push(cb);
    cb.cancel = (0, _io427945dd.o)(function () {
      ensureCanMutateNextTakers();
      (0, _io427945dd.r)(nextTakers, cb);
    });
  }, _ref.close = close, _ref;
}

function stdChannel() {
  var chan = multicastChannel();
  var put = chan.put;

  chan.put = function (input) {
    if (input[_symbols.SAGA_ACTION]) {
      put(input);
      return;
    }

    asap(function () {
      put(input);
    });
  };

  return chan;
}

var RUNNING = 0;
var CANCELLED = 1;
var ABORTED = 2;
var DONE = 3;

function resolvePromise(promise, cb) {
  var cancelPromise = promise[_symbols.CANCEL];

  if ((0, _is.func)(cancelPromise)) {
    cb.cancel = cancelPromise;
  }

  promise.then(cb, function (error) {
    cb(error, true);
  });
}

var current = 0;

var nextSagaId = function () {
  return ++current;
};

var _effectRunnerMap;

function getIteratorMetaInfo(iterator, fn) {
  if (iterator.isSagaIterator) {
    return {
      name: iterator.meta.name
    };
  }

  return (0, _io427945dd.j)(fn);
}

function createTaskIterator(_ref) {
  var context = _ref.context,
      fn = _ref.fn,
      args = _ref.args; // catch synchronous failures; see #152 and #441

  try {
    var result = fn.apply(context, args); // i.e. a generator function returns an iterator

    if ((0, _is.iterator)(result)) {
      return result;
    }

    var resolved = false;

    var next = function next(arg) {
      if (!resolved) {
        resolved = true; // Only promises returned from fork will be interpreted. See #1573

        return {
          value: result,
          done: !(0, _is.promise)(result)
        };
      } else {
        return {
          value: arg,
          done: true
        };
      }
    };

    return (0, _io427945dd.q)(next);
  } catch (err) {
    // do not bubble up synchronous failures for detached forks
    // instead create a failed task. See #152 and #441
    return (0, _io427945dd.q)(function () {
      throw err;
    });
  }
}

function runPutEffect(env, _ref2, cb) {
  var channel = _ref2.channel,
      action = _ref2.action,
      resolve = _ref2.resolve;
  /**
   Schedule the put in case another saga is holding a lock.
   The put will be executed atomically. ie nested puts will execute after
   this put has terminated.
   **/

  asap(function () {
    var result;

    try {
      result = (channel ? channel.put : env.dispatch)(action);
    } catch (error) {
      cb(error, true);
      return;
    }

    if (resolve && (0, _is.promise)(result)) {
      resolvePromise(result, cb);
    } else {
      cb(result);
    }
  }); // Put effects are non cancellables
}

function runTakeEffect(env, _ref3, cb) {
  var _ref3$channel = _ref3.channel,
      channel = _ref3$channel === void 0 ? env.channel : _ref3$channel,
      pattern = _ref3.pattern,
      maybe = _ref3.maybe;

  var takeCb = function takeCb(input) {
    if (input instanceof Error) {
      cb(input, true);
      return;
    }

    if (isEnd(input) && !maybe) {
      cb(_symbols.TERMINATE);
      return;
    }

    cb(input);
  };

  try {
    channel.take(takeCb, (0, _is.notUndef)(pattern) ? matcher(pattern) : null);
  } catch (err) {
    cb(err, true);
    return;
  }

  cb.cancel = takeCb.cancel;
}

function runCallEffect(env, _ref4, cb, _ref5) {
  var context = _ref4.context,
      fn = _ref4.fn,
      args = _ref4.args;
  var task = _ref5.task; // catch synchronous failures; see #152

  try {
    var result = fn.apply(context, args);

    if ((0, _is.promise)(result)) {
      resolvePromise(result, cb);
      return;
    }

    if ((0, _is.iterator)(result)) {
      // resolve iterator
      proc(env, result, task.context, current, (0, _io427945dd.j)(fn),
      /* isRoot */
      false, cb);
      return;
    }

    cb(result);
  } catch (error) {
    cb(error, true);
  }
}

function runCPSEffect(env, _ref6, cb) {
  var context = _ref6.context,
      fn = _ref6.fn,
      args = _ref6.args; // CPS (ie node style functions) can define their own cancellation logic
  // by setting cancel field on the cb
  // catch synchronous failures; see #152

  try {
    var cpsCb = function cpsCb(err, res) {
      if ((0, _is.undef)(err)) {
        cb(res);
      } else {
        cb(err, true);
      }
    };

    fn.apply(context, args.concat(cpsCb));

    if (cpsCb.cancel) {
      cb.cancel = cpsCb.cancel;
    }
  } catch (error) {
    cb(error, true);
  }
}

function runForkEffect(env, _ref7, cb, _ref8) {
  var context = _ref7.context,
      fn = _ref7.fn,
      args = _ref7.args,
      detached = _ref7.detached;
  var parent = _ref8.task;
  var taskIterator = createTaskIterator({
    context: context,
    fn: fn,
    args: args
  });
  var meta = getIteratorMetaInfo(taskIterator, fn);
  immediately(function () {
    var child = proc(env, taskIterator, parent.context, current, meta, detached, undefined);

    if (detached) {
      cb(child);
    } else {
      if (child.isRunning()) {
        parent.queue.addTask(child);
        cb(child);
      } else if (child.isAborted()) {
        parent.queue.abort(child.error());
      } else {
        cb(child);
      }
    }
  }); // Fork effects are non cancellables
}

function runJoinEffect(env, taskOrTasks, cb, _ref9) {
  var task = _ref9.task;

  var joinSingleTask = function joinSingleTask(taskToJoin, cb) {
    if (taskToJoin.isRunning()) {
      var joiner = {
        task: task,
        cb: cb
      };

      cb.cancel = function () {
        if (taskToJoin.isRunning()) (0, _io427945dd.r)(taskToJoin.joiners, joiner);
      };

      taskToJoin.joiners.push(joiner);
    } else {
      if (taskToJoin.isAborted()) {
        cb(taskToJoin.error(), true);
      } else {
        cb(taskToJoin.result());
      }
    }
  };

  if ((0, _is.array)(taskOrTasks)) {
    if (taskOrTasks.length === 0) {
      cb([]);
      return;
    }

    var childCallbacks = (0, _io427945dd.l)(taskOrTasks, cb);
    taskOrTasks.forEach(function (t, i) {
      joinSingleTask(t, childCallbacks[i]);
    });
  } else {
    joinSingleTask(taskOrTasks, cb);
  }
}

function cancelSingleTask(taskToCancel) {
  if (taskToCancel.isRunning()) {
    taskToCancel.cancel();
  }
}

function runCancelEffect(env, taskOrTasks, cb, _ref10) {
  var task = _ref10.task;

  if (taskOrTasks === _symbols.SELF_CANCELLATION) {
    cancelSingleTask(task);
  } else if ((0, _is.array)(taskOrTasks)) {
    taskOrTasks.forEach(cancelSingleTask);
  } else {
    cancelSingleTask(taskOrTasks);
  }

  cb(); // cancel effects are non cancellables
}

function runAllEffect(env, effects, cb, _ref11) {
  var digestEffect = _ref11.digestEffect;
  var effectId = current;
  var keys = Object.keys(effects);

  if (keys.length === 0) {
    cb((0, _is.array)(effects) ? [] : {});
    return;
  }

  var childCallbacks = (0, _io427945dd.l)(effects, cb);
  keys.forEach(function (key) {
    digestEffect(effects[key], effectId, childCallbacks[key], key);
  });
}

function runRaceEffect(env, effects, cb, _ref12) {
  var digestEffect = _ref12.digestEffect;
  var effectId = current;
  var keys = Object.keys(effects);
  var response = (0, _is.array)(effects) ? (0, _io427945dd.m)(keys.length) : {};
  var childCbs = {};
  var completed = false;
  keys.forEach(function (key) {
    var chCbAtKey = function chCbAtKey(res, isErr) {
      if (completed) {
        return;
      }

      if (isErr || (0, _io427945dd.s)(res)) {
        // Race Auto cancellation
        cb.cancel();
        cb(res, isErr);
      } else {
        cb.cancel();
        completed = true;
        response[key] = res;
        cb(response);
      }
    };

    chCbAtKey.cancel = _io427945dd.t;
    childCbs[key] = chCbAtKey;
  });

  cb.cancel = function () {
    // prevents unnecessary cancellation
    if (!completed) {
      completed = true;
      keys.forEach(function (key) {
        return childCbs[key].cancel();
      });
    }
  };

  keys.forEach(function (key) {
    if (completed) {
      return;
    }

    digestEffect(effects[key], effectId, childCbs[key], key);
  });
}

function runSelectEffect(env, _ref13, cb) {
  var selector = _ref13.selector,
      args = _ref13.args;

  try {
    var state = selector.apply(void 0, [env.getState()].concat(args));
    cb(state);
  } catch (error) {
    cb(error, true);
  }
}

function runChannelEffect(env, _ref14, cb) {
  var pattern = _ref14.pattern,
      buffer = _ref14.buffer;
  var chan = channel(buffer);
  var match = matcher(pattern);

  var taker = function taker(action) {
    if (!isEnd(action)) {
      env.channel.take(taker, match);
    }

    chan.put(action);
  };

  var close = chan.close;

  chan.close = function () {
    taker.cancel();
    close();
  };

  env.channel.take(taker, match);
  cb(chan);
}

function runCancelledEffect(env, data, cb, _ref15) {
  var task = _ref15.task;
  cb(task.isCancelled());
}

function runFlushEffect(env, channel, cb) {
  channel.flush(cb);
}

function runGetContextEffect(env, prop, cb, _ref16) {
  var task = _ref16.task;
  cb(task.context[prop]);
}

function runSetContextEffect(env, props, cb, _ref17) {
  var task = _ref17.task;
  (0, _io427945dd.p)(task.context, props);
  cb();
}

var effectRunnerMap = (_effectRunnerMap = {}, _effectRunnerMap[_io427945dd.T] = runTakeEffect, _effectRunnerMap[_io427945dd.P] = runPutEffect, _effectRunnerMap[_io427945dd.A] = runAllEffect, _effectRunnerMap[_io427945dd.R] = runRaceEffect, _effectRunnerMap[_io427945dd.C] = runCallEffect, _effectRunnerMap[_io427945dd.a] = runCPSEffect, _effectRunnerMap[_io427945dd.F] = runForkEffect, _effectRunnerMap[_io427945dd.J] = runJoinEffect, _effectRunnerMap[_io427945dd.b] = runCancelEffect, _effectRunnerMap[_io427945dd.S] = runSelectEffect, _effectRunnerMap[_io427945dd.d] = runChannelEffect, _effectRunnerMap[_io427945dd.f] = runCancelledEffect, _effectRunnerMap[_io427945dd.g] = runFlushEffect, _effectRunnerMap[_io427945dd.G] = runGetContextEffect, _effectRunnerMap[_io427945dd.h] = runSetContextEffect, _effectRunnerMap);
/**
 Used to track a parent task and its forks
 In the fork model, forked tasks are attached by default to their parent
 We model this using the concept of Parent task && main Task
 main task is the main flow of the current Generator, the parent tasks is the
 aggregation of the main tasks + all its forked tasks.
 Thus the whole model represents an execution tree with multiple branches (vs the
 linear execution tree in sequential (non parallel) programming)

 A parent tasks has the following semantics
 - It completes if all its forks either complete or all cancelled
 - If it's cancelled, all forks are cancelled as well
 - It aborts if any uncaught error bubbles up from forks
 - If it completes, the return value is the one returned by the main task
 **/

function forkQueue(mainTask, onAbort, cont) {
  var tasks = [];
  var result;
  var completed = false;
  addTask(mainTask);

  var getTasks = function getTasks() {
    return tasks;
  };

  function abort(err) {
    onAbort();
    cancelAll();
    cont(err, true);
  }

  function addTask(task) {
    tasks.push(task);

    task.cont = function (res, isErr) {
      if (completed) {
        return;
      }

      (0, _io427945dd.r)(tasks, task);
      task.cont = _io427945dd.t;

      if (isErr) {
        abort(res);
      } else {
        if (task === mainTask) {
          result = res;
        }

        if (!tasks.length) {
          completed = true;
          cont(result);
        }
      }
    };
  }

  function cancelAll() {
    if (completed) {
      return;
    }

    completed = true;
    tasks.forEach(function (t) {
      t.cont = _io427945dd.t;
      t.cancel();
    });
    tasks = [];
  }

  return {
    addTask: addTask,
    cancelAll: cancelAll,
    abort: abort,
    getTasks: getTasks
  };
} // there can be only a single saga error created at any given moment


function formatLocation(fileName, lineNumber) {
  return fileName + "?" + lineNumber;
}

function effectLocationAsString(effect) {
  var location = (0, _io427945dd.v)(effect);

  if (location) {
    var code = location.code,
        fileName = location.fileName,
        lineNumber = location.lineNumber;
    var source = code + "  " + formatLocation(fileName, lineNumber);
    return source;
  }

  return '';
}

function sagaLocationAsString(sagaMeta) {
  var name = sagaMeta.name,
      location = sagaMeta.location;

  if (location) {
    return name + "  " + formatLocation(location.fileName, location.lineNumber);
  }

  return name;
}

function cancelledTasksAsString(sagaStack) {
  var cancelledTasks = (0, _io427945dd.u)(function (i) {
    return i.cancelledTasks;
  }, sagaStack);

  if (!cancelledTasks.length) {
    return '';
  }

  return ['Tasks cancelled due to error:'].concat(cancelledTasks).join('\n');
}

var crashedEffect = null;
var sagaStack = [];

var addSagaFrame = function addSagaFrame(frame) {
  frame.crashedEffect = crashedEffect;
  sagaStack.push(frame);
};

var clear = function clear() {
  crashedEffect = null;
  sagaStack.length = 0;
}; // this sets crashed effect for the soon-to-be-reported saga frame
// this slightly streatches the singleton nature of this module into wrong direction
// as it's even less obvious what's the data flow here, but it is what it is for now


var setCrashedEffect = function setCrashedEffect(effect) {
  crashedEffect = effect;
};
/**
  @returns {string}

  @example
  The above error occurred in task errorInPutSaga {pathToFile}
  when executing effect put({type: 'REDUCER_ACTION_ERROR_IN_PUT'}) {pathToFile}
      created by fetchSaga {pathToFile}
      created by rootSaga {pathToFile}
*/


var toString = function toString() {
  var firstSaga = sagaStack[0],
      otherSagas = sagaStack.slice(1);
  var crashedEffectLocation = firstSaga.crashedEffect ? effectLocationAsString(firstSaga.crashedEffect) : null;
  var errorMessage = "The above error occurred in task " + sagaLocationAsString(firstSaga.meta) + (crashedEffectLocation ? " \n when executing effect " + crashedEffectLocation : '');
  return [errorMessage].concat(otherSagas.map(function (s) {
    return "    created by " + sagaLocationAsString(s.meta);
  }), [cancelledTasksAsString(sagaStack)]).join('\n');
};

function newTask(env, mainTask, parentContext, parentEffectId, meta, isRoot, cont) {
  var _task;

  if (cont === void 0) {
    cont = _io427945dd.t;
  }

  var status = RUNNING;
  var taskResult;
  var taskError;
  var deferredEnd = null;
  var cancelledDueToErrorTasks = [];
  var context = Object.create(parentContext);
  var queue = forkQueue(mainTask, function onAbort() {
    cancelledDueToErrorTasks.push.apply(cancelledDueToErrorTasks, queue.getTasks().map(function (t) {
      return t.meta.name;
    }));
  }, end);
  /**
   This may be called by a parent generator to trigger/propagate cancellation
   cancel all pending tasks (including the main task), then end the current task.
    Cancellation propagates down to the whole execution tree held by this Parent task
   It's also propagated to all joiners of this task and their execution tree/joiners
    Cancellation is noop for terminated/Cancelled tasks tasks
   **/

  function cancel() {
    if (status === RUNNING) {
      // Setting status to CANCELLED does not necessarily mean that the task/iterators are stopped
      // effects in the iterator's finally block will still be executed
      status = CANCELLED;
      queue.cancelAll(); // Ending with a TASK_CANCEL will propagate the Cancellation to all joiners

      end(_symbols.TASK_CANCEL, false);
    }
  }

  function end(result, isErr) {
    if (!isErr) {
      // The status here may be RUNNING or CANCELLED
      // If the status is CANCELLED, then we do not need to change it here
      if (result === _symbols.TASK_CANCEL) {
        status = CANCELLED;
      } else if (status !== CANCELLED) {
        status = DONE;
      }

      taskResult = result;
      deferredEnd && deferredEnd.resolve(result);
    } else {
      status = ABORTED;
      addSagaFrame({
        meta: meta,
        cancelledTasks: cancelledDueToErrorTasks
      });

      if (task.isRoot) {
        var sagaStack = toString(); // we've dumped the saga stack to string and are passing it to user's code
        // we know that it won't be needed anymore and we need to clear it

        clear();
        env.onError(result, {
          sagaStack: sagaStack
        });
      }

      taskError = result;
      deferredEnd && deferredEnd.reject(result);
    }

    task.cont(result, isErr);
    task.joiners.forEach(function (joiner) {
      joiner.cb(result, isErr);
    });
    task.joiners = null;
  }

  function setContext(props) {
    if ("development" !== 'production') {
      (0, _io427945dd.c)(props, _is.object, (0, _io427945dd.w)('task', props));
    }

    (0, _io427945dd.p)(context, props);
  }

  function toPromise() {
    if (deferredEnd) {
      return deferredEnd.promise;
    }

    deferredEnd = (0, _deferred.default)();

    if (status === ABORTED) {
      deferredEnd.reject(taskError);
    } else if (status !== RUNNING) {
      deferredEnd.resolve(taskResult);
    }

    return deferredEnd.promise;
  }

  var task = (_task = {}, _task[_symbols.TASK] = true, _task.id = parentEffectId, _task.meta = meta, _task.isRoot = isRoot, _task.context = context, _task.joiners = [], _task.queue = queue, _task.cancel = cancel, _task.cont = cont, _task.end = end, _task.setContext = setContext, _task.toPromise = toPromise, _task.isRunning = function isRunning() {
    return status === RUNNING;
  }, _task.isCancelled = function isCancelled() {
    return status === CANCELLED || status === RUNNING && mainTask.status === CANCELLED;
  }, _task.isAborted = function isAborted() {
    return status === ABORTED;
  }, _task.result = function result() {
    return taskResult;
  }, _task.error = function error() {
    return taskError;
  }, _task);
  return task;
}

function proc(env, iterator$1, parentContext, parentEffectId, meta, isRoot, cont) {
  if ("development" !== 'production' && iterator$1[_io427945dd.x]) {
    throw new Error("redux-saga doesn't support async generators, please use only regular ones");
  }

  var finalRunEffect = env.finalizeRunEffect(runEffect);
  /**
    Tracks the current effect cancellation
    Each time the generator progresses. calling runEffect will set a new value
    on it. It allows propagating cancellation to child effects
  **/

  next.cancel = _io427945dd.t;
  /** Creates a main task to track the main flow */

  var mainTask = {
    meta: meta,
    cancel: cancelMain,
    status: RUNNING
  };
  /**
   Creates a new task descriptor for this generator.
   A task is the aggregation of it's mainTask and all it's forked tasks.
   **/

  var task = newTask(env, mainTask, parentContext, parentEffectId, meta, isRoot, cont);
  var executingContext = {
    task: task,
    digestEffect: digestEffect
  };
  /**
    cancellation of the main task. We'll simply resume the Generator with a TASK_CANCEL
  **/

  function cancelMain() {
    if (mainTask.status === RUNNING) {
      mainTask.status = CANCELLED;
      next(_symbols.TASK_CANCEL);
    }
  }
  /**
    attaches cancellation logic to this task's continuation
    this will permit cancellation to propagate down the call chain
  **/


  if (cont) {
    cont.cancel = task.cancel;
  } // kicks up the generator


  next(); // then return the task descriptor to the caller

  return task;
  /**
   * This is the generator driver
   * It's a recursive async/continuation function which calls itself
   * until the generator terminates or throws
   * @param {internal commands(TASK_CANCEL | TERMINATE) | any} arg - value, generator will be resumed with.
   * @param {boolean} isErr - the flag shows if effect finished with an error
   *
   * receives either (command | effect result, false) or (any thrown thing, true)
   */

  function next(arg, isErr) {
    try {
      var result;

      if (isErr) {
        result = iterator$1.throw(arg); // user handled the error, we can clear bookkept values

        clear();
      } else if ((0, _io427945dd.y)(arg)) {
        /**
          getting TASK_CANCEL automatically cancels the main task
          We can get this value here
           - By cancelling the parent task manually
          - By joining a Cancelled task
        **/
        mainTask.status = CANCELLED;
        /**
          Cancels the current effect; this will propagate the cancellation down to any called tasks
        **/

        next.cancel();
        /**
          If this Generator has a `return` method then invokes it
          This will jump to the finally block
        **/

        result = (0, _is.func)(iterator$1.return) ? iterator$1.return(_symbols.TASK_CANCEL) : {
          done: true,
          value: _symbols.TASK_CANCEL
        };
      } else if ((0, _io427945dd.z)(arg)) {
        // We get TERMINATE flag, i.e. by taking from a channel that ended using `take` (and not `takem` used to trap End of channels)
        result = (0, _is.func)(iterator$1.return) ? iterator$1.return() : {
          done: true
        };
      } else {
        result = iterator$1.next(arg);
      }

      if (!result.done) {
        digestEffect(result.value, parentEffectId, next);
      } else {
        /**
          This Generator has ended, terminate the main task and notify the fork queue
        **/
        if (mainTask.status !== CANCELLED) {
          mainTask.status = DONE;
        }

        mainTask.cont(result.value);
      }
    } catch (error) {
      if (mainTask.status === CANCELLED) {
        throw error;
      }

      mainTask.status = ABORTED;
      mainTask.cont(error, true);
    }
  }

  function runEffect(effect, effectId, currCb) {
    /**
      each effect runner must attach its own logic of cancellation to the provided callback
      it allows this generator to propagate cancellation downward.
       ATTENTION! effect runners must setup the cancel logic by setting cb.cancel = [cancelMethod]
      And the setup must occur before calling the callback
       This is a sort of inversion of control: called async functions are responsible
      of completing the flow by calling the provided continuation; while caller functions
      are responsible for aborting the current flow by calling the attached cancel function
       Library users can attach their own cancellation logic to promises by defining a
      promise[CANCEL] method in their returned promises
      ATTENTION! calling cancel must have no effect on an already completed or cancelled effect
    **/
    if ((0, _is.promise)(effect)) {
      resolvePromise(effect, currCb);
    } else if ((0, _is.iterator)(effect)) {
      // resolve iterator
      proc(env, effect, task.context, effectId, meta,
      /* isRoot */
      false, currCb);
    } else if (effect && effect[_symbols.IO]) {
      var effectRunner = effectRunnerMap[effect.type];
      effectRunner(env, effect.payload, currCb, executingContext);
    } else {
      // anything else returned as is
      currCb(effect);
    }
  }

  function digestEffect(effect, parentEffectId, cb, label) {
    if (label === void 0) {
      label = '';
    }

    var effectId = nextSagaId();
    env.sagaMonitor && env.sagaMonitor.effectTriggered({
      effectId: effectId,
      parentEffectId: parentEffectId,
      label: label,
      effect: effect
    });
    /**
      completion callback and cancel callback are mutually exclusive
      We can't cancel an already completed effect
      And We can't complete an already cancelled effectId
    **/

    var effectSettled; // Completion callback passed to the appropriate effect runner

    function currCb(res, isErr) {
      if (effectSettled) {
        return;
      }

      effectSettled = true;
      cb.cancel = _io427945dd.t; // defensive measure

      if (env.sagaMonitor) {
        if (isErr) {
          env.sagaMonitor.effectRejected(effectId, res);
        } else {
          env.sagaMonitor.effectResolved(effectId, res);
        }
      }

      if (isErr) {
        setCrashedEffect(effect);
      }

      cb(res, isErr);
    } // tracks down the current cancel


    currCb.cancel = _io427945dd.t; // setup cancellation logic on the parent cb

    cb.cancel = function () {
      // prevents cancelling an already completed effect
      if (effectSettled) {
        return;
      }

      effectSettled = true;
      currCb.cancel(); // propagates cancel downward

      currCb.cancel = _io427945dd.t; // defensive measure

      env.sagaMonitor && env.sagaMonitor.effectCancelled(effectId);
    };

    finalRunEffect(effect, effectId, currCb);
  }
}

var RUN_SAGA_SIGNATURE = 'runSaga(options, saga, ...args)';
var NON_GENERATOR_ERR = RUN_SAGA_SIGNATURE + ": saga argument must be a Generator function!";

function runSaga(_ref, saga) {
  var _ref$channel = _ref.channel,
      channel = _ref$channel === void 0 ? stdChannel() : _ref$channel,
      dispatch = _ref.dispatch,
      getState = _ref.getState,
      _ref$context = _ref.context,
      context = _ref$context === void 0 ? {} : _ref$context,
      sagaMonitor = _ref.sagaMonitor,
      effectMiddlewares = _ref.effectMiddlewares,
      _ref$onError = _ref.onError,
      onError = _ref$onError === void 0 ? _io427945dd.B : _ref$onError;

  if ("development" !== 'production') {
    (0, _io427945dd.c)(saga, _is.func, NON_GENERATOR_ERR);
  }

  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }

  var iterator$1 = saga.apply(void 0, args);

  if ("development" !== 'production') {
    (0, _io427945dd.c)(iterator$1, _is.iterator, NON_GENERATOR_ERR);
  }

  var effectId = nextSagaId();

  if (sagaMonitor) {
    // monitors are expected to have a certain interface, let's fill-in any missing ones
    sagaMonitor.rootSagaStarted = sagaMonitor.rootSagaStarted || _io427945dd.t;
    sagaMonitor.effectTriggered = sagaMonitor.effectTriggered || _io427945dd.t;
    sagaMonitor.effectResolved = sagaMonitor.effectResolved || _io427945dd.t;
    sagaMonitor.effectRejected = sagaMonitor.effectRejected || _io427945dd.t;
    sagaMonitor.effectCancelled = sagaMonitor.effectCancelled || _io427945dd.t;
    sagaMonitor.actionDispatched = sagaMonitor.actionDispatched || _io427945dd.t;
    sagaMonitor.rootSagaStarted({
      effectId: effectId,
      saga: saga,
      args: args
    });
  }

  if ("development" !== 'production') {
    if ((0, _is.notUndef)(dispatch)) {
      (0, _io427945dd.c)(dispatch, _is.func, 'dispatch must be a function');
    }

    if ((0, _is.notUndef)(getState)) {
      (0, _io427945dd.c)(getState, _is.func, 'getState must be a function');
    }

    if ((0, _is.notUndef)(effectMiddlewares)) {
      var MIDDLEWARE_TYPE_ERROR = 'effectMiddlewares must be an array of functions';
      (0, _io427945dd.c)(effectMiddlewares, _is.array, MIDDLEWARE_TYPE_ERROR);
      effectMiddlewares.forEach(function (effectMiddleware) {
        return (0, _io427945dd.c)(effectMiddleware, _is.func, MIDDLEWARE_TYPE_ERROR);
      });
    }

    (0, _io427945dd.c)(onError, _is.func, 'onError passed to the redux-saga is not a function!');
  }

  var finalizeRunEffect;

  if (effectMiddlewares) {
    var middleware = _redux.compose.apply(void 0, effectMiddlewares);

    finalizeRunEffect = function finalizeRunEffect(runEffect) {
      return function (effect, effectId, currCb) {
        var plainRunEffect = function plainRunEffect(eff) {
          return runEffect(eff, effectId, currCb);
        };

        return middleware(plainRunEffect)(effect);
      };
    };
  } else {
    finalizeRunEffect = _io427945dd.E;
  }

  var env = {
    channel: channel,
    dispatch: (0, _io427945dd.D)(dispatch),
    getState: getState,
    sagaMonitor: sagaMonitor,
    onError: onError,
    finalizeRunEffect: finalizeRunEffect
  };
  return immediately(function () {
    var task = proc(env, iterator$1, context, effectId, (0, _io427945dd.j)(saga),
    /* isRoot */
    true, undefined);

    if (sagaMonitor) {
      sagaMonitor.effectResolved(effectId, task);
    }

    return task;
  });
}

function sagaMiddlewareFactory(_ref) {
  if (_ref === void 0) {
    _ref = {};
  }

  var _ref2 = _ref,
      _ref2$context = _ref2.context,
      context = _ref2$context === void 0 ? {} : _ref2$context,
      _ref2$channel = _ref2.channel,
      channel = _ref2$channel === void 0 ? stdChannel() : _ref2$channel,
      sagaMonitor = _ref2.sagaMonitor,
      options = (0, _objectWithoutPropertiesLoose2.default)(_ref2, ["context", "channel", "sagaMonitor"]);
  var boundRunSaga;

  if ("development" !== 'production') {
    (0, _io427945dd.c)(channel, _is.channel, 'options.channel passed to the Saga middleware is not a channel');
  }

  function sagaMiddleware(_ref3) {
    var getState = _ref3.getState,
        dispatch = _ref3.dispatch;
    boundRunSaga = runSaga.bind(null, (0, _extends2.default)({}, options, {
      context: context,
      channel: channel,
      dispatch: dispatch,
      getState: getState,
      sagaMonitor: sagaMonitor
    }));
    return function (next) {
      return function (action) {
        if (sagaMonitor && sagaMonitor.actionDispatched) {
          sagaMonitor.actionDispatched(action);
        }

        var result = next(action); // hit reducers

        channel.put(action);
        return result;
      };
    };
  }

  sagaMiddleware.run = function () {
    if ("development" !== 'production' && !boundRunSaga) {
      throw new Error('Before running a Saga, you must mount the Saga middleware on the Store using applyMiddleware');
    }

    return boundRunSaga.apply(void 0, arguments);
  };

  sagaMiddleware.setContext = function (props) {
    if ("development" !== 'production') {
      (0, _io427945dd.c)(props, _is.object, (0, _io427945dd.w)('sagaMiddleware', props));
    }

    (0, _io427945dd.p)(context, props);
  };

  return sagaMiddleware;
}

var _default = sagaMiddlewareFactory;
exports.default = _default;
},{"@redux-saga/symbols":"../node_modules/@redux-saga/symbols/dist/redux-saga-symbols.esm.js","@babel/runtime/helpers/esm/extends":"../node_modules/@babel/runtime/helpers/esm/extends.js","@babel/runtime/helpers/esm/objectWithoutPropertiesLoose":"../node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js","@redux-saga/is":"../node_modules/@redux-saga/is/dist/redux-saga-is.esm.js","./io-427945dd.js":"../node_modules/@redux-saga/core/dist/io-427945dd.js","redux":"../node_modules/redux/es/redux.js","@redux-saga/deferred":"../node_modules/@redux-saga/deferred/dist/redux-saga-deferred.esm.js","@redux-saga/delay-p":"../node_modules/@redux-saga/delay-p/dist/redux-saga-delay-p.esm.js"}],"../node_modules/redux-saga/dist/redux-saga-core-npm-proxy.esm.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
var _exportNames = {};
exports.default = void 0;

var _core = _interopRequireWildcard(require("@redux-saga/core"));

Object.keys(_core).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function () {
      return _core[key];
    }
  });
});

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function () { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; if (obj != null) { var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

var _default = _core.default;
exports.default = _default;
},{"@redux-saga/core":"../node_modules/@redux-saga/core/dist/redux-saga-core.esm.js"}],"../node_modules/redux-persist/es/constants.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.DEFAULT_VERSION = exports.REGISTER = exports.PURGE = exports.PERSIST = exports.PAUSE = exports.REHYDRATE = exports.FLUSH = exports.KEY_PREFIX = void 0;
var KEY_PREFIX = 'persist:';
exports.KEY_PREFIX = KEY_PREFIX;
var FLUSH = 'persist/FLUSH';
exports.FLUSH = FLUSH;
var REHYDRATE = 'persist/REHYDRATE';
exports.REHYDRATE = REHYDRATE;
var PAUSE = 'persist/PAUSE';
exports.PAUSE = PAUSE;
var PERSIST = 'persist/PERSIST';
exports.PERSIST = PERSIST;
var PURGE = 'persist/PURGE';
exports.PURGE = PURGE;
var REGISTER = 'persist/REGISTER';
exports.REGISTER = REGISTER;
var DEFAULT_VERSION = -1;
exports.DEFAULT_VERSION = DEFAULT_VERSION;
},{}],"../node_modules/redux-persist/es/stateReconciler/autoMergeLevel1.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = autoMergeLevel1;

function _typeof(obj) {
  if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
    _typeof = function _typeof(obj) {
      return typeof obj;
    };
  } else {
    _typeof = function _typeof(obj) {
      return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    };
  }

  return _typeof(obj);
}

function ownKeys(object, enumerableOnly) {
  var keys = Object.keys(object);

  if (Object.getOwnPropertySymbols) {
    var symbols = Object.getOwnPropertySymbols(object);
    if (enumerableOnly) symbols = symbols.filter(function (sym) {
      return Object.getOwnPropertyDescriptor(object, sym).enumerable;
    });
    keys.push.apply(keys, symbols);
  }

  return keys;
}

function _objectSpread(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i] != null ? arguments[i] : {};

    if (i % 2) {
      ownKeys(source, true).forEach(function (key) {
        _defineProperty(target, key, source[key]);
      });
    } else if (Object.getOwnPropertyDescriptors) {
      Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
    } else {
      ownKeys(source).forEach(function (key) {
        Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
      });
    }
  }

  return target;
}

function _defineProperty(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
  } else {
    obj[key] = value;
  }

  return obj;
}
/*
  autoMergeLevel1: 
    - merges 1 level of substate
    - skips substate if already modified
*/


function autoMergeLevel1(inboundState, originalState, reducedState, _ref) {
  var debug = _ref.debug;

  var newState = _objectSpread({}, reducedState); // only rehydrate if inboundState exists and is an object


  if (inboundState && _typeof(inboundState) === 'object') {
    Object.keys(inboundState).forEach(function (key) {
      // ignore _persist data
      if (key === '_persist') return; // if reducer modifies substate, skip auto rehydration

      if (originalState[key] !== reducedState[key]) {
        if ("development" !== 'production' && debug) console.log('redux-persist/stateReconciler: sub state for key `%s` modified, skipping.', key);
        return;
      } // otherwise hard set the new value


      newState[key] = inboundState[key];
    });
  }

  if ("development" !== 'production' && debug && inboundState && _typeof(inboundState) === 'object') console.log("redux-persist/stateReconciler: rehydrated keys '".concat(Object.keys(inboundState).join(', '), "'"));
  return newState;
}
},{}],"../node_modules/redux-persist/es/createPersistoid.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = createPersistoid;

var _constants = require("./constants");

// @TODO remove once flow < 0.63 support is no longer required.
function createPersistoid(config) {
  // defaults
  var blacklist = config.blacklist || null;
  var whitelist = config.whitelist || null;
  var transforms = config.transforms || [];
  var throttle = config.throttle || 0;
  var storageKey = "".concat(config.keyPrefix !== undefined ? config.keyPrefix : _constants.KEY_PREFIX).concat(config.key);
  var storage = config.storage;
  var serialize;

  if (config.serialize === false) {
    serialize = function serialize(x) {
      return x;
    };
  } else if (typeof config.serialize === 'function') {
    serialize = config.serialize;
  } else {
    serialize = defaultSerialize;
  }

  var writeFailHandler = config.writeFailHandler || null; // initialize stateful values

  var lastState = {};
  var stagedState = {};
  var keysToProcess = [];
  var timeIterator = null;
  var writePromise = null;

  var update = function update(state) {
    // add any changed keys to the queue
    Object.keys(state).forEach(function (key) {
      if (!passWhitelistBlacklist(key)) return; // is keyspace ignored? noop

      if (lastState[key] === state[key]) return; // value unchanged? noop

      if (keysToProcess.indexOf(key) !== -1) return; // is key already queued? noop

      keysToProcess.push(key); // add key to queue
    }); //if any key is missing in the new state which was present in the lastState,
    //add it for processing too

    Object.keys(lastState).forEach(function (key) {
      if (state[key] === undefined && passWhitelistBlacklist(key) && keysToProcess.indexOf(key) === -1 && lastState[key] !== undefined) {
        keysToProcess.push(key);
      }
    }); // start the time iterator if not running (read: throttle)

    if (timeIterator === null) {
      timeIterator = setInterval(processNextKey, throttle);
    }

    lastState = state;
  };

  function processNextKey() {
    if (keysToProcess.length === 0) {
      if (timeIterator) clearInterval(timeIterator);
      timeIterator = null;
      return;
    }

    var key = keysToProcess.shift();
    var endState = transforms.reduce(function (subState, transformer) {
      return transformer.in(subState, key, lastState);
    }, lastState[key]);

    if (endState !== undefined) {
      try {
        stagedState[key] = serialize(endState);
      } catch (err) {
        console.error('redux-persist/createPersistoid: error serializing state', err);
      }
    } else {
      //if the endState is undefined, no need to persist the existing serialized content
      delete stagedState[key];
    }

    if (keysToProcess.length === 0) {
      writeStagedState();
    }
  }

  function writeStagedState() {
    // cleanup any removed keys just before write.
    Object.keys(stagedState).forEach(function (key) {
      if (lastState[key] === undefined) {
        delete stagedState[key];
      }
    });
    writePromise = storage.setItem(storageKey, serialize(stagedState)).catch(onWriteFail);
  }

  function passWhitelistBlacklist(key) {
    if (whitelist && whitelist.indexOf(key) === -1 && key !== '_persist') return false;
    if (blacklist && blacklist.indexOf(key) !== -1) return false;
    return true;
  }

  function onWriteFail(err) {
    // @TODO add fail handlers (typically storage full)
    if (writeFailHandler) writeFailHandler(err);

    if (err && "development" !== 'production') {
      console.error('Error storing data', err);
    }
  }

  var flush = function flush() {
    while (keysToProcess.length !== 0) {
      processNextKey();
    }

    return writePromise || Promise.resolve();
  }; // return `persistoid`


  return {
    update: update,
    flush: flush
  };
} // @NOTE in the future this may be exposed via config


function defaultSerialize(data) {
  return JSON.stringify(data);
}
},{"./constants":"../node_modules/redux-persist/es/constants.js"}],"../node_modules/redux-persist/es/getStoredState.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getStoredState;

var _constants = require("./constants");

function getStoredState(config) {
  var transforms = config.transforms || [];
  var storageKey = "".concat(config.keyPrefix !== undefined ? config.keyPrefix : _constants.KEY_PREFIX).concat(config.key);
  var storage = config.storage;
  var debug = config.debug;
  var deserialize;

  if (config.deserialize === false) {
    deserialize = function deserialize(x) {
      return x;
    };
  } else if (typeof config.deserialize === 'function') {
    deserialize = config.deserialize;
  } else {
    deserialize = defaultDeserialize;
  }

  return storage.getItem(storageKey).then(function (serialized) {
    if (!serialized) return undefined;else {
      try {
        var state = {};
        var rawState = deserialize(serialized);
        Object.keys(rawState).forEach(function (key) {
          state[key] = transforms.reduceRight(function (subState, transformer) {
            return transformer.out(subState, key, rawState);
          }, deserialize(rawState[key]));
        });
        return state;
      } catch (err) {
        if ("development" !== 'production' && debug) console.log("redux-persist/getStoredState: Error restoring data ".concat(serialized), err);
        throw err;
      }
    }
  });
}

function defaultDeserialize(serial) {
  return JSON.parse(serial);
}
},{"./constants":"../node_modules/redux-persist/es/constants.js"}],"../node_modules/redux-persist/es/purgeStoredState.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = purgeStoredState;

var _constants = require("./constants");

function purgeStoredState(config) {
  var storage = config.storage;
  var storageKey = "".concat(config.keyPrefix !== undefined ? config.keyPrefix : _constants.KEY_PREFIX).concat(config.key);
  return storage.removeItem(storageKey, warnIfRemoveError);
}

function warnIfRemoveError(err) {
  if (err && "development" !== 'production') {
    console.error('redux-persist/purgeStoredState: Error purging data stored state', err);
  }
}
},{"./constants":"../node_modules/redux-persist/es/constants.js"}],"../node_modules/redux-persist/es/persistReducer.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = persistReducer;

var _constants = require("./constants");

var _autoMergeLevel = _interopRequireDefault(require("./stateReconciler/autoMergeLevel1"));

var _createPersistoid = _interopRequireDefault(require("./createPersistoid"));

var _getStoredState = _interopRequireDefault(require("./getStoredState"));

var _purgeStoredState = _interopRequireDefault(require("./purgeStoredState"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function ownKeys(object, enumerableOnly) {
  var keys = Object.keys(object);

  if (Object.getOwnPropertySymbols) {
    var symbols = Object.getOwnPropertySymbols(object);
    if (enumerableOnly) symbols = symbols.filter(function (sym) {
      return Object.getOwnPropertyDescriptor(object, sym).enumerable;
    });
    keys.push.apply(keys, symbols);
  }

  return keys;
}

function _objectSpread(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i] != null ? arguments[i] : {};

    if (i % 2) {
      ownKeys(source, true).forEach(function (key) {
        _defineProperty(target, key, source[key]);
      });
    } else if (Object.getOwnPropertyDescriptors) {
      Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
    } else {
      ownKeys(source).forEach(function (key) {
        Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
      });
    }
  }

  return target;
}

function _defineProperty(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
  } else {
    obj[key] = value;
  }

  return obj;
}

function _objectWithoutProperties(source, excluded) {
  if (source == null) return {};

  var target = _objectWithoutPropertiesLoose(source, excluded);

  var key, i;

  if (Object.getOwnPropertySymbols) {
    var sourceSymbolKeys = Object.getOwnPropertySymbols(source);

    for (i = 0; i < sourceSymbolKeys.length; i++) {
      key = sourceSymbolKeys[i];
      if (excluded.indexOf(key) >= 0) continue;
      if (!Object.prototype.propertyIsEnumerable.call(source, key)) continue;
      target[key] = source[key];
    }
  }

  return target;
}

function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null) return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;

  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0) continue;
    target[key] = source[key];
  }

  return target;
}

var DEFAULT_TIMEOUT = 5000;
/*
  @TODO add validation / handling for:
  - persisting a reducer which has nested _persist
  - handling actions that fire before reydrate is called
*/

function persistReducer(config, baseReducer) {
  if ("development" !== 'production') {
    if (!config) throw new Error('config is required for persistReducer');
    if (!config.key) throw new Error('key is required in persistor config');
    if (!config.storage) throw new Error("redux-persist: config.storage is required. Try using one of the provided storage engines `import storage from 'redux-persist/lib/storage'`");
  }

  var version = config.version !== undefined ? config.version : _constants.DEFAULT_VERSION;
  var debug = config.debug || false;
  var stateReconciler = config.stateReconciler === undefined ? _autoMergeLevel.default : config.stateReconciler;
  var getStoredState = config.getStoredState || _getStoredState.default;
  var timeout = config.timeout !== undefined ? config.timeout : DEFAULT_TIMEOUT;
  var _persistoid = null;
  var _purge = false;
  var _paused = true;

  var conditionalUpdate = function conditionalUpdate(state) {
    // update the persistoid only if we are rehydrated and not paused
    state._persist.rehydrated && _persistoid && !_paused && _persistoid.update(state);
    return state;
  };

  return function (state, action) {
    var _ref = state || {},
        _persist = _ref._persist,
        rest = _objectWithoutProperties(_ref, ["_persist"]); // $FlowIgnore need to update State type


    var restState = rest;

    if (action.type === _constants.PERSIST) {
      var _sealed = false;

      var _rehydrate = function _rehydrate(payload, err) {
        // dev warning if we are already sealed
        if ("development" !== 'production' && _sealed) console.error("redux-persist: rehydrate for \"".concat(config.key, "\" called after timeout."), payload, err); // only rehydrate if we are not already sealed

        if (!_sealed) {
          action.rehydrate(config.key, payload, err);
          _sealed = true;
        }
      };

      timeout && setTimeout(function () {
        !_sealed && _rehydrate(undefined, new Error("redux-persist: persist timed out for persist key \"".concat(config.key, "\"")));
      }, timeout); // @NOTE PERSIST resumes if paused.

      _paused = false; // @NOTE only ever create persistoid once, ensure we call it at least once, even if _persist has already been set

      if (!_persistoid) _persistoid = (0, _createPersistoid.default)(config); // @NOTE PERSIST can be called multiple times, noop after the first

      if (_persist) {
        // We still need to call the base reducer because there might be nested
        // uses of persistReducer which need to be aware of the PERSIST action
        return _objectSpread({}, baseReducer(restState, action), {
          _persist: _persist
        });
      }

      if (typeof action.rehydrate !== 'function' || typeof action.register !== 'function') throw new Error('redux-persist: either rehydrate or register is not a function on the PERSIST action. This can happen if the action is being replayed. This is an unexplored use case, please open an issue and we will figure out a resolution.');
      action.register(config.key);
      getStoredState(config).then(function (restoredState) {
        var migrate = config.migrate || function (s, v) {
          return Promise.resolve(s);
        };

        migrate(restoredState, version).then(function (migratedState) {
          _rehydrate(migratedState);
        }, function (migrateErr) {
          if ("development" !== 'production' && migrateErr) console.error('redux-persist: migration error', migrateErr);

          _rehydrate(undefined, migrateErr);
        });
      }, function (err) {
        _rehydrate(undefined, err);
      });
      return _objectSpread({}, baseReducer(restState, action), {
        _persist: {
          version: version,
          rehydrated: false
        }
      });
    } else if (action.type === _constants.PURGE) {
      _purge = true;
      action.result((0, _purgeStoredState.default)(config));
      return _objectSpread({}, baseReducer(restState, action), {
        _persist: _persist
      });
    } else if (action.type === _constants.FLUSH) {
      action.result(_persistoid && _persistoid.flush());
      return _objectSpread({}, baseReducer(restState, action), {
        _persist: _persist
      });
    } else if (action.type === _constants.PAUSE) {
      _paused = true;
    } else if (action.type === _constants.REHYDRATE) {
      // noop on restState if purging
      if (_purge) return _objectSpread({}, restState, {
        _persist: _objectSpread({}, _persist, {
          rehydrated: true
        }) // @NOTE if key does not match, will continue to default else below

      });

      if (action.key === config.key) {
        var reducedState = baseReducer(restState, action);
        var inboundState = action.payload; // only reconcile state if stateReconciler and inboundState are both defined

        var reconciledRest = stateReconciler !== false && inboundState !== undefined ? stateReconciler(inboundState, state, reducedState, config) : reducedState;

        var _newState = _objectSpread({}, reconciledRest, {
          _persist: _objectSpread({}, _persist, {
            rehydrated: true
          })
        });

        return conditionalUpdate(_newState);
      }
    } // if we have not already handled PERSIST, straight passthrough


    if (!_persist) return baseReducer(state, action); // run base reducer:
    // is state modified ? return original : return updated

    var newState = baseReducer(restState, action);
    if (newState === restState) return state;
    return conditionalUpdate(_objectSpread({}, newState, {
      _persist: _persist
    }));
  };
}
},{"./constants":"../node_modules/redux-persist/es/constants.js","./stateReconciler/autoMergeLevel1":"../node_modules/redux-persist/es/stateReconciler/autoMergeLevel1.js","./createPersistoid":"../node_modules/redux-persist/es/createPersistoid.js","./getStoredState":"../node_modules/redux-persist/es/getStoredState.js","./purgeStoredState":"../node_modules/redux-persist/es/purgeStoredState.js"}],"../node_modules/redux-persist/es/stateReconciler/autoMergeLevel2.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = autoMergeLevel2;

function _typeof(obj) {
  if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
    _typeof = function _typeof(obj) {
      return typeof obj;
    };
  } else {
    _typeof = function _typeof(obj) {
      return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    };
  }

  return _typeof(obj);
}

function ownKeys(object, enumerableOnly) {
  var keys = Object.keys(object);

  if (Object.getOwnPropertySymbols) {
    var symbols = Object.getOwnPropertySymbols(object);
    if (enumerableOnly) symbols = symbols.filter(function (sym) {
      return Object.getOwnPropertyDescriptor(object, sym).enumerable;
    });
    keys.push.apply(keys, symbols);
  }

  return keys;
}

function _objectSpread(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i] != null ? arguments[i] : {};

    if (i % 2) {
      ownKeys(source, true).forEach(function (key) {
        _defineProperty(target, key, source[key]);
      });
    } else if (Object.getOwnPropertyDescriptors) {
      Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
    } else {
      ownKeys(source).forEach(function (key) {
        Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
      });
    }
  }

  return target;
}

function _defineProperty(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
  } else {
    obj[key] = value;
  }

  return obj;
}
/*
  autoMergeLevel2: 
    - merges 2 level of substate
    - skips substate if already modified
    - this is essentially redux-perist v4 behavior
*/


function autoMergeLevel2(inboundState, originalState, reducedState, _ref) {
  var debug = _ref.debug;

  var newState = _objectSpread({}, reducedState); // only rehydrate if inboundState exists and is an object


  if (inboundState && _typeof(inboundState) === 'object') {
    Object.keys(inboundState).forEach(function (key) {
      // ignore _persist data
      if (key === '_persist') return; // if reducer modifies substate, skip auto rehydration

      if (originalState[key] !== reducedState[key]) {
        if ("development" !== 'production' && debug) console.log('redux-persist/stateReconciler: sub state for key `%s` modified, skipping.', key);
        return;
      }

      if (isPlainEnoughObject(reducedState[key])) {
        // if object is plain enough shallow merge the new values (hence "Level2")
        newState[key] = _objectSpread({}, newState[key], {}, inboundState[key]);
        return;
      } // otherwise hard set


      newState[key] = inboundState[key];
    });
  }

  if ("development" !== 'production' && debug && inboundState && _typeof(inboundState) === 'object') console.log("redux-persist/stateReconciler: rehydrated keys '".concat(Object.keys(inboundState).join(', '), "'"));
  return newState;
}

function isPlainEnoughObject(o) {
  return o !== null && !Array.isArray(o) && _typeof(o) === 'object';
}
},{}],"../node_modules/redux-persist/es/persistCombineReducers.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = persistCombineReducers;

var _redux = require("redux");

var _persistReducer = _interopRequireDefault(require("./persistReducer"));

var _autoMergeLevel = _interopRequireDefault(require("./stateReconciler/autoMergeLevel2"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// combineReducers + persistReducer with stateReconciler defaulted to autoMergeLevel2
function persistCombineReducers(config, reducers) {
  config.stateReconciler = config.stateReconciler === undefined ? _autoMergeLevel.default : config.stateReconciler;
  return (0, _persistReducer.default)(config, (0, _redux.combineReducers)(reducers));
}
},{"redux":"../node_modules/redux/es/redux.js","./persistReducer":"../node_modules/redux-persist/es/persistReducer.js","./stateReconciler/autoMergeLevel2":"../node_modules/redux-persist/es/stateReconciler/autoMergeLevel2.js"}],"../node_modules/redux-persist/es/persistStore.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = persistStore;

var _redux = require("redux");

var _constants = require("./constants");

function _toConsumableArray(arr) {
  return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread();
}

function _nonIterableSpread() {
  throw new TypeError("Invalid attempt to spread non-iterable instance");
}

function _iterableToArray(iter) {
  if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter);
}

function _arrayWithoutHoles(arr) {
  if (Array.isArray(arr)) {
    for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) {
      arr2[i] = arr[i];
    }

    return arr2;
  }
}

function ownKeys(object, enumerableOnly) {
  var keys = Object.keys(object);

  if (Object.getOwnPropertySymbols) {
    var symbols = Object.getOwnPropertySymbols(object);
    if (enumerableOnly) symbols = symbols.filter(function (sym) {
      return Object.getOwnPropertyDescriptor(object, sym).enumerable;
    });
    keys.push.apply(keys, symbols);
  }

  return keys;
}

function _objectSpread(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i] != null ? arguments[i] : {};

    if (i % 2) {
      ownKeys(source, true).forEach(function (key) {
        _defineProperty(target, key, source[key]);
      });
    } else if (Object.getOwnPropertyDescriptors) {
      Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
    } else {
      ownKeys(source).forEach(function (key) {
        Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
      });
    }
  }

  return target;
}

function _defineProperty(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
  } else {
    obj[key] = value;
  }

  return obj;
}

var initialState = {
  registry: [],
  bootstrapped: false
};

var persistorReducer = function persistorReducer() {
  var state = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : initialState;
  var action = arguments.length > 1 ? arguments[1] : undefined;

  switch (action.type) {
    case _constants.REGISTER:
      return _objectSpread({}, state, {
        registry: [].concat(_toConsumableArray(state.registry), [action.key])
      });

    case _constants.REHYDRATE:
      var firstIndex = state.registry.indexOf(action.key);

      var registry = _toConsumableArray(state.registry);

      registry.splice(firstIndex, 1);
      return _objectSpread({}, state, {
        registry: registry,
        bootstrapped: registry.length === 0
      });

    default:
      return state;
  }
};

function persistStore(store, options, cb) {
  // help catch incorrect usage of passing PersistConfig in as PersistorOptions
  if ("development" !== 'production') {
    var optionsToTest = options || {};
    var bannedKeys = ['blacklist', 'whitelist', 'transforms', 'storage', 'keyPrefix', 'migrate'];
    bannedKeys.forEach(function (k) {
      if (!!optionsToTest[k]) console.error("redux-persist: invalid option passed to persistStore: \"".concat(k, "\". You may be incorrectly passing persistConfig into persistStore, whereas it should be passed into persistReducer."));
    });
  }

  var boostrappedCb = cb || false;

  var _pStore = (0, _redux.createStore)(persistorReducer, initialState, options && options.enhancer ? options.enhancer : undefined);

  var register = function register(key) {
    _pStore.dispatch({
      type: _constants.REGISTER,
      key: key
    });
  };

  var rehydrate = function rehydrate(key, payload, err) {
    var rehydrateAction = {
      type: _constants.REHYDRATE,
      payload: payload,
      err: err,
      key: key // dispatch to `store` to rehydrate and `persistor` to track result

    };
    store.dispatch(rehydrateAction);

    _pStore.dispatch(rehydrateAction);

    if (boostrappedCb && persistor.getState().bootstrapped) {
      boostrappedCb();
      boostrappedCb = false;
    }
  };

  var persistor = _objectSpread({}, _pStore, {
    purge: function purge() {
      var results = [];
      store.dispatch({
        type: _constants.PURGE,
        result: function result(purgeResult) {
          results.push(purgeResult);
        }
      });
      return Promise.all(results);
    },
    flush: function flush() {
      var results = [];
      store.dispatch({
        type: _constants.FLUSH,
        result: function result(flushResult) {
          results.push(flushResult);
        }
      });
      return Promise.all(results);
    },
    pause: function pause() {
      store.dispatch({
        type: _constants.PAUSE
      });
    },
    persist: function persist() {
      store.dispatch({
        type: _constants.PERSIST,
        register: register,
        rehydrate: rehydrate
      });
    }
  });

  if (!(options && options.manualPersist)) {
    persistor.persist();
  }

  return persistor;
}
},{"redux":"../node_modules/redux/es/redux.js","./constants":"../node_modules/redux-persist/es/constants.js"}],"../node_modules/redux-persist/es/createMigrate.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = createMigrate;

var _constants = require("./constants");

function createMigrate(migrations, config) {
  var _ref = config || {},
      debug = _ref.debug;

  return function (state, currentVersion) {
    if (!state) {
      if ("development" !== 'production' && debug) console.log('redux-persist: no inbound state, skipping migration');
      return Promise.resolve(undefined);
    }

    var inboundVersion = state._persist && state._persist.version !== undefined ? state._persist.version : _constants.DEFAULT_VERSION;

    if (inboundVersion === currentVersion) {
      if ("development" !== 'production' && debug) console.log('redux-persist: versions match, noop migration');
      return Promise.resolve(state);
    }

    if (inboundVersion > currentVersion) {
      if ("development" !== 'production') console.error('redux-persist: downgrading version is not supported');
      return Promise.resolve(state);
    }

    var migrationKeys = Object.keys(migrations).map(function (ver) {
      return parseInt(ver);
    }).filter(function (key) {
      return currentVersion >= key && key > inboundVersion;
    }).sort(function (a, b) {
      return a - b;
    });
    if ("development" !== 'production' && debug) console.log('redux-persist: migrationKeys', migrationKeys);

    try {
      var migratedState = migrationKeys.reduce(function (state, versionKey) {
        if ("development" !== 'production' && debug) console.log('redux-persist: running migration for versionKey', versionKey);
        return migrations[versionKey](state);
      }, state);
      return Promise.resolve(migratedState);
    } catch (err) {
      return Promise.reject(err);
    }
  };
}
},{"./constants":"../node_modules/redux-persist/es/constants.js"}],"../node_modules/redux-persist/es/createTransform.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = createTransform;

function createTransform( // @NOTE inbound: transform state coming from redux on its way to being serialized and stored
inbound, // @NOTE outbound: transform state coming from storage, on its way to be rehydrated into redux
outbound) {
  var config = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
  var whitelist = config.whitelist || null;
  var blacklist = config.blacklist || null;

  function whitelistBlacklistCheck(key) {
    if (whitelist && whitelist.indexOf(key) === -1) return true;
    if (blacklist && blacklist.indexOf(key) !== -1) return true;
    return false;
  }

  return {
    in: function _in(state, key, fullState) {
      return !whitelistBlacklistCheck(key) && inbound ? inbound(state, key, fullState) : state;
    },
    out: function out(state, key, fullState) {
      return !whitelistBlacklistCheck(key) && outbound ? outbound(state, key, fullState) : state;
    }
  };
}
},{}],"../node_modules/redux-persist/es/index.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
var _exportNames = {
  persistReducer: true,
  persistCombineReducers: true,
  persistStore: true,
  createMigrate: true,
  createTransform: true,
  getStoredState: true,
  createPersistoid: true,
  purgeStoredState: true
};
Object.defineProperty(exports, "persistReducer", {
  enumerable: true,
  get: function () {
    return _persistReducer.default;
  }
});
Object.defineProperty(exports, "persistCombineReducers", {
  enumerable: true,
  get: function () {
    return _persistCombineReducers.default;
  }
});
Object.defineProperty(exports, "persistStore", {
  enumerable: true,
  get: function () {
    return _persistStore.default;
  }
});
Object.defineProperty(exports, "createMigrate", {
  enumerable: true,
  get: function () {
    return _createMigrate.default;
  }
});
Object.defineProperty(exports, "createTransform", {
  enumerable: true,
  get: function () {
    return _createTransform.default;
  }
});
Object.defineProperty(exports, "getStoredState", {
  enumerable: true,
  get: function () {
    return _getStoredState.default;
  }
});
Object.defineProperty(exports, "createPersistoid", {
  enumerable: true,
  get: function () {
    return _createPersistoid.default;
  }
});
Object.defineProperty(exports, "purgeStoredState", {
  enumerable: true,
  get: function () {
    return _purgeStoredState.default;
  }
});

var _persistReducer = _interopRequireDefault(require("./persistReducer"));

var _persistCombineReducers = _interopRequireDefault(require("./persistCombineReducers"));

var _persistStore = _interopRequireDefault(require("./persistStore"));

var _createMigrate = _interopRequireDefault(require("./createMigrate"));

var _createTransform = _interopRequireDefault(require("./createTransform"));

var _getStoredState = _interopRequireDefault(require("./getStoredState"));

var _createPersistoid = _interopRequireDefault(require("./createPersistoid"));

var _purgeStoredState = _interopRequireDefault(require("./purgeStoredState"));

var _constants = require("./constants");

Object.keys(_constants).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function () {
      return _constants[key];
    }
  });
});

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
},{"./persistReducer":"../node_modules/redux-persist/es/persistReducer.js","./persistCombineReducers":"../node_modules/redux-persist/es/persistCombineReducers.js","./persistStore":"../node_modules/redux-persist/es/persistStore.js","./createMigrate":"../node_modules/redux-persist/es/createMigrate.js","./createTransform":"../node_modules/redux-persist/es/createTransform.js","./getStoredState":"../node_modules/redux-persist/es/getStoredState.js","./createPersistoid":"../node_modules/redux-persist/es/createPersistoid.js","./purgeStoredState":"../node_modules/redux-persist/es/purgeStoredState.js","./constants":"../node_modules/redux-persist/es/constants.js"}],"../node_modules/redux-persist/lib/storage/getStorage.js":[function(require,module,exports) {
"use strict";

exports.__esModule = true;
exports.default = getStorage;

function _typeof(obj) {
  if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
    _typeof = function _typeof(obj) {
      return typeof obj;
    };
  } else {
    _typeof = function _typeof(obj) {
      return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    };
  }

  return _typeof(obj);
}

function noop() {}

var noopStorage = {
  getItem: noop,
  setItem: noop,
  removeItem: noop
};

function hasStorage(storageType) {
  if ((typeof self === "undefined" ? "undefined" : _typeof(self)) !== 'object' || !(storageType in self)) {
    return false;
  }

  try {
    var storage = self[storageType];
    var testKey = "redux-persist ".concat(storageType, " test");
    storage.setItem(testKey, 'test');
    storage.getItem(testKey);
    storage.removeItem(testKey);
  } catch (e) {
    if ("development" !== 'production') console.warn("redux-persist ".concat(storageType, " test failed, persistence will be disabled."));
    return false;
  }

  return true;
}

function getStorage(type) {
  var storageType = "".concat(type, "Storage");
  if (hasStorage(storageType)) return self[storageType];else {
    if ("development" !== 'production') {
      console.error("redux-persist failed to create sync storage. falling back to noop storage.");
    }

    return noopStorage;
  }
}
},{}],"../node_modules/redux-persist/lib/storage/createWebStorage.js":[function(require,module,exports) {
"use strict";

exports.__esModule = true;
exports.default = createWebStorage;

var _getStorage = _interopRequireDefault(require("./getStorage"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function createWebStorage(type) {
  var storage = (0, _getStorage.default)(type);
  return {
    getItem: function getItem(key) {
      return new Promise(function (resolve, reject) {
        resolve(storage.getItem(key));
      });
    },
    setItem: function setItem(key, item) {
      return new Promise(function (resolve, reject) {
        resolve(storage.setItem(key, item));
      });
    },
    removeItem: function removeItem(key) {
      return new Promise(function (resolve, reject) {
        resolve(storage.removeItem(key));
      });
    }
  };
}
},{"./getStorage":"../node_modules/redux-persist/lib/storage/getStorage.js"}],"../node_modules/redux-persist/lib/storage/index.js":[function(require,module,exports) {
"use strict";

exports.__esModule = true;
exports.default = void 0;

var _createWebStorage = _interopRequireDefault(require("./createWebStorage"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var _default = (0, _createWebStorage.default)('local');

exports.default = _default;
},{"./createWebStorage":"../node_modules/redux-persist/lib/storage/createWebStorage.js"}],"redux/store.ts":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.persistor = exports.store = void 0;

var _redux = require("redux");

var _reduxSaga = _interopRequireDefault(require("redux-saga"));

var _nameListStore = require("./nameListStore");

var _stackStore = require("./stackStore");

var _sagas = require("./sagas");

var _reduxPersist = require("redux-persist");

var _storage = _interopRequireDefault(require("redux-persist/lib/storage"));

var _appStore = require("./appStore");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _typeof(obj) { if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; } return _typeof(obj); }

var reducer = (0, _redux.combineReducers)({
  app: _appStore.appReducer,
  names: _nameListStore.nameListReducer,
  stack: _stackStore.stackReducer
});
var sagaMiddleware = (0, _reduxSaga.default)();
var composeEnhancers = (typeof window === "undefined" ? "undefined" : _typeof(window)) === "object" && window["__REDUX_DEVTOOLS_EXTENSION_COMPOSE__"] ? window["__REDUX_DEVTOOLS_EXTENSION_COMPOSE__"]({}) : _redux.compose;
var persistConfig = {
  key: "root",
  storage: _storage.default
};
var persistedReducer = (0, _reduxPersist.persistReducer)(persistConfig, reducer);
var enhancer = composeEnhancers((0, _redux.applyMiddleware)(sagaMiddleware));
var store = (0, _redux.createStore)(persistedReducer, enhancer);
exports.store = store;
var persistor = (0, _reduxPersist.persistStore)(store);
exports.persistor = persistor;
sagaMiddleware.run(_sagas.sagas);
},{"redux":"../node_modules/redux/es/redux.js","redux-saga":"../node_modules/redux-saga/dist/redux-saga-core-npm-proxy.esm.js","./nameListStore":"redux/nameListStore.ts","./stackStore":"redux/stackStore.ts","./sagas":"redux/sagas.ts","redux-persist":"../node_modules/redux-persist/es/index.js","redux-persist/lib/storage":"../node_modules/redux-persist/lib/storage/index.js","./appStore":"redux/appStore.ts"}],"index.less":[function(require,module,exports) {
var reloadCSS = require('_css_loader');

module.hot.dispose(reloadCSS);
module.hot.accept(reloadCSS);
module.exports = {};
},{"_css_loader":"../node_modules/parcel-bundler/src/builtins/css-loader.js"}],"tailwind.less":[function(require,module,exports) {
var reloadCSS = require('_css_loader');

module.hot.dispose(reloadCSS);
module.hot.accept(reloadCSS);
module.exports = {
  "sm:sr-only": "_sm:sr-only_6d3e9",
  "sm:not-sr-only": "_sm:not-sr-only_6d3e9",
  "sm:focus:sr-only": "_sm:focus:sr-only_6d3e9",
  "sm:focus:not-sr-only": "_sm:focus:not-sr-only_6d3e9",
  "sm:appearance-none": "_sm:appearance-none_6d3e9",
  "sm:bg-fixed": "_sm:bg-fixed_6d3e9",
  "sm:bg-local": "_sm:bg-local_6d3e9",
  "sm:bg-scroll": "_sm:bg-scroll_6d3e9",
  "sm:bg-transparent": "_sm:bg-transparent_6d3e9",
  "sm:bg-black": "_sm:bg-black_6d3e9",
  "sm:bg-white": "_sm:bg-white_6d3e9",
  "sm:bg-gray-100": "_sm:bg-gray-100_6d3e9",
  "sm:bg-gray-200": "_sm:bg-gray-200_6d3e9",
  "sm:bg-gray-300": "_sm:bg-gray-300_6d3e9",
  "sm:bg-gray-400": "_sm:bg-gray-400_6d3e9",
  "sm:bg-gray-500": "_sm:bg-gray-500_6d3e9",
  "sm:bg-gray-600": "_sm:bg-gray-600_6d3e9",
  "sm:bg-gray-700": "_sm:bg-gray-700_6d3e9",
  "sm:bg-gray-800": "_sm:bg-gray-800_6d3e9",
  "sm:bg-gray-900": "_sm:bg-gray-900_6d3e9",
  "sm:bg-red-100": "_sm:bg-red-100_6d3e9",
  "sm:bg-red-200": "_sm:bg-red-200_6d3e9",
  "sm:bg-red-300": "_sm:bg-red-300_6d3e9",
  "sm:bg-red-400": "_sm:bg-red-400_6d3e9",
  "sm:bg-red-500": "_sm:bg-red-500_6d3e9",
  "sm:bg-red-600": "_sm:bg-red-600_6d3e9",
  "sm:bg-red-700": "_sm:bg-red-700_6d3e9",
  "sm:bg-red-800": "_sm:bg-red-800_6d3e9",
  "sm:bg-red-900": "_sm:bg-red-900_6d3e9",
  "sm:bg-orange-100": "_sm:bg-orange-100_6d3e9",
  "sm:bg-orange-200": "_sm:bg-orange-200_6d3e9",
  "sm:bg-orange-300": "_sm:bg-orange-300_6d3e9",
  "sm:bg-orange-400": "_sm:bg-orange-400_6d3e9",
  "sm:bg-orange-500": "_sm:bg-orange-500_6d3e9",
  "sm:bg-orange-600": "_sm:bg-orange-600_6d3e9",
  "sm:bg-orange-700": "_sm:bg-orange-700_6d3e9",
  "sm:bg-orange-800": "_sm:bg-orange-800_6d3e9",
  "sm:bg-orange-900": "_sm:bg-orange-900_6d3e9",
  "sm:bg-yellow-100": "_sm:bg-yellow-100_6d3e9",
  "sm:bg-yellow-200": "_sm:bg-yellow-200_6d3e9",
  "sm:bg-yellow-300": "_sm:bg-yellow-300_6d3e9",
  "sm:bg-yellow-400": "_sm:bg-yellow-400_6d3e9",
  "sm:bg-yellow-500": "_sm:bg-yellow-500_6d3e9",
  "sm:bg-yellow-600": "_sm:bg-yellow-600_6d3e9",
  "sm:bg-yellow-700": "_sm:bg-yellow-700_6d3e9",
  "sm:bg-yellow-800": "_sm:bg-yellow-800_6d3e9",
  "sm:bg-yellow-900": "_sm:bg-yellow-900_6d3e9",
  "sm:bg-green-100": "_sm:bg-green-100_6d3e9",
  "sm:bg-green-200": "_sm:bg-green-200_6d3e9",
  "sm:bg-green-300": "_sm:bg-green-300_6d3e9",
  "sm:bg-green-400": "_sm:bg-green-400_6d3e9",
  "sm:bg-green-500": "_sm:bg-green-500_6d3e9",
  "sm:bg-green-600": "_sm:bg-green-600_6d3e9",
  "sm:bg-green-700": "_sm:bg-green-700_6d3e9",
  "sm:bg-green-800": "_sm:bg-green-800_6d3e9",
  "sm:bg-green-900": "_sm:bg-green-900_6d3e9",
  "sm:bg-teal-100": "_sm:bg-teal-100_6d3e9",
  "sm:bg-teal-200": "_sm:bg-teal-200_6d3e9",
  "sm:bg-teal-300": "_sm:bg-teal-300_6d3e9",
  "sm:bg-teal-400": "_sm:bg-teal-400_6d3e9",
  "sm:bg-teal-500": "_sm:bg-teal-500_6d3e9",
  "sm:bg-teal-600": "_sm:bg-teal-600_6d3e9",
  "sm:bg-teal-700": "_sm:bg-teal-700_6d3e9",
  "sm:bg-teal-800": "_sm:bg-teal-800_6d3e9",
  "sm:bg-teal-900": "_sm:bg-teal-900_6d3e9",
  "sm:bg-blue-100": "_sm:bg-blue-100_6d3e9",
  "sm:bg-blue-200": "_sm:bg-blue-200_6d3e9",
  "sm:bg-blue-300": "_sm:bg-blue-300_6d3e9",
  "sm:bg-blue-400": "_sm:bg-blue-400_6d3e9",
  "sm:bg-blue-500": "_sm:bg-blue-500_6d3e9",
  "sm:bg-blue-600": "_sm:bg-blue-600_6d3e9",
  "sm:bg-blue-700": "_sm:bg-blue-700_6d3e9",
  "sm:bg-blue-800": "_sm:bg-blue-800_6d3e9",
  "sm:bg-blue-900": "_sm:bg-blue-900_6d3e9",
  "sm:bg-indigo-100": "_sm:bg-indigo-100_6d3e9",
  "sm:bg-indigo-200": "_sm:bg-indigo-200_6d3e9",
  "sm:bg-indigo-300": "_sm:bg-indigo-300_6d3e9",
  "sm:bg-indigo-400": "_sm:bg-indigo-400_6d3e9",
  "sm:bg-indigo-500": "_sm:bg-indigo-500_6d3e9",
  "sm:bg-indigo-600": "_sm:bg-indigo-600_6d3e9",
  "sm:bg-indigo-700": "_sm:bg-indigo-700_6d3e9",
  "sm:bg-indigo-800": "_sm:bg-indigo-800_6d3e9",
  "sm:bg-indigo-900": "_sm:bg-indigo-900_6d3e9",
  "sm:bg-purple-100": "_sm:bg-purple-100_6d3e9",
  "sm:bg-purple-200": "_sm:bg-purple-200_6d3e9",
  "sm:bg-purple-300": "_sm:bg-purple-300_6d3e9",
  "sm:bg-purple-400": "_sm:bg-purple-400_6d3e9",
  "sm:bg-purple-500": "_sm:bg-purple-500_6d3e9",
  "sm:bg-purple-600": "_sm:bg-purple-600_6d3e9",
  "sm:bg-purple-700": "_sm:bg-purple-700_6d3e9",
  "sm:bg-purple-800": "_sm:bg-purple-800_6d3e9",
  "sm:bg-purple-900": "_sm:bg-purple-900_6d3e9",
  "sm:bg-pink-100": "_sm:bg-pink-100_6d3e9",
  "sm:bg-pink-200": "_sm:bg-pink-200_6d3e9",
  "sm:bg-pink-300": "_sm:bg-pink-300_6d3e9",
  "sm:bg-pink-400": "_sm:bg-pink-400_6d3e9",
  "sm:bg-pink-500": "_sm:bg-pink-500_6d3e9",
  "sm:bg-pink-600": "_sm:bg-pink-600_6d3e9",
  "sm:bg-pink-700": "_sm:bg-pink-700_6d3e9",
  "sm:bg-pink-800": "_sm:bg-pink-800_6d3e9",
  "sm:bg-pink-900": "_sm:bg-pink-900_6d3e9",
  "sm:bg-primary-100": "_sm:bg-primary-100_6d3e9",
  "sm:bg-primary-200": "_sm:bg-primary-200_6d3e9",
  "sm:bg-primary-300": "_sm:bg-primary-300_6d3e9",
  "sm:bg-primary-400": "_sm:bg-primary-400_6d3e9",
  "sm:bg-primary-500": "_sm:bg-primary-500_6d3e9",
  "sm:bg-primary-600": "_sm:bg-primary-600_6d3e9",
  "sm:bg-primary-700": "_sm:bg-primary-700_6d3e9",
  "sm:bg-primary-800": "_sm:bg-primary-800_6d3e9",
  "sm:bg-primary-900": "_sm:bg-primary-900_6d3e9",
  "sm:bg-secondary-100": "_sm:bg-secondary-100_6d3e9",
  "sm:bg-secondary-200": "_sm:bg-secondary-200_6d3e9",
  "sm:bg-secondary-300": "_sm:bg-secondary-300_6d3e9",
  "sm:bg-secondary-400": "_sm:bg-secondary-400_6d3e9",
  "sm:bg-secondary-500": "_sm:bg-secondary-500_6d3e9",
  "sm:bg-secondary-600": "_sm:bg-secondary-600_6d3e9",
  "sm:bg-secondary-700": "_sm:bg-secondary-700_6d3e9",
  "sm:bg-secondary-800": "_sm:bg-secondary-800_6d3e9",
  "sm:bg-secondary-900": "_sm:bg-secondary-900_6d3e9",
  "sm:hover:bg-transparent": "_sm:hover:bg-transparent_6d3e9",
  "sm:hover:bg-black": "_sm:hover:bg-black_6d3e9",
  "sm:hover:bg-white": "_sm:hover:bg-white_6d3e9",
  "sm:hover:bg-gray-100": "_sm:hover:bg-gray-100_6d3e9",
  "sm:hover:bg-gray-200": "_sm:hover:bg-gray-200_6d3e9",
  "sm:hover:bg-gray-300": "_sm:hover:bg-gray-300_6d3e9",
  "sm:hover:bg-gray-400": "_sm:hover:bg-gray-400_6d3e9",
  "sm:hover:bg-gray-500": "_sm:hover:bg-gray-500_6d3e9",
  "sm:hover:bg-gray-600": "_sm:hover:bg-gray-600_6d3e9",
  "sm:hover:bg-gray-700": "_sm:hover:bg-gray-700_6d3e9",
  "sm:hover:bg-gray-800": "_sm:hover:bg-gray-800_6d3e9",
  "sm:hover:bg-gray-900": "_sm:hover:bg-gray-900_6d3e9",
  "sm:hover:bg-red-100": "_sm:hover:bg-red-100_6d3e9",
  "sm:hover:bg-red-200": "_sm:hover:bg-red-200_6d3e9",
  "sm:hover:bg-red-300": "_sm:hover:bg-red-300_6d3e9",
  "sm:hover:bg-red-400": "_sm:hover:bg-red-400_6d3e9",
  "sm:hover:bg-red-500": "_sm:hover:bg-red-500_6d3e9",
  "sm:hover:bg-red-600": "_sm:hover:bg-red-600_6d3e9",
  "sm:hover:bg-red-700": "_sm:hover:bg-red-700_6d3e9",
  "sm:hover:bg-red-800": "_sm:hover:bg-red-800_6d3e9",
  "sm:hover:bg-red-900": "_sm:hover:bg-red-900_6d3e9",
  "sm:hover:bg-orange-100": "_sm:hover:bg-orange-100_6d3e9",
  "sm:hover:bg-orange-200": "_sm:hover:bg-orange-200_6d3e9",
  "sm:hover:bg-orange-300": "_sm:hover:bg-orange-300_6d3e9",
  "sm:hover:bg-orange-400": "_sm:hover:bg-orange-400_6d3e9",
  "sm:hover:bg-orange-500": "_sm:hover:bg-orange-500_6d3e9",
  "sm:hover:bg-orange-600": "_sm:hover:bg-orange-600_6d3e9",
  "sm:hover:bg-orange-700": "_sm:hover:bg-orange-700_6d3e9",
  "sm:hover:bg-orange-800": "_sm:hover:bg-orange-800_6d3e9",
  "sm:hover:bg-orange-900": "_sm:hover:bg-orange-900_6d3e9",
  "sm:hover:bg-yellow-100": "_sm:hover:bg-yellow-100_6d3e9",
  "sm:hover:bg-yellow-200": "_sm:hover:bg-yellow-200_6d3e9",
  "sm:hover:bg-yellow-300": "_sm:hover:bg-yellow-300_6d3e9",
  "sm:hover:bg-yellow-400": "_sm:hover:bg-yellow-400_6d3e9",
  "sm:hover:bg-yellow-500": "_sm:hover:bg-yellow-500_6d3e9",
  "sm:hover:bg-yellow-600": "_sm:hover:bg-yellow-600_6d3e9",
  "sm:hover:bg-yellow-700": "_sm:hover:bg-yellow-700_6d3e9",
  "sm:hover:bg-yellow-800": "_sm:hover:bg-yellow-800_6d3e9",
  "sm:hover:bg-yellow-900": "_sm:hover:bg-yellow-900_6d3e9",
  "sm:hover:bg-green-100": "_sm:hover:bg-green-100_6d3e9",
  "sm:hover:bg-green-200": "_sm:hover:bg-green-200_6d3e9",
  "sm:hover:bg-green-300": "_sm:hover:bg-green-300_6d3e9",
  "sm:hover:bg-green-400": "_sm:hover:bg-green-400_6d3e9",
  "sm:hover:bg-green-500": "_sm:hover:bg-green-500_6d3e9",
  "sm:hover:bg-green-600": "_sm:hover:bg-green-600_6d3e9",
  "sm:hover:bg-green-700": "_sm:hover:bg-green-700_6d3e9",
  "sm:hover:bg-green-800": "_sm:hover:bg-green-800_6d3e9",
  "sm:hover:bg-green-900": "_sm:hover:bg-green-900_6d3e9",
  "sm:hover:bg-teal-100": "_sm:hover:bg-teal-100_6d3e9",
  "sm:hover:bg-teal-200": "_sm:hover:bg-teal-200_6d3e9",
  "sm:hover:bg-teal-300": "_sm:hover:bg-teal-300_6d3e9",
  "sm:hover:bg-teal-400": "_sm:hover:bg-teal-400_6d3e9",
  "sm:hover:bg-teal-500": "_sm:hover:bg-teal-500_6d3e9",
  "sm:hover:bg-teal-600": "_sm:hover:bg-teal-600_6d3e9",
  "sm:hover:bg-teal-700": "_sm:hover:bg-teal-700_6d3e9",
  "sm:hover:bg-teal-800": "_sm:hover:bg-teal-800_6d3e9",
  "sm:hover:bg-teal-900": "_sm:hover:bg-teal-900_6d3e9",
  "sm:hover:bg-blue-100": "_sm:hover:bg-blue-100_6d3e9",
  "sm:hover:bg-blue-200": "_sm:hover:bg-blue-200_6d3e9",
  "sm:hover:bg-blue-300": "_sm:hover:bg-blue-300_6d3e9",
  "sm:hover:bg-blue-400": "_sm:hover:bg-blue-400_6d3e9",
  "sm:hover:bg-blue-500": "_sm:hover:bg-blue-500_6d3e9",
  "sm:hover:bg-blue-600": "_sm:hover:bg-blue-600_6d3e9",
  "sm:hover:bg-blue-700": "_sm:hover:bg-blue-700_6d3e9",
  "sm:hover:bg-blue-800": "_sm:hover:bg-blue-800_6d3e9",
  "sm:hover:bg-blue-900": "_sm:hover:bg-blue-900_6d3e9",
  "sm:hover:bg-indigo-100": "_sm:hover:bg-indigo-100_6d3e9",
  "sm:hover:bg-indigo-200": "_sm:hover:bg-indigo-200_6d3e9",
  "sm:hover:bg-indigo-300": "_sm:hover:bg-indigo-300_6d3e9",
  "sm:hover:bg-indigo-400": "_sm:hover:bg-indigo-400_6d3e9",
  "sm:hover:bg-indigo-500": "_sm:hover:bg-indigo-500_6d3e9",
  "sm:hover:bg-indigo-600": "_sm:hover:bg-indigo-600_6d3e9",
  "sm:hover:bg-indigo-700": "_sm:hover:bg-indigo-700_6d3e9",
  "sm:hover:bg-indigo-800": "_sm:hover:bg-indigo-800_6d3e9",
  "sm:hover:bg-indigo-900": "_sm:hover:bg-indigo-900_6d3e9",
  "sm:hover:bg-purple-100": "_sm:hover:bg-purple-100_6d3e9",
  "sm:hover:bg-purple-200": "_sm:hover:bg-purple-200_6d3e9",
  "sm:hover:bg-purple-300": "_sm:hover:bg-purple-300_6d3e9",
  "sm:hover:bg-purple-400": "_sm:hover:bg-purple-400_6d3e9",
  "sm:hover:bg-purple-500": "_sm:hover:bg-purple-500_6d3e9",
  "sm:hover:bg-purple-600": "_sm:hover:bg-purple-600_6d3e9",
  "sm:hover:bg-purple-700": "_sm:hover:bg-purple-700_6d3e9",
  "sm:hover:bg-purple-800": "_sm:hover:bg-purple-800_6d3e9",
  "sm:hover:bg-purple-900": "_sm:hover:bg-purple-900_6d3e9",
  "sm:hover:bg-pink-100": "_sm:hover:bg-pink-100_6d3e9",
  "sm:hover:bg-pink-200": "_sm:hover:bg-pink-200_6d3e9",
  "sm:hover:bg-pink-300": "_sm:hover:bg-pink-300_6d3e9",
  "sm:hover:bg-pink-400": "_sm:hover:bg-pink-400_6d3e9",
  "sm:hover:bg-pink-500": "_sm:hover:bg-pink-500_6d3e9",
  "sm:hover:bg-pink-600": "_sm:hover:bg-pink-600_6d3e9",
  "sm:hover:bg-pink-700": "_sm:hover:bg-pink-700_6d3e9",
  "sm:hover:bg-pink-800": "_sm:hover:bg-pink-800_6d3e9",
  "sm:hover:bg-pink-900": "_sm:hover:bg-pink-900_6d3e9",
  "sm:hover:bg-primary-100": "_sm:hover:bg-primary-100_6d3e9",
  "sm:hover:bg-primary-200": "_sm:hover:bg-primary-200_6d3e9",
  "sm:hover:bg-primary-300": "_sm:hover:bg-primary-300_6d3e9",
  "sm:hover:bg-primary-400": "_sm:hover:bg-primary-400_6d3e9",
  "sm:hover:bg-primary-500": "_sm:hover:bg-primary-500_6d3e9",
  "sm:hover:bg-primary-600": "_sm:hover:bg-primary-600_6d3e9",
  "sm:hover:bg-primary-700": "_sm:hover:bg-primary-700_6d3e9",
  "sm:hover:bg-primary-800": "_sm:hover:bg-primary-800_6d3e9",
  "sm:hover:bg-primary-900": "_sm:hover:bg-primary-900_6d3e9",
  "sm:hover:bg-secondary-100": "_sm:hover:bg-secondary-100_6d3e9",
  "sm:hover:bg-secondary-200": "_sm:hover:bg-secondary-200_6d3e9",
  "sm:hover:bg-secondary-300": "_sm:hover:bg-secondary-300_6d3e9",
  "sm:hover:bg-secondary-400": "_sm:hover:bg-secondary-400_6d3e9",
  "sm:hover:bg-secondary-500": "_sm:hover:bg-secondary-500_6d3e9",
  "sm:hover:bg-secondary-600": "_sm:hover:bg-secondary-600_6d3e9",
  "sm:hover:bg-secondary-700": "_sm:hover:bg-secondary-700_6d3e9",
  "sm:hover:bg-secondary-800": "_sm:hover:bg-secondary-800_6d3e9",
  "sm:hover:bg-secondary-900": "_sm:hover:bg-secondary-900_6d3e9",
  "sm:focus:bg-transparent": "_sm:focus:bg-transparent_6d3e9",
  "sm:focus:bg-black": "_sm:focus:bg-black_6d3e9",
  "sm:focus:bg-white": "_sm:focus:bg-white_6d3e9",
  "sm:focus:bg-gray-100": "_sm:focus:bg-gray-100_6d3e9",
  "sm:focus:bg-gray-200": "_sm:focus:bg-gray-200_6d3e9",
  "sm:focus:bg-gray-300": "_sm:focus:bg-gray-300_6d3e9",
  "sm:focus:bg-gray-400": "_sm:focus:bg-gray-400_6d3e9",
  "sm:focus:bg-gray-500": "_sm:focus:bg-gray-500_6d3e9",
  "sm:focus:bg-gray-600": "_sm:focus:bg-gray-600_6d3e9",
  "sm:focus:bg-gray-700": "_sm:focus:bg-gray-700_6d3e9",
  "sm:focus:bg-gray-800": "_sm:focus:bg-gray-800_6d3e9",
  "sm:focus:bg-gray-900": "_sm:focus:bg-gray-900_6d3e9",
  "sm:focus:bg-red-100": "_sm:focus:bg-red-100_6d3e9",
  "sm:focus:bg-red-200": "_sm:focus:bg-red-200_6d3e9",
  "sm:focus:bg-red-300": "_sm:focus:bg-red-300_6d3e9",
  "sm:focus:bg-red-400": "_sm:focus:bg-red-400_6d3e9",
  "sm:focus:bg-red-500": "_sm:focus:bg-red-500_6d3e9",
  "sm:focus:bg-red-600": "_sm:focus:bg-red-600_6d3e9",
  "sm:focus:bg-red-700": "_sm:focus:bg-red-700_6d3e9",
  "sm:focus:bg-red-800": "_sm:focus:bg-red-800_6d3e9",
  "sm:focus:bg-red-900": "_sm:focus:bg-red-900_6d3e9",
  "sm:focus:bg-orange-100": "_sm:focus:bg-orange-100_6d3e9",
  "sm:focus:bg-orange-200": "_sm:focus:bg-orange-200_6d3e9",
  "sm:focus:bg-orange-300": "_sm:focus:bg-orange-300_6d3e9",
  "sm:focus:bg-orange-400": "_sm:focus:bg-orange-400_6d3e9",
  "sm:focus:bg-orange-500": "_sm:focus:bg-orange-500_6d3e9",
  "sm:focus:bg-orange-600": "_sm:focus:bg-orange-600_6d3e9",
  "sm:focus:bg-orange-700": "_sm:focus:bg-orange-700_6d3e9",
  "sm:focus:bg-orange-800": "_sm:focus:bg-orange-800_6d3e9",
  "sm:focus:bg-orange-900": "_sm:focus:bg-orange-900_6d3e9",
  "sm:focus:bg-yellow-100": "_sm:focus:bg-yellow-100_6d3e9",
  "sm:focus:bg-yellow-200": "_sm:focus:bg-yellow-200_6d3e9",
  "sm:focus:bg-yellow-300": "_sm:focus:bg-yellow-300_6d3e9",
  "sm:focus:bg-yellow-400": "_sm:focus:bg-yellow-400_6d3e9",
  "sm:focus:bg-yellow-500": "_sm:focus:bg-yellow-500_6d3e9",
  "sm:focus:bg-yellow-600": "_sm:focus:bg-yellow-600_6d3e9",
  "sm:focus:bg-yellow-700": "_sm:focus:bg-yellow-700_6d3e9",
  "sm:focus:bg-yellow-800": "_sm:focus:bg-yellow-800_6d3e9",
  "sm:focus:bg-yellow-900": "_sm:focus:bg-yellow-900_6d3e9",
  "sm:focus:bg-green-100": "_sm:focus:bg-green-100_6d3e9",
  "sm:focus:bg-green-200": "_sm:focus:bg-green-200_6d3e9",
  "sm:focus:bg-green-300": "_sm:focus:bg-green-300_6d3e9",
  "sm:focus:bg-green-400": "_sm:focus:bg-green-400_6d3e9",
  "sm:focus:bg-green-500": "_sm:focus:bg-green-500_6d3e9",
  "sm:focus:bg-green-600": "_sm:focus:bg-green-600_6d3e9",
  "sm:focus:bg-green-700": "_sm:focus:bg-green-700_6d3e9",
  "sm:focus:bg-green-800": "_sm:focus:bg-green-800_6d3e9",
  "sm:focus:bg-green-900": "_sm:focus:bg-green-900_6d3e9",
  "sm:focus:bg-teal-100": "_sm:focus:bg-teal-100_6d3e9",
  "sm:focus:bg-teal-200": "_sm:focus:bg-teal-200_6d3e9",
  "sm:focus:bg-teal-300": "_sm:focus:bg-teal-300_6d3e9",
  "sm:focus:bg-teal-400": "_sm:focus:bg-teal-400_6d3e9",
  "sm:focus:bg-teal-500": "_sm:focus:bg-teal-500_6d3e9",
  "sm:focus:bg-teal-600": "_sm:focus:bg-teal-600_6d3e9",
  "sm:focus:bg-teal-700": "_sm:focus:bg-teal-700_6d3e9",
  "sm:focus:bg-teal-800": "_sm:focus:bg-teal-800_6d3e9",
  "sm:focus:bg-teal-900": "_sm:focus:bg-teal-900_6d3e9",
  "sm:focus:bg-blue-100": "_sm:focus:bg-blue-100_6d3e9",
  "sm:focus:bg-blue-200": "_sm:focus:bg-blue-200_6d3e9",
  "sm:focus:bg-blue-300": "_sm:focus:bg-blue-300_6d3e9",
  "sm:focus:bg-blue-400": "_sm:focus:bg-blue-400_6d3e9",
  "sm:focus:bg-blue-500": "_sm:focus:bg-blue-500_6d3e9",
  "sm:focus:bg-blue-600": "_sm:focus:bg-blue-600_6d3e9",
  "sm:focus:bg-blue-700": "_sm:focus:bg-blue-700_6d3e9",
  "sm:focus:bg-blue-800": "_sm:focus:bg-blue-800_6d3e9",
  "sm:focus:bg-blue-900": "_sm:focus:bg-blue-900_6d3e9",
  "sm:focus:bg-indigo-100": "_sm:focus:bg-indigo-100_6d3e9",
  "sm:focus:bg-indigo-200": "_sm:focus:bg-indigo-200_6d3e9",
  "sm:focus:bg-indigo-300": "_sm:focus:bg-indigo-300_6d3e9",
  "sm:focus:bg-indigo-400": "_sm:focus:bg-indigo-400_6d3e9",
  "sm:focus:bg-indigo-500": "_sm:focus:bg-indigo-500_6d3e9",
  "sm:focus:bg-indigo-600": "_sm:focus:bg-indigo-600_6d3e9",
  "sm:focus:bg-indigo-700": "_sm:focus:bg-indigo-700_6d3e9",
  "sm:focus:bg-indigo-800": "_sm:focus:bg-indigo-800_6d3e9",
  "sm:focus:bg-indigo-900": "_sm:focus:bg-indigo-900_6d3e9",
  "sm:focus:bg-purple-100": "_sm:focus:bg-purple-100_6d3e9",
  "sm:focus:bg-purple-200": "_sm:focus:bg-purple-200_6d3e9",
  "sm:focus:bg-purple-300": "_sm:focus:bg-purple-300_6d3e9",
  "sm:focus:bg-purple-400": "_sm:focus:bg-purple-400_6d3e9",
  "sm:focus:bg-purple-500": "_sm:focus:bg-purple-500_6d3e9",
  "sm:focus:bg-purple-600": "_sm:focus:bg-purple-600_6d3e9",
  "sm:focus:bg-purple-700": "_sm:focus:bg-purple-700_6d3e9",
  "sm:focus:bg-purple-800": "_sm:focus:bg-purple-800_6d3e9",
  "sm:focus:bg-purple-900": "_sm:focus:bg-purple-900_6d3e9",
  "sm:focus:bg-pink-100": "_sm:focus:bg-pink-100_6d3e9",
  "sm:focus:bg-pink-200": "_sm:focus:bg-pink-200_6d3e9",
  "sm:focus:bg-pink-300": "_sm:focus:bg-pink-300_6d3e9",
  "sm:focus:bg-pink-400": "_sm:focus:bg-pink-400_6d3e9",
  "sm:focus:bg-pink-500": "_sm:focus:bg-pink-500_6d3e9",
  "sm:focus:bg-pink-600": "_sm:focus:bg-pink-600_6d3e9",
  "sm:focus:bg-pink-700": "_sm:focus:bg-pink-700_6d3e9",
  "sm:focus:bg-pink-800": "_sm:focus:bg-pink-800_6d3e9",
  "sm:focus:bg-pink-900": "_sm:focus:bg-pink-900_6d3e9",
  "sm:focus:bg-primary-100": "_sm:focus:bg-primary-100_6d3e9",
  "sm:focus:bg-primary-200": "_sm:focus:bg-primary-200_6d3e9",
  "sm:focus:bg-primary-300": "_sm:focus:bg-primary-300_6d3e9",
  "sm:focus:bg-primary-400": "_sm:focus:bg-primary-400_6d3e9",
  "sm:focus:bg-primary-500": "_sm:focus:bg-primary-500_6d3e9",
  "sm:focus:bg-primary-600": "_sm:focus:bg-primary-600_6d3e9",
  "sm:focus:bg-primary-700": "_sm:focus:bg-primary-700_6d3e9",
  "sm:focus:bg-primary-800": "_sm:focus:bg-primary-800_6d3e9",
  "sm:focus:bg-primary-900": "_sm:focus:bg-primary-900_6d3e9",
  "sm:focus:bg-secondary-100": "_sm:focus:bg-secondary-100_6d3e9",
  "sm:focus:bg-secondary-200": "_sm:focus:bg-secondary-200_6d3e9",
  "sm:focus:bg-secondary-300": "_sm:focus:bg-secondary-300_6d3e9",
  "sm:focus:bg-secondary-400": "_sm:focus:bg-secondary-400_6d3e9",
  "sm:focus:bg-secondary-500": "_sm:focus:bg-secondary-500_6d3e9",
  "sm:focus:bg-secondary-600": "_sm:focus:bg-secondary-600_6d3e9",
  "sm:focus:bg-secondary-700": "_sm:focus:bg-secondary-700_6d3e9",
  "sm:focus:bg-secondary-800": "_sm:focus:bg-secondary-800_6d3e9",
  "sm:focus:bg-secondary-900": "_sm:focus:bg-secondary-900_6d3e9",
  "sm:active:bg-transparent": "_sm:active:bg-transparent_6d3e9",
  "sm:active:bg-black": "_sm:active:bg-black_6d3e9",
  "sm:active:bg-white": "_sm:active:bg-white_6d3e9",
  "sm:active:bg-gray-100": "_sm:active:bg-gray-100_6d3e9",
  "sm:active:bg-gray-200": "_sm:active:bg-gray-200_6d3e9",
  "sm:active:bg-gray-300": "_sm:active:bg-gray-300_6d3e9",
  "sm:active:bg-gray-400": "_sm:active:bg-gray-400_6d3e9",
  "sm:active:bg-gray-500": "_sm:active:bg-gray-500_6d3e9",
  "sm:active:bg-gray-600": "_sm:active:bg-gray-600_6d3e9",
  "sm:active:bg-gray-700": "_sm:active:bg-gray-700_6d3e9",
  "sm:active:bg-gray-800": "_sm:active:bg-gray-800_6d3e9",
  "sm:active:bg-gray-900": "_sm:active:bg-gray-900_6d3e9",
  "sm:active:bg-red-100": "_sm:active:bg-red-100_6d3e9",
  "sm:active:bg-red-200": "_sm:active:bg-red-200_6d3e9",
  "sm:active:bg-red-300": "_sm:active:bg-red-300_6d3e9",
  "sm:active:bg-red-400": "_sm:active:bg-red-400_6d3e9",
  "sm:active:bg-red-500": "_sm:active:bg-red-500_6d3e9",
  "sm:active:bg-red-600": "_sm:active:bg-red-600_6d3e9",
  "sm:active:bg-red-700": "_sm:active:bg-red-700_6d3e9",
  "sm:active:bg-red-800": "_sm:active:bg-red-800_6d3e9",
  "sm:active:bg-red-900": "_sm:active:bg-red-900_6d3e9",
  "sm:active:bg-orange-100": "_sm:active:bg-orange-100_6d3e9",
  "sm:active:bg-orange-200": "_sm:active:bg-orange-200_6d3e9",
  "sm:active:bg-orange-300": "_sm:active:bg-orange-300_6d3e9",
  "sm:active:bg-orange-400": "_sm:active:bg-orange-400_6d3e9",
  "sm:active:bg-orange-500": "_sm:active:bg-orange-500_6d3e9",
  "sm:active:bg-orange-600": "_sm:active:bg-orange-600_6d3e9",
  "sm:active:bg-orange-700": "_sm:active:bg-orange-700_6d3e9",
  "sm:active:bg-orange-800": "_sm:active:bg-orange-800_6d3e9",
  "sm:active:bg-orange-900": "_sm:active:bg-orange-900_6d3e9",
  "sm:active:bg-yellow-100": "_sm:active:bg-yellow-100_6d3e9",
  "sm:active:bg-yellow-200": "_sm:active:bg-yellow-200_6d3e9",
  "sm:active:bg-yellow-300": "_sm:active:bg-yellow-300_6d3e9",
  "sm:active:bg-yellow-400": "_sm:active:bg-yellow-400_6d3e9",
  "sm:active:bg-yellow-500": "_sm:active:bg-yellow-500_6d3e9",
  "sm:active:bg-yellow-600": "_sm:active:bg-yellow-600_6d3e9",
  "sm:active:bg-yellow-700": "_sm:active:bg-yellow-700_6d3e9",
  "sm:active:bg-yellow-800": "_sm:active:bg-yellow-800_6d3e9",
  "sm:active:bg-yellow-900": "_sm:active:bg-yellow-900_6d3e9",
  "sm:active:bg-green-100": "_sm:active:bg-green-100_6d3e9",
  "sm:active:bg-green-200": "_sm:active:bg-green-200_6d3e9",
  "sm:active:bg-green-300": "_sm:active:bg-green-300_6d3e9",
  "sm:active:bg-green-400": "_sm:active:bg-green-400_6d3e9",
  "sm:active:bg-green-500": "_sm:active:bg-green-500_6d3e9",
  "sm:active:bg-green-600": "_sm:active:bg-green-600_6d3e9",
  "sm:active:bg-green-700": "_sm:active:bg-green-700_6d3e9",
  "sm:active:bg-green-800": "_sm:active:bg-green-800_6d3e9",
  "sm:active:bg-green-900": "_sm:active:bg-green-900_6d3e9",
  "sm:active:bg-teal-100": "_sm:active:bg-teal-100_6d3e9",
  "sm:active:bg-teal-200": "_sm:active:bg-teal-200_6d3e9",
  "sm:active:bg-teal-300": "_sm:active:bg-teal-300_6d3e9",
  "sm:active:bg-teal-400": "_sm:active:bg-teal-400_6d3e9",
  "sm:active:bg-teal-500": "_sm:active:bg-teal-500_6d3e9",
  "sm:active:bg-teal-600": "_sm:active:bg-teal-600_6d3e9",
  "sm:active:bg-teal-700": "_sm:active:bg-teal-700_6d3e9",
  "sm:active:bg-teal-800": "_sm:active:bg-teal-800_6d3e9",
  "sm:active:bg-teal-900": "_sm:active:bg-teal-900_6d3e9",
  "sm:active:bg-blue-100": "_sm:active:bg-blue-100_6d3e9",
  "sm:active:bg-blue-200": "_sm:active:bg-blue-200_6d3e9",
  "sm:active:bg-blue-300": "_sm:active:bg-blue-300_6d3e9",
  "sm:active:bg-blue-400": "_sm:active:bg-blue-400_6d3e9",
  "sm:active:bg-blue-500": "_sm:active:bg-blue-500_6d3e9",
  "sm:active:bg-blue-600": "_sm:active:bg-blue-600_6d3e9",
  "sm:active:bg-blue-700": "_sm:active:bg-blue-700_6d3e9",
  "sm:active:bg-blue-800": "_sm:active:bg-blue-800_6d3e9",
  "sm:active:bg-blue-900": "_sm:active:bg-blue-900_6d3e9",
  "sm:active:bg-indigo-100": "_sm:active:bg-indigo-100_6d3e9",
  "sm:active:bg-indigo-200": "_sm:active:bg-indigo-200_6d3e9",
  "sm:active:bg-indigo-300": "_sm:active:bg-indigo-300_6d3e9",
  "sm:active:bg-indigo-400": "_sm:active:bg-indigo-400_6d3e9",
  "sm:active:bg-indigo-500": "_sm:active:bg-indigo-500_6d3e9",
  "sm:active:bg-indigo-600": "_sm:active:bg-indigo-600_6d3e9",
  "sm:active:bg-indigo-700": "_sm:active:bg-indigo-700_6d3e9",
  "sm:active:bg-indigo-800": "_sm:active:bg-indigo-800_6d3e9",
  "sm:active:bg-indigo-900": "_sm:active:bg-indigo-900_6d3e9",
  "sm:active:bg-purple-100": "_sm:active:bg-purple-100_6d3e9",
  "sm:active:bg-purple-200": "_sm:active:bg-purple-200_6d3e9",
  "sm:active:bg-purple-300": "_sm:active:bg-purple-300_6d3e9",
  "sm:active:bg-purple-400": "_sm:active:bg-purple-400_6d3e9",
  "sm:active:bg-purple-500": "_sm:active:bg-purple-500_6d3e9",
  "sm:active:bg-purple-600": "_sm:active:bg-purple-600_6d3e9",
  "sm:active:bg-purple-700": "_sm:active:bg-purple-700_6d3e9",
  "sm:active:bg-purple-800": "_sm:active:bg-purple-800_6d3e9",
  "sm:active:bg-purple-900": "_sm:active:bg-purple-900_6d3e9",
  "sm:active:bg-pink-100": "_sm:active:bg-pink-100_6d3e9",
  "sm:active:bg-pink-200": "_sm:active:bg-pink-200_6d3e9",
  "sm:active:bg-pink-300": "_sm:active:bg-pink-300_6d3e9",
  "sm:active:bg-pink-400": "_sm:active:bg-pink-400_6d3e9",
  "sm:active:bg-pink-500": "_sm:active:bg-pink-500_6d3e9",
  "sm:active:bg-pink-600": "_sm:active:bg-pink-600_6d3e9",
  "sm:active:bg-pink-700": "_sm:active:bg-pink-700_6d3e9",
  "sm:active:bg-pink-800": "_sm:active:bg-pink-800_6d3e9",
  "sm:active:bg-pink-900": "_sm:active:bg-pink-900_6d3e9",
  "sm:active:bg-primary-100": "_sm:active:bg-primary-100_6d3e9",
  "sm:active:bg-primary-200": "_sm:active:bg-primary-200_6d3e9",
  "sm:active:bg-primary-300": "_sm:active:bg-primary-300_6d3e9",
  "sm:active:bg-primary-400": "_sm:active:bg-primary-400_6d3e9",
  "sm:active:bg-primary-500": "_sm:active:bg-primary-500_6d3e9",
  "sm:active:bg-primary-600": "_sm:active:bg-primary-600_6d3e9",
  "sm:active:bg-primary-700": "_sm:active:bg-primary-700_6d3e9",
  "sm:active:bg-primary-800": "_sm:active:bg-primary-800_6d3e9",
  "sm:active:bg-primary-900": "_sm:active:bg-primary-900_6d3e9",
  "sm:active:bg-secondary-100": "_sm:active:bg-secondary-100_6d3e9",
  "sm:active:bg-secondary-200": "_sm:active:bg-secondary-200_6d3e9",
  "sm:active:bg-secondary-300": "_sm:active:bg-secondary-300_6d3e9",
  "sm:active:bg-secondary-400": "_sm:active:bg-secondary-400_6d3e9",
  "sm:active:bg-secondary-500": "_sm:active:bg-secondary-500_6d3e9",
  "sm:active:bg-secondary-600": "_sm:active:bg-secondary-600_6d3e9",
  "sm:active:bg-secondary-700": "_sm:active:bg-secondary-700_6d3e9",
  "sm:active:bg-secondary-800": "_sm:active:bg-secondary-800_6d3e9",
  "sm:active:bg-secondary-900": "_sm:active:bg-secondary-900_6d3e9",
  "sm:bg-bottom": "_sm:bg-bottom_6d3e9",
  "sm:bg-center": "_sm:bg-center_6d3e9",
  "sm:bg-left": "_sm:bg-left_6d3e9",
  "sm:bg-left-bottom": "_sm:bg-left-bottom_6d3e9",
  "sm:bg-left-top": "_sm:bg-left-top_6d3e9",
  "sm:bg-right": "_sm:bg-right_6d3e9",
  "sm:bg-right-bottom": "_sm:bg-right-bottom_6d3e9",
  "sm:bg-right-top": "_sm:bg-right-top_6d3e9",
  "sm:bg-top": "_sm:bg-top_6d3e9",
  "sm:bg-repeat": "_sm:bg-repeat_6d3e9",
  "sm:bg-no-repeat": "_sm:bg-no-repeat_6d3e9",
  "sm:bg-repeat-x": "_sm:bg-repeat-x_6d3e9",
  "sm:bg-repeat-y": "_sm:bg-repeat-y_6d3e9",
  "sm:bg-repeat-round": "_sm:bg-repeat-round_6d3e9",
  "sm:bg-repeat-space": "_sm:bg-repeat-space_6d3e9",
  "sm:bg-auto": "_sm:bg-auto_6d3e9",
  "sm:bg-cover": "_sm:bg-cover_6d3e9",
  "sm:bg-contain": "_sm:bg-contain_6d3e9",
  "sm:border-collapse": "_sm:border-collapse_6d3e9",
  "sm:border-separate": "_sm:border-separate_6d3e9",
  "sm:border-transparent": "_sm:border-transparent_6d3e9",
  "sm:border-black": "_sm:border-black_6d3e9",
  "sm:border-white": "_sm:border-white_6d3e9",
  "sm:border-gray-100": "_sm:border-gray-100_6d3e9",
  "sm:border-gray-200": "_sm:border-gray-200_6d3e9",
  "sm:border-gray-300": "_sm:border-gray-300_6d3e9",
  "sm:border-gray-400": "_sm:border-gray-400_6d3e9",
  "sm:border-gray-500": "_sm:border-gray-500_6d3e9",
  "sm:border-gray-600": "_sm:border-gray-600_6d3e9",
  "sm:border-gray-700": "_sm:border-gray-700_6d3e9",
  "sm:border-gray-800": "_sm:border-gray-800_6d3e9",
  "sm:border-gray-900": "_sm:border-gray-900_6d3e9",
  "sm:border-red-100": "_sm:border-red-100_6d3e9",
  "sm:border-red-200": "_sm:border-red-200_6d3e9",
  "sm:border-red-300": "_sm:border-red-300_6d3e9",
  "sm:border-red-400": "_sm:border-red-400_6d3e9",
  "sm:border-red-500": "_sm:border-red-500_6d3e9",
  "sm:border-red-600": "_sm:border-red-600_6d3e9",
  "sm:border-red-700": "_sm:border-red-700_6d3e9",
  "sm:border-red-800": "_sm:border-red-800_6d3e9",
  "sm:border-red-900": "_sm:border-red-900_6d3e9",
  "sm:border-orange-100": "_sm:border-orange-100_6d3e9",
  "sm:border-orange-200": "_sm:border-orange-200_6d3e9",
  "sm:border-orange-300": "_sm:border-orange-300_6d3e9",
  "sm:border-orange-400": "_sm:border-orange-400_6d3e9",
  "sm:border-orange-500": "_sm:border-orange-500_6d3e9",
  "sm:border-orange-600": "_sm:border-orange-600_6d3e9",
  "sm:border-orange-700": "_sm:border-orange-700_6d3e9",
  "sm:border-orange-800": "_sm:border-orange-800_6d3e9",
  "sm:border-orange-900": "_sm:border-orange-900_6d3e9",
  "sm:border-yellow-100": "_sm:border-yellow-100_6d3e9",
  "sm:border-yellow-200": "_sm:border-yellow-200_6d3e9",
  "sm:border-yellow-300": "_sm:border-yellow-300_6d3e9",
  "sm:border-yellow-400": "_sm:border-yellow-400_6d3e9",
  "sm:border-yellow-500": "_sm:border-yellow-500_6d3e9",
  "sm:border-yellow-600": "_sm:border-yellow-600_6d3e9",
  "sm:border-yellow-700": "_sm:border-yellow-700_6d3e9",
  "sm:border-yellow-800": "_sm:border-yellow-800_6d3e9",
  "sm:border-yellow-900": "_sm:border-yellow-900_6d3e9",
  "sm:border-green-100": "_sm:border-green-100_6d3e9",
  "sm:border-green-200": "_sm:border-green-200_6d3e9",
  "sm:border-green-300": "_sm:border-green-300_6d3e9",
  "sm:border-green-400": "_sm:border-green-400_6d3e9",
  "sm:border-green-500": "_sm:border-green-500_6d3e9",
  "sm:border-green-600": "_sm:border-green-600_6d3e9",
  "sm:border-green-700": "_sm:border-green-700_6d3e9",
  "sm:border-green-800": "_sm:border-green-800_6d3e9",
  "sm:border-green-900": "_sm:border-green-900_6d3e9",
  "sm:border-teal-100": "_sm:border-teal-100_6d3e9",
  "sm:border-teal-200": "_sm:border-teal-200_6d3e9",
  "sm:border-teal-300": "_sm:border-teal-300_6d3e9",
  "sm:border-teal-400": "_sm:border-teal-400_6d3e9",
  "sm:border-teal-500": "_sm:border-teal-500_6d3e9",
  "sm:border-teal-600": "_sm:border-teal-600_6d3e9",
  "sm:border-teal-700": "_sm:border-teal-700_6d3e9",
  "sm:border-teal-800": "_sm:border-teal-800_6d3e9",
  "sm:border-teal-900": "_sm:border-teal-900_6d3e9",
  "sm:border-blue-100": "_sm:border-blue-100_6d3e9",
  "sm:border-blue-200": "_sm:border-blue-200_6d3e9",
  "sm:border-blue-300": "_sm:border-blue-300_6d3e9",
  "sm:border-blue-400": "_sm:border-blue-400_6d3e9",
  "sm:border-blue-500": "_sm:border-blue-500_6d3e9",
  "sm:border-blue-600": "_sm:border-blue-600_6d3e9",
  "sm:border-blue-700": "_sm:border-blue-700_6d3e9",
  "sm:border-blue-800": "_sm:border-blue-800_6d3e9",
  "sm:border-blue-900": "_sm:border-blue-900_6d3e9",
  "sm:border-indigo-100": "_sm:border-indigo-100_6d3e9",
  "sm:border-indigo-200": "_sm:border-indigo-200_6d3e9",
  "sm:border-indigo-300": "_sm:border-indigo-300_6d3e9",
  "sm:border-indigo-400": "_sm:border-indigo-400_6d3e9",
  "sm:border-indigo-500": "_sm:border-indigo-500_6d3e9",
  "sm:border-indigo-600": "_sm:border-indigo-600_6d3e9",
  "sm:border-indigo-700": "_sm:border-indigo-700_6d3e9",
  "sm:border-indigo-800": "_sm:border-indigo-800_6d3e9",
  "sm:border-indigo-900": "_sm:border-indigo-900_6d3e9",
  "sm:border-purple-100": "_sm:border-purple-100_6d3e9",
  "sm:border-purple-200": "_sm:border-purple-200_6d3e9",
  "sm:border-purple-300": "_sm:border-purple-300_6d3e9",
  "sm:border-purple-400": "_sm:border-purple-400_6d3e9",
  "sm:border-purple-500": "_sm:border-purple-500_6d3e9",
  "sm:border-purple-600": "_sm:border-purple-600_6d3e9",
  "sm:border-purple-700": "_sm:border-purple-700_6d3e9",
  "sm:border-purple-800": "_sm:border-purple-800_6d3e9",
  "sm:border-purple-900": "_sm:border-purple-900_6d3e9",
  "sm:border-pink-100": "_sm:border-pink-100_6d3e9",
  "sm:border-pink-200": "_sm:border-pink-200_6d3e9",
  "sm:border-pink-300": "_sm:border-pink-300_6d3e9",
  "sm:border-pink-400": "_sm:border-pink-400_6d3e9",
  "sm:border-pink-500": "_sm:border-pink-500_6d3e9",
  "sm:border-pink-600": "_sm:border-pink-600_6d3e9",
  "sm:border-pink-700": "_sm:border-pink-700_6d3e9",
  "sm:border-pink-800": "_sm:border-pink-800_6d3e9",
  "sm:border-pink-900": "_sm:border-pink-900_6d3e9",
  "sm:border-primary-100": "_sm:border-primary-100_6d3e9",
  "sm:border-primary-200": "_sm:border-primary-200_6d3e9",
  "sm:border-primary-300": "_sm:border-primary-300_6d3e9",
  "sm:border-primary-400": "_sm:border-primary-400_6d3e9",
  "sm:border-primary-500": "_sm:border-primary-500_6d3e9",
  "sm:border-primary-600": "_sm:border-primary-600_6d3e9",
  "sm:border-primary-700": "_sm:border-primary-700_6d3e9",
  "sm:border-primary-800": "_sm:border-primary-800_6d3e9",
  "sm:border-primary-900": "_sm:border-primary-900_6d3e9",
  "sm:border-secondary-100": "_sm:border-secondary-100_6d3e9",
  "sm:border-secondary-200": "_sm:border-secondary-200_6d3e9",
  "sm:border-secondary-300": "_sm:border-secondary-300_6d3e9",
  "sm:border-secondary-400": "_sm:border-secondary-400_6d3e9",
  "sm:border-secondary-500": "_sm:border-secondary-500_6d3e9",
  "sm:border-secondary-600": "_sm:border-secondary-600_6d3e9",
  "sm:border-secondary-700": "_sm:border-secondary-700_6d3e9",
  "sm:border-secondary-800": "_sm:border-secondary-800_6d3e9",
  "sm:border-secondary-900": "_sm:border-secondary-900_6d3e9",
  "sm:hover:border-transparent": "_sm:hover:border-transparent_6d3e9",
  "sm:hover:border-black": "_sm:hover:border-black_6d3e9",
  "sm:hover:border-white": "_sm:hover:border-white_6d3e9",
  "sm:hover:border-gray-100": "_sm:hover:border-gray-100_6d3e9",
  "sm:hover:border-gray-200": "_sm:hover:border-gray-200_6d3e9",
  "sm:hover:border-gray-300": "_sm:hover:border-gray-300_6d3e9",
  "sm:hover:border-gray-400": "_sm:hover:border-gray-400_6d3e9",
  "sm:hover:border-gray-500": "_sm:hover:border-gray-500_6d3e9",
  "sm:hover:border-gray-600": "_sm:hover:border-gray-600_6d3e9",
  "sm:hover:border-gray-700": "_sm:hover:border-gray-700_6d3e9",
  "sm:hover:border-gray-800": "_sm:hover:border-gray-800_6d3e9",
  "sm:hover:border-gray-900": "_sm:hover:border-gray-900_6d3e9",
  "sm:hover:border-red-100": "_sm:hover:border-red-100_6d3e9",
  "sm:hover:border-red-200": "_sm:hover:border-red-200_6d3e9",
  "sm:hover:border-red-300": "_sm:hover:border-red-300_6d3e9",
  "sm:hover:border-red-400": "_sm:hover:border-red-400_6d3e9",
  "sm:hover:border-red-500": "_sm:hover:border-red-500_6d3e9",
  "sm:hover:border-red-600": "_sm:hover:border-red-600_6d3e9",
  "sm:hover:border-red-700": "_sm:hover:border-red-700_6d3e9",
  "sm:hover:border-red-800": "_sm:hover:border-red-800_6d3e9",
  "sm:hover:border-red-900": "_sm:hover:border-red-900_6d3e9",
  "sm:hover:border-orange-100": "_sm:hover:border-orange-100_6d3e9",
  "sm:hover:border-orange-200": "_sm:hover:border-orange-200_6d3e9",
  "sm:hover:border-orange-300": "_sm:hover:border-orange-300_6d3e9",
  "sm:hover:border-orange-400": "_sm:hover:border-orange-400_6d3e9",
  "sm:hover:border-orange-500": "_sm:hover:border-orange-500_6d3e9",
  "sm:hover:border-orange-600": "_sm:hover:border-orange-600_6d3e9",
  "sm:hover:border-orange-700": "_sm:hover:border-orange-700_6d3e9",
  "sm:hover:border-orange-800": "_sm:hover:border-orange-800_6d3e9",
  "sm:hover:border-orange-900": "_sm:hover:border-orange-900_6d3e9",
  "sm:hover:border-yellow-100": "_sm:hover:border-yellow-100_6d3e9",
  "sm:hover:border-yellow-200": "_sm:hover:border-yellow-200_6d3e9",
  "sm:hover:border-yellow-300": "_sm:hover:border-yellow-300_6d3e9",
  "sm:hover:border-yellow-400": "_sm:hover:border-yellow-400_6d3e9",
  "sm:hover:border-yellow-500": "_sm:hover:border-yellow-500_6d3e9",
  "sm:hover:border-yellow-600": "_sm:hover:border-yellow-600_6d3e9",
  "sm:hover:border-yellow-700": "_sm:hover:border-yellow-700_6d3e9",
  "sm:hover:border-yellow-800": "_sm:hover:border-yellow-800_6d3e9",
  "sm:hover:border-yellow-900": "_sm:hover:border-yellow-900_6d3e9",
  "sm:hover:border-green-100": "_sm:hover:border-green-100_6d3e9",
  "sm:hover:border-green-200": "_sm:hover:border-green-200_6d3e9",
  "sm:hover:border-green-300": "_sm:hover:border-green-300_6d3e9",
  "sm:hover:border-green-400": "_sm:hover:border-green-400_6d3e9",
  "sm:hover:border-green-500": "_sm:hover:border-green-500_6d3e9",
  "sm:hover:border-green-600": "_sm:hover:border-green-600_6d3e9",
  "sm:hover:border-green-700": "_sm:hover:border-green-700_6d3e9",
  "sm:hover:border-green-800": "_sm:hover:border-green-800_6d3e9",
  "sm:hover:border-green-900": "_sm:hover:border-green-900_6d3e9",
  "sm:hover:border-teal-100": "_sm:hover:border-teal-100_6d3e9",
  "sm:hover:border-teal-200": "_sm:hover:border-teal-200_6d3e9",
  "sm:hover:border-teal-300": "_sm:hover:border-teal-300_6d3e9",
  "sm:hover:border-teal-400": "_sm:hover:border-teal-400_6d3e9",
  "sm:hover:border-teal-500": "_sm:hover:border-teal-500_6d3e9",
  "sm:hover:border-teal-600": "_sm:hover:border-teal-600_6d3e9",
  "sm:hover:border-teal-700": "_sm:hover:border-teal-700_6d3e9",
  "sm:hover:border-teal-800": "_sm:hover:border-teal-800_6d3e9",
  "sm:hover:border-teal-900": "_sm:hover:border-teal-900_6d3e9",
  "sm:hover:border-blue-100": "_sm:hover:border-blue-100_6d3e9",
  "sm:hover:border-blue-200": "_sm:hover:border-blue-200_6d3e9",
  "sm:hover:border-blue-300": "_sm:hover:border-blue-300_6d3e9",
  "sm:hover:border-blue-400": "_sm:hover:border-blue-400_6d3e9",
  "sm:hover:border-blue-500": "_sm:hover:border-blue-500_6d3e9",
  "sm:hover:border-blue-600": "_sm:hover:border-blue-600_6d3e9",
  "sm:hover:border-blue-700": "_sm:hover:border-blue-700_6d3e9",
  "sm:hover:border-blue-800": "_sm:hover:border-blue-800_6d3e9",
  "sm:hover:border-blue-900": "_sm:hover:border-blue-900_6d3e9",
  "sm:hover:border-indigo-100": "_sm:hover:border-indigo-100_6d3e9",
  "sm:hover:border-indigo-200": "_sm:hover:border-indigo-200_6d3e9",
  "sm:hover:border-indigo-300": "_sm:hover:border-indigo-300_6d3e9",
  "sm:hover:border-indigo-400": "_sm:hover:border-indigo-400_6d3e9",
  "sm:hover:border-indigo-500": "_sm:hover:border-indigo-500_6d3e9",
  "sm:hover:border-indigo-600": "_sm:hover:border-indigo-600_6d3e9",
  "sm:hover:border-indigo-700": "_sm:hover:border-indigo-700_6d3e9",
  "sm:hover:border-indigo-800": "_sm:hover:border-indigo-800_6d3e9",
  "sm:hover:border-indigo-900": "_sm:hover:border-indigo-900_6d3e9",
  "sm:hover:border-purple-100": "_sm:hover:border-purple-100_6d3e9",
  "sm:hover:border-purple-200": "_sm:hover:border-purple-200_6d3e9",
  "sm:hover:border-purple-300": "_sm:hover:border-purple-300_6d3e9",
  "sm:hover:border-purple-400": "_sm:hover:border-purple-400_6d3e9",
  "sm:hover:border-purple-500": "_sm:hover:border-purple-500_6d3e9",
  "sm:hover:border-purple-600": "_sm:hover:border-purple-600_6d3e9",
  "sm:hover:border-purple-700": "_sm:hover:border-purple-700_6d3e9",
  "sm:hover:border-purple-800": "_sm:hover:border-purple-800_6d3e9",
  "sm:hover:border-purple-900": "_sm:hover:border-purple-900_6d3e9",
  "sm:hover:border-pink-100": "_sm:hover:border-pink-100_6d3e9",
  "sm:hover:border-pink-200": "_sm:hover:border-pink-200_6d3e9",
  "sm:hover:border-pink-300": "_sm:hover:border-pink-300_6d3e9",
  "sm:hover:border-pink-400": "_sm:hover:border-pink-400_6d3e9",
  "sm:hover:border-pink-500": "_sm:hover:border-pink-500_6d3e9",
  "sm:hover:border-pink-600": "_sm:hover:border-pink-600_6d3e9",
  "sm:hover:border-pink-700": "_sm:hover:border-pink-700_6d3e9",
  "sm:hover:border-pink-800": "_sm:hover:border-pink-800_6d3e9",
  "sm:hover:border-pink-900": "_sm:hover:border-pink-900_6d3e9",
  "sm:hover:border-primary-100": "_sm:hover:border-primary-100_6d3e9",
  "sm:hover:border-primary-200": "_sm:hover:border-primary-200_6d3e9",
  "sm:hover:border-primary-300": "_sm:hover:border-primary-300_6d3e9",
  "sm:hover:border-primary-400": "_sm:hover:border-primary-400_6d3e9",
  "sm:hover:border-primary-500": "_sm:hover:border-primary-500_6d3e9",
  "sm:hover:border-primary-600": "_sm:hover:border-primary-600_6d3e9",
  "sm:hover:border-primary-700": "_sm:hover:border-primary-700_6d3e9",
  "sm:hover:border-primary-800": "_sm:hover:border-primary-800_6d3e9",
  "sm:hover:border-primary-900": "_sm:hover:border-primary-900_6d3e9",
  "sm:hover:border-secondary-100": "_sm:hover:border-secondary-100_6d3e9",
  "sm:hover:border-secondary-200": "_sm:hover:border-secondary-200_6d3e9",
  "sm:hover:border-secondary-300": "_sm:hover:border-secondary-300_6d3e9",
  "sm:hover:border-secondary-400": "_sm:hover:border-secondary-400_6d3e9",
  "sm:hover:border-secondary-500": "_sm:hover:border-secondary-500_6d3e9",
  "sm:hover:border-secondary-600": "_sm:hover:border-secondary-600_6d3e9",
  "sm:hover:border-secondary-700": "_sm:hover:border-secondary-700_6d3e9",
  "sm:hover:border-secondary-800": "_sm:hover:border-secondary-800_6d3e9",
  "sm:hover:border-secondary-900": "_sm:hover:border-secondary-900_6d3e9",
  "sm:focus:border-transparent": "_sm:focus:border-transparent_6d3e9",
  "sm:focus:border-black": "_sm:focus:border-black_6d3e9",
  "sm:focus:border-white": "_sm:focus:border-white_6d3e9",
  "sm:focus:border-gray-100": "_sm:focus:border-gray-100_6d3e9",
  "sm:focus:border-gray-200": "_sm:focus:border-gray-200_6d3e9",
  "sm:focus:border-gray-300": "_sm:focus:border-gray-300_6d3e9",
  "sm:focus:border-gray-400": "_sm:focus:border-gray-400_6d3e9",
  "sm:focus:border-gray-500": "_sm:focus:border-gray-500_6d3e9",
  "sm:focus:border-gray-600": "_sm:focus:border-gray-600_6d3e9",
  "sm:focus:border-gray-700": "_sm:focus:border-gray-700_6d3e9",
  "sm:focus:border-gray-800": "_sm:focus:border-gray-800_6d3e9",
  "sm:focus:border-gray-900": "_sm:focus:border-gray-900_6d3e9",
  "sm:focus:border-red-100": "_sm:focus:border-red-100_6d3e9",
  "sm:focus:border-red-200": "_sm:focus:border-red-200_6d3e9",
  "sm:focus:border-red-300": "_sm:focus:border-red-300_6d3e9",
  "sm:focus:border-red-400": "_sm:focus:border-red-400_6d3e9",
  "sm:focus:border-red-500": "_sm:focus:border-red-500_6d3e9",
  "sm:focus:border-red-600": "_sm:focus:border-red-600_6d3e9",
  "sm:focus:border-red-700": "_sm:focus:border-red-700_6d3e9",
  "sm:focus:border-red-800": "_sm:focus:border-red-800_6d3e9",
  "sm:focus:border-red-900": "_sm:focus:border-red-900_6d3e9",
  "sm:focus:border-orange-100": "_sm:focus:border-orange-100_6d3e9",
  "sm:focus:border-orange-200": "_sm:focus:border-orange-200_6d3e9",
  "sm:focus:border-orange-300": "_sm:focus:border-orange-300_6d3e9",
  "sm:focus:border-orange-400": "_sm:focus:border-orange-400_6d3e9",
  "sm:focus:border-orange-500": "_sm:focus:border-orange-500_6d3e9",
  "sm:focus:border-orange-600": "_sm:focus:border-orange-600_6d3e9",
  "sm:focus:border-orange-700": "_sm:focus:border-orange-700_6d3e9",
  "sm:focus:border-orange-800": "_sm:focus:border-orange-800_6d3e9",
  "sm:focus:border-orange-900": "_sm:focus:border-orange-900_6d3e9",
  "sm:focus:border-yellow-100": "_sm:focus:border-yellow-100_6d3e9",
  "sm:focus:border-yellow-200": "_sm:focus:border-yellow-200_6d3e9",
  "sm:focus:border-yellow-300": "_sm:focus:border-yellow-300_6d3e9",
  "sm:focus:border-yellow-400": "_sm:focus:border-yellow-400_6d3e9",
  "sm:focus:border-yellow-500": "_sm:focus:border-yellow-500_6d3e9",
  "sm:focus:border-yellow-600": "_sm:focus:border-yellow-600_6d3e9",
  "sm:focus:border-yellow-700": "_sm:focus:border-yellow-700_6d3e9",
  "sm:focus:border-yellow-800": "_sm:focus:border-yellow-800_6d3e9",
  "sm:focus:border-yellow-900": "_sm:focus:border-yellow-900_6d3e9",
  "sm:focus:border-green-100": "_sm:focus:border-green-100_6d3e9",
  "sm:focus:border-green-200": "_sm:focus:border-green-200_6d3e9",
  "sm:focus:border-green-300": "_sm:focus:border-green-300_6d3e9",
  "sm:focus:border-green-400": "_sm:focus:border-green-400_6d3e9",
  "sm:focus:border-green-500": "_sm:focus:border-green-500_6d3e9",
  "sm:focus:border-green-600": "_sm:focus:border-green-600_6d3e9",
  "sm:focus:border-green-700": "_sm:focus:border-green-700_6d3e9",
  "sm:focus:border-green-800": "_sm:focus:border-green-800_6d3e9",
  "sm:focus:border-green-900": "_sm:focus:border-green-900_6d3e9",
  "sm:focus:border-teal-100": "_sm:focus:border-teal-100_6d3e9",
  "sm:focus:border-teal-200": "_sm:focus:border-teal-200_6d3e9",
  "sm:focus:border-teal-300": "_sm:focus:border-teal-300_6d3e9",
  "sm:focus:border-teal-400": "_sm:focus:border-teal-400_6d3e9",
  "sm:focus:border-teal-500": "_sm:focus:border-teal-500_6d3e9",
  "sm:focus:border-teal-600": "_sm:focus:border-teal-600_6d3e9",
  "sm:focus:border-teal-700": "_sm:focus:border-teal-700_6d3e9",
  "sm:focus:border-teal-800": "_sm:focus:border-teal-800_6d3e9",
  "sm:focus:border-teal-900": "_sm:focus:border-teal-900_6d3e9",
  "sm:focus:border-blue-100": "_sm:focus:border-blue-100_6d3e9",
  "sm:focus:border-blue-200": "_sm:focus:border-blue-200_6d3e9",
  "sm:focus:border-blue-300": "_sm:focus:border-blue-300_6d3e9",
  "sm:focus:border-blue-400": "_sm:focus:border-blue-400_6d3e9",
  "sm:focus:border-blue-500": "_sm:focus:border-blue-500_6d3e9",
  "sm:focus:border-blue-600": "_sm:focus:border-blue-600_6d3e9",
  "sm:focus:border-blue-700": "_sm:focus:border-blue-700_6d3e9",
  "sm:focus:border-blue-800": "_sm:focus:border-blue-800_6d3e9",
  "sm:focus:border-blue-900": "_sm:focus:border-blue-900_6d3e9",
  "sm:focus:border-indigo-100": "_sm:focus:border-indigo-100_6d3e9",
  "sm:focus:border-indigo-200": "_sm:focus:border-indigo-200_6d3e9",
  "sm:focus:border-indigo-300": "_sm:focus:border-indigo-300_6d3e9",
  "sm:focus:border-indigo-400": "_sm:focus:border-indigo-400_6d3e9",
  "sm:focus:border-indigo-500": "_sm:focus:border-indigo-500_6d3e9",
  "sm:focus:border-indigo-600": "_sm:focus:border-indigo-600_6d3e9",
  "sm:focus:border-indigo-700": "_sm:focus:border-indigo-700_6d3e9",
  "sm:focus:border-indigo-800": "_sm:focus:border-indigo-800_6d3e9",
  "sm:focus:border-indigo-900": "_sm:focus:border-indigo-900_6d3e9",
  "sm:focus:border-purple-100": "_sm:focus:border-purple-100_6d3e9",
  "sm:focus:border-purple-200": "_sm:focus:border-purple-200_6d3e9",
  "sm:focus:border-purple-300": "_sm:focus:border-purple-300_6d3e9",
  "sm:focus:border-purple-400": "_sm:focus:border-purple-400_6d3e9",
  "sm:focus:border-purple-500": "_sm:focus:border-purple-500_6d3e9",
  "sm:focus:border-purple-600": "_sm:focus:border-purple-600_6d3e9",
  "sm:focus:border-purple-700": "_sm:focus:border-purple-700_6d3e9",
  "sm:focus:border-purple-800": "_sm:focus:border-purple-800_6d3e9",
  "sm:focus:border-purple-900": "_sm:focus:border-purple-900_6d3e9",
  "sm:focus:border-pink-100": "_sm:focus:border-pink-100_6d3e9",
  "sm:focus:border-pink-200": "_sm:focus:border-pink-200_6d3e9",
  "sm:focus:border-pink-300": "_sm:focus:border-pink-300_6d3e9",
  "sm:focus:border-pink-400": "_sm:focus:border-pink-400_6d3e9",
  "sm:focus:border-pink-500": "_sm:focus:border-pink-500_6d3e9",
  "sm:focus:border-pink-600": "_sm:focus:border-pink-600_6d3e9",
  "sm:focus:border-pink-700": "_sm:focus:border-pink-700_6d3e9",
  "sm:focus:border-pink-800": "_sm:focus:border-pink-800_6d3e9",
  "sm:focus:border-pink-900": "_sm:focus:border-pink-900_6d3e9",
  "sm:focus:border-primary-100": "_sm:focus:border-primary-100_6d3e9",
  "sm:focus:border-primary-200": "_sm:focus:border-primary-200_6d3e9",
  "sm:focus:border-primary-300": "_sm:focus:border-primary-300_6d3e9",
  "sm:focus:border-primary-400": "_sm:focus:border-primary-400_6d3e9",
  "sm:focus:border-primary-500": "_sm:focus:border-primary-500_6d3e9",
  "sm:focus:border-primary-600": "_sm:focus:border-primary-600_6d3e9",
  "sm:focus:border-primary-700": "_sm:focus:border-primary-700_6d3e9",
  "sm:focus:border-primary-800": "_sm:focus:border-primary-800_6d3e9",
  "sm:focus:border-primary-900": "_sm:focus:border-primary-900_6d3e9",
  "sm:focus:border-secondary-100": "_sm:focus:border-secondary-100_6d3e9",
  "sm:focus:border-secondary-200": "_sm:focus:border-secondary-200_6d3e9",
  "sm:focus:border-secondary-300": "_sm:focus:border-secondary-300_6d3e9",
  "sm:focus:border-secondary-400": "_sm:focus:border-secondary-400_6d3e9",
  "sm:focus:border-secondary-500": "_sm:focus:border-secondary-500_6d3e9",
  "sm:focus:border-secondary-600": "_sm:focus:border-secondary-600_6d3e9",
  "sm:focus:border-secondary-700": "_sm:focus:border-secondary-700_6d3e9",
  "sm:focus:border-secondary-800": "_sm:focus:border-secondary-800_6d3e9",
  "sm:focus:border-secondary-900": "_sm:focus:border-secondary-900_6d3e9",
  "sm:rounded-none": "_sm:rounded-none_6d3e9",
  "sm:rounded-sm": "_sm:rounded-sm_6d3e9",
  "sm:rounded": "_sm:rounded_6d3e9",
  "sm:rounded-lg": "_sm:rounded-lg_6d3e9",
  "sm:rounded-full": "_sm:rounded-full_6d3e9",
  "sm:rounded-t-none": "_sm:rounded-t-none_6d3e9",
  "sm:rounded-r-none": "_sm:rounded-r-none_6d3e9",
  "sm:rounded-b-none": "_sm:rounded-b-none_6d3e9",
  "sm:rounded-l-none": "_sm:rounded-l-none_6d3e9",
  "sm:rounded-t-sm": "_sm:rounded-t-sm_6d3e9",
  "sm:rounded-r-sm": "_sm:rounded-r-sm_6d3e9",
  "sm:rounded-b-sm": "_sm:rounded-b-sm_6d3e9",
  "sm:rounded-l-sm": "_sm:rounded-l-sm_6d3e9",
  "sm:rounded-t": "_sm:rounded-t_6d3e9",
  "sm:rounded-r": "_sm:rounded-r_6d3e9",
  "sm:rounded-b": "_sm:rounded-b_6d3e9",
  "sm:rounded-l": "_sm:rounded-l_6d3e9",
  "sm:rounded-t-lg": "_sm:rounded-t-lg_6d3e9",
  "sm:rounded-r-lg": "_sm:rounded-r-lg_6d3e9",
  "sm:rounded-b-lg": "_sm:rounded-b-lg_6d3e9",
  "sm:rounded-l-lg": "_sm:rounded-l-lg_6d3e9",
  "sm:rounded-t-full": "_sm:rounded-t-full_6d3e9",
  "sm:rounded-r-full": "_sm:rounded-r-full_6d3e9",
  "sm:rounded-b-full": "_sm:rounded-b-full_6d3e9",
  "sm:rounded-l-full": "_sm:rounded-l-full_6d3e9",
  "sm:rounded-tl-none": "_sm:rounded-tl-none_6d3e9",
  "sm:rounded-tr-none": "_sm:rounded-tr-none_6d3e9",
  "sm:rounded-br-none": "_sm:rounded-br-none_6d3e9",
  "sm:rounded-bl-none": "_sm:rounded-bl-none_6d3e9",
  "sm:rounded-tl-sm": "_sm:rounded-tl-sm_6d3e9",
  "sm:rounded-tr-sm": "_sm:rounded-tr-sm_6d3e9",
  "sm:rounded-br-sm": "_sm:rounded-br-sm_6d3e9",
  "sm:rounded-bl-sm": "_sm:rounded-bl-sm_6d3e9",
  "sm:rounded-tl": "_sm:rounded-tl_6d3e9",
  "sm:rounded-tr": "_sm:rounded-tr_6d3e9",
  "sm:rounded-br": "_sm:rounded-br_6d3e9",
  "sm:rounded-bl": "_sm:rounded-bl_6d3e9",
  "sm:rounded-tl-lg": "_sm:rounded-tl-lg_6d3e9",
  "sm:rounded-tr-lg": "_sm:rounded-tr-lg_6d3e9",
  "sm:rounded-br-lg": "_sm:rounded-br-lg_6d3e9",
  "sm:rounded-bl-lg": "_sm:rounded-bl-lg_6d3e9",
  "sm:rounded-tl-full": "_sm:rounded-tl-full_6d3e9",
  "sm:rounded-tr-full": "_sm:rounded-tr-full_6d3e9",
  "sm:rounded-br-full": "_sm:rounded-br-full_6d3e9",
  "sm:rounded-bl-full": "_sm:rounded-bl-full_6d3e9",
  "sm:border-solid": "_sm:border-solid_6d3e9",
  "sm:border-dashed": "_sm:border-dashed_6d3e9",
  "sm:border-dotted": "_sm:border-dotted_6d3e9",
  "sm:border-double": "_sm:border-double_6d3e9",
  "sm:border-none": "_sm:border-none_6d3e9",
  "sm:border-0": "_sm:border-0_6d3e9",
  "sm:border-2": "_sm:border-2_6d3e9",
  "sm:border-4": "_sm:border-4_6d3e9",
  "sm:border-8": "_sm:border-8_6d3e9",
  "sm:border": "_sm:border_6d3e9",
  "sm:border-t-0": "_sm:border-t-0_6d3e9",
  "sm:border-r-0": "_sm:border-r-0_6d3e9",
  "sm:border-b-0": "_sm:border-b-0_6d3e9",
  "sm:border-l-0": "_sm:border-l-0_6d3e9",
  "sm:border-t-2": "_sm:border-t-2_6d3e9",
  "sm:border-r-2": "_sm:border-r-2_6d3e9",
  "sm:border-b-2": "_sm:border-b-2_6d3e9",
  "sm:border-l-2": "_sm:border-l-2_6d3e9",
  "sm:border-t-4": "_sm:border-t-4_6d3e9",
  "sm:border-r-4": "_sm:border-r-4_6d3e9",
  "sm:border-b-4": "_sm:border-b-4_6d3e9",
  "sm:border-l-4": "_sm:border-l-4_6d3e9",
  "sm:border-t-8": "_sm:border-t-8_6d3e9",
  "sm:border-r-8": "_sm:border-r-8_6d3e9",
  "sm:border-b-8": "_sm:border-b-8_6d3e9",
  "sm:border-l-8": "_sm:border-l-8_6d3e9",
  "sm:border-t": "_sm:border-t_6d3e9",
  "sm:border-r": "_sm:border-r_6d3e9",
  "sm:border-b": "_sm:border-b_6d3e9",
  "sm:border-l": "_sm:border-l_6d3e9",
  "sm:first:border-0": "_sm:first:border-0_6d3e9",
  "sm:first:border-2": "_sm:first:border-2_6d3e9",
  "sm:first:border-4": "_sm:first:border-4_6d3e9",
  "sm:first:border-8": "_sm:first:border-8_6d3e9",
  "sm:first:border": "_sm:first:border_6d3e9",
  "sm:first:border-t-0": "_sm:first:border-t-0_6d3e9",
  "sm:first:border-r-0": "_sm:first:border-r-0_6d3e9",
  "sm:first:border-b-0": "_sm:first:border-b-0_6d3e9",
  "sm:first:border-l-0": "_sm:first:border-l-0_6d3e9",
  "sm:first:border-t-2": "_sm:first:border-t-2_6d3e9",
  "sm:first:border-r-2": "_sm:first:border-r-2_6d3e9",
  "sm:first:border-b-2": "_sm:first:border-b-2_6d3e9",
  "sm:first:border-l-2": "_sm:first:border-l-2_6d3e9",
  "sm:first:border-t-4": "_sm:first:border-t-4_6d3e9",
  "sm:first:border-r-4": "_sm:first:border-r-4_6d3e9",
  "sm:first:border-b-4": "_sm:first:border-b-4_6d3e9",
  "sm:first:border-l-4": "_sm:first:border-l-4_6d3e9",
  "sm:first:border-t-8": "_sm:first:border-t-8_6d3e9",
  "sm:first:border-r-8": "_sm:first:border-r-8_6d3e9",
  "sm:first:border-b-8": "_sm:first:border-b-8_6d3e9",
  "sm:first:border-l-8": "_sm:first:border-l-8_6d3e9",
  "sm:first:border-t": "_sm:first:border-t_6d3e9",
  "sm:first:border-r": "_sm:first:border-r_6d3e9",
  "sm:first:border-b": "_sm:first:border-b_6d3e9",
  "sm:first:border-l": "_sm:first:border-l_6d3e9",
  "sm:hover:border-0": "_sm:hover:border-0_6d3e9",
  "sm:hover:border-2": "_sm:hover:border-2_6d3e9",
  "sm:hover:border-4": "_sm:hover:border-4_6d3e9",
  "sm:hover:border-8": "_sm:hover:border-8_6d3e9",
  "sm:hover:border": "_sm:hover:border_6d3e9",
  "sm:hover:border-t-0": "_sm:hover:border-t-0_6d3e9",
  "sm:hover:border-r-0": "_sm:hover:border-r-0_6d3e9",
  "sm:hover:border-b-0": "_sm:hover:border-b-0_6d3e9",
  "sm:hover:border-l-0": "_sm:hover:border-l-0_6d3e9",
  "sm:hover:border-t-2": "_sm:hover:border-t-2_6d3e9",
  "sm:hover:border-r-2": "_sm:hover:border-r-2_6d3e9",
  "sm:hover:border-b-2": "_sm:hover:border-b-2_6d3e9",
  "sm:hover:border-l-2": "_sm:hover:border-l-2_6d3e9",
  "sm:hover:border-t-4": "_sm:hover:border-t-4_6d3e9",
  "sm:hover:border-r-4": "_sm:hover:border-r-4_6d3e9",
  "sm:hover:border-b-4": "_sm:hover:border-b-4_6d3e9",
  "sm:hover:border-l-4": "_sm:hover:border-l-4_6d3e9",
  "sm:hover:border-t-8": "_sm:hover:border-t-8_6d3e9",
  "sm:hover:border-r-8": "_sm:hover:border-r-8_6d3e9",
  "sm:hover:border-b-8": "_sm:hover:border-b-8_6d3e9",
  "sm:hover:border-l-8": "_sm:hover:border-l-8_6d3e9",
  "sm:hover:border-t": "_sm:hover:border-t_6d3e9",
  "sm:hover:border-r": "_sm:hover:border-r_6d3e9",
  "sm:hover:border-b": "_sm:hover:border-b_6d3e9",
  "sm:hover:border-l": "_sm:hover:border-l_6d3e9",
  "sm:focus:border-0": "_sm:focus:border-0_6d3e9",
  "sm:focus:border-2": "_sm:focus:border-2_6d3e9",
  "sm:focus:border-4": "_sm:focus:border-4_6d3e9",
  "sm:focus:border-8": "_sm:focus:border-8_6d3e9",
  "sm:focus:border": "_sm:focus:border_6d3e9",
  "sm:focus:border-t-0": "_sm:focus:border-t-0_6d3e9",
  "sm:focus:border-r-0": "_sm:focus:border-r-0_6d3e9",
  "sm:focus:border-b-0": "_sm:focus:border-b-0_6d3e9",
  "sm:focus:border-l-0": "_sm:focus:border-l-0_6d3e9",
  "sm:focus:border-t-2": "_sm:focus:border-t-2_6d3e9",
  "sm:focus:border-r-2": "_sm:focus:border-r-2_6d3e9",
  "sm:focus:border-b-2": "_sm:focus:border-b-2_6d3e9",
  "sm:focus:border-l-2": "_sm:focus:border-l-2_6d3e9",
  "sm:focus:border-t-4": "_sm:focus:border-t-4_6d3e9",
  "sm:focus:border-r-4": "_sm:focus:border-r-4_6d3e9",
  "sm:focus:border-b-4": "_sm:focus:border-b-4_6d3e9",
  "sm:focus:border-l-4": "_sm:focus:border-l-4_6d3e9",
  "sm:focus:border-t-8": "_sm:focus:border-t-8_6d3e9",
  "sm:focus:border-r-8": "_sm:focus:border-r-8_6d3e9",
  "sm:focus:border-b-8": "_sm:focus:border-b-8_6d3e9",
  "sm:focus:border-l-8": "_sm:focus:border-l-8_6d3e9",
  "sm:focus:border-t": "_sm:focus:border-t_6d3e9",
  "sm:focus:border-r": "_sm:focus:border-r_6d3e9",
  "sm:focus:border-b": "_sm:focus:border-b_6d3e9",
  "sm:focus:border-l": "_sm:focus:border-l_6d3e9",
  "sm:cursor-auto": "_sm:cursor-auto_6d3e9",
  "sm:cursor-default": "_sm:cursor-default_6d3e9",
  "sm:cursor-pointer": "_sm:cursor-pointer_6d3e9",
  "sm:cursor-wait": "_sm:cursor-wait_6d3e9",
  "sm:cursor-text": "_sm:cursor-text_6d3e9",
  "sm:cursor-move": "_sm:cursor-move_6d3e9",
  "sm:cursor-not-allowed": "_sm:cursor-not-allowed_6d3e9",
  "sm:block": "_sm:block_6d3e9",
  "sm:inline-block": "_sm:inline-block_6d3e9",
  "sm:inline": "_sm:inline_6d3e9",
  "sm:flex": "_sm:flex_6d3e9",
  "sm:inline-flex": "_sm:inline-flex_6d3e9",
  "sm:table": "_sm:table_6d3e9",
  "sm:table-row": "_sm:table-row_6d3e9",
  "sm:table-cell": "_sm:table-cell_6d3e9",
  "sm:hidden": "_sm:hidden_6d3e9",
  "sm:flex-row": "_sm:flex-row_6d3e9",
  "sm:flex-row-reverse": "_sm:flex-row-reverse_6d3e9",
  "sm:flex-col": "_sm:flex-col_6d3e9",
  "sm:flex-col-reverse": "_sm:flex-col-reverse_6d3e9",
  "sm:flex-wrap": "_sm:flex-wrap_6d3e9",
  "sm:flex-wrap-reverse": "_sm:flex-wrap-reverse_6d3e9",
  "sm:flex-no-wrap": "_sm:flex-no-wrap_6d3e9",
  "sm:items-start": "_sm:items-start_6d3e9",
  "sm:items-end": "_sm:items-end_6d3e9",
  "sm:items-center": "_sm:items-center_6d3e9",
  "sm:items-baseline": "_sm:items-baseline_6d3e9",
  "sm:items-stretch": "_sm:items-stretch_6d3e9",
  "sm:self-auto": "_sm:self-auto_6d3e9",
  "sm:self-start": "_sm:self-start_6d3e9",
  "sm:self-end": "_sm:self-end_6d3e9",
  "sm:self-center": "_sm:self-center_6d3e9",
  "sm:self-stretch": "_sm:self-stretch_6d3e9",
  "sm:justify-start": "_sm:justify-start_6d3e9",
  "sm:justify-end": "_sm:justify-end_6d3e9",
  "sm:justify-center": "_sm:justify-center_6d3e9",
  "sm:justify-between": "_sm:justify-between_6d3e9",
  "sm:justify-around": "_sm:justify-around_6d3e9",
  "sm:content-center": "_sm:content-center_6d3e9",
  "sm:content-start": "_sm:content-start_6d3e9",
  "sm:content-end": "_sm:content-end_6d3e9",
  "sm:content-between": "_sm:content-between_6d3e9",
  "sm:content-around": "_sm:content-around_6d3e9",
  "sm:flex-1": "_sm:flex-1_6d3e9",
  "sm:flex-auto": "_sm:flex-auto_6d3e9",
  "sm:flex-initial": "_sm:flex-initial_6d3e9",
  "sm:flex-none": "_sm:flex-none_6d3e9",
  "sm:flex-grow-0": "_sm:flex-grow-0_6d3e9",
  "sm:flex-grow": "_sm:flex-grow_6d3e9",
  "sm:flex-shrink-0": "_sm:flex-shrink-0_6d3e9",
  "sm:flex-shrink": "_sm:flex-shrink_6d3e9",
  "sm:order-1": "_sm:order-1_6d3e9",
  "sm:order-2": "_sm:order-2_6d3e9",
  "sm:order-3": "_sm:order-3_6d3e9",
  "sm:order-4": "_sm:order-4_6d3e9",
  "sm:order-5": "_sm:order-5_6d3e9",
  "sm:order-6": "_sm:order-6_6d3e9",
  "sm:order-7": "_sm:order-7_6d3e9",
  "sm:order-8": "_sm:order-8_6d3e9",
  "sm:order-9": "_sm:order-9_6d3e9",
  "sm:order-10": "_sm:order-10_6d3e9",
  "sm:order-11": "_sm:order-11_6d3e9",
  "sm:order-12": "_sm:order-12_6d3e9",
  "sm:order-first": "_sm:order-first_6d3e9",
  "sm:order-last": "_sm:order-last_6d3e9",
  "sm:order-none": "_sm:order-none_6d3e9",
  "sm:float-right": "_sm:float-right_6d3e9",
  "sm:float-left": "_sm:float-left_6d3e9",
  "sm:float-none": "_sm:float-none_6d3e9",
  "sm:clearfix": "_sm:clearfix_6d3e9",
  "sm:font-sans": "_sm:font-sans_6d3e9",
  "sm:font-serif": "_sm:font-serif_6d3e9",
  "sm:font-mono": "_sm:font-mono_6d3e9",
  "sm:font-hairline": "_sm:font-hairline_6d3e9",
  "sm:font-thin": "_sm:font-thin_6d3e9",
  "sm:font-light": "_sm:font-light_6d3e9",
  "sm:font-normal": "_sm:font-normal_6d3e9",
  "sm:font-medium": "_sm:font-medium_6d3e9",
  "sm:font-semibold": "_sm:font-semibold_6d3e9",
  "sm:font-bold": "_sm:font-bold_6d3e9",
  "sm:font-extrabold": "_sm:font-extrabold_6d3e9",
  "sm:font-black": "_sm:font-black_6d3e9",
  "sm:hover:font-hairline": "_sm:hover:font-hairline_6d3e9",
  "sm:hover:font-thin": "_sm:hover:font-thin_6d3e9",
  "sm:hover:font-light": "_sm:hover:font-light_6d3e9",
  "sm:hover:font-normal": "_sm:hover:font-normal_6d3e9",
  "sm:hover:font-medium": "_sm:hover:font-medium_6d3e9",
  "sm:hover:font-semibold": "_sm:hover:font-semibold_6d3e9",
  "sm:hover:font-bold": "_sm:hover:font-bold_6d3e9",
  "sm:hover:font-extrabold": "_sm:hover:font-extrabold_6d3e9",
  "sm:hover:font-black": "_sm:hover:font-black_6d3e9",
  "sm:focus:font-hairline": "_sm:focus:font-hairline_6d3e9",
  "sm:focus:font-thin": "_sm:focus:font-thin_6d3e9",
  "sm:focus:font-light": "_sm:focus:font-light_6d3e9",
  "sm:focus:font-normal": "_sm:focus:font-normal_6d3e9",
  "sm:focus:font-medium": "_sm:focus:font-medium_6d3e9",
  "sm:focus:font-semibold": "_sm:focus:font-semibold_6d3e9",
  "sm:focus:font-bold": "_sm:focus:font-bold_6d3e9",
  "sm:focus:font-extrabold": "_sm:focus:font-extrabold_6d3e9",
  "sm:focus:font-black": "_sm:focus:font-black_6d3e9",
  "sm:h-0": "_sm:h-0_6d3e9",
  "sm:h-1": "_sm:h-1_6d3e9",
  "sm:h-2": "_sm:h-2_6d3e9",
  "sm:h-3": "_sm:h-3_6d3e9",
  "sm:h-4": "_sm:h-4_6d3e9",
  "sm:h-5": "_sm:h-5_6d3e9",
  "sm:h-6": "_sm:h-6_6d3e9",
  "sm:h-8": "_sm:h-8_6d3e9",
  "sm:h-10": "_sm:h-10_6d3e9",
  "sm:h-12": "_sm:h-12_6d3e9",
  "sm:h-16": "_sm:h-16_6d3e9",
  "sm:h-20": "_sm:h-20_6d3e9",
  "sm:h-24": "_sm:h-24_6d3e9",
  "sm:h-32": "_sm:h-32_6d3e9",
  "sm:h-40": "_sm:h-40_6d3e9",
  "sm:h-48": "_sm:h-48_6d3e9",
  "sm:h-56": "_sm:h-56_6d3e9",
  "sm:h-64": "_sm:h-64_6d3e9",
  "sm:h-auto": "_sm:h-auto_6d3e9",
  "sm:h-px": "_sm:h-px_6d3e9",
  "sm:h-full": "_sm:h-full_6d3e9",
  "sm:h-screen": "_sm:h-screen_6d3e9",
  "sm:leading-none": "_sm:leading-none_6d3e9",
  "sm:leading-tight": "_sm:leading-tight_6d3e9",
  "sm:leading-snug": "_sm:leading-snug_6d3e9",
  "sm:leading-normal": "_sm:leading-normal_6d3e9",
  "sm:leading-relaxed": "_sm:leading-relaxed_6d3e9",
  "sm:leading-loose": "_sm:leading-loose_6d3e9",
  "sm:list-inside": "_sm:list-inside_6d3e9",
  "sm:list-outside": "_sm:list-outside_6d3e9",
  "sm:list-none": "_sm:list-none_6d3e9",
  "sm:list-disc": "_sm:list-disc_6d3e9",
  "sm:list-decimal": "_sm:list-decimal_6d3e9",
  "sm:m-0": "_sm:m-0_6d3e9",
  "sm:m-1": "_sm:m-1_6d3e9",
  "sm:m-2": "_sm:m-2_6d3e9",
  "sm:m-3": "_sm:m-3_6d3e9",
  "sm:m-4": "_sm:m-4_6d3e9",
  "sm:m-5": "_sm:m-5_6d3e9",
  "sm:m-6": "_sm:m-6_6d3e9",
  "sm:m-8": "_sm:m-8_6d3e9",
  "sm:m-10": "_sm:m-10_6d3e9",
  "sm:m-12": "_sm:m-12_6d3e9",
  "sm:m-16": "_sm:m-16_6d3e9",
  "sm:m-20": "_sm:m-20_6d3e9",
  "sm:m-24": "_sm:m-24_6d3e9",
  "sm:m-32": "_sm:m-32_6d3e9",
  "sm:m-40": "_sm:m-40_6d3e9",
  "sm:m-48": "_sm:m-48_6d3e9",
  "sm:m-56": "_sm:m-56_6d3e9",
  "sm:m-64": "_sm:m-64_6d3e9",
  "sm:m-auto": "_sm:m-auto_6d3e9",
  "sm:m-px": "_sm:m-px_6d3e9",
  "sm:-m-1": "_sm:-m-1_6d3e9",
  "sm:-m-2": "_sm:-m-2_6d3e9",
  "sm:-m-3": "_sm:-m-3_6d3e9",
  "sm:-m-4": "_sm:-m-4_6d3e9",
  "sm:-m-5": "_sm:-m-5_6d3e9",
  "sm:-m-6": "_sm:-m-6_6d3e9",
  "sm:-m-8": "_sm:-m-8_6d3e9",
  "sm:-m-10": "_sm:-m-10_6d3e9",
  "sm:-m-12": "_sm:-m-12_6d3e9",
  "sm:-m-16": "_sm:-m-16_6d3e9",
  "sm:-m-20": "_sm:-m-20_6d3e9",
  "sm:-m-24": "_sm:-m-24_6d3e9",
  "sm:-m-32": "_sm:-m-32_6d3e9",
  "sm:-m-40": "_sm:-m-40_6d3e9",
  "sm:-m-48": "_sm:-m-48_6d3e9",
  "sm:-m-56": "_sm:-m-56_6d3e9",
  "sm:-m-64": "_sm:-m-64_6d3e9",
  "sm:-m-px": "_sm:-m-px_6d3e9",
  "sm:my-0": "_sm:my-0_6d3e9",
  "sm:mx-0": "_sm:mx-0_6d3e9",
  "sm:my-1": "_sm:my-1_6d3e9",
  "sm:mx-1": "_sm:mx-1_6d3e9",
  "sm:my-2": "_sm:my-2_6d3e9",
  "sm:mx-2": "_sm:mx-2_6d3e9",
  "sm:my-3": "_sm:my-3_6d3e9",
  "sm:mx-3": "_sm:mx-3_6d3e9",
  "sm:my-4": "_sm:my-4_6d3e9",
  "sm:mx-4": "_sm:mx-4_6d3e9",
  "sm:my-5": "_sm:my-5_6d3e9",
  "sm:mx-5": "_sm:mx-5_6d3e9",
  "sm:my-6": "_sm:my-6_6d3e9",
  "sm:mx-6": "_sm:mx-6_6d3e9",
  "sm:my-8": "_sm:my-8_6d3e9",
  "sm:mx-8": "_sm:mx-8_6d3e9",
  "sm:my-10": "_sm:my-10_6d3e9",
  "sm:mx-10": "_sm:mx-10_6d3e9",
  "sm:my-12": "_sm:my-12_6d3e9",
  "sm:mx-12": "_sm:mx-12_6d3e9",
  "sm:my-16": "_sm:my-16_6d3e9",
  "sm:mx-16": "_sm:mx-16_6d3e9",
  "sm:my-20": "_sm:my-20_6d3e9",
  "sm:mx-20": "_sm:mx-20_6d3e9",
  "sm:my-24": "_sm:my-24_6d3e9",
  "sm:mx-24": "_sm:mx-24_6d3e9",
  "sm:my-32": "_sm:my-32_6d3e9",
  "sm:mx-32": "_sm:mx-32_6d3e9",
  "sm:my-40": "_sm:my-40_6d3e9",
  "sm:mx-40": "_sm:mx-40_6d3e9",
  "sm:my-48": "_sm:my-48_6d3e9",
  "sm:mx-48": "_sm:mx-48_6d3e9",
  "sm:my-56": "_sm:my-56_6d3e9",
  "sm:mx-56": "_sm:mx-56_6d3e9",
  "sm:my-64": "_sm:my-64_6d3e9",
  "sm:mx-64": "_sm:mx-64_6d3e9",
  "sm:my-auto": "_sm:my-auto_6d3e9",
  "sm:mx-auto": "_sm:mx-auto_6d3e9",
  "sm:my-px": "_sm:my-px_6d3e9",
  "sm:mx-px": "_sm:mx-px_6d3e9",
  "sm:-my-1": "_sm:-my-1_6d3e9",
  "sm:-mx-1": "_sm:-mx-1_6d3e9",
  "sm:-my-2": "_sm:-my-2_6d3e9",
  "sm:-mx-2": "_sm:-mx-2_6d3e9",
  "sm:-my-3": "_sm:-my-3_6d3e9",
  "sm:-mx-3": "_sm:-mx-3_6d3e9",
  "sm:-my-4": "_sm:-my-4_6d3e9",
  "sm:-mx-4": "_sm:-mx-4_6d3e9",
  "sm:-my-5": "_sm:-my-5_6d3e9",
  "sm:-mx-5": "_sm:-mx-5_6d3e9",
  "sm:-my-6": "_sm:-my-6_6d3e9",
  "sm:-mx-6": "_sm:-mx-6_6d3e9",
  "sm:-my-8": "_sm:-my-8_6d3e9",
  "sm:-mx-8": "_sm:-mx-8_6d3e9",
  "sm:-my-10": "_sm:-my-10_6d3e9",
  "sm:-mx-10": "_sm:-mx-10_6d3e9",
  "sm:-my-12": "_sm:-my-12_6d3e9",
  "sm:-mx-12": "_sm:-mx-12_6d3e9",
  "sm:-my-16": "_sm:-my-16_6d3e9",
  "sm:-mx-16": "_sm:-mx-16_6d3e9",
  "sm:-my-20": "_sm:-my-20_6d3e9",
  "sm:-mx-20": "_sm:-mx-20_6d3e9",
  "sm:-my-24": "_sm:-my-24_6d3e9",
  "sm:-mx-24": "_sm:-mx-24_6d3e9",
  "sm:-my-32": "_sm:-my-32_6d3e9",
  "sm:-mx-32": "_sm:-mx-32_6d3e9",
  "sm:-my-40": "_sm:-my-40_6d3e9",
  "sm:-mx-40": "_sm:-mx-40_6d3e9",
  "sm:-my-48": "_sm:-my-48_6d3e9",
  "sm:-mx-48": "_sm:-mx-48_6d3e9",
  "sm:-my-56": "_sm:-my-56_6d3e9",
  "sm:-mx-56": "_sm:-mx-56_6d3e9",
  "sm:-my-64": "_sm:-my-64_6d3e9",
  "sm:-mx-64": "_sm:-mx-64_6d3e9",
  "sm:-my-px": "_sm:-my-px_6d3e9",
  "sm:-mx-px": "_sm:-mx-px_6d3e9",
  "sm:mt-0": "_sm:mt-0_6d3e9",
  "sm:mr-0": "_sm:mr-0_6d3e9",
  "sm:mb-0": "_sm:mb-0_6d3e9",
  "sm:ml-0": "_sm:ml-0_6d3e9",
  "sm:mt-1": "_sm:mt-1_6d3e9",
  "sm:mr-1": "_sm:mr-1_6d3e9",
  "sm:mb-1": "_sm:mb-1_6d3e9",
  "sm:ml-1": "_sm:ml-1_6d3e9",
  "sm:mt-2": "_sm:mt-2_6d3e9",
  "sm:mr-2": "_sm:mr-2_6d3e9",
  "sm:mb-2": "_sm:mb-2_6d3e9",
  "sm:ml-2": "_sm:ml-2_6d3e9",
  "sm:mt-3": "_sm:mt-3_6d3e9",
  "sm:mr-3": "_sm:mr-3_6d3e9",
  "sm:mb-3": "_sm:mb-3_6d3e9",
  "sm:ml-3": "_sm:ml-3_6d3e9",
  "sm:mt-4": "_sm:mt-4_6d3e9",
  "sm:mr-4": "_sm:mr-4_6d3e9",
  "sm:mb-4": "_sm:mb-4_6d3e9",
  "sm:ml-4": "_sm:ml-4_6d3e9",
  "sm:mt-5": "_sm:mt-5_6d3e9",
  "sm:mr-5": "_sm:mr-5_6d3e9",
  "sm:mb-5": "_sm:mb-5_6d3e9",
  "sm:ml-5": "_sm:ml-5_6d3e9",
  "sm:mt-6": "_sm:mt-6_6d3e9",
  "sm:mr-6": "_sm:mr-6_6d3e9",
  "sm:mb-6": "_sm:mb-6_6d3e9",
  "sm:ml-6": "_sm:ml-6_6d3e9",
  "sm:mt-8": "_sm:mt-8_6d3e9",
  "sm:mr-8": "_sm:mr-8_6d3e9",
  "sm:mb-8": "_sm:mb-8_6d3e9",
  "sm:ml-8": "_sm:ml-8_6d3e9",
  "sm:mt-10": "_sm:mt-10_6d3e9",
  "sm:mr-10": "_sm:mr-10_6d3e9",
  "sm:mb-10": "_sm:mb-10_6d3e9",
  "sm:ml-10": "_sm:ml-10_6d3e9",
  "sm:mt-12": "_sm:mt-12_6d3e9",
  "sm:mr-12": "_sm:mr-12_6d3e9",
  "sm:mb-12": "_sm:mb-12_6d3e9",
  "sm:ml-12": "_sm:ml-12_6d3e9",
  "sm:mt-16": "_sm:mt-16_6d3e9",
  "sm:mr-16": "_sm:mr-16_6d3e9",
  "sm:mb-16": "_sm:mb-16_6d3e9",
  "sm:ml-16": "_sm:ml-16_6d3e9",
  "sm:mt-20": "_sm:mt-20_6d3e9",
  "sm:mr-20": "_sm:mr-20_6d3e9",
  "sm:mb-20": "_sm:mb-20_6d3e9",
  "sm:ml-20": "_sm:ml-20_6d3e9",
  "sm:mt-24": "_sm:mt-24_6d3e9",
  "sm:mr-24": "_sm:mr-24_6d3e9",
  "sm:mb-24": "_sm:mb-24_6d3e9",
  "sm:ml-24": "_sm:ml-24_6d3e9",
  "sm:mt-32": "_sm:mt-32_6d3e9",
  "sm:mr-32": "_sm:mr-32_6d3e9",
  "sm:mb-32": "_sm:mb-32_6d3e9",
  "sm:ml-32": "_sm:ml-32_6d3e9",
  "sm:mt-40": "_sm:mt-40_6d3e9",
  "sm:mr-40": "_sm:mr-40_6d3e9",
  "sm:mb-40": "_sm:mb-40_6d3e9",
  "sm:ml-40": "_sm:ml-40_6d3e9",
  "sm:mt-48": "_sm:mt-48_6d3e9",
  "sm:mr-48": "_sm:mr-48_6d3e9",
  "sm:mb-48": "_sm:mb-48_6d3e9",
  "sm:ml-48": "_sm:ml-48_6d3e9",
  "sm:mt-56": "_sm:mt-56_6d3e9",
  "sm:mr-56": "_sm:mr-56_6d3e9",
  "sm:mb-56": "_sm:mb-56_6d3e9",
  "sm:ml-56": "_sm:ml-56_6d3e9",
  "sm:mt-64": "_sm:mt-64_6d3e9",
  "sm:mr-64": "_sm:mr-64_6d3e9",
  "sm:mb-64": "_sm:mb-64_6d3e9",
  "sm:ml-64": "_sm:ml-64_6d3e9",
  "sm:mt-auto": "_sm:mt-auto_6d3e9",
  "sm:mr-auto": "_sm:mr-auto_6d3e9",
  "sm:mb-auto": "_sm:mb-auto_6d3e9",
  "sm:ml-auto": "_sm:ml-auto_6d3e9",
  "sm:mt-px": "_sm:mt-px_6d3e9",
  "sm:mr-px": "_sm:mr-px_6d3e9",
  "sm:mb-px": "_sm:mb-px_6d3e9",
  "sm:ml-px": "_sm:ml-px_6d3e9",
  "sm:-mt-1": "_sm:-mt-1_6d3e9",
  "sm:-mr-1": "_sm:-mr-1_6d3e9",
  "sm:-mb-1": "_sm:-mb-1_6d3e9",
  "sm:-ml-1": "_sm:-ml-1_6d3e9",
  "sm:-mt-2": "_sm:-mt-2_6d3e9",
  "sm:-mr-2": "_sm:-mr-2_6d3e9",
  "sm:-mb-2": "_sm:-mb-2_6d3e9",
  "sm:-ml-2": "_sm:-ml-2_6d3e9",
  "sm:-mt-3": "_sm:-mt-3_6d3e9",
  "sm:-mr-3": "_sm:-mr-3_6d3e9",
  "sm:-mb-3": "_sm:-mb-3_6d3e9",
  "sm:-ml-3": "_sm:-ml-3_6d3e9",
  "sm:-mt-4": "_sm:-mt-4_6d3e9",
  "sm:-mr-4": "_sm:-mr-4_6d3e9",
  "sm:-mb-4": "_sm:-mb-4_6d3e9",
  "sm:-ml-4": "_sm:-ml-4_6d3e9",
  "sm:-mt-5": "_sm:-mt-5_6d3e9",
  "sm:-mr-5": "_sm:-mr-5_6d3e9",
  "sm:-mb-5": "_sm:-mb-5_6d3e9",
  "sm:-ml-5": "_sm:-ml-5_6d3e9",
  "sm:-mt-6": "_sm:-mt-6_6d3e9",
  "sm:-mr-6": "_sm:-mr-6_6d3e9",
  "sm:-mb-6": "_sm:-mb-6_6d3e9",
  "sm:-ml-6": "_sm:-ml-6_6d3e9",
  "sm:-mt-8": "_sm:-mt-8_6d3e9",
  "sm:-mr-8": "_sm:-mr-8_6d3e9",
  "sm:-mb-8": "_sm:-mb-8_6d3e9",
  "sm:-ml-8": "_sm:-ml-8_6d3e9",
  "sm:-mt-10": "_sm:-mt-10_6d3e9",
  "sm:-mr-10": "_sm:-mr-10_6d3e9",
  "sm:-mb-10": "_sm:-mb-10_6d3e9",
  "sm:-ml-10": "_sm:-ml-10_6d3e9",
  "sm:-mt-12": "_sm:-mt-12_6d3e9",
  "sm:-mr-12": "_sm:-mr-12_6d3e9",
  "sm:-mb-12": "_sm:-mb-12_6d3e9",
  "sm:-ml-12": "_sm:-ml-12_6d3e9",
  "sm:-mt-16": "_sm:-mt-16_6d3e9",
  "sm:-mr-16": "_sm:-mr-16_6d3e9",
  "sm:-mb-16": "_sm:-mb-16_6d3e9",
  "sm:-ml-16": "_sm:-ml-16_6d3e9",
  "sm:-mt-20": "_sm:-mt-20_6d3e9",
  "sm:-mr-20": "_sm:-mr-20_6d3e9",
  "sm:-mb-20": "_sm:-mb-20_6d3e9",
  "sm:-ml-20": "_sm:-ml-20_6d3e9",
  "sm:-mt-24": "_sm:-mt-24_6d3e9",
  "sm:-mr-24": "_sm:-mr-24_6d3e9",
  "sm:-mb-24": "_sm:-mb-24_6d3e9",
  "sm:-ml-24": "_sm:-ml-24_6d3e9",
  "sm:-mt-32": "_sm:-mt-32_6d3e9",
  "sm:-mr-32": "_sm:-mr-32_6d3e9",
  "sm:-mb-32": "_sm:-mb-32_6d3e9",
  "sm:-ml-32": "_sm:-ml-32_6d3e9",
  "sm:-mt-40": "_sm:-mt-40_6d3e9",
  "sm:-mr-40": "_sm:-mr-40_6d3e9",
  "sm:-mb-40": "_sm:-mb-40_6d3e9",
  "sm:-ml-40": "_sm:-ml-40_6d3e9",
  "sm:-mt-48": "_sm:-mt-48_6d3e9",
  "sm:-mr-48": "_sm:-mr-48_6d3e9",
  "sm:-mb-48": "_sm:-mb-48_6d3e9",
  "sm:-ml-48": "_sm:-ml-48_6d3e9",
  "sm:-mt-56": "_sm:-mt-56_6d3e9",
  "sm:-mr-56": "_sm:-mr-56_6d3e9",
  "sm:-mb-56": "_sm:-mb-56_6d3e9",
  "sm:-ml-56": "_sm:-ml-56_6d3e9",
  "sm:-mt-64": "_sm:-mt-64_6d3e9",
  "sm:-mr-64": "_sm:-mr-64_6d3e9",
  "sm:-mb-64": "_sm:-mb-64_6d3e9",
  "sm:-ml-64": "_sm:-ml-64_6d3e9",
  "sm:-mt-px": "_sm:-mt-px_6d3e9",
  "sm:-mr-px": "_sm:-mr-px_6d3e9",
  "sm:-mb-px": "_sm:-mb-px_6d3e9",
  "sm:-ml-px": "_sm:-ml-px_6d3e9",
  "sm:max-h-full": "_sm:max-h-full_6d3e9",
  "sm:max-h-screen": "_sm:max-h-screen_6d3e9",
  "sm:max-w-xs": "_sm:max-w-xs_6d3e9",
  "sm:max-w-sm": "_sm:max-w-sm_6d3e9",
  "sm:max-w-md": "_sm:max-w-md_6d3e9",
  "sm:max-w-lg": "_sm:max-w-lg_6d3e9",
  "sm:max-w-xl": "_sm:max-w-xl_6d3e9",
  "sm:max-w-2xl": "_sm:max-w-2xl_6d3e9",
  "sm:max-w-3xl": "_sm:max-w-3xl_6d3e9",
  "sm:max-w-4xl": "_sm:max-w-4xl_6d3e9",
  "sm:max-w-5xl": "_sm:max-w-5xl_6d3e9",
  "sm:max-w-6xl": "_sm:max-w-6xl_6d3e9",
  "sm:max-w-full": "_sm:max-w-full_6d3e9",
  "sm:min-h-0": "_sm:min-h-0_6d3e9",
  "sm:min-h-full": "_sm:min-h-full_6d3e9",
  "sm:min-h-screen": "_sm:min-h-screen_6d3e9",
  "sm:min-w-0": "_sm:min-w-0_6d3e9",
  "sm:min-w-full": "_sm:min-w-full_6d3e9",
  "sm:object-contain": "_sm:object-contain_6d3e9",
  "sm:object-cover": "_sm:object-cover_6d3e9",
  "sm:object-fill": "_sm:object-fill_6d3e9",
  "sm:object-none": "_sm:object-none_6d3e9",
  "sm:object-scale-down": "_sm:object-scale-down_6d3e9",
  "sm:object-bottom": "_sm:object-bottom_6d3e9",
  "sm:object-center": "_sm:object-center_6d3e9",
  "sm:object-left": "_sm:object-left_6d3e9",
  "sm:object-left-bottom": "_sm:object-left-bottom_6d3e9",
  "sm:object-left-top": "_sm:object-left-top_6d3e9",
  "sm:object-right": "_sm:object-right_6d3e9",
  "sm:object-right-bottom": "_sm:object-right-bottom_6d3e9",
  "sm:object-right-top": "_sm:object-right-top_6d3e9",
  "sm:object-top": "_sm:object-top_6d3e9",
  "sm:opacity-0": "_sm:opacity-0_6d3e9",
  "sm:opacity-25": "_sm:opacity-25_6d3e9",
  "sm:opacity-50": "_sm:opacity-50_6d3e9",
  "sm:opacity-75": "_sm:opacity-75_6d3e9",
  "sm:opacity-100": "_sm:opacity-100_6d3e9",
  "sm:hover:opacity-0": "_sm:hover:opacity-0_6d3e9",
  "sm:hover:opacity-25": "_sm:hover:opacity-25_6d3e9",
  "sm:hover:opacity-50": "_sm:hover:opacity-50_6d3e9",
  "sm:hover:opacity-75": "_sm:hover:opacity-75_6d3e9",
  "sm:hover:opacity-100": "_sm:hover:opacity-100_6d3e9",
  "sm:focus:opacity-0": "_sm:focus:opacity-0_6d3e9",
  "sm:focus:opacity-25": "_sm:focus:opacity-25_6d3e9",
  "sm:focus:opacity-50": "_sm:focus:opacity-50_6d3e9",
  "sm:focus:opacity-75": "_sm:focus:opacity-75_6d3e9",
  "sm:focus:opacity-100": "_sm:focus:opacity-100_6d3e9",
  "sm:outline-none": "_sm:outline-none_6d3e9",
  "sm:focus:outline-none": "_sm:focus:outline-none_6d3e9",
  "sm:overflow-auto": "_sm:overflow-auto_6d3e9",
  "sm:overflow-hidden": "_sm:overflow-hidden_6d3e9",
  "sm:overflow-visible": "_sm:overflow-visible_6d3e9",
  "sm:overflow-scroll": "_sm:overflow-scroll_6d3e9",
  "sm:overflow-x-auto": "_sm:overflow-x-auto_6d3e9",
  "sm:overflow-y-auto": "_sm:overflow-y-auto_6d3e9",
  "sm:overflow-x-hidden": "_sm:overflow-x-hidden_6d3e9",
  "sm:overflow-y-hidden": "_sm:overflow-y-hidden_6d3e9",
  "sm:overflow-x-visible": "_sm:overflow-x-visible_6d3e9",
  "sm:overflow-y-visible": "_sm:overflow-y-visible_6d3e9",
  "sm:overflow-x-scroll": "_sm:overflow-x-scroll_6d3e9",
  "sm:overflow-y-scroll": "_sm:overflow-y-scroll_6d3e9",
  "sm:scrolling-touch": "_sm:scrolling-touch_6d3e9",
  "sm:scrolling-auto": "_sm:scrolling-auto_6d3e9",
  "sm:p-0": "_sm:p-0_6d3e9",
  "sm:p-1": "_sm:p-1_6d3e9",
  "sm:p-2": "_sm:p-2_6d3e9",
  "sm:p-3": "_sm:p-3_6d3e9",
  "sm:p-4": "_sm:p-4_6d3e9",
  "sm:p-5": "_sm:p-5_6d3e9",
  "sm:p-6": "_sm:p-6_6d3e9",
  "sm:p-8": "_sm:p-8_6d3e9",
  "sm:p-10": "_sm:p-10_6d3e9",
  "sm:p-12": "_sm:p-12_6d3e9",
  "sm:p-16": "_sm:p-16_6d3e9",
  "sm:p-20": "_sm:p-20_6d3e9",
  "sm:p-24": "_sm:p-24_6d3e9",
  "sm:p-32": "_sm:p-32_6d3e9",
  "sm:p-40": "_sm:p-40_6d3e9",
  "sm:p-48": "_sm:p-48_6d3e9",
  "sm:p-56": "_sm:p-56_6d3e9",
  "sm:p-64": "_sm:p-64_6d3e9",
  "sm:p-px": "_sm:p-px_6d3e9",
  "sm:py-0": "_sm:py-0_6d3e9",
  "sm:px-0": "_sm:px-0_6d3e9",
  "sm:py-1": "_sm:py-1_6d3e9",
  "sm:px-1": "_sm:px-1_6d3e9",
  "sm:py-2": "_sm:py-2_6d3e9",
  "sm:px-2": "_sm:px-2_6d3e9",
  "sm:py-3": "_sm:py-3_6d3e9",
  "sm:px-3": "_sm:px-3_6d3e9",
  "sm:py-4": "_sm:py-4_6d3e9",
  "sm:px-4": "_sm:px-4_6d3e9",
  "sm:py-5": "_sm:py-5_6d3e9",
  "sm:px-5": "_sm:px-5_6d3e9",
  "sm:py-6": "_sm:py-6_6d3e9",
  "sm:px-6": "_sm:px-6_6d3e9",
  "sm:py-8": "_sm:py-8_6d3e9",
  "sm:px-8": "_sm:px-8_6d3e9",
  "sm:py-10": "_sm:py-10_6d3e9",
  "sm:px-10": "_sm:px-10_6d3e9",
  "sm:py-12": "_sm:py-12_6d3e9",
  "sm:px-12": "_sm:px-12_6d3e9",
  "sm:py-16": "_sm:py-16_6d3e9",
  "sm:px-16": "_sm:px-16_6d3e9",
  "sm:py-20": "_sm:py-20_6d3e9",
  "sm:px-20": "_sm:px-20_6d3e9",
  "sm:py-24": "_sm:py-24_6d3e9",
  "sm:px-24": "_sm:px-24_6d3e9",
  "sm:py-32": "_sm:py-32_6d3e9",
  "sm:px-32": "_sm:px-32_6d3e9",
  "sm:py-40": "_sm:py-40_6d3e9",
  "sm:px-40": "_sm:px-40_6d3e9",
  "sm:py-48": "_sm:py-48_6d3e9",
  "sm:px-48": "_sm:px-48_6d3e9",
  "sm:py-56": "_sm:py-56_6d3e9",
  "sm:px-56": "_sm:px-56_6d3e9",
  "sm:py-64": "_sm:py-64_6d3e9",
  "sm:px-64": "_sm:px-64_6d3e9",
  "sm:py-px": "_sm:py-px_6d3e9",
  "sm:px-px": "_sm:px-px_6d3e9",
  "sm:pt-0": "_sm:pt-0_6d3e9",
  "sm:pr-0": "_sm:pr-0_6d3e9",
  "sm:pb-0": "_sm:pb-0_6d3e9",
  "sm:pl-0": "_sm:pl-0_6d3e9",
  "sm:pt-1": "_sm:pt-1_6d3e9",
  "sm:pr-1": "_sm:pr-1_6d3e9",
  "sm:pb-1": "_sm:pb-1_6d3e9",
  "sm:pl-1": "_sm:pl-1_6d3e9",
  "sm:pt-2": "_sm:pt-2_6d3e9",
  "sm:pr-2": "_sm:pr-2_6d3e9",
  "sm:pb-2": "_sm:pb-2_6d3e9",
  "sm:pl-2": "_sm:pl-2_6d3e9",
  "sm:pt-3": "_sm:pt-3_6d3e9",
  "sm:pr-3": "_sm:pr-3_6d3e9",
  "sm:pb-3": "_sm:pb-3_6d3e9",
  "sm:pl-3": "_sm:pl-3_6d3e9",
  "sm:pt-4": "_sm:pt-4_6d3e9",
  "sm:pr-4": "_sm:pr-4_6d3e9",
  "sm:pb-4": "_sm:pb-4_6d3e9",
  "sm:pl-4": "_sm:pl-4_6d3e9",
  "sm:pt-5": "_sm:pt-5_6d3e9",
  "sm:pr-5": "_sm:pr-5_6d3e9",
  "sm:pb-5": "_sm:pb-5_6d3e9",
  "sm:pl-5": "_sm:pl-5_6d3e9",
  "sm:pt-6": "_sm:pt-6_6d3e9",
  "sm:pr-6": "_sm:pr-6_6d3e9",
  "sm:pb-6": "_sm:pb-6_6d3e9",
  "sm:pl-6": "_sm:pl-6_6d3e9",
  "sm:pt-8": "_sm:pt-8_6d3e9",
  "sm:pr-8": "_sm:pr-8_6d3e9",
  "sm:pb-8": "_sm:pb-8_6d3e9",
  "sm:pl-8": "_sm:pl-8_6d3e9",
  "sm:pt-10": "_sm:pt-10_6d3e9",
  "sm:pr-10": "_sm:pr-10_6d3e9",
  "sm:pb-10": "_sm:pb-10_6d3e9",
  "sm:pl-10": "_sm:pl-10_6d3e9",
  "sm:pt-12": "_sm:pt-12_6d3e9",
  "sm:pr-12": "_sm:pr-12_6d3e9",
  "sm:pb-12": "_sm:pb-12_6d3e9",
  "sm:pl-12": "_sm:pl-12_6d3e9",
  "sm:pt-16": "_sm:pt-16_6d3e9",
  "sm:pr-16": "_sm:pr-16_6d3e9",
  "sm:pb-16": "_sm:pb-16_6d3e9",
  "sm:pl-16": "_sm:pl-16_6d3e9",
  "sm:pt-20": "_sm:pt-20_6d3e9",
  "sm:pr-20": "_sm:pr-20_6d3e9",
  "sm:pb-20": "_sm:pb-20_6d3e9",
  "sm:pl-20": "_sm:pl-20_6d3e9",
  "sm:pt-24": "_sm:pt-24_6d3e9",
  "sm:pr-24": "_sm:pr-24_6d3e9",
  "sm:pb-24": "_sm:pb-24_6d3e9",
  "sm:pl-24": "_sm:pl-24_6d3e9",
  "sm:pt-32": "_sm:pt-32_6d3e9",
  "sm:pr-32": "_sm:pr-32_6d3e9",
  "sm:pb-32": "_sm:pb-32_6d3e9",
  "sm:pl-32": "_sm:pl-32_6d3e9",
  "sm:pt-40": "_sm:pt-40_6d3e9",
  "sm:pr-40": "_sm:pr-40_6d3e9",
  "sm:pb-40": "_sm:pb-40_6d3e9",
  "sm:pl-40": "_sm:pl-40_6d3e9",
  "sm:pt-48": "_sm:pt-48_6d3e9",
  "sm:pr-48": "_sm:pr-48_6d3e9",
  "sm:pb-48": "_sm:pb-48_6d3e9",
  "sm:pl-48": "_sm:pl-48_6d3e9",
  "sm:pt-56": "_sm:pt-56_6d3e9",
  "sm:pr-56": "_sm:pr-56_6d3e9",
  "sm:pb-56": "_sm:pb-56_6d3e9",
  "sm:pl-56": "_sm:pl-56_6d3e9",
  "sm:pt-64": "_sm:pt-64_6d3e9",
  "sm:pr-64": "_sm:pr-64_6d3e9",
  "sm:pb-64": "_sm:pb-64_6d3e9",
  "sm:pl-64": "_sm:pl-64_6d3e9",
  "sm:pt-px": "_sm:pt-px_6d3e9",
  "sm:pr-px": "_sm:pr-px_6d3e9",
  "sm:pb-px": "_sm:pb-px_6d3e9",
  "sm:pl-px": "_sm:pl-px_6d3e9",
  "sm:placeholder-transparent": "_sm:placeholder-transparent_6d3e9",
  "sm:placeholder-black": "_sm:placeholder-black_6d3e9",
  "sm:placeholder-white": "_sm:placeholder-white_6d3e9",
  "sm:placeholder-gray-100": "_sm:placeholder-gray-100_6d3e9",
  "sm:placeholder-gray-200": "_sm:placeholder-gray-200_6d3e9",
  "sm:placeholder-gray-300": "_sm:placeholder-gray-300_6d3e9",
  "sm:placeholder-gray-400": "_sm:placeholder-gray-400_6d3e9",
  "sm:placeholder-gray-500": "_sm:placeholder-gray-500_6d3e9",
  "sm:placeholder-gray-600": "_sm:placeholder-gray-600_6d3e9",
  "sm:placeholder-gray-700": "_sm:placeholder-gray-700_6d3e9",
  "sm:placeholder-gray-800": "_sm:placeholder-gray-800_6d3e9",
  "sm:placeholder-gray-900": "_sm:placeholder-gray-900_6d3e9",
  "sm:placeholder-red-100": "_sm:placeholder-red-100_6d3e9",
  "sm:placeholder-red-200": "_sm:placeholder-red-200_6d3e9",
  "sm:placeholder-red-300": "_sm:placeholder-red-300_6d3e9",
  "sm:placeholder-red-400": "_sm:placeholder-red-400_6d3e9",
  "sm:placeholder-red-500": "_sm:placeholder-red-500_6d3e9",
  "sm:placeholder-red-600": "_sm:placeholder-red-600_6d3e9",
  "sm:placeholder-red-700": "_sm:placeholder-red-700_6d3e9",
  "sm:placeholder-red-800": "_sm:placeholder-red-800_6d3e9",
  "sm:placeholder-red-900": "_sm:placeholder-red-900_6d3e9",
  "sm:placeholder-orange-100": "_sm:placeholder-orange-100_6d3e9",
  "sm:placeholder-orange-200": "_sm:placeholder-orange-200_6d3e9",
  "sm:placeholder-orange-300": "_sm:placeholder-orange-300_6d3e9",
  "sm:placeholder-orange-400": "_sm:placeholder-orange-400_6d3e9",
  "sm:placeholder-orange-500": "_sm:placeholder-orange-500_6d3e9",
  "sm:placeholder-orange-600": "_sm:placeholder-orange-600_6d3e9",
  "sm:placeholder-orange-700": "_sm:placeholder-orange-700_6d3e9",
  "sm:placeholder-orange-800": "_sm:placeholder-orange-800_6d3e9",
  "sm:placeholder-orange-900": "_sm:placeholder-orange-900_6d3e9",
  "sm:placeholder-yellow-100": "_sm:placeholder-yellow-100_6d3e9",
  "sm:placeholder-yellow-200": "_sm:placeholder-yellow-200_6d3e9",
  "sm:placeholder-yellow-300": "_sm:placeholder-yellow-300_6d3e9",
  "sm:placeholder-yellow-400": "_sm:placeholder-yellow-400_6d3e9",
  "sm:placeholder-yellow-500": "_sm:placeholder-yellow-500_6d3e9",
  "sm:placeholder-yellow-600": "_sm:placeholder-yellow-600_6d3e9",
  "sm:placeholder-yellow-700": "_sm:placeholder-yellow-700_6d3e9",
  "sm:placeholder-yellow-800": "_sm:placeholder-yellow-800_6d3e9",
  "sm:placeholder-yellow-900": "_sm:placeholder-yellow-900_6d3e9",
  "sm:placeholder-green-100": "_sm:placeholder-green-100_6d3e9",
  "sm:placeholder-green-200": "_sm:placeholder-green-200_6d3e9",
  "sm:placeholder-green-300": "_sm:placeholder-green-300_6d3e9",
  "sm:placeholder-green-400": "_sm:placeholder-green-400_6d3e9",
  "sm:placeholder-green-500": "_sm:placeholder-green-500_6d3e9",
  "sm:placeholder-green-600": "_sm:placeholder-green-600_6d3e9",
  "sm:placeholder-green-700": "_sm:placeholder-green-700_6d3e9",
  "sm:placeholder-green-800": "_sm:placeholder-green-800_6d3e9",
  "sm:placeholder-green-900": "_sm:placeholder-green-900_6d3e9",
  "sm:placeholder-teal-100": "_sm:placeholder-teal-100_6d3e9",
  "sm:placeholder-teal-200": "_sm:placeholder-teal-200_6d3e9",
  "sm:placeholder-teal-300": "_sm:placeholder-teal-300_6d3e9",
  "sm:placeholder-teal-400": "_sm:placeholder-teal-400_6d3e9",
  "sm:placeholder-teal-500": "_sm:placeholder-teal-500_6d3e9",
  "sm:placeholder-teal-600": "_sm:placeholder-teal-600_6d3e9",
  "sm:placeholder-teal-700": "_sm:placeholder-teal-700_6d3e9",
  "sm:placeholder-teal-800": "_sm:placeholder-teal-800_6d3e9",
  "sm:placeholder-teal-900": "_sm:placeholder-teal-900_6d3e9",
  "sm:placeholder-blue-100": "_sm:placeholder-blue-100_6d3e9",
  "sm:placeholder-blue-200": "_sm:placeholder-blue-200_6d3e9",
  "sm:placeholder-blue-300": "_sm:placeholder-blue-300_6d3e9",
  "sm:placeholder-blue-400": "_sm:placeholder-blue-400_6d3e9",
  "sm:placeholder-blue-500": "_sm:placeholder-blue-500_6d3e9",
  "sm:placeholder-blue-600": "_sm:placeholder-blue-600_6d3e9",
  "sm:placeholder-blue-700": "_sm:placeholder-blue-700_6d3e9",
  "sm:placeholder-blue-800": "_sm:placeholder-blue-800_6d3e9",
  "sm:placeholder-blue-900": "_sm:placeholder-blue-900_6d3e9",
  "sm:placeholder-indigo-100": "_sm:placeholder-indigo-100_6d3e9",
  "sm:placeholder-indigo-200": "_sm:placeholder-indigo-200_6d3e9",
  "sm:placeholder-indigo-300": "_sm:placeholder-indigo-300_6d3e9",
  "sm:placeholder-indigo-400": "_sm:placeholder-indigo-400_6d3e9",
  "sm:placeholder-indigo-500": "_sm:placeholder-indigo-500_6d3e9",
  "sm:placeholder-indigo-600": "_sm:placeholder-indigo-600_6d3e9",
  "sm:placeholder-indigo-700": "_sm:placeholder-indigo-700_6d3e9",
  "sm:placeholder-indigo-800": "_sm:placeholder-indigo-800_6d3e9",
  "sm:placeholder-indigo-900": "_sm:placeholder-indigo-900_6d3e9",
  "sm:placeholder-purple-100": "_sm:placeholder-purple-100_6d3e9",
  "sm:placeholder-purple-200": "_sm:placeholder-purple-200_6d3e9",
  "sm:placeholder-purple-300": "_sm:placeholder-purple-300_6d3e9",
  "sm:placeholder-purple-400": "_sm:placeholder-purple-400_6d3e9",
  "sm:placeholder-purple-500": "_sm:placeholder-purple-500_6d3e9",
  "sm:placeholder-purple-600": "_sm:placeholder-purple-600_6d3e9",
  "sm:placeholder-purple-700": "_sm:placeholder-purple-700_6d3e9",
  "sm:placeholder-purple-800": "_sm:placeholder-purple-800_6d3e9",
  "sm:placeholder-purple-900": "_sm:placeholder-purple-900_6d3e9",
  "sm:placeholder-pink-100": "_sm:placeholder-pink-100_6d3e9",
  "sm:placeholder-pink-200": "_sm:placeholder-pink-200_6d3e9",
  "sm:placeholder-pink-300": "_sm:placeholder-pink-300_6d3e9",
  "sm:placeholder-pink-400": "_sm:placeholder-pink-400_6d3e9",
  "sm:placeholder-pink-500": "_sm:placeholder-pink-500_6d3e9",
  "sm:placeholder-pink-600": "_sm:placeholder-pink-600_6d3e9",
  "sm:placeholder-pink-700": "_sm:placeholder-pink-700_6d3e9",
  "sm:placeholder-pink-800": "_sm:placeholder-pink-800_6d3e9",
  "sm:placeholder-pink-900": "_sm:placeholder-pink-900_6d3e9",
  "sm:placeholder-primary-100": "_sm:placeholder-primary-100_6d3e9",
  "sm:placeholder-primary-200": "_sm:placeholder-primary-200_6d3e9",
  "sm:placeholder-primary-300": "_sm:placeholder-primary-300_6d3e9",
  "sm:placeholder-primary-400": "_sm:placeholder-primary-400_6d3e9",
  "sm:placeholder-primary-500": "_sm:placeholder-primary-500_6d3e9",
  "sm:placeholder-primary-600": "_sm:placeholder-primary-600_6d3e9",
  "sm:placeholder-primary-700": "_sm:placeholder-primary-700_6d3e9",
  "sm:placeholder-primary-800": "_sm:placeholder-primary-800_6d3e9",
  "sm:placeholder-primary-900": "_sm:placeholder-primary-900_6d3e9",
  "sm:placeholder-secondary-100": "_sm:placeholder-secondary-100_6d3e9",
  "sm:placeholder-secondary-200": "_sm:placeholder-secondary-200_6d3e9",
  "sm:placeholder-secondary-300": "_sm:placeholder-secondary-300_6d3e9",
  "sm:placeholder-secondary-400": "_sm:placeholder-secondary-400_6d3e9",
  "sm:placeholder-secondary-500": "_sm:placeholder-secondary-500_6d3e9",
  "sm:placeholder-secondary-600": "_sm:placeholder-secondary-600_6d3e9",
  "sm:placeholder-secondary-700": "_sm:placeholder-secondary-700_6d3e9",
  "sm:placeholder-secondary-800": "_sm:placeholder-secondary-800_6d3e9",
  "sm:placeholder-secondary-900": "_sm:placeholder-secondary-900_6d3e9",
  "sm:focus:placeholder-transparent": "_sm:focus:placeholder-transparent_6d3e9",
  "sm:focus:placeholder-black": "_sm:focus:placeholder-black_6d3e9",
  "sm:focus:placeholder-white": "_sm:focus:placeholder-white_6d3e9",
  "sm:focus:placeholder-gray-100": "_sm:focus:placeholder-gray-100_6d3e9",
  "sm:focus:placeholder-gray-200": "_sm:focus:placeholder-gray-200_6d3e9",
  "sm:focus:placeholder-gray-300": "_sm:focus:placeholder-gray-300_6d3e9",
  "sm:focus:placeholder-gray-400": "_sm:focus:placeholder-gray-400_6d3e9",
  "sm:focus:placeholder-gray-500": "_sm:focus:placeholder-gray-500_6d3e9",
  "sm:focus:placeholder-gray-600": "_sm:focus:placeholder-gray-600_6d3e9",
  "sm:focus:placeholder-gray-700": "_sm:focus:placeholder-gray-700_6d3e9",
  "sm:focus:placeholder-gray-800": "_sm:focus:placeholder-gray-800_6d3e9",
  "sm:focus:placeholder-gray-900": "_sm:focus:placeholder-gray-900_6d3e9",
  "sm:focus:placeholder-red-100": "_sm:focus:placeholder-red-100_6d3e9",
  "sm:focus:placeholder-red-200": "_sm:focus:placeholder-red-200_6d3e9",
  "sm:focus:placeholder-red-300": "_sm:focus:placeholder-red-300_6d3e9",
  "sm:focus:placeholder-red-400": "_sm:focus:placeholder-red-400_6d3e9",
  "sm:focus:placeholder-red-500": "_sm:focus:placeholder-red-500_6d3e9",
  "sm:focus:placeholder-red-600": "_sm:focus:placeholder-red-600_6d3e9",
  "sm:focus:placeholder-red-700": "_sm:focus:placeholder-red-700_6d3e9",
  "sm:focus:placeholder-red-800": "_sm:focus:placeholder-red-800_6d3e9",
  "sm:focus:placeholder-red-900": "_sm:focus:placeholder-red-900_6d3e9",
  "sm:focus:placeholder-orange-100": "_sm:focus:placeholder-orange-100_6d3e9",
  "sm:focus:placeholder-orange-200": "_sm:focus:placeholder-orange-200_6d3e9",
  "sm:focus:placeholder-orange-300": "_sm:focus:placeholder-orange-300_6d3e9",
  "sm:focus:placeholder-orange-400": "_sm:focus:placeholder-orange-400_6d3e9",
  "sm:focus:placeholder-orange-500": "_sm:focus:placeholder-orange-500_6d3e9",
  "sm:focus:placeholder-orange-600": "_sm:focus:placeholder-orange-600_6d3e9",
  "sm:focus:placeholder-orange-700": "_sm:focus:placeholder-orange-700_6d3e9",
  "sm:focus:placeholder-orange-800": "_sm:focus:placeholder-orange-800_6d3e9",
  "sm:focus:placeholder-orange-900": "_sm:focus:placeholder-orange-900_6d3e9",
  "sm:focus:placeholder-yellow-100": "_sm:focus:placeholder-yellow-100_6d3e9",
  "sm:focus:placeholder-yellow-200": "_sm:focus:placeholder-yellow-200_6d3e9",
  "sm:focus:placeholder-yellow-300": "_sm:focus:placeholder-yellow-300_6d3e9",
  "sm:focus:placeholder-yellow-400": "_sm:focus:placeholder-yellow-400_6d3e9",
  "sm:focus:placeholder-yellow-500": "_sm:focus:placeholder-yellow-500_6d3e9",
  "sm:focus:placeholder-yellow-600": "_sm:focus:placeholder-yellow-600_6d3e9",
  "sm:focus:placeholder-yellow-700": "_sm:focus:placeholder-yellow-700_6d3e9",
  "sm:focus:placeholder-yellow-800": "_sm:focus:placeholder-yellow-800_6d3e9",
  "sm:focus:placeholder-yellow-900": "_sm:focus:placeholder-yellow-900_6d3e9",
  "sm:focus:placeholder-green-100": "_sm:focus:placeholder-green-100_6d3e9",
  "sm:focus:placeholder-green-200": "_sm:focus:placeholder-green-200_6d3e9",
  "sm:focus:placeholder-green-300": "_sm:focus:placeholder-green-300_6d3e9",
  "sm:focus:placeholder-green-400": "_sm:focus:placeholder-green-400_6d3e9",
  "sm:focus:placeholder-green-500": "_sm:focus:placeholder-green-500_6d3e9",
  "sm:focus:placeholder-green-600": "_sm:focus:placeholder-green-600_6d3e9",
  "sm:focus:placeholder-green-700": "_sm:focus:placeholder-green-700_6d3e9",
  "sm:focus:placeholder-green-800": "_sm:focus:placeholder-green-800_6d3e9",
  "sm:focus:placeholder-green-900": "_sm:focus:placeholder-green-900_6d3e9",
  "sm:focus:placeholder-teal-100": "_sm:focus:placeholder-teal-100_6d3e9",
  "sm:focus:placeholder-teal-200": "_sm:focus:placeholder-teal-200_6d3e9",
  "sm:focus:placeholder-teal-300": "_sm:focus:placeholder-teal-300_6d3e9",
  "sm:focus:placeholder-teal-400": "_sm:focus:placeholder-teal-400_6d3e9",
  "sm:focus:placeholder-teal-500": "_sm:focus:placeholder-teal-500_6d3e9",
  "sm:focus:placeholder-teal-600": "_sm:focus:placeholder-teal-600_6d3e9",
  "sm:focus:placeholder-teal-700": "_sm:focus:placeholder-teal-700_6d3e9",
  "sm:focus:placeholder-teal-800": "_sm:focus:placeholder-teal-800_6d3e9",
  "sm:focus:placeholder-teal-900": "_sm:focus:placeholder-teal-900_6d3e9",
  "sm:focus:placeholder-blue-100": "_sm:focus:placeholder-blue-100_6d3e9",
  "sm:focus:placeholder-blue-200": "_sm:focus:placeholder-blue-200_6d3e9",
  "sm:focus:placeholder-blue-300": "_sm:focus:placeholder-blue-300_6d3e9",
  "sm:focus:placeholder-blue-400": "_sm:focus:placeholder-blue-400_6d3e9",
  "sm:focus:placeholder-blue-500": "_sm:focus:placeholder-blue-500_6d3e9",
  "sm:focus:placeholder-blue-600": "_sm:focus:placeholder-blue-600_6d3e9",
  "sm:focus:placeholder-blue-700": "_sm:focus:placeholder-blue-700_6d3e9",
  "sm:focus:placeholder-blue-800": "_sm:focus:placeholder-blue-800_6d3e9",
  "sm:focus:placeholder-blue-900": "_sm:focus:placeholder-blue-900_6d3e9",
  "sm:focus:placeholder-indigo-100": "_sm:focus:placeholder-indigo-100_6d3e9",
  "sm:focus:placeholder-indigo-200": "_sm:focus:placeholder-indigo-200_6d3e9",
  "sm:focus:placeholder-indigo-300": "_sm:focus:placeholder-indigo-300_6d3e9",
  "sm:focus:placeholder-indigo-400": "_sm:focus:placeholder-indigo-400_6d3e9",
  "sm:focus:placeholder-indigo-500": "_sm:focus:placeholder-indigo-500_6d3e9",
  "sm:focus:placeholder-indigo-600": "_sm:focus:placeholder-indigo-600_6d3e9",
  "sm:focus:placeholder-indigo-700": "_sm:focus:placeholder-indigo-700_6d3e9",
  "sm:focus:placeholder-indigo-800": "_sm:focus:placeholder-indigo-800_6d3e9",
  "sm:focus:placeholder-indigo-900": "_sm:focus:placeholder-indigo-900_6d3e9",
  "sm:focus:placeholder-purple-100": "_sm:focus:placeholder-purple-100_6d3e9",
  "sm:focus:placeholder-purple-200": "_sm:focus:placeholder-purple-200_6d3e9",
  "sm:focus:placeholder-purple-300": "_sm:focus:placeholder-purple-300_6d3e9",
  "sm:focus:placeholder-purple-400": "_sm:focus:placeholder-purple-400_6d3e9",
  "sm:focus:placeholder-purple-500": "_sm:focus:placeholder-purple-500_6d3e9",
  "sm:focus:placeholder-purple-600": "_sm:focus:placeholder-purple-600_6d3e9",
  "sm:focus:placeholder-purple-700": "_sm:focus:placeholder-purple-700_6d3e9",
  "sm:focus:placeholder-purple-800": "_sm:focus:placeholder-purple-800_6d3e9",
  "sm:focus:placeholder-purple-900": "_sm:focus:placeholder-purple-900_6d3e9",
  "sm:focus:placeholder-pink-100": "_sm:focus:placeholder-pink-100_6d3e9",
  "sm:focus:placeholder-pink-200": "_sm:focus:placeholder-pink-200_6d3e9",
  "sm:focus:placeholder-pink-300": "_sm:focus:placeholder-pink-300_6d3e9",
  "sm:focus:placeholder-pink-400": "_sm:focus:placeholder-pink-400_6d3e9",
  "sm:focus:placeholder-pink-500": "_sm:focus:placeholder-pink-500_6d3e9",
  "sm:focus:placeholder-pink-600": "_sm:focus:placeholder-pink-600_6d3e9",
  "sm:focus:placeholder-pink-700": "_sm:focus:placeholder-pink-700_6d3e9",
  "sm:focus:placeholder-pink-800": "_sm:focus:placeholder-pink-800_6d3e9",
  "sm:focus:placeholder-pink-900": "_sm:focus:placeholder-pink-900_6d3e9",
  "sm:focus:placeholder-primary-100": "_sm:focus:placeholder-primary-100_6d3e9",
  "sm:focus:placeholder-primary-200": "_sm:focus:placeholder-primary-200_6d3e9",
  "sm:focus:placeholder-primary-300": "_sm:focus:placeholder-primary-300_6d3e9",
  "sm:focus:placeholder-primary-400": "_sm:focus:placeholder-primary-400_6d3e9",
  "sm:focus:placeholder-primary-500": "_sm:focus:placeholder-primary-500_6d3e9",
  "sm:focus:placeholder-primary-600": "_sm:focus:placeholder-primary-600_6d3e9",
  "sm:focus:placeholder-primary-700": "_sm:focus:placeholder-primary-700_6d3e9",
  "sm:focus:placeholder-primary-800": "_sm:focus:placeholder-primary-800_6d3e9",
  "sm:focus:placeholder-primary-900": "_sm:focus:placeholder-primary-900_6d3e9",
  "sm:focus:placeholder-secondary-100": "_sm:focus:placeholder-secondary-100_6d3e9",
  "sm:focus:placeholder-secondary-200": "_sm:focus:placeholder-secondary-200_6d3e9",
  "sm:focus:placeholder-secondary-300": "_sm:focus:placeholder-secondary-300_6d3e9",
  "sm:focus:placeholder-secondary-400": "_sm:focus:placeholder-secondary-400_6d3e9",
  "sm:focus:placeholder-secondary-500": "_sm:focus:placeholder-secondary-500_6d3e9",
  "sm:focus:placeholder-secondary-600": "_sm:focus:placeholder-secondary-600_6d3e9",
  "sm:focus:placeholder-secondary-700": "_sm:focus:placeholder-secondary-700_6d3e9",
  "sm:focus:placeholder-secondary-800": "_sm:focus:placeholder-secondary-800_6d3e9",
  "sm:focus:placeholder-secondary-900": "_sm:focus:placeholder-secondary-900_6d3e9",
  "sm:pointer-events-none": "_sm:pointer-events-none_6d3e9",
  "sm:pointer-events-auto": "_sm:pointer-events-auto_6d3e9",
  "sm:static": "_sm:static_6d3e9",
  "sm:fixed": "_sm:fixed_6d3e9",
  "sm:absolute": "_sm:absolute_6d3e9",
  "sm:relative": "_sm:relative_6d3e9",
  "sm:sticky": "_sm:sticky_6d3e9",
  "sm:inset-0": "_sm:inset-0_6d3e9",
  "sm:inset-auto": "_sm:inset-auto_6d3e9",
  "sm:inset-y-0": "_sm:inset-y-0_6d3e9",
  "sm:inset-x-0": "_sm:inset-x-0_6d3e9",
  "sm:inset-y-auto": "_sm:inset-y-auto_6d3e9",
  "sm:inset-x-auto": "_sm:inset-x-auto_6d3e9",
  "sm:top-0": "_sm:top-0_6d3e9",
  "sm:right-0": "_sm:right-0_6d3e9",
  "sm:bottom-0": "_sm:bottom-0_6d3e9",
  "sm:left-0": "_sm:left-0_6d3e9",
  "sm:top-auto": "_sm:top-auto_6d3e9",
  "sm:right-auto": "_sm:right-auto_6d3e9",
  "sm:bottom-auto": "_sm:bottom-auto_6d3e9",
  "sm:left-auto": "_sm:left-auto_6d3e9",
  "sm:resize-none": "_sm:resize-none_6d3e9",
  "sm:resize-y": "_sm:resize-y_6d3e9",
  "sm:resize-x": "_sm:resize-x_6d3e9",
  "sm:resize": "_sm:resize_6d3e9",
  "sm:shadow": "_sm:shadow_6d3e9",
  "sm:shadow-md": "_sm:shadow-md_6d3e9",
  "sm:shadow-lg": "_sm:shadow-lg_6d3e9",
  "sm:shadow-xl": "_sm:shadow-xl_6d3e9",
  "sm:shadow-2xl": "_sm:shadow-2xl_6d3e9",
  "sm:shadow-inner": "_sm:shadow-inner_6d3e9",
  "sm:shadow-outline": "_sm:shadow-outline_6d3e9",
  "sm:shadow-none": "_sm:shadow-none_6d3e9",
  "sm:hover:shadow": "_sm:hover:shadow_6d3e9",
  "sm:hover:shadow-md": "_sm:hover:shadow-md_6d3e9",
  "sm:hover:shadow-lg": "_sm:hover:shadow-lg_6d3e9",
  "sm:hover:shadow-xl": "_sm:hover:shadow-xl_6d3e9",
  "sm:hover:shadow-2xl": "_sm:hover:shadow-2xl_6d3e9",
  "sm:hover:shadow-inner": "_sm:hover:shadow-inner_6d3e9",
  "sm:hover:shadow-outline": "_sm:hover:shadow-outline_6d3e9",
  "sm:hover:shadow-none": "_sm:hover:shadow-none_6d3e9",
  "sm:focus:shadow": "_sm:focus:shadow_6d3e9",
  "sm:focus:shadow-md": "_sm:focus:shadow-md_6d3e9",
  "sm:focus:shadow-lg": "_sm:focus:shadow-lg_6d3e9",
  "sm:focus:shadow-xl": "_sm:focus:shadow-xl_6d3e9",
  "sm:focus:shadow-2xl": "_sm:focus:shadow-2xl_6d3e9",
  "sm:focus:shadow-inner": "_sm:focus:shadow-inner_6d3e9",
  "sm:focus:shadow-outline": "_sm:focus:shadow-outline_6d3e9",
  "sm:focus:shadow-none": "_sm:focus:shadow-none_6d3e9",
  "sm:fill-current": "_sm:fill-current_6d3e9",
  "sm:stroke-current": "_sm:stroke-current_6d3e9",
  "sm:table-auto": "_sm:table-auto_6d3e9",
  "sm:table-fixed": "_sm:table-fixed_6d3e9",
  "sm:text-left": "_sm:text-left_6d3e9",
  "sm:text-center": "_sm:text-center_6d3e9",
  "sm:text-right": "_sm:text-right_6d3e9",
  "sm:text-justify": "_sm:text-justify_6d3e9",
  "sm:text-transparent": "_sm:text-transparent_6d3e9",
  "sm:text-black": "_sm:text-black_6d3e9",
  "sm:text-white": "_sm:text-white_6d3e9",
  "sm:text-gray-100": "_sm:text-gray-100_6d3e9",
  "sm:text-gray-200": "_sm:text-gray-200_6d3e9",
  "sm:text-gray-300": "_sm:text-gray-300_6d3e9",
  "sm:text-gray-400": "_sm:text-gray-400_6d3e9",
  "sm:text-gray-500": "_sm:text-gray-500_6d3e9",
  "sm:text-gray-600": "_sm:text-gray-600_6d3e9",
  "sm:text-gray-700": "_sm:text-gray-700_6d3e9",
  "sm:text-gray-800": "_sm:text-gray-800_6d3e9",
  "sm:text-gray-900": "_sm:text-gray-900_6d3e9",
  "sm:text-red-100": "_sm:text-red-100_6d3e9",
  "sm:text-red-200": "_sm:text-red-200_6d3e9",
  "sm:text-red-300": "_sm:text-red-300_6d3e9",
  "sm:text-red-400": "_sm:text-red-400_6d3e9",
  "sm:text-red-500": "_sm:text-red-500_6d3e9",
  "sm:text-red-600": "_sm:text-red-600_6d3e9",
  "sm:text-red-700": "_sm:text-red-700_6d3e9",
  "sm:text-red-800": "_sm:text-red-800_6d3e9",
  "sm:text-red-900": "_sm:text-red-900_6d3e9",
  "sm:text-orange-100": "_sm:text-orange-100_6d3e9",
  "sm:text-orange-200": "_sm:text-orange-200_6d3e9",
  "sm:text-orange-300": "_sm:text-orange-300_6d3e9",
  "sm:text-orange-400": "_sm:text-orange-400_6d3e9",
  "sm:text-orange-500": "_sm:text-orange-500_6d3e9",
  "sm:text-orange-600": "_sm:text-orange-600_6d3e9",
  "sm:text-orange-700": "_sm:text-orange-700_6d3e9",
  "sm:text-orange-800": "_sm:text-orange-800_6d3e9",
  "sm:text-orange-900": "_sm:text-orange-900_6d3e9",
  "sm:text-yellow-100": "_sm:text-yellow-100_6d3e9",
  "sm:text-yellow-200": "_sm:text-yellow-200_6d3e9",
  "sm:text-yellow-300": "_sm:text-yellow-300_6d3e9",
  "sm:text-yellow-400": "_sm:text-yellow-400_6d3e9",
  "sm:text-yellow-500": "_sm:text-yellow-500_6d3e9",
  "sm:text-yellow-600": "_sm:text-yellow-600_6d3e9",
  "sm:text-yellow-700": "_sm:text-yellow-700_6d3e9",
  "sm:text-yellow-800": "_sm:text-yellow-800_6d3e9",
  "sm:text-yellow-900": "_sm:text-yellow-900_6d3e9",
  "sm:text-green-100": "_sm:text-green-100_6d3e9",
  "sm:text-green-200": "_sm:text-green-200_6d3e9",
  "sm:text-green-300": "_sm:text-green-300_6d3e9",
  "sm:text-green-400": "_sm:text-green-400_6d3e9",
  "sm:text-green-500": "_sm:text-green-500_6d3e9",
  "sm:text-green-600": "_sm:text-green-600_6d3e9",
  "sm:text-green-700": "_sm:text-green-700_6d3e9",
  "sm:text-green-800": "_sm:text-green-800_6d3e9",
  "sm:text-green-900": "_sm:text-green-900_6d3e9",
  "sm:text-teal-100": "_sm:text-teal-100_6d3e9",
  "sm:text-teal-200": "_sm:text-teal-200_6d3e9",
  "sm:text-teal-300": "_sm:text-teal-300_6d3e9",
  "sm:text-teal-400": "_sm:text-teal-400_6d3e9",
  "sm:text-teal-500": "_sm:text-teal-500_6d3e9",
  "sm:text-teal-600": "_sm:text-teal-600_6d3e9",
  "sm:text-teal-700": "_sm:text-teal-700_6d3e9",
  "sm:text-teal-800": "_sm:text-teal-800_6d3e9",
  "sm:text-teal-900": "_sm:text-teal-900_6d3e9",
  "sm:text-blue-100": "_sm:text-blue-100_6d3e9",
  "sm:text-blue-200": "_sm:text-blue-200_6d3e9",
  "sm:text-blue-300": "_sm:text-blue-300_6d3e9",
  "sm:text-blue-400": "_sm:text-blue-400_6d3e9",
  "sm:text-blue-500": "_sm:text-blue-500_6d3e9",
  "sm:text-blue-600": "_sm:text-blue-600_6d3e9",
  "sm:text-blue-700": "_sm:text-blue-700_6d3e9",
  "sm:text-blue-800": "_sm:text-blue-800_6d3e9",
  "sm:text-blue-900": "_sm:text-blue-900_6d3e9",
  "sm:text-indigo-100": "_sm:text-indigo-100_6d3e9",
  "sm:text-indigo-200": "_sm:text-indigo-200_6d3e9",
  "sm:text-indigo-300": "_sm:text-indigo-300_6d3e9",
  "sm:text-indigo-400": "_sm:text-indigo-400_6d3e9",
  "sm:text-indigo-500": "_sm:text-indigo-500_6d3e9",
  "sm:text-indigo-600": "_sm:text-indigo-600_6d3e9",
  "sm:text-indigo-700": "_sm:text-indigo-700_6d3e9",
  "sm:text-indigo-800": "_sm:text-indigo-800_6d3e9",
  "sm:text-indigo-900": "_sm:text-indigo-900_6d3e9",
  "sm:text-purple-100": "_sm:text-purple-100_6d3e9",
  "sm:text-purple-200": "_sm:text-purple-200_6d3e9",
  "sm:text-purple-300": "_sm:text-purple-300_6d3e9",
  "sm:text-purple-400": "_sm:text-purple-400_6d3e9",
  "sm:text-purple-500": "_sm:text-purple-500_6d3e9",
  "sm:text-purple-600": "_sm:text-purple-600_6d3e9",
  "sm:text-purple-700": "_sm:text-purple-700_6d3e9",
  "sm:text-purple-800": "_sm:text-purple-800_6d3e9",
  "sm:text-purple-900": "_sm:text-purple-900_6d3e9",
  "sm:text-pink-100": "_sm:text-pink-100_6d3e9",
  "sm:text-pink-200": "_sm:text-pink-200_6d3e9",
  "sm:text-pink-300": "_sm:text-pink-300_6d3e9",
  "sm:text-pink-400": "_sm:text-pink-400_6d3e9",
  "sm:text-pink-500": "_sm:text-pink-500_6d3e9",
  "sm:text-pink-600": "_sm:text-pink-600_6d3e9",
  "sm:text-pink-700": "_sm:text-pink-700_6d3e9",
  "sm:text-pink-800": "_sm:text-pink-800_6d3e9",
  "sm:text-pink-900": "_sm:text-pink-900_6d3e9",
  "sm:text-primary-100": "_sm:text-primary-100_6d3e9",
  "sm:text-primary-200": "_sm:text-primary-200_6d3e9",
  "sm:text-primary-300": "_sm:text-primary-300_6d3e9",
  "sm:text-primary-400": "_sm:text-primary-400_6d3e9",
  "sm:text-primary-500": "_sm:text-primary-500_6d3e9",
  "sm:text-primary-600": "_sm:text-primary-600_6d3e9",
  "sm:text-primary-700": "_sm:text-primary-700_6d3e9",
  "sm:text-primary-800": "_sm:text-primary-800_6d3e9",
  "sm:text-primary-900": "_sm:text-primary-900_6d3e9",
  "sm:text-secondary-100": "_sm:text-secondary-100_6d3e9",
  "sm:text-secondary-200": "_sm:text-secondary-200_6d3e9",
  "sm:text-secondary-300": "_sm:text-secondary-300_6d3e9",
  "sm:text-secondary-400": "_sm:text-secondary-400_6d3e9",
  "sm:text-secondary-500": "_sm:text-secondary-500_6d3e9",
  "sm:text-secondary-600": "_sm:text-secondary-600_6d3e9",
  "sm:text-secondary-700": "_sm:text-secondary-700_6d3e9",
  "sm:text-secondary-800": "_sm:text-secondary-800_6d3e9",
  "sm:text-secondary-900": "_sm:text-secondary-900_6d3e9",
  "sm:hover:text-transparent": "_sm:hover:text-transparent_6d3e9",
  "sm:hover:text-black": "_sm:hover:text-black_6d3e9",
  "sm:hover:text-white": "_sm:hover:text-white_6d3e9",
  "sm:hover:text-gray-100": "_sm:hover:text-gray-100_6d3e9",
  "sm:hover:text-gray-200": "_sm:hover:text-gray-200_6d3e9",
  "sm:hover:text-gray-300": "_sm:hover:text-gray-300_6d3e9",
  "sm:hover:text-gray-400": "_sm:hover:text-gray-400_6d3e9",
  "sm:hover:text-gray-500": "_sm:hover:text-gray-500_6d3e9",
  "sm:hover:text-gray-600": "_sm:hover:text-gray-600_6d3e9",
  "sm:hover:text-gray-700": "_sm:hover:text-gray-700_6d3e9",
  "sm:hover:text-gray-800": "_sm:hover:text-gray-800_6d3e9",
  "sm:hover:text-gray-900": "_sm:hover:text-gray-900_6d3e9",
  "sm:hover:text-red-100": "_sm:hover:text-red-100_6d3e9",
  "sm:hover:text-red-200": "_sm:hover:text-red-200_6d3e9",
  "sm:hover:text-red-300": "_sm:hover:text-red-300_6d3e9",
  "sm:hover:text-red-400": "_sm:hover:text-red-400_6d3e9",
  "sm:hover:text-red-500": "_sm:hover:text-red-500_6d3e9",
  "sm:hover:text-red-600": "_sm:hover:text-red-600_6d3e9",
  "sm:hover:text-red-700": "_sm:hover:text-red-700_6d3e9",
  "sm:hover:text-red-800": "_sm:hover:text-red-800_6d3e9",
  "sm:hover:text-red-900": "_sm:hover:text-red-900_6d3e9",
  "sm:hover:text-orange-100": "_sm:hover:text-orange-100_6d3e9",
  "sm:hover:text-orange-200": "_sm:hover:text-orange-200_6d3e9",
  "sm:hover:text-orange-300": "_sm:hover:text-orange-300_6d3e9",
  "sm:hover:text-orange-400": "_sm:hover:text-orange-400_6d3e9",
  "sm:hover:text-orange-500": "_sm:hover:text-orange-500_6d3e9",
  "sm:hover:text-orange-600": "_sm:hover:text-orange-600_6d3e9",
  "sm:hover:text-orange-700": "_sm:hover:text-orange-700_6d3e9",
  "sm:hover:text-orange-800": "_sm:hover:text-orange-800_6d3e9",
  "sm:hover:text-orange-900": "_sm:hover:text-orange-900_6d3e9",
  "sm:hover:text-yellow-100": "_sm:hover:text-yellow-100_6d3e9",
  "sm:hover:text-yellow-200": "_sm:hover:text-yellow-200_6d3e9",
  "sm:hover:text-yellow-300": "_sm:hover:text-yellow-300_6d3e9",
  "sm:hover:text-yellow-400": "_sm:hover:text-yellow-400_6d3e9",
  "sm:hover:text-yellow-500": "_sm:hover:text-yellow-500_6d3e9",
  "sm:hover:text-yellow-600": "_sm:hover:text-yellow-600_6d3e9",
  "sm:hover:text-yellow-700": "_sm:hover:text-yellow-700_6d3e9",
  "sm:hover:text-yellow-800": "_sm:hover:text-yellow-800_6d3e9",
  "sm:hover:text-yellow-900": "_sm:hover:text-yellow-900_6d3e9",
  "sm:hover:text-green-100": "_sm:hover:text-green-100_6d3e9",
  "sm:hover:text-green-200": "_sm:hover:text-green-200_6d3e9",
  "sm:hover:text-green-300": "_sm:hover:text-green-300_6d3e9",
  "sm:hover:text-green-400": "_sm:hover:text-green-400_6d3e9",
  "sm:hover:text-green-500": "_sm:hover:text-green-500_6d3e9",
  "sm:hover:text-green-600": "_sm:hover:text-green-600_6d3e9",
  "sm:hover:text-green-700": "_sm:hover:text-green-700_6d3e9",
  "sm:hover:text-green-800": "_sm:hover:text-green-800_6d3e9",
  "sm:hover:text-green-900": "_sm:hover:text-green-900_6d3e9",
  "sm:hover:text-teal-100": "_sm:hover:text-teal-100_6d3e9",
  "sm:hover:text-teal-200": "_sm:hover:text-teal-200_6d3e9",
  "sm:hover:text-teal-300": "_sm:hover:text-teal-300_6d3e9",
  "sm:hover:text-teal-400": "_sm:hover:text-teal-400_6d3e9",
  "sm:hover:text-teal-500": "_sm:hover:text-teal-500_6d3e9",
  "sm:hover:text-teal-600": "_sm:hover:text-teal-600_6d3e9",
  "sm:hover:text-teal-700": "_sm:hover:text-teal-700_6d3e9",
  "sm:hover:text-teal-800": "_sm:hover:text-teal-800_6d3e9",
  "sm:hover:text-teal-900": "_sm:hover:text-teal-900_6d3e9",
  "sm:hover:text-blue-100": "_sm:hover:text-blue-100_6d3e9",
  "sm:hover:text-blue-200": "_sm:hover:text-blue-200_6d3e9",
  "sm:hover:text-blue-300": "_sm:hover:text-blue-300_6d3e9",
  "sm:hover:text-blue-400": "_sm:hover:text-blue-400_6d3e9",
  "sm:hover:text-blue-500": "_sm:hover:text-blue-500_6d3e9",
  "sm:hover:text-blue-600": "_sm:hover:text-blue-600_6d3e9",
  "sm:hover:text-blue-700": "_sm:hover:text-blue-700_6d3e9",
  "sm:hover:text-blue-800": "_sm:hover:text-blue-800_6d3e9",
  "sm:hover:text-blue-900": "_sm:hover:text-blue-900_6d3e9",
  "sm:hover:text-indigo-100": "_sm:hover:text-indigo-100_6d3e9",
  "sm:hover:text-indigo-200": "_sm:hover:text-indigo-200_6d3e9",
  "sm:hover:text-indigo-300": "_sm:hover:text-indigo-300_6d3e9",
  "sm:hover:text-indigo-400": "_sm:hover:text-indigo-400_6d3e9",
  "sm:hover:text-indigo-500": "_sm:hover:text-indigo-500_6d3e9",
  "sm:hover:text-indigo-600": "_sm:hover:text-indigo-600_6d3e9",
  "sm:hover:text-indigo-700": "_sm:hover:text-indigo-700_6d3e9",
  "sm:hover:text-indigo-800": "_sm:hover:text-indigo-800_6d3e9",
  "sm:hover:text-indigo-900": "_sm:hover:text-indigo-900_6d3e9",
  "sm:hover:text-purple-100": "_sm:hover:text-purple-100_6d3e9",
  "sm:hover:text-purple-200": "_sm:hover:text-purple-200_6d3e9",
  "sm:hover:text-purple-300": "_sm:hover:text-purple-300_6d3e9",
  "sm:hover:text-purple-400": "_sm:hover:text-purple-400_6d3e9",
  "sm:hover:text-purple-500": "_sm:hover:text-purple-500_6d3e9",
  "sm:hover:text-purple-600": "_sm:hover:text-purple-600_6d3e9",
  "sm:hover:text-purple-700": "_sm:hover:text-purple-700_6d3e9",
  "sm:hover:text-purple-800": "_sm:hover:text-purple-800_6d3e9",
  "sm:hover:text-purple-900": "_sm:hover:text-purple-900_6d3e9",
  "sm:hover:text-pink-100": "_sm:hover:text-pink-100_6d3e9",
  "sm:hover:text-pink-200": "_sm:hover:text-pink-200_6d3e9",
  "sm:hover:text-pink-300": "_sm:hover:text-pink-300_6d3e9",
  "sm:hover:text-pink-400": "_sm:hover:text-pink-400_6d3e9",
  "sm:hover:text-pink-500": "_sm:hover:text-pink-500_6d3e9",
  "sm:hover:text-pink-600": "_sm:hover:text-pink-600_6d3e9",
  "sm:hover:text-pink-700": "_sm:hover:text-pink-700_6d3e9",
  "sm:hover:text-pink-800": "_sm:hover:text-pink-800_6d3e9",
  "sm:hover:text-pink-900": "_sm:hover:text-pink-900_6d3e9",
  "sm:hover:text-primary-100": "_sm:hover:text-primary-100_6d3e9",
  "sm:hover:text-primary-200": "_sm:hover:text-primary-200_6d3e9",
  "sm:hover:text-primary-300": "_sm:hover:text-primary-300_6d3e9",
  "sm:hover:text-primary-400": "_sm:hover:text-primary-400_6d3e9",
  "sm:hover:text-primary-500": "_sm:hover:text-primary-500_6d3e9",
  "sm:hover:text-primary-600": "_sm:hover:text-primary-600_6d3e9",
  "sm:hover:text-primary-700": "_sm:hover:text-primary-700_6d3e9",
  "sm:hover:text-primary-800": "_sm:hover:text-primary-800_6d3e9",
  "sm:hover:text-primary-900": "_sm:hover:text-primary-900_6d3e9",
  "sm:hover:text-secondary-100": "_sm:hover:text-secondary-100_6d3e9",
  "sm:hover:text-secondary-200": "_sm:hover:text-secondary-200_6d3e9",
  "sm:hover:text-secondary-300": "_sm:hover:text-secondary-300_6d3e9",
  "sm:hover:text-secondary-400": "_sm:hover:text-secondary-400_6d3e9",
  "sm:hover:text-secondary-500": "_sm:hover:text-secondary-500_6d3e9",
  "sm:hover:text-secondary-600": "_sm:hover:text-secondary-600_6d3e9",
  "sm:hover:text-secondary-700": "_sm:hover:text-secondary-700_6d3e9",
  "sm:hover:text-secondary-800": "_sm:hover:text-secondary-800_6d3e9",
  "sm:hover:text-secondary-900": "_sm:hover:text-secondary-900_6d3e9",
  "sm:focus:text-transparent": "_sm:focus:text-transparent_6d3e9",
  "sm:focus:text-black": "_sm:focus:text-black_6d3e9",
  "sm:focus:text-white": "_sm:focus:text-white_6d3e9",
  "sm:focus:text-gray-100": "_sm:focus:text-gray-100_6d3e9",
  "sm:focus:text-gray-200": "_sm:focus:text-gray-200_6d3e9",
  "sm:focus:text-gray-300": "_sm:focus:text-gray-300_6d3e9",
  "sm:focus:text-gray-400": "_sm:focus:text-gray-400_6d3e9",
  "sm:focus:text-gray-500": "_sm:focus:text-gray-500_6d3e9",
  "sm:focus:text-gray-600": "_sm:focus:text-gray-600_6d3e9",
  "sm:focus:text-gray-700": "_sm:focus:text-gray-700_6d3e9",
  "sm:focus:text-gray-800": "_sm:focus:text-gray-800_6d3e9",
  "sm:focus:text-gray-900": "_sm:focus:text-gray-900_6d3e9",
  "sm:focus:text-red-100": "_sm:focus:text-red-100_6d3e9",
  "sm:focus:text-red-200": "_sm:focus:text-red-200_6d3e9",
  "sm:focus:text-red-300": "_sm:focus:text-red-300_6d3e9",
  "sm:focus:text-red-400": "_sm:focus:text-red-400_6d3e9",
  "sm:focus:text-red-500": "_sm:focus:text-red-500_6d3e9",
  "sm:focus:text-red-600": "_sm:focus:text-red-600_6d3e9",
  "sm:focus:text-red-700": "_sm:focus:text-red-700_6d3e9",
  "sm:focus:text-red-800": "_sm:focus:text-red-800_6d3e9",
  "sm:focus:text-red-900": "_sm:focus:text-red-900_6d3e9",
  "sm:focus:text-orange-100": "_sm:focus:text-orange-100_6d3e9",
  "sm:focus:text-orange-200": "_sm:focus:text-orange-200_6d3e9",
  "sm:focus:text-orange-300": "_sm:focus:text-orange-300_6d3e9",
  "sm:focus:text-orange-400": "_sm:focus:text-orange-400_6d3e9",
  "sm:focus:text-orange-500": "_sm:focus:text-orange-500_6d3e9",
  "sm:focus:text-orange-600": "_sm:focus:text-orange-600_6d3e9",
  "sm:focus:text-orange-700": "_sm:focus:text-orange-700_6d3e9",
  "sm:focus:text-orange-800": "_sm:focus:text-orange-800_6d3e9",
  "sm:focus:text-orange-900": "_sm:focus:text-orange-900_6d3e9",
  "sm:focus:text-yellow-100": "_sm:focus:text-yellow-100_6d3e9",
  "sm:focus:text-yellow-200": "_sm:focus:text-yellow-200_6d3e9",
  "sm:focus:text-yellow-300": "_sm:focus:text-yellow-300_6d3e9",
  "sm:focus:text-yellow-400": "_sm:focus:text-yellow-400_6d3e9",
  "sm:focus:text-yellow-500": "_sm:focus:text-yellow-500_6d3e9",
  "sm:focus:text-yellow-600": "_sm:focus:text-yellow-600_6d3e9",
  "sm:focus:text-yellow-700": "_sm:focus:text-yellow-700_6d3e9",
  "sm:focus:text-yellow-800": "_sm:focus:text-yellow-800_6d3e9",
  "sm:focus:text-yellow-900": "_sm:focus:text-yellow-900_6d3e9",
  "sm:focus:text-green-100": "_sm:focus:text-green-100_6d3e9",
  "sm:focus:text-green-200": "_sm:focus:text-green-200_6d3e9",
  "sm:focus:text-green-300": "_sm:focus:text-green-300_6d3e9",
  "sm:focus:text-green-400": "_sm:focus:text-green-400_6d3e9",
  "sm:focus:text-green-500": "_sm:focus:text-green-500_6d3e9",
  "sm:focus:text-green-600": "_sm:focus:text-green-600_6d3e9",
  "sm:focus:text-green-700": "_sm:focus:text-green-700_6d3e9",
  "sm:focus:text-green-800": "_sm:focus:text-green-800_6d3e9",
  "sm:focus:text-green-900": "_sm:focus:text-green-900_6d3e9",
  "sm:focus:text-teal-100": "_sm:focus:text-teal-100_6d3e9",
  "sm:focus:text-teal-200": "_sm:focus:text-teal-200_6d3e9",
  "sm:focus:text-teal-300": "_sm:focus:text-teal-300_6d3e9",
  "sm:focus:text-teal-400": "_sm:focus:text-teal-400_6d3e9",
  "sm:focus:text-teal-500": "_sm:focus:text-teal-500_6d3e9",
  "sm:focus:text-teal-600": "_sm:focus:text-teal-600_6d3e9",
  "sm:focus:text-teal-700": "_sm:focus:text-teal-700_6d3e9",
  "sm:focus:text-teal-800": "_sm:focus:text-teal-800_6d3e9",
  "sm:focus:text-teal-900": "_sm:focus:text-teal-900_6d3e9",
  "sm:focus:text-blue-100": "_sm:focus:text-blue-100_6d3e9",
  "sm:focus:text-blue-200": "_sm:focus:text-blue-200_6d3e9",
  "sm:focus:text-blue-300": "_sm:focus:text-blue-300_6d3e9",
  "sm:focus:text-blue-400": "_sm:focus:text-blue-400_6d3e9",
  "sm:focus:text-blue-500": "_sm:focus:text-blue-500_6d3e9",
  "sm:focus:text-blue-600": "_sm:focus:text-blue-600_6d3e9",
  "sm:focus:text-blue-700": "_sm:focus:text-blue-700_6d3e9",
  "sm:focus:text-blue-800": "_sm:focus:text-blue-800_6d3e9",
  "sm:focus:text-blue-900": "_sm:focus:text-blue-900_6d3e9",
  "sm:focus:text-indigo-100": "_sm:focus:text-indigo-100_6d3e9",
  "sm:focus:text-indigo-200": "_sm:focus:text-indigo-200_6d3e9",
  "sm:focus:text-indigo-300": "_sm:focus:text-indigo-300_6d3e9",
  "sm:focus:text-indigo-400": "_sm:focus:text-indigo-400_6d3e9",
  "sm:focus:text-indigo-500": "_sm:focus:text-indigo-500_6d3e9",
  "sm:focus:text-indigo-600": "_sm:focus:text-indigo-600_6d3e9",
  "sm:focus:text-indigo-700": "_sm:focus:text-indigo-700_6d3e9",
  "sm:focus:text-indigo-800": "_sm:focus:text-indigo-800_6d3e9",
  "sm:focus:text-indigo-900": "_sm:focus:text-indigo-900_6d3e9",
  "sm:focus:text-purple-100": "_sm:focus:text-purple-100_6d3e9",
  "sm:focus:text-purple-200": "_sm:focus:text-purple-200_6d3e9",
  "sm:focus:text-purple-300": "_sm:focus:text-purple-300_6d3e9",
  "sm:focus:text-purple-400": "_sm:focus:text-purple-400_6d3e9",
  "sm:focus:text-purple-500": "_sm:focus:text-purple-500_6d3e9",
  "sm:focus:text-purple-600": "_sm:focus:text-purple-600_6d3e9",
  "sm:focus:text-purple-700": "_sm:focus:text-purple-700_6d3e9",
  "sm:focus:text-purple-800": "_sm:focus:text-purple-800_6d3e9",
  "sm:focus:text-purple-900": "_sm:focus:text-purple-900_6d3e9",
  "sm:focus:text-pink-100": "_sm:focus:text-pink-100_6d3e9",
  "sm:focus:text-pink-200": "_sm:focus:text-pink-200_6d3e9",
  "sm:focus:text-pink-300": "_sm:focus:text-pink-300_6d3e9",
  "sm:focus:text-pink-400": "_sm:focus:text-pink-400_6d3e9",
  "sm:focus:text-pink-500": "_sm:focus:text-pink-500_6d3e9",
  "sm:focus:text-pink-600": "_sm:focus:text-pink-600_6d3e9",
  "sm:focus:text-pink-700": "_sm:focus:text-pink-700_6d3e9",
  "sm:focus:text-pink-800": "_sm:focus:text-pink-800_6d3e9",
  "sm:focus:text-pink-900": "_sm:focus:text-pink-900_6d3e9",
  "sm:focus:text-primary-100": "_sm:focus:text-primary-100_6d3e9",
  "sm:focus:text-primary-200": "_sm:focus:text-primary-200_6d3e9",
  "sm:focus:text-primary-300": "_sm:focus:text-primary-300_6d3e9",
  "sm:focus:text-primary-400": "_sm:focus:text-primary-400_6d3e9",
  "sm:focus:text-primary-500": "_sm:focus:text-primary-500_6d3e9",
  "sm:focus:text-primary-600": "_sm:focus:text-primary-600_6d3e9",
  "sm:focus:text-primary-700": "_sm:focus:text-primary-700_6d3e9",
  "sm:focus:text-primary-800": "_sm:focus:text-primary-800_6d3e9",
  "sm:focus:text-primary-900": "_sm:focus:text-primary-900_6d3e9",
  "sm:focus:text-secondary-100": "_sm:focus:text-secondary-100_6d3e9",
  "sm:focus:text-secondary-200": "_sm:focus:text-secondary-200_6d3e9",
  "sm:focus:text-secondary-300": "_sm:focus:text-secondary-300_6d3e9",
  "sm:focus:text-secondary-400": "_sm:focus:text-secondary-400_6d3e9",
  "sm:focus:text-secondary-500": "_sm:focus:text-secondary-500_6d3e9",
  "sm:focus:text-secondary-600": "_sm:focus:text-secondary-600_6d3e9",
  "sm:focus:text-secondary-700": "_sm:focus:text-secondary-700_6d3e9",
  "sm:focus:text-secondary-800": "_sm:focus:text-secondary-800_6d3e9",
  "sm:focus:text-secondary-900": "_sm:focus:text-secondary-900_6d3e9",
  "sm:text-xs": "_sm:text-xs_6d3e9",
  "sm:text-sm": "_sm:text-sm_6d3e9",
  "sm:text-base": "_sm:text-base_6d3e9",
  "sm:text-lg": "_sm:text-lg_6d3e9",
  "sm:text-xl": "_sm:text-xl_6d3e9",
  "sm:text-2xl": "_sm:text-2xl_6d3e9",
  "sm:text-3xl": "_sm:text-3xl_6d3e9",
  "sm:text-4xl": "_sm:text-4xl_6d3e9",
  "sm:text-5xl": "_sm:text-5xl_6d3e9",
  "sm:text-6xl": "_sm:text-6xl_6d3e9",
  "sm:italic": "_sm:italic_6d3e9",
  "sm:not-italic": "_sm:not-italic_6d3e9",
  "sm:uppercase": "_sm:uppercase_6d3e9",
  "sm:lowercase": "_sm:lowercase_6d3e9",
  "sm:capitalize": "_sm:capitalize_6d3e9",
  "sm:normal-case": "_sm:normal-case_6d3e9",
  "sm:underline": "_sm:underline_6d3e9",
  "sm:line-through": "_sm:line-through_6d3e9",
  "sm:no-underline": "_sm:no-underline_6d3e9",
  "sm:hover:underline": "_sm:hover:underline_6d3e9",
  "sm:hover:line-through": "_sm:hover:line-through_6d3e9",
  "sm:hover:no-underline": "_sm:hover:no-underline_6d3e9",
  "sm:focus:underline": "_sm:focus:underline_6d3e9",
  "sm:focus:line-through": "_sm:focus:line-through_6d3e9",
  "sm:focus:no-underline": "_sm:focus:no-underline_6d3e9",
  "sm:antialiased": "_sm:antialiased_6d3e9",
  "sm:subpixel-antialiased": "_sm:subpixel-antialiased_6d3e9",
  "sm:tracking-tighter": "_sm:tracking-tighter_6d3e9",
  "sm:tracking-tight": "_sm:tracking-tight_6d3e9",
  "sm:tracking-normal": "_sm:tracking-normal_6d3e9",
  "sm:tracking-wide": "_sm:tracking-wide_6d3e9",
  "sm:tracking-wider": "_sm:tracking-wider_6d3e9",
  "sm:tracking-widest": "_sm:tracking-widest_6d3e9",
  "sm:select-none": "_sm:select-none_6d3e9",
  "sm:select-text": "_sm:select-text_6d3e9",
  "sm:select-all": "_sm:select-all_6d3e9",
  "sm:select-auto": "_sm:select-auto_6d3e9",
  "sm:align-baseline": "_sm:align-baseline_6d3e9",
  "sm:align-top": "_sm:align-top_6d3e9",
  "sm:align-middle": "_sm:align-middle_6d3e9",
  "sm:align-bottom": "_sm:align-bottom_6d3e9",
  "sm:align-text-top": "_sm:align-text-top_6d3e9",
  "sm:align-text-bottom": "_sm:align-text-bottom_6d3e9",
  "sm:visible": "_sm:visible_6d3e9",
  "sm:invisible": "_sm:invisible_6d3e9",
  "sm:whitespace-normal": "_sm:whitespace-normal_6d3e9",
  "sm:whitespace-no-wrap": "_sm:whitespace-no-wrap_6d3e9",
  "sm:whitespace-pre": "_sm:whitespace-pre_6d3e9",
  "sm:whitespace-pre-line": "_sm:whitespace-pre-line_6d3e9",
  "sm:whitespace-pre-wrap": "_sm:whitespace-pre-wrap_6d3e9",
  "sm:break-normal": "_sm:break-normal_6d3e9",
  "sm:break-words": "_sm:break-words_6d3e9",
  "sm:break-all": "_sm:break-all_6d3e9",
  "sm:truncate": "_sm:truncate_6d3e9",
  "sm:w-0": "_sm:w-0_6d3e9",
  "sm:w-1": "_sm:w-1_6d3e9",
  "sm:w-2": "_sm:w-2_6d3e9",
  "sm:w-3": "_sm:w-3_6d3e9",
  "sm:w-4": "_sm:w-4_6d3e9",
  "sm:w-5": "_sm:w-5_6d3e9",
  "sm:w-6": "_sm:w-6_6d3e9",
  "sm:w-8": "_sm:w-8_6d3e9",
  "sm:w-10": "_sm:w-10_6d3e9",
  "sm:w-12": "_sm:w-12_6d3e9",
  "sm:w-16": "_sm:w-16_6d3e9",
  "sm:w-20": "_sm:w-20_6d3e9",
  "sm:w-24": "_sm:w-24_6d3e9",
  "sm:w-32": "_sm:w-32_6d3e9",
  "sm:w-40": "_sm:w-40_6d3e9",
  "sm:w-48": "_sm:w-48_6d3e9",
  "sm:w-56": "_sm:w-56_6d3e9",
  "sm:w-64": "_sm:w-64_6d3e9",
  "sm:w-auto": "_sm:w-auto_6d3e9",
  "sm:w-px": "_sm:w-px_6d3e9",
  "sm:w-1/2": "_sm:w-1/2_6d3e9",
  "sm:w-1/3": "_sm:w-1/3_6d3e9",
  "sm:w-2/3": "_sm:w-2/3_6d3e9",
  "sm:w-1/4": "_sm:w-1/4_6d3e9",
  "sm:w-2/4": "_sm:w-2/4_6d3e9",
  "sm:w-3/4": "_sm:w-3/4_6d3e9",
  "sm:w-1/5": "_sm:w-1/5_6d3e9",
  "sm:w-2/5": "_sm:w-2/5_6d3e9",
  "sm:w-3/5": "_sm:w-3/5_6d3e9",
  "sm:w-4/5": "_sm:w-4/5_6d3e9",
  "sm:w-1/6": "_sm:w-1/6_6d3e9",
  "sm:w-2/6": "_sm:w-2/6_6d3e9",
  "sm:w-3/6": "_sm:w-3/6_6d3e9",
  "sm:w-4/6": "_sm:w-4/6_6d3e9",
  "sm:w-5/6": "_sm:w-5/6_6d3e9",
  "sm:w-1/12": "_sm:w-1/12_6d3e9",
  "sm:w-2/12": "_sm:w-2/12_6d3e9",
  "sm:w-3/12": "_sm:w-3/12_6d3e9",
  "sm:w-4/12": "_sm:w-4/12_6d3e9",
  "sm:w-5/12": "_sm:w-5/12_6d3e9",
  "sm:w-6/12": "_sm:w-6/12_6d3e9",
  "sm:w-7/12": "_sm:w-7/12_6d3e9",
  "sm:w-8/12": "_sm:w-8/12_6d3e9",
  "sm:w-9/12": "_sm:w-9/12_6d3e9",
  "sm:w-10/12": "_sm:w-10/12_6d3e9",
  "sm:w-11/12": "_sm:w-11/12_6d3e9",
  "sm:w-full": "_sm:w-full_6d3e9",
  "sm:w-screen": "_sm:w-screen_6d3e9",
  "sm:z-0": "_sm:z-0_6d3e9",
  "sm:z-10": "_sm:z-10_6d3e9",
  "sm:z-20": "_sm:z-20_6d3e9",
  "sm:z-30": "_sm:z-30_6d3e9",
  "sm:z-40": "_sm:z-40_6d3e9",
  "sm:z-50": "_sm:z-50_6d3e9",
  "sm:z-auto": "_sm:z-auto_6d3e9",
  "md:sr-only": "_md:sr-only_6d3e9",
  "md:not-sr-only": "_md:not-sr-only_6d3e9",
  "md:focus:sr-only": "_md:focus:sr-only_6d3e9",
  "md:focus:not-sr-only": "_md:focus:not-sr-only_6d3e9",
  "md:appearance-none": "_md:appearance-none_6d3e9",
  "md:bg-fixed": "_md:bg-fixed_6d3e9",
  "md:bg-local": "_md:bg-local_6d3e9",
  "md:bg-scroll": "_md:bg-scroll_6d3e9",
  "md:bg-transparent": "_md:bg-transparent_6d3e9",
  "md:bg-black": "_md:bg-black_6d3e9",
  "md:bg-white": "_md:bg-white_6d3e9",
  "md:bg-gray-100": "_md:bg-gray-100_6d3e9",
  "md:bg-gray-200": "_md:bg-gray-200_6d3e9",
  "md:bg-gray-300": "_md:bg-gray-300_6d3e9",
  "md:bg-gray-400": "_md:bg-gray-400_6d3e9",
  "md:bg-gray-500": "_md:bg-gray-500_6d3e9",
  "md:bg-gray-600": "_md:bg-gray-600_6d3e9",
  "md:bg-gray-700": "_md:bg-gray-700_6d3e9",
  "md:bg-gray-800": "_md:bg-gray-800_6d3e9",
  "md:bg-gray-900": "_md:bg-gray-900_6d3e9",
  "md:bg-red-100": "_md:bg-red-100_6d3e9",
  "md:bg-red-200": "_md:bg-red-200_6d3e9",
  "md:bg-red-300": "_md:bg-red-300_6d3e9",
  "md:bg-red-400": "_md:bg-red-400_6d3e9",
  "md:bg-red-500": "_md:bg-red-500_6d3e9",
  "md:bg-red-600": "_md:bg-red-600_6d3e9",
  "md:bg-red-700": "_md:bg-red-700_6d3e9",
  "md:bg-red-800": "_md:bg-red-800_6d3e9",
  "md:bg-red-900": "_md:bg-red-900_6d3e9",
  "md:bg-orange-100": "_md:bg-orange-100_6d3e9",
  "md:bg-orange-200": "_md:bg-orange-200_6d3e9",
  "md:bg-orange-300": "_md:bg-orange-300_6d3e9",
  "md:bg-orange-400": "_md:bg-orange-400_6d3e9",
  "md:bg-orange-500": "_md:bg-orange-500_6d3e9",
  "md:bg-orange-600": "_md:bg-orange-600_6d3e9",
  "md:bg-orange-700": "_md:bg-orange-700_6d3e9",
  "md:bg-orange-800": "_md:bg-orange-800_6d3e9",
  "md:bg-orange-900": "_md:bg-orange-900_6d3e9",
  "md:bg-yellow-100": "_md:bg-yellow-100_6d3e9",
  "md:bg-yellow-200": "_md:bg-yellow-200_6d3e9",
  "md:bg-yellow-300": "_md:bg-yellow-300_6d3e9",
  "md:bg-yellow-400": "_md:bg-yellow-400_6d3e9",
  "md:bg-yellow-500": "_md:bg-yellow-500_6d3e9",
  "md:bg-yellow-600": "_md:bg-yellow-600_6d3e9",
  "md:bg-yellow-700": "_md:bg-yellow-700_6d3e9",
  "md:bg-yellow-800": "_md:bg-yellow-800_6d3e9",
  "md:bg-yellow-900": "_md:bg-yellow-900_6d3e9",
  "md:bg-green-100": "_md:bg-green-100_6d3e9",
  "md:bg-green-200": "_md:bg-green-200_6d3e9",
  "md:bg-green-300": "_md:bg-green-300_6d3e9",
  "md:bg-green-400": "_md:bg-green-400_6d3e9",
  "md:bg-green-500": "_md:bg-green-500_6d3e9",
  "md:bg-green-600": "_md:bg-green-600_6d3e9",
  "md:bg-green-700": "_md:bg-green-700_6d3e9",
  "md:bg-green-800": "_md:bg-green-800_6d3e9",
  "md:bg-green-900": "_md:bg-green-900_6d3e9",
  "md:bg-teal-100": "_md:bg-teal-100_6d3e9",
  "md:bg-teal-200": "_md:bg-teal-200_6d3e9",
  "md:bg-teal-300": "_md:bg-teal-300_6d3e9",
  "md:bg-teal-400": "_md:bg-teal-400_6d3e9",
  "md:bg-teal-500": "_md:bg-teal-500_6d3e9",
  "md:bg-teal-600": "_md:bg-teal-600_6d3e9",
  "md:bg-teal-700": "_md:bg-teal-700_6d3e9",
  "md:bg-teal-800": "_md:bg-teal-800_6d3e9",
  "md:bg-teal-900": "_md:bg-teal-900_6d3e9",
  "md:bg-blue-100": "_md:bg-blue-100_6d3e9",
  "md:bg-blue-200": "_md:bg-blue-200_6d3e9",
  "md:bg-blue-300": "_md:bg-blue-300_6d3e9",
  "md:bg-blue-400": "_md:bg-blue-400_6d3e9",
  "md:bg-blue-500": "_md:bg-blue-500_6d3e9",
  "md:bg-blue-600": "_md:bg-blue-600_6d3e9",
  "md:bg-blue-700": "_md:bg-blue-700_6d3e9",
  "md:bg-blue-800": "_md:bg-blue-800_6d3e9",
  "md:bg-blue-900": "_md:bg-blue-900_6d3e9",
  "md:bg-indigo-100": "_md:bg-indigo-100_6d3e9",
  "md:bg-indigo-200": "_md:bg-indigo-200_6d3e9",
  "md:bg-indigo-300": "_md:bg-indigo-300_6d3e9",
  "md:bg-indigo-400": "_md:bg-indigo-400_6d3e9",
  "md:bg-indigo-500": "_md:bg-indigo-500_6d3e9",
  "md:bg-indigo-600": "_md:bg-indigo-600_6d3e9",
  "md:bg-indigo-700": "_md:bg-indigo-700_6d3e9",
  "md:bg-indigo-800": "_md:bg-indigo-800_6d3e9",
  "md:bg-indigo-900": "_md:bg-indigo-900_6d3e9",
  "md:bg-purple-100": "_md:bg-purple-100_6d3e9",
  "md:bg-purple-200": "_md:bg-purple-200_6d3e9",
  "md:bg-purple-300": "_md:bg-purple-300_6d3e9",
  "md:bg-purple-400": "_md:bg-purple-400_6d3e9",
  "md:bg-purple-500": "_md:bg-purple-500_6d3e9",
  "md:bg-purple-600": "_md:bg-purple-600_6d3e9",
  "md:bg-purple-700": "_md:bg-purple-700_6d3e9",
  "md:bg-purple-800": "_md:bg-purple-800_6d3e9",
  "md:bg-purple-900": "_md:bg-purple-900_6d3e9",
  "md:bg-pink-100": "_md:bg-pink-100_6d3e9",
  "md:bg-pink-200": "_md:bg-pink-200_6d3e9",
  "md:bg-pink-300": "_md:bg-pink-300_6d3e9",
  "md:bg-pink-400": "_md:bg-pink-400_6d3e9",
  "md:bg-pink-500": "_md:bg-pink-500_6d3e9",
  "md:bg-pink-600": "_md:bg-pink-600_6d3e9",
  "md:bg-pink-700": "_md:bg-pink-700_6d3e9",
  "md:bg-pink-800": "_md:bg-pink-800_6d3e9",
  "md:bg-pink-900": "_md:bg-pink-900_6d3e9",
  "md:bg-primary-100": "_md:bg-primary-100_6d3e9",
  "md:bg-primary-200": "_md:bg-primary-200_6d3e9",
  "md:bg-primary-300": "_md:bg-primary-300_6d3e9",
  "md:bg-primary-400": "_md:bg-primary-400_6d3e9",
  "md:bg-primary-500": "_md:bg-primary-500_6d3e9",
  "md:bg-primary-600": "_md:bg-primary-600_6d3e9",
  "md:bg-primary-700": "_md:bg-primary-700_6d3e9",
  "md:bg-primary-800": "_md:bg-primary-800_6d3e9",
  "md:bg-primary-900": "_md:bg-primary-900_6d3e9",
  "md:bg-secondary-100": "_md:bg-secondary-100_6d3e9",
  "md:bg-secondary-200": "_md:bg-secondary-200_6d3e9",
  "md:bg-secondary-300": "_md:bg-secondary-300_6d3e9",
  "md:bg-secondary-400": "_md:bg-secondary-400_6d3e9",
  "md:bg-secondary-500": "_md:bg-secondary-500_6d3e9",
  "md:bg-secondary-600": "_md:bg-secondary-600_6d3e9",
  "md:bg-secondary-700": "_md:bg-secondary-700_6d3e9",
  "md:bg-secondary-800": "_md:bg-secondary-800_6d3e9",
  "md:bg-secondary-900": "_md:bg-secondary-900_6d3e9",
  "md:hover:bg-transparent": "_md:hover:bg-transparent_6d3e9",
  "md:hover:bg-black": "_md:hover:bg-black_6d3e9",
  "md:hover:bg-white": "_md:hover:bg-white_6d3e9",
  "md:hover:bg-gray-100": "_md:hover:bg-gray-100_6d3e9",
  "md:hover:bg-gray-200": "_md:hover:bg-gray-200_6d3e9",
  "md:hover:bg-gray-300": "_md:hover:bg-gray-300_6d3e9",
  "md:hover:bg-gray-400": "_md:hover:bg-gray-400_6d3e9",
  "md:hover:bg-gray-500": "_md:hover:bg-gray-500_6d3e9",
  "md:hover:bg-gray-600": "_md:hover:bg-gray-600_6d3e9",
  "md:hover:bg-gray-700": "_md:hover:bg-gray-700_6d3e9",
  "md:hover:bg-gray-800": "_md:hover:bg-gray-800_6d3e9",
  "md:hover:bg-gray-900": "_md:hover:bg-gray-900_6d3e9",
  "md:hover:bg-red-100": "_md:hover:bg-red-100_6d3e9",
  "md:hover:bg-red-200": "_md:hover:bg-red-200_6d3e9",
  "md:hover:bg-red-300": "_md:hover:bg-red-300_6d3e9",
  "md:hover:bg-red-400": "_md:hover:bg-red-400_6d3e9",
  "md:hover:bg-red-500": "_md:hover:bg-red-500_6d3e9",
  "md:hover:bg-red-600": "_md:hover:bg-red-600_6d3e9",
  "md:hover:bg-red-700": "_md:hover:bg-red-700_6d3e9",
  "md:hover:bg-red-800": "_md:hover:bg-red-800_6d3e9",
  "md:hover:bg-red-900": "_md:hover:bg-red-900_6d3e9",
  "md:hover:bg-orange-100": "_md:hover:bg-orange-100_6d3e9",
  "md:hover:bg-orange-200": "_md:hover:bg-orange-200_6d3e9",
  "md:hover:bg-orange-300": "_md:hover:bg-orange-300_6d3e9",
  "md:hover:bg-orange-400": "_md:hover:bg-orange-400_6d3e9",
  "md:hover:bg-orange-500": "_md:hover:bg-orange-500_6d3e9",
  "md:hover:bg-orange-600": "_md:hover:bg-orange-600_6d3e9",
  "md:hover:bg-orange-700": "_md:hover:bg-orange-700_6d3e9",
  "md:hover:bg-orange-800": "_md:hover:bg-orange-800_6d3e9",
  "md:hover:bg-orange-900": "_md:hover:bg-orange-900_6d3e9",
  "md:hover:bg-yellow-100": "_md:hover:bg-yellow-100_6d3e9",
  "md:hover:bg-yellow-200": "_md:hover:bg-yellow-200_6d3e9",
  "md:hover:bg-yellow-300": "_md:hover:bg-yellow-300_6d3e9",
  "md:hover:bg-yellow-400": "_md:hover:bg-yellow-400_6d3e9",
  "md:hover:bg-yellow-500": "_md:hover:bg-yellow-500_6d3e9",
  "md:hover:bg-yellow-600": "_md:hover:bg-yellow-600_6d3e9",
  "md:hover:bg-yellow-700": "_md:hover:bg-yellow-700_6d3e9",
  "md:hover:bg-yellow-800": "_md:hover:bg-yellow-800_6d3e9",
  "md:hover:bg-yellow-900": "_md:hover:bg-yellow-900_6d3e9",
  "md:hover:bg-green-100": "_md:hover:bg-green-100_6d3e9",
  "md:hover:bg-green-200": "_md:hover:bg-green-200_6d3e9",
  "md:hover:bg-green-300": "_md:hover:bg-green-300_6d3e9",
  "md:hover:bg-green-400": "_md:hover:bg-green-400_6d3e9",
  "md:hover:bg-green-500": "_md:hover:bg-green-500_6d3e9",
  "md:hover:bg-green-600": "_md:hover:bg-green-600_6d3e9",
  "md:hover:bg-green-700": "_md:hover:bg-green-700_6d3e9",
  "md:hover:bg-green-800": "_md:hover:bg-green-800_6d3e9",
  "md:hover:bg-green-900": "_md:hover:bg-green-900_6d3e9",
  "md:hover:bg-teal-100": "_md:hover:bg-teal-100_6d3e9",
  "md:hover:bg-teal-200": "_md:hover:bg-teal-200_6d3e9",
  "md:hover:bg-teal-300": "_md:hover:bg-teal-300_6d3e9",
  "md:hover:bg-teal-400": "_md:hover:bg-teal-400_6d3e9",
  "md:hover:bg-teal-500": "_md:hover:bg-teal-500_6d3e9",
  "md:hover:bg-teal-600": "_md:hover:bg-teal-600_6d3e9",
  "md:hover:bg-teal-700": "_md:hover:bg-teal-700_6d3e9",
  "md:hover:bg-teal-800": "_md:hover:bg-teal-800_6d3e9",
  "md:hover:bg-teal-900": "_md:hover:bg-teal-900_6d3e9",
  "md:hover:bg-blue-100": "_md:hover:bg-blue-100_6d3e9",
  "md:hover:bg-blue-200": "_md:hover:bg-blue-200_6d3e9",
  "md:hover:bg-blue-300": "_md:hover:bg-blue-300_6d3e9",
  "md:hover:bg-blue-400": "_md:hover:bg-blue-400_6d3e9",
  "md:hover:bg-blue-500": "_md:hover:bg-blue-500_6d3e9",
  "md:hover:bg-blue-600": "_md:hover:bg-blue-600_6d3e9",
  "md:hover:bg-blue-700": "_md:hover:bg-blue-700_6d3e9",
  "md:hover:bg-blue-800": "_md:hover:bg-blue-800_6d3e9",
  "md:hover:bg-blue-900": "_md:hover:bg-blue-900_6d3e9",
  "md:hover:bg-indigo-100": "_md:hover:bg-indigo-100_6d3e9",
  "md:hover:bg-indigo-200": "_md:hover:bg-indigo-200_6d3e9",
  "md:hover:bg-indigo-300": "_md:hover:bg-indigo-300_6d3e9",
  "md:hover:bg-indigo-400": "_md:hover:bg-indigo-400_6d3e9",
  "md:hover:bg-indigo-500": "_md:hover:bg-indigo-500_6d3e9",
  "md:hover:bg-indigo-600": "_md:hover:bg-indigo-600_6d3e9",
  "md:hover:bg-indigo-700": "_md:hover:bg-indigo-700_6d3e9",
  "md:hover:bg-indigo-800": "_md:hover:bg-indigo-800_6d3e9",
  "md:hover:bg-indigo-900": "_md:hover:bg-indigo-900_6d3e9",
  "md:hover:bg-purple-100": "_md:hover:bg-purple-100_6d3e9",
  "md:hover:bg-purple-200": "_md:hover:bg-purple-200_6d3e9",
  "md:hover:bg-purple-300": "_md:hover:bg-purple-300_6d3e9",
  "md:hover:bg-purple-400": "_md:hover:bg-purple-400_6d3e9",
  "md:hover:bg-purple-500": "_md:hover:bg-purple-500_6d3e9",
  "md:hover:bg-purple-600": "_md:hover:bg-purple-600_6d3e9",
  "md:hover:bg-purple-700": "_md:hover:bg-purple-700_6d3e9",
  "md:hover:bg-purple-800": "_md:hover:bg-purple-800_6d3e9",
  "md:hover:bg-purple-900": "_md:hover:bg-purple-900_6d3e9",
  "md:hover:bg-pink-100": "_md:hover:bg-pink-100_6d3e9",
  "md:hover:bg-pink-200": "_md:hover:bg-pink-200_6d3e9",
  "md:hover:bg-pink-300": "_md:hover:bg-pink-300_6d3e9",
  "md:hover:bg-pink-400": "_md:hover:bg-pink-400_6d3e9",
  "md:hover:bg-pink-500": "_md:hover:bg-pink-500_6d3e9",
  "md:hover:bg-pink-600": "_md:hover:bg-pink-600_6d3e9",
  "md:hover:bg-pink-700": "_md:hover:bg-pink-700_6d3e9",
  "md:hover:bg-pink-800": "_md:hover:bg-pink-800_6d3e9",
  "md:hover:bg-pink-900": "_md:hover:bg-pink-900_6d3e9",
  "md:hover:bg-primary-100": "_md:hover:bg-primary-100_6d3e9",
  "md:hover:bg-primary-200": "_md:hover:bg-primary-200_6d3e9",
  "md:hover:bg-primary-300": "_md:hover:bg-primary-300_6d3e9",
  "md:hover:bg-primary-400": "_md:hover:bg-primary-400_6d3e9",
  "md:hover:bg-primary-500": "_md:hover:bg-primary-500_6d3e9",
  "md:hover:bg-primary-600": "_md:hover:bg-primary-600_6d3e9",
  "md:hover:bg-primary-700": "_md:hover:bg-primary-700_6d3e9",
  "md:hover:bg-primary-800": "_md:hover:bg-primary-800_6d3e9",
  "md:hover:bg-primary-900": "_md:hover:bg-primary-900_6d3e9",
  "md:hover:bg-secondary-100": "_md:hover:bg-secondary-100_6d3e9",
  "md:hover:bg-secondary-200": "_md:hover:bg-secondary-200_6d3e9",
  "md:hover:bg-secondary-300": "_md:hover:bg-secondary-300_6d3e9",
  "md:hover:bg-secondary-400": "_md:hover:bg-secondary-400_6d3e9",
  "md:hover:bg-secondary-500": "_md:hover:bg-secondary-500_6d3e9",
  "md:hover:bg-secondary-600": "_md:hover:bg-secondary-600_6d3e9",
  "md:hover:bg-secondary-700": "_md:hover:bg-secondary-700_6d3e9",
  "md:hover:bg-secondary-800": "_md:hover:bg-secondary-800_6d3e9",
  "md:hover:bg-secondary-900": "_md:hover:bg-secondary-900_6d3e9",
  "md:focus:bg-transparent": "_md:focus:bg-transparent_6d3e9",
  "md:focus:bg-black": "_md:focus:bg-black_6d3e9",
  "md:focus:bg-white": "_md:focus:bg-white_6d3e9",
  "md:focus:bg-gray-100": "_md:focus:bg-gray-100_6d3e9",
  "md:focus:bg-gray-200": "_md:focus:bg-gray-200_6d3e9",
  "md:focus:bg-gray-300": "_md:focus:bg-gray-300_6d3e9",
  "md:focus:bg-gray-400": "_md:focus:bg-gray-400_6d3e9",
  "md:focus:bg-gray-500": "_md:focus:bg-gray-500_6d3e9",
  "md:focus:bg-gray-600": "_md:focus:bg-gray-600_6d3e9",
  "md:focus:bg-gray-700": "_md:focus:bg-gray-700_6d3e9",
  "md:focus:bg-gray-800": "_md:focus:bg-gray-800_6d3e9",
  "md:focus:bg-gray-900": "_md:focus:bg-gray-900_6d3e9",
  "md:focus:bg-red-100": "_md:focus:bg-red-100_6d3e9",
  "md:focus:bg-red-200": "_md:focus:bg-red-200_6d3e9",
  "md:focus:bg-red-300": "_md:focus:bg-red-300_6d3e9",
  "md:focus:bg-red-400": "_md:focus:bg-red-400_6d3e9",
  "md:focus:bg-red-500": "_md:focus:bg-red-500_6d3e9",
  "md:focus:bg-red-600": "_md:focus:bg-red-600_6d3e9",
  "md:focus:bg-red-700": "_md:focus:bg-red-700_6d3e9",
  "md:focus:bg-red-800": "_md:focus:bg-red-800_6d3e9",
  "md:focus:bg-red-900": "_md:focus:bg-red-900_6d3e9",
  "md:focus:bg-orange-100": "_md:focus:bg-orange-100_6d3e9",
  "md:focus:bg-orange-200": "_md:focus:bg-orange-200_6d3e9",
  "md:focus:bg-orange-300": "_md:focus:bg-orange-300_6d3e9",
  "md:focus:bg-orange-400": "_md:focus:bg-orange-400_6d3e9",
  "md:focus:bg-orange-500": "_md:focus:bg-orange-500_6d3e9",
  "md:focus:bg-orange-600": "_md:focus:bg-orange-600_6d3e9",
  "md:focus:bg-orange-700": "_md:focus:bg-orange-700_6d3e9",
  "md:focus:bg-orange-800": "_md:focus:bg-orange-800_6d3e9",
  "md:focus:bg-orange-900": "_md:focus:bg-orange-900_6d3e9",
  "md:focus:bg-yellow-100": "_md:focus:bg-yellow-100_6d3e9",
  "md:focus:bg-yellow-200": "_md:focus:bg-yellow-200_6d3e9",
  "md:focus:bg-yellow-300": "_md:focus:bg-yellow-300_6d3e9",
  "md:focus:bg-yellow-400": "_md:focus:bg-yellow-400_6d3e9",
  "md:focus:bg-yellow-500": "_md:focus:bg-yellow-500_6d3e9",
  "md:focus:bg-yellow-600": "_md:focus:bg-yellow-600_6d3e9",
  "md:focus:bg-yellow-700": "_md:focus:bg-yellow-700_6d3e9",
  "md:focus:bg-yellow-800": "_md:focus:bg-yellow-800_6d3e9",
  "md:focus:bg-yellow-900": "_md:focus:bg-yellow-900_6d3e9",
  "md:focus:bg-green-100": "_md:focus:bg-green-100_6d3e9",
  "md:focus:bg-green-200": "_md:focus:bg-green-200_6d3e9",
  "md:focus:bg-green-300": "_md:focus:bg-green-300_6d3e9",
  "md:focus:bg-green-400": "_md:focus:bg-green-400_6d3e9",
  "md:focus:bg-green-500": "_md:focus:bg-green-500_6d3e9",
  "md:focus:bg-green-600": "_md:focus:bg-green-600_6d3e9",
  "md:focus:bg-green-700": "_md:focus:bg-green-700_6d3e9",
  "md:focus:bg-green-800": "_md:focus:bg-green-800_6d3e9",
  "md:focus:bg-green-900": "_md:focus:bg-green-900_6d3e9",
  "md:focus:bg-teal-100": "_md:focus:bg-teal-100_6d3e9",
  "md:focus:bg-teal-200": "_md:focus:bg-teal-200_6d3e9",
  "md:focus:bg-teal-300": "_md:focus:bg-teal-300_6d3e9",
  "md:focus:bg-teal-400": "_md:focus:bg-teal-400_6d3e9",
  "md:focus:bg-teal-500": "_md:focus:bg-teal-500_6d3e9",
  "md:focus:bg-teal-600": "_md:focus:bg-teal-600_6d3e9",
  "md:focus:bg-teal-700": "_md:focus:bg-teal-700_6d3e9",
  "md:focus:bg-teal-800": "_md:focus:bg-teal-800_6d3e9",
  "md:focus:bg-teal-900": "_md:focus:bg-teal-900_6d3e9",
  "md:focus:bg-blue-100": "_md:focus:bg-blue-100_6d3e9",
  "md:focus:bg-blue-200": "_md:focus:bg-blue-200_6d3e9",
  "md:focus:bg-blue-300": "_md:focus:bg-blue-300_6d3e9",
  "md:focus:bg-blue-400": "_md:focus:bg-blue-400_6d3e9",
  "md:focus:bg-blue-500": "_md:focus:bg-blue-500_6d3e9",
  "md:focus:bg-blue-600": "_md:focus:bg-blue-600_6d3e9",
  "md:focus:bg-blue-700": "_md:focus:bg-blue-700_6d3e9",
  "md:focus:bg-blue-800": "_md:focus:bg-blue-800_6d3e9",
  "md:focus:bg-blue-900": "_md:focus:bg-blue-900_6d3e9",
  "md:focus:bg-indigo-100": "_md:focus:bg-indigo-100_6d3e9",
  "md:focus:bg-indigo-200": "_md:focus:bg-indigo-200_6d3e9",
  "md:focus:bg-indigo-300": "_md:focus:bg-indigo-300_6d3e9",
  "md:focus:bg-indigo-400": "_md:focus:bg-indigo-400_6d3e9",
  "md:focus:bg-indigo-500": "_md:focus:bg-indigo-500_6d3e9",
  "md:focus:bg-indigo-600": "_md:focus:bg-indigo-600_6d3e9",
  "md:focus:bg-indigo-700": "_md:focus:bg-indigo-700_6d3e9",
  "md:focus:bg-indigo-800": "_md:focus:bg-indigo-800_6d3e9",
  "md:focus:bg-indigo-900": "_md:focus:bg-indigo-900_6d3e9",
  "md:focus:bg-purple-100": "_md:focus:bg-purple-100_6d3e9",
  "md:focus:bg-purple-200": "_md:focus:bg-purple-200_6d3e9",
  "md:focus:bg-purple-300": "_md:focus:bg-purple-300_6d3e9",
  "md:focus:bg-purple-400": "_md:focus:bg-purple-400_6d3e9",
  "md:focus:bg-purple-500": "_md:focus:bg-purple-500_6d3e9",
  "md:focus:bg-purple-600": "_md:focus:bg-purple-600_6d3e9",
  "md:focus:bg-purple-700": "_md:focus:bg-purple-700_6d3e9",
  "md:focus:bg-purple-800": "_md:focus:bg-purple-800_6d3e9",
  "md:focus:bg-purple-900": "_md:focus:bg-purple-900_6d3e9",
  "md:focus:bg-pink-100": "_md:focus:bg-pink-100_6d3e9",
  "md:focus:bg-pink-200": "_md:focus:bg-pink-200_6d3e9",
  "md:focus:bg-pink-300": "_md:focus:bg-pink-300_6d3e9",
  "md:focus:bg-pink-400": "_md:focus:bg-pink-400_6d3e9",
  "md:focus:bg-pink-500": "_md:focus:bg-pink-500_6d3e9",
  "md:focus:bg-pink-600": "_md:focus:bg-pink-600_6d3e9",
  "md:focus:bg-pink-700": "_md:focus:bg-pink-700_6d3e9",
  "md:focus:bg-pink-800": "_md:focus:bg-pink-800_6d3e9",
  "md:focus:bg-pink-900": "_md:focus:bg-pink-900_6d3e9",
  "md:focus:bg-primary-100": "_md:focus:bg-primary-100_6d3e9",
  "md:focus:bg-primary-200": "_md:focus:bg-primary-200_6d3e9",
  "md:focus:bg-primary-300": "_md:focus:bg-primary-300_6d3e9",
  "md:focus:bg-primary-400": "_md:focus:bg-primary-400_6d3e9",
  "md:focus:bg-primary-500": "_md:focus:bg-primary-500_6d3e9",
  "md:focus:bg-primary-600": "_md:focus:bg-primary-600_6d3e9",
  "md:focus:bg-primary-700": "_md:focus:bg-primary-700_6d3e9",
  "md:focus:bg-primary-800": "_md:focus:bg-primary-800_6d3e9",
  "md:focus:bg-primary-900": "_md:focus:bg-primary-900_6d3e9",
  "md:focus:bg-secondary-100": "_md:focus:bg-secondary-100_6d3e9",
  "md:focus:bg-secondary-200": "_md:focus:bg-secondary-200_6d3e9",
  "md:focus:bg-secondary-300": "_md:focus:bg-secondary-300_6d3e9",
  "md:focus:bg-secondary-400": "_md:focus:bg-secondary-400_6d3e9",
  "md:focus:bg-secondary-500": "_md:focus:bg-secondary-500_6d3e9",
  "md:focus:bg-secondary-600": "_md:focus:bg-secondary-600_6d3e9",
  "md:focus:bg-secondary-700": "_md:focus:bg-secondary-700_6d3e9",
  "md:focus:bg-secondary-800": "_md:focus:bg-secondary-800_6d3e9",
  "md:focus:bg-secondary-900": "_md:focus:bg-secondary-900_6d3e9",
  "md:active:bg-transparent": "_md:active:bg-transparent_6d3e9",
  "md:active:bg-black": "_md:active:bg-black_6d3e9",
  "md:active:bg-white": "_md:active:bg-white_6d3e9",
  "md:active:bg-gray-100": "_md:active:bg-gray-100_6d3e9",
  "md:active:bg-gray-200": "_md:active:bg-gray-200_6d3e9",
  "md:active:bg-gray-300": "_md:active:bg-gray-300_6d3e9",
  "md:active:bg-gray-400": "_md:active:bg-gray-400_6d3e9",
  "md:active:bg-gray-500": "_md:active:bg-gray-500_6d3e9",
  "md:active:bg-gray-600": "_md:active:bg-gray-600_6d3e9",
  "md:active:bg-gray-700": "_md:active:bg-gray-700_6d3e9",
  "md:active:bg-gray-800": "_md:active:bg-gray-800_6d3e9",
  "md:active:bg-gray-900": "_md:active:bg-gray-900_6d3e9",
  "md:active:bg-red-100": "_md:active:bg-red-100_6d3e9",
  "md:active:bg-red-200": "_md:active:bg-red-200_6d3e9",
  "md:active:bg-red-300": "_md:active:bg-red-300_6d3e9",
  "md:active:bg-red-400": "_md:active:bg-red-400_6d3e9",
  "md:active:bg-red-500": "_md:active:bg-red-500_6d3e9",
  "md:active:bg-red-600": "_md:active:bg-red-600_6d3e9",
  "md:active:bg-red-700": "_md:active:bg-red-700_6d3e9",
  "md:active:bg-red-800": "_md:active:bg-red-800_6d3e9",
  "md:active:bg-red-900": "_md:active:bg-red-900_6d3e9",
  "md:active:bg-orange-100": "_md:active:bg-orange-100_6d3e9",
  "md:active:bg-orange-200": "_md:active:bg-orange-200_6d3e9",
  "md:active:bg-orange-300": "_md:active:bg-orange-300_6d3e9",
  "md:active:bg-orange-400": "_md:active:bg-orange-400_6d3e9",
  "md:active:bg-orange-500": "_md:active:bg-orange-500_6d3e9",
  "md:active:bg-orange-600": "_md:active:bg-orange-600_6d3e9",
  "md:active:bg-orange-700": "_md:active:bg-orange-700_6d3e9",
  "md:active:bg-orange-800": "_md:active:bg-orange-800_6d3e9",
  "md:active:bg-orange-900": "_md:active:bg-orange-900_6d3e9",
  "md:active:bg-yellow-100": "_md:active:bg-yellow-100_6d3e9",
  "md:active:bg-yellow-200": "_md:active:bg-yellow-200_6d3e9",
  "md:active:bg-yellow-300": "_md:active:bg-yellow-300_6d3e9",
  "md:active:bg-yellow-400": "_md:active:bg-yellow-400_6d3e9",
  "md:active:bg-yellow-500": "_md:active:bg-yellow-500_6d3e9",
  "md:active:bg-yellow-600": "_md:active:bg-yellow-600_6d3e9",
  "md:active:bg-yellow-700": "_md:active:bg-yellow-700_6d3e9",
  "md:active:bg-yellow-800": "_md:active:bg-yellow-800_6d3e9",
  "md:active:bg-yellow-900": "_md:active:bg-yellow-900_6d3e9",
  "md:active:bg-green-100": "_md:active:bg-green-100_6d3e9",
  "md:active:bg-green-200": "_md:active:bg-green-200_6d3e9",
  "md:active:bg-green-300": "_md:active:bg-green-300_6d3e9",
  "md:active:bg-green-400": "_md:active:bg-green-400_6d3e9",
  "md:active:bg-green-500": "_md:active:bg-green-500_6d3e9",
  "md:active:bg-green-600": "_md:active:bg-green-600_6d3e9",
  "md:active:bg-green-700": "_md:active:bg-green-700_6d3e9",
  "md:active:bg-green-800": "_md:active:bg-green-800_6d3e9",
  "md:active:bg-green-900": "_md:active:bg-green-900_6d3e9",
  "md:active:bg-teal-100": "_md:active:bg-teal-100_6d3e9",
  "md:active:bg-teal-200": "_md:active:bg-teal-200_6d3e9",
  "md:active:bg-teal-300": "_md:active:bg-teal-300_6d3e9",
  "md:active:bg-teal-400": "_md:active:bg-teal-400_6d3e9",
  "md:active:bg-teal-500": "_md:active:bg-teal-500_6d3e9",
  "md:active:bg-teal-600": "_md:active:bg-teal-600_6d3e9",
  "md:active:bg-teal-700": "_md:active:bg-teal-700_6d3e9",
  "md:active:bg-teal-800": "_md:active:bg-teal-800_6d3e9",
  "md:active:bg-teal-900": "_md:active:bg-teal-900_6d3e9",
  "md:active:bg-blue-100": "_md:active:bg-blue-100_6d3e9",
  "md:active:bg-blue-200": "_md:active:bg-blue-200_6d3e9",
  "md:active:bg-blue-300": "_md:active:bg-blue-300_6d3e9",
  "md:active:bg-blue-400": "_md:active:bg-blue-400_6d3e9",
  "md:active:bg-blue-500": "_md:active:bg-blue-500_6d3e9",
  "md:active:bg-blue-600": "_md:active:bg-blue-600_6d3e9",
  "md:active:bg-blue-700": "_md:active:bg-blue-700_6d3e9",
  "md:active:bg-blue-800": "_md:active:bg-blue-800_6d3e9",
  "md:active:bg-blue-900": "_md:active:bg-blue-900_6d3e9",
  "md:active:bg-indigo-100": "_md:active:bg-indigo-100_6d3e9",
  "md:active:bg-indigo-200": "_md:active:bg-indigo-200_6d3e9",
  "md:active:bg-indigo-300": "_md:active:bg-indigo-300_6d3e9",
  "md:active:bg-indigo-400": "_md:active:bg-indigo-400_6d3e9",
  "md:active:bg-indigo-500": "_md:active:bg-indigo-500_6d3e9",
  "md:active:bg-indigo-600": "_md:active:bg-indigo-600_6d3e9",
  "md:active:bg-indigo-700": "_md:active:bg-indigo-700_6d3e9",
  "md:active:bg-indigo-800": "_md:active:bg-indigo-800_6d3e9",
  "md:active:bg-indigo-900": "_md:active:bg-indigo-900_6d3e9",
  "md:active:bg-purple-100": "_md:active:bg-purple-100_6d3e9",
  "md:active:bg-purple-200": "_md:active:bg-purple-200_6d3e9",
  "md:active:bg-purple-300": "_md:active:bg-purple-300_6d3e9",
  "md:active:bg-purple-400": "_md:active:bg-purple-400_6d3e9",
  "md:active:bg-purple-500": "_md:active:bg-purple-500_6d3e9",
  "md:active:bg-purple-600": "_md:active:bg-purple-600_6d3e9",
  "md:active:bg-purple-700": "_md:active:bg-purple-700_6d3e9",
  "md:active:bg-purple-800": "_md:active:bg-purple-800_6d3e9",
  "md:active:bg-purple-900": "_md:active:bg-purple-900_6d3e9",
  "md:active:bg-pink-100": "_md:active:bg-pink-100_6d3e9",
  "md:active:bg-pink-200": "_md:active:bg-pink-200_6d3e9",
  "md:active:bg-pink-300": "_md:active:bg-pink-300_6d3e9",
  "md:active:bg-pink-400": "_md:active:bg-pink-400_6d3e9",
  "md:active:bg-pink-500": "_md:active:bg-pink-500_6d3e9",
  "md:active:bg-pink-600": "_md:active:bg-pink-600_6d3e9",
  "md:active:bg-pink-700": "_md:active:bg-pink-700_6d3e9",
  "md:active:bg-pink-800": "_md:active:bg-pink-800_6d3e9",
  "md:active:bg-pink-900": "_md:active:bg-pink-900_6d3e9",
  "md:active:bg-primary-100": "_md:active:bg-primary-100_6d3e9",
  "md:active:bg-primary-200": "_md:active:bg-primary-200_6d3e9",
  "md:active:bg-primary-300": "_md:active:bg-primary-300_6d3e9",
  "md:active:bg-primary-400": "_md:active:bg-primary-400_6d3e9",
  "md:active:bg-primary-500": "_md:active:bg-primary-500_6d3e9",
  "md:active:bg-primary-600": "_md:active:bg-primary-600_6d3e9",
  "md:active:bg-primary-700": "_md:active:bg-primary-700_6d3e9",
  "md:active:bg-primary-800": "_md:active:bg-primary-800_6d3e9",
  "md:active:bg-primary-900": "_md:active:bg-primary-900_6d3e9",
  "md:active:bg-secondary-100": "_md:active:bg-secondary-100_6d3e9",
  "md:active:bg-secondary-200": "_md:active:bg-secondary-200_6d3e9",
  "md:active:bg-secondary-300": "_md:active:bg-secondary-300_6d3e9",
  "md:active:bg-secondary-400": "_md:active:bg-secondary-400_6d3e9",
  "md:active:bg-secondary-500": "_md:active:bg-secondary-500_6d3e9",
  "md:active:bg-secondary-600": "_md:active:bg-secondary-600_6d3e9",
  "md:active:bg-secondary-700": "_md:active:bg-secondary-700_6d3e9",
  "md:active:bg-secondary-800": "_md:active:bg-secondary-800_6d3e9",
  "md:active:bg-secondary-900": "_md:active:bg-secondary-900_6d3e9",
  "md:bg-bottom": "_md:bg-bottom_6d3e9",
  "md:bg-center": "_md:bg-center_6d3e9",
  "md:bg-left": "_md:bg-left_6d3e9",
  "md:bg-left-bottom": "_md:bg-left-bottom_6d3e9",
  "md:bg-left-top": "_md:bg-left-top_6d3e9",
  "md:bg-right": "_md:bg-right_6d3e9",
  "md:bg-right-bottom": "_md:bg-right-bottom_6d3e9",
  "md:bg-right-top": "_md:bg-right-top_6d3e9",
  "md:bg-top": "_md:bg-top_6d3e9",
  "md:bg-repeat": "_md:bg-repeat_6d3e9",
  "md:bg-no-repeat": "_md:bg-no-repeat_6d3e9",
  "md:bg-repeat-x": "_md:bg-repeat-x_6d3e9",
  "md:bg-repeat-y": "_md:bg-repeat-y_6d3e9",
  "md:bg-repeat-round": "_md:bg-repeat-round_6d3e9",
  "md:bg-repeat-space": "_md:bg-repeat-space_6d3e9",
  "md:bg-auto": "_md:bg-auto_6d3e9",
  "md:bg-cover": "_md:bg-cover_6d3e9",
  "md:bg-contain": "_md:bg-contain_6d3e9",
  "md:border-collapse": "_md:border-collapse_6d3e9",
  "md:border-separate": "_md:border-separate_6d3e9",
  "md:border-transparent": "_md:border-transparent_6d3e9",
  "md:border-black": "_md:border-black_6d3e9",
  "md:border-white": "_md:border-white_6d3e9",
  "md:border-gray-100": "_md:border-gray-100_6d3e9",
  "md:border-gray-200": "_md:border-gray-200_6d3e9",
  "md:border-gray-300": "_md:border-gray-300_6d3e9",
  "md:border-gray-400": "_md:border-gray-400_6d3e9",
  "md:border-gray-500": "_md:border-gray-500_6d3e9",
  "md:border-gray-600": "_md:border-gray-600_6d3e9",
  "md:border-gray-700": "_md:border-gray-700_6d3e9",
  "md:border-gray-800": "_md:border-gray-800_6d3e9",
  "md:border-gray-900": "_md:border-gray-900_6d3e9",
  "md:border-red-100": "_md:border-red-100_6d3e9",
  "md:border-red-200": "_md:border-red-200_6d3e9",
  "md:border-red-300": "_md:border-red-300_6d3e9",
  "md:border-red-400": "_md:border-red-400_6d3e9",
  "md:border-red-500": "_md:border-red-500_6d3e9",
  "md:border-red-600": "_md:border-red-600_6d3e9",
  "md:border-red-700": "_md:border-red-700_6d3e9",
  "md:border-red-800": "_md:border-red-800_6d3e9",
  "md:border-red-900": "_md:border-red-900_6d3e9",
  "md:border-orange-100": "_md:border-orange-100_6d3e9",
  "md:border-orange-200": "_md:border-orange-200_6d3e9",
  "md:border-orange-300": "_md:border-orange-300_6d3e9",
  "md:border-orange-400": "_md:border-orange-400_6d3e9",
  "md:border-orange-500": "_md:border-orange-500_6d3e9",
  "md:border-orange-600": "_md:border-orange-600_6d3e9",
  "md:border-orange-700": "_md:border-orange-700_6d3e9",
  "md:border-orange-800": "_md:border-orange-800_6d3e9",
  "md:border-orange-900": "_md:border-orange-900_6d3e9",
  "md:border-yellow-100": "_md:border-yellow-100_6d3e9",
  "md:border-yellow-200": "_md:border-yellow-200_6d3e9",
  "md:border-yellow-300": "_md:border-yellow-300_6d3e9",
  "md:border-yellow-400": "_md:border-yellow-400_6d3e9",
  "md:border-yellow-500": "_md:border-yellow-500_6d3e9",
  "md:border-yellow-600": "_md:border-yellow-600_6d3e9",
  "md:border-yellow-700": "_md:border-yellow-700_6d3e9",
  "md:border-yellow-800": "_md:border-yellow-800_6d3e9",
  "md:border-yellow-900": "_md:border-yellow-900_6d3e9",
  "md:border-green-100": "_md:border-green-100_6d3e9",
  "md:border-green-200": "_md:border-green-200_6d3e9",
  "md:border-green-300": "_md:border-green-300_6d3e9",
  "md:border-green-400": "_md:border-green-400_6d3e9",
  "md:border-green-500": "_md:border-green-500_6d3e9",
  "md:border-green-600": "_md:border-green-600_6d3e9",
  "md:border-green-700": "_md:border-green-700_6d3e9",
  "md:border-green-800": "_md:border-green-800_6d3e9",
  "md:border-green-900": "_md:border-green-900_6d3e9",
  "md:border-teal-100": "_md:border-teal-100_6d3e9",
  "md:border-teal-200": "_md:border-teal-200_6d3e9",
  "md:border-teal-300": "_md:border-teal-300_6d3e9",
  "md:border-teal-400": "_md:border-teal-400_6d3e9",
  "md:border-teal-500": "_md:border-teal-500_6d3e9",
  "md:border-teal-600": "_md:border-teal-600_6d3e9",
  "md:border-teal-700": "_md:border-teal-700_6d3e9",
  "md:border-teal-800": "_md:border-teal-800_6d3e9",
  "md:border-teal-900": "_md:border-teal-900_6d3e9",
  "md:border-blue-100": "_md:border-blue-100_6d3e9",
  "md:border-blue-200": "_md:border-blue-200_6d3e9",
  "md:border-blue-300": "_md:border-blue-300_6d3e9",
  "md:border-blue-400": "_md:border-blue-400_6d3e9",
  "md:border-blue-500": "_md:border-blue-500_6d3e9",
  "md:border-blue-600": "_md:border-blue-600_6d3e9",
  "md:border-blue-700": "_md:border-blue-700_6d3e9",
  "md:border-blue-800": "_md:border-blue-800_6d3e9",
  "md:border-blue-900": "_md:border-blue-900_6d3e9",
  "md:border-indigo-100": "_md:border-indigo-100_6d3e9",
  "md:border-indigo-200": "_md:border-indigo-200_6d3e9",
  "md:border-indigo-300": "_md:border-indigo-300_6d3e9",
  "md:border-indigo-400": "_md:border-indigo-400_6d3e9",
  "md:border-indigo-500": "_md:border-indigo-500_6d3e9",
  "md:border-indigo-600": "_md:border-indigo-600_6d3e9",
  "md:border-indigo-700": "_md:border-indigo-700_6d3e9",
  "md:border-indigo-800": "_md:border-indigo-800_6d3e9",
  "md:border-indigo-900": "_md:border-indigo-900_6d3e9",
  "md:border-purple-100": "_md:border-purple-100_6d3e9",
  "md:border-purple-200": "_md:border-purple-200_6d3e9",
  "md:border-purple-300": "_md:border-purple-300_6d3e9",
  "md:border-purple-400": "_md:border-purple-400_6d3e9",
  "md:border-purple-500": "_md:border-purple-500_6d3e9",
  "md:border-purple-600": "_md:border-purple-600_6d3e9",
  "md:border-purple-700": "_md:border-purple-700_6d3e9",
  "md:border-purple-800": "_md:border-purple-800_6d3e9",
  "md:border-purple-900": "_md:border-purple-900_6d3e9",
  "md:border-pink-100": "_md:border-pink-100_6d3e9",
  "md:border-pink-200": "_md:border-pink-200_6d3e9",
  "md:border-pink-300": "_md:border-pink-300_6d3e9",
  "md:border-pink-400": "_md:border-pink-400_6d3e9",
  "md:border-pink-500": "_md:border-pink-500_6d3e9",
  "md:border-pink-600": "_md:border-pink-600_6d3e9",
  "md:border-pink-700": "_md:border-pink-700_6d3e9",
  "md:border-pink-800": "_md:border-pink-800_6d3e9",
  "md:border-pink-900": "_md:border-pink-900_6d3e9",
  "md:border-primary-100": "_md:border-primary-100_6d3e9",
  "md:border-primary-200": "_md:border-primary-200_6d3e9",
  "md:border-primary-300": "_md:border-primary-300_6d3e9",
  "md:border-primary-400": "_md:border-primary-400_6d3e9",
  "md:border-primary-500": "_md:border-primary-500_6d3e9",
  "md:border-primary-600": "_md:border-primary-600_6d3e9",
  "md:border-primary-700": "_md:border-primary-700_6d3e9",
  "md:border-primary-800": "_md:border-primary-800_6d3e9",
  "md:border-primary-900": "_md:border-primary-900_6d3e9",
  "md:border-secondary-100": "_md:border-secondary-100_6d3e9",
  "md:border-secondary-200": "_md:border-secondary-200_6d3e9",
  "md:border-secondary-300": "_md:border-secondary-300_6d3e9",
  "md:border-secondary-400": "_md:border-secondary-400_6d3e9",
  "md:border-secondary-500": "_md:border-secondary-500_6d3e9",
  "md:border-secondary-600": "_md:border-secondary-600_6d3e9",
  "md:border-secondary-700": "_md:border-secondary-700_6d3e9",
  "md:border-secondary-800": "_md:border-secondary-800_6d3e9",
  "md:border-secondary-900": "_md:border-secondary-900_6d3e9",
  "md:hover:border-transparent": "_md:hover:border-transparent_6d3e9",
  "md:hover:border-black": "_md:hover:border-black_6d3e9",
  "md:hover:border-white": "_md:hover:border-white_6d3e9",
  "md:hover:border-gray-100": "_md:hover:border-gray-100_6d3e9",
  "md:hover:border-gray-200": "_md:hover:border-gray-200_6d3e9",
  "md:hover:border-gray-300": "_md:hover:border-gray-300_6d3e9",
  "md:hover:border-gray-400": "_md:hover:border-gray-400_6d3e9",
  "md:hover:border-gray-500": "_md:hover:border-gray-500_6d3e9",
  "md:hover:border-gray-600": "_md:hover:border-gray-600_6d3e9",
  "md:hover:border-gray-700": "_md:hover:border-gray-700_6d3e9",
  "md:hover:border-gray-800": "_md:hover:border-gray-800_6d3e9",
  "md:hover:border-gray-900": "_md:hover:border-gray-900_6d3e9",
  "md:hover:border-red-100": "_md:hover:border-red-100_6d3e9",
  "md:hover:border-red-200": "_md:hover:border-red-200_6d3e9",
  "md:hover:border-red-300": "_md:hover:border-red-300_6d3e9",
  "md:hover:border-red-400": "_md:hover:border-red-400_6d3e9",
  "md:hover:border-red-500": "_md:hover:border-red-500_6d3e9",
  "md:hover:border-red-600": "_md:hover:border-red-600_6d3e9",
  "md:hover:border-red-700": "_md:hover:border-red-700_6d3e9",
  "md:hover:border-red-800": "_md:hover:border-red-800_6d3e9",
  "md:hover:border-red-900": "_md:hover:border-red-900_6d3e9",
  "md:hover:border-orange-100": "_md:hover:border-orange-100_6d3e9",
  "md:hover:border-orange-200": "_md:hover:border-orange-200_6d3e9",
  "md:hover:border-orange-300": "_md:hover:border-orange-300_6d3e9",
  "md:hover:border-orange-400": "_md:hover:border-orange-400_6d3e9",
  "md:hover:border-orange-500": "_md:hover:border-orange-500_6d3e9",
  "md:hover:border-orange-600": "_md:hover:border-orange-600_6d3e9",
  "md:hover:border-orange-700": "_md:hover:border-orange-700_6d3e9",
  "md:hover:border-orange-800": "_md:hover:border-orange-800_6d3e9",
  "md:hover:border-orange-900": "_md:hover:border-orange-900_6d3e9",
  "md:hover:border-yellow-100": "_md:hover:border-yellow-100_6d3e9",
  "md:hover:border-yellow-200": "_md:hover:border-yellow-200_6d3e9",
  "md:hover:border-yellow-300": "_md:hover:border-yellow-300_6d3e9",
  "md:hover:border-yellow-400": "_md:hover:border-yellow-400_6d3e9",
  "md:hover:border-yellow-500": "_md:hover:border-yellow-500_6d3e9",
  "md:hover:border-yellow-600": "_md:hover:border-yellow-600_6d3e9",
  "md:hover:border-yellow-700": "_md:hover:border-yellow-700_6d3e9",
  "md:hover:border-yellow-800": "_md:hover:border-yellow-800_6d3e9",
  "md:hover:border-yellow-900": "_md:hover:border-yellow-900_6d3e9",
  "md:hover:border-green-100": "_md:hover:border-green-100_6d3e9",
  "md:hover:border-green-200": "_md:hover:border-green-200_6d3e9",
  "md:hover:border-green-300": "_md:hover:border-green-300_6d3e9",
  "md:hover:border-green-400": "_md:hover:border-green-400_6d3e9",
  "md:hover:border-green-500": "_md:hover:border-green-500_6d3e9",
  "md:hover:border-green-600": "_md:hover:border-green-600_6d3e9",
  "md:hover:border-green-700": "_md:hover:border-green-700_6d3e9",
  "md:hover:border-green-800": "_md:hover:border-green-800_6d3e9",
  "md:hover:border-green-900": "_md:hover:border-green-900_6d3e9",
  "md:hover:border-teal-100": "_md:hover:border-teal-100_6d3e9",
  "md:hover:border-teal-200": "_md:hover:border-teal-200_6d3e9",
  "md:hover:border-teal-300": "_md:hover:border-teal-300_6d3e9",
  "md:hover:border-teal-400": "_md:hover:border-teal-400_6d3e9",
  "md:hover:border-teal-500": "_md:hover:border-teal-500_6d3e9",
  "md:hover:border-teal-600": "_md:hover:border-teal-600_6d3e9",
  "md:hover:border-teal-700": "_md:hover:border-teal-700_6d3e9",
  "md:hover:border-teal-800": "_md:hover:border-teal-800_6d3e9",
  "md:hover:border-teal-900": "_md:hover:border-teal-900_6d3e9",
  "md:hover:border-blue-100": "_md:hover:border-blue-100_6d3e9",
  "md:hover:border-blue-200": "_md:hover:border-blue-200_6d3e9",
  "md:hover:border-blue-300": "_md:hover:border-blue-300_6d3e9",
  "md:hover:border-blue-400": "_md:hover:border-blue-400_6d3e9",
  "md:hover:border-blue-500": "_md:hover:border-blue-500_6d3e9",
  "md:hover:border-blue-600": "_md:hover:border-blue-600_6d3e9",
  "md:hover:border-blue-700": "_md:hover:border-blue-700_6d3e9",
  "md:hover:border-blue-800": "_md:hover:border-blue-800_6d3e9",
  "md:hover:border-blue-900": "_md:hover:border-blue-900_6d3e9",
  "md:hover:border-indigo-100": "_md:hover:border-indigo-100_6d3e9",
  "md:hover:border-indigo-200": "_md:hover:border-indigo-200_6d3e9",
  "md:hover:border-indigo-300": "_md:hover:border-indigo-300_6d3e9",
  "md:hover:border-indigo-400": "_md:hover:border-indigo-400_6d3e9",
  "md:hover:border-indigo-500": "_md:hover:border-indigo-500_6d3e9",
  "md:hover:border-indigo-600": "_md:hover:border-indigo-600_6d3e9",
  "md:hover:border-indigo-700": "_md:hover:border-indigo-700_6d3e9",
  "md:hover:border-indigo-800": "_md:hover:border-indigo-800_6d3e9",
  "md:hover:border-indigo-900": "_md:hover:border-indigo-900_6d3e9",
  "md:hover:border-purple-100": "_md:hover:border-purple-100_6d3e9",
  "md:hover:border-purple-200": "_md:hover:border-purple-200_6d3e9",
  "md:hover:border-purple-300": "_md:hover:border-purple-300_6d3e9",
  "md:hover:border-purple-400": "_md:hover:border-purple-400_6d3e9",
  "md:hover:border-purple-500": "_md:hover:border-purple-500_6d3e9",
  "md:hover:border-purple-600": "_md:hover:border-purple-600_6d3e9",
  "md:hover:border-purple-700": "_md:hover:border-purple-700_6d3e9",
  "md:hover:border-purple-800": "_md:hover:border-purple-800_6d3e9",
  "md:hover:border-purple-900": "_md:hover:border-purple-900_6d3e9",
  "md:hover:border-pink-100": "_md:hover:border-pink-100_6d3e9",
  "md:hover:border-pink-200": "_md:hover:border-pink-200_6d3e9",
  "md:hover:border-pink-300": "_md:hover:border-pink-300_6d3e9",
  "md:hover:border-pink-400": "_md:hover:border-pink-400_6d3e9",
  "md:hover:border-pink-500": "_md:hover:border-pink-500_6d3e9",
  "md:hover:border-pink-600": "_md:hover:border-pink-600_6d3e9",
  "md:hover:border-pink-700": "_md:hover:border-pink-700_6d3e9",
  "md:hover:border-pink-800": "_md:hover:border-pink-800_6d3e9",
  "md:hover:border-pink-900": "_md:hover:border-pink-900_6d3e9",
  "md:hover:border-primary-100": "_md:hover:border-primary-100_6d3e9",
  "md:hover:border-primary-200": "_md:hover:border-primary-200_6d3e9",
  "md:hover:border-primary-300": "_md:hover:border-primary-300_6d3e9",
  "md:hover:border-primary-400": "_md:hover:border-primary-400_6d3e9",
  "md:hover:border-primary-500": "_md:hover:border-primary-500_6d3e9",
  "md:hover:border-primary-600": "_md:hover:border-primary-600_6d3e9",
  "md:hover:border-primary-700": "_md:hover:border-primary-700_6d3e9",
  "md:hover:border-primary-800": "_md:hover:border-primary-800_6d3e9",
  "md:hover:border-primary-900": "_md:hover:border-primary-900_6d3e9",
  "md:hover:border-secondary-100": "_md:hover:border-secondary-100_6d3e9",
  "md:hover:border-secondary-200": "_md:hover:border-secondary-200_6d3e9",
  "md:hover:border-secondary-300": "_md:hover:border-secondary-300_6d3e9",
  "md:hover:border-secondary-400": "_md:hover:border-secondary-400_6d3e9",
  "md:hover:border-secondary-500": "_md:hover:border-secondary-500_6d3e9",
  "md:hover:border-secondary-600": "_md:hover:border-secondary-600_6d3e9",
  "md:hover:border-secondary-700": "_md:hover:border-secondary-700_6d3e9",
  "md:hover:border-secondary-800": "_md:hover:border-secondary-800_6d3e9",
  "md:hover:border-secondary-900": "_md:hover:border-secondary-900_6d3e9",
  "md:focus:border-transparent": "_md:focus:border-transparent_6d3e9",
  "md:focus:border-black": "_md:focus:border-black_6d3e9",
  "md:focus:border-white": "_md:focus:border-white_6d3e9",
  "md:focus:border-gray-100": "_md:focus:border-gray-100_6d3e9",
  "md:focus:border-gray-200": "_md:focus:border-gray-200_6d3e9",
  "md:focus:border-gray-300": "_md:focus:border-gray-300_6d3e9",
  "md:focus:border-gray-400": "_md:focus:border-gray-400_6d3e9",
  "md:focus:border-gray-500": "_md:focus:border-gray-500_6d3e9",
  "md:focus:border-gray-600": "_md:focus:border-gray-600_6d3e9",
  "md:focus:border-gray-700": "_md:focus:border-gray-700_6d3e9",
  "md:focus:border-gray-800": "_md:focus:border-gray-800_6d3e9",
  "md:focus:border-gray-900": "_md:focus:border-gray-900_6d3e9",
  "md:focus:border-red-100": "_md:focus:border-red-100_6d3e9",
  "md:focus:border-red-200": "_md:focus:border-red-200_6d3e9",
  "md:focus:border-red-300": "_md:focus:border-red-300_6d3e9",
  "md:focus:border-red-400": "_md:focus:border-red-400_6d3e9",
  "md:focus:border-red-500": "_md:focus:border-red-500_6d3e9",
  "md:focus:border-red-600": "_md:focus:border-red-600_6d3e9",
  "md:focus:border-red-700": "_md:focus:border-red-700_6d3e9",
  "md:focus:border-red-800": "_md:focus:border-red-800_6d3e9",
  "md:focus:border-red-900": "_md:focus:border-red-900_6d3e9",
  "md:focus:border-orange-100": "_md:focus:border-orange-100_6d3e9",
  "md:focus:border-orange-200": "_md:focus:border-orange-200_6d3e9",
  "md:focus:border-orange-300": "_md:focus:border-orange-300_6d3e9",
  "md:focus:border-orange-400": "_md:focus:border-orange-400_6d3e9",
  "md:focus:border-orange-500": "_md:focus:border-orange-500_6d3e9",
  "md:focus:border-orange-600": "_md:focus:border-orange-600_6d3e9",
  "md:focus:border-orange-700": "_md:focus:border-orange-700_6d3e9",
  "md:focus:border-orange-800": "_md:focus:border-orange-800_6d3e9",
  "md:focus:border-orange-900": "_md:focus:border-orange-900_6d3e9",
  "md:focus:border-yellow-100": "_md:focus:border-yellow-100_6d3e9",
  "md:focus:border-yellow-200": "_md:focus:border-yellow-200_6d3e9",
  "md:focus:border-yellow-300": "_md:focus:border-yellow-300_6d3e9",
  "md:focus:border-yellow-400": "_md:focus:border-yellow-400_6d3e9",
  "md:focus:border-yellow-500": "_md:focus:border-yellow-500_6d3e9",
  "md:focus:border-yellow-600": "_md:focus:border-yellow-600_6d3e9",
  "md:focus:border-yellow-700": "_md:focus:border-yellow-700_6d3e9",
  "md:focus:border-yellow-800": "_md:focus:border-yellow-800_6d3e9",
  "md:focus:border-yellow-900": "_md:focus:border-yellow-900_6d3e9",
  "md:focus:border-green-100": "_md:focus:border-green-100_6d3e9",
  "md:focus:border-green-200": "_md:focus:border-green-200_6d3e9",
  "md:focus:border-green-300": "_md:focus:border-green-300_6d3e9",
  "md:focus:border-green-400": "_md:focus:border-green-400_6d3e9",
  "md:focus:border-green-500": "_md:focus:border-green-500_6d3e9",
  "md:focus:border-green-600": "_md:focus:border-green-600_6d3e9",
  "md:focus:border-green-700": "_md:focus:border-green-700_6d3e9",
  "md:focus:border-green-800": "_md:focus:border-green-800_6d3e9",
  "md:focus:border-green-900": "_md:focus:border-green-900_6d3e9",
  "md:focus:border-teal-100": "_md:focus:border-teal-100_6d3e9",
  "md:focus:border-teal-200": "_md:focus:border-teal-200_6d3e9",
  "md:focus:border-teal-300": "_md:focus:border-teal-300_6d3e9",
  "md:focus:border-teal-400": "_md:focus:border-teal-400_6d3e9",
  "md:focus:border-teal-500": "_md:focus:border-teal-500_6d3e9",
  "md:focus:border-teal-600": "_md:focus:border-teal-600_6d3e9",
  "md:focus:border-teal-700": "_md:focus:border-teal-700_6d3e9",
  "md:focus:border-teal-800": "_md:focus:border-teal-800_6d3e9",
  "md:focus:border-teal-900": "_md:focus:border-teal-900_6d3e9",
  "md:focus:border-blue-100": "_md:focus:border-blue-100_6d3e9",
  "md:focus:border-blue-200": "_md:focus:border-blue-200_6d3e9",
  "md:focus:border-blue-300": "_md:focus:border-blue-300_6d3e9",
  "md:focus:border-blue-400": "_md:focus:border-blue-400_6d3e9",
  "md:focus:border-blue-500": "_md:focus:border-blue-500_6d3e9",
  "md:focus:border-blue-600": "_md:focus:border-blue-600_6d3e9",
  "md:focus:border-blue-700": "_md:focus:border-blue-700_6d3e9",
  "md:focus:border-blue-800": "_md:focus:border-blue-800_6d3e9",
  "md:focus:border-blue-900": "_md:focus:border-blue-900_6d3e9",
  "md:focus:border-indigo-100": "_md:focus:border-indigo-100_6d3e9",
  "md:focus:border-indigo-200": "_md:focus:border-indigo-200_6d3e9",
  "md:focus:border-indigo-300": "_md:focus:border-indigo-300_6d3e9",
  "md:focus:border-indigo-400": "_md:focus:border-indigo-400_6d3e9",
  "md:focus:border-indigo-500": "_md:focus:border-indigo-500_6d3e9",
  "md:focus:border-indigo-600": "_md:focus:border-indigo-600_6d3e9",
  "md:focus:border-indigo-700": "_md:focus:border-indigo-700_6d3e9",
  "md:focus:border-indigo-800": "_md:focus:border-indigo-800_6d3e9",
  "md:focus:border-indigo-900": "_md:focus:border-indigo-900_6d3e9",
  "md:focus:border-purple-100": "_md:focus:border-purple-100_6d3e9",
  "md:focus:border-purple-200": "_md:focus:border-purple-200_6d3e9",
  "md:focus:border-purple-300": "_md:focus:border-purple-300_6d3e9",
  "md:focus:border-purple-400": "_md:focus:border-purple-400_6d3e9",
  "md:focus:border-purple-500": "_md:focus:border-purple-500_6d3e9",
  "md:focus:border-purple-600": "_md:focus:border-purple-600_6d3e9",
  "md:focus:border-purple-700": "_md:focus:border-purple-700_6d3e9",
  "md:focus:border-purple-800": "_md:focus:border-purple-800_6d3e9",
  "md:focus:border-purple-900": "_md:focus:border-purple-900_6d3e9",
  "md:focus:border-pink-100": "_md:focus:border-pink-100_6d3e9",
  "md:focus:border-pink-200": "_md:focus:border-pink-200_6d3e9",
  "md:focus:border-pink-300": "_md:focus:border-pink-300_6d3e9",
  "md:focus:border-pink-400": "_md:focus:border-pink-400_6d3e9",
  "md:focus:border-pink-500": "_md:focus:border-pink-500_6d3e9",
  "md:focus:border-pink-600": "_md:focus:border-pink-600_6d3e9",
  "md:focus:border-pink-700": "_md:focus:border-pink-700_6d3e9",
  "md:focus:border-pink-800": "_md:focus:border-pink-800_6d3e9",
  "md:focus:border-pink-900": "_md:focus:border-pink-900_6d3e9",
  "md:focus:border-primary-100": "_md:focus:border-primary-100_6d3e9",
  "md:focus:border-primary-200": "_md:focus:border-primary-200_6d3e9",
  "md:focus:border-primary-300": "_md:focus:border-primary-300_6d3e9",
  "md:focus:border-primary-400": "_md:focus:border-primary-400_6d3e9",
  "md:focus:border-primary-500": "_md:focus:border-primary-500_6d3e9",
  "md:focus:border-primary-600": "_md:focus:border-primary-600_6d3e9",
  "md:focus:border-primary-700": "_md:focus:border-primary-700_6d3e9",
  "md:focus:border-primary-800": "_md:focus:border-primary-800_6d3e9",
  "md:focus:border-primary-900": "_md:focus:border-primary-900_6d3e9",
  "md:focus:border-secondary-100": "_md:focus:border-secondary-100_6d3e9",
  "md:focus:border-secondary-200": "_md:focus:border-secondary-200_6d3e9",
  "md:focus:border-secondary-300": "_md:focus:border-secondary-300_6d3e9",
  "md:focus:border-secondary-400": "_md:focus:border-secondary-400_6d3e9",
  "md:focus:border-secondary-500": "_md:focus:border-secondary-500_6d3e9",
  "md:focus:border-secondary-600": "_md:focus:border-secondary-600_6d3e9",
  "md:focus:border-secondary-700": "_md:focus:border-secondary-700_6d3e9",
  "md:focus:border-secondary-800": "_md:focus:border-secondary-800_6d3e9",
  "md:focus:border-secondary-900": "_md:focus:border-secondary-900_6d3e9",
  "md:rounded-none": "_md:rounded-none_6d3e9",
  "md:rounded-sm": "_md:rounded-sm_6d3e9",
  "md:rounded": "_md:rounded_6d3e9",
  "md:rounded-lg": "_md:rounded-lg_6d3e9",
  "md:rounded-full": "_md:rounded-full_6d3e9",
  "md:rounded-t-none": "_md:rounded-t-none_6d3e9",
  "md:rounded-r-none": "_md:rounded-r-none_6d3e9",
  "md:rounded-b-none": "_md:rounded-b-none_6d3e9",
  "md:rounded-l-none": "_md:rounded-l-none_6d3e9",
  "md:rounded-t-sm": "_md:rounded-t-sm_6d3e9",
  "md:rounded-r-sm": "_md:rounded-r-sm_6d3e9",
  "md:rounded-b-sm": "_md:rounded-b-sm_6d3e9",
  "md:rounded-l-sm": "_md:rounded-l-sm_6d3e9",
  "md:rounded-t": "_md:rounded-t_6d3e9",
  "md:rounded-r": "_md:rounded-r_6d3e9",
  "md:rounded-b": "_md:rounded-b_6d3e9",
  "md:rounded-l": "_md:rounded-l_6d3e9",
  "md:rounded-t-lg": "_md:rounded-t-lg_6d3e9",
  "md:rounded-r-lg": "_md:rounded-r-lg_6d3e9",
  "md:rounded-b-lg": "_md:rounded-b-lg_6d3e9",
  "md:rounded-l-lg": "_md:rounded-l-lg_6d3e9",
  "md:rounded-t-full": "_md:rounded-t-full_6d3e9",
  "md:rounded-r-full": "_md:rounded-r-full_6d3e9",
  "md:rounded-b-full": "_md:rounded-b-full_6d3e9",
  "md:rounded-l-full": "_md:rounded-l-full_6d3e9",
  "md:rounded-tl-none": "_md:rounded-tl-none_6d3e9",
  "md:rounded-tr-none": "_md:rounded-tr-none_6d3e9",
  "md:rounded-br-none": "_md:rounded-br-none_6d3e9",
  "md:rounded-bl-none": "_md:rounded-bl-none_6d3e9",
  "md:rounded-tl-sm": "_md:rounded-tl-sm_6d3e9",
  "md:rounded-tr-sm": "_md:rounded-tr-sm_6d3e9",
  "md:rounded-br-sm": "_md:rounded-br-sm_6d3e9",
  "md:rounded-bl-sm": "_md:rounded-bl-sm_6d3e9",
  "md:rounded-tl": "_md:rounded-tl_6d3e9",
  "md:rounded-tr": "_md:rounded-tr_6d3e9",
  "md:rounded-br": "_md:rounded-br_6d3e9",
  "md:rounded-bl": "_md:rounded-bl_6d3e9",
  "md:rounded-tl-lg": "_md:rounded-tl-lg_6d3e9",
  "md:rounded-tr-lg": "_md:rounded-tr-lg_6d3e9",
  "md:rounded-br-lg": "_md:rounded-br-lg_6d3e9",
  "md:rounded-bl-lg": "_md:rounded-bl-lg_6d3e9",
  "md:rounded-tl-full": "_md:rounded-tl-full_6d3e9",
  "md:rounded-tr-full": "_md:rounded-tr-full_6d3e9",
  "md:rounded-br-full": "_md:rounded-br-full_6d3e9",
  "md:rounded-bl-full": "_md:rounded-bl-full_6d3e9",
  "md:border-solid": "_md:border-solid_6d3e9",
  "md:border-dashed": "_md:border-dashed_6d3e9",
  "md:border-dotted": "_md:border-dotted_6d3e9",
  "md:border-double": "_md:border-double_6d3e9",
  "md:border-none": "_md:border-none_6d3e9",
  "md:border-0": "_md:border-0_6d3e9",
  "md:border-2": "_md:border-2_6d3e9",
  "md:border-4": "_md:border-4_6d3e9",
  "md:border-8": "_md:border-8_6d3e9",
  "md:border": "_md:border_6d3e9",
  "md:border-t-0": "_md:border-t-0_6d3e9",
  "md:border-r-0": "_md:border-r-0_6d3e9",
  "md:border-b-0": "_md:border-b-0_6d3e9",
  "md:border-l-0": "_md:border-l-0_6d3e9",
  "md:border-t-2": "_md:border-t-2_6d3e9",
  "md:border-r-2": "_md:border-r-2_6d3e9",
  "md:border-b-2": "_md:border-b-2_6d3e9",
  "md:border-l-2": "_md:border-l-2_6d3e9",
  "md:border-t-4": "_md:border-t-4_6d3e9",
  "md:border-r-4": "_md:border-r-4_6d3e9",
  "md:border-b-4": "_md:border-b-4_6d3e9",
  "md:border-l-4": "_md:border-l-4_6d3e9",
  "md:border-t-8": "_md:border-t-8_6d3e9",
  "md:border-r-8": "_md:border-r-8_6d3e9",
  "md:border-b-8": "_md:border-b-8_6d3e9",
  "md:border-l-8": "_md:border-l-8_6d3e9",
  "md:border-t": "_md:border-t_6d3e9",
  "md:border-r": "_md:border-r_6d3e9",
  "md:border-b": "_md:border-b_6d3e9",
  "md:border-l": "_md:border-l_6d3e9",
  "md:first:border-0": "_md:first:border-0_6d3e9",
  "md:first:border-2": "_md:first:border-2_6d3e9",
  "md:first:border-4": "_md:first:border-4_6d3e9",
  "md:first:border-8": "_md:first:border-8_6d3e9",
  "md:first:border": "_md:first:border_6d3e9",
  "md:first:border-t-0": "_md:first:border-t-0_6d3e9",
  "md:first:border-r-0": "_md:first:border-r-0_6d3e9",
  "md:first:border-b-0": "_md:first:border-b-0_6d3e9",
  "md:first:border-l-0": "_md:first:border-l-0_6d3e9",
  "md:first:border-t-2": "_md:first:border-t-2_6d3e9",
  "md:first:border-r-2": "_md:first:border-r-2_6d3e9",
  "md:first:border-b-2": "_md:first:border-b-2_6d3e9",
  "md:first:border-l-2": "_md:first:border-l-2_6d3e9",
  "md:first:border-t-4": "_md:first:border-t-4_6d3e9",
  "md:first:border-r-4": "_md:first:border-r-4_6d3e9",
  "md:first:border-b-4": "_md:first:border-b-4_6d3e9",
  "md:first:border-l-4": "_md:first:border-l-4_6d3e9",
  "md:first:border-t-8": "_md:first:border-t-8_6d3e9",
  "md:first:border-r-8": "_md:first:border-r-8_6d3e9",
  "md:first:border-b-8": "_md:first:border-b-8_6d3e9",
  "md:first:border-l-8": "_md:first:border-l-8_6d3e9",
  "md:first:border-t": "_md:first:border-t_6d3e9",
  "md:first:border-r": "_md:first:border-r_6d3e9",
  "md:first:border-b": "_md:first:border-b_6d3e9",
  "md:first:border-l": "_md:first:border-l_6d3e9",
  "md:hover:border-0": "_md:hover:border-0_6d3e9",
  "md:hover:border-2": "_md:hover:border-2_6d3e9",
  "md:hover:border-4": "_md:hover:border-4_6d3e9",
  "md:hover:border-8": "_md:hover:border-8_6d3e9",
  "md:hover:border": "_md:hover:border_6d3e9",
  "md:hover:border-t-0": "_md:hover:border-t-0_6d3e9",
  "md:hover:border-r-0": "_md:hover:border-r-0_6d3e9",
  "md:hover:border-b-0": "_md:hover:border-b-0_6d3e9",
  "md:hover:border-l-0": "_md:hover:border-l-0_6d3e9",
  "md:hover:border-t-2": "_md:hover:border-t-2_6d3e9",
  "md:hover:border-r-2": "_md:hover:border-r-2_6d3e9",
  "md:hover:border-b-2": "_md:hover:border-b-2_6d3e9",
  "md:hover:border-l-2": "_md:hover:border-l-2_6d3e9",
  "md:hover:border-t-4": "_md:hover:border-t-4_6d3e9",
  "md:hover:border-r-4": "_md:hover:border-r-4_6d3e9",
  "md:hover:border-b-4": "_md:hover:border-b-4_6d3e9",
  "md:hover:border-l-4": "_md:hover:border-l-4_6d3e9",
  "md:hover:border-t-8": "_md:hover:border-t-8_6d3e9",
  "md:hover:border-r-8": "_md:hover:border-r-8_6d3e9",
  "md:hover:border-b-8": "_md:hover:border-b-8_6d3e9",
  "md:hover:border-l-8": "_md:hover:border-l-8_6d3e9",
  "md:hover:border-t": "_md:hover:border-t_6d3e9",
  "md:hover:border-r": "_md:hover:border-r_6d3e9",
  "md:hover:border-b": "_md:hover:border-b_6d3e9",
  "md:hover:border-l": "_md:hover:border-l_6d3e9",
  "md:focus:border-0": "_md:focus:border-0_6d3e9",
  "md:focus:border-2": "_md:focus:border-2_6d3e9",
  "md:focus:border-4": "_md:focus:border-4_6d3e9",
  "md:focus:border-8": "_md:focus:border-8_6d3e9",
  "md:focus:border": "_md:focus:border_6d3e9",
  "md:focus:border-t-0": "_md:focus:border-t-0_6d3e9",
  "md:focus:border-r-0": "_md:focus:border-r-0_6d3e9",
  "md:focus:border-b-0": "_md:focus:border-b-0_6d3e9",
  "md:focus:border-l-0": "_md:focus:border-l-0_6d3e9",
  "md:focus:border-t-2": "_md:focus:border-t-2_6d3e9",
  "md:focus:border-r-2": "_md:focus:border-r-2_6d3e9",
  "md:focus:border-b-2": "_md:focus:border-b-2_6d3e9",
  "md:focus:border-l-2": "_md:focus:border-l-2_6d3e9",
  "md:focus:border-t-4": "_md:focus:border-t-4_6d3e9",
  "md:focus:border-r-4": "_md:focus:border-r-4_6d3e9",
  "md:focus:border-b-4": "_md:focus:border-b-4_6d3e9",
  "md:focus:border-l-4": "_md:focus:border-l-4_6d3e9",
  "md:focus:border-t-8": "_md:focus:border-t-8_6d3e9",
  "md:focus:border-r-8": "_md:focus:border-r-8_6d3e9",
  "md:focus:border-b-8": "_md:focus:border-b-8_6d3e9",
  "md:focus:border-l-8": "_md:focus:border-l-8_6d3e9",
  "md:focus:border-t": "_md:focus:border-t_6d3e9",
  "md:focus:border-r": "_md:focus:border-r_6d3e9",
  "md:focus:border-b": "_md:focus:border-b_6d3e9",
  "md:focus:border-l": "_md:focus:border-l_6d3e9",
  "md:cursor-auto": "_md:cursor-auto_6d3e9",
  "md:cursor-default": "_md:cursor-default_6d3e9",
  "md:cursor-pointer": "_md:cursor-pointer_6d3e9",
  "md:cursor-wait": "_md:cursor-wait_6d3e9",
  "md:cursor-text": "_md:cursor-text_6d3e9",
  "md:cursor-move": "_md:cursor-move_6d3e9",
  "md:cursor-not-allowed": "_md:cursor-not-allowed_6d3e9",
  "md:block": "_md:block_6d3e9",
  "md:inline-block": "_md:inline-block_6d3e9",
  "md:inline": "_md:inline_6d3e9",
  "md:flex": "_md:flex_6d3e9",
  "md:inline-flex": "_md:inline-flex_6d3e9",
  "md:table": "_md:table_6d3e9",
  "md:table-row": "_md:table-row_6d3e9",
  "md:table-cell": "_md:table-cell_6d3e9",
  "md:hidden": "_md:hidden_6d3e9",
  "md:flex-row": "_md:flex-row_6d3e9",
  "md:flex-row-reverse": "_md:flex-row-reverse_6d3e9",
  "md:flex-col": "_md:flex-col_6d3e9",
  "md:flex-col-reverse": "_md:flex-col-reverse_6d3e9",
  "md:flex-wrap": "_md:flex-wrap_6d3e9",
  "md:flex-wrap-reverse": "_md:flex-wrap-reverse_6d3e9",
  "md:flex-no-wrap": "_md:flex-no-wrap_6d3e9",
  "md:items-start": "_md:items-start_6d3e9",
  "md:items-end": "_md:items-end_6d3e9",
  "md:items-center": "_md:items-center_6d3e9",
  "md:items-baseline": "_md:items-baseline_6d3e9",
  "md:items-stretch": "_md:items-stretch_6d3e9",
  "md:self-auto": "_md:self-auto_6d3e9",
  "md:self-start": "_md:self-start_6d3e9",
  "md:self-end": "_md:self-end_6d3e9",
  "md:self-center": "_md:self-center_6d3e9",
  "md:self-stretch": "_md:self-stretch_6d3e9",
  "md:justify-start": "_md:justify-start_6d3e9",
  "md:justify-end": "_md:justify-end_6d3e9",
  "md:justify-center": "_md:justify-center_6d3e9",
  "md:justify-between": "_md:justify-between_6d3e9",
  "md:justify-around": "_md:justify-around_6d3e9",
  "md:content-center": "_md:content-center_6d3e9",
  "md:content-start": "_md:content-start_6d3e9",
  "md:content-end": "_md:content-end_6d3e9",
  "md:content-between": "_md:content-between_6d3e9",
  "md:content-around": "_md:content-around_6d3e9",
  "md:flex-1": "_md:flex-1_6d3e9",
  "md:flex-auto": "_md:flex-auto_6d3e9",
  "md:flex-initial": "_md:flex-initial_6d3e9",
  "md:flex-none": "_md:flex-none_6d3e9",
  "md:flex-grow-0": "_md:flex-grow-0_6d3e9",
  "md:flex-grow": "_md:flex-grow_6d3e9",
  "md:flex-shrink-0": "_md:flex-shrink-0_6d3e9",
  "md:flex-shrink": "_md:flex-shrink_6d3e9",
  "md:order-1": "_md:order-1_6d3e9",
  "md:order-2": "_md:order-2_6d3e9",
  "md:order-3": "_md:order-3_6d3e9",
  "md:order-4": "_md:order-4_6d3e9",
  "md:order-5": "_md:order-5_6d3e9",
  "md:order-6": "_md:order-6_6d3e9",
  "md:order-7": "_md:order-7_6d3e9",
  "md:order-8": "_md:order-8_6d3e9",
  "md:order-9": "_md:order-9_6d3e9",
  "md:order-10": "_md:order-10_6d3e9",
  "md:order-11": "_md:order-11_6d3e9",
  "md:order-12": "_md:order-12_6d3e9",
  "md:order-first": "_md:order-first_6d3e9",
  "md:order-last": "_md:order-last_6d3e9",
  "md:order-none": "_md:order-none_6d3e9",
  "md:float-right": "_md:float-right_6d3e9",
  "md:float-left": "_md:float-left_6d3e9",
  "md:float-none": "_md:float-none_6d3e9",
  "md:clearfix": "_md:clearfix_6d3e9",
  "md:font-sans": "_md:font-sans_6d3e9",
  "md:font-serif": "_md:font-serif_6d3e9",
  "md:font-mono": "_md:font-mono_6d3e9",
  "md:font-hairline": "_md:font-hairline_6d3e9",
  "md:font-thin": "_md:font-thin_6d3e9",
  "md:font-light": "_md:font-light_6d3e9",
  "md:font-normal": "_md:font-normal_6d3e9",
  "md:font-medium": "_md:font-medium_6d3e9",
  "md:font-semibold": "_md:font-semibold_6d3e9",
  "md:font-bold": "_md:font-bold_6d3e9",
  "md:font-extrabold": "_md:font-extrabold_6d3e9",
  "md:font-black": "_md:font-black_6d3e9",
  "md:hover:font-hairline": "_md:hover:font-hairline_6d3e9",
  "md:hover:font-thin": "_md:hover:font-thin_6d3e9",
  "md:hover:font-light": "_md:hover:font-light_6d3e9",
  "md:hover:font-normal": "_md:hover:font-normal_6d3e9",
  "md:hover:font-medium": "_md:hover:font-medium_6d3e9",
  "md:hover:font-semibold": "_md:hover:font-semibold_6d3e9",
  "md:hover:font-bold": "_md:hover:font-bold_6d3e9",
  "md:hover:font-extrabold": "_md:hover:font-extrabold_6d3e9",
  "md:hover:font-black": "_md:hover:font-black_6d3e9",
  "md:focus:font-hairline": "_md:focus:font-hairline_6d3e9",
  "md:focus:font-thin": "_md:focus:font-thin_6d3e9",
  "md:focus:font-light": "_md:focus:font-light_6d3e9",
  "md:focus:font-normal": "_md:focus:font-normal_6d3e9",
  "md:focus:font-medium": "_md:focus:font-medium_6d3e9",
  "md:focus:font-semibold": "_md:focus:font-semibold_6d3e9",
  "md:focus:font-bold": "_md:focus:font-bold_6d3e9",
  "md:focus:font-extrabold": "_md:focus:font-extrabold_6d3e9",
  "md:focus:font-black": "_md:focus:font-black_6d3e9",
  "md:h-0": "_md:h-0_6d3e9",
  "md:h-1": "_md:h-1_6d3e9",
  "md:h-2": "_md:h-2_6d3e9",
  "md:h-3": "_md:h-3_6d3e9",
  "md:h-4": "_md:h-4_6d3e9",
  "md:h-5": "_md:h-5_6d3e9",
  "md:h-6": "_md:h-6_6d3e9",
  "md:h-8": "_md:h-8_6d3e9",
  "md:h-10": "_md:h-10_6d3e9",
  "md:h-12": "_md:h-12_6d3e9",
  "md:h-16": "_md:h-16_6d3e9",
  "md:h-20": "_md:h-20_6d3e9",
  "md:h-24": "_md:h-24_6d3e9",
  "md:h-32": "_md:h-32_6d3e9",
  "md:h-40": "_md:h-40_6d3e9",
  "md:h-48": "_md:h-48_6d3e9",
  "md:h-56": "_md:h-56_6d3e9",
  "md:h-64": "_md:h-64_6d3e9",
  "md:h-auto": "_md:h-auto_6d3e9",
  "md:h-px": "_md:h-px_6d3e9",
  "md:h-full": "_md:h-full_6d3e9",
  "md:h-screen": "_md:h-screen_6d3e9",
  "md:leading-none": "_md:leading-none_6d3e9",
  "md:leading-tight": "_md:leading-tight_6d3e9",
  "md:leading-snug": "_md:leading-snug_6d3e9",
  "md:leading-normal": "_md:leading-normal_6d3e9",
  "md:leading-relaxed": "_md:leading-relaxed_6d3e9",
  "md:leading-loose": "_md:leading-loose_6d3e9",
  "md:list-inside": "_md:list-inside_6d3e9",
  "md:list-outside": "_md:list-outside_6d3e9",
  "md:list-none": "_md:list-none_6d3e9",
  "md:list-disc": "_md:list-disc_6d3e9",
  "md:list-decimal": "_md:list-decimal_6d3e9",
  "md:m-0": "_md:m-0_6d3e9",
  "md:m-1": "_md:m-1_6d3e9",
  "md:m-2": "_md:m-2_6d3e9",
  "md:m-3": "_md:m-3_6d3e9",
  "md:m-4": "_md:m-4_6d3e9",
  "md:m-5": "_md:m-5_6d3e9",
  "md:m-6": "_md:m-6_6d3e9",
  "md:m-8": "_md:m-8_6d3e9",
  "md:m-10": "_md:m-10_6d3e9",
  "md:m-12": "_md:m-12_6d3e9",
  "md:m-16": "_md:m-16_6d3e9",
  "md:m-20": "_md:m-20_6d3e9",
  "md:m-24": "_md:m-24_6d3e9",
  "md:m-32": "_md:m-32_6d3e9",
  "md:m-40": "_md:m-40_6d3e9",
  "md:m-48": "_md:m-48_6d3e9",
  "md:m-56": "_md:m-56_6d3e9",
  "md:m-64": "_md:m-64_6d3e9",
  "md:m-auto": "_md:m-auto_6d3e9",
  "md:m-px": "_md:m-px_6d3e9",
  "md:-m-1": "_md:-m-1_6d3e9",
  "md:-m-2": "_md:-m-2_6d3e9",
  "md:-m-3": "_md:-m-3_6d3e9",
  "md:-m-4": "_md:-m-4_6d3e9",
  "md:-m-5": "_md:-m-5_6d3e9",
  "md:-m-6": "_md:-m-6_6d3e9",
  "md:-m-8": "_md:-m-8_6d3e9",
  "md:-m-10": "_md:-m-10_6d3e9",
  "md:-m-12": "_md:-m-12_6d3e9",
  "md:-m-16": "_md:-m-16_6d3e9",
  "md:-m-20": "_md:-m-20_6d3e9",
  "md:-m-24": "_md:-m-24_6d3e9",
  "md:-m-32": "_md:-m-32_6d3e9",
  "md:-m-40": "_md:-m-40_6d3e9",
  "md:-m-48": "_md:-m-48_6d3e9",
  "md:-m-56": "_md:-m-56_6d3e9",
  "md:-m-64": "_md:-m-64_6d3e9",
  "md:-m-px": "_md:-m-px_6d3e9",
  "md:my-0": "_md:my-0_6d3e9",
  "md:mx-0": "_md:mx-0_6d3e9",
  "md:my-1": "_md:my-1_6d3e9",
  "md:mx-1": "_md:mx-1_6d3e9",
  "md:my-2": "_md:my-2_6d3e9",
  "md:mx-2": "_md:mx-2_6d3e9",
  "md:my-3": "_md:my-3_6d3e9",
  "md:mx-3": "_md:mx-3_6d3e9",
  "md:my-4": "_md:my-4_6d3e9",
  "md:mx-4": "_md:mx-4_6d3e9",
  "md:my-5": "_md:my-5_6d3e9",
  "md:mx-5": "_md:mx-5_6d3e9",
  "md:my-6": "_md:my-6_6d3e9",
  "md:mx-6": "_md:mx-6_6d3e9",
  "md:my-8": "_md:my-8_6d3e9",
  "md:mx-8": "_md:mx-8_6d3e9",
  "md:my-10": "_md:my-10_6d3e9",
  "md:mx-10": "_md:mx-10_6d3e9",
  "md:my-12": "_md:my-12_6d3e9",
  "md:mx-12": "_md:mx-12_6d3e9",
  "md:my-16": "_md:my-16_6d3e9",
  "md:mx-16": "_md:mx-16_6d3e9",
  "md:my-20": "_md:my-20_6d3e9",
  "md:mx-20": "_md:mx-20_6d3e9",
  "md:my-24": "_md:my-24_6d3e9",
  "md:mx-24": "_md:mx-24_6d3e9",
  "md:my-32": "_md:my-32_6d3e9",
  "md:mx-32": "_md:mx-32_6d3e9",
  "md:my-40": "_md:my-40_6d3e9",
  "md:mx-40": "_md:mx-40_6d3e9",
  "md:my-48": "_md:my-48_6d3e9",
  "md:mx-48": "_md:mx-48_6d3e9",
  "md:my-56": "_md:my-56_6d3e9",
  "md:mx-56": "_md:mx-56_6d3e9",
  "md:my-64": "_md:my-64_6d3e9",
  "md:mx-64": "_md:mx-64_6d3e9",
  "md:my-auto": "_md:my-auto_6d3e9",
  "md:mx-auto": "_md:mx-auto_6d3e9",
  "md:my-px": "_md:my-px_6d3e9",
  "md:mx-px": "_md:mx-px_6d3e9",
  "md:-my-1": "_md:-my-1_6d3e9",
  "md:-mx-1": "_md:-mx-1_6d3e9",
  "md:-my-2": "_md:-my-2_6d3e9",
  "md:-mx-2": "_md:-mx-2_6d3e9",
  "md:-my-3": "_md:-my-3_6d3e9",
  "md:-mx-3": "_md:-mx-3_6d3e9",
  "md:-my-4": "_md:-my-4_6d3e9",
  "md:-mx-4": "_md:-mx-4_6d3e9",
  "md:-my-5": "_md:-my-5_6d3e9",
  "md:-mx-5": "_md:-mx-5_6d3e9",
  "md:-my-6": "_md:-my-6_6d3e9",
  "md:-mx-6": "_md:-mx-6_6d3e9",
  "md:-my-8": "_md:-my-8_6d3e9",
  "md:-mx-8": "_md:-mx-8_6d3e9",
  "md:-my-10": "_md:-my-10_6d3e9",
  "md:-mx-10": "_md:-mx-10_6d3e9",
  "md:-my-12": "_md:-my-12_6d3e9",
  "md:-mx-12": "_md:-mx-12_6d3e9",
  "md:-my-16": "_md:-my-16_6d3e9",
  "md:-mx-16": "_md:-mx-16_6d3e9",
  "md:-my-20": "_md:-my-20_6d3e9",
  "md:-mx-20": "_md:-mx-20_6d3e9",
  "md:-my-24": "_md:-my-24_6d3e9",
  "md:-mx-24": "_md:-mx-24_6d3e9",
  "md:-my-32": "_md:-my-32_6d3e9",
  "md:-mx-32": "_md:-mx-32_6d3e9",
  "md:-my-40": "_md:-my-40_6d3e9",
  "md:-mx-40": "_md:-mx-40_6d3e9",
  "md:-my-48": "_md:-my-48_6d3e9",
  "md:-mx-48": "_md:-mx-48_6d3e9",
  "md:-my-56": "_md:-my-56_6d3e9",
  "md:-mx-56": "_md:-mx-56_6d3e9",
  "md:-my-64": "_md:-my-64_6d3e9",
  "md:-mx-64": "_md:-mx-64_6d3e9",
  "md:-my-px": "_md:-my-px_6d3e9",
  "md:-mx-px": "_md:-mx-px_6d3e9",
  "md:mt-0": "_md:mt-0_6d3e9",
  "md:mr-0": "_md:mr-0_6d3e9",
  "md:mb-0": "_md:mb-0_6d3e9",
  "md:ml-0": "_md:ml-0_6d3e9",
  "md:mt-1": "_md:mt-1_6d3e9",
  "md:mr-1": "_md:mr-1_6d3e9",
  "md:mb-1": "_md:mb-1_6d3e9",
  "md:ml-1": "_md:ml-1_6d3e9",
  "md:mt-2": "_md:mt-2_6d3e9",
  "md:mr-2": "_md:mr-2_6d3e9",
  "md:mb-2": "_md:mb-2_6d3e9",
  "md:ml-2": "_md:ml-2_6d3e9",
  "md:mt-3": "_md:mt-3_6d3e9",
  "md:mr-3": "_md:mr-3_6d3e9",
  "md:mb-3": "_md:mb-3_6d3e9",
  "md:ml-3": "_md:ml-3_6d3e9",
  "md:mt-4": "_md:mt-4_6d3e9",
  "md:mr-4": "_md:mr-4_6d3e9",
  "md:mb-4": "_md:mb-4_6d3e9",
  "md:ml-4": "_md:ml-4_6d3e9",
  "md:mt-5": "_md:mt-5_6d3e9",
  "md:mr-5": "_md:mr-5_6d3e9",
  "md:mb-5": "_md:mb-5_6d3e9",
  "md:ml-5": "_md:ml-5_6d3e9",
  "md:mt-6": "_md:mt-6_6d3e9",
  "md:mr-6": "_md:mr-6_6d3e9",
  "md:mb-6": "_md:mb-6_6d3e9",
  "md:ml-6": "_md:ml-6_6d3e9",
  "md:mt-8": "_md:mt-8_6d3e9",
  "md:mr-8": "_md:mr-8_6d3e9",
  "md:mb-8": "_md:mb-8_6d3e9",
  "md:ml-8": "_md:ml-8_6d3e9",
  "md:mt-10": "_md:mt-10_6d3e9",
  "md:mr-10": "_md:mr-10_6d3e9",
  "md:mb-10": "_md:mb-10_6d3e9",
  "md:ml-10": "_md:ml-10_6d3e9",
  "md:mt-12": "_md:mt-12_6d3e9",
  "md:mr-12": "_md:mr-12_6d3e9",
  "md:mb-12": "_md:mb-12_6d3e9",
  "md:ml-12": "_md:ml-12_6d3e9",
  "md:mt-16": "_md:mt-16_6d3e9",
  "md:mr-16": "_md:mr-16_6d3e9",
  "md:mb-16": "_md:mb-16_6d3e9",
  "md:ml-16": "_md:ml-16_6d3e9",
  "md:mt-20": "_md:mt-20_6d3e9",
  "md:mr-20": "_md:mr-20_6d3e9",
  "md:mb-20": "_md:mb-20_6d3e9",
  "md:ml-20": "_md:ml-20_6d3e9",
  "md:mt-24": "_md:mt-24_6d3e9",
  "md:mr-24": "_md:mr-24_6d3e9",
  "md:mb-24": "_md:mb-24_6d3e9",
  "md:ml-24": "_md:ml-24_6d3e9",
  "md:mt-32": "_md:mt-32_6d3e9",
  "md:mr-32": "_md:mr-32_6d3e9",
  "md:mb-32": "_md:mb-32_6d3e9",
  "md:ml-32": "_md:ml-32_6d3e9",
  "md:mt-40": "_md:mt-40_6d3e9",
  "md:mr-40": "_md:mr-40_6d3e9",
  "md:mb-40": "_md:mb-40_6d3e9",
  "md:ml-40": "_md:ml-40_6d3e9",
  "md:mt-48": "_md:mt-48_6d3e9",
  "md:mr-48": "_md:mr-48_6d3e9",
  "md:mb-48": "_md:mb-48_6d3e9",
  "md:ml-48": "_md:ml-48_6d3e9",
  "md:mt-56": "_md:mt-56_6d3e9",
  "md:mr-56": "_md:mr-56_6d3e9",
  "md:mb-56": "_md:mb-56_6d3e9",
  "md:ml-56": "_md:ml-56_6d3e9",
  "md:mt-64": "_md:mt-64_6d3e9",
  "md:mr-64": "_md:mr-64_6d3e9",
  "md:mb-64": "_md:mb-64_6d3e9",
  "md:ml-64": "_md:ml-64_6d3e9",
  "md:mt-auto": "_md:mt-auto_6d3e9",
  "md:mr-auto": "_md:mr-auto_6d3e9",
  "md:mb-auto": "_md:mb-auto_6d3e9",
  "md:ml-auto": "_md:ml-auto_6d3e9",
  "md:mt-px": "_md:mt-px_6d3e9",
  "md:mr-px": "_md:mr-px_6d3e9",
  "md:mb-px": "_md:mb-px_6d3e9",
  "md:ml-px": "_md:ml-px_6d3e9",
  "md:-mt-1": "_md:-mt-1_6d3e9",
  "md:-mr-1": "_md:-mr-1_6d3e9",
  "md:-mb-1": "_md:-mb-1_6d3e9",
  "md:-ml-1": "_md:-ml-1_6d3e9",
  "md:-mt-2": "_md:-mt-2_6d3e9",
  "md:-mr-2": "_md:-mr-2_6d3e9",
  "md:-mb-2": "_md:-mb-2_6d3e9",
  "md:-ml-2": "_md:-ml-2_6d3e9",
  "md:-mt-3": "_md:-mt-3_6d3e9",
  "md:-mr-3": "_md:-mr-3_6d3e9",
  "md:-mb-3": "_md:-mb-3_6d3e9",
  "md:-ml-3": "_md:-ml-3_6d3e9",
  "md:-mt-4": "_md:-mt-4_6d3e9",
  "md:-mr-4": "_md:-mr-4_6d3e9",
  "md:-mb-4": "_md:-mb-4_6d3e9",
  "md:-ml-4": "_md:-ml-4_6d3e9",
  "md:-mt-5": "_md:-mt-5_6d3e9",
  "md:-mr-5": "_md:-mr-5_6d3e9",
  "md:-mb-5": "_md:-mb-5_6d3e9",
  "md:-ml-5": "_md:-ml-5_6d3e9",
  "md:-mt-6": "_md:-mt-6_6d3e9",
  "md:-mr-6": "_md:-mr-6_6d3e9",
  "md:-mb-6": "_md:-mb-6_6d3e9",
  "md:-ml-6": "_md:-ml-6_6d3e9",
  "md:-mt-8": "_md:-mt-8_6d3e9",
  "md:-mr-8": "_md:-mr-8_6d3e9",
  "md:-mb-8": "_md:-mb-8_6d3e9",
  "md:-ml-8": "_md:-ml-8_6d3e9",
  "md:-mt-10": "_md:-mt-10_6d3e9",
  "md:-mr-10": "_md:-mr-10_6d3e9",
  "md:-mb-10": "_md:-mb-10_6d3e9",
  "md:-ml-10": "_md:-ml-10_6d3e9",
  "md:-mt-12": "_md:-mt-12_6d3e9",
  "md:-mr-12": "_md:-mr-12_6d3e9",
  "md:-mb-12": "_md:-mb-12_6d3e9",
  "md:-ml-12": "_md:-ml-12_6d3e9",
  "md:-mt-16": "_md:-mt-16_6d3e9",
  "md:-mr-16": "_md:-mr-16_6d3e9",
  "md:-mb-16": "_md:-mb-16_6d3e9",
  "md:-ml-16": "_md:-ml-16_6d3e9",
  "md:-mt-20": "_md:-mt-20_6d3e9",
  "md:-mr-20": "_md:-mr-20_6d3e9",
  "md:-mb-20": "_md:-mb-20_6d3e9",
  "md:-ml-20": "_md:-ml-20_6d3e9",
  "md:-mt-24": "_md:-mt-24_6d3e9",
  "md:-mr-24": "_md:-mr-24_6d3e9",
  "md:-mb-24": "_md:-mb-24_6d3e9",
  "md:-ml-24": "_md:-ml-24_6d3e9",
  "md:-mt-32": "_md:-mt-32_6d3e9",
  "md:-mr-32": "_md:-mr-32_6d3e9",
  "md:-mb-32": "_md:-mb-32_6d3e9",
  "md:-ml-32": "_md:-ml-32_6d3e9",
  "md:-mt-40": "_md:-mt-40_6d3e9",
  "md:-mr-40": "_md:-mr-40_6d3e9",
  "md:-mb-40": "_md:-mb-40_6d3e9",
  "md:-ml-40": "_md:-ml-40_6d3e9",
  "md:-mt-48": "_md:-mt-48_6d3e9",
  "md:-mr-48": "_md:-mr-48_6d3e9",
  "md:-mb-48": "_md:-mb-48_6d3e9",
  "md:-ml-48": "_md:-ml-48_6d3e9",
  "md:-mt-56": "_md:-mt-56_6d3e9",
  "md:-mr-56": "_md:-mr-56_6d3e9",
  "md:-mb-56": "_md:-mb-56_6d3e9",
  "md:-ml-56": "_md:-ml-56_6d3e9",
  "md:-mt-64": "_md:-mt-64_6d3e9",
  "md:-mr-64": "_md:-mr-64_6d3e9",
  "md:-mb-64": "_md:-mb-64_6d3e9",
  "md:-ml-64": "_md:-ml-64_6d3e9",
  "md:-mt-px": "_md:-mt-px_6d3e9",
  "md:-mr-px": "_md:-mr-px_6d3e9",
  "md:-mb-px": "_md:-mb-px_6d3e9",
  "md:-ml-px": "_md:-ml-px_6d3e9",
  "md:max-h-full": "_md:max-h-full_6d3e9",
  "md:max-h-screen": "_md:max-h-screen_6d3e9",
  "md:max-w-xs": "_md:max-w-xs_6d3e9",
  "md:max-w-sm": "_md:max-w-sm_6d3e9",
  "md:max-w-md": "_md:max-w-md_6d3e9",
  "md:max-w-lg": "_md:max-w-lg_6d3e9",
  "md:max-w-xl": "_md:max-w-xl_6d3e9",
  "md:max-w-2xl": "_md:max-w-2xl_6d3e9",
  "md:max-w-3xl": "_md:max-w-3xl_6d3e9",
  "md:max-w-4xl": "_md:max-w-4xl_6d3e9",
  "md:max-w-5xl": "_md:max-w-5xl_6d3e9",
  "md:max-w-6xl": "_md:max-w-6xl_6d3e9",
  "md:max-w-full": "_md:max-w-full_6d3e9",
  "md:min-h-0": "_md:min-h-0_6d3e9",
  "md:min-h-full": "_md:min-h-full_6d3e9",
  "md:min-h-screen": "_md:min-h-screen_6d3e9",
  "md:min-w-0": "_md:min-w-0_6d3e9",
  "md:min-w-full": "_md:min-w-full_6d3e9",
  "md:object-contain": "_md:object-contain_6d3e9",
  "md:object-cover": "_md:object-cover_6d3e9",
  "md:object-fill": "_md:object-fill_6d3e9",
  "md:object-none": "_md:object-none_6d3e9",
  "md:object-scale-down": "_md:object-scale-down_6d3e9",
  "md:object-bottom": "_md:object-bottom_6d3e9",
  "md:object-center": "_md:object-center_6d3e9",
  "md:object-left": "_md:object-left_6d3e9",
  "md:object-left-bottom": "_md:object-left-bottom_6d3e9",
  "md:object-left-top": "_md:object-left-top_6d3e9",
  "md:object-right": "_md:object-right_6d3e9",
  "md:object-right-bottom": "_md:object-right-bottom_6d3e9",
  "md:object-right-top": "_md:object-right-top_6d3e9",
  "md:object-top": "_md:object-top_6d3e9",
  "md:opacity-0": "_md:opacity-0_6d3e9",
  "md:opacity-25": "_md:opacity-25_6d3e9",
  "md:opacity-50": "_md:opacity-50_6d3e9",
  "md:opacity-75": "_md:opacity-75_6d3e9",
  "md:opacity-100": "_md:opacity-100_6d3e9",
  "md:hover:opacity-0": "_md:hover:opacity-0_6d3e9",
  "md:hover:opacity-25": "_md:hover:opacity-25_6d3e9",
  "md:hover:opacity-50": "_md:hover:opacity-50_6d3e9",
  "md:hover:opacity-75": "_md:hover:opacity-75_6d3e9",
  "md:hover:opacity-100": "_md:hover:opacity-100_6d3e9",
  "md:focus:opacity-0": "_md:focus:opacity-0_6d3e9",
  "md:focus:opacity-25": "_md:focus:opacity-25_6d3e9",
  "md:focus:opacity-50": "_md:focus:opacity-50_6d3e9",
  "md:focus:opacity-75": "_md:focus:opacity-75_6d3e9",
  "md:focus:opacity-100": "_md:focus:opacity-100_6d3e9",
  "md:outline-none": "_md:outline-none_6d3e9",
  "md:focus:outline-none": "_md:focus:outline-none_6d3e9",
  "md:overflow-auto": "_md:overflow-auto_6d3e9",
  "md:overflow-hidden": "_md:overflow-hidden_6d3e9",
  "md:overflow-visible": "_md:overflow-visible_6d3e9",
  "md:overflow-scroll": "_md:overflow-scroll_6d3e9",
  "md:overflow-x-auto": "_md:overflow-x-auto_6d3e9",
  "md:overflow-y-auto": "_md:overflow-y-auto_6d3e9",
  "md:overflow-x-hidden": "_md:overflow-x-hidden_6d3e9",
  "md:overflow-y-hidden": "_md:overflow-y-hidden_6d3e9",
  "md:overflow-x-visible": "_md:overflow-x-visible_6d3e9",
  "md:overflow-y-visible": "_md:overflow-y-visible_6d3e9",
  "md:overflow-x-scroll": "_md:overflow-x-scroll_6d3e9",
  "md:overflow-y-scroll": "_md:overflow-y-scroll_6d3e9",
  "md:scrolling-touch": "_md:scrolling-touch_6d3e9",
  "md:scrolling-auto": "_md:scrolling-auto_6d3e9",
  "md:p-0": "_md:p-0_6d3e9",
  "md:p-1": "_md:p-1_6d3e9",
  "md:p-2": "_md:p-2_6d3e9",
  "md:p-3": "_md:p-3_6d3e9",
  "md:p-4": "_md:p-4_6d3e9",
  "md:p-5": "_md:p-5_6d3e9",
  "md:p-6": "_md:p-6_6d3e9",
  "md:p-8": "_md:p-8_6d3e9",
  "md:p-10": "_md:p-10_6d3e9",
  "md:p-12": "_md:p-12_6d3e9",
  "md:p-16": "_md:p-16_6d3e9",
  "md:p-20": "_md:p-20_6d3e9",
  "md:p-24": "_md:p-24_6d3e9",
  "md:p-32": "_md:p-32_6d3e9",
  "md:p-40": "_md:p-40_6d3e9",
  "md:p-48": "_md:p-48_6d3e9",
  "md:p-56": "_md:p-56_6d3e9",
  "md:p-64": "_md:p-64_6d3e9",
  "md:p-px": "_md:p-px_6d3e9",
  "md:py-0": "_md:py-0_6d3e9",
  "md:px-0": "_md:px-0_6d3e9",
  "md:py-1": "_md:py-1_6d3e9",
  "md:px-1": "_md:px-1_6d3e9",
  "md:py-2": "_md:py-2_6d3e9",
  "md:px-2": "_md:px-2_6d3e9",
  "md:py-3": "_md:py-3_6d3e9",
  "md:px-3": "_md:px-3_6d3e9",
  "md:py-4": "_md:py-4_6d3e9",
  "md:px-4": "_md:px-4_6d3e9",
  "md:py-5": "_md:py-5_6d3e9",
  "md:px-5": "_md:px-5_6d3e9",
  "md:py-6": "_md:py-6_6d3e9",
  "md:px-6": "_md:px-6_6d3e9",
  "md:py-8": "_md:py-8_6d3e9",
  "md:px-8": "_md:px-8_6d3e9",
  "md:py-10": "_md:py-10_6d3e9",
  "md:px-10": "_md:px-10_6d3e9",
  "md:py-12": "_md:py-12_6d3e9",
  "md:px-12": "_md:px-12_6d3e9",
  "md:py-16": "_md:py-16_6d3e9",
  "md:px-16": "_md:px-16_6d3e9",
  "md:py-20": "_md:py-20_6d3e9",
  "md:px-20": "_md:px-20_6d3e9",
  "md:py-24": "_md:py-24_6d3e9",
  "md:px-24": "_md:px-24_6d3e9",
  "md:py-32": "_md:py-32_6d3e9",
  "md:px-32": "_md:px-32_6d3e9",
  "md:py-40": "_md:py-40_6d3e9",
  "md:px-40": "_md:px-40_6d3e9",
  "md:py-48": "_md:py-48_6d3e9",
  "md:px-48": "_md:px-48_6d3e9",
  "md:py-56": "_md:py-56_6d3e9",
  "md:px-56": "_md:px-56_6d3e9",
  "md:py-64": "_md:py-64_6d3e9",
  "md:px-64": "_md:px-64_6d3e9",
  "md:py-px": "_md:py-px_6d3e9",
  "md:px-px": "_md:px-px_6d3e9",
  "md:pt-0": "_md:pt-0_6d3e9",
  "md:pr-0": "_md:pr-0_6d3e9",
  "md:pb-0": "_md:pb-0_6d3e9",
  "md:pl-0": "_md:pl-0_6d3e9",
  "md:pt-1": "_md:pt-1_6d3e9",
  "md:pr-1": "_md:pr-1_6d3e9",
  "md:pb-1": "_md:pb-1_6d3e9",
  "md:pl-1": "_md:pl-1_6d3e9",
  "md:pt-2": "_md:pt-2_6d3e9",
  "md:pr-2": "_md:pr-2_6d3e9",
  "md:pb-2": "_md:pb-2_6d3e9",
  "md:pl-2": "_md:pl-2_6d3e9",
  "md:pt-3": "_md:pt-3_6d3e9",
  "md:pr-3": "_md:pr-3_6d3e9",
  "md:pb-3": "_md:pb-3_6d3e9",
  "md:pl-3": "_md:pl-3_6d3e9",
  "md:pt-4": "_md:pt-4_6d3e9",
  "md:pr-4": "_md:pr-4_6d3e9",
  "md:pb-4": "_md:pb-4_6d3e9",
  "md:pl-4": "_md:pl-4_6d3e9",
  "md:pt-5": "_md:pt-5_6d3e9",
  "md:pr-5": "_md:pr-5_6d3e9",
  "md:pb-5": "_md:pb-5_6d3e9",
  "md:pl-5": "_md:pl-5_6d3e9",
  "md:pt-6": "_md:pt-6_6d3e9",
  "md:pr-6": "_md:pr-6_6d3e9",
  "md:pb-6": "_md:pb-6_6d3e9",
  "md:pl-6": "_md:pl-6_6d3e9",
  "md:pt-8": "_md:pt-8_6d3e9",
  "md:pr-8": "_md:pr-8_6d3e9",
  "md:pb-8": "_md:pb-8_6d3e9",
  "md:pl-8": "_md:pl-8_6d3e9",
  "md:pt-10": "_md:pt-10_6d3e9",
  "md:pr-10": "_md:pr-10_6d3e9",
  "md:pb-10": "_md:pb-10_6d3e9",
  "md:pl-10": "_md:pl-10_6d3e9",
  "md:pt-12": "_md:pt-12_6d3e9",
  "md:pr-12": "_md:pr-12_6d3e9",
  "md:pb-12": "_md:pb-12_6d3e9",
  "md:pl-12": "_md:pl-12_6d3e9",
  "md:pt-16": "_md:pt-16_6d3e9",
  "md:pr-16": "_md:pr-16_6d3e9",
  "md:pb-16": "_md:pb-16_6d3e9",
  "md:pl-16": "_md:pl-16_6d3e9",
  "md:pt-20": "_md:pt-20_6d3e9",
  "md:pr-20": "_md:pr-20_6d3e9",
  "md:pb-20": "_md:pb-20_6d3e9",
  "md:pl-20": "_md:pl-20_6d3e9",
  "md:pt-24": "_md:pt-24_6d3e9",
  "md:pr-24": "_md:pr-24_6d3e9",
  "md:pb-24": "_md:pb-24_6d3e9",
  "md:pl-24": "_md:pl-24_6d3e9",
  "md:pt-32": "_md:pt-32_6d3e9",
  "md:pr-32": "_md:pr-32_6d3e9",
  "md:pb-32": "_md:pb-32_6d3e9",
  "md:pl-32": "_md:pl-32_6d3e9",
  "md:pt-40": "_md:pt-40_6d3e9",
  "md:pr-40": "_md:pr-40_6d3e9",
  "md:pb-40": "_md:pb-40_6d3e9",
  "md:pl-40": "_md:pl-40_6d3e9",
  "md:pt-48": "_md:pt-48_6d3e9",
  "md:pr-48": "_md:pr-48_6d3e9",
  "md:pb-48": "_md:pb-48_6d3e9",
  "md:pl-48": "_md:pl-48_6d3e9",
  "md:pt-56": "_md:pt-56_6d3e9",
  "md:pr-56": "_md:pr-56_6d3e9",
  "md:pb-56": "_md:pb-56_6d3e9",
  "md:pl-56": "_md:pl-56_6d3e9",
  "md:pt-64": "_md:pt-64_6d3e9",
  "md:pr-64": "_md:pr-64_6d3e9",
  "md:pb-64": "_md:pb-64_6d3e9",
  "md:pl-64": "_md:pl-64_6d3e9",
  "md:pt-px": "_md:pt-px_6d3e9",
  "md:pr-px": "_md:pr-px_6d3e9",
  "md:pb-px": "_md:pb-px_6d3e9",
  "md:pl-px": "_md:pl-px_6d3e9",
  "md:placeholder-transparent": "_md:placeholder-transparent_6d3e9",
  "md:placeholder-black": "_md:placeholder-black_6d3e9",
  "md:placeholder-white": "_md:placeholder-white_6d3e9",
  "md:placeholder-gray-100": "_md:placeholder-gray-100_6d3e9",
  "md:placeholder-gray-200": "_md:placeholder-gray-200_6d3e9",
  "md:placeholder-gray-300": "_md:placeholder-gray-300_6d3e9",
  "md:placeholder-gray-400": "_md:placeholder-gray-400_6d3e9",
  "md:placeholder-gray-500": "_md:placeholder-gray-500_6d3e9",
  "md:placeholder-gray-600": "_md:placeholder-gray-600_6d3e9",
  "md:placeholder-gray-700": "_md:placeholder-gray-700_6d3e9",
  "md:placeholder-gray-800": "_md:placeholder-gray-800_6d3e9",
  "md:placeholder-gray-900": "_md:placeholder-gray-900_6d3e9",
  "md:placeholder-red-100": "_md:placeholder-red-100_6d3e9",
  "md:placeholder-red-200": "_md:placeholder-red-200_6d3e9",
  "md:placeholder-red-300": "_md:placeholder-red-300_6d3e9",
  "md:placeholder-red-400": "_md:placeholder-red-400_6d3e9",
  "md:placeholder-red-500": "_md:placeholder-red-500_6d3e9",
  "md:placeholder-red-600": "_md:placeholder-red-600_6d3e9",
  "md:placeholder-red-700": "_md:placeholder-red-700_6d3e9",
  "md:placeholder-red-800": "_md:placeholder-red-800_6d3e9",
  "md:placeholder-red-900": "_md:placeholder-red-900_6d3e9",
  "md:placeholder-orange-100": "_md:placeholder-orange-100_6d3e9",
  "md:placeholder-orange-200": "_md:placeholder-orange-200_6d3e9",
  "md:placeholder-orange-300": "_md:placeholder-orange-300_6d3e9",
  "md:placeholder-orange-400": "_md:placeholder-orange-400_6d3e9",
  "md:placeholder-orange-500": "_md:placeholder-orange-500_6d3e9",
  "md:placeholder-orange-600": "_md:placeholder-orange-600_6d3e9",
  "md:placeholder-orange-700": "_md:placeholder-orange-700_6d3e9",
  "md:placeholder-orange-800": "_md:placeholder-orange-800_6d3e9",
  "md:placeholder-orange-900": "_md:placeholder-orange-900_6d3e9",
  "md:placeholder-yellow-100": "_md:placeholder-yellow-100_6d3e9",
  "md:placeholder-yellow-200": "_md:placeholder-yellow-200_6d3e9",
  "md:placeholder-yellow-300": "_md:placeholder-yellow-300_6d3e9",
  "md:placeholder-yellow-400": "_md:placeholder-yellow-400_6d3e9",
  "md:placeholder-yellow-500": "_md:placeholder-yellow-500_6d3e9",
  "md:placeholder-yellow-600": "_md:placeholder-yellow-600_6d3e9",
  "md:placeholder-yellow-700": "_md:placeholder-yellow-700_6d3e9",
  "md:placeholder-yellow-800": "_md:placeholder-yellow-800_6d3e9",
  "md:placeholder-yellow-900": "_md:placeholder-yellow-900_6d3e9",
  "md:placeholder-green-100": "_md:placeholder-green-100_6d3e9",
  "md:placeholder-green-200": "_md:placeholder-green-200_6d3e9",
  "md:placeholder-green-300": "_md:placeholder-green-300_6d3e9",
  "md:placeholder-green-400": "_md:placeholder-green-400_6d3e9",
  "md:placeholder-green-500": "_md:placeholder-green-500_6d3e9",
  "md:placeholder-green-600": "_md:placeholder-green-600_6d3e9",
  "md:placeholder-green-700": "_md:placeholder-green-700_6d3e9",
  "md:placeholder-green-800": "_md:placeholder-green-800_6d3e9",
  "md:placeholder-green-900": "_md:placeholder-green-900_6d3e9",
  "md:placeholder-teal-100": "_md:placeholder-teal-100_6d3e9",
  "md:placeholder-teal-200": "_md:placeholder-teal-200_6d3e9",
  "md:placeholder-teal-300": "_md:placeholder-teal-300_6d3e9",
  "md:placeholder-teal-400": "_md:placeholder-teal-400_6d3e9",
  "md:placeholder-teal-500": "_md:placeholder-teal-500_6d3e9",
  "md:placeholder-teal-600": "_md:placeholder-teal-600_6d3e9",
  "md:placeholder-teal-700": "_md:placeholder-teal-700_6d3e9",
  "md:placeholder-teal-800": "_md:placeholder-teal-800_6d3e9",
  "md:placeholder-teal-900": "_md:placeholder-teal-900_6d3e9",
  "md:placeholder-blue-100": "_md:placeholder-blue-100_6d3e9",
  "md:placeholder-blue-200": "_md:placeholder-blue-200_6d3e9",
  "md:placeholder-blue-300": "_md:placeholder-blue-300_6d3e9",
  "md:placeholder-blue-400": "_md:placeholder-blue-400_6d3e9",
  "md:placeholder-blue-500": "_md:placeholder-blue-500_6d3e9",
  "md:placeholder-blue-600": "_md:placeholder-blue-600_6d3e9",
  "md:placeholder-blue-700": "_md:placeholder-blue-700_6d3e9",
  "md:placeholder-blue-800": "_md:placeholder-blue-800_6d3e9",
  "md:placeholder-blue-900": "_md:placeholder-blue-900_6d3e9",
  "md:placeholder-indigo-100": "_md:placeholder-indigo-100_6d3e9",
  "md:placeholder-indigo-200": "_md:placeholder-indigo-200_6d3e9",
  "md:placeholder-indigo-300": "_md:placeholder-indigo-300_6d3e9",
  "md:placeholder-indigo-400": "_md:placeholder-indigo-400_6d3e9",
  "md:placeholder-indigo-500": "_md:placeholder-indigo-500_6d3e9",
  "md:placeholder-indigo-600": "_md:placeholder-indigo-600_6d3e9",
  "md:placeholder-indigo-700": "_md:placeholder-indigo-700_6d3e9",
  "md:placeholder-indigo-800": "_md:placeholder-indigo-800_6d3e9",
  "md:placeholder-indigo-900": "_md:placeholder-indigo-900_6d3e9",
  "md:placeholder-purple-100": "_md:placeholder-purple-100_6d3e9",
  "md:placeholder-purple-200": "_md:placeholder-purple-200_6d3e9",
  "md:placeholder-purple-300": "_md:placeholder-purple-300_6d3e9",
  "md:placeholder-purple-400": "_md:placeholder-purple-400_6d3e9",
  "md:placeholder-purple-500": "_md:placeholder-purple-500_6d3e9",
  "md:placeholder-purple-600": "_md:placeholder-purple-600_6d3e9",
  "md:placeholder-purple-700": "_md:placeholder-purple-700_6d3e9",
  "md:placeholder-purple-800": "_md:placeholder-purple-800_6d3e9",
  "md:placeholder-purple-900": "_md:placeholder-purple-900_6d3e9",
  "md:placeholder-pink-100": "_md:placeholder-pink-100_6d3e9",
  "md:placeholder-pink-200": "_md:placeholder-pink-200_6d3e9",
  "md:placeholder-pink-300": "_md:placeholder-pink-300_6d3e9",
  "md:placeholder-pink-400": "_md:placeholder-pink-400_6d3e9",
  "md:placeholder-pink-500": "_md:placeholder-pink-500_6d3e9",
  "md:placeholder-pink-600": "_md:placeholder-pink-600_6d3e9",
  "md:placeholder-pink-700": "_md:placeholder-pink-700_6d3e9",
  "md:placeholder-pink-800": "_md:placeholder-pink-800_6d3e9",
  "md:placeholder-pink-900": "_md:placeholder-pink-900_6d3e9",
  "md:placeholder-primary-100": "_md:placeholder-primary-100_6d3e9",
  "md:placeholder-primary-200": "_md:placeholder-primary-200_6d3e9",
  "md:placeholder-primary-300": "_md:placeholder-primary-300_6d3e9",
  "md:placeholder-primary-400": "_md:placeholder-primary-400_6d3e9",
  "md:placeholder-primary-500": "_md:placeholder-primary-500_6d3e9",
  "md:placeholder-primary-600": "_md:placeholder-primary-600_6d3e9",
  "md:placeholder-primary-700": "_md:placeholder-primary-700_6d3e9",
  "md:placeholder-primary-800": "_md:placeholder-primary-800_6d3e9",
  "md:placeholder-primary-900": "_md:placeholder-primary-900_6d3e9",
  "md:placeholder-secondary-100": "_md:placeholder-secondary-100_6d3e9",
  "md:placeholder-secondary-200": "_md:placeholder-secondary-200_6d3e9",
  "md:placeholder-secondary-300": "_md:placeholder-secondary-300_6d3e9",
  "md:placeholder-secondary-400": "_md:placeholder-secondary-400_6d3e9",
  "md:placeholder-secondary-500": "_md:placeholder-secondary-500_6d3e9",
  "md:placeholder-secondary-600": "_md:placeholder-secondary-600_6d3e9",
  "md:placeholder-secondary-700": "_md:placeholder-secondary-700_6d3e9",
  "md:placeholder-secondary-800": "_md:placeholder-secondary-800_6d3e9",
  "md:placeholder-secondary-900": "_md:placeholder-secondary-900_6d3e9",
  "md:focus:placeholder-transparent": "_md:focus:placeholder-transparent_6d3e9",
  "md:focus:placeholder-black": "_md:focus:placeholder-black_6d3e9",
  "md:focus:placeholder-white": "_md:focus:placeholder-white_6d3e9",
  "md:focus:placeholder-gray-100": "_md:focus:placeholder-gray-100_6d3e9",
  "md:focus:placeholder-gray-200": "_md:focus:placeholder-gray-200_6d3e9",
  "md:focus:placeholder-gray-300": "_md:focus:placeholder-gray-300_6d3e9",
  "md:focus:placeholder-gray-400": "_md:focus:placeholder-gray-400_6d3e9",
  "md:focus:placeholder-gray-500": "_md:focus:placeholder-gray-500_6d3e9",
  "md:focus:placeholder-gray-600": "_md:focus:placeholder-gray-600_6d3e9",
  "md:focus:placeholder-gray-700": "_md:focus:placeholder-gray-700_6d3e9",
  "md:focus:placeholder-gray-800": "_md:focus:placeholder-gray-800_6d3e9",
  "md:focus:placeholder-gray-900": "_md:focus:placeholder-gray-900_6d3e9",
  "md:focus:placeholder-red-100": "_md:focus:placeholder-red-100_6d3e9",
  "md:focus:placeholder-red-200": "_md:focus:placeholder-red-200_6d3e9",
  "md:focus:placeholder-red-300": "_md:focus:placeholder-red-300_6d3e9",
  "md:focus:placeholder-red-400": "_md:focus:placeholder-red-400_6d3e9",
  "md:focus:placeholder-red-500": "_md:focus:placeholder-red-500_6d3e9",
  "md:focus:placeholder-red-600": "_md:focus:placeholder-red-600_6d3e9",
  "md:focus:placeholder-red-700": "_md:focus:placeholder-red-700_6d3e9",
  "md:focus:placeholder-red-800": "_md:focus:placeholder-red-800_6d3e9",
  "md:focus:placeholder-red-900": "_md:focus:placeholder-red-900_6d3e9",
  "md:focus:placeholder-orange-100": "_md:focus:placeholder-orange-100_6d3e9",
  "md:focus:placeholder-orange-200": "_md:focus:placeholder-orange-200_6d3e9",
  "md:focus:placeholder-orange-300": "_md:focus:placeholder-orange-300_6d3e9",
  "md:focus:placeholder-orange-400": "_md:focus:placeholder-orange-400_6d3e9",
  "md:focus:placeholder-orange-500": "_md:focus:placeholder-orange-500_6d3e9",
  "md:focus:placeholder-orange-600": "_md:focus:placeholder-orange-600_6d3e9",
  "md:focus:placeholder-orange-700": "_md:focus:placeholder-orange-700_6d3e9",
  "md:focus:placeholder-orange-800": "_md:focus:placeholder-orange-800_6d3e9",
  "md:focus:placeholder-orange-900": "_md:focus:placeholder-orange-900_6d3e9",
  "md:focus:placeholder-yellow-100": "_md:focus:placeholder-yellow-100_6d3e9",
  "md:focus:placeholder-yellow-200": "_md:focus:placeholder-yellow-200_6d3e9",
  "md:focus:placeholder-yellow-300": "_md:focus:placeholder-yellow-300_6d3e9",
  "md:focus:placeholder-yellow-400": "_md:focus:placeholder-yellow-400_6d3e9",
  "md:focus:placeholder-yellow-500": "_md:focus:placeholder-yellow-500_6d3e9",
  "md:focus:placeholder-yellow-600": "_md:focus:placeholder-yellow-600_6d3e9",
  "md:focus:placeholder-yellow-700": "_md:focus:placeholder-yellow-700_6d3e9",
  "md:focus:placeholder-yellow-800": "_md:focus:placeholder-yellow-800_6d3e9",
  "md:focus:placeholder-yellow-900": "_md:focus:placeholder-yellow-900_6d3e9",
  "md:focus:placeholder-green-100": "_md:focus:placeholder-green-100_6d3e9",
  "md:focus:placeholder-green-200": "_md:focus:placeholder-green-200_6d3e9",
  "md:focus:placeholder-green-300": "_md:focus:placeholder-green-300_6d3e9",
  "md:focus:placeholder-green-400": "_md:focus:placeholder-green-400_6d3e9",
  "md:focus:placeholder-green-500": "_md:focus:placeholder-green-500_6d3e9",
  "md:focus:placeholder-green-600": "_md:focus:placeholder-green-600_6d3e9",
  "md:focus:placeholder-green-700": "_md:focus:placeholder-green-700_6d3e9",
  "md:focus:placeholder-green-800": "_md:focus:placeholder-green-800_6d3e9",
  "md:focus:placeholder-green-900": "_md:focus:placeholder-green-900_6d3e9",
  "md:focus:placeholder-teal-100": "_md:focus:placeholder-teal-100_6d3e9",
  "md:focus:placeholder-teal-200": "_md:focus:placeholder-teal-200_6d3e9",
  "md:focus:placeholder-teal-300": "_md:focus:placeholder-teal-300_6d3e9",
  "md:focus:placeholder-teal-400": "_md:focus:placeholder-teal-400_6d3e9",
  "md:focus:placeholder-teal-500": "_md:focus:placeholder-teal-500_6d3e9",
  "md:focus:placeholder-teal-600": "_md:focus:placeholder-teal-600_6d3e9",
  "md:focus:placeholder-teal-700": "_md:focus:placeholder-teal-700_6d3e9",
  "md:focus:placeholder-teal-800": "_md:focus:placeholder-teal-800_6d3e9",
  "md:focus:placeholder-teal-900": "_md:focus:placeholder-teal-900_6d3e9",
  "md:focus:placeholder-blue-100": "_md:focus:placeholder-blue-100_6d3e9",
  "md:focus:placeholder-blue-200": "_md:focus:placeholder-blue-200_6d3e9",
  "md:focus:placeholder-blue-300": "_md:focus:placeholder-blue-300_6d3e9",
  "md:focus:placeholder-blue-400": "_md:focus:placeholder-blue-400_6d3e9",
  "md:focus:placeholder-blue-500": "_md:focus:placeholder-blue-500_6d3e9",
  "md:focus:placeholder-blue-600": "_md:focus:placeholder-blue-600_6d3e9",
  "md:focus:placeholder-blue-700": "_md:focus:placeholder-blue-700_6d3e9",
  "md:focus:placeholder-blue-800": "_md:focus:placeholder-blue-800_6d3e9",
  "md:focus:placeholder-blue-900": "_md:focus:placeholder-blue-900_6d3e9",
  "md:focus:placeholder-indigo-100": "_md:focus:placeholder-indigo-100_6d3e9",
  "md:focus:placeholder-indigo-200": "_md:focus:placeholder-indigo-200_6d3e9",
  "md:focus:placeholder-indigo-300": "_md:focus:placeholder-indigo-300_6d3e9",
  "md:focus:placeholder-indigo-400": "_md:focus:placeholder-indigo-400_6d3e9",
  "md:focus:placeholder-indigo-500": "_md:focus:placeholder-indigo-500_6d3e9",
  "md:focus:placeholder-indigo-600": "_md:focus:placeholder-indigo-600_6d3e9",
  "md:focus:placeholder-indigo-700": "_md:focus:placeholder-indigo-700_6d3e9",
  "md:focus:placeholder-indigo-800": "_md:focus:placeholder-indigo-800_6d3e9",
  "md:focus:placeholder-indigo-900": "_md:focus:placeholder-indigo-900_6d3e9",
  "md:focus:placeholder-purple-100": "_md:focus:placeholder-purple-100_6d3e9",
  "md:focus:placeholder-purple-200": "_md:focus:placeholder-purple-200_6d3e9",
  "md:focus:placeholder-purple-300": "_md:focus:placeholder-purple-300_6d3e9",
  "md:focus:placeholder-purple-400": "_md:focus:placeholder-purple-400_6d3e9",
  "md:focus:placeholder-purple-500": "_md:focus:placeholder-purple-500_6d3e9",
  "md:focus:placeholder-purple-600": "_md:focus:placeholder-purple-600_6d3e9",
  "md:focus:placeholder-purple-700": "_md:focus:placeholder-purple-700_6d3e9",
  "md:focus:placeholder-purple-800": "_md:focus:placeholder-purple-800_6d3e9",
  "md:focus:placeholder-purple-900": "_md:focus:placeholder-purple-900_6d3e9",
  "md:focus:placeholder-pink-100": "_md:focus:placeholder-pink-100_6d3e9",
  "md:focus:placeholder-pink-200": "_md:focus:placeholder-pink-200_6d3e9",
  "md:focus:placeholder-pink-300": "_md:focus:placeholder-pink-300_6d3e9",
  "md:focus:placeholder-pink-400": "_md:focus:placeholder-pink-400_6d3e9",
  "md:focus:placeholder-pink-500": "_md:focus:placeholder-pink-500_6d3e9",
  "md:focus:placeholder-pink-600": "_md:focus:placeholder-pink-600_6d3e9",
  "md:focus:placeholder-pink-700": "_md:focus:placeholder-pink-700_6d3e9",
  "md:focus:placeholder-pink-800": "_md:focus:placeholder-pink-800_6d3e9",
  "md:focus:placeholder-pink-900": "_md:focus:placeholder-pink-900_6d3e9",
  "md:focus:placeholder-primary-100": "_md:focus:placeholder-primary-100_6d3e9",
  "md:focus:placeholder-primary-200": "_md:focus:placeholder-primary-200_6d3e9",
  "md:focus:placeholder-primary-300": "_md:focus:placeholder-primary-300_6d3e9",
  "md:focus:placeholder-primary-400": "_md:focus:placeholder-primary-400_6d3e9",
  "md:focus:placeholder-primary-500": "_md:focus:placeholder-primary-500_6d3e9",
  "md:focus:placeholder-primary-600": "_md:focus:placeholder-primary-600_6d3e9",
  "md:focus:placeholder-primary-700": "_md:focus:placeholder-primary-700_6d3e9",
  "md:focus:placeholder-primary-800": "_md:focus:placeholder-primary-800_6d3e9",
  "md:focus:placeholder-primary-900": "_md:focus:placeholder-primary-900_6d3e9",
  "md:focus:placeholder-secondary-100": "_md:focus:placeholder-secondary-100_6d3e9",
  "md:focus:placeholder-secondary-200": "_md:focus:placeholder-secondary-200_6d3e9",
  "md:focus:placeholder-secondary-300": "_md:focus:placeholder-secondary-300_6d3e9",
  "md:focus:placeholder-secondary-400": "_md:focus:placeholder-secondary-400_6d3e9",
  "md:focus:placeholder-secondary-500": "_md:focus:placeholder-secondary-500_6d3e9",
  "md:focus:placeholder-secondary-600": "_md:focus:placeholder-secondary-600_6d3e9",
  "md:focus:placeholder-secondary-700": "_md:focus:placeholder-secondary-700_6d3e9",
  "md:focus:placeholder-secondary-800": "_md:focus:placeholder-secondary-800_6d3e9",
  "md:focus:placeholder-secondary-900": "_md:focus:placeholder-secondary-900_6d3e9",
  "md:pointer-events-none": "_md:pointer-events-none_6d3e9",
  "md:pointer-events-auto": "_md:pointer-events-auto_6d3e9",
  "md:static": "_md:static_6d3e9",
  "md:fixed": "_md:fixed_6d3e9",
  "md:absolute": "_md:absolute_6d3e9",
  "md:relative": "_md:relative_6d3e9",
  "md:sticky": "_md:sticky_6d3e9",
  "md:inset-0": "_md:inset-0_6d3e9",
  "md:inset-auto": "_md:inset-auto_6d3e9",
  "md:inset-y-0": "_md:inset-y-0_6d3e9",
  "md:inset-x-0": "_md:inset-x-0_6d3e9",
  "md:inset-y-auto": "_md:inset-y-auto_6d3e9",
  "md:inset-x-auto": "_md:inset-x-auto_6d3e9",
  "md:top-0": "_md:top-0_6d3e9",
  "md:right-0": "_md:right-0_6d3e9",
  "md:bottom-0": "_md:bottom-0_6d3e9",
  "md:left-0": "_md:left-0_6d3e9",
  "md:top-auto": "_md:top-auto_6d3e9",
  "md:right-auto": "_md:right-auto_6d3e9",
  "md:bottom-auto": "_md:bottom-auto_6d3e9",
  "md:left-auto": "_md:left-auto_6d3e9",
  "md:resize-none": "_md:resize-none_6d3e9",
  "md:resize-y": "_md:resize-y_6d3e9",
  "md:resize-x": "_md:resize-x_6d3e9",
  "md:resize": "_md:resize_6d3e9",
  "md:shadow": "_md:shadow_6d3e9",
  "md:shadow-md": "_md:shadow-md_6d3e9",
  "md:shadow-lg": "_md:shadow-lg_6d3e9",
  "md:shadow-xl": "_md:shadow-xl_6d3e9",
  "md:shadow-2xl": "_md:shadow-2xl_6d3e9",
  "md:shadow-inner": "_md:shadow-inner_6d3e9",
  "md:shadow-outline": "_md:shadow-outline_6d3e9",
  "md:shadow-none": "_md:shadow-none_6d3e9",
  "md:hover:shadow": "_md:hover:shadow_6d3e9",
  "md:hover:shadow-md": "_md:hover:shadow-md_6d3e9",
  "md:hover:shadow-lg": "_md:hover:shadow-lg_6d3e9",
  "md:hover:shadow-xl": "_md:hover:shadow-xl_6d3e9",
  "md:hover:shadow-2xl": "_md:hover:shadow-2xl_6d3e9",
  "md:hover:shadow-inner": "_md:hover:shadow-inner_6d3e9",
  "md:hover:shadow-outline": "_md:hover:shadow-outline_6d3e9",
  "md:hover:shadow-none": "_md:hover:shadow-none_6d3e9",
  "md:focus:shadow": "_md:focus:shadow_6d3e9",
  "md:focus:shadow-md": "_md:focus:shadow-md_6d3e9",
  "md:focus:shadow-lg": "_md:focus:shadow-lg_6d3e9",
  "md:focus:shadow-xl": "_md:focus:shadow-xl_6d3e9",
  "md:focus:shadow-2xl": "_md:focus:shadow-2xl_6d3e9",
  "md:focus:shadow-inner": "_md:focus:shadow-inner_6d3e9",
  "md:focus:shadow-outline": "_md:focus:shadow-outline_6d3e9",
  "md:focus:shadow-none": "_md:focus:shadow-none_6d3e9",
  "md:fill-current": "_md:fill-current_6d3e9",
  "md:stroke-current": "_md:stroke-current_6d3e9",
  "md:table-auto": "_md:table-auto_6d3e9",
  "md:table-fixed": "_md:table-fixed_6d3e9",
  "md:text-left": "_md:text-left_6d3e9",
  "md:text-center": "_md:text-center_6d3e9",
  "md:text-right": "_md:text-right_6d3e9",
  "md:text-justify": "_md:text-justify_6d3e9",
  "md:text-transparent": "_md:text-transparent_6d3e9",
  "md:text-black": "_md:text-black_6d3e9",
  "md:text-white": "_md:text-white_6d3e9",
  "md:text-gray-100": "_md:text-gray-100_6d3e9",
  "md:text-gray-200": "_md:text-gray-200_6d3e9",
  "md:text-gray-300": "_md:text-gray-300_6d3e9",
  "md:text-gray-400": "_md:text-gray-400_6d3e9",
  "md:text-gray-500": "_md:text-gray-500_6d3e9",
  "md:text-gray-600": "_md:text-gray-600_6d3e9",
  "md:text-gray-700": "_md:text-gray-700_6d3e9",
  "md:text-gray-800": "_md:text-gray-800_6d3e9",
  "md:text-gray-900": "_md:text-gray-900_6d3e9",
  "md:text-red-100": "_md:text-red-100_6d3e9",
  "md:text-red-200": "_md:text-red-200_6d3e9",
  "md:text-red-300": "_md:text-red-300_6d3e9",
  "md:text-red-400": "_md:text-red-400_6d3e9",
  "md:text-red-500": "_md:text-red-500_6d3e9",
  "md:text-red-600": "_md:text-red-600_6d3e9",
  "md:text-red-700": "_md:text-red-700_6d3e9",
  "md:text-red-800": "_md:text-red-800_6d3e9",
  "md:text-red-900": "_md:text-red-900_6d3e9",
  "md:text-orange-100": "_md:text-orange-100_6d3e9",
  "md:text-orange-200": "_md:text-orange-200_6d3e9",
  "md:text-orange-300": "_md:text-orange-300_6d3e9",
  "md:text-orange-400": "_md:text-orange-400_6d3e9",
  "md:text-orange-500": "_md:text-orange-500_6d3e9",
  "md:text-orange-600": "_md:text-orange-600_6d3e9",
  "md:text-orange-700": "_md:text-orange-700_6d3e9",
  "md:text-orange-800": "_md:text-orange-800_6d3e9",
  "md:text-orange-900": "_md:text-orange-900_6d3e9",
  "md:text-yellow-100": "_md:text-yellow-100_6d3e9",
  "md:text-yellow-200": "_md:text-yellow-200_6d3e9",
  "md:text-yellow-300": "_md:text-yellow-300_6d3e9",
  "md:text-yellow-400": "_md:text-yellow-400_6d3e9",
  "md:text-yellow-500": "_md:text-yellow-500_6d3e9",
  "md:text-yellow-600": "_md:text-yellow-600_6d3e9",
  "md:text-yellow-700": "_md:text-yellow-700_6d3e9",
  "md:text-yellow-800": "_md:text-yellow-800_6d3e9",
  "md:text-yellow-900": "_md:text-yellow-900_6d3e9",
  "md:text-green-100": "_md:text-green-100_6d3e9",
  "md:text-green-200": "_md:text-green-200_6d3e9",
  "md:text-green-300": "_md:text-green-300_6d3e9",
  "md:text-green-400": "_md:text-green-400_6d3e9",
  "md:text-green-500": "_md:text-green-500_6d3e9",
  "md:text-green-600": "_md:text-green-600_6d3e9",
  "md:text-green-700": "_md:text-green-700_6d3e9",
  "md:text-green-800": "_md:text-green-800_6d3e9",
  "md:text-green-900": "_md:text-green-900_6d3e9",
  "md:text-teal-100": "_md:text-teal-100_6d3e9",
  "md:text-teal-200": "_md:text-teal-200_6d3e9",
  "md:text-teal-300": "_md:text-teal-300_6d3e9",
  "md:text-teal-400": "_md:text-teal-400_6d3e9",
  "md:text-teal-500": "_md:text-teal-500_6d3e9",
  "md:text-teal-600": "_md:text-teal-600_6d3e9",
  "md:text-teal-700": "_md:text-teal-700_6d3e9",
  "md:text-teal-800": "_md:text-teal-800_6d3e9",
  "md:text-teal-900": "_md:text-teal-900_6d3e9",
  "md:text-blue-100": "_md:text-blue-100_6d3e9",
  "md:text-blue-200": "_md:text-blue-200_6d3e9",
  "md:text-blue-300": "_md:text-blue-300_6d3e9",
  "md:text-blue-400": "_md:text-blue-400_6d3e9",
  "md:text-blue-500": "_md:text-blue-500_6d3e9",
  "md:text-blue-600": "_md:text-blue-600_6d3e9",
  "md:text-blue-700": "_md:text-blue-700_6d3e9",
  "md:text-blue-800": "_md:text-blue-800_6d3e9",
  "md:text-blue-900": "_md:text-blue-900_6d3e9",
  "md:text-indigo-100": "_md:text-indigo-100_6d3e9",
  "md:text-indigo-200": "_md:text-indigo-200_6d3e9",
  "md:text-indigo-300": "_md:text-indigo-300_6d3e9",
  "md:text-indigo-400": "_md:text-indigo-400_6d3e9",
  "md:text-indigo-500": "_md:text-indigo-500_6d3e9",
  "md:text-indigo-600": "_md:text-indigo-600_6d3e9",
  "md:text-indigo-700": "_md:text-indigo-700_6d3e9",
  "md:text-indigo-800": "_md:text-indigo-800_6d3e9",
  "md:text-indigo-900": "_md:text-indigo-900_6d3e9",
  "md:text-purple-100": "_md:text-purple-100_6d3e9",
  "md:text-purple-200": "_md:text-purple-200_6d3e9",
  "md:text-purple-300": "_md:text-purple-300_6d3e9",
  "md:text-purple-400": "_md:text-purple-400_6d3e9",
  "md:text-purple-500": "_md:text-purple-500_6d3e9",
  "md:text-purple-600": "_md:text-purple-600_6d3e9",
  "md:text-purple-700": "_md:text-purple-700_6d3e9",
  "md:text-purple-800": "_md:text-purple-800_6d3e9",
  "md:text-purple-900": "_md:text-purple-900_6d3e9",
  "md:text-pink-100": "_md:text-pink-100_6d3e9",
  "md:text-pink-200": "_md:text-pink-200_6d3e9",
  "md:text-pink-300": "_md:text-pink-300_6d3e9",
  "md:text-pink-400": "_md:text-pink-400_6d3e9",
  "md:text-pink-500": "_md:text-pink-500_6d3e9",
  "md:text-pink-600": "_md:text-pink-600_6d3e9",
  "md:text-pink-700": "_md:text-pink-700_6d3e9",
  "md:text-pink-800": "_md:text-pink-800_6d3e9",
  "md:text-pink-900": "_md:text-pink-900_6d3e9",
  "md:text-primary-100": "_md:text-primary-100_6d3e9",
  "md:text-primary-200": "_md:text-primary-200_6d3e9",
  "md:text-primary-300": "_md:text-primary-300_6d3e9",
  "md:text-primary-400": "_md:text-primary-400_6d3e9",
  "md:text-primary-500": "_md:text-primary-500_6d3e9",
  "md:text-primary-600": "_md:text-primary-600_6d3e9",
  "md:text-primary-700": "_md:text-primary-700_6d3e9",
  "md:text-primary-800": "_md:text-primary-800_6d3e9",
  "md:text-primary-900": "_md:text-primary-900_6d3e9",
  "md:text-secondary-100": "_md:text-secondary-100_6d3e9",
  "md:text-secondary-200": "_md:text-secondary-200_6d3e9",
  "md:text-secondary-300": "_md:text-secondary-300_6d3e9",
  "md:text-secondary-400": "_md:text-secondary-400_6d3e9",
  "md:text-secondary-500": "_md:text-secondary-500_6d3e9",
  "md:text-secondary-600": "_md:text-secondary-600_6d3e9",
  "md:text-secondary-700": "_md:text-secondary-700_6d3e9",
  "md:text-secondary-800": "_md:text-secondary-800_6d3e9",
  "md:text-secondary-900": "_md:text-secondary-900_6d3e9",
  "md:hover:text-transparent": "_md:hover:text-transparent_6d3e9",
  "md:hover:text-black": "_md:hover:text-black_6d3e9",
  "md:hover:text-white": "_md:hover:text-white_6d3e9",
  "md:hover:text-gray-100": "_md:hover:text-gray-100_6d3e9",
  "md:hover:text-gray-200": "_md:hover:text-gray-200_6d3e9",
  "md:hover:text-gray-300": "_md:hover:text-gray-300_6d3e9",
  "md:hover:text-gray-400": "_md:hover:text-gray-400_6d3e9",
  "md:hover:text-gray-500": "_md:hover:text-gray-500_6d3e9",
  "md:hover:text-gray-600": "_md:hover:text-gray-600_6d3e9",
  "md:hover:text-gray-700": "_md:hover:text-gray-700_6d3e9",
  "md:hover:text-gray-800": "_md:hover:text-gray-800_6d3e9",
  "md:hover:text-gray-900": "_md:hover:text-gray-900_6d3e9",
  "md:hover:text-red-100": "_md:hover:text-red-100_6d3e9",
  "md:hover:text-red-200": "_md:hover:text-red-200_6d3e9",
  "md:hover:text-red-300": "_md:hover:text-red-300_6d3e9",
  "md:hover:text-red-400": "_md:hover:text-red-400_6d3e9",
  "md:hover:text-red-500": "_md:hover:text-red-500_6d3e9",
  "md:hover:text-red-600": "_md:hover:text-red-600_6d3e9",
  "md:hover:text-red-700": "_md:hover:text-red-700_6d3e9",
  "md:hover:text-red-800": "_md:hover:text-red-800_6d3e9",
  "md:hover:text-red-900": "_md:hover:text-red-900_6d3e9",
  "md:hover:text-orange-100": "_md:hover:text-orange-100_6d3e9",
  "md:hover:text-orange-200": "_md:hover:text-orange-200_6d3e9",
  "md:hover:text-orange-300": "_md:hover:text-orange-300_6d3e9",
  "md:hover:text-orange-400": "_md:hover:text-orange-400_6d3e9",
  "md:hover:text-orange-500": "_md:hover:text-orange-500_6d3e9",
  "md:hover:text-orange-600": "_md:hover:text-orange-600_6d3e9",
  "md:hover:text-orange-700": "_md:hover:text-orange-700_6d3e9",
  "md:hover:text-orange-800": "_md:hover:text-orange-800_6d3e9",
  "md:hover:text-orange-900": "_md:hover:text-orange-900_6d3e9",
  "md:hover:text-yellow-100": "_md:hover:text-yellow-100_6d3e9",
  "md:hover:text-yellow-200": "_md:hover:text-yellow-200_6d3e9",
  "md:hover:text-yellow-300": "_md:hover:text-yellow-300_6d3e9",
  "md:hover:text-yellow-400": "_md:hover:text-yellow-400_6d3e9",
  "md:hover:text-yellow-500": "_md:hover:text-yellow-500_6d3e9",
  "md:hover:text-yellow-600": "_md:hover:text-yellow-600_6d3e9",
  "md:hover:text-yellow-700": "_md:hover:text-yellow-700_6d3e9",
  "md:hover:text-yellow-800": "_md:hover:text-yellow-800_6d3e9",
  "md:hover:text-yellow-900": "_md:hover:text-yellow-900_6d3e9",
  "md:hover:text-green-100": "_md:hover:text-green-100_6d3e9",
  "md:hover:text-green-200": "_md:hover:text-green-200_6d3e9",
  "md:hover:text-green-300": "_md:hover:text-green-300_6d3e9",
  "md:hover:text-green-400": "_md:hover:text-green-400_6d3e9",
  "md:hover:text-green-500": "_md:hover:text-green-500_6d3e9",
  "md:hover:text-green-600": "_md:hover:text-green-600_6d3e9",
  "md:hover:text-green-700": "_md:hover:text-green-700_6d3e9",
  "md:hover:text-green-800": "_md:hover:text-green-800_6d3e9",
  "md:hover:text-green-900": "_md:hover:text-green-900_6d3e9",
  "md:hover:text-teal-100": "_md:hover:text-teal-100_6d3e9",
  "md:hover:text-teal-200": "_md:hover:text-teal-200_6d3e9",
  "md:hover:text-teal-300": "_md:hover:text-teal-300_6d3e9",
  "md:hover:text-teal-400": "_md:hover:text-teal-400_6d3e9",
  "md:hover:text-teal-500": "_md:hover:text-teal-500_6d3e9",
  "md:hover:text-teal-600": "_md:hover:text-teal-600_6d3e9",
  "md:hover:text-teal-700": "_md:hover:text-teal-700_6d3e9",
  "md:hover:text-teal-800": "_md:hover:text-teal-800_6d3e9",
  "md:hover:text-teal-900": "_md:hover:text-teal-900_6d3e9",
  "md:hover:text-blue-100": "_md:hover:text-blue-100_6d3e9",
  "md:hover:text-blue-200": "_md:hover:text-blue-200_6d3e9",
  "md:hover:text-blue-300": "_md:hover:text-blue-300_6d3e9",
  "md:hover:text-blue-400": "_md:hover:text-blue-400_6d3e9",
  "md:hover:text-blue-500": "_md:hover:text-blue-500_6d3e9",
  "md:hover:text-blue-600": "_md:hover:text-blue-600_6d3e9",
  "md:hover:text-blue-700": "_md:hover:text-blue-700_6d3e9",
  "md:hover:text-blue-800": "_md:hover:text-blue-800_6d3e9",
  "md:hover:text-blue-900": "_md:hover:text-blue-900_6d3e9",
  "md:hover:text-indigo-100": "_md:hover:text-indigo-100_6d3e9",
  "md:hover:text-indigo-200": "_md:hover:text-indigo-200_6d3e9",
  "md:hover:text-indigo-300": "_md:hover:text-indigo-300_6d3e9",
  "md:hover:text-indigo-400": "_md:hover:text-indigo-400_6d3e9",
  "md:hover:text-indigo-500": "_md:hover:text-indigo-500_6d3e9",
  "md:hover:text-indigo-600": "_md:hover:text-indigo-600_6d3e9",
  "md:hover:text-indigo-700": "_md:hover:text-indigo-700_6d3e9",
  "md:hover:text-indigo-800": "_md:hover:text-indigo-800_6d3e9",
  "md:hover:text-indigo-900": "_md:hover:text-indigo-900_6d3e9",
  "md:hover:text-purple-100": "_md:hover:text-purple-100_6d3e9",
  "md:hover:text-purple-200": "_md:hover:text-purple-200_6d3e9",
  "md:hover:text-purple-300": "_md:hover:text-purple-300_6d3e9",
  "md:hover:text-purple-400": "_md:hover:text-purple-400_6d3e9",
  "md:hover:text-purple-500": "_md:hover:text-purple-500_6d3e9",
  "md:hover:text-purple-600": "_md:hover:text-purple-600_6d3e9",
  "md:hover:text-purple-700": "_md:hover:text-purple-700_6d3e9",
  "md:hover:text-purple-800": "_md:hover:text-purple-800_6d3e9",
  "md:hover:text-purple-900": "_md:hover:text-purple-900_6d3e9",
  "md:hover:text-pink-100": "_md:hover:text-pink-100_6d3e9",
  "md:hover:text-pink-200": "_md:hover:text-pink-200_6d3e9",
  "md:hover:text-pink-300": "_md:hover:text-pink-300_6d3e9",
  "md:hover:text-pink-400": "_md:hover:text-pink-400_6d3e9",
  "md:hover:text-pink-500": "_md:hover:text-pink-500_6d3e9",
  "md:hover:text-pink-600": "_md:hover:text-pink-600_6d3e9",
  "md:hover:text-pink-700": "_md:hover:text-pink-700_6d3e9",
  "md:hover:text-pink-800": "_md:hover:text-pink-800_6d3e9",
  "md:hover:text-pink-900": "_md:hover:text-pink-900_6d3e9",
  "md:hover:text-primary-100": "_md:hover:text-primary-100_6d3e9",
  "md:hover:text-primary-200": "_md:hover:text-primary-200_6d3e9",
  "md:hover:text-primary-300": "_md:hover:text-primary-300_6d3e9",
  "md:hover:text-primary-400": "_md:hover:text-primary-400_6d3e9",
  "md:hover:text-primary-500": "_md:hover:text-primary-500_6d3e9",
  "md:hover:text-primary-600": "_md:hover:text-primary-600_6d3e9",
  "md:hover:text-primary-700": "_md:hover:text-primary-700_6d3e9",
  "md:hover:text-primary-800": "_md:hover:text-primary-800_6d3e9",
  "md:hover:text-primary-900": "_md:hover:text-primary-900_6d3e9",
  "md:hover:text-secondary-100": "_md:hover:text-secondary-100_6d3e9",
  "md:hover:text-secondary-200": "_md:hover:text-secondary-200_6d3e9",
  "md:hover:text-secondary-300": "_md:hover:text-secondary-300_6d3e9",
  "md:hover:text-secondary-400": "_md:hover:text-secondary-400_6d3e9",
  "md:hover:text-secondary-500": "_md:hover:text-secondary-500_6d3e9",
  "md:hover:text-secondary-600": "_md:hover:text-secondary-600_6d3e9",
  "md:hover:text-secondary-700": "_md:hover:text-secondary-700_6d3e9",
  "md:hover:text-secondary-800": "_md:hover:text-secondary-800_6d3e9",
  "md:hover:text-secondary-900": "_md:hover:text-secondary-900_6d3e9",
  "md:focus:text-transparent": "_md:focus:text-transparent_6d3e9",
  "md:focus:text-black": "_md:focus:text-black_6d3e9",
  "md:focus:text-white": "_md:focus:text-white_6d3e9",
  "md:focus:text-gray-100": "_md:focus:text-gray-100_6d3e9",
  "md:focus:text-gray-200": "_md:focus:text-gray-200_6d3e9",
  "md:focus:text-gray-300": "_md:focus:text-gray-300_6d3e9",
  "md:focus:text-gray-400": "_md:focus:text-gray-400_6d3e9",
  "md:focus:text-gray-500": "_md:focus:text-gray-500_6d3e9",
  "md:focus:text-gray-600": "_md:focus:text-gray-600_6d3e9",
  "md:focus:text-gray-700": "_md:focus:text-gray-700_6d3e9",
  "md:focus:text-gray-800": "_md:focus:text-gray-800_6d3e9",
  "md:focus:text-gray-900": "_md:focus:text-gray-900_6d3e9",
  "md:focus:text-red-100": "_md:focus:text-red-100_6d3e9",
  "md:focus:text-red-200": "_md:focus:text-red-200_6d3e9",
  "md:focus:text-red-300": "_md:focus:text-red-300_6d3e9",
  "md:focus:text-red-400": "_md:focus:text-red-400_6d3e9",
  "md:focus:text-red-500": "_md:focus:text-red-500_6d3e9",
  "md:focus:text-red-600": "_md:focus:text-red-600_6d3e9",
  "md:focus:text-red-700": "_md:focus:text-red-700_6d3e9",
  "md:focus:text-red-800": "_md:focus:text-red-800_6d3e9",
  "md:focus:text-red-900": "_md:focus:text-red-900_6d3e9",
  "md:focus:text-orange-100": "_md:focus:text-orange-100_6d3e9",
  "md:focus:text-orange-200": "_md:focus:text-orange-200_6d3e9",
  "md:focus:text-orange-300": "_md:focus:text-orange-300_6d3e9",
  "md:focus:text-orange-400": "_md:focus:text-orange-400_6d3e9",
  "md:focus:text-orange-500": "_md:focus:text-orange-500_6d3e9",
  "md:focus:text-orange-600": "_md:focus:text-orange-600_6d3e9",
  "md:focus:text-orange-700": "_md:focus:text-orange-700_6d3e9",
  "md:focus:text-orange-800": "_md:focus:text-orange-800_6d3e9",
  "md:focus:text-orange-900": "_md:focus:text-orange-900_6d3e9",
  "md:focus:text-yellow-100": "_md:focus:text-yellow-100_6d3e9",
  "md:focus:text-yellow-200": "_md:focus:text-yellow-200_6d3e9",
  "md:focus:text-yellow-300": "_md:focus:text-yellow-300_6d3e9",
  "md:focus:text-yellow-400": "_md:focus:text-yellow-400_6d3e9",
  "md:focus:text-yellow-500": "_md:focus:text-yellow-500_6d3e9",
  "md:focus:text-yellow-600": "_md:focus:text-yellow-600_6d3e9",
  "md:focus:text-yellow-700": "_md:focus:text-yellow-700_6d3e9",
  "md:focus:text-yellow-800": "_md:focus:text-yellow-800_6d3e9",
  "md:focus:text-yellow-900": "_md:focus:text-yellow-900_6d3e9",
  "md:focus:text-green-100": "_md:focus:text-green-100_6d3e9",
  "md:focus:text-green-200": "_md:focus:text-green-200_6d3e9",
  "md:focus:text-green-300": "_md:focus:text-green-300_6d3e9",
  "md:focus:text-green-400": "_md:focus:text-green-400_6d3e9",
  "md:focus:text-green-500": "_md:focus:text-green-500_6d3e9",
  "md:focus:text-green-600": "_md:focus:text-green-600_6d3e9",
  "md:focus:text-green-700": "_md:focus:text-green-700_6d3e9",
  "md:focus:text-green-800": "_md:focus:text-green-800_6d3e9",
  "md:focus:text-green-900": "_md:focus:text-green-900_6d3e9",
  "md:focus:text-teal-100": "_md:focus:text-teal-100_6d3e9",
  "md:focus:text-teal-200": "_md:focus:text-teal-200_6d3e9",
  "md:focus:text-teal-300": "_md:focus:text-teal-300_6d3e9",
  "md:focus:text-teal-400": "_md:focus:text-teal-400_6d3e9",
  "md:focus:text-teal-500": "_md:focus:text-teal-500_6d3e9",
  "md:focus:text-teal-600": "_md:focus:text-teal-600_6d3e9",
  "md:focus:text-teal-700": "_md:focus:text-teal-700_6d3e9",
  "md:focus:text-teal-800": "_md:focus:text-teal-800_6d3e9",
  "md:focus:text-teal-900": "_md:focus:text-teal-900_6d3e9",
  "md:focus:text-blue-100": "_md:focus:text-blue-100_6d3e9",
  "md:focus:text-blue-200": "_md:focus:text-blue-200_6d3e9",
  "md:focus:text-blue-300": "_md:focus:text-blue-300_6d3e9",
  "md:focus:text-blue-400": "_md:focus:text-blue-400_6d3e9",
  "md:focus:text-blue-500": "_md:focus:text-blue-500_6d3e9",
  "md:focus:text-blue-600": "_md:focus:text-blue-600_6d3e9",
  "md:focus:text-blue-700": "_md:focus:text-blue-700_6d3e9",
  "md:focus:text-blue-800": "_md:focus:text-blue-800_6d3e9",
  "md:focus:text-blue-900": "_md:focus:text-blue-900_6d3e9",
  "md:focus:text-indigo-100": "_md:focus:text-indigo-100_6d3e9",
  "md:focus:text-indigo-200": "_md:focus:text-indigo-200_6d3e9",
  "md:focus:text-indigo-300": "_md:focus:text-indigo-300_6d3e9",
  "md:focus:text-indigo-400": "_md:focus:text-indigo-400_6d3e9",
  "md:focus:text-indigo-500": "_md:focus:text-indigo-500_6d3e9",
  "md:focus:text-indigo-600": "_md:focus:text-indigo-600_6d3e9",
  "md:focus:text-indigo-700": "_md:focus:text-indigo-700_6d3e9",
  "md:focus:text-indigo-800": "_md:focus:text-indigo-800_6d3e9",
  "md:focus:text-indigo-900": "_md:focus:text-indigo-900_6d3e9",
  "md:focus:text-purple-100": "_md:focus:text-purple-100_6d3e9",
  "md:focus:text-purple-200": "_md:focus:text-purple-200_6d3e9",
  "md:focus:text-purple-300": "_md:focus:text-purple-300_6d3e9",
  "md:focus:text-purple-400": "_md:focus:text-purple-400_6d3e9",
  "md:focus:text-purple-500": "_md:focus:text-purple-500_6d3e9",
  "md:focus:text-purple-600": "_md:focus:text-purple-600_6d3e9",
  "md:focus:text-purple-700": "_md:focus:text-purple-700_6d3e9",
  "md:focus:text-purple-800": "_md:focus:text-purple-800_6d3e9",
  "md:focus:text-purple-900": "_md:focus:text-purple-900_6d3e9",
  "md:focus:text-pink-100": "_md:focus:text-pink-100_6d3e9",
  "md:focus:text-pink-200": "_md:focus:text-pink-200_6d3e9",
  "md:focus:text-pink-300": "_md:focus:text-pink-300_6d3e9",
  "md:focus:text-pink-400": "_md:focus:text-pink-400_6d3e9",
  "md:focus:text-pink-500": "_md:focus:text-pink-500_6d3e9",
  "md:focus:text-pink-600": "_md:focus:text-pink-600_6d3e9",
  "md:focus:text-pink-700": "_md:focus:text-pink-700_6d3e9",
  "md:focus:text-pink-800": "_md:focus:text-pink-800_6d3e9",
  "md:focus:text-pink-900": "_md:focus:text-pink-900_6d3e9",
  "md:focus:text-primary-100": "_md:focus:text-primary-100_6d3e9",
  "md:focus:text-primary-200": "_md:focus:text-primary-200_6d3e9",
  "md:focus:text-primary-300": "_md:focus:text-primary-300_6d3e9",
  "md:focus:text-primary-400": "_md:focus:text-primary-400_6d3e9",
  "md:focus:text-primary-500": "_md:focus:text-primary-500_6d3e9",
  "md:focus:text-primary-600": "_md:focus:text-primary-600_6d3e9",
  "md:focus:text-primary-700": "_md:focus:text-primary-700_6d3e9",
  "md:focus:text-primary-800": "_md:focus:text-primary-800_6d3e9",
  "md:focus:text-primary-900": "_md:focus:text-primary-900_6d3e9",
  "md:focus:text-secondary-100": "_md:focus:text-secondary-100_6d3e9",
  "md:focus:text-secondary-200": "_md:focus:text-secondary-200_6d3e9",
  "md:focus:text-secondary-300": "_md:focus:text-secondary-300_6d3e9",
  "md:focus:text-secondary-400": "_md:focus:text-secondary-400_6d3e9",
  "md:focus:text-secondary-500": "_md:focus:text-secondary-500_6d3e9",
  "md:focus:text-secondary-600": "_md:focus:text-secondary-600_6d3e9",
  "md:focus:text-secondary-700": "_md:focus:text-secondary-700_6d3e9",
  "md:focus:text-secondary-800": "_md:focus:text-secondary-800_6d3e9",
  "md:focus:text-secondary-900": "_md:focus:text-secondary-900_6d3e9",
  "md:text-xs": "_md:text-xs_6d3e9",
  "md:text-sm": "_md:text-sm_6d3e9",
  "md:text-base": "_md:text-base_6d3e9",
  "md:text-lg": "_md:text-lg_6d3e9",
  "md:text-xl": "_md:text-xl_6d3e9",
  "md:text-2xl": "_md:text-2xl_6d3e9",
  "md:text-3xl": "_md:text-3xl_6d3e9",
  "md:text-4xl": "_md:text-4xl_6d3e9",
  "md:text-5xl": "_md:text-5xl_6d3e9",
  "md:text-6xl": "_md:text-6xl_6d3e9",
  "md:italic": "_md:italic_6d3e9",
  "md:not-italic": "_md:not-italic_6d3e9",
  "md:uppercase": "_md:uppercase_6d3e9",
  "md:lowercase": "_md:lowercase_6d3e9",
  "md:capitalize": "_md:capitalize_6d3e9",
  "md:normal-case": "_md:normal-case_6d3e9",
  "md:underline": "_md:underline_6d3e9",
  "md:line-through": "_md:line-through_6d3e9",
  "md:no-underline": "_md:no-underline_6d3e9",
  "md:hover:underline": "_md:hover:underline_6d3e9",
  "md:hover:line-through": "_md:hover:line-through_6d3e9",
  "md:hover:no-underline": "_md:hover:no-underline_6d3e9",
  "md:focus:underline": "_md:focus:underline_6d3e9",
  "md:focus:line-through": "_md:focus:line-through_6d3e9",
  "md:focus:no-underline": "_md:focus:no-underline_6d3e9",
  "md:antialiased": "_md:antialiased_6d3e9",
  "md:subpixel-antialiased": "_md:subpixel-antialiased_6d3e9",
  "md:tracking-tighter": "_md:tracking-tighter_6d3e9",
  "md:tracking-tight": "_md:tracking-tight_6d3e9",
  "md:tracking-normal": "_md:tracking-normal_6d3e9",
  "md:tracking-wide": "_md:tracking-wide_6d3e9",
  "md:tracking-wider": "_md:tracking-wider_6d3e9",
  "md:tracking-widest": "_md:tracking-widest_6d3e9",
  "md:select-none": "_md:select-none_6d3e9",
  "md:select-text": "_md:select-text_6d3e9",
  "md:select-all": "_md:select-all_6d3e9",
  "md:select-auto": "_md:select-auto_6d3e9",
  "md:align-baseline": "_md:align-baseline_6d3e9",
  "md:align-top": "_md:align-top_6d3e9",
  "md:align-middle": "_md:align-middle_6d3e9",
  "md:align-bottom": "_md:align-bottom_6d3e9",
  "md:align-text-top": "_md:align-text-top_6d3e9",
  "md:align-text-bottom": "_md:align-text-bottom_6d3e9",
  "md:visible": "_md:visible_6d3e9",
  "md:invisible": "_md:invisible_6d3e9",
  "md:whitespace-normal": "_md:whitespace-normal_6d3e9",
  "md:whitespace-no-wrap": "_md:whitespace-no-wrap_6d3e9",
  "md:whitespace-pre": "_md:whitespace-pre_6d3e9",
  "md:whitespace-pre-line": "_md:whitespace-pre-line_6d3e9",
  "md:whitespace-pre-wrap": "_md:whitespace-pre-wrap_6d3e9",
  "md:break-normal": "_md:break-normal_6d3e9",
  "md:break-words": "_md:break-words_6d3e9",
  "md:break-all": "_md:break-all_6d3e9",
  "md:truncate": "_md:truncate_6d3e9",
  "md:w-0": "_md:w-0_6d3e9",
  "md:w-1": "_md:w-1_6d3e9",
  "md:w-2": "_md:w-2_6d3e9",
  "md:w-3": "_md:w-3_6d3e9",
  "md:w-4": "_md:w-4_6d3e9",
  "md:w-5": "_md:w-5_6d3e9",
  "md:w-6": "_md:w-6_6d3e9",
  "md:w-8": "_md:w-8_6d3e9",
  "md:w-10": "_md:w-10_6d3e9",
  "md:w-12": "_md:w-12_6d3e9",
  "md:w-16": "_md:w-16_6d3e9",
  "md:w-20": "_md:w-20_6d3e9",
  "md:w-24": "_md:w-24_6d3e9",
  "md:w-32": "_md:w-32_6d3e9",
  "md:w-40": "_md:w-40_6d3e9",
  "md:w-48": "_md:w-48_6d3e9",
  "md:w-56": "_md:w-56_6d3e9",
  "md:w-64": "_md:w-64_6d3e9",
  "md:w-auto": "_md:w-auto_6d3e9",
  "md:w-px": "_md:w-px_6d3e9",
  "md:w-1/2": "_md:w-1/2_6d3e9",
  "md:w-1/3": "_md:w-1/3_6d3e9",
  "md:w-2/3": "_md:w-2/3_6d3e9",
  "md:w-1/4": "_md:w-1/4_6d3e9",
  "md:w-2/4": "_md:w-2/4_6d3e9",
  "md:w-3/4": "_md:w-3/4_6d3e9",
  "md:w-1/5": "_md:w-1/5_6d3e9",
  "md:w-2/5": "_md:w-2/5_6d3e9",
  "md:w-3/5": "_md:w-3/5_6d3e9",
  "md:w-4/5": "_md:w-4/5_6d3e9",
  "md:w-1/6": "_md:w-1/6_6d3e9",
  "md:w-2/6": "_md:w-2/6_6d3e9",
  "md:w-3/6": "_md:w-3/6_6d3e9",
  "md:w-4/6": "_md:w-4/6_6d3e9",
  "md:w-5/6": "_md:w-5/6_6d3e9",
  "md:w-1/12": "_md:w-1/12_6d3e9",
  "md:w-2/12": "_md:w-2/12_6d3e9",
  "md:w-3/12": "_md:w-3/12_6d3e9",
  "md:w-4/12": "_md:w-4/12_6d3e9",
  "md:w-5/12": "_md:w-5/12_6d3e9",
  "md:w-6/12": "_md:w-6/12_6d3e9",
  "md:w-7/12": "_md:w-7/12_6d3e9",
  "md:w-8/12": "_md:w-8/12_6d3e9",
  "md:w-9/12": "_md:w-9/12_6d3e9",
  "md:w-10/12": "_md:w-10/12_6d3e9",
  "md:w-11/12": "_md:w-11/12_6d3e9",
  "md:w-full": "_md:w-full_6d3e9",
  "md:w-screen": "_md:w-screen_6d3e9",
  "md:z-0": "_md:z-0_6d3e9",
  "md:z-10": "_md:z-10_6d3e9",
  "md:z-20": "_md:z-20_6d3e9",
  "md:z-30": "_md:z-30_6d3e9",
  "md:z-40": "_md:z-40_6d3e9",
  "md:z-50": "_md:z-50_6d3e9",
  "md:z-auto": "_md:z-auto_6d3e9",
  "lg:sr-only": "_lg:sr-only_6d3e9",
  "lg:not-sr-only": "_lg:not-sr-only_6d3e9",
  "lg:focus:sr-only": "_lg:focus:sr-only_6d3e9",
  "lg:focus:not-sr-only": "_lg:focus:not-sr-only_6d3e9",
  "lg:appearance-none": "_lg:appearance-none_6d3e9",
  "lg:bg-fixed": "_lg:bg-fixed_6d3e9",
  "lg:bg-local": "_lg:bg-local_6d3e9",
  "lg:bg-scroll": "_lg:bg-scroll_6d3e9",
  "lg:bg-transparent": "_lg:bg-transparent_6d3e9",
  "lg:bg-black": "_lg:bg-black_6d3e9",
  "lg:bg-white": "_lg:bg-white_6d3e9",
  "lg:bg-gray-100": "_lg:bg-gray-100_6d3e9",
  "lg:bg-gray-200": "_lg:bg-gray-200_6d3e9",
  "lg:bg-gray-300": "_lg:bg-gray-300_6d3e9",
  "lg:bg-gray-400": "_lg:bg-gray-400_6d3e9",
  "lg:bg-gray-500": "_lg:bg-gray-500_6d3e9",
  "lg:bg-gray-600": "_lg:bg-gray-600_6d3e9",
  "lg:bg-gray-700": "_lg:bg-gray-700_6d3e9",
  "lg:bg-gray-800": "_lg:bg-gray-800_6d3e9",
  "lg:bg-gray-900": "_lg:bg-gray-900_6d3e9",
  "lg:bg-red-100": "_lg:bg-red-100_6d3e9",
  "lg:bg-red-200": "_lg:bg-red-200_6d3e9",
  "lg:bg-red-300": "_lg:bg-red-300_6d3e9",
  "lg:bg-red-400": "_lg:bg-red-400_6d3e9",
  "lg:bg-red-500": "_lg:bg-red-500_6d3e9",
  "lg:bg-red-600": "_lg:bg-red-600_6d3e9",
  "lg:bg-red-700": "_lg:bg-red-700_6d3e9",
  "lg:bg-red-800": "_lg:bg-red-800_6d3e9",
  "lg:bg-red-900": "_lg:bg-red-900_6d3e9",
  "lg:bg-orange-100": "_lg:bg-orange-100_6d3e9",
  "lg:bg-orange-200": "_lg:bg-orange-200_6d3e9",
  "lg:bg-orange-300": "_lg:bg-orange-300_6d3e9",
  "lg:bg-orange-400": "_lg:bg-orange-400_6d3e9",
  "lg:bg-orange-500": "_lg:bg-orange-500_6d3e9",
  "lg:bg-orange-600": "_lg:bg-orange-600_6d3e9",
  "lg:bg-orange-700": "_lg:bg-orange-700_6d3e9",
  "lg:bg-orange-800": "_lg:bg-orange-800_6d3e9",
  "lg:bg-orange-900": "_lg:bg-orange-900_6d3e9",
  "lg:bg-yellow-100": "_lg:bg-yellow-100_6d3e9",
  "lg:bg-yellow-200": "_lg:bg-yellow-200_6d3e9",
  "lg:bg-yellow-300": "_lg:bg-yellow-300_6d3e9",
  "lg:bg-yellow-400": "_lg:bg-yellow-400_6d3e9",
  "lg:bg-yellow-500": "_lg:bg-yellow-500_6d3e9",
  "lg:bg-yellow-600": "_lg:bg-yellow-600_6d3e9",
  "lg:bg-yellow-700": "_lg:bg-yellow-700_6d3e9",
  "lg:bg-yellow-800": "_lg:bg-yellow-800_6d3e9",
  "lg:bg-yellow-900": "_lg:bg-yellow-900_6d3e9",
  "lg:bg-green-100": "_lg:bg-green-100_6d3e9",
  "lg:bg-green-200": "_lg:bg-green-200_6d3e9",
  "lg:bg-green-300": "_lg:bg-green-300_6d3e9",
  "lg:bg-green-400": "_lg:bg-green-400_6d3e9",
  "lg:bg-green-500": "_lg:bg-green-500_6d3e9",
  "lg:bg-green-600": "_lg:bg-green-600_6d3e9",
  "lg:bg-green-700": "_lg:bg-green-700_6d3e9",
  "lg:bg-green-800": "_lg:bg-green-800_6d3e9",
  "lg:bg-green-900": "_lg:bg-green-900_6d3e9",
  "lg:bg-teal-100": "_lg:bg-teal-100_6d3e9",
  "lg:bg-teal-200": "_lg:bg-teal-200_6d3e9",
  "lg:bg-teal-300": "_lg:bg-teal-300_6d3e9",
  "lg:bg-teal-400": "_lg:bg-teal-400_6d3e9",
  "lg:bg-teal-500": "_lg:bg-teal-500_6d3e9",
  "lg:bg-teal-600": "_lg:bg-teal-600_6d3e9",
  "lg:bg-teal-700": "_lg:bg-teal-700_6d3e9",
  "lg:bg-teal-800": "_lg:bg-teal-800_6d3e9",
  "lg:bg-teal-900": "_lg:bg-teal-900_6d3e9",
  "lg:bg-blue-100": "_lg:bg-blue-100_6d3e9",
  "lg:bg-blue-200": "_lg:bg-blue-200_6d3e9",
  "lg:bg-blue-300": "_lg:bg-blue-300_6d3e9",
  "lg:bg-blue-400": "_lg:bg-blue-400_6d3e9",
  "lg:bg-blue-500": "_lg:bg-blue-500_6d3e9",
  "lg:bg-blue-600": "_lg:bg-blue-600_6d3e9",
  "lg:bg-blue-700": "_lg:bg-blue-700_6d3e9",
  "lg:bg-blue-800": "_lg:bg-blue-800_6d3e9",
  "lg:bg-blue-900": "_lg:bg-blue-900_6d3e9",
  "lg:bg-indigo-100": "_lg:bg-indigo-100_6d3e9",
  "lg:bg-indigo-200": "_lg:bg-indigo-200_6d3e9",
  "lg:bg-indigo-300": "_lg:bg-indigo-300_6d3e9",
  "lg:bg-indigo-400": "_lg:bg-indigo-400_6d3e9",
  "lg:bg-indigo-500": "_lg:bg-indigo-500_6d3e9",
  "lg:bg-indigo-600": "_lg:bg-indigo-600_6d3e9",
  "lg:bg-indigo-700": "_lg:bg-indigo-700_6d3e9",
  "lg:bg-indigo-800": "_lg:bg-indigo-800_6d3e9",
  "lg:bg-indigo-900": "_lg:bg-indigo-900_6d3e9",
  "lg:bg-purple-100": "_lg:bg-purple-100_6d3e9",
  "lg:bg-purple-200": "_lg:bg-purple-200_6d3e9",
  "lg:bg-purple-300": "_lg:bg-purple-300_6d3e9",
  "lg:bg-purple-400": "_lg:bg-purple-400_6d3e9",
  "lg:bg-purple-500": "_lg:bg-purple-500_6d3e9",
  "lg:bg-purple-600": "_lg:bg-purple-600_6d3e9",
  "lg:bg-purple-700": "_lg:bg-purple-700_6d3e9",
  "lg:bg-purple-800": "_lg:bg-purple-800_6d3e9",
  "lg:bg-purple-900": "_lg:bg-purple-900_6d3e9",
  "lg:bg-pink-100": "_lg:bg-pink-100_6d3e9",
  "lg:bg-pink-200": "_lg:bg-pink-200_6d3e9",
  "lg:bg-pink-300": "_lg:bg-pink-300_6d3e9",
  "lg:bg-pink-400": "_lg:bg-pink-400_6d3e9",
  "lg:bg-pink-500": "_lg:bg-pink-500_6d3e9",
  "lg:bg-pink-600": "_lg:bg-pink-600_6d3e9",
  "lg:bg-pink-700": "_lg:bg-pink-700_6d3e9",
  "lg:bg-pink-800": "_lg:bg-pink-800_6d3e9",
  "lg:bg-pink-900": "_lg:bg-pink-900_6d3e9",
  "lg:bg-primary-100": "_lg:bg-primary-100_6d3e9",
  "lg:bg-primary-200": "_lg:bg-primary-200_6d3e9",
  "lg:bg-primary-300": "_lg:bg-primary-300_6d3e9",
  "lg:bg-primary-400": "_lg:bg-primary-400_6d3e9",
  "lg:bg-primary-500": "_lg:bg-primary-500_6d3e9",
  "lg:bg-primary-600": "_lg:bg-primary-600_6d3e9",
  "lg:bg-primary-700": "_lg:bg-primary-700_6d3e9",
  "lg:bg-primary-800": "_lg:bg-primary-800_6d3e9",
  "lg:bg-primary-900": "_lg:bg-primary-900_6d3e9",
  "lg:bg-secondary-100": "_lg:bg-secondary-100_6d3e9",
  "lg:bg-secondary-200": "_lg:bg-secondary-200_6d3e9",
  "lg:bg-secondary-300": "_lg:bg-secondary-300_6d3e9",
  "lg:bg-secondary-400": "_lg:bg-secondary-400_6d3e9",
  "lg:bg-secondary-500": "_lg:bg-secondary-500_6d3e9",
  "lg:bg-secondary-600": "_lg:bg-secondary-600_6d3e9",
  "lg:bg-secondary-700": "_lg:bg-secondary-700_6d3e9",
  "lg:bg-secondary-800": "_lg:bg-secondary-800_6d3e9",
  "lg:bg-secondary-900": "_lg:bg-secondary-900_6d3e9",
  "lg:hover:bg-transparent": "_lg:hover:bg-transparent_6d3e9",
  "lg:hover:bg-black": "_lg:hover:bg-black_6d3e9",
  "lg:hover:bg-white": "_lg:hover:bg-white_6d3e9",
  "lg:hover:bg-gray-100": "_lg:hover:bg-gray-100_6d3e9",
  "lg:hover:bg-gray-200": "_lg:hover:bg-gray-200_6d3e9",
  "lg:hover:bg-gray-300": "_lg:hover:bg-gray-300_6d3e9",
  "lg:hover:bg-gray-400": "_lg:hover:bg-gray-400_6d3e9",
  "lg:hover:bg-gray-500": "_lg:hover:bg-gray-500_6d3e9",
  "lg:hover:bg-gray-600": "_lg:hover:bg-gray-600_6d3e9",
  "lg:hover:bg-gray-700": "_lg:hover:bg-gray-700_6d3e9",
  "lg:hover:bg-gray-800": "_lg:hover:bg-gray-800_6d3e9",
  "lg:hover:bg-gray-900": "_lg:hover:bg-gray-900_6d3e9",
  "lg:hover:bg-red-100": "_lg:hover:bg-red-100_6d3e9",
  "lg:hover:bg-red-200": "_lg:hover:bg-red-200_6d3e9",
  "lg:hover:bg-red-300": "_lg:hover:bg-red-300_6d3e9",
  "lg:hover:bg-red-400": "_lg:hover:bg-red-400_6d3e9",
  "lg:hover:bg-red-500": "_lg:hover:bg-red-500_6d3e9",
  "lg:hover:bg-red-600": "_lg:hover:bg-red-600_6d3e9",
  "lg:hover:bg-red-700": "_lg:hover:bg-red-700_6d3e9",
  "lg:hover:bg-red-800": "_lg:hover:bg-red-800_6d3e9",
  "lg:hover:bg-red-900": "_lg:hover:bg-red-900_6d3e9",
  "lg:hover:bg-orange-100": "_lg:hover:bg-orange-100_6d3e9",
  "lg:hover:bg-orange-200": "_lg:hover:bg-orange-200_6d3e9",
  "lg:hover:bg-orange-300": "_lg:hover:bg-orange-300_6d3e9",
  "lg:hover:bg-orange-400": "_lg:hover:bg-orange-400_6d3e9",
  "lg:hover:bg-orange-500": "_lg:hover:bg-orange-500_6d3e9",
  "lg:hover:bg-orange-600": "_lg:hover:bg-orange-600_6d3e9",
  "lg:hover:bg-orange-700": "_lg:hover:bg-orange-700_6d3e9",
  "lg:hover:bg-orange-800": "_lg:hover:bg-orange-800_6d3e9",
  "lg:hover:bg-orange-900": "_lg:hover:bg-orange-900_6d3e9",
  "lg:hover:bg-yellow-100": "_lg:hover:bg-yellow-100_6d3e9",
  "lg:hover:bg-yellow-200": "_lg:hover:bg-yellow-200_6d3e9",
  "lg:hover:bg-yellow-300": "_lg:hover:bg-yellow-300_6d3e9",
  "lg:hover:bg-yellow-400": "_lg:hover:bg-yellow-400_6d3e9",
  "lg:hover:bg-yellow-500": "_lg:hover:bg-yellow-500_6d3e9",
  "lg:hover:bg-yellow-600": "_lg:hover:bg-yellow-600_6d3e9",
  "lg:hover:bg-yellow-700": "_lg:hover:bg-yellow-700_6d3e9",
  "lg:hover:bg-yellow-800": "_lg:hover:bg-yellow-800_6d3e9",
  "lg:hover:bg-yellow-900": "_lg:hover:bg-yellow-900_6d3e9",
  "lg:hover:bg-green-100": "_lg:hover:bg-green-100_6d3e9",
  "lg:hover:bg-green-200": "_lg:hover:bg-green-200_6d3e9",
  "lg:hover:bg-green-300": "_lg:hover:bg-green-300_6d3e9",
  "lg:hover:bg-green-400": "_lg:hover:bg-green-400_6d3e9",
  "lg:hover:bg-green-500": "_lg:hover:bg-green-500_6d3e9",
  "lg:hover:bg-green-600": "_lg:hover:bg-green-600_6d3e9",
  "lg:hover:bg-green-700": "_lg:hover:bg-green-700_6d3e9",
  "lg:hover:bg-green-800": "_lg:hover:bg-green-800_6d3e9",
  "lg:hover:bg-green-900": "_lg:hover:bg-green-900_6d3e9",
  "lg:hover:bg-teal-100": "_lg:hover:bg-teal-100_6d3e9",
  "lg:hover:bg-teal-200": "_lg:hover:bg-teal-200_6d3e9",
  "lg:hover:bg-teal-300": "_lg:hover:bg-teal-300_6d3e9",
  "lg:hover:bg-teal-400": "_lg:hover:bg-teal-400_6d3e9",
  "lg:hover:bg-teal-500": "_lg:hover:bg-teal-500_6d3e9",
  "lg:hover:bg-teal-600": "_lg:hover:bg-teal-600_6d3e9",
  "lg:hover:bg-teal-700": "_lg:hover:bg-teal-700_6d3e9",
  "lg:hover:bg-teal-800": "_lg:hover:bg-teal-800_6d3e9",
  "lg:hover:bg-teal-900": "_lg:hover:bg-teal-900_6d3e9",
  "lg:hover:bg-blue-100": "_lg:hover:bg-blue-100_6d3e9",
  "lg:hover:bg-blue-200": "_lg:hover:bg-blue-200_6d3e9",
  "lg:hover:bg-blue-300": "_lg:hover:bg-blue-300_6d3e9",
  "lg:hover:bg-blue-400": "_lg:hover:bg-blue-400_6d3e9",
  "lg:hover:bg-blue-500": "_lg:hover:bg-blue-500_6d3e9",
  "lg:hover:bg-blue-600": "_lg:hover:bg-blue-600_6d3e9",
  "lg:hover:bg-blue-700": "_lg:hover:bg-blue-700_6d3e9",
  "lg:hover:bg-blue-800": "_lg:hover:bg-blue-800_6d3e9",
  "lg:hover:bg-blue-900": "_lg:hover:bg-blue-900_6d3e9",
  "lg:hover:bg-indigo-100": "_lg:hover:bg-indigo-100_6d3e9",
  "lg:hover:bg-indigo-200": "_lg:hover:bg-indigo-200_6d3e9",
  "lg:hover:bg-indigo-300": "_lg:hover:bg-indigo-300_6d3e9",
  "lg:hover:bg-indigo-400": "_lg:hover:bg-indigo-400_6d3e9",
  "lg:hover:bg-indigo-500": "_lg:hover:bg-indigo-500_6d3e9",
  "lg:hover:bg-indigo-600": "_lg:hover:bg-indigo-600_6d3e9",
  "lg:hover:bg-indigo-700": "_lg:hover:bg-indigo-700_6d3e9",
  "lg:hover:bg-indigo-800": "_lg:hover:bg-indigo-800_6d3e9",
  "lg:hover:bg-indigo-900": "_lg:hover:bg-indigo-900_6d3e9",
  "lg:hover:bg-purple-100": "_lg:hover:bg-purple-100_6d3e9",
  "lg:hover:bg-purple-200": "_lg:hover:bg-purple-200_6d3e9",
  "lg:hover:bg-purple-300": "_lg:hover:bg-purple-300_6d3e9",
  "lg:hover:bg-purple-400": "_lg:hover:bg-purple-400_6d3e9",
  "lg:hover:bg-purple-500": "_lg:hover:bg-purple-500_6d3e9",
  "lg:hover:bg-purple-600": "_lg:hover:bg-purple-600_6d3e9",
  "lg:hover:bg-purple-700": "_lg:hover:bg-purple-700_6d3e9",
  "lg:hover:bg-purple-800": "_lg:hover:bg-purple-800_6d3e9",
  "lg:hover:bg-purple-900": "_lg:hover:bg-purple-900_6d3e9",
  "lg:hover:bg-pink-100": "_lg:hover:bg-pink-100_6d3e9",
  "lg:hover:bg-pink-200": "_lg:hover:bg-pink-200_6d3e9",
  "lg:hover:bg-pink-300": "_lg:hover:bg-pink-300_6d3e9",
  "lg:hover:bg-pink-400": "_lg:hover:bg-pink-400_6d3e9",
  "lg:hover:bg-pink-500": "_lg:hover:bg-pink-500_6d3e9",
  "lg:hover:bg-pink-600": "_lg:hover:bg-pink-600_6d3e9",
  "lg:hover:bg-pink-700": "_lg:hover:bg-pink-700_6d3e9",
  "lg:hover:bg-pink-800": "_lg:hover:bg-pink-800_6d3e9",
  "lg:hover:bg-pink-900": "_lg:hover:bg-pink-900_6d3e9",
  "lg:hover:bg-primary-100": "_lg:hover:bg-primary-100_6d3e9",
  "lg:hover:bg-primary-200": "_lg:hover:bg-primary-200_6d3e9",
  "lg:hover:bg-primary-300": "_lg:hover:bg-primary-300_6d3e9",
  "lg:hover:bg-primary-400": "_lg:hover:bg-primary-400_6d3e9",
  "lg:hover:bg-primary-500": "_lg:hover:bg-primary-500_6d3e9",
  "lg:hover:bg-primary-600": "_lg:hover:bg-primary-600_6d3e9",
  "lg:hover:bg-primary-700": "_lg:hover:bg-primary-700_6d3e9",
  "lg:hover:bg-primary-800": "_lg:hover:bg-primary-800_6d3e9",
  "lg:hover:bg-primary-900": "_lg:hover:bg-primary-900_6d3e9",
  "lg:hover:bg-secondary-100": "_lg:hover:bg-secondary-100_6d3e9",
  "lg:hover:bg-secondary-200": "_lg:hover:bg-secondary-200_6d3e9",
  "lg:hover:bg-secondary-300": "_lg:hover:bg-secondary-300_6d3e9",
  "lg:hover:bg-secondary-400": "_lg:hover:bg-secondary-400_6d3e9",
  "lg:hover:bg-secondary-500": "_lg:hover:bg-secondary-500_6d3e9",
  "lg:hover:bg-secondary-600": "_lg:hover:bg-secondary-600_6d3e9",
  "lg:hover:bg-secondary-700": "_lg:hover:bg-secondary-700_6d3e9",
  "lg:hover:bg-secondary-800": "_lg:hover:bg-secondary-800_6d3e9",
  "lg:hover:bg-secondary-900": "_lg:hover:bg-secondary-900_6d3e9",
  "lg:focus:bg-transparent": "_lg:focus:bg-transparent_6d3e9",
  "lg:focus:bg-black": "_lg:focus:bg-black_6d3e9",
  "lg:focus:bg-white": "_lg:focus:bg-white_6d3e9",
  "lg:focus:bg-gray-100": "_lg:focus:bg-gray-100_6d3e9",
  "lg:focus:bg-gray-200": "_lg:focus:bg-gray-200_6d3e9",
  "lg:focus:bg-gray-300": "_lg:focus:bg-gray-300_6d3e9",
  "lg:focus:bg-gray-400": "_lg:focus:bg-gray-400_6d3e9",
  "lg:focus:bg-gray-500": "_lg:focus:bg-gray-500_6d3e9",
  "lg:focus:bg-gray-600": "_lg:focus:bg-gray-600_6d3e9",
  "lg:focus:bg-gray-700": "_lg:focus:bg-gray-700_6d3e9",
  "lg:focus:bg-gray-800": "_lg:focus:bg-gray-800_6d3e9",
  "lg:focus:bg-gray-900": "_lg:focus:bg-gray-900_6d3e9",
  "lg:focus:bg-red-100": "_lg:focus:bg-red-100_6d3e9",
  "lg:focus:bg-red-200": "_lg:focus:bg-red-200_6d3e9",
  "lg:focus:bg-red-300": "_lg:focus:bg-red-300_6d3e9",
  "lg:focus:bg-red-400": "_lg:focus:bg-red-400_6d3e9",
  "lg:focus:bg-red-500": "_lg:focus:bg-red-500_6d3e9",
  "lg:focus:bg-red-600": "_lg:focus:bg-red-600_6d3e9",
  "lg:focus:bg-red-700": "_lg:focus:bg-red-700_6d3e9",
  "lg:focus:bg-red-800": "_lg:focus:bg-red-800_6d3e9",
  "lg:focus:bg-red-900": "_lg:focus:bg-red-900_6d3e9",
  "lg:focus:bg-orange-100": "_lg:focus:bg-orange-100_6d3e9",
  "lg:focus:bg-orange-200": "_lg:focus:bg-orange-200_6d3e9",
  "lg:focus:bg-orange-300": "_lg:focus:bg-orange-300_6d3e9",
  "lg:focus:bg-orange-400": "_lg:focus:bg-orange-400_6d3e9",
  "lg:focus:bg-orange-500": "_lg:focus:bg-orange-500_6d3e9",
  "lg:focus:bg-orange-600": "_lg:focus:bg-orange-600_6d3e9",
  "lg:focus:bg-orange-700": "_lg:focus:bg-orange-700_6d3e9",
  "lg:focus:bg-orange-800": "_lg:focus:bg-orange-800_6d3e9",
  "lg:focus:bg-orange-900": "_lg:focus:bg-orange-900_6d3e9",
  "lg:focus:bg-yellow-100": "_lg:focus:bg-yellow-100_6d3e9",
  "lg:focus:bg-yellow-200": "_lg:focus:bg-yellow-200_6d3e9",
  "lg:focus:bg-yellow-300": "_lg:focus:bg-yellow-300_6d3e9",
  "lg:focus:bg-yellow-400": "_lg:focus:bg-yellow-400_6d3e9",
  "lg:focus:bg-yellow-500": "_lg:focus:bg-yellow-500_6d3e9",
  "lg:focus:bg-yellow-600": "_lg:focus:bg-yellow-600_6d3e9",
  "lg:focus:bg-yellow-700": "_lg:focus:bg-yellow-700_6d3e9",
  "lg:focus:bg-yellow-800": "_lg:focus:bg-yellow-800_6d3e9",
  "lg:focus:bg-yellow-900": "_lg:focus:bg-yellow-900_6d3e9",
  "lg:focus:bg-green-100": "_lg:focus:bg-green-100_6d3e9",
  "lg:focus:bg-green-200": "_lg:focus:bg-green-200_6d3e9",
  "lg:focus:bg-green-300": "_lg:focus:bg-green-300_6d3e9",
  "lg:focus:bg-green-400": "_lg:focus:bg-green-400_6d3e9",
  "lg:focus:bg-green-500": "_lg:focus:bg-green-500_6d3e9",
  "lg:focus:bg-green-600": "_lg:focus:bg-green-600_6d3e9",
  "lg:focus:bg-green-700": "_lg:focus:bg-green-700_6d3e9",
  "lg:focus:bg-green-800": "_lg:focus:bg-green-800_6d3e9",
  "lg:focus:bg-green-900": "_lg:focus:bg-green-900_6d3e9",
  "lg:focus:bg-teal-100": "_lg:focus:bg-teal-100_6d3e9",
  "lg:focus:bg-teal-200": "_lg:focus:bg-teal-200_6d3e9",
  "lg:focus:bg-teal-300": "_lg:focus:bg-teal-300_6d3e9",
  "lg:focus:bg-teal-400": "_lg:focus:bg-teal-400_6d3e9",
  "lg:focus:bg-teal-500": "_lg:focus:bg-teal-500_6d3e9",
  "lg:focus:bg-teal-600": "_lg:focus:bg-teal-600_6d3e9",
  "lg:focus:bg-teal-700": "_lg:focus:bg-teal-700_6d3e9",
  "lg:focus:bg-teal-800": "_lg:focus:bg-teal-800_6d3e9",
  "lg:focus:bg-teal-900": "_lg:focus:bg-teal-900_6d3e9",
  "lg:focus:bg-blue-100": "_lg:focus:bg-blue-100_6d3e9",
  "lg:focus:bg-blue-200": "_lg:focus:bg-blue-200_6d3e9",
  "lg:focus:bg-blue-300": "_lg:focus:bg-blue-300_6d3e9",
  "lg:focus:bg-blue-400": "_lg:focus:bg-blue-400_6d3e9",
  "lg:focus:bg-blue-500": "_lg:focus:bg-blue-500_6d3e9",
  "lg:focus:bg-blue-600": "_lg:focus:bg-blue-600_6d3e9",
  "lg:focus:bg-blue-700": "_lg:focus:bg-blue-700_6d3e9",
  "lg:focus:bg-blue-800": "_lg:focus:bg-blue-800_6d3e9",
  "lg:focus:bg-blue-900": "_lg:focus:bg-blue-900_6d3e9",
  "lg:focus:bg-indigo-100": "_lg:focus:bg-indigo-100_6d3e9",
  "lg:focus:bg-indigo-200": "_lg:focus:bg-indigo-200_6d3e9",
  "lg:focus:bg-indigo-300": "_lg:focus:bg-indigo-300_6d3e9",
  "lg:focus:bg-indigo-400": "_lg:focus:bg-indigo-400_6d3e9",
  "lg:focus:bg-indigo-500": "_lg:focus:bg-indigo-500_6d3e9",
  "lg:focus:bg-indigo-600": "_lg:focus:bg-indigo-600_6d3e9",
  "lg:focus:bg-indigo-700": "_lg:focus:bg-indigo-700_6d3e9",
  "lg:focus:bg-indigo-800": "_lg:focus:bg-indigo-800_6d3e9",
  "lg:focus:bg-indigo-900": "_lg:focus:bg-indigo-900_6d3e9",
  "lg:focus:bg-purple-100": "_lg:focus:bg-purple-100_6d3e9",
  "lg:focus:bg-purple-200": "_lg:focus:bg-purple-200_6d3e9",
  "lg:focus:bg-purple-300": "_lg:focus:bg-purple-300_6d3e9",
  "lg:focus:bg-purple-400": "_lg:focus:bg-purple-400_6d3e9",
  "lg:focus:bg-purple-500": "_lg:focus:bg-purple-500_6d3e9",
  "lg:focus:bg-purple-600": "_lg:focus:bg-purple-600_6d3e9",
  "lg:focus:bg-purple-700": "_lg:focus:bg-purple-700_6d3e9",
  "lg:focus:bg-purple-800": "_lg:focus:bg-purple-800_6d3e9",
  "lg:focus:bg-purple-900": "_lg:focus:bg-purple-900_6d3e9",
  "lg:focus:bg-pink-100": "_lg:focus:bg-pink-100_6d3e9",
  "lg:focus:bg-pink-200": "_lg:focus:bg-pink-200_6d3e9",
  "lg:focus:bg-pink-300": "_lg:focus:bg-pink-300_6d3e9",
  "lg:focus:bg-pink-400": "_lg:focus:bg-pink-400_6d3e9",
  "lg:focus:bg-pink-500": "_lg:focus:bg-pink-500_6d3e9",
  "lg:focus:bg-pink-600": "_lg:focus:bg-pink-600_6d3e9",
  "lg:focus:bg-pink-700": "_lg:focus:bg-pink-700_6d3e9",
  "lg:focus:bg-pink-800": "_lg:focus:bg-pink-800_6d3e9",
  "lg:focus:bg-pink-900": "_lg:focus:bg-pink-900_6d3e9",
  "lg:focus:bg-primary-100": "_lg:focus:bg-primary-100_6d3e9",
  "lg:focus:bg-primary-200": "_lg:focus:bg-primary-200_6d3e9",
  "lg:focus:bg-primary-300": "_lg:focus:bg-primary-300_6d3e9",
  "lg:focus:bg-primary-400": "_lg:focus:bg-primary-400_6d3e9",
  "lg:focus:bg-primary-500": "_lg:focus:bg-primary-500_6d3e9",
  "lg:focus:bg-primary-600": "_lg:focus:bg-primary-600_6d3e9",
  "lg:focus:bg-primary-700": "_lg:focus:bg-primary-700_6d3e9",
  "lg:focus:bg-primary-800": "_lg:focus:bg-primary-800_6d3e9",
  "lg:focus:bg-primary-900": "_lg:focus:bg-primary-900_6d3e9",
  "lg:focus:bg-secondary-100": "_lg:focus:bg-secondary-100_6d3e9",
  "lg:focus:bg-secondary-200": "_lg:focus:bg-secondary-200_6d3e9",
  "lg:focus:bg-secondary-300": "_lg:focus:bg-secondary-300_6d3e9",
  "lg:focus:bg-secondary-400": "_lg:focus:bg-secondary-400_6d3e9",
  "lg:focus:bg-secondary-500": "_lg:focus:bg-secondary-500_6d3e9",
  "lg:focus:bg-secondary-600": "_lg:focus:bg-secondary-600_6d3e9",
  "lg:focus:bg-secondary-700": "_lg:focus:bg-secondary-700_6d3e9",
  "lg:focus:bg-secondary-800": "_lg:focus:bg-secondary-800_6d3e9",
  "lg:focus:bg-secondary-900": "_lg:focus:bg-secondary-900_6d3e9",
  "lg:active:bg-transparent": "_lg:active:bg-transparent_6d3e9",
  "lg:active:bg-black": "_lg:active:bg-black_6d3e9",
  "lg:active:bg-white": "_lg:active:bg-white_6d3e9",
  "lg:active:bg-gray-100": "_lg:active:bg-gray-100_6d3e9",
  "lg:active:bg-gray-200": "_lg:active:bg-gray-200_6d3e9",
  "lg:active:bg-gray-300": "_lg:active:bg-gray-300_6d3e9",
  "lg:active:bg-gray-400": "_lg:active:bg-gray-400_6d3e9",
  "lg:active:bg-gray-500": "_lg:active:bg-gray-500_6d3e9",
  "lg:active:bg-gray-600": "_lg:active:bg-gray-600_6d3e9",
  "lg:active:bg-gray-700": "_lg:active:bg-gray-700_6d3e9",
  "lg:active:bg-gray-800": "_lg:active:bg-gray-800_6d3e9",
  "lg:active:bg-gray-900": "_lg:active:bg-gray-900_6d3e9",
  "lg:active:bg-red-100": "_lg:active:bg-red-100_6d3e9",
  "lg:active:bg-red-200": "_lg:active:bg-red-200_6d3e9",
  "lg:active:bg-red-300": "_lg:active:bg-red-300_6d3e9",
  "lg:active:bg-red-400": "_lg:active:bg-red-400_6d3e9",
  "lg:active:bg-red-500": "_lg:active:bg-red-500_6d3e9",
  "lg:active:bg-red-600": "_lg:active:bg-red-600_6d3e9",
  "lg:active:bg-red-700": "_lg:active:bg-red-700_6d3e9",
  "lg:active:bg-red-800": "_lg:active:bg-red-800_6d3e9",
  "lg:active:bg-red-900": "_lg:active:bg-red-900_6d3e9",
  "lg:active:bg-orange-100": "_lg:active:bg-orange-100_6d3e9",
  "lg:active:bg-orange-200": "_lg:active:bg-orange-200_6d3e9",
  "lg:active:bg-orange-300": "_lg:active:bg-orange-300_6d3e9",
  "lg:active:bg-orange-400": "_lg:active:bg-orange-400_6d3e9",
  "lg:active:bg-orange-500": "_lg:active:bg-orange-500_6d3e9",
  "lg:active:bg-orange-600": "_lg:active:bg-orange-600_6d3e9",
  "lg:active:bg-orange-700": "_lg:active:bg-orange-700_6d3e9",
  "lg:active:bg-orange-800": "_lg:active:bg-orange-800_6d3e9",
  "lg:active:bg-orange-900": "_lg:active:bg-orange-900_6d3e9",
  "lg:active:bg-yellow-100": "_lg:active:bg-yellow-100_6d3e9",
  "lg:active:bg-yellow-200": "_lg:active:bg-yellow-200_6d3e9",
  "lg:active:bg-yellow-300": "_lg:active:bg-yellow-300_6d3e9",
  "lg:active:bg-yellow-400": "_lg:active:bg-yellow-400_6d3e9",
  "lg:active:bg-yellow-500": "_lg:active:bg-yellow-500_6d3e9",
  "lg:active:bg-yellow-600": "_lg:active:bg-yellow-600_6d3e9",
  "lg:active:bg-yellow-700": "_lg:active:bg-yellow-700_6d3e9",
  "lg:active:bg-yellow-800": "_lg:active:bg-yellow-800_6d3e9",
  "lg:active:bg-yellow-900": "_lg:active:bg-yellow-900_6d3e9",
  "lg:active:bg-green-100": "_lg:active:bg-green-100_6d3e9",
  "lg:active:bg-green-200": "_lg:active:bg-green-200_6d3e9",
  "lg:active:bg-green-300": "_lg:active:bg-green-300_6d3e9",
  "lg:active:bg-green-400": "_lg:active:bg-green-400_6d3e9",
  "lg:active:bg-green-500": "_lg:active:bg-green-500_6d3e9",
  "lg:active:bg-green-600": "_lg:active:bg-green-600_6d3e9",
  "lg:active:bg-green-700": "_lg:active:bg-green-700_6d3e9",
  "lg:active:bg-green-800": "_lg:active:bg-green-800_6d3e9",
  "lg:active:bg-green-900": "_lg:active:bg-green-900_6d3e9",
  "lg:active:bg-teal-100": "_lg:active:bg-teal-100_6d3e9",
  "lg:active:bg-teal-200": "_lg:active:bg-teal-200_6d3e9",
  "lg:active:bg-teal-300": "_lg:active:bg-teal-300_6d3e9",
  "lg:active:bg-teal-400": "_lg:active:bg-teal-400_6d3e9",
  "lg:active:bg-teal-500": "_lg:active:bg-teal-500_6d3e9",
  "lg:active:bg-teal-600": "_lg:active:bg-teal-600_6d3e9",
  "lg:active:bg-teal-700": "_lg:active:bg-teal-700_6d3e9",
  "lg:active:bg-teal-800": "_lg:active:bg-teal-800_6d3e9",
  "lg:active:bg-teal-900": "_lg:active:bg-teal-900_6d3e9",
  "lg:active:bg-blue-100": "_lg:active:bg-blue-100_6d3e9",
  "lg:active:bg-blue-200": "_lg:active:bg-blue-200_6d3e9",
  "lg:active:bg-blue-300": "_lg:active:bg-blue-300_6d3e9",
  "lg:active:bg-blue-400": "_lg:active:bg-blue-400_6d3e9",
  "lg:active:bg-blue-500": "_lg:active:bg-blue-500_6d3e9",
  "lg:active:bg-blue-600": "_lg:active:bg-blue-600_6d3e9",
  "lg:active:bg-blue-700": "_lg:active:bg-blue-700_6d3e9",
  "lg:active:bg-blue-800": "_lg:active:bg-blue-800_6d3e9",
  "lg:active:bg-blue-900": "_lg:active:bg-blue-900_6d3e9",
  "lg:active:bg-indigo-100": "_lg:active:bg-indigo-100_6d3e9",
  "lg:active:bg-indigo-200": "_lg:active:bg-indigo-200_6d3e9",
  "lg:active:bg-indigo-300": "_lg:active:bg-indigo-300_6d3e9",
  "lg:active:bg-indigo-400": "_lg:active:bg-indigo-400_6d3e9",
  "lg:active:bg-indigo-500": "_lg:active:bg-indigo-500_6d3e9",
  "lg:active:bg-indigo-600": "_lg:active:bg-indigo-600_6d3e9",
  "lg:active:bg-indigo-700": "_lg:active:bg-indigo-700_6d3e9",
  "lg:active:bg-indigo-800": "_lg:active:bg-indigo-800_6d3e9",
  "lg:active:bg-indigo-900": "_lg:active:bg-indigo-900_6d3e9",
  "lg:active:bg-purple-100": "_lg:active:bg-purple-100_6d3e9",
  "lg:active:bg-purple-200": "_lg:active:bg-purple-200_6d3e9",
  "lg:active:bg-purple-300": "_lg:active:bg-purple-300_6d3e9",
  "lg:active:bg-purple-400": "_lg:active:bg-purple-400_6d3e9",
  "lg:active:bg-purple-500": "_lg:active:bg-purple-500_6d3e9",
  "lg:active:bg-purple-600": "_lg:active:bg-purple-600_6d3e9",
  "lg:active:bg-purple-700": "_lg:active:bg-purple-700_6d3e9",
  "lg:active:bg-purple-800": "_lg:active:bg-purple-800_6d3e9",
  "lg:active:bg-purple-900": "_lg:active:bg-purple-900_6d3e9",
  "lg:active:bg-pink-100": "_lg:active:bg-pink-100_6d3e9",
  "lg:active:bg-pink-200": "_lg:active:bg-pink-200_6d3e9",
  "lg:active:bg-pink-300": "_lg:active:bg-pink-300_6d3e9",
  "lg:active:bg-pink-400": "_lg:active:bg-pink-400_6d3e9",
  "lg:active:bg-pink-500": "_lg:active:bg-pink-500_6d3e9",
  "lg:active:bg-pink-600": "_lg:active:bg-pink-600_6d3e9",
  "lg:active:bg-pink-700": "_lg:active:bg-pink-700_6d3e9",
  "lg:active:bg-pink-800": "_lg:active:bg-pink-800_6d3e9",
  "lg:active:bg-pink-900": "_lg:active:bg-pink-900_6d3e9",
  "lg:active:bg-primary-100": "_lg:active:bg-primary-100_6d3e9",
  "lg:active:bg-primary-200": "_lg:active:bg-primary-200_6d3e9",
  "lg:active:bg-primary-300": "_lg:active:bg-primary-300_6d3e9",
  "lg:active:bg-primary-400": "_lg:active:bg-primary-400_6d3e9",
  "lg:active:bg-primary-500": "_lg:active:bg-primary-500_6d3e9",
  "lg:active:bg-primary-600": "_lg:active:bg-primary-600_6d3e9",
  "lg:active:bg-primary-700": "_lg:active:bg-primary-700_6d3e9",
  "lg:active:bg-primary-800": "_lg:active:bg-primary-800_6d3e9",
  "lg:active:bg-primary-900": "_lg:active:bg-primary-900_6d3e9",
  "lg:active:bg-secondary-100": "_lg:active:bg-secondary-100_6d3e9",
  "lg:active:bg-secondary-200": "_lg:active:bg-secondary-200_6d3e9",
  "lg:active:bg-secondary-300": "_lg:active:bg-secondary-300_6d3e9",
  "lg:active:bg-secondary-400": "_lg:active:bg-secondary-400_6d3e9",
  "lg:active:bg-secondary-500": "_lg:active:bg-secondary-500_6d3e9",
  "lg:active:bg-secondary-600": "_lg:active:bg-secondary-600_6d3e9",
  "lg:active:bg-secondary-700": "_lg:active:bg-secondary-700_6d3e9",
  "lg:active:bg-secondary-800": "_lg:active:bg-secondary-800_6d3e9",
  "lg:active:bg-secondary-900": "_lg:active:bg-secondary-900_6d3e9",
  "lg:bg-bottom": "_lg:bg-bottom_6d3e9",
  "lg:bg-center": "_lg:bg-center_6d3e9",
  "lg:bg-left": "_lg:bg-left_6d3e9",
  "lg:bg-left-bottom": "_lg:bg-left-bottom_6d3e9",
  "lg:bg-left-top": "_lg:bg-left-top_6d3e9",
  "lg:bg-right": "_lg:bg-right_6d3e9",
  "lg:bg-right-bottom": "_lg:bg-right-bottom_6d3e9",
  "lg:bg-right-top": "_lg:bg-right-top_6d3e9",
  "lg:bg-top": "_lg:bg-top_6d3e9",
  "lg:bg-repeat": "_lg:bg-repeat_6d3e9",
  "lg:bg-no-repeat": "_lg:bg-no-repeat_6d3e9",
  "lg:bg-repeat-x": "_lg:bg-repeat-x_6d3e9",
  "lg:bg-repeat-y": "_lg:bg-repeat-y_6d3e9",
  "lg:bg-repeat-round": "_lg:bg-repeat-round_6d3e9",
  "lg:bg-repeat-space": "_lg:bg-repeat-space_6d3e9",
  "lg:bg-auto": "_lg:bg-auto_6d3e9",
  "lg:bg-cover": "_lg:bg-cover_6d3e9",
  "lg:bg-contain": "_lg:bg-contain_6d3e9",
  "lg:border-collapse": "_lg:border-collapse_6d3e9",
  "lg:border-separate": "_lg:border-separate_6d3e9",
  "lg:border-transparent": "_lg:border-transparent_6d3e9",
  "lg:border-black": "_lg:border-black_6d3e9",
  "lg:border-white": "_lg:border-white_6d3e9",
  "lg:border-gray-100": "_lg:border-gray-100_6d3e9",
  "lg:border-gray-200": "_lg:border-gray-200_6d3e9",
  "lg:border-gray-300": "_lg:border-gray-300_6d3e9",
  "lg:border-gray-400": "_lg:border-gray-400_6d3e9",
  "lg:border-gray-500": "_lg:border-gray-500_6d3e9",
  "lg:border-gray-600": "_lg:border-gray-600_6d3e9",
  "lg:border-gray-700": "_lg:border-gray-700_6d3e9",
  "lg:border-gray-800": "_lg:border-gray-800_6d3e9",
  "lg:border-gray-900": "_lg:border-gray-900_6d3e9",
  "lg:border-red-100": "_lg:border-red-100_6d3e9",
  "lg:border-red-200": "_lg:border-red-200_6d3e9",
  "lg:border-red-300": "_lg:border-red-300_6d3e9",
  "lg:border-red-400": "_lg:border-red-400_6d3e9",
  "lg:border-red-500": "_lg:border-red-500_6d3e9",
  "lg:border-red-600": "_lg:border-red-600_6d3e9",
  "lg:border-red-700": "_lg:border-red-700_6d3e9",
  "lg:border-red-800": "_lg:border-red-800_6d3e9",
  "lg:border-red-900": "_lg:border-red-900_6d3e9",
  "lg:border-orange-100": "_lg:border-orange-100_6d3e9",
  "lg:border-orange-200": "_lg:border-orange-200_6d3e9",
  "lg:border-orange-300": "_lg:border-orange-300_6d3e9",
  "lg:border-orange-400": "_lg:border-orange-400_6d3e9",
  "lg:border-orange-500": "_lg:border-orange-500_6d3e9",
  "lg:border-orange-600": "_lg:border-orange-600_6d3e9",
  "lg:border-orange-700": "_lg:border-orange-700_6d3e9",
  "lg:border-orange-800": "_lg:border-orange-800_6d3e9",
  "lg:border-orange-900": "_lg:border-orange-900_6d3e9",
  "lg:border-yellow-100": "_lg:border-yellow-100_6d3e9",
  "lg:border-yellow-200": "_lg:border-yellow-200_6d3e9",
  "lg:border-yellow-300": "_lg:border-yellow-300_6d3e9",
  "lg:border-yellow-400": "_lg:border-yellow-400_6d3e9",
  "lg:border-yellow-500": "_lg:border-yellow-500_6d3e9",
  "lg:border-yellow-600": "_lg:border-yellow-600_6d3e9",
  "lg:border-yellow-700": "_lg:border-yellow-700_6d3e9",
  "lg:border-yellow-800": "_lg:border-yellow-800_6d3e9",
  "lg:border-yellow-900": "_lg:border-yellow-900_6d3e9",
  "lg:border-green-100": "_lg:border-green-100_6d3e9",
  "lg:border-green-200": "_lg:border-green-200_6d3e9",
  "lg:border-green-300": "_lg:border-green-300_6d3e9",
  "lg:border-green-400": "_lg:border-green-400_6d3e9",
  "lg:border-green-500": "_lg:border-green-500_6d3e9",
  "lg:border-green-600": "_lg:border-green-600_6d3e9",
  "lg:border-green-700": "_lg:border-green-700_6d3e9",
  "lg:border-green-800": "_lg:border-green-800_6d3e9",
  "lg:border-green-900": "_lg:border-green-900_6d3e9",
  "lg:border-teal-100": "_lg:border-teal-100_6d3e9",
  "lg:border-teal-200": "_lg:border-teal-200_6d3e9",
  "lg:border-teal-300": "_lg:border-teal-300_6d3e9",
  "lg:border-teal-400": "_lg:border-teal-400_6d3e9",
  "lg:border-teal-500": "_lg:border-teal-500_6d3e9",
  "lg:border-teal-600": "_lg:border-teal-600_6d3e9",
  "lg:border-teal-700": "_lg:border-teal-700_6d3e9",
  "lg:border-teal-800": "_lg:border-teal-800_6d3e9",
  "lg:border-teal-900": "_lg:border-teal-900_6d3e9",
  "lg:border-blue-100": "_lg:border-blue-100_6d3e9",
  "lg:border-blue-200": "_lg:border-blue-200_6d3e9",
  "lg:border-blue-300": "_lg:border-blue-300_6d3e9",
  "lg:border-blue-400": "_lg:border-blue-400_6d3e9",
  "lg:border-blue-500": "_lg:border-blue-500_6d3e9",
  "lg:border-blue-600": "_lg:border-blue-600_6d3e9",
  "lg:border-blue-700": "_lg:border-blue-700_6d3e9",
  "lg:border-blue-800": "_lg:border-blue-800_6d3e9",
  "lg:border-blue-900": "_lg:border-blue-900_6d3e9",
  "lg:border-indigo-100": "_lg:border-indigo-100_6d3e9",
  "lg:border-indigo-200": "_lg:border-indigo-200_6d3e9",
  "lg:border-indigo-300": "_lg:border-indigo-300_6d3e9",
  "lg:border-indigo-400": "_lg:border-indigo-400_6d3e9",
  "lg:border-indigo-500": "_lg:border-indigo-500_6d3e9",
  "lg:border-indigo-600": "_lg:border-indigo-600_6d3e9",
  "lg:border-indigo-700": "_lg:border-indigo-700_6d3e9",
  "lg:border-indigo-800": "_lg:border-indigo-800_6d3e9",
  "lg:border-indigo-900": "_lg:border-indigo-900_6d3e9",
  "lg:border-purple-100": "_lg:border-purple-100_6d3e9",
  "lg:border-purple-200": "_lg:border-purple-200_6d3e9",
  "lg:border-purple-300": "_lg:border-purple-300_6d3e9",
  "lg:border-purple-400": "_lg:border-purple-400_6d3e9",
  "lg:border-purple-500": "_lg:border-purple-500_6d3e9",
  "lg:border-purple-600": "_lg:border-purple-600_6d3e9",
  "lg:border-purple-700": "_lg:border-purple-700_6d3e9",
  "lg:border-purple-800": "_lg:border-purple-800_6d3e9",
  "lg:border-purple-900": "_lg:border-purple-900_6d3e9",
  "lg:border-pink-100": "_lg:border-pink-100_6d3e9",
  "lg:border-pink-200": "_lg:border-pink-200_6d3e9",
  "lg:border-pink-300": "_lg:border-pink-300_6d3e9",
  "lg:border-pink-400": "_lg:border-pink-400_6d3e9",
  "lg:border-pink-500": "_lg:border-pink-500_6d3e9",
  "lg:border-pink-600": "_lg:border-pink-600_6d3e9",
  "lg:border-pink-700": "_lg:border-pink-700_6d3e9",
  "lg:border-pink-800": "_lg:border-pink-800_6d3e9",
  "lg:border-pink-900": "_lg:border-pink-900_6d3e9",
  "lg:border-primary-100": "_lg:border-primary-100_6d3e9",
  "lg:border-primary-200": "_lg:border-primary-200_6d3e9",
  "lg:border-primary-300": "_lg:border-primary-300_6d3e9",
  "lg:border-primary-400": "_lg:border-primary-400_6d3e9",
  "lg:border-primary-500": "_lg:border-primary-500_6d3e9",
  "lg:border-primary-600": "_lg:border-primary-600_6d3e9",
  "lg:border-primary-700": "_lg:border-primary-700_6d3e9",
  "lg:border-primary-800": "_lg:border-primary-800_6d3e9",
  "lg:border-primary-900": "_lg:border-primary-900_6d3e9",
  "lg:border-secondary-100": "_lg:border-secondary-100_6d3e9",
  "lg:border-secondary-200": "_lg:border-secondary-200_6d3e9",
  "lg:border-secondary-300": "_lg:border-secondary-300_6d3e9",
  "lg:border-secondary-400": "_lg:border-secondary-400_6d3e9",
  "lg:border-secondary-500": "_lg:border-secondary-500_6d3e9",
  "lg:border-secondary-600": "_lg:border-secondary-600_6d3e9",
  "lg:border-secondary-700": "_lg:border-secondary-700_6d3e9",
  "lg:border-secondary-800": "_lg:border-secondary-800_6d3e9",
  "lg:border-secondary-900": "_lg:border-secondary-900_6d3e9",
  "lg:hover:border-transparent": "_lg:hover:border-transparent_6d3e9",
  "lg:hover:border-black": "_lg:hover:border-black_6d3e9",
  "lg:hover:border-white": "_lg:hover:border-white_6d3e9",
  "lg:hover:border-gray-100": "_lg:hover:border-gray-100_6d3e9",
  "lg:hover:border-gray-200": "_lg:hover:border-gray-200_6d3e9",
  "lg:hover:border-gray-300": "_lg:hover:border-gray-300_6d3e9",
  "lg:hover:border-gray-400": "_lg:hover:border-gray-400_6d3e9",
  "lg:hover:border-gray-500": "_lg:hover:border-gray-500_6d3e9",
  "lg:hover:border-gray-600": "_lg:hover:border-gray-600_6d3e9",
  "lg:hover:border-gray-700": "_lg:hover:border-gray-700_6d3e9",
  "lg:hover:border-gray-800": "_lg:hover:border-gray-800_6d3e9",
  "lg:hover:border-gray-900": "_lg:hover:border-gray-900_6d3e9",
  "lg:hover:border-red-100": "_lg:hover:border-red-100_6d3e9",
  "lg:hover:border-red-200": "_lg:hover:border-red-200_6d3e9",
  "lg:hover:border-red-300": "_lg:hover:border-red-300_6d3e9",
  "lg:hover:border-red-400": "_lg:hover:border-red-400_6d3e9",
  "lg:hover:border-red-500": "_lg:hover:border-red-500_6d3e9",
  "lg:hover:border-red-600": "_lg:hover:border-red-600_6d3e9",
  "lg:hover:border-red-700": "_lg:hover:border-red-700_6d3e9",
  "lg:hover:border-red-800": "_lg:hover:border-red-800_6d3e9",
  "lg:hover:border-red-900": "_lg:hover:border-red-900_6d3e9",
  "lg:hover:border-orange-100": "_lg:hover:border-orange-100_6d3e9",
  "lg:hover:border-orange-200": "_lg:hover:border-orange-200_6d3e9",
  "lg:hover:border-orange-300": "_lg:hover:border-orange-300_6d3e9",
  "lg:hover:border-orange-400": "_lg:hover:border-orange-400_6d3e9",
  "lg:hover:border-orange-500": "_lg:hover:border-orange-500_6d3e9",
  "lg:hover:border-orange-600": "_lg:hover:border-orange-600_6d3e9",
  "lg:hover:border-orange-700": "_lg:hover:border-orange-700_6d3e9",
  "lg:hover:border-orange-800": "_lg:hover:border-orange-800_6d3e9",
  "lg:hover:border-orange-900": "_lg:hover:border-orange-900_6d3e9",
  "lg:hover:border-yellow-100": "_lg:hover:border-yellow-100_6d3e9",
  "lg:hover:border-yellow-200": "_lg:hover:border-yellow-200_6d3e9",
  "lg:hover:border-yellow-300": "_lg:hover:border-yellow-300_6d3e9",
  "lg:hover:border-yellow-400": "_lg:hover:border-yellow-400_6d3e9",
  "lg:hover:border-yellow-500": "_lg:hover:border-yellow-500_6d3e9",
  "lg:hover:border-yellow-600": "_lg:hover:border-yellow-600_6d3e9",
  "lg:hover:border-yellow-700": "_lg:hover:border-yellow-700_6d3e9",
  "lg:hover:border-yellow-800": "_lg:hover:border-yellow-800_6d3e9",
  "lg:hover:border-yellow-900": "_lg:hover:border-yellow-900_6d3e9",
  "lg:hover:border-green-100": "_lg:hover:border-green-100_6d3e9",
  "lg:hover:border-green-200": "_lg:hover:border-green-200_6d3e9",
  "lg:hover:border-green-300": "_lg:hover:border-green-300_6d3e9",
  "lg:hover:border-green-400": "_lg:hover:border-green-400_6d3e9",
  "lg:hover:border-green-500": "_lg:hover:border-green-500_6d3e9",
  "lg:hover:border-green-600": "_lg:hover:border-green-600_6d3e9",
  "lg:hover:border-green-700": "_lg:hover:border-green-700_6d3e9",
  "lg:hover:border-green-800": "_lg:hover:border-green-800_6d3e9",
  "lg:hover:border-green-900": "_lg:hover:border-green-900_6d3e9",
  "lg:hover:border-teal-100": "_lg:hover:border-teal-100_6d3e9",
  "lg:hover:border-teal-200": "_lg:hover:border-teal-200_6d3e9",
  "lg:hover:border-teal-300": "_lg:hover:border-teal-300_6d3e9",
  "lg:hover:border-teal-400": "_lg:hover:border-teal-400_6d3e9",
  "lg:hover:border-teal-500": "_lg:hover:border-teal-500_6d3e9",
  "lg:hover:border-teal-600": "_lg:hover:border-teal-600_6d3e9",
  "lg:hover:border-teal-700": "_lg:hover:border-teal-700_6d3e9",
  "lg:hover:border-teal-800": "_lg:hover:border-teal-800_6d3e9",
  "lg:hover:border-teal-900": "_lg:hover:border-teal-900_6d3e9",
  "lg:hover:border-blue-100": "_lg:hover:border-blue-100_6d3e9",
  "lg:hover:border-blue-200": "_lg:hover:border-blue-200_6d3e9",
  "lg:hover:border-blue-300": "_lg:hover:border-blue-300_6d3e9",
  "lg:hover:border-blue-400": "_lg:hover:border-blue-400_6d3e9",
  "lg:hover:border-blue-500": "_lg:hover:border-blue-500_6d3e9",
  "lg:hover:border-blue-600": "_lg:hover:border-blue-600_6d3e9",
  "lg:hover:border-blue-700": "_lg:hover:border-blue-700_6d3e9",
  "lg:hover:border-blue-800": "_lg:hover:border-blue-800_6d3e9",
  "lg:hover:border-blue-900": "_lg:hover:border-blue-900_6d3e9",
  "lg:hover:border-indigo-100": "_lg:hover:border-indigo-100_6d3e9",
  "lg:hover:border-indigo-200": "_lg:hover:border-indigo-200_6d3e9",
  "lg:hover:border-indigo-300": "_lg:hover:border-indigo-300_6d3e9",
  "lg:hover:border-indigo-400": "_lg:hover:border-indigo-400_6d3e9",
  "lg:hover:border-indigo-500": "_lg:hover:border-indigo-500_6d3e9",
  "lg:hover:border-indigo-600": "_lg:hover:border-indigo-600_6d3e9",
  "lg:hover:border-indigo-700": "_lg:hover:border-indigo-700_6d3e9",
  "lg:hover:border-indigo-800": "_lg:hover:border-indigo-800_6d3e9",
  "lg:hover:border-indigo-900": "_lg:hover:border-indigo-900_6d3e9",
  "lg:hover:border-purple-100": "_lg:hover:border-purple-100_6d3e9",
  "lg:hover:border-purple-200": "_lg:hover:border-purple-200_6d3e9",
  "lg:hover:border-purple-300": "_lg:hover:border-purple-300_6d3e9",
  "lg:hover:border-purple-400": "_lg:hover:border-purple-400_6d3e9",
  "lg:hover:border-purple-500": "_lg:hover:border-purple-500_6d3e9",
  "lg:hover:border-purple-600": "_lg:hover:border-purple-600_6d3e9",
  "lg:hover:border-purple-700": "_lg:hover:border-purple-700_6d3e9",
  "lg:hover:border-purple-800": "_lg:hover:border-purple-800_6d3e9",
  "lg:hover:border-purple-900": "_lg:hover:border-purple-900_6d3e9",
  "lg:hover:border-pink-100": "_lg:hover:border-pink-100_6d3e9",
  "lg:hover:border-pink-200": "_lg:hover:border-pink-200_6d3e9",
  "lg:hover:border-pink-300": "_lg:hover:border-pink-300_6d3e9",
  "lg:hover:border-pink-400": "_lg:hover:border-pink-400_6d3e9",
  "lg:hover:border-pink-500": "_lg:hover:border-pink-500_6d3e9",
  "lg:hover:border-pink-600": "_lg:hover:border-pink-600_6d3e9",
  "lg:hover:border-pink-700": "_lg:hover:border-pink-700_6d3e9",
  "lg:hover:border-pink-800": "_lg:hover:border-pink-800_6d3e9",
  "lg:hover:border-pink-900": "_lg:hover:border-pink-900_6d3e9",
  "lg:hover:border-primary-100": "_lg:hover:border-primary-100_6d3e9",
  "lg:hover:border-primary-200": "_lg:hover:border-primary-200_6d3e9",
  "lg:hover:border-primary-300": "_lg:hover:border-primary-300_6d3e9",
  "lg:hover:border-primary-400": "_lg:hover:border-primary-400_6d3e9",
  "lg:hover:border-primary-500": "_lg:hover:border-primary-500_6d3e9",
  "lg:hover:border-primary-600": "_lg:hover:border-primary-600_6d3e9",
  "lg:hover:border-primary-700": "_lg:hover:border-primary-700_6d3e9",
  "lg:hover:border-primary-800": "_lg:hover:border-primary-800_6d3e9",
  "lg:hover:border-primary-900": "_lg:hover:border-primary-900_6d3e9",
  "lg:hover:border-secondary-100": "_lg:hover:border-secondary-100_6d3e9",
  "lg:hover:border-secondary-200": "_lg:hover:border-secondary-200_6d3e9",
  "lg:hover:border-secondary-300": "_lg:hover:border-secondary-300_6d3e9",
  "lg:hover:border-secondary-400": "_lg:hover:border-secondary-400_6d3e9",
  "lg:hover:border-secondary-500": "_lg:hover:border-secondary-500_6d3e9",
  "lg:hover:border-secondary-600": "_lg:hover:border-secondary-600_6d3e9",
  "lg:hover:border-secondary-700": "_lg:hover:border-secondary-700_6d3e9",
  "lg:hover:border-secondary-800": "_lg:hover:border-secondary-800_6d3e9",
  "lg:hover:border-secondary-900": "_lg:hover:border-secondary-900_6d3e9",
  "lg:focus:border-transparent": "_lg:focus:border-transparent_6d3e9",
  "lg:focus:border-black": "_lg:focus:border-black_6d3e9",
  "lg:focus:border-white": "_lg:focus:border-white_6d3e9",
  "lg:focus:border-gray-100": "_lg:focus:border-gray-100_6d3e9",
  "lg:focus:border-gray-200": "_lg:focus:border-gray-200_6d3e9",
  "lg:focus:border-gray-300": "_lg:focus:border-gray-300_6d3e9",
  "lg:focus:border-gray-400": "_lg:focus:border-gray-400_6d3e9",
  "lg:focus:border-gray-500": "_lg:focus:border-gray-500_6d3e9",
  "lg:focus:border-gray-600": "_lg:focus:border-gray-600_6d3e9",
  "lg:focus:border-gray-700": "_lg:focus:border-gray-700_6d3e9",
  "lg:focus:border-gray-800": "_lg:focus:border-gray-800_6d3e9",
  "lg:focus:border-gray-900": "_lg:focus:border-gray-900_6d3e9",
  "lg:focus:border-red-100": "_lg:focus:border-red-100_6d3e9",
  "lg:focus:border-red-200": "_lg:focus:border-red-200_6d3e9",
  "lg:focus:border-red-300": "_lg:focus:border-red-300_6d3e9",
  "lg:focus:border-red-400": "_lg:focus:border-red-400_6d3e9",
  "lg:focus:border-red-500": "_lg:focus:border-red-500_6d3e9",
  "lg:focus:border-red-600": "_lg:focus:border-red-600_6d3e9",
  "lg:focus:border-red-700": "_lg:focus:border-red-700_6d3e9",
  "lg:focus:border-red-800": "_lg:focus:border-red-800_6d3e9",
  "lg:focus:border-red-900": "_lg:focus:border-red-900_6d3e9",
  "lg:focus:border-orange-100": "_lg:focus:border-orange-100_6d3e9",
  "lg:focus:border-orange-200": "_lg:focus:border-orange-200_6d3e9",
  "lg:focus:border-orange-300": "_lg:focus:border-orange-300_6d3e9",
  "lg:focus:border-orange-400": "_lg:focus:border-orange-400_6d3e9",
  "lg:focus:border-orange-500": "_lg:focus:border-orange-500_6d3e9",
  "lg:focus:border-orange-600": "_lg:focus:border-orange-600_6d3e9",
  "lg:focus:border-orange-700": "_lg:focus:border-orange-700_6d3e9",
  "lg:focus:border-orange-800": "_lg:focus:border-orange-800_6d3e9",
  "lg:focus:border-orange-900": "_lg:focus:border-orange-900_6d3e9",
  "lg:focus:border-yellow-100": "_lg:focus:border-yellow-100_6d3e9",
  "lg:focus:border-yellow-200": "_lg:focus:border-yellow-200_6d3e9",
  "lg:focus:border-yellow-300": "_lg:focus:border-yellow-300_6d3e9",
  "lg:focus:border-yellow-400": "_lg:focus:border-yellow-400_6d3e9",
  "lg:focus:border-yellow-500": "_lg:focus:border-yellow-500_6d3e9",
  "lg:focus:border-yellow-600": "_lg:focus:border-yellow-600_6d3e9",
  "lg:focus:border-yellow-700": "_lg:focus:border-yellow-700_6d3e9",
  "lg:focus:border-yellow-800": "_lg:focus:border-yellow-800_6d3e9",
  "lg:focus:border-yellow-900": "_lg:focus:border-yellow-900_6d3e9",
  "lg:focus:border-green-100": "_lg:focus:border-green-100_6d3e9",
  "lg:focus:border-green-200": "_lg:focus:border-green-200_6d3e9",
  "lg:focus:border-green-300": "_lg:focus:border-green-300_6d3e9",
  "lg:focus:border-green-400": "_lg:focus:border-green-400_6d3e9",
  "lg:focus:border-green-500": "_lg:focus:border-green-500_6d3e9",
  "lg:focus:border-green-600": "_lg:focus:border-green-600_6d3e9",
  "lg:focus:border-green-700": "_lg:focus:border-green-700_6d3e9",
  "lg:focus:border-green-800": "_lg:focus:border-green-800_6d3e9",
  "lg:focus:border-green-900": "_lg:focus:border-green-900_6d3e9",
  "lg:focus:border-teal-100": "_lg:focus:border-teal-100_6d3e9",
  "lg:focus:border-teal-200": "_lg:focus:border-teal-200_6d3e9",
  "lg:focus:border-teal-300": "_lg:focus:border-teal-300_6d3e9",
  "lg:focus:border-teal-400": "_lg:focus:border-teal-400_6d3e9",
  "lg:focus:border-teal-500": "_lg:focus:border-teal-500_6d3e9",
  "lg:focus:border-teal-600": "_lg:focus:border-teal-600_6d3e9",
  "lg:focus:border-teal-700": "_lg:focus:border-teal-700_6d3e9",
  "lg:focus:border-teal-800": "_lg:focus:border-teal-800_6d3e9",
  "lg:focus:border-teal-900": "_lg:focus:border-teal-900_6d3e9",
  "lg:focus:border-blue-100": "_lg:focus:border-blue-100_6d3e9",
  "lg:focus:border-blue-200": "_lg:focus:border-blue-200_6d3e9",
  "lg:focus:border-blue-300": "_lg:focus:border-blue-300_6d3e9",
  "lg:focus:border-blue-400": "_lg:focus:border-blue-400_6d3e9",
  "lg:focus:border-blue-500": "_lg:focus:border-blue-500_6d3e9",
  "lg:focus:border-blue-600": "_lg:focus:border-blue-600_6d3e9",
  "lg:focus:border-blue-700": "_lg:focus:border-blue-700_6d3e9",
  "lg:focus:border-blue-800": "_lg:focus:border-blue-800_6d3e9",
  "lg:focus:border-blue-900": "_lg:focus:border-blue-900_6d3e9",
  "lg:focus:border-indigo-100": "_lg:focus:border-indigo-100_6d3e9",
  "lg:focus:border-indigo-200": "_lg:focus:border-indigo-200_6d3e9",
  "lg:focus:border-indigo-300": "_lg:focus:border-indigo-300_6d3e9",
  "lg:focus:border-indigo-400": "_lg:focus:border-indigo-400_6d3e9",
  "lg:focus:border-indigo-500": "_lg:focus:border-indigo-500_6d3e9",
  "lg:focus:border-indigo-600": "_lg:focus:border-indigo-600_6d3e9",
  "lg:focus:border-indigo-700": "_lg:focus:border-indigo-700_6d3e9",
  "lg:focus:border-indigo-800": "_lg:focus:border-indigo-800_6d3e9",
  "lg:focus:border-indigo-900": "_lg:focus:border-indigo-900_6d3e9",
  "lg:focus:border-purple-100": "_lg:focus:border-purple-100_6d3e9",
  "lg:focus:border-purple-200": "_lg:focus:border-purple-200_6d3e9",
  "lg:focus:border-purple-300": "_lg:focus:border-purple-300_6d3e9",
  "lg:focus:border-purple-400": "_lg:focus:border-purple-400_6d3e9",
  "lg:focus:border-purple-500": "_lg:focus:border-purple-500_6d3e9",
  "lg:focus:border-purple-600": "_lg:focus:border-purple-600_6d3e9",
  "lg:focus:border-purple-700": "_lg:focus:border-purple-700_6d3e9",
  "lg:focus:border-purple-800": "_lg:focus:border-purple-800_6d3e9",
  "lg:focus:border-purple-900": "_lg:focus:border-purple-900_6d3e9",
  "lg:focus:border-pink-100": "_lg:focus:border-pink-100_6d3e9",
  "lg:focus:border-pink-200": "_lg:focus:border-pink-200_6d3e9",
  "lg:focus:border-pink-300": "_lg:focus:border-pink-300_6d3e9",
  "lg:focus:border-pink-400": "_lg:focus:border-pink-400_6d3e9",
  "lg:focus:border-pink-500": "_lg:focus:border-pink-500_6d3e9",
  "lg:focus:border-pink-600": "_lg:focus:border-pink-600_6d3e9",
  "lg:focus:border-pink-700": "_lg:focus:border-pink-700_6d3e9",
  "lg:focus:border-pink-800": "_lg:focus:border-pink-800_6d3e9",
  "lg:focus:border-pink-900": "_lg:focus:border-pink-900_6d3e9",
  "lg:focus:border-primary-100": "_lg:focus:border-primary-100_6d3e9",
  "lg:focus:border-primary-200": "_lg:focus:border-primary-200_6d3e9",
  "lg:focus:border-primary-300": "_lg:focus:border-primary-300_6d3e9",
  "lg:focus:border-primary-400": "_lg:focus:border-primary-400_6d3e9",
  "lg:focus:border-primary-500": "_lg:focus:border-primary-500_6d3e9",
  "lg:focus:border-primary-600": "_lg:focus:border-primary-600_6d3e9",
  "lg:focus:border-primary-700": "_lg:focus:border-primary-700_6d3e9",
  "lg:focus:border-primary-800": "_lg:focus:border-primary-800_6d3e9",
  "lg:focus:border-primary-900": "_lg:focus:border-primary-900_6d3e9",
  "lg:focus:border-secondary-100": "_lg:focus:border-secondary-100_6d3e9",
  "lg:focus:border-secondary-200": "_lg:focus:border-secondary-200_6d3e9",
  "lg:focus:border-secondary-300": "_lg:focus:border-secondary-300_6d3e9",
  "lg:focus:border-secondary-400": "_lg:focus:border-secondary-400_6d3e9",
  "lg:focus:border-secondary-500": "_lg:focus:border-secondary-500_6d3e9",
  "lg:focus:border-secondary-600": "_lg:focus:border-secondary-600_6d3e9",
  "lg:focus:border-secondary-700": "_lg:focus:border-secondary-700_6d3e9",
  "lg:focus:border-secondary-800": "_lg:focus:border-secondary-800_6d3e9",
  "lg:focus:border-secondary-900": "_lg:focus:border-secondary-900_6d3e9",
  "lg:rounded-none": "_lg:rounded-none_6d3e9",
  "lg:rounded-sm": "_lg:rounded-sm_6d3e9",
  "lg:rounded": "_lg:rounded_6d3e9",
  "lg:rounded-lg": "_lg:rounded-lg_6d3e9",
  "lg:rounded-full": "_lg:rounded-full_6d3e9",
  "lg:rounded-t-none": "_lg:rounded-t-none_6d3e9",
  "lg:rounded-r-none": "_lg:rounded-r-none_6d3e9",
  "lg:rounded-b-none": "_lg:rounded-b-none_6d3e9",
  "lg:rounded-l-none": "_lg:rounded-l-none_6d3e9",
  "lg:rounded-t-sm": "_lg:rounded-t-sm_6d3e9",
  "lg:rounded-r-sm": "_lg:rounded-r-sm_6d3e9",
  "lg:rounded-b-sm": "_lg:rounded-b-sm_6d3e9",
  "lg:rounded-l-sm": "_lg:rounded-l-sm_6d3e9",
  "lg:rounded-t": "_lg:rounded-t_6d3e9",
  "lg:rounded-r": "_lg:rounded-r_6d3e9",
  "lg:rounded-b": "_lg:rounded-b_6d3e9",
  "lg:rounded-l": "_lg:rounded-l_6d3e9",
  "lg:rounded-t-lg": "_lg:rounded-t-lg_6d3e9",
  "lg:rounded-r-lg": "_lg:rounded-r-lg_6d3e9",
  "lg:rounded-b-lg": "_lg:rounded-b-lg_6d3e9",
  "lg:rounded-l-lg": "_lg:rounded-l-lg_6d3e9",
  "lg:rounded-t-full": "_lg:rounded-t-full_6d3e9",
  "lg:rounded-r-full": "_lg:rounded-r-full_6d3e9",
  "lg:rounded-b-full": "_lg:rounded-b-full_6d3e9",
  "lg:rounded-l-full": "_lg:rounded-l-full_6d3e9",
  "lg:rounded-tl-none": "_lg:rounded-tl-none_6d3e9",
  "lg:rounded-tr-none": "_lg:rounded-tr-none_6d3e9",
  "lg:rounded-br-none": "_lg:rounded-br-none_6d3e9",
  "lg:rounded-bl-none": "_lg:rounded-bl-none_6d3e9",
  "lg:rounded-tl-sm": "_lg:rounded-tl-sm_6d3e9",
  "lg:rounded-tr-sm": "_lg:rounded-tr-sm_6d3e9",
  "lg:rounded-br-sm": "_lg:rounded-br-sm_6d3e9",
  "lg:rounded-bl-sm": "_lg:rounded-bl-sm_6d3e9",
  "lg:rounded-tl": "_lg:rounded-tl_6d3e9",
  "lg:rounded-tr": "_lg:rounded-tr_6d3e9",
  "lg:rounded-br": "_lg:rounded-br_6d3e9",
  "lg:rounded-bl": "_lg:rounded-bl_6d3e9",
  "lg:rounded-tl-lg": "_lg:rounded-tl-lg_6d3e9",
  "lg:rounded-tr-lg": "_lg:rounded-tr-lg_6d3e9",
  "lg:rounded-br-lg": "_lg:rounded-br-lg_6d3e9",
  "lg:rounded-bl-lg": "_lg:rounded-bl-lg_6d3e9",
  "lg:rounded-tl-full": "_lg:rounded-tl-full_6d3e9",
  "lg:rounded-tr-full": "_lg:rounded-tr-full_6d3e9",
  "lg:rounded-br-full": "_lg:rounded-br-full_6d3e9",
  "lg:rounded-bl-full": "_lg:rounded-bl-full_6d3e9",
  "lg:border-solid": "_lg:border-solid_6d3e9",
  "lg:border-dashed": "_lg:border-dashed_6d3e9",
  "lg:border-dotted": "_lg:border-dotted_6d3e9",
  "lg:border-double": "_lg:border-double_6d3e9",
  "lg:border-none": "_lg:border-none_6d3e9",
  "lg:border-0": "_lg:border-0_6d3e9",
  "lg:border-2": "_lg:border-2_6d3e9",
  "lg:border-4": "_lg:border-4_6d3e9",
  "lg:border-8": "_lg:border-8_6d3e9",
  "lg:border": "_lg:border_6d3e9",
  "lg:border-t-0": "_lg:border-t-0_6d3e9",
  "lg:border-r-0": "_lg:border-r-0_6d3e9",
  "lg:border-b-0": "_lg:border-b-0_6d3e9",
  "lg:border-l-0": "_lg:border-l-0_6d3e9",
  "lg:border-t-2": "_lg:border-t-2_6d3e9",
  "lg:border-r-2": "_lg:border-r-2_6d3e9",
  "lg:border-b-2": "_lg:border-b-2_6d3e9",
  "lg:border-l-2": "_lg:border-l-2_6d3e9",
  "lg:border-t-4": "_lg:border-t-4_6d3e9",
  "lg:border-r-4": "_lg:border-r-4_6d3e9",
  "lg:border-b-4": "_lg:border-b-4_6d3e9",
  "lg:border-l-4": "_lg:border-l-4_6d3e9",
  "lg:border-t-8": "_lg:border-t-8_6d3e9",
  "lg:border-r-8": "_lg:border-r-8_6d3e9",
  "lg:border-b-8": "_lg:border-b-8_6d3e9",
  "lg:border-l-8": "_lg:border-l-8_6d3e9",
  "lg:border-t": "_lg:border-t_6d3e9",
  "lg:border-r": "_lg:border-r_6d3e9",
  "lg:border-b": "_lg:border-b_6d3e9",
  "lg:border-l": "_lg:border-l_6d3e9",
  "lg:first:border-0": "_lg:first:border-0_6d3e9",
  "lg:first:border-2": "_lg:first:border-2_6d3e9",
  "lg:first:border-4": "_lg:first:border-4_6d3e9",
  "lg:first:border-8": "_lg:first:border-8_6d3e9",
  "lg:first:border": "_lg:first:border_6d3e9",
  "lg:first:border-t-0": "_lg:first:border-t-0_6d3e9",
  "lg:first:border-r-0": "_lg:first:border-r-0_6d3e9",
  "lg:first:border-b-0": "_lg:first:border-b-0_6d3e9",
  "lg:first:border-l-0": "_lg:first:border-l-0_6d3e9",
  "lg:first:border-t-2": "_lg:first:border-t-2_6d3e9",
  "lg:first:border-r-2": "_lg:first:border-r-2_6d3e9",
  "lg:first:border-b-2": "_lg:first:border-b-2_6d3e9",
  "lg:first:border-l-2": "_lg:first:border-l-2_6d3e9",
  "lg:first:border-t-4": "_lg:first:border-t-4_6d3e9",
  "lg:first:border-r-4": "_lg:first:border-r-4_6d3e9",
  "lg:first:border-b-4": "_lg:first:border-b-4_6d3e9",
  "lg:first:border-l-4": "_lg:first:border-l-4_6d3e9",
  "lg:first:border-t-8": "_lg:first:border-t-8_6d3e9",
  "lg:first:border-r-8": "_lg:first:border-r-8_6d3e9",
  "lg:first:border-b-8": "_lg:first:border-b-8_6d3e9",
  "lg:first:border-l-8": "_lg:first:border-l-8_6d3e9",
  "lg:first:border-t": "_lg:first:border-t_6d3e9",
  "lg:first:border-r": "_lg:first:border-r_6d3e9",
  "lg:first:border-b": "_lg:first:border-b_6d3e9",
  "lg:first:border-l": "_lg:first:border-l_6d3e9",
  "lg:hover:border-0": "_lg:hover:border-0_6d3e9",
  "lg:hover:border-2": "_lg:hover:border-2_6d3e9",
  "lg:hover:border-4": "_lg:hover:border-4_6d3e9",
  "lg:hover:border-8": "_lg:hover:border-8_6d3e9",
  "lg:hover:border": "_lg:hover:border_6d3e9",
  "lg:hover:border-t-0": "_lg:hover:border-t-0_6d3e9",
  "lg:hover:border-r-0": "_lg:hover:border-r-0_6d3e9",
  "lg:hover:border-b-0": "_lg:hover:border-b-0_6d3e9",
  "lg:hover:border-l-0": "_lg:hover:border-l-0_6d3e9",
  "lg:hover:border-t-2": "_lg:hover:border-t-2_6d3e9",
  "lg:hover:border-r-2": "_lg:hover:border-r-2_6d3e9",
  "lg:hover:border-b-2": "_lg:hover:border-b-2_6d3e9",
  "lg:hover:border-l-2": "_lg:hover:border-l-2_6d3e9",
  "lg:hover:border-t-4": "_lg:hover:border-t-4_6d3e9",
  "lg:hover:border-r-4": "_lg:hover:border-r-4_6d3e9",
  "lg:hover:border-b-4": "_lg:hover:border-b-4_6d3e9",
  "lg:hover:border-l-4": "_lg:hover:border-l-4_6d3e9",
  "lg:hover:border-t-8": "_lg:hover:border-t-8_6d3e9",
  "lg:hover:border-r-8": "_lg:hover:border-r-8_6d3e9",
  "lg:hover:border-b-8": "_lg:hover:border-b-8_6d3e9",
  "lg:hover:border-l-8": "_lg:hover:border-l-8_6d3e9",
  "lg:hover:border-t": "_lg:hover:border-t_6d3e9",
  "lg:hover:border-r": "_lg:hover:border-r_6d3e9",
  "lg:hover:border-b": "_lg:hover:border-b_6d3e9",
  "lg:hover:border-l": "_lg:hover:border-l_6d3e9",
  "lg:focus:border-0": "_lg:focus:border-0_6d3e9",
  "lg:focus:border-2": "_lg:focus:border-2_6d3e9",
  "lg:focus:border-4": "_lg:focus:border-4_6d3e9",
  "lg:focus:border-8": "_lg:focus:border-8_6d3e9",
  "lg:focus:border": "_lg:focus:border_6d3e9",
  "lg:focus:border-t-0": "_lg:focus:border-t-0_6d3e9",
  "lg:focus:border-r-0": "_lg:focus:border-r-0_6d3e9",
  "lg:focus:border-b-0": "_lg:focus:border-b-0_6d3e9",
  "lg:focus:border-l-0": "_lg:focus:border-l-0_6d3e9",
  "lg:focus:border-t-2": "_lg:focus:border-t-2_6d3e9",
  "lg:focus:border-r-2": "_lg:focus:border-r-2_6d3e9",
  "lg:focus:border-b-2": "_lg:focus:border-b-2_6d3e9",
  "lg:focus:border-l-2": "_lg:focus:border-l-2_6d3e9",
  "lg:focus:border-t-4": "_lg:focus:border-t-4_6d3e9",
  "lg:focus:border-r-4": "_lg:focus:border-r-4_6d3e9",
  "lg:focus:border-b-4": "_lg:focus:border-b-4_6d3e9",
  "lg:focus:border-l-4": "_lg:focus:border-l-4_6d3e9",
  "lg:focus:border-t-8": "_lg:focus:border-t-8_6d3e9",
  "lg:focus:border-r-8": "_lg:focus:border-r-8_6d3e9",
  "lg:focus:border-b-8": "_lg:focus:border-b-8_6d3e9",
  "lg:focus:border-l-8": "_lg:focus:border-l-8_6d3e9",
  "lg:focus:border-t": "_lg:focus:border-t_6d3e9",
  "lg:focus:border-r": "_lg:focus:border-r_6d3e9",
  "lg:focus:border-b": "_lg:focus:border-b_6d3e9",
  "lg:focus:border-l": "_lg:focus:border-l_6d3e9",
  "lg:cursor-auto": "_lg:cursor-auto_6d3e9",
  "lg:cursor-default": "_lg:cursor-default_6d3e9",
  "lg:cursor-pointer": "_lg:cursor-pointer_6d3e9",
  "lg:cursor-wait": "_lg:cursor-wait_6d3e9",
  "lg:cursor-text": "_lg:cursor-text_6d3e9",
  "lg:cursor-move": "_lg:cursor-move_6d3e9",
  "lg:cursor-not-allowed": "_lg:cursor-not-allowed_6d3e9",
  "lg:block": "_lg:block_6d3e9",
  "lg:inline-block": "_lg:inline-block_6d3e9",
  "lg:inline": "_lg:inline_6d3e9",
  "lg:flex": "_lg:flex_6d3e9",
  "lg:inline-flex": "_lg:inline-flex_6d3e9",
  "lg:table": "_lg:table_6d3e9",
  "lg:table-row": "_lg:table-row_6d3e9",
  "lg:table-cell": "_lg:table-cell_6d3e9",
  "lg:hidden": "_lg:hidden_6d3e9",
  "lg:flex-row": "_lg:flex-row_6d3e9",
  "lg:flex-row-reverse": "_lg:flex-row-reverse_6d3e9",
  "lg:flex-col": "_lg:flex-col_6d3e9",
  "lg:flex-col-reverse": "_lg:flex-col-reverse_6d3e9",
  "lg:flex-wrap": "_lg:flex-wrap_6d3e9",
  "lg:flex-wrap-reverse": "_lg:flex-wrap-reverse_6d3e9",
  "lg:flex-no-wrap": "_lg:flex-no-wrap_6d3e9",
  "lg:items-start": "_lg:items-start_6d3e9",
  "lg:items-end": "_lg:items-end_6d3e9",
  "lg:items-center": "_lg:items-center_6d3e9",
  "lg:items-baseline": "_lg:items-baseline_6d3e9",
  "lg:items-stretch": "_lg:items-stretch_6d3e9",
  "lg:self-auto": "_lg:self-auto_6d3e9",
  "lg:self-start": "_lg:self-start_6d3e9",
  "lg:self-end": "_lg:self-end_6d3e9",
  "lg:self-center": "_lg:self-center_6d3e9",
  "lg:self-stretch": "_lg:self-stretch_6d3e9",
  "lg:justify-start": "_lg:justify-start_6d3e9",
  "lg:justify-end": "_lg:justify-end_6d3e9",
  "lg:justify-center": "_lg:justify-center_6d3e9",
  "lg:justify-between": "_lg:justify-between_6d3e9",
  "lg:justify-around": "_lg:justify-around_6d3e9",
  "lg:content-center": "_lg:content-center_6d3e9",
  "lg:content-start": "_lg:content-start_6d3e9",
  "lg:content-end": "_lg:content-end_6d3e9",
  "lg:content-between": "_lg:content-between_6d3e9",
  "lg:content-around": "_lg:content-around_6d3e9",
  "lg:flex-1": "_lg:flex-1_6d3e9",
  "lg:flex-auto": "_lg:flex-auto_6d3e9",
  "lg:flex-initial": "_lg:flex-initial_6d3e9",
  "lg:flex-none": "_lg:flex-none_6d3e9",
  "lg:flex-grow-0": "_lg:flex-grow-0_6d3e9",
  "lg:flex-grow": "_lg:flex-grow_6d3e9",
  "lg:flex-shrink-0": "_lg:flex-shrink-0_6d3e9",
  "lg:flex-shrink": "_lg:flex-shrink_6d3e9",
  "lg:order-1": "_lg:order-1_6d3e9",
  "lg:order-2": "_lg:order-2_6d3e9",
  "lg:order-3": "_lg:order-3_6d3e9",
  "lg:order-4": "_lg:order-4_6d3e9",
  "lg:order-5": "_lg:order-5_6d3e9",
  "lg:order-6": "_lg:order-6_6d3e9",
  "lg:order-7": "_lg:order-7_6d3e9",
  "lg:order-8": "_lg:order-8_6d3e9",
  "lg:order-9": "_lg:order-9_6d3e9",
  "lg:order-10": "_lg:order-10_6d3e9",
  "lg:order-11": "_lg:order-11_6d3e9",
  "lg:order-12": "_lg:order-12_6d3e9",
  "lg:order-first": "_lg:order-first_6d3e9",
  "lg:order-last": "_lg:order-last_6d3e9",
  "lg:order-none": "_lg:order-none_6d3e9",
  "lg:float-right": "_lg:float-right_6d3e9",
  "lg:float-left": "_lg:float-left_6d3e9",
  "lg:float-none": "_lg:float-none_6d3e9",
  "lg:clearfix": "_lg:clearfix_6d3e9",
  "lg:font-sans": "_lg:font-sans_6d3e9",
  "lg:font-serif": "_lg:font-serif_6d3e9",
  "lg:font-mono": "_lg:font-mono_6d3e9",
  "lg:font-hairline": "_lg:font-hairline_6d3e9",
  "lg:font-thin": "_lg:font-thin_6d3e9",
  "lg:font-light": "_lg:font-light_6d3e9",
  "lg:font-normal": "_lg:font-normal_6d3e9",
  "lg:font-medium": "_lg:font-medium_6d3e9",
  "lg:font-semibold": "_lg:font-semibold_6d3e9",
  "lg:font-bold": "_lg:font-bold_6d3e9",
  "lg:font-extrabold": "_lg:font-extrabold_6d3e9",
  "lg:font-black": "_lg:font-black_6d3e9",
  "lg:hover:font-hairline": "_lg:hover:font-hairline_6d3e9",
  "lg:hover:font-thin": "_lg:hover:font-thin_6d3e9",
  "lg:hover:font-light": "_lg:hover:font-light_6d3e9",
  "lg:hover:font-normal": "_lg:hover:font-normal_6d3e9",
  "lg:hover:font-medium": "_lg:hover:font-medium_6d3e9",
  "lg:hover:font-semibold": "_lg:hover:font-semibold_6d3e9",
  "lg:hover:font-bold": "_lg:hover:font-bold_6d3e9",
  "lg:hover:font-extrabold": "_lg:hover:font-extrabold_6d3e9",
  "lg:hover:font-black": "_lg:hover:font-black_6d3e9",
  "lg:focus:font-hairline": "_lg:focus:font-hairline_6d3e9",
  "lg:focus:font-thin": "_lg:focus:font-thin_6d3e9",
  "lg:focus:font-light": "_lg:focus:font-light_6d3e9",
  "lg:focus:font-normal": "_lg:focus:font-normal_6d3e9",
  "lg:focus:font-medium": "_lg:focus:font-medium_6d3e9",
  "lg:focus:font-semibold": "_lg:focus:font-semibold_6d3e9",
  "lg:focus:font-bold": "_lg:focus:font-bold_6d3e9",
  "lg:focus:font-extrabold": "_lg:focus:font-extrabold_6d3e9",
  "lg:focus:font-black": "_lg:focus:font-black_6d3e9",
  "lg:h-0": "_lg:h-0_6d3e9",
  "lg:h-1": "_lg:h-1_6d3e9",
  "lg:h-2": "_lg:h-2_6d3e9",
  "lg:h-3": "_lg:h-3_6d3e9",
  "lg:h-4": "_lg:h-4_6d3e9",
  "lg:h-5": "_lg:h-5_6d3e9",
  "lg:h-6": "_lg:h-6_6d3e9",
  "lg:h-8": "_lg:h-8_6d3e9",
  "lg:h-10": "_lg:h-10_6d3e9",
  "lg:h-12": "_lg:h-12_6d3e9",
  "lg:h-16": "_lg:h-16_6d3e9",
  "lg:h-20": "_lg:h-20_6d3e9",
  "lg:h-24": "_lg:h-24_6d3e9",
  "lg:h-32": "_lg:h-32_6d3e9",
  "lg:h-40": "_lg:h-40_6d3e9",
  "lg:h-48": "_lg:h-48_6d3e9",
  "lg:h-56": "_lg:h-56_6d3e9",
  "lg:h-64": "_lg:h-64_6d3e9",
  "lg:h-auto": "_lg:h-auto_6d3e9",
  "lg:h-px": "_lg:h-px_6d3e9",
  "lg:h-full": "_lg:h-full_6d3e9",
  "lg:h-screen": "_lg:h-screen_6d3e9",
  "lg:leading-none": "_lg:leading-none_6d3e9",
  "lg:leading-tight": "_lg:leading-tight_6d3e9",
  "lg:leading-snug": "_lg:leading-snug_6d3e9",
  "lg:leading-normal": "_lg:leading-normal_6d3e9",
  "lg:leading-relaxed": "_lg:leading-relaxed_6d3e9",
  "lg:leading-loose": "_lg:leading-loose_6d3e9",
  "lg:list-inside": "_lg:list-inside_6d3e9",
  "lg:list-outside": "_lg:list-outside_6d3e9",
  "lg:list-none": "_lg:list-none_6d3e9",
  "lg:list-disc": "_lg:list-disc_6d3e9",
  "lg:list-decimal": "_lg:list-decimal_6d3e9",
  "lg:m-0": "_lg:m-0_6d3e9",
  "lg:m-1": "_lg:m-1_6d3e9",
  "lg:m-2": "_lg:m-2_6d3e9",
  "lg:m-3": "_lg:m-3_6d3e9",
  "lg:m-4": "_lg:m-4_6d3e9",
  "lg:m-5": "_lg:m-5_6d3e9",
  "lg:m-6": "_lg:m-6_6d3e9",
  "lg:m-8": "_lg:m-8_6d3e9",
  "lg:m-10": "_lg:m-10_6d3e9",
  "lg:m-12": "_lg:m-12_6d3e9",
  "lg:m-16": "_lg:m-16_6d3e9",
  "lg:m-20": "_lg:m-20_6d3e9",
  "lg:m-24": "_lg:m-24_6d3e9",
  "lg:m-32": "_lg:m-32_6d3e9",
  "lg:m-40": "_lg:m-40_6d3e9",
  "lg:m-48": "_lg:m-48_6d3e9",
  "lg:m-56": "_lg:m-56_6d3e9",
  "lg:m-64": "_lg:m-64_6d3e9",
  "lg:m-auto": "_lg:m-auto_6d3e9",
  "lg:m-px": "_lg:m-px_6d3e9",
  "lg:-m-1": "_lg:-m-1_6d3e9",
  "lg:-m-2": "_lg:-m-2_6d3e9",
  "lg:-m-3": "_lg:-m-3_6d3e9",
  "lg:-m-4": "_lg:-m-4_6d3e9",
  "lg:-m-5": "_lg:-m-5_6d3e9",
  "lg:-m-6": "_lg:-m-6_6d3e9",
  "lg:-m-8": "_lg:-m-8_6d3e9",
  "lg:-m-10": "_lg:-m-10_6d3e9",
  "lg:-m-12": "_lg:-m-12_6d3e9",
  "lg:-m-16": "_lg:-m-16_6d3e9",
  "lg:-m-20": "_lg:-m-20_6d3e9",
  "lg:-m-24": "_lg:-m-24_6d3e9",
  "lg:-m-32": "_lg:-m-32_6d3e9",
  "lg:-m-40": "_lg:-m-40_6d3e9",
  "lg:-m-48": "_lg:-m-48_6d3e9",
  "lg:-m-56": "_lg:-m-56_6d3e9",
  "lg:-m-64": "_lg:-m-64_6d3e9",
  "lg:-m-px": "_lg:-m-px_6d3e9",
  "lg:my-0": "_lg:my-0_6d3e9",
  "lg:mx-0": "_lg:mx-0_6d3e9",
  "lg:my-1": "_lg:my-1_6d3e9",
  "lg:mx-1": "_lg:mx-1_6d3e9",
  "lg:my-2": "_lg:my-2_6d3e9",
  "lg:mx-2": "_lg:mx-2_6d3e9",
  "lg:my-3": "_lg:my-3_6d3e9",
  "lg:mx-3": "_lg:mx-3_6d3e9",
  "lg:my-4": "_lg:my-4_6d3e9",
  "lg:mx-4": "_lg:mx-4_6d3e9",
  "lg:my-5": "_lg:my-5_6d3e9",
  "lg:mx-5": "_lg:mx-5_6d3e9",
  "lg:my-6": "_lg:my-6_6d3e9",
  "lg:mx-6": "_lg:mx-6_6d3e9",
  "lg:my-8": "_lg:my-8_6d3e9",
  "lg:mx-8": "_lg:mx-8_6d3e9",
  "lg:my-10": "_lg:my-10_6d3e9",
  "lg:mx-10": "_lg:mx-10_6d3e9",
  "lg:my-12": "_lg:my-12_6d3e9",
  "lg:mx-12": "_lg:mx-12_6d3e9",
  "lg:my-16": "_lg:my-16_6d3e9",
  "lg:mx-16": "_lg:mx-16_6d3e9",
  "lg:my-20": "_lg:my-20_6d3e9",
  "lg:mx-20": "_lg:mx-20_6d3e9",
  "lg:my-24": "_lg:my-24_6d3e9",
  "lg:mx-24": "_lg:mx-24_6d3e9",
  "lg:my-32": "_lg:my-32_6d3e9",
  "lg:mx-32": "_lg:mx-32_6d3e9",
  "lg:my-40": "_lg:my-40_6d3e9",
  "lg:mx-40": "_lg:mx-40_6d3e9",
  "lg:my-48": "_lg:my-48_6d3e9",
  "lg:mx-48": "_lg:mx-48_6d3e9",
  "lg:my-56": "_lg:my-56_6d3e9",
  "lg:mx-56": "_lg:mx-56_6d3e9",
  "lg:my-64": "_lg:my-64_6d3e9",
  "lg:mx-64": "_lg:mx-64_6d3e9",
  "lg:my-auto": "_lg:my-auto_6d3e9",
  "lg:mx-auto": "_lg:mx-auto_6d3e9",
  "lg:my-px": "_lg:my-px_6d3e9",
  "lg:mx-px": "_lg:mx-px_6d3e9",
  "lg:-my-1": "_lg:-my-1_6d3e9",
  "lg:-mx-1": "_lg:-mx-1_6d3e9",
  "lg:-my-2": "_lg:-my-2_6d3e9",
  "lg:-mx-2": "_lg:-mx-2_6d3e9",
  "lg:-my-3": "_lg:-my-3_6d3e9",
  "lg:-mx-3": "_lg:-mx-3_6d3e9",
  "lg:-my-4": "_lg:-my-4_6d3e9",
  "lg:-mx-4": "_lg:-mx-4_6d3e9",
  "lg:-my-5": "_lg:-my-5_6d3e9",
  "lg:-mx-5": "_lg:-mx-5_6d3e9",
  "lg:-my-6": "_lg:-my-6_6d3e9",
  "lg:-mx-6": "_lg:-mx-6_6d3e9",
  "lg:-my-8": "_lg:-my-8_6d3e9",
  "lg:-mx-8": "_lg:-mx-8_6d3e9",
  "lg:-my-10": "_lg:-my-10_6d3e9",
  "lg:-mx-10": "_lg:-mx-10_6d3e9",
  "lg:-my-12": "_lg:-my-12_6d3e9",
  "lg:-mx-12": "_lg:-mx-12_6d3e9",
  "lg:-my-16": "_lg:-my-16_6d3e9",
  "lg:-mx-16": "_lg:-mx-16_6d3e9",
  "lg:-my-20": "_lg:-my-20_6d3e9",
  "lg:-mx-20": "_lg:-mx-20_6d3e9",
  "lg:-my-24": "_lg:-my-24_6d3e9",
  "lg:-mx-24": "_lg:-mx-24_6d3e9",
  "lg:-my-32": "_lg:-my-32_6d3e9",
  "lg:-mx-32": "_lg:-mx-32_6d3e9",
  "lg:-my-40": "_lg:-my-40_6d3e9",
  "lg:-mx-40": "_lg:-mx-40_6d3e9",
  "lg:-my-48": "_lg:-my-48_6d3e9",
  "lg:-mx-48": "_lg:-mx-48_6d3e9",
  "lg:-my-56": "_lg:-my-56_6d3e9",
  "lg:-mx-56": "_lg:-mx-56_6d3e9",
  "lg:-my-64": "_lg:-my-64_6d3e9",
  "lg:-mx-64": "_lg:-mx-64_6d3e9",
  "lg:-my-px": "_lg:-my-px_6d3e9",
  "lg:-mx-px": "_lg:-mx-px_6d3e9",
  "lg:mt-0": "_lg:mt-0_6d3e9",
  "lg:mr-0": "_lg:mr-0_6d3e9",
  "lg:mb-0": "_lg:mb-0_6d3e9",
  "lg:ml-0": "_lg:ml-0_6d3e9",
  "lg:mt-1": "_lg:mt-1_6d3e9",
  "lg:mr-1": "_lg:mr-1_6d3e9",
  "lg:mb-1": "_lg:mb-1_6d3e9",
  "lg:ml-1": "_lg:ml-1_6d3e9",
  "lg:mt-2": "_lg:mt-2_6d3e9",
  "lg:mr-2": "_lg:mr-2_6d3e9",
  "lg:mb-2": "_lg:mb-2_6d3e9",
  "lg:ml-2": "_lg:ml-2_6d3e9",
  "lg:mt-3": "_lg:mt-3_6d3e9",
  "lg:mr-3": "_lg:mr-3_6d3e9",
  "lg:mb-3": "_lg:mb-3_6d3e9",
  "lg:ml-3": "_lg:ml-3_6d3e9",
  "lg:mt-4": "_lg:mt-4_6d3e9",
  "lg:mr-4": "_lg:mr-4_6d3e9",
  "lg:mb-4": "_lg:mb-4_6d3e9",
  "lg:ml-4": "_lg:ml-4_6d3e9",
  "lg:mt-5": "_lg:mt-5_6d3e9",
  "lg:mr-5": "_lg:mr-5_6d3e9",
  "lg:mb-5": "_lg:mb-5_6d3e9",
  "lg:ml-5": "_lg:ml-5_6d3e9",
  "lg:mt-6": "_lg:mt-6_6d3e9",
  "lg:mr-6": "_lg:mr-6_6d3e9",
  "lg:mb-6": "_lg:mb-6_6d3e9",
  "lg:ml-6": "_lg:ml-6_6d3e9",
  "lg:mt-8": "_lg:mt-8_6d3e9",
  "lg:mr-8": "_lg:mr-8_6d3e9",
  "lg:mb-8": "_lg:mb-8_6d3e9",
  "lg:ml-8": "_lg:ml-8_6d3e9",
  "lg:mt-10": "_lg:mt-10_6d3e9",
  "lg:mr-10": "_lg:mr-10_6d3e9",
  "lg:mb-10": "_lg:mb-10_6d3e9",
  "lg:ml-10": "_lg:ml-10_6d3e9",
  "lg:mt-12": "_lg:mt-12_6d3e9",
  "lg:mr-12": "_lg:mr-12_6d3e9",
  "lg:mb-12": "_lg:mb-12_6d3e9",
  "lg:ml-12": "_lg:ml-12_6d3e9",
  "lg:mt-16": "_lg:mt-16_6d3e9",
  "lg:mr-16": "_lg:mr-16_6d3e9",
  "lg:mb-16": "_lg:mb-16_6d3e9",
  "lg:ml-16": "_lg:ml-16_6d3e9",
  "lg:mt-20": "_lg:mt-20_6d3e9",
  "lg:mr-20": "_lg:mr-20_6d3e9",
  "lg:mb-20": "_lg:mb-20_6d3e9",
  "lg:ml-20": "_lg:ml-20_6d3e9",
  "lg:mt-24": "_lg:mt-24_6d3e9",
  "lg:mr-24": "_lg:mr-24_6d3e9",
  "lg:mb-24": "_lg:mb-24_6d3e9",
  "lg:ml-24": "_lg:ml-24_6d3e9",
  "lg:mt-32": "_lg:mt-32_6d3e9",
  "lg:mr-32": "_lg:mr-32_6d3e9",
  "lg:mb-32": "_lg:mb-32_6d3e9",
  "lg:ml-32": "_lg:ml-32_6d3e9",
  "lg:mt-40": "_lg:mt-40_6d3e9",
  "lg:mr-40": "_lg:mr-40_6d3e9",
  "lg:mb-40": "_lg:mb-40_6d3e9",
  "lg:ml-40": "_lg:ml-40_6d3e9",
  "lg:mt-48": "_lg:mt-48_6d3e9",
  "lg:mr-48": "_lg:mr-48_6d3e9",
  "lg:mb-48": "_lg:mb-48_6d3e9",
  "lg:ml-48": "_lg:ml-48_6d3e9",
  "lg:mt-56": "_lg:mt-56_6d3e9",
  "lg:mr-56": "_lg:mr-56_6d3e9",
  "lg:mb-56": "_lg:mb-56_6d3e9",
  "lg:ml-56": "_lg:ml-56_6d3e9",
  "lg:mt-64": "_lg:mt-64_6d3e9",
  "lg:mr-64": "_lg:mr-64_6d3e9",
  "lg:mb-64": "_lg:mb-64_6d3e9",
  "lg:ml-64": "_lg:ml-64_6d3e9",
  "lg:mt-auto": "_lg:mt-auto_6d3e9",
  "lg:mr-auto": "_lg:mr-auto_6d3e9",
  "lg:mb-auto": "_lg:mb-auto_6d3e9",
  "lg:ml-auto": "_lg:ml-auto_6d3e9",
  "lg:mt-px": "_lg:mt-px_6d3e9",
  "lg:mr-px": "_lg:mr-px_6d3e9",
  "lg:mb-px": "_lg:mb-px_6d3e9",
  "lg:ml-px": "_lg:ml-px_6d3e9",
  "lg:-mt-1": "_lg:-mt-1_6d3e9",
  "lg:-mr-1": "_lg:-mr-1_6d3e9",
  "lg:-mb-1": "_lg:-mb-1_6d3e9",
  "lg:-ml-1": "_lg:-ml-1_6d3e9",
  "lg:-mt-2": "_lg:-mt-2_6d3e9",
  "lg:-mr-2": "_lg:-mr-2_6d3e9",
  "lg:-mb-2": "_lg:-mb-2_6d3e9",
  "lg:-ml-2": "_lg:-ml-2_6d3e9",
  "lg:-mt-3": "_lg:-mt-3_6d3e9",
  "lg:-mr-3": "_lg:-mr-3_6d3e9",
  "lg:-mb-3": "_lg:-mb-3_6d3e9",
  "lg:-ml-3": "_lg:-ml-3_6d3e9",
  "lg:-mt-4": "_lg:-mt-4_6d3e9",
  "lg:-mr-4": "_lg:-mr-4_6d3e9",
  "lg:-mb-4": "_lg:-mb-4_6d3e9",
  "lg:-ml-4": "_lg:-ml-4_6d3e9",
  "lg:-mt-5": "_lg:-mt-5_6d3e9",
  "lg:-mr-5": "_lg:-mr-5_6d3e9",
  "lg:-mb-5": "_lg:-mb-5_6d3e9",
  "lg:-ml-5": "_lg:-ml-5_6d3e9",
  "lg:-mt-6": "_lg:-mt-6_6d3e9",
  "lg:-mr-6": "_lg:-mr-6_6d3e9",
  "lg:-mb-6": "_lg:-mb-6_6d3e9",
  "lg:-ml-6": "_lg:-ml-6_6d3e9",
  "lg:-mt-8": "_lg:-mt-8_6d3e9",
  "lg:-mr-8": "_lg:-mr-8_6d3e9",
  "lg:-mb-8": "_lg:-mb-8_6d3e9",
  "lg:-ml-8": "_lg:-ml-8_6d3e9",
  "lg:-mt-10": "_lg:-mt-10_6d3e9",
  "lg:-mr-10": "_lg:-mr-10_6d3e9",
  "lg:-mb-10": "_lg:-mb-10_6d3e9",
  "lg:-ml-10": "_lg:-ml-10_6d3e9",
  "lg:-mt-12": "_lg:-mt-12_6d3e9",
  "lg:-mr-12": "_lg:-mr-12_6d3e9",
  "lg:-mb-12": "_lg:-mb-12_6d3e9",
  "lg:-ml-12": "_lg:-ml-12_6d3e9",
  "lg:-mt-16": "_lg:-mt-16_6d3e9",
  "lg:-mr-16": "_lg:-mr-16_6d3e9",
  "lg:-mb-16": "_lg:-mb-16_6d3e9",
  "lg:-ml-16": "_lg:-ml-16_6d3e9",
  "lg:-mt-20": "_lg:-mt-20_6d3e9",
  "lg:-mr-20": "_lg:-mr-20_6d3e9",
  "lg:-mb-20": "_lg:-mb-20_6d3e9",
  "lg:-ml-20": "_lg:-ml-20_6d3e9",
  "lg:-mt-24": "_lg:-mt-24_6d3e9",
  "lg:-mr-24": "_lg:-mr-24_6d3e9",
  "lg:-mb-24": "_lg:-mb-24_6d3e9",
  "lg:-ml-24": "_lg:-ml-24_6d3e9",
  "lg:-mt-32": "_lg:-mt-32_6d3e9",
  "lg:-mr-32": "_lg:-mr-32_6d3e9",
  "lg:-mb-32": "_lg:-mb-32_6d3e9",
  "lg:-ml-32": "_lg:-ml-32_6d3e9",
  "lg:-mt-40": "_lg:-mt-40_6d3e9",
  "lg:-mr-40": "_lg:-mr-40_6d3e9",
  "lg:-mb-40": "_lg:-mb-40_6d3e9",
  "lg:-ml-40": "_lg:-ml-40_6d3e9",
  "lg:-mt-48": "_lg:-mt-48_6d3e9",
  "lg:-mr-48": "_lg:-mr-48_6d3e9",
  "lg:-mb-48": "_lg:-mb-48_6d3e9",
  "lg:-ml-48": "_lg:-ml-48_6d3e9",
  "lg:-mt-56": "_lg:-mt-56_6d3e9",
  "lg:-mr-56": "_lg:-mr-56_6d3e9",
  "lg:-mb-56": "_lg:-mb-56_6d3e9",
  "lg:-ml-56": "_lg:-ml-56_6d3e9",
  "lg:-mt-64": "_lg:-mt-64_6d3e9",
  "lg:-mr-64": "_lg:-mr-64_6d3e9",
  "lg:-mb-64": "_lg:-mb-64_6d3e9",
  "lg:-ml-64": "_lg:-ml-64_6d3e9",
  "lg:-mt-px": "_lg:-mt-px_6d3e9",
  "lg:-mr-px": "_lg:-mr-px_6d3e9",
  "lg:-mb-px": "_lg:-mb-px_6d3e9",
  "lg:-ml-px": "_lg:-ml-px_6d3e9",
  "lg:max-h-full": "_lg:max-h-full_6d3e9",
  "lg:max-h-screen": "_lg:max-h-screen_6d3e9",
  "lg:max-w-xs": "_lg:max-w-xs_6d3e9",
  "lg:max-w-sm": "_lg:max-w-sm_6d3e9",
  "lg:max-w-md": "_lg:max-w-md_6d3e9",
  "lg:max-w-lg": "_lg:max-w-lg_6d3e9",
  "lg:max-w-xl": "_lg:max-w-xl_6d3e9",
  "lg:max-w-2xl": "_lg:max-w-2xl_6d3e9",
  "lg:max-w-3xl": "_lg:max-w-3xl_6d3e9",
  "lg:max-w-4xl": "_lg:max-w-4xl_6d3e9",
  "lg:max-w-5xl": "_lg:max-w-5xl_6d3e9",
  "lg:max-w-6xl": "_lg:max-w-6xl_6d3e9",
  "lg:max-w-full": "_lg:max-w-full_6d3e9",
  "lg:min-h-0": "_lg:min-h-0_6d3e9",
  "lg:min-h-full": "_lg:min-h-full_6d3e9",
  "lg:min-h-screen": "_lg:min-h-screen_6d3e9",
  "lg:min-w-0": "_lg:min-w-0_6d3e9",
  "lg:min-w-full": "_lg:min-w-full_6d3e9",
  "lg:object-contain": "_lg:object-contain_6d3e9",
  "lg:object-cover": "_lg:object-cover_6d3e9",
  "lg:object-fill": "_lg:object-fill_6d3e9",
  "lg:object-none": "_lg:object-none_6d3e9",
  "lg:object-scale-down": "_lg:object-scale-down_6d3e9",
  "lg:object-bottom": "_lg:object-bottom_6d3e9",
  "lg:object-center": "_lg:object-center_6d3e9",
  "lg:object-left": "_lg:object-left_6d3e9",
  "lg:object-left-bottom": "_lg:object-left-bottom_6d3e9",
  "lg:object-left-top": "_lg:object-left-top_6d3e9",
  "lg:object-right": "_lg:object-right_6d3e9",
  "lg:object-right-bottom": "_lg:object-right-bottom_6d3e9",
  "lg:object-right-top": "_lg:object-right-top_6d3e9",
  "lg:object-top": "_lg:object-top_6d3e9",
  "lg:opacity-0": "_lg:opacity-0_6d3e9",
  "lg:opacity-25": "_lg:opacity-25_6d3e9",
  "lg:opacity-50": "_lg:opacity-50_6d3e9",
  "lg:opacity-75": "_lg:opacity-75_6d3e9",
  "lg:opacity-100": "_lg:opacity-100_6d3e9",
  "lg:hover:opacity-0": "_lg:hover:opacity-0_6d3e9",
  "lg:hover:opacity-25": "_lg:hover:opacity-25_6d3e9",
  "lg:hover:opacity-50": "_lg:hover:opacity-50_6d3e9",
  "lg:hover:opacity-75": "_lg:hover:opacity-75_6d3e9",
  "lg:hover:opacity-100": "_lg:hover:opacity-100_6d3e9",
  "lg:focus:opacity-0": "_lg:focus:opacity-0_6d3e9",
  "lg:focus:opacity-25": "_lg:focus:opacity-25_6d3e9",
  "lg:focus:opacity-50": "_lg:focus:opacity-50_6d3e9",
  "lg:focus:opacity-75": "_lg:focus:opacity-75_6d3e9",
  "lg:focus:opacity-100": "_lg:focus:opacity-100_6d3e9",
  "lg:outline-none": "_lg:outline-none_6d3e9",
  "lg:focus:outline-none": "_lg:focus:outline-none_6d3e9",
  "lg:overflow-auto": "_lg:overflow-auto_6d3e9",
  "lg:overflow-hidden": "_lg:overflow-hidden_6d3e9",
  "lg:overflow-visible": "_lg:overflow-visible_6d3e9",
  "lg:overflow-scroll": "_lg:overflow-scroll_6d3e9",
  "lg:overflow-x-auto": "_lg:overflow-x-auto_6d3e9",
  "lg:overflow-y-auto": "_lg:overflow-y-auto_6d3e9",
  "lg:overflow-x-hidden": "_lg:overflow-x-hidden_6d3e9",
  "lg:overflow-y-hidden": "_lg:overflow-y-hidden_6d3e9",
  "lg:overflow-x-visible": "_lg:overflow-x-visible_6d3e9",
  "lg:overflow-y-visible": "_lg:overflow-y-visible_6d3e9",
  "lg:overflow-x-scroll": "_lg:overflow-x-scroll_6d3e9",
  "lg:overflow-y-scroll": "_lg:overflow-y-scroll_6d3e9",
  "lg:scrolling-touch": "_lg:scrolling-touch_6d3e9",
  "lg:scrolling-auto": "_lg:scrolling-auto_6d3e9",
  "lg:p-0": "_lg:p-0_6d3e9",
  "lg:p-1": "_lg:p-1_6d3e9",
  "lg:p-2": "_lg:p-2_6d3e9",
  "lg:p-3": "_lg:p-3_6d3e9",
  "lg:p-4": "_lg:p-4_6d3e9",
  "lg:p-5": "_lg:p-5_6d3e9",
  "lg:p-6": "_lg:p-6_6d3e9",
  "lg:p-8": "_lg:p-8_6d3e9",
  "lg:p-10": "_lg:p-10_6d3e9",
  "lg:p-12": "_lg:p-12_6d3e9",
  "lg:p-16": "_lg:p-16_6d3e9",
  "lg:p-20": "_lg:p-20_6d3e9",
  "lg:p-24": "_lg:p-24_6d3e9",
  "lg:p-32": "_lg:p-32_6d3e9",
  "lg:p-40": "_lg:p-40_6d3e9",
  "lg:p-48": "_lg:p-48_6d3e9",
  "lg:p-56": "_lg:p-56_6d3e9",
  "lg:p-64": "_lg:p-64_6d3e9",
  "lg:p-px": "_lg:p-px_6d3e9",
  "lg:py-0": "_lg:py-0_6d3e9",
  "lg:px-0": "_lg:px-0_6d3e9",
  "lg:py-1": "_lg:py-1_6d3e9",
  "lg:px-1": "_lg:px-1_6d3e9",
  "lg:py-2": "_lg:py-2_6d3e9",
  "lg:px-2": "_lg:px-2_6d3e9",
  "lg:py-3": "_lg:py-3_6d3e9",
  "lg:px-3": "_lg:px-3_6d3e9",
  "lg:py-4": "_lg:py-4_6d3e9",
  "lg:px-4": "_lg:px-4_6d3e9",
  "lg:py-5": "_lg:py-5_6d3e9",
  "lg:px-5": "_lg:px-5_6d3e9",
  "lg:py-6": "_lg:py-6_6d3e9",
  "lg:px-6": "_lg:px-6_6d3e9",
  "lg:py-8": "_lg:py-8_6d3e9",
  "lg:px-8": "_lg:px-8_6d3e9",
  "lg:py-10": "_lg:py-10_6d3e9",
  "lg:px-10": "_lg:px-10_6d3e9",
  "lg:py-12": "_lg:py-12_6d3e9",
  "lg:px-12": "_lg:px-12_6d3e9",
  "lg:py-16": "_lg:py-16_6d3e9",
  "lg:px-16": "_lg:px-16_6d3e9",
  "lg:py-20": "_lg:py-20_6d3e9",
  "lg:px-20": "_lg:px-20_6d3e9",
  "lg:py-24": "_lg:py-24_6d3e9",
  "lg:px-24": "_lg:px-24_6d3e9",
  "lg:py-32": "_lg:py-32_6d3e9",
  "lg:px-32": "_lg:px-32_6d3e9",
  "lg:py-40": "_lg:py-40_6d3e9",
  "lg:px-40": "_lg:px-40_6d3e9",
  "lg:py-48": "_lg:py-48_6d3e9",
  "lg:px-48": "_lg:px-48_6d3e9",
  "lg:py-56": "_lg:py-56_6d3e9",
  "lg:px-56": "_lg:px-56_6d3e9",
  "lg:py-64": "_lg:py-64_6d3e9",
  "lg:px-64": "_lg:px-64_6d3e9",
  "lg:py-px": "_lg:py-px_6d3e9",
  "lg:px-px": "_lg:px-px_6d3e9",
  "lg:pt-0": "_lg:pt-0_6d3e9",
  "lg:pr-0": "_lg:pr-0_6d3e9",
  "lg:pb-0": "_lg:pb-0_6d3e9",
  "lg:pl-0": "_lg:pl-0_6d3e9",
  "lg:pt-1": "_lg:pt-1_6d3e9",
  "lg:pr-1": "_lg:pr-1_6d3e9",
  "lg:pb-1": "_lg:pb-1_6d3e9",
  "lg:pl-1": "_lg:pl-1_6d3e9",
  "lg:pt-2": "_lg:pt-2_6d3e9",
  "lg:pr-2": "_lg:pr-2_6d3e9",
  "lg:pb-2": "_lg:pb-2_6d3e9",
  "lg:pl-2": "_lg:pl-2_6d3e9",
  "lg:pt-3": "_lg:pt-3_6d3e9",
  "lg:pr-3": "_lg:pr-3_6d3e9",
  "lg:pb-3": "_lg:pb-3_6d3e9",
  "lg:pl-3": "_lg:pl-3_6d3e9",
  "lg:pt-4": "_lg:pt-4_6d3e9",
  "lg:pr-4": "_lg:pr-4_6d3e9",
  "lg:pb-4": "_lg:pb-4_6d3e9",
  "lg:pl-4": "_lg:pl-4_6d3e9",
  "lg:pt-5": "_lg:pt-5_6d3e9",
  "lg:pr-5": "_lg:pr-5_6d3e9",
  "lg:pb-5": "_lg:pb-5_6d3e9",
  "lg:pl-5": "_lg:pl-5_6d3e9",
  "lg:pt-6": "_lg:pt-6_6d3e9",
  "lg:pr-6": "_lg:pr-6_6d3e9",
  "lg:pb-6": "_lg:pb-6_6d3e9",
  "lg:pl-6": "_lg:pl-6_6d3e9",
  "lg:pt-8": "_lg:pt-8_6d3e9",
  "lg:pr-8": "_lg:pr-8_6d3e9",
  "lg:pb-8": "_lg:pb-8_6d3e9",
  "lg:pl-8": "_lg:pl-8_6d3e9",
  "lg:pt-10": "_lg:pt-10_6d3e9",
  "lg:pr-10": "_lg:pr-10_6d3e9",
  "lg:pb-10": "_lg:pb-10_6d3e9",
  "lg:pl-10": "_lg:pl-10_6d3e9",
  "lg:pt-12": "_lg:pt-12_6d3e9",
  "lg:pr-12": "_lg:pr-12_6d3e9",
  "lg:pb-12": "_lg:pb-12_6d3e9",
  "lg:pl-12": "_lg:pl-12_6d3e9",
  "lg:pt-16": "_lg:pt-16_6d3e9",
  "lg:pr-16": "_lg:pr-16_6d3e9",
  "lg:pb-16": "_lg:pb-16_6d3e9",
  "lg:pl-16": "_lg:pl-16_6d3e9",
  "lg:pt-20": "_lg:pt-20_6d3e9",
  "lg:pr-20": "_lg:pr-20_6d3e9",
  "lg:pb-20": "_lg:pb-20_6d3e9",
  "lg:pl-20": "_lg:pl-20_6d3e9",
  "lg:pt-24": "_lg:pt-24_6d3e9",
  "lg:pr-24": "_lg:pr-24_6d3e9",
  "lg:pb-24": "_lg:pb-24_6d3e9",
  "lg:pl-24": "_lg:pl-24_6d3e9",
  "lg:pt-32": "_lg:pt-32_6d3e9",
  "lg:pr-32": "_lg:pr-32_6d3e9",
  "lg:pb-32": "_lg:pb-32_6d3e9",
  "lg:pl-32": "_lg:pl-32_6d3e9",
  "lg:pt-40": "_lg:pt-40_6d3e9",
  "lg:pr-40": "_lg:pr-40_6d3e9",
  "lg:pb-40": "_lg:pb-40_6d3e9",
  "lg:pl-40": "_lg:pl-40_6d3e9",
  "lg:pt-48": "_lg:pt-48_6d3e9",
  "lg:pr-48": "_lg:pr-48_6d3e9",
  "lg:pb-48": "_lg:pb-48_6d3e9",
  "lg:pl-48": "_lg:pl-48_6d3e9",
  "lg:pt-56": "_lg:pt-56_6d3e9",
  "lg:pr-56": "_lg:pr-56_6d3e9",
  "lg:pb-56": "_lg:pb-56_6d3e9",
  "lg:pl-56": "_lg:pl-56_6d3e9",
  "lg:pt-64": "_lg:pt-64_6d3e9",
  "lg:pr-64": "_lg:pr-64_6d3e9",
  "lg:pb-64": "_lg:pb-64_6d3e9",
  "lg:pl-64": "_lg:pl-64_6d3e9",
  "lg:pt-px": "_lg:pt-px_6d3e9",
  "lg:pr-px": "_lg:pr-px_6d3e9",
  "lg:pb-px": "_lg:pb-px_6d3e9",
  "lg:pl-px": "_lg:pl-px_6d3e9",
  "lg:placeholder-transparent": "_lg:placeholder-transparent_6d3e9",
  "lg:placeholder-black": "_lg:placeholder-black_6d3e9",
  "lg:placeholder-white": "_lg:placeholder-white_6d3e9",
  "lg:placeholder-gray-100": "_lg:placeholder-gray-100_6d3e9",
  "lg:placeholder-gray-200": "_lg:placeholder-gray-200_6d3e9",
  "lg:placeholder-gray-300": "_lg:placeholder-gray-300_6d3e9",
  "lg:placeholder-gray-400": "_lg:placeholder-gray-400_6d3e9",
  "lg:placeholder-gray-500": "_lg:placeholder-gray-500_6d3e9",
  "lg:placeholder-gray-600": "_lg:placeholder-gray-600_6d3e9",
  "lg:placeholder-gray-700": "_lg:placeholder-gray-700_6d3e9",
  "lg:placeholder-gray-800": "_lg:placeholder-gray-800_6d3e9",
  "lg:placeholder-gray-900": "_lg:placeholder-gray-900_6d3e9",
  "lg:placeholder-red-100": "_lg:placeholder-red-100_6d3e9",
  "lg:placeholder-red-200": "_lg:placeholder-red-200_6d3e9",
  "lg:placeholder-red-300": "_lg:placeholder-red-300_6d3e9",
  "lg:placeholder-red-400": "_lg:placeholder-red-400_6d3e9",
  "lg:placeholder-red-500": "_lg:placeholder-red-500_6d3e9",
  "lg:placeholder-red-600": "_lg:placeholder-red-600_6d3e9",
  "lg:placeholder-red-700": "_lg:placeholder-red-700_6d3e9",
  "lg:placeholder-red-800": "_lg:placeholder-red-800_6d3e9",
  "lg:placeholder-red-900": "_lg:placeholder-red-900_6d3e9",
  "lg:placeholder-orange-100": "_lg:placeholder-orange-100_6d3e9",
  "lg:placeholder-orange-200": "_lg:placeholder-orange-200_6d3e9",
  "lg:placeholder-orange-300": "_lg:placeholder-orange-300_6d3e9",
  "lg:placeholder-orange-400": "_lg:placeholder-orange-400_6d3e9",
  "lg:placeholder-orange-500": "_lg:placeholder-orange-500_6d3e9",
  "lg:placeholder-orange-600": "_lg:placeholder-orange-600_6d3e9",
  "lg:placeholder-orange-700": "_lg:placeholder-orange-700_6d3e9",
  "lg:placeholder-orange-800": "_lg:placeholder-orange-800_6d3e9",
  "lg:placeholder-orange-900": "_lg:placeholder-orange-900_6d3e9",
  "lg:placeholder-yellow-100": "_lg:placeholder-yellow-100_6d3e9",
  "lg:placeholder-yellow-200": "_lg:placeholder-yellow-200_6d3e9",
  "lg:placeholder-yellow-300": "_lg:placeholder-yellow-300_6d3e9",
  "lg:placeholder-yellow-400": "_lg:placeholder-yellow-400_6d3e9",
  "lg:placeholder-yellow-500": "_lg:placeholder-yellow-500_6d3e9",
  "lg:placeholder-yellow-600": "_lg:placeholder-yellow-600_6d3e9",
  "lg:placeholder-yellow-700": "_lg:placeholder-yellow-700_6d3e9",
  "lg:placeholder-yellow-800": "_lg:placeholder-yellow-800_6d3e9",
  "lg:placeholder-yellow-900": "_lg:placeholder-yellow-900_6d3e9",
  "lg:placeholder-green-100": "_lg:placeholder-green-100_6d3e9",
  "lg:placeholder-green-200": "_lg:placeholder-green-200_6d3e9",
  "lg:placeholder-green-300": "_lg:placeholder-green-300_6d3e9",
  "lg:placeholder-green-400": "_lg:placeholder-green-400_6d3e9",
  "lg:placeholder-green-500": "_lg:placeholder-green-500_6d3e9",
  "lg:placeholder-green-600": "_lg:placeholder-green-600_6d3e9",
  "lg:placeholder-green-700": "_lg:placeholder-green-700_6d3e9",
  "lg:placeholder-green-800": "_lg:placeholder-green-800_6d3e9",
  "lg:placeholder-green-900": "_lg:placeholder-green-900_6d3e9",
  "lg:placeholder-teal-100": "_lg:placeholder-teal-100_6d3e9",
  "lg:placeholder-teal-200": "_lg:placeholder-teal-200_6d3e9",
  "lg:placeholder-teal-300": "_lg:placeholder-teal-300_6d3e9",
  "lg:placeholder-teal-400": "_lg:placeholder-teal-400_6d3e9",
  "lg:placeholder-teal-500": "_lg:placeholder-teal-500_6d3e9",
  "lg:placeholder-teal-600": "_lg:placeholder-teal-600_6d3e9",
  "lg:placeholder-teal-700": "_lg:placeholder-teal-700_6d3e9",
  "lg:placeholder-teal-800": "_lg:placeholder-teal-800_6d3e9",
  "lg:placeholder-teal-900": "_lg:placeholder-teal-900_6d3e9",
  "lg:placeholder-blue-100": "_lg:placeholder-blue-100_6d3e9",
  "lg:placeholder-blue-200": "_lg:placeholder-blue-200_6d3e9",
  "lg:placeholder-blue-300": "_lg:placeholder-blue-300_6d3e9",
  "lg:placeholder-blue-400": "_lg:placeholder-blue-400_6d3e9",
  "lg:placeholder-blue-500": "_lg:placeholder-blue-500_6d3e9",
  "lg:placeholder-blue-600": "_lg:placeholder-blue-600_6d3e9",
  "lg:placeholder-blue-700": "_lg:placeholder-blue-700_6d3e9",
  "lg:placeholder-blue-800": "_lg:placeholder-blue-800_6d3e9",
  "lg:placeholder-blue-900": "_lg:placeholder-blue-900_6d3e9",
  "lg:placeholder-indigo-100": "_lg:placeholder-indigo-100_6d3e9",
  "lg:placeholder-indigo-200": "_lg:placeholder-indigo-200_6d3e9",
  "lg:placeholder-indigo-300": "_lg:placeholder-indigo-300_6d3e9",
  "lg:placeholder-indigo-400": "_lg:placeholder-indigo-400_6d3e9",
  "lg:placeholder-indigo-500": "_lg:placeholder-indigo-500_6d3e9",
  "lg:placeholder-indigo-600": "_lg:placeholder-indigo-600_6d3e9",
  "lg:placeholder-indigo-700": "_lg:placeholder-indigo-700_6d3e9",
  "lg:placeholder-indigo-800": "_lg:placeholder-indigo-800_6d3e9",
  "lg:placeholder-indigo-900": "_lg:placeholder-indigo-900_6d3e9",
  "lg:placeholder-purple-100": "_lg:placeholder-purple-100_6d3e9",
  "lg:placeholder-purple-200": "_lg:placeholder-purple-200_6d3e9",
  "lg:placeholder-purple-300": "_lg:placeholder-purple-300_6d3e9",
  "lg:placeholder-purple-400": "_lg:placeholder-purple-400_6d3e9",
  "lg:placeholder-purple-500": "_lg:placeholder-purple-500_6d3e9",
  "lg:placeholder-purple-600": "_lg:placeholder-purple-600_6d3e9",
  "lg:placeholder-purple-700": "_lg:placeholder-purple-700_6d3e9",
  "lg:placeholder-purple-800": "_lg:placeholder-purple-800_6d3e9",
  "lg:placeholder-purple-900": "_lg:placeholder-purple-900_6d3e9",
  "lg:placeholder-pink-100": "_lg:placeholder-pink-100_6d3e9",
  "lg:placeholder-pink-200": "_lg:placeholder-pink-200_6d3e9",
  "lg:placeholder-pink-300": "_lg:placeholder-pink-300_6d3e9",
  "lg:placeholder-pink-400": "_lg:placeholder-pink-400_6d3e9",
  "lg:placeholder-pink-500": "_lg:placeholder-pink-500_6d3e9",
  "lg:placeholder-pink-600": "_lg:placeholder-pink-600_6d3e9",
  "lg:placeholder-pink-700": "_lg:placeholder-pink-700_6d3e9",
  "lg:placeholder-pink-800": "_lg:placeholder-pink-800_6d3e9",
  "lg:placeholder-pink-900": "_lg:placeholder-pink-900_6d3e9",
  "lg:placeholder-primary-100": "_lg:placeholder-primary-100_6d3e9",
  "lg:placeholder-primary-200": "_lg:placeholder-primary-200_6d3e9",
  "lg:placeholder-primary-300": "_lg:placeholder-primary-300_6d3e9",
  "lg:placeholder-primary-400": "_lg:placeholder-primary-400_6d3e9",
  "lg:placeholder-primary-500": "_lg:placeholder-primary-500_6d3e9",
  "lg:placeholder-primary-600": "_lg:placeholder-primary-600_6d3e9",
  "lg:placeholder-primary-700": "_lg:placeholder-primary-700_6d3e9",
  "lg:placeholder-primary-800": "_lg:placeholder-primary-800_6d3e9",
  "lg:placeholder-primary-900": "_lg:placeholder-primary-900_6d3e9",
  "lg:placeholder-secondary-100": "_lg:placeholder-secondary-100_6d3e9",
  "lg:placeholder-secondary-200": "_lg:placeholder-secondary-200_6d3e9",
  "lg:placeholder-secondary-300": "_lg:placeholder-secondary-300_6d3e9",
  "lg:placeholder-secondary-400": "_lg:placeholder-secondary-400_6d3e9",
  "lg:placeholder-secondary-500": "_lg:placeholder-secondary-500_6d3e9",
  "lg:placeholder-secondary-600": "_lg:placeholder-secondary-600_6d3e9",
  "lg:placeholder-secondary-700": "_lg:placeholder-secondary-700_6d3e9",
  "lg:placeholder-secondary-800": "_lg:placeholder-secondary-800_6d3e9",
  "lg:placeholder-secondary-900": "_lg:placeholder-secondary-900_6d3e9",
  "lg:focus:placeholder-transparent": "_lg:focus:placeholder-transparent_6d3e9",
  "lg:focus:placeholder-black": "_lg:focus:placeholder-black_6d3e9",
  "lg:focus:placeholder-white": "_lg:focus:placeholder-white_6d3e9",
  "lg:focus:placeholder-gray-100": "_lg:focus:placeholder-gray-100_6d3e9",
  "lg:focus:placeholder-gray-200": "_lg:focus:placeholder-gray-200_6d3e9",
  "lg:focus:placeholder-gray-300": "_lg:focus:placeholder-gray-300_6d3e9",
  "lg:focus:placeholder-gray-400": "_lg:focus:placeholder-gray-400_6d3e9",
  "lg:focus:placeholder-gray-500": "_lg:focus:placeholder-gray-500_6d3e9",
  "lg:focus:placeholder-gray-600": "_lg:focus:placeholder-gray-600_6d3e9",
  "lg:focus:placeholder-gray-700": "_lg:focus:placeholder-gray-700_6d3e9",
  "lg:focus:placeholder-gray-800": "_lg:focus:placeholder-gray-800_6d3e9",
  "lg:focus:placeholder-gray-900": "_lg:focus:placeholder-gray-900_6d3e9",
  "lg:focus:placeholder-red-100": "_lg:focus:placeholder-red-100_6d3e9",
  "lg:focus:placeholder-red-200": "_lg:focus:placeholder-red-200_6d3e9",
  "lg:focus:placeholder-red-300": "_lg:focus:placeholder-red-300_6d3e9",
  "lg:focus:placeholder-red-400": "_lg:focus:placeholder-red-400_6d3e9",
  "lg:focus:placeholder-red-500": "_lg:focus:placeholder-red-500_6d3e9",
  "lg:focus:placeholder-red-600": "_lg:focus:placeholder-red-600_6d3e9",
  "lg:focus:placeholder-red-700": "_lg:focus:placeholder-red-700_6d3e9",
  "lg:focus:placeholder-red-800": "_lg:focus:placeholder-red-800_6d3e9",
  "lg:focus:placeholder-red-900": "_lg:focus:placeholder-red-900_6d3e9",
  "lg:focus:placeholder-orange-100": "_lg:focus:placeholder-orange-100_6d3e9",
  "lg:focus:placeholder-orange-200": "_lg:focus:placeholder-orange-200_6d3e9",
  "lg:focus:placeholder-orange-300": "_lg:focus:placeholder-orange-300_6d3e9",
  "lg:focus:placeholder-orange-400": "_lg:focus:placeholder-orange-400_6d3e9",
  "lg:focus:placeholder-orange-500": "_lg:focus:placeholder-orange-500_6d3e9",
  "lg:focus:placeholder-orange-600": "_lg:focus:placeholder-orange-600_6d3e9",
  "lg:focus:placeholder-orange-700": "_lg:focus:placeholder-orange-700_6d3e9",
  "lg:focus:placeholder-orange-800": "_lg:focus:placeholder-orange-800_6d3e9",
  "lg:focus:placeholder-orange-900": "_lg:focus:placeholder-orange-900_6d3e9",
  "lg:focus:placeholder-yellow-100": "_lg:focus:placeholder-yellow-100_6d3e9",
  "lg:focus:placeholder-yellow-200": "_lg:focus:placeholder-yellow-200_6d3e9",
  "lg:focus:placeholder-yellow-300": "_lg:focus:placeholder-yellow-300_6d3e9",
  "lg:focus:placeholder-yellow-400": "_lg:focus:placeholder-yellow-400_6d3e9",
  "lg:focus:placeholder-yellow-500": "_lg:focus:placeholder-yellow-500_6d3e9",
  "lg:focus:placeholder-yellow-600": "_lg:focus:placeholder-yellow-600_6d3e9",
  "lg:focus:placeholder-yellow-700": "_lg:focus:placeholder-yellow-700_6d3e9",
  "lg:focus:placeholder-yellow-800": "_lg:focus:placeholder-yellow-800_6d3e9",
  "lg:focus:placeholder-yellow-900": "_lg:focus:placeholder-yellow-900_6d3e9",
  "lg:focus:placeholder-green-100": "_lg:focus:placeholder-green-100_6d3e9",
  "lg:focus:placeholder-green-200": "_lg:focus:placeholder-green-200_6d3e9",
  "lg:focus:placeholder-green-300": "_lg:focus:placeholder-green-300_6d3e9",
  "lg:focus:placeholder-green-400": "_lg:focus:placeholder-green-400_6d3e9",
  "lg:focus:placeholder-green-500": "_lg:focus:placeholder-green-500_6d3e9",
  "lg:focus:placeholder-green-600": "_lg:focus:placeholder-green-600_6d3e9",
  "lg:focus:placeholder-green-700": "_lg:focus:placeholder-green-700_6d3e9",
  "lg:focus:placeholder-green-800": "_lg:focus:placeholder-green-800_6d3e9",
  "lg:focus:placeholder-green-900": "_lg:focus:placeholder-green-900_6d3e9",
  "lg:focus:placeholder-teal-100": "_lg:focus:placeholder-teal-100_6d3e9",
  "lg:focus:placeholder-teal-200": "_lg:focus:placeholder-teal-200_6d3e9",
  "lg:focus:placeholder-teal-300": "_lg:focus:placeholder-teal-300_6d3e9",
  "lg:focus:placeholder-teal-400": "_lg:focus:placeholder-teal-400_6d3e9",
  "lg:focus:placeholder-teal-500": "_lg:focus:placeholder-teal-500_6d3e9",
  "lg:focus:placeholder-teal-600": "_lg:focus:placeholder-teal-600_6d3e9",
  "lg:focus:placeholder-teal-700": "_lg:focus:placeholder-teal-700_6d3e9",
  "lg:focus:placeholder-teal-800": "_lg:focus:placeholder-teal-800_6d3e9",
  "lg:focus:placeholder-teal-900": "_lg:focus:placeholder-teal-900_6d3e9",
  "lg:focus:placeholder-blue-100": "_lg:focus:placeholder-blue-100_6d3e9",
  "lg:focus:placeholder-blue-200": "_lg:focus:placeholder-blue-200_6d3e9",
  "lg:focus:placeholder-blue-300": "_lg:focus:placeholder-blue-300_6d3e9",
  "lg:focus:placeholder-blue-400": "_lg:focus:placeholder-blue-400_6d3e9",
  "lg:focus:placeholder-blue-500": "_lg:focus:placeholder-blue-500_6d3e9",
  "lg:focus:placeholder-blue-600": "_lg:focus:placeholder-blue-600_6d3e9",
  "lg:focus:placeholder-blue-700": "_lg:focus:placeholder-blue-700_6d3e9",
  "lg:focus:placeholder-blue-800": "_lg:focus:placeholder-blue-800_6d3e9",
  "lg:focus:placeholder-blue-900": "_lg:focus:placeholder-blue-900_6d3e9",
  "lg:focus:placeholder-indigo-100": "_lg:focus:placeholder-indigo-100_6d3e9",
  "lg:focus:placeholder-indigo-200": "_lg:focus:placeholder-indigo-200_6d3e9",
  "lg:focus:placeholder-indigo-300": "_lg:focus:placeholder-indigo-300_6d3e9",
  "lg:focus:placeholder-indigo-400": "_lg:focus:placeholder-indigo-400_6d3e9",
  "lg:focus:placeholder-indigo-500": "_lg:focus:placeholder-indigo-500_6d3e9",
  "lg:focus:placeholder-indigo-600": "_lg:focus:placeholder-indigo-600_6d3e9",
  "lg:focus:placeholder-indigo-700": "_lg:focus:placeholder-indigo-700_6d3e9",
  "lg:focus:placeholder-indigo-800": "_lg:focus:placeholder-indigo-800_6d3e9",
  "lg:focus:placeholder-indigo-900": "_lg:focus:placeholder-indigo-900_6d3e9",
  "lg:focus:placeholder-purple-100": "_lg:focus:placeholder-purple-100_6d3e9",
  "lg:focus:placeholder-purple-200": "_lg:focus:placeholder-purple-200_6d3e9",
  "lg:focus:placeholder-purple-300": "_lg:focus:placeholder-purple-300_6d3e9",
  "lg:focus:placeholder-purple-400": "_lg:focus:placeholder-purple-400_6d3e9",
  "lg:focus:placeholder-purple-500": "_lg:focus:placeholder-purple-500_6d3e9",
  "lg:focus:placeholder-purple-600": "_lg:focus:placeholder-purple-600_6d3e9",
  "lg:focus:placeholder-purple-700": "_lg:focus:placeholder-purple-700_6d3e9",
  "lg:focus:placeholder-purple-800": "_lg:focus:placeholder-purple-800_6d3e9",
  "lg:focus:placeholder-purple-900": "_lg:focus:placeholder-purple-900_6d3e9",
  "lg:focus:placeholder-pink-100": "_lg:focus:placeholder-pink-100_6d3e9",
  "lg:focus:placeholder-pink-200": "_lg:focus:placeholder-pink-200_6d3e9",
  "lg:focus:placeholder-pink-300": "_lg:focus:placeholder-pink-300_6d3e9",
  "lg:focus:placeholder-pink-400": "_lg:focus:placeholder-pink-400_6d3e9",
  "lg:focus:placeholder-pink-500": "_lg:focus:placeholder-pink-500_6d3e9",
  "lg:focus:placeholder-pink-600": "_lg:focus:placeholder-pink-600_6d3e9",
  "lg:focus:placeholder-pink-700": "_lg:focus:placeholder-pink-700_6d3e9",
  "lg:focus:placeholder-pink-800": "_lg:focus:placeholder-pink-800_6d3e9",
  "lg:focus:placeholder-pink-900": "_lg:focus:placeholder-pink-900_6d3e9",
  "lg:focus:placeholder-primary-100": "_lg:focus:placeholder-primary-100_6d3e9",
  "lg:focus:placeholder-primary-200": "_lg:focus:placeholder-primary-200_6d3e9",
  "lg:focus:placeholder-primary-300": "_lg:focus:placeholder-primary-300_6d3e9",
  "lg:focus:placeholder-primary-400": "_lg:focus:placeholder-primary-400_6d3e9",
  "lg:focus:placeholder-primary-500": "_lg:focus:placeholder-primary-500_6d3e9",
  "lg:focus:placeholder-primary-600": "_lg:focus:placeholder-primary-600_6d3e9",
  "lg:focus:placeholder-primary-700": "_lg:focus:placeholder-primary-700_6d3e9",
  "lg:focus:placeholder-primary-800": "_lg:focus:placeholder-primary-800_6d3e9",
  "lg:focus:placeholder-primary-900": "_lg:focus:placeholder-primary-900_6d3e9",
  "lg:focus:placeholder-secondary-100": "_lg:focus:placeholder-secondary-100_6d3e9",
  "lg:focus:placeholder-secondary-200": "_lg:focus:placeholder-secondary-200_6d3e9",
  "lg:focus:placeholder-secondary-300": "_lg:focus:placeholder-secondary-300_6d3e9",
  "lg:focus:placeholder-secondary-400": "_lg:focus:placeholder-secondary-400_6d3e9",
  "lg:focus:placeholder-secondary-500": "_lg:focus:placeholder-secondary-500_6d3e9",
  "lg:focus:placeholder-secondary-600": "_lg:focus:placeholder-secondary-600_6d3e9",
  "lg:focus:placeholder-secondary-700": "_lg:focus:placeholder-secondary-700_6d3e9",
  "lg:focus:placeholder-secondary-800": "_lg:focus:placeholder-secondary-800_6d3e9",
  "lg:focus:placeholder-secondary-900": "_lg:focus:placeholder-secondary-900_6d3e9",
  "lg:pointer-events-none": "_lg:pointer-events-none_6d3e9",
  "lg:pointer-events-auto": "_lg:pointer-events-auto_6d3e9",
  "lg:static": "_lg:static_6d3e9",
  "lg:fixed": "_lg:fixed_6d3e9",
  "lg:absolute": "_lg:absolute_6d3e9",
  "lg:relative": "_lg:relative_6d3e9",
  "lg:sticky": "_lg:sticky_6d3e9",
  "lg:inset-0": "_lg:inset-0_6d3e9",
  "lg:inset-auto": "_lg:inset-auto_6d3e9",
  "lg:inset-y-0": "_lg:inset-y-0_6d3e9",
  "lg:inset-x-0": "_lg:inset-x-0_6d3e9",
  "lg:inset-y-auto": "_lg:inset-y-auto_6d3e9",
  "lg:inset-x-auto": "_lg:inset-x-auto_6d3e9",
  "lg:top-0": "_lg:top-0_6d3e9",
  "lg:right-0": "_lg:right-0_6d3e9",
  "lg:bottom-0": "_lg:bottom-0_6d3e9",
  "lg:left-0": "_lg:left-0_6d3e9",
  "lg:top-auto": "_lg:top-auto_6d3e9",
  "lg:right-auto": "_lg:right-auto_6d3e9",
  "lg:bottom-auto": "_lg:bottom-auto_6d3e9",
  "lg:left-auto": "_lg:left-auto_6d3e9",
  "lg:resize-none": "_lg:resize-none_6d3e9",
  "lg:resize-y": "_lg:resize-y_6d3e9",
  "lg:resize-x": "_lg:resize-x_6d3e9",
  "lg:resize": "_lg:resize_6d3e9",
  "lg:shadow": "_lg:shadow_6d3e9",
  "lg:shadow-md": "_lg:shadow-md_6d3e9",
  "lg:shadow-lg": "_lg:shadow-lg_6d3e9",
  "lg:shadow-xl": "_lg:shadow-xl_6d3e9",
  "lg:shadow-2xl": "_lg:shadow-2xl_6d3e9",
  "lg:shadow-inner": "_lg:shadow-inner_6d3e9",
  "lg:shadow-outline": "_lg:shadow-outline_6d3e9",
  "lg:shadow-none": "_lg:shadow-none_6d3e9",
  "lg:hover:shadow": "_lg:hover:shadow_6d3e9",
  "lg:hover:shadow-md": "_lg:hover:shadow-md_6d3e9",
  "lg:hover:shadow-lg": "_lg:hover:shadow-lg_6d3e9",
  "lg:hover:shadow-xl": "_lg:hover:shadow-xl_6d3e9",
  "lg:hover:shadow-2xl": "_lg:hover:shadow-2xl_6d3e9",
  "lg:hover:shadow-inner": "_lg:hover:shadow-inner_6d3e9",
  "lg:hover:shadow-outline": "_lg:hover:shadow-outline_6d3e9",
  "lg:hover:shadow-none": "_lg:hover:shadow-none_6d3e9",
  "lg:focus:shadow": "_lg:focus:shadow_6d3e9",
  "lg:focus:shadow-md": "_lg:focus:shadow-md_6d3e9",
  "lg:focus:shadow-lg": "_lg:focus:shadow-lg_6d3e9",
  "lg:focus:shadow-xl": "_lg:focus:shadow-xl_6d3e9",
  "lg:focus:shadow-2xl": "_lg:focus:shadow-2xl_6d3e9",
  "lg:focus:shadow-inner": "_lg:focus:shadow-inner_6d3e9",
  "lg:focus:shadow-outline": "_lg:focus:shadow-outline_6d3e9",
  "lg:focus:shadow-none": "_lg:focus:shadow-none_6d3e9",
  "lg:fill-current": "_lg:fill-current_6d3e9",
  "lg:stroke-current": "_lg:stroke-current_6d3e9",
  "lg:table-auto": "_lg:table-auto_6d3e9",
  "lg:table-fixed": "_lg:table-fixed_6d3e9",
  "lg:text-left": "_lg:text-left_6d3e9",
  "lg:text-center": "_lg:text-center_6d3e9",
  "lg:text-right": "_lg:text-right_6d3e9",
  "lg:text-justify": "_lg:text-justify_6d3e9",
  "lg:text-transparent": "_lg:text-transparent_6d3e9",
  "lg:text-black": "_lg:text-black_6d3e9",
  "lg:text-white": "_lg:text-white_6d3e9",
  "lg:text-gray-100": "_lg:text-gray-100_6d3e9",
  "lg:text-gray-200": "_lg:text-gray-200_6d3e9",
  "lg:text-gray-300": "_lg:text-gray-300_6d3e9",
  "lg:text-gray-400": "_lg:text-gray-400_6d3e9",
  "lg:text-gray-500": "_lg:text-gray-500_6d3e9",
  "lg:text-gray-600": "_lg:text-gray-600_6d3e9",
  "lg:text-gray-700": "_lg:text-gray-700_6d3e9",
  "lg:text-gray-800": "_lg:text-gray-800_6d3e9",
  "lg:text-gray-900": "_lg:text-gray-900_6d3e9",
  "lg:text-red-100": "_lg:text-red-100_6d3e9",
  "lg:text-red-200": "_lg:text-red-200_6d3e9",
  "lg:text-red-300": "_lg:text-red-300_6d3e9",
  "lg:text-red-400": "_lg:text-red-400_6d3e9",
  "lg:text-red-500": "_lg:text-red-500_6d3e9",
  "lg:text-red-600": "_lg:text-red-600_6d3e9",
  "lg:text-red-700": "_lg:text-red-700_6d3e9",
  "lg:text-red-800": "_lg:text-red-800_6d3e9",
  "lg:text-red-900": "_lg:text-red-900_6d3e9",
  "lg:text-orange-100": "_lg:text-orange-100_6d3e9",
  "lg:text-orange-200": "_lg:text-orange-200_6d3e9",
  "lg:text-orange-300": "_lg:text-orange-300_6d3e9",
  "lg:text-orange-400": "_lg:text-orange-400_6d3e9",
  "lg:text-orange-500": "_lg:text-orange-500_6d3e9",
  "lg:text-orange-600": "_lg:text-orange-600_6d3e9",
  "lg:text-orange-700": "_lg:text-orange-700_6d3e9",
  "lg:text-orange-800": "_lg:text-orange-800_6d3e9",
  "lg:text-orange-900": "_lg:text-orange-900_6d3e9",
  "lg:text-yellow-100": "_lg:text-yellow-100_6d3e9",
  "lg:text-yellow-200": "_lg:text-yellow-200_6d3e9",
  "lg:text-yellow-300": "_lg:text-yellow-300_6d3e9",
  "lg:text-yellow-400": "_lg:text-yellow-400_6d3e9",
  "lg:text-yellow-500": "_lg:text-yellow-500_6d3e9",
  "lg:text-yellow-600": "_lg:text-yellow-600_6d3e9",
  "lg:text-yellow-700": "_lg:text-yellow-700_6d3e9",
  "lg:text-yellow-800": "_lg:text-yellow-800_6d3e9",
  "lg:text-yellow-900": "_lg:text-yellow-900_6d3e9",
  "lg:text-green-100": "_lg:text-green-100_6d3e9",
  "lg:text-green-200": "_lg:text-green-200_6d3e9",
  "lg:text-green-300": "_lg:text-green-300_6d3e9",
  "lg:text-green-400": "_lg:text-green-400_6d3e9",
  "lg:text-green-500": "_lg:text-green-500_6d3e9",
  "lg:text-green-600": "_lg:text-green-600_6d3e9",
  "lg:text-green-700": "_lg:text-green-700_6d3e9",
  "lg:text-green-800": "_lg:text-green-800_6d3e9",
  "lg:text-green-900": "_lg:text-green-900_6d3e9",
  "lg:text-teal-100": "_lg:text-teal-100_6d3e9",
  "lg:text-teal-200": "_lg:text-teal-200_6d3e9",
  "lg:text-teal-300": "_lg:text-teal-300_6d3e9",
  "lg:text-teal-400": "_lg:text-teal-400_6d3e9",
  "lg:text-teal-500": "_lg:text-teal-500_6d3e9",
  "lg:text-teal-600": "_lg:text-teal-600_6d3e9",
  "lg:text-teal-700": "_lg:text-teal-700_6d3e9",
  "lg:text-teal-800": "_lg:text-teal-800_6d3e9",
  "lg:text-teal-900": "_lg:text-teal-900_6d3e9",
  "lg:text-blue-100": "_lg:text-blue-100_6d3e9",
  "lg:text-blue-200": "_lg:text-blue-200_6d3e9",
  "lg:text-blue-300": "_lg:text-blue-300_6d3e9",
  "lg:text-blue-400": "_lg:text-blue-400_6d3e9",
  "lg:text-blue-500": "_lg:text-blue-500_6d3e9",
  "lg:text-blue-600": "_lg:text-blue-600_6d3e9",
  "lg:text-blue-700": "_lg:text-blue-700_6d3e9",
  "lg:text-blue-800": "_lg:text-blue-800_6d3e9",
  "lg:text-blue-900": "_lg:text-blue-900_6d3e9",
  "lg:text-indigo-100": "_lg:text-indigo-100_6d3e9",
  "lg:text-indigo-200": "_lg:text-indigo-200_6d3e9",
  "lg:text-indigo-300": "_lg:text-indigo-300_6d3e9",
  "lg:text-indigo-400": "_lg:text-indigo-400_6d3e9",
  "lg:text-indigo-500": "_lg:text-indigo-500_6d3e9",
  "lg:text-indigo-600": "_lg:text-indigo-600_6d3e9",
  "lg:text-indigo-700": "_lg:text-indigo-700_6d3e9",
  "lg:text-indigo-800": "_lg:text-indigo-800_6d3e9",
  "lg:text-indigo-900": "_lg:text-indigo-900_6d3e9",
  "lg:text-purple-100": "_lg:text-purple-100_6d3e9",
  "lg:text-purple-200": "_lg:text-purple-200_6d3e9",
  "lg:text-purple-300": "_lg:text-purple-300_6d3e9",
  "lg:text-purple-400": "_lg:text-purple-400_6d3e9",
  "lg:text-purple-500": "_lg:text-purple-500_6d3e9",
  "lg:text-purple-600": "_lg:text-purple-600_6d3e9",
  "lg:text-purple-700": "_lg:text-purple-700_6d3e9",
  "lg:text-purple-800": "_lg:text-purple-800_6d3e9",
  "lg:text-purple-900": "_lg:text-purple-900_6d3e9",
  "lg:text-pink-100": "_lg:text-pink-100_6d3e9",
  "lg:text-pink-200": "_lg:text-pink-200_6d3e9",
  "lg:text-pink-300": "_lg:text-pink-300_6d3e9",
  "lg:text-pink-400": "_lg:text-pink-400_6d3e9",
  "lg:text-pink-500": "_lg:text-pink-500_6d3e9",
  "lg:text-pink-600": "_lg:text-pink-600_6d3e9",
  "lg:text-pink-700": "_lg:text-pink-700_6d3e9",
  "lg:text-pink-800": "_lg:text-pink-800_6d3e9",
  "lg:text-pink-900": "_lg:text-pink-900_6d3e9",
  "lg:text-primary-100": "_lg:text-primary-100_6d3e9",
  "lg:text-primary-200": "_lg:text-primary-200_6d3e9",
  "lg:text-primary-300": "_lg:text-primary-300_6d3e9",
  "lg:text-primary-400": "_lg:text-primary-400_6d3e9",
  "lg:text-primary-500": "_lg:text-primary-500_6d3e9",
  "lg:text-primary-600": "_lg:text-primary-600_6d3e9",
  "lg:text-primary-700": "_lg:text-primary-700_6d3e9",
  "lg:text-primary-800": "_lg:text-primary-800_6d3e9",
  "lg:text-primary-900": "_lg:text-primary-900_6d3e9",
  "lg:text-secondary-100": "_lg:text-secondary-100_6d3e9",
  "lg:text-secondary-200": "_lg:text-secondary-200_6d3e9",
  "lg:text-secondary-300": "_lg:text-secondary-300_6d3e9",
  "lg:text-secondary-400": "_lg:text-secondary-400_6d3e9",
  "lg:text-secondary-500": "_lg:text-secondary-500_6d3e9",
  "lg:text-secondary-600": "_lg:text-secondary-600_6d3e9",
  "lg:text-secondary-700": "_lg:text-secondary-700_6d3e9",
  "lg:text-secondary-800": "_lg:text-secondary-800_6d3e9",
  "lg:text-secondary-900": "_lg:text-secondary-900_6d3e9",
  "lg:hover:text-transparent": "_lg:hover:text-transparent_6d3e9",
  "lg:hover:text-black": "_lg:hover:text-black_6d3e9",
  "lg:hover:text-white": "_lg:hover:text-white_6d3e9",
  "lg:hover:text-gray-100": "_lg:hover:text-gray-100_6d3e9",
  "lg:hover:text-gray-200": "_lg:hover:text-gray-200_6d3e9",
  "lg:hover:text-gray-300": "_lg:hover:text-gray-300_6d3e9",
  "lg:hover:text-gray-400": "_lg:hover:text-gray-400_6d3e9",
  "lg:hover:text-gray-500": "_lg:hover:text-gray-500_6d3e9",
  "lg:hover:text-gray-600": "_lg:hover:text-gray-600_6d3e9",
  "lg:hover:text-gray-700": "_lg:hover:text-gray-700_6d3e9",
  "lg:hover:text-gray-800": "_lg:hover:text-gray-800_6d3e9",
  "lg:hover:text-gray-900": "_lg:hover:text-gray-900_6d3e9",
  "lg:hover:text-red-100": "_lg:hover:text-red-100_6d3e9",
  "lg:hover:text-red-200": "_lg:hover:text-red-200_6d3e9",
  "lg:hover:text-red-300": "_lg:hover:text-red-300_6d3e9",
  "lg:hover:text-red-400": "_lg:hover:text-red-400_6d3e9",
  "lg:hover:text-red-500": "_lg:hover:text-red-500_6d3e9",
  "lg:hover:text-red-600": "_lg:hover:text-red-600_6d3e9",
  "lg:hover:text-red-700": "_lg:hover:text-red-700_6d3e9",
  "lg:hover:text-red-800": "_lg:hover:text-red-800_6d3e9",
  "lg:hover:text-red-900": "_lg:hover:text-red-900_6d3e9",
  "lg:hover:text-orange-100": "_lg:hover:text-orange-100_6d3e9",
  "lg:hover:text-orange-200": "_lg:hover:text-orange-200_6d3e9",
  "lg:hover:text-orange-300": "_lg:hover:text-orange-300_6d3e9",
  "lg:hover:text-orange-400": "_lg:hover:text-orange-400_6d3e9",
  "lg:hover:text-orange-500": "_lg:hover:text-orange-500_6d3e9",
  "lg:hover:text-orange-600": "_lg:hover:text-orange-600_6d3e9",
  "lg:hover:text-orange-700": "_lg:hover:text-orange-700_6d3e9",
  "lg:hover:text-orange-800": "_lg:hover:text-orange-800_6d3e9",
  "lg:hover:text-orange-900": "_lg:hover:text-orange-900_6d3e9",
  "lg:hover:text-yellow-100": "_lg:hover:text-yellow-100_6d3e9",
  "lg:hover:text-yellow-200": "_lg:hover:text-yellow-200_6d3e9",
  "lg:hover:text-yellow-300": "_lg:hover:text-yellow-300_6d3e9",
  "lg:hover:text-yellow-400": "_lg:hover:text-yellow-400_6d3e9",
  "lg:hover:text-yellow-500": "_lg:hover:text-yellow-500_6d3e9",
  "lg:hover:text-yellow-600": "_lg:hover:text-yellow-600_6d3e9",
  "lg:hover:text-yellow-700": "_lg:hover:text-yellow-700_6d3e9",
  "lg:hover:text-yellow-800": "_lg:hover:text-yellow-800_6d3e9",
  "lg:hover:text-yellow-900": "_lg:hover:text-yellow-900_6d3e9",
  "lg:hover:text-green-100": "_lg:hover:text-green-100_6d3e9",
  "lg:hover:text-green-200": "_lg:hover:text-green-200_6d3e9",
  "lg:hover:text-green-300": "_lg:hover:text-green-300_6d3e9",
  "lg:hover:text-green-400": "_lg:hover:text-green-400_6d3e9",
  "lg:hover:text-green-500": "_lg:hover:text-green-500_6d3e9",
  "lg:hover:text-green-600": "_lg:hover:text-green-600_6d3e9",
  "lg:hover:text-green-700": "_lg:hover:text-green-700_6d3e9",
  "lg:hover:text-green-800": "_lg:hover:text-green-800_6d3e9",
  "lg:hover:text-green-900": "_lg:hover:text-green-900_6d3e9",
  "lg:hover:text-teal-100": "_lg:hover:text-teal-100_6d3e9",
  "lg:hover:text-teal-200": "_lg:hover:text-teal-200_6d3e9",
  "lg:hover:text-teal-300": "_lg:hover:text-teal-300_6d3e9",
  "lg:hover:text-teal-400": "_lg:hover:text-teal-400_6d3e9",
  "lg:hover:text-teal-500": "_lg:hover:text-teal-500_6d3e9",
  "lg:hover:text-teal-600": "_lg:hover:text-teal-600_6d3e9",
  "lg:hover:text-teal-700": "_lg:hover:text-teal-700_6d3e9",
  "lg:hover:text-teal-800": "_lg:hover:text-teal-800_6d3e9",
  "lg:hover:text-teal-900": "_lg:hover:text-teal-900_6d3e9",
  "lg:hover:text-blue-100": "_lg:hover:text-blue-100_6d3e9",
  "lg:hover:text-blue-200": "_lg:hover:text-blue-200_6d3e9",
  "lg:hover:text-blue-300": "_lg:hover:text-blue-300_6d3e9",
  "lg:hover:text-blue-400": "_lg:hover:text-blue-400_6d3e9",
  "lg:hover:text-blue-500": "_lg:hover:text-blue-500_6d3e9",
  "lg:hover:text-blue-600": "_lg:hover:text-blue-600_6d3e9",
  "lg:hover:text-blue-700": "_lg:hover:text-blue-700_6d3e9",
  "lg:hover:text-blue-800": "_lg:hover:text-blue-800_6d3e9",
  "lg:hover:text-blue-900": "_lg:hover:text-blue-900_6d3e9",
  "lg:hover:text-indigo-100": "_lg:hover:text-indigo-100_6d3e9",
  "lg:hover:text-indigo-200": "_lg:hover:text-indigo-200_6d3e9",
  "lg:hover:text-indigo-300": "_lg:hover:text-indigo-300_6d3e9",
  "lg:hover:text-indigo-400": "_lg:hover:text-indigo-400_6d3e9",
  "lg:hover:text-indigo-500": "_lg:hover:text-indigo-500_6d3e9",
  "lg:hover:text-indigo-600": "_lg:hover:text-indigo-600_6d3e9",
  "lg:hover:text-indigo-700": "_lg:hover:text-indigo-700_6d3e9",
  "lg:hover:text-indigo-800": "_lg:hover:text-indigo-800_6d3e9",
  "lg:hover:text-indigo-900": "_lg:hover:text-indigo-900_6d3e9",
  "lg:hover:text-purple-100": "_lg:hover:text-purple-100_6d3e9",
  "lg:hover:text-purple-200": "_lg:hover:text-purple-200_6d3e9",
  "lg:hover:text-purple-300": "_lg:hover:text-purple-300_6d3e9",
  "lg:hover:text-purple-400": "_lg:hover:text-purple-400_6d3e9",
  "lg:hover:text-purple-500": "_lg:hover:text-purple-500_6d3e9",
  "lg:hover:text-purple-600": "_lg:hover:text-purple-600_6d3e9",
  "lg:hover:text-purple-700": "_lg:hover:text-purple-700_6d3e9",
  "lg:hover:text-purple-800": "_lg:hover:text-purple-800_6d3e9",
  "lg:hover:text-purple-900": "_lg:hover:text-purple-900_6d3e9",
  "lg:hover:text-pink-100": "_lg:hover:text-pink-100_6d3e9",
  "lg:hover:text-pink-200": "_lg:hover:text-pink-200_6d3e9",
  "lg:hover:text-pink-300": "_lg:hover:text-pink-300_6d3e9",
  "lg:hover:text-pink-400": "_lg:hover:text-pink-400_6d3e9",
  "lg:hover:text-pink-500": "_lg:hover:text-pink-500_6d3e9",
  "lg:hover:text-pink-600": "_lg:hover:text-pink-600_6d3e9",
  "lg:hover:text-pink-700": "_lg:hover:text-pink-700_6d3e9",
  "lg:hover:text-pink-800": "_lg:hover:text-pink-800_6d3e9",
  "lg:hover:text-pink-900": "_lg:hover:text-pink-900_6d3e9",
  "lg:hover:text-primary-100": "_lg:hover:text-primary-100_6d3e9",
  "lg:hover:text-primary-200": "_lg:hover:text-primary-200_6d3e9",
  "lg:hover:text-primary-300": "_lg:hover:text-primary-300_6d3e9",
  "lg:hover:text-primary-400": "_lg:hover:text-primary-400_6d3e9",
  "lg:hover:text-primary-500": "_lg:hover:text-primary-500_6d3e9",
  "lg:hover:text-primary-600": "_lg:hover:text-primary-600_6d3e9",
  "lg:hover:text-primary-700": "_lg:hover:text-primary-700_6d3e9",
  "lg:hover:text-primary-800": "_lg:hover:text-primary-800_6d3e9",
  "lg:hover:text-primary-900": "_lg:hover:text-primary-900_6d3e9",
  "lg:hover:text-secondary-100": "_lg:hover:text-secondary-100_6d3e9",
  "lg:hover:text-secondary-200": "_lg:hover:text-secondary-200_6d3e9",
  "lg:hover:text-secondary-300": "_lg:hover:text-secondary-300_6d3e9",
  "lg:hover:text-secondary-400": "_lg:hover:text-secondary-400_6d3e9",
  "lg:hover:text-secondary-500": "_lg:hover:text-secondary-500_6d3e9",
  "lg:hover:text-secondary-600": "_lg:hover:text-secondary-600_6d3e9",
  "lg:hover:text-secondary-700": "_lg:hover:text-secondary-700_6d3e9",
  "lg:hover:text-secondary-800": "_lg:hover:text-secondary-800_6d3e9",
  "lg:hover:text-secondary-900": "_lg:hover:text-secondary-900_6d3e9",
  "lg:focus:text-transparent": "_lg:focus:text-transparent_6d3e9",
  "lg:focus:text-black": "_lg:focus:text-black_6d3e9",
  "lg:focus:text-white": "_lg:focus:text-white_6d3e9",
  "lg:focus:text-gray-100": "_lg:focus:text-gray-100_6d3e9",
  "lg:focus:text-gray-200": "_lg:focus:text-gray-200_6d3e9",
  "lg:focus:text-gray-300": "_lg:focus:text-gray-300_6d3e9",
  "lg:focus:text-gray-400": "_lg:focus:text-gray-400_6d3e9",
  "lg:focus:text-gray-500": "_lg:focus:text-gray-500_6d3e9",
  "lg:focus:text-gray-600": "_lg:focus:text-gray-600_6d3e9",
  "lg:focus:text-gray-700": "_lg:focus:text-gray-700_6d3e9",
  "lg:focus:text-gray-800": "_lg:focus:text-gray-800_6d3e9",
  "lg:focus:text-gray-900": "_lg:focus:text-gray-900_6d3e9",
  "lg:focus:text-red-100": "_lg:focus:text-red-100_6d3e9",
  "lg:focus:text-red-200": "_lg:focus:text-red-200_6d3e9",
  "lg:focus:text-red-300": "_lg:focus:text-red-300_6d3e9",
  "lg:focus:text-red-400": "_lg:focus:text-red-400_6d3e9",
  "lg:focus:text-red-500": "_lg:focus:text-red-500_6d3e9",
  "lg:focus:text-red-600": "_lg:focus:text-red-600_6d3e9",
  "lg:focus:text-red-700": "_lg:focus:text-red-700_6d3e9",
  "lg:focus:text-red-800": "_lg:focus:text-red-800_6d3e9",
  "lg:focus:text-red-900": "_lg:focus:text-red-900_6d3e9",
  "lg:focus:text-orange-100": "_lg:focus:text-orange-100_6d3e9",
  "lg:focus:text-orange-200": "_lg:focus:text-orange-200_6d3e9",
  "lg:focus:text-orange-300": "_lg:focus:text-orange-300_6d3e9",
  "lg:focus:text-orange-400": "_lg:focus:text-orange-400_6d3e9",
  "lg:focus:text-orange-500": "_lg:focus:text-orange-500_6d3e9",
  "lg:focus:text-orange-600": "_lg:focus:text-orange-600_6d3e9",
  "lg:focus:text-orange-700": "_lg:focus:text-orange-700_6d3e9",
  "lg:focus:text-orange-800": "_lg:focus:text-orange-800_6d3e9",
  "lg:focus:text-orange-900": "_lg:focus:text-orange-900_6d3e9",
  "lg:focus:text-yellow-100": "_lg:focus:text-yellow-100_6d3e9",
  "lg:focus:text-yellow-200": "_lg:focus:text-yellow-200_6d3e9",
  "lg:focus:text-yellow-300": "_lg:focus:text-yellow-300_6d3e9",
  "lg:focus:text-yellow-400": "_lg:focus:text-yellow-400_6d3e9",
  "lg:focus:text-yellow-500": "_lg:focus:text-yellow-500_6d3e9",
  "lg:focus:text-yellow-600": "_lg:focus:text-yellow-600_6d3e9",
  "lg:focus:text-yellow-700": "_lg:focus:text-yellow-700_6d3e9",
  "lg:focus:text-yellow-800": "_lg:focus:text-yellow-800_6d3e9",
  "lg:focus:text-yellow-900": "_lg:focus:text-yellow-900_6d3e9",
  "lg:focus:text-green-100": "_lg:focus:text-green-100_6d3e9",
  "lg:focus:text-green-200": "_lg:focus:text-green-200_6d3e9",
  "lg:focus:text-green-300": "_lg:focus:text-green-300_6d3e9",
  "lg:focus:text-green-400": "_lg:focus:text-green-400_6d3e9",
  "lg:focus:text-green-500": "_lg:focus:text-green-500_6d3e9",
  "lg:focus:text-green-600": "_lg:focus:text-green-600_6d3e9",
  "lg:focus:text-green-700": "_lg:focus:text-green-700_6d3e9",
  "lg:focus:text-green-800": "_lg:focus:text-green-800_6d3e9",
  "lg:focus:text-green-900": "_lg:focus:text-green-900_6d3e9",
  "lg:focus:text-teal-100": "_lg:focus:text-teal-100_6d3e9",
  "lg:focus:text-teal-200": "_lg:focus:text-teal-200_6d3e9",
  "lg:focus:text-teal-300": "_lg:focus:text-teal-300_6d3e9",
  "lg:focus:text-teal-400": "_lg:focus:text-teal-400_6d3e9",
  "lg:focus:text-teal-500": "_lg:focus:text-teal-500_6d3e9",
  "lg:focus:text-teal-600": "_lg:focus:text-teal-600_6d3e9",
  "lg:focus:text-teal-700": "_lg:focus:text-teal-700_6d3e9",
  "lg:focus:text-teal-800": "_lg:focus:text-teal-800_6d3e9",
  "lg:focus:text-teal-900": "_lg:focus:text-teal-900_6d3e9",
  "lg:focus:text-blue-100": "_lg:focus:text-blue-100_6d3e9",
  "lg:focus:text-blue-200": "_lg:focus:text-blue-200_6d3e9",
  "lg:focus:text-blue-300": "_lg:focus:text-blue-300_6d3e9",
  "lg:focus:text-blue-400": "_lg:focus:text-blue-400_6d3e9",
  "lg:focus:text-blue-500": "_lg:focus:text-blue-500_6d3e9",
  "lg:focus:text-blue-600": "_lg:focus:text-blue-600_6d3e9",
  "lg:focus:text-blue-700": "_lg:focus:text-blue-700_6d3e9",
  "lg:focus:text-blue-800": "_lg:focus:text-blue-800_6d3e9",
  "lg:focus:text-blue-900": "_lg:focus:text-blue-900_6d3e9",
  "lg:focus:text-indigo-100": "_lg:focus:text-indigo-100_6d3e9",
  "lg:focus:text-indigo-200": "_lg:focus:text-indigo-200_6d3e9",
  "lg:focus:text-indigo-300": "_lg:focus:text-indigo-300_6d3e9",
  "lg:focus:text-indigo-400": "_lg:focus:text-indigo-400_6d3e9",
  "lg:focus:text-indigo-500": "_lg:focus:text-indigo-500_6d3e9",
  "lg:focus:text-indigo-600": "_lg:focus:text-indigo-600_6d3e9",
  "lg:focus:text-indigo-700": "_lg:focus:text-indigo-700_6d3e9",
  "lg:focus:text-indigo-800": "_lg:focus:text-indigo-800_6d3e9",
  "lg:focus:text-indigo-900": "_lg:focus:text-indigo-900_6d3e9",
  "lg:focus:text-purple-100": "_lg:focus:text-purple-100_6d3e9",
  "lg:focus:text-purple-200": "_lg:focus:text-purple-200_6d3e9",
  "lg:focus:text-purple-300": "_lg:focus:text-purple-300_6d3e9",
  "lg:focus:text-purple-400": "_lg:focus:text-purple-400_6d3e9",
  "lg:focus:text-purple-500": "_lg:focus:text-purple-500_6d3e9",
  "lg:focus:text-purple-600": "_lg:focus:text-purple-600_6d3e9",
  "lg:focus:text-purple-700": "_lg:focus:text-purple-700_6d3e9",
  "lg:focus:text-purple-800": "_lg:focus:text-purple-800_6d3e9",
  "lg:focus:text-purple-900": "_lg:focus:text-purple-900_6d3e9",
  "lg:focus:text-pink-100": "_lg:focus:text-pink-100_6d3e9",
  "lg:focus:text-pink-200": "_lg:focus:text-pink-200_6d3e9",
  "lg:focus:text-pink-300": "_lg:focus:text-pink-300_6d3e9",
  "lg:focus:text-pink-400": "_lg:focus:text-pink-400_6d3e9",
  "lg:focus:text-pink-500": "_lg:focus:text-pink-500_6d3e9",
  "lg:focus:text-pink-600": "_lg:focus:text-pink-600_6d3e9",
  "lg:focus:text-pink-700": "_lg:focus:text-pink-700_6d3e9",
  "lg:focus:text-pink-800": "_lg:focus:text-pink-800_6d3e9",
  "lg:focus:text-pink-900": "_lg:focus:text-pink-900_6d3e9",
  "lg:focus:text-primary-100": "_lg:focus:text-primary-100_6d3e9",
  "lg:focus:text-primary-200": "_lg:focus:text-primary-200_6d3e9",
  "lg:focus:text-primary-300": "_lg:focus:text-primary-300_6d3e9",
  "lg:focus:text-primary-400": "_lg:focus:text-primary-400_6d3e9",
  "lg:focus:text-primary-500": "_lg:focus:text-primary-500_6d3e9",
  "lg:focus:text-primary-600": "_lg:focus:text-primary-600_6d3e9",
  "lg:focus:text-primary-700": "_lg:focus:text-primary-700_6d3e9",
  "lg:focus:text-primary-800": "_lg:focus:text-primary-800_6d3e9",
  "lg:focus:text-primary-900": "_lg:focus:text-primary-900_6d3e9",
  "lg:focus:text-secondary-100": "_lg:focus:text-secondary-100_6d3e9",
  "lg:focus:text-secondary-200": "_lg:focus:text-secondary-200_6d3e9",
  "lg:focus:text-secondary-300": "_lg:focus:text-secondary-300_6d3e9",
  "lg:focus:text-secondary-400": "_lg:focus:text-secondary-400_6d3e9",
  "lg:focus:text-secondary-500": "_lg:focus:text-secondary-500_6d3e9",
  "lg:focus:text-secondary-600": "_lg:focus:text-secondary-600_6d3e9",
  "lg:focus:text-secondary-700": "_lg:focus:text-secondary-700_6d3e9",
  "lg:focus:text-secondary-800": "_lg:focus:text-secondary-800_6d3e9",
  "lg:focus:text-secondary-900": "_lg:focus:text-secondary-900_6d3e9",
  "lg:text-xs": "_lg:text-xs_6d3e9",
  "lg:text-sm": "_lg:text-sm_6d3e9",
  "lg:text-base": "_lg:text-base_6d3e9",
  "lg:text-lg": "_lg:text-lg_6d3e9",
  "lg:text-xl": "_lg:text-xl_6d3e9",
  "lg:text-2xl": "_lg:text-2xl_6d3e9",
  "lg:text-3xl": "_lg:text-3xl_6d3e9",
  "lg:text-4xl": "_lg:text-4xl_6d3e9",
  "lg:text-5xl": "_lg:text-5xl_6d3e9",
  "lg:text-6xl": "_lg:text-6xl_6d3e9",
  "lg:italic": "_lg:italic_6d3e9",
  "lg:not-italic": "_lg:not-italic_6d3e9",
  "lg:uppercase": "_lg:uppercase_6d3e9",
  "lg:lowercase": "_lg:lowercase_6d3e9",
  "lg:capitalize": "_lg:capitalize_6d3e9",
  "lg:normal-case": "_lg:normal-case_6d3e9",
  "lg:underline": "_lg:underline_6d3e9",
  "lg:line-through": "_lg:line-through_6d3e9",
  "lg:no-underline": "_lg:no-underline_6d3e9",
  "lg:hover:underline": "_lg:hover:underline_6d3e9",
  "lg:hover:line-through": "_lg:hover:line-through_6d3e9",
  "lg:hover:no-underline": "_lg:hover:no-underline_6d3e9",
  "lg:focus:underline": "_lg:focus:underline_6d3e9",
  "lg:focus:line-through": "_lg:focus:line-through_6d3e9",
  "lg:focus:no-underline": "_lg:focus:no-underline_6d3e9",
  "lg:antialiased": "_lg:antialiased_6d3e9",
  "lg:subpixel-antialiased": "_lg:subpixel-antialiased_6d3e9",
  "lg:tracking-tighter": "_lg:tracking-tighter_6d3e9",
  "lg:tracking-tight": "_lg:tracking-tight_6d3e9",
  "lg:tracking-normal": "_lg:tracking-normal_6d3e9",
  "lg:tracking-wide": "_lg:tracking-wide_6d3e9",
  "lg:tracking-wider": "_lg:tracking-wider_6d3e9",
  "lg:tracking-widest": "_lg:tracking-widest_6d3e9",
  "lg:select-none": "_lg:select-none_6d3e9",
  "lg:select-text": "_lg:select-text_6d3e9",
  "lg:select-all": "_lg:select-all_6d3e9",
  "lg:select-auto": "_lg:select-auto_6d3e9",
  "lg:align-baseline": "_lg:align-baseline_6d3e9",
  "lg:align-top": "_lg:align-top_6d3e9",
  "lg:align-middle": "_lg:align-middle_6d3e9",
  "lg:align-bottom": "_lg:align-bottom_6d3e9",
  "lg:align-text-top": "_lg:align-text-top_6d3e9",
  "lg:align-text-bottom": "_lg:align-text-bottom_6d3e9",
  "lg:visible": "_lg:visible_6d3e9",
  "lg:invisible": "_lg:invisible_6d3e9",
  "lg:whitespace-normal": "_lg:whitespace-normal_6d3e9",
  "lg:whitespace-no-wrap": "_lg:whitespace-no-wrap_6d3e9",
  "lg:whitespace-pre": "_lg:whitespace-pre_6d3e9",
  "lg:whitespace-pre-line": "_lg:whitespace-pre-line_6d3e9",
  "lg:whitespace-pre-wrap": "_lg:whitespace-pre-wrap_6d3e9",
  "lg:break-normal": "_lg:break-normal_6d3e9",
  "lg:break-words": "_lg:break-words_6d3e9",
  "lg:break-all": "_lg:break-all_6d3e9",
  "lg:truncate": "_lg:truncate_6d3e9",
  "lg:w-0": "_lg:w-0_6d3e9",
  "lg:w-1": "_lg:w-1_6d3e9",
  "lg:w-2": "_lg:w-2_6d3e9",
  "lg:w-3": "_lg:w-3_6d3e9",
  "lg:w-4": "_lg:w-4_6d3e9",
  "lg:w-5": "_lg:w-5_6d3e9",
  "lg:w-6": "_lg:w-6_6d3e9",
  "lg:w-8": "_lg:w-8_6d3e9",
  "lg:w-10": "_lg:w-10_6d3e9",
  "lg:w-12": "_lg:w-12_6d3e9",
  "lg:w-16": "_lg:w-16_6d3e9",
  "lg:w-20": "_lg:w-20_6d3e9",
  "lg:w-24": "_lg:w-24_6d3e9",
  "lg:w-32": "_lg:w-32_6d3e9",
  "lg:w-40": "_lg:w-40_6d3e9",
  "lg:w-48": "_lg:w-48_6d3e9",
  "lg:w-56": "_lg:w-56_6d3e9",
  "lg:w-64": "_lg:w-64_6d3e9",
  "lg:w-auto": "_lg:w-auto_6d3e9",
  "lg:w-px": "_lg:w-px_6d3e9",
  "lg:w-1/2": "_lg:w-1/2_6d3e9",
  "lg:w-1/3": "_lg:w-1/3_6d3e9",
  "lg:w-2/3": "_lg:w-2/3_6d3e9",
  "lg:w-1/4": "_lg:w-1/4_6d3e9",
  "lg:w-2/4": "_lg:w-2/4_6d3e9",
  "lg:w-3/4": "_lg:w-3/4_6d3e9",
  "lg:w-1/5": "_lg:w-1/5_6d3e9",
  "lg:w-2/5": "_lg:w-2/5_6d3e9",
  "lg:w-3/5": "_lg:w-3/5_6d3e9",
  "lg:w-4/5": "_lg:w-4/5_6d3e9",
  "lg:w-1/6": "_lg:w-1/6_6d3e9",
  "lg:w-2/6": "_lg:w-2/6_6d3e9",
  "lg:w-3/6": "_lg:w-3/6_6d3e9",
  "lg:w-4/6": "_lg:w-4/6_6d3e9",
  "lg:w-5/6": "_lg:w-5/6_6d3e9",
  "lg:w-1/12": "_lg:w-1/12_6d3e9",
  "lg:w-2/12": "_lg:w-2/12_6d3e9",
  "lg:w-3/12": "_lg:w-3/12_6d3e9",
  "lg:w-4/12": "_lg:w-4/12_6d3e9",
  "lg:w-5/12": "_lg:w-5/12_6d3e9",
  "lg:w-6/12": "_lg:w-6/12_6d3e9",
  "lg:w-7/12": "_lg:w-7/12_6d3e9",
  "lg:w-8/12": "_lg:w-8/12_6d3e9",
  "lg:w-9/12": "_lg:w-9/12_6d3e9",
  "lg:w-10/12": "_lg:w-10/12_6d3e9",
  "lg:w-11/12": "_lg:w-11/12_6d3e9",
  "lg:w-full": "_lg:w-full_6d3e9",
  "lg:w-screen": "_lg:w-screen_6d3e9",
  "lg:z-0": "_lg:z-0_6d3e9",
  "lg:z-10": "_lg:z-10_6d3e9",
  "lg:z-20": "_lg:z-20_6d3e9",
  "lg:z-30": "_lg:z-30_6d3e9",
  "lg:z-40": "_lg:z-40_6d3e9",
  "lg:z-50": "_lg:z-50_6d3e9",
  "lg:z-auto": "_lg:z-auto_6d3e9",
  "xl:sr-only": "_xl:sr-only_6d3e9",
  "xl:not-sr-only": "_xl:not-sr-only_6d3e9",
  "xl:focus:sr-only": "_xl:focus:sr-only_6d3e9",
  "xl:focus:not-sr-only": "_xl:focus:not-sr-only_6d3e9",
  "xl:appearance-none": "_xl:appearance-none_6d3e9",
  "xl:bg-fixed": "_xl:bg-fixed_6d3e9",
  "xl:bg-local": "_xl:bg-local_6d3e9",
  "xl:bg-scroll": "_xl:bg-scroll_6d3e9",
  "xl:bg-transparent": "_xl:bg-transparent_6d3e9",
  "xl:bg-black": "_xl:bg-black_6d3e9",
  "xl:bg-white": "_xl:bg-white_6d3e9",
  "xl:bg-gray-100": "_xl:bg-gray-100_6d3e9",
  "xl:bg-gray-200": "_xl:bg-gray-200_6d3e9",
  "xl:bg-gray-300": "_xl:bg-gray-300_6d3e9",
  "xl:bg-gray-400": "_xl:bg-gray-400_6d3e9",
  "xl:bg-gray-500": "_xl:bg-gray-500_6d3e9",
  "xl:bg-gray-600": "_xl:bg-gray-600_6d3e9",
  "xl:bg-gray-700": "_xl:bg-gray-700_6d3e9",
  "xl:bg-gray-800": "_xl:bg-gray-800_6d3e9",
  "xl:bg-gray-900": "_xl:bg-gray-900_6d3e9",
  "xl:bg-red-100": "_xl:bg-red-100_6d3e9",
  "xl:bg-red-200": "_xl:bg-red-200_6d3e9",
  "xl:bg-red-300": "_xl:bg-red-300_6d3e9",
  "xl:bg-red-400": "_xl:bg-red-400_6d3e9",
  "xl:bg-red-500": "_xl:bg-red-500_6d3e9",
  "xl:bg-red-600": "_xl:bg-red-600_6d3e9",
  "xl:bg-red-700": "_xl:bg-red-700_6d3e9",
  "xl:bg-red-800": "_xl:bg-red-800_6d3e9",
  "xl:bg-red-900": "_xl:bg-red-900_6d3e9",
  "xl:bg-orange-100": "_xl:bg-orange-100_6d3e9",
  "xl:bg-orange-200": "_xl:bg-orange-200_6d3e9",
  "xl:bg-orange-300": "_xl:bg-orange-300_6d3e9",
  "xl:bg-orange-400": "_xl:bg-orange-400_6d3e9",
  "xl:bg-orange-500": "_xl:bg-orange-500_6d3e9",
  "xl:bg-orange-600": "_xl:bg-orange-600_6d3e9",
  "xl:bg-orange-700": "_xl:bg-orange-700_6d3e9",
  "xl:bg-orange-800": "_xl:bg-orange-800_6d3e9",
  "xl:bg-orange-900": "_xl:bg-orange-900_6d3e9",
  "xl:bg-yellow-100": "_xl:bg-yellow-100_6d3e9",
  "xl:bg-yellow-200": "_xl:bg-yellow-200_6d3e9",
  "xl:bg-yellow-300": "_xl:bg-yellow-300_6d3e9",
  "xl:bg-yellow-400": "_xl:bg-yellow-400_6d3e9",
  "xl:bg-yellow-500": "_xl:bg-yellow-500_6d3e9",
  "xl:bg-yellow-600": "_xl:bg-yellow-600_6d3e9",
  "xl:bg-yellow-700": "_xl:bg-yellow-700_6d3e9",
  "xl:bg-yellow-800": "_xl:bg-yellow-800_6d3e9",
  "xl:bg-yellow-900": "_xl:bg-yellow-900_6d3e9",
  "xl:bg-green-100": "_xl:bg-green-100_6d3e9",
  "xl:bg-green-200": "_xl:bg-green-200_6d3e9",
  "xl:bg-green-300": "_xl:bg-green-300_6d3e9",
  "xl:bg-green-400": "_xl:bg-green-400_6d3e9",
  "xl:bg-green-500": "_xl:bg-green-500_6d3e9",
  "xl:bg-green-600": "_xl:bg-green-600_6d3e9",
  "xl:bg-green-700": "_xl:bg-green-700_6d3e9",
  "xl:bg-green-800": "_xl:bg-green-800_6d3e9",
  "xl:bg-green-900": "_xl:bg-green-900_6d3e9",
  "xl:bg-teal-100": "_xl:bg-teal-100_6d3e9",
  "xl:bg-teal-200": "_xl:bg-teal-200_6d3e9",
  "xl:bg-teal-300": "_xl:bg-teal-300_6d3e9",
  "xl:bg-teal-400": "_xl:bg-teal-400_6d3e9",
  "xl:bg-teal-500": "_xl:bg-teal-500_6d3e9",
  "xl:bg-teal-600": "_xl:bg-teal-600_6d3e9",
  "xl:bg-teal-700": "_xl:bg-teal-700_6d3e9",
  "xl:bg-teal-800": "_xl:bg-teal-800_6d3e9",
  "xl:bg-teal-900": "_xl:bg-teal-900_6d3e9",
  "xl:bg-blue-100": "_xl:bg-blue-100_6d3e9",
  "xl:bg-blue-200": "_xl:bg-blue-200_6d3e9",
  "xl:bg-blue-300": "_xl:bg-blue-300_6d3e9",
  "xl:bg-blue-400": "_xl:bg-blue-400_6d3e9",
  "xl:bg-blue-500": "_xl:bg-blue-500_6d3e9",
  "xl:bg-blue-600": "_xl:bg-blue-600_6d3e9",
  "xl:bg-blue-700": "_xl:bg-blue-700_6d3e9",
  "xl:bg-blue-800": "_xl:bg-blue-800_6d3e9",
  "xl:bg-blue-900": "_xl:bg-blue-900_6d3e9",
  "xl:bg-indigo-100": "_xl:bg-indigo-100_6d3e9",
  "xl:bg-indigo-200": "_xl:bg-indigo-200_6d3e9",
  "xl:bg-indigo-300": "_xl:bg-indigo-300_6d3e9",
  "xl:bg-indigo-400": "_xl:bg-indigo-400_6d3e9",
  "xl:bg-indigo-500": "_xl:bg-indigo-500_6d3e9",
  "xl:bg-indigo-600": "_xl:bg-indigo-600_6d3e9",
  "xl:bg-indigo-700": "_xl:bg-indigo-700_6d3e9",
  "xl:bg-indigo-800": "_xl:bg-indigo-800_6d3e9",
  "xl:bg-indigo-900": "_xl:bg-indigo-900_6d3e9",
  "xl:bg-purple-100": "_xl:bg-purple-100_6d3e9",
  "xl:bg-purple-200": "_xl:bg-purple-200_6d3e9",
  "xl:bg-purple-300": "_xl:bg-purple-300_6d3e9",
  "xl:bg-purple-400": "_xl:bg-purple-400_6d3e9",
  "xl:bg-purple-500": "_xl:bg-purple-500_6d3e9",
  "xl:bg-purple-600": "_xl:bg-purple-600_6d3e9",
  "xl:bg-purple-700": "_xl:bg-purple-700_6d3e9",
  "xl:bg-purple-800": "_xl:bg-purple-800_6d3e9",
  "xl:bg-purple-900": "_xl:bg-purple-900_6d3e9",
  "xl:bg-pink-100": "_xl:bg-pink-100_6d3e9",
  "xl:bg-pink-200": "_xl:bg-pink-200_6d3e9",
  "xl:bg-pink-300": "_xl:bg-pink-300_6d3e9",
  "xl:bg-pink-400": "_xl:bg-pink-400_6d3e9",
  "xl:bg-pink-500": "_xl:bg-pink-500_6d3e9",
  "xl:bg-pink-600": "_xl:bg-pink-600_6d3e9",
  "xl:bg-pink-700": "_xl:bg-pink-700_6d3e9",
  "xl:bg-pink-800": "_xl:bg-pink-800_6d3e9",
  "xl:bg-pink-900": "_xl:bg-pink-900_6d3e9",
  "xl:bg-primary-100": "_xl:bg-primary-100_6d3e9",
  "xl:bg-primary-200": "_xl:bg-primary-200_6d3e9",
  "xl:bg-primary-300": "_xl:bg-primary-300_6d3e9",
  "xl:bg-primary-400": "_xl:bg-primary-400_6d3e9",
  "xl:bg-primary-500": "_xl:bg-primary-500_6d3e9",
  "xl:bg-primary-600": "_xl:bg-primary-600_6d3e9",
  "xl:bg-primary-700": "_xl:bg-primary-700_6d3e9",
  "xl:bg-primary-800": "_xl:bg-primary-800_6d3e9",
  "xl:bg-primary-900": "_xl:bg-primary-900_6d3e9",
  "xl:bg-secondary-100": "_xl:bg-secondary-100_6d3e9",
  "xl:bg-secondary-200": "_xl:bg-secondary-200_6d3e9",
  "xl:bg-secondary-300": "_xl:bg-secondary-300_6d3e9",
  "xl:bg-secondary-400": "_xl:bg-secondary-400_6d3e9",
  "xl:bg-secondary-500": "_xl:bg-secondary-500_6d3e9",
  "xl:bg-secondary-600": "_xl:bg-secondary-600_6d3e9",
  "xl:bg-secondary-700": "_xl:bg-secondary-700_6d3e9",
  "xl:bg-secondary-800": "_xl:bg-secondary-800_6d3e9",
  "xl:bg-secondary-900": "_xl:bg-secondary-900_6d3e9",
  "xl:hover:bg-transparent": "_xl:hover:bg-transparent_6d3e9",
  "xl:hover:bg-black": "_xl:hover:bg-black_6d3e9",
  "xl:hover:bg-white": "_xl:hover:bg-white_6d3e9",
  "xl:hover:bg-gray-100": "_xl:hover:bg-gray-100_6d3e9",
  "xl:hover:bg-gray-200": "_xl:hover:bg-gray-200_6d3e9",
  "xl:hover:bg-gray-300": "_xl:hover:bg-gray-300_6d3e9",
  "xl:hover:bg-gray-400": "_xl:hover:bg-gray-400_6d3e9",
  "xl:hover:bg-gray-500": "_xl:hover:bg-gray-500_6d3e9",
  "xl:hover:bg-gray-600": "_xl:hover:bg-gray-600_6d3e9",
  "xl:hover:bg-gray-700": "_xl:hover:bg-gray-700_6d3e9",
  "xl:hover:bg-gray-800": "_xl:hover:bg-gray-800_6d3e9",
  "xl:hover:bg-gray-900": "_xl:hover:bg-gray-900_6d3e9",
  "xl:hover:bg-red-100": "_xl:hover:bg-red-100_6d3e9",
  "xl:hover:bg-red-200": "_xl:hover:bg-red-200_6d3e9",
  "xl:hover:bg-red-300": "_xl:hover:bg-red-300_6d3e9",
  "xl:hover:bg-red-400": "_xl:hover:bg-red-400_6d3e9",
  "xl:hover:bg-red-500": "_xl:hover:bg-red-500_6d3e9",
  "xl:hover:bg-red-600": "_xl:hover:bg-red-600_6d3e9",
  "xl:hover:bg-red-700": "_xl:hover:bg-red-700_6d3e9",
  "xl:hover:bg-red-800": "_xl:hover:bg-red-800_6d3e9",
  "xl:hover:bg-red-900": "_xl:hover:bg-red-900_6d3e9",
  "xl:hover:bg-orange-100": "_xl:hover:bg-orange-100_6d3e9",
  "xl:hover:bg-orange-200": "_xl:hover:bg-orange-200_6d3e9",
  "xl:hover:bg-orange-300": "_xl:hover:bg-orange-300_6d3e9",
  "xl:hover:bg-orange-400": "_xl:hover:bg-orange-400_6d3e9",
  "xl:hover:bg-orange-500": "_xl:hover:bg-orange-500_6d3e9",
  "xl:hover:bg-orange-600": "_xl:hover:bg-orange-600_6d3e9",
  "xl:hover:bg-orange-700": "_xl:hover:bg-orange-700_6d3e9",
  "xl:hover:bg-orange-800": "_xl:hover:bg-orange-800_6d3e9",
  "xl:hover:bg-orange-900": "_xl:hover:bg-orange-900_6d3e9",
  "xl:hover:bg-yellow-100": "_xl:hover:bg-yellow-100_6d3e9",
  "xl:hover:bg-yellow-200": "_xl:hover:bg-yellow-200_6d3e9",
  "xl:hover:bg-yellow-300": "_xl:hover:bg-yellow-300_6d3e9",
  "xl:hover:bg-yellow-400": "_xl:hover:bg-yellow-400_6d3e9",
  "xl:hover:bg-yellow-500": "_xl:hover:bg-yellow-500_6d3e9",
  "xl:hover:bg-yellow-600": "_xl:hover:bg-yellow-600_6d3e9",
  "xl:hover:bg-yellow-700": "_xl:hover:bg-yellow-700_6d3e9",
  "xl:hover:bg-yellow-800": "_xl:hover:bg-yellow-800_6d3e9",
  "xl:hover:bg-yellow-900": "_xl:hover:bg-yellow-900_6d3e9",
  "xl:hover:bg-green-100": "_xl:hover:bg-green-100_6d3e9",
  "xl:hover:bg-green-200": "_xl:hover:bg-green-200_6d3e9",
  "xl:hover:bg-green-300": "_xl:hover:bg-green-300_6d3e9",
  "xl:hover:bg-green-400": "_xl:hover:bg-green-400_6d3e9",
  "xl:hover:bg-green-500": "_xl:hover:bg-green-500_6d3e9",
  "xl:hover:bg-green-600": "_xl:hover:bg-green-600_6d3e9",
  "xl:hover:bg-green-700": "_xl:hover:bg-green-700_6d3e9",
  "xl:hover:bg-green-800": "_xl:hover:bg-green-800_6d3e9",
  "xl:hover:bg-green-900": "_xl:hover:bg-green-900_6d3e9",
  "xl:hover:bg-teal-100": "_xl:hover:bg-teal-100_6d3e9",
  "xl:hover:bg-teal-200": "_xl:hover:bg-teal-200_6d3e9",
  "xl:hover:bg-teal-300": "_xl:hover:bg-teal-300_6d3e9",
  "xl:hover:bg-teal-400": "_xl:hover:bg-teal-400_6d3e9",
  "xl:hover:bg-teal-500": "_xl:hover:bg-teal-500_6d3e9",
  "xl:hover:bg-teal-600": "_xl:hover:bg-teal-600_6d3e9",
  "xl:hover:bg-teal-700": "_xl:hover:bg-teal-700_6d3e9",
  "xl:hover:bg-teal-800": "_xl:hover:bg-teal-800_6d3e9",
  "xl:hover:bg-teal-900": "_xl:hover:bg-teal-900_6d3e9",
  "xl:hover:bg-blue-100": "_xl:hover:bg-blue-100_6d3e9",
  "xl:hover:bg-blue-200": "_xl:hover:bg-blue-200_6d3e9",
  "xl:hover:bg-blue-300": "_xl:hover:bg-blue-300_6d3e9",
  "xl:hover:bg-blue-400": "_xl:hover:bg-blue-400_6d3e9",
  "xl:hover:bg-blue-500": "_xl:hover:bg-blue-500_6d3e9",
  "xl:hover:bg-blue-600": "_xl:hover:bg-blue-600_6d3e9",
  "xl:hover:bg-blue-700": "_xl:hover:bg-blue-700_6d3e9",
  "xl:hover:bg-blue-800": "_xl:hover:bg-blue-800_6d3e9",
  "xl:hover:bg-blue-900": "_xl:hover:bg-blue-900_6d3e9",
  "xl:hover:bg-indigo-100": "_xl:hover:bg-indigo-100_6d3e9",
  "xl:hover:bg-indigo-200": "_xl:hover:bg-indigo-200_6d3e9",
  "xl:hover:bg-indigo-300": "_xl:hover:bg-indigo-300_6d3e9",
  "xl:hover:bg-indigo-400": "_xl:hover:bg-indigo-400_6d3e9",
  "xl:hover:bg-indigo-500": "_xl:hover:bg-indigo-500_6d3e9",
  "xl:hover:bg-indigo-600": "_xl:hover:bg-indigo-600_6d3e9",
  "xl:hover:bg-indigo-700": "_xl:hover:bg-indigo-700_6d3e9",
  "xl:hover:bg-indigo-800": "_xl:hover:bg-indigo-800_6d3e9",
  "xl:hover:bg-indigo-900": "_xl:hover:bg-indigo-900_6d3e9",
  "xl:hover:bg-purple-100": "_xl:hover:bg-purple-100_6d3e9",
  "xl:hover:bg-purple-200": "_xl:hover:bg-purple-200_6d3e9",
  "xl:hover:bg-purple-300": "_xl:hover:bg-purple-300_6d3e9",
  "xl:hover:bg-purple-400": "_xl:hover:bg-purple-400_6d3e9",
  "xl:hover:bg-purple-500": "_xl:hover:bg-purple-500_6d3e9",
  "xl:hover:bg-purple-600": "_xl:hover:bg-purple-600_6d3e9",
  "xl:hover:bg-purple-700": "_xl:hover:bg-purple-700_6d3e9",
  "xl:hover:bg-purple-800": "_xl:hover:bg-purple-800_6d3e9",
  "xl:hover:bg-purple-900": "_xl:hover:bg-purple-900_6d3e9",
  "xl:hover:bg-pink-100": "_xl:hover:bg-pink-100_6d3e9",
  "xl:hover:bg-pink-200": "_xl:hover:bg-pink-200_6d3e9",
  "xl:hover:bg-pink-300": "_xl:hover:bg-pink-300_6d3e9",
  "xl:hover:bg-pink-400": "_xl:hover:bg-pink-400_6d3e9",
  "xl:hover:bg-pink-500": "_xl:hover:bg-pink-500_6d3e9",
  "xl:hover:bg-pink-600": "_xl:hover:bg-pink-600_6d3e9",
  "xl:hover:bg-pink-700": "_xl:hover:bg-pink-700_6d3e9",
  "xl:hover:bg-pink-800": "_xl:hover:bg-pink-800_6d3e9",
  "xl:hover:bg-pink-900": "_xl:hover:bg-pink-900_6d3e9",
  "xl:hover:bg-primary-100": "_xl:hover:bg-primary-100_6d3e9",
  "xl:hover:bg-primary-200": "_xl:hover:bg-primary-200_6d3e9",
  "xl:hover:bg-primary-300": "_xl:hover:bg-primary-300_6d3e9",
  "xl:hover:bg-primary-400": "_xl:hover:bg-primary-400_6d3e9",
  "xl:hover:bg-primary-500": "_xl:hover:bg-primary-500_6d3e9",
  "xl:hover:bg-primary-600": "_xl:hover:bg-primary-600_6d3e9",
  "xl:hover:bg-primary-700": "_xl:hover:bg-primary-700_6d3e9",
  "xl:hover:bg-primary-800": "_xl:hover:bg-primary-800_6d3e9",
  "xl:hover:bg-primary-900": "_xl:hover:bg-primary-900_6d3e9",
  "xl:hover:bg-secondary-100": "_xl:hover:bg-secondary-100_6d3e9",
  "xl:hover:bg-secondary-200": "_xl:hover:bg-secondary-200_6d3e9",
  "xl:hover:bg-secondary-300": "_xl:hover:bg-secondary-300_6d3e9",
  "xl:hover:bg-secondary-400": "_xl:hover:bg-secondary-400_6d3e9",
  "xl:hover:bg-secondary-500": "_xl:hover:bg-secondary-500_6d3e9",
  "xl:hover:bg-secondary-600": "_xl:hover:bg-secondary-600_6d3e9",
  "xl:hover:bg-secondary-700": "_xl:hover:bg-secondary-700_6d3e9",
  "xl:hover:bg-secondary-800": "_xl:hover:bg-secondary-800_6d3e9",
  "xl:hover:bg-secondary-900": "_xl:hover:bg-secondary-900_6d3e9",
  "xl:focus:bg-transparent": "_xl:focus:bg-transparent_6d3e9",
  "xl:focus:bg-black": "_xl:focus:bg-black_6d3e9",
  "xl:focus:bg-white": "_xl:focus:bg-white_6d3e9",
  "xl:focus:bg-gray-100": "_xl:focus:bg-gray-100_6d3e9",
  "xl:focus:bg-gray-200": "_xl:focus:bg-gray-200_6d3e9",
  "xl:focus:bg-gray-300": "_xl:focus:bg-gray-300_6d3e9",
  "xl:focus:bg-gray-400": "_xl:focus:bg-gray-400_6d3e9",
  "xl:focus:bg-gray-500": "_xl:focus:bg-gray-500_6d3e9",
  "xl:focus:bg-gray-600": "_xl:focus:bg-gray-600_6d3e9",
  "xl:focus:bg-gray-700": "_xl:focus:bg-gray-700_6d3e9",
  "xl:focus:bg-gray-800": "_xl:focus:bg-gray-800_6d3e9",
  "xl:focus:bg-gray-900": "_xl:focus:bg-gray-900_6d3e9",
  "xl:focus:bg-red-100": "_xl:focus:bg-red-100_6d3e9",
  "xl:focus:bg-red-200": "_xl:focus:bg-red-200_6d3e9",
  "xl:focus:bg-red-300": "_xl:focus:bg-red-300_6d3e9",
  "xl:focus:bg-red-400": "_xl:focus:bg-red-400_6d3e9",
  "xl:focus:bg-red-500": "_xl:focus:bg-red-500_6d3e9",
  "xl:focus:bg-red-600": "_xl:focus:bg-red-600_6d3e9",
  "xl:focus:bg-red-700": "_xl:focus:bg-red-700_6d3e9",
  "xl:focus:bg-red-800": "_xl:focus:bg-red-800_6d3e9",
  "xl:focus:bg-red-900": "_xl:focus:bg-red-900_6d3e9",
  "xl:focus:bg-orange-100": "_xl:focus:bg-orange-100_6d3e9",
  "xl:focus:bg-orange-200": "_xl:focus:bg-orange-200_6d3e9",
  "xl:focus:bg-orange-300": "_xl:focus:bg-orange-300_6d3e9",
  "xl:focus:bg-orange-400": "_xl:focus:bg-orange-400_6d3e9",
  "xl:focus:bg-orange-500": "_xl:focus:bg-orange-500_6d3e9",
  "xl:focus:bg-orange-600": "_xl:focus:bg-orange-600_6d3e9",
  "xl:focus:bg-orange-700": "_xl:focus:bg-orange-700_6d3e9",
  "xl:focus:bg-orange-800": "_xl:focus:bg-orange-800_6d3e9",
  "xl:focus:bg-orange-900": "_xl:focus:bg-orange-900_6d3e9",
  "xl:focus:bg-yellow-100": "_xl:focus:bg-yellow-100_6d3e9",
  "xl:focus:bg-yellow-200": "_xl:focus:bg-yellow-200_6d3e9",
  "xl:focus:bg-yellow-300": "_xl:focus:bg-yellow-300_6d3e9",
  "xl:focus:bg-yellow-400": "_xl:focus:bg-yellow-400_6d3e9",
  "xl:focus:bg-yellow-500": "_xl:focus:bg-yellow-500_6d3e9",
  "xl:focus:bg-yellow-600": "_xl:focus:bg-yellow-600_6d3e9",
  "xl:focus:bg-yellow-700": "_xl:focus:bg-yellow-700_6d3e9",
  "xl:focus:bg-yellow-800": "_xl:focus:bg-yellow-800_6d3e9",
  "xl:focus:bg-yellow-900": "_xl:focus:bg-yellow-900_6d3e9",
  "xl:focus:bg-green-100": "_xl:focus:bg-green-100_6d3e9",
  "xl:focus:bg-green-200": "_xl:focus:bg-green-200_6d3e9",
  "xl:focus:bg-green-300": "_xl:focus:bg-green-300_6d3e9",
  "xl:focus:bg-green-400": "_xl:focus:bg-green-400_6d3e9",
  "xl:focus:bg-green-500": "_xl:focus:bg-green-500_6d3e9",
  "xl:focus:bg-green-600": "_xl:focus:bg-green-600_6d3e9",
  "xl:focus:bg-green-700": "_xl:focus:bg-green-700_6d3e9",
  "xl:focus:bg-green-800": "_xl:focus:bg-green-800_6d3e9",
  "xl:focus:bg-green-900": "_xl:focus:bg-green-900_6d3e9",
  "xl:focus:bg-teal-100": "_xl:focus:bg-teal-100_6d3e9",
  "xl:focus:bg-teal-200": "_xl:focus:bg-teal-200_6d3e9",
  "xl:focus:bg-teal-300": "_xl:focus:bg-teal-300_6d3e9",
  "xl:focus:bg-teal-400": "_xl:focus:bg-teal-400_6d3e9",
  "xl:focus:bg-teal-500": "_xl:focus:bg-teal-500_6d3e9",
  "xl:focus:bg-teal-600": "_xl:focus:bg-teal-600_6d3e9",
  "xl:focus:bg-teal-700": "_xl:focus:bg-teal-700_6d3e9",
  "xl:focus:bg-teal-800": "_xl:focus:bg-teal-800_6d3e9",
  "xl:focus:bg-teal-900": "_xl:focus:bg-teal-900_6d3e9",
  "xl:focus:bg-blue-100": "_xl:focus:bg-blue-100_6d3e9",
  "xl:focus:bg-blue-200": "_xl:focus:bg-blue-200_6d3e9",
  "xl:focus:bg-blue-300": "_xl:focus:bg-blue-300_6d3e9",
  "xl:focus:bg-blue-400": "_xl:focus:bg-blue-400_6d3e9",
  "xl:focus:bg-blue-500": "_xl:focus:bg-blue-500_6d3e9",
  "xl:focus:bg-blue-600": "_xl:focus:bg-blue-600_6d3e9",
  "xl:focus:bg-blue-700": "_xl:focus:bg-blue-700_6d3e9",
  "xl:focus:bg-blue-800": "_xl:focus:bg-blue-800_6d3e9",
  "xl:focus:bg-blue-900": "_xl:focus:bg-blue-900_6d3e9",
  "xl:focus:bg-indigo-100": "_xl:focus:bg-indigo-100_6d3e9",
  "xl:focus:bg-indigo-200": "_xl:focus:bg-indigo-200_6d3e9",
  "xl:focus:bg-indigo-300": "_xl:focus:bg-indigo-300_6d3e9",
  "xl:focus:bg-indigo-400": "_xl:focus:bg-indigo-400_6d3e9",
  "xl:focus:bg-indigo-500": "_xl:focus:bg-indigo-500_6d3e9",
  "xl:focus:bg-indigo-600": "_xl:focus:bg-indigo-600_6d3e9",
  "xl:focus:bg-indigo-700": "_xl:focus:bg-indigo-700_6d3e9",
  "xl:focus:bg-indigo-800": "_xl:focus:bg-indigo-800_6d3e9",
  "xl:focus:bg-indigo-900": "_xl:focus:bg-indigo-900_6d3e9",
  "xl:focus:bg-purple-100": "_xl:focus:bg-purple-100_6d3e9",
  "xl:focus:bg-purple-200": "_xl:focus:bg-purple-200_6d3e9",
  "xl:focus:bg-purple-300": "_xl:focus:bg-purple-300_6d3e9",
  "xl:focus:bg-purple-400": "_xl:focus:bg-purple-400_6d3e9",
  "xl:focus:bg-purple-500": "_xl:focus:bg-purple-500_6d3e9",
  "xl:focus:bg-purple-600": "_xl:focus:bg-purple-600_6d3e9",
  "xl:focus:bg-purple-700": "_xl:focus:bg-purple-700_6d3e9",
  "xl:focus:bg-purple-800": "_xl:focus:bg-purple-800_6d3e9",
  "xl:focus:bg-purple-900": "_xl:focus:bg-purple-900_6d3e9",
  "xl:focus:bg-pink-100": "_xl:focus:bg-pink-100_6d3e9",
  "xl:focus:bg-pink-200": "_xl:focus:bg-pink-200_6d3e9",
  "xl:focus:bg-pink-300": "_xl:focus:bg-pink-300_6d3e9",
  "xl:focus:bg-pink-400": "_xl:focus:bg-pink-400_6d3e9",
  "xl:focus:bg-pink-500": "_xl:focus:bg-pink-500_6d3e9",
  "xl:focus:bg-pink-600": "_xl:focus:bg-pink-600_6d3e9",
  "xl:focus:bg-pink-700": "_xl:focus:bg-pink-700_6d3e9",
  "xl:focus:bg-pink-800": "_xl:focus:bg-pink-800_6d3e9",
  "xl:focus:bg-pink-900": "_xl:focus:bg-pink-900_6d3e9",
  "xl:focus:bg-primary-100": "_xl:focus:bg-primary-100_6d3e9",
  "xl:focus:bg-primary-200": "_xl:focus:bg-primary-200_6d3e9",
  "xl:focus:bg-primary-300": "_xl:focus:bg-primary-300_6d3e9",
  "xl:focus:bg-primary-400": "_xl:focus:bg-primary-400_6d3e9",
  "xl:focus:bg-primary-500": "_xl:focus:bg-primary-500_6d3e9",
  "xl:focus:bg-primary-600": "_xl:focus:bg-primary-600_6d3e9",
  "xl:focus:bg-primary-700": "_xl:focus:bg-primary-700_6d3e9",
  "xl:focus:bg-primary-800": "_xl:focus:bg-primary-800_6d3e9",
  "xl:focus:bg-primary-900": "_xl:focus:bg-primary-900_6d3e9",
  "xl:focus:bg-secondary-100": "_xl:focus:bg-secondary-100_6d3e9",
  "xl:focus:bg-secondary-200": "_xl:focus:bg-secondary-200_6d3e9",
  "xl:focus:bg-secondary-300": "_xl:focus:bg-secondary-300_6d3e9",
  "xl:focus:bg-secondary-400": "_xl:focus:bg-secondary-400_6d3e9",
  "xl:focus:bg-secondary-500": "_xl:focus:bg-secondary-500_6d3e9",
  "xl:focus:bg-secondary-600": "_xl:focus:bg-secondary-600_6d3e9",
  "xl:focus:bg-secondary-700": "_xl:focus:bg-secondary-700_6d3e9",
  "xl:focus:bg-secondary-800": "_xl:focus:bg-secondary-800_6d3e9",
  "xl:focus:bg-secondary-900": "_xl:focus:bg-secondary-900_6d3e9",
  "xl:active:bg-transparent": "_xl:active:bg-transparent_6d3e9",
  "xl:active:bg-black": "_xl:active:bg-black_6d3e9",
  "xl:active:bg-white": "_xl:active:bg-white_6d3e9",
  "xl:active:bg-gray-100": "_xl:active:bg-gray-100_6d3e9",
  "xl:active:bg-gray-200": "_xl:active:bg-gray-200_6d3e9",
  "xl:active:bg-gray-300": "_xl:active:bg-gray-300_6d3e9",
  "xl:active:bg-gray-400": "_xl:active:bg-gray-400_6d3e9",
  "xl:active:bg-gray-500": "_xl:active:bg-gray-500_6d3e9",
  "xl:active:bg-gray-600": "_xl:active:bg-gray-600_6d3e9",
  "xl:active:bg-gray-700": "_xl:active:bg-gray-700_6d3e9",
  "xl:active:bg-gray-800": "_xl:active:bg-gray-800_6d3e9",
  "xl:active:bg-gray-900": "_xl:active:bg-gray-900_6d3e9",
  "xl:active:bg-red-100": "_xl:active:bg-red-100_6d3e9",
  "xl:active:bg-red-200": "_xl:active:bg-red-200_6d3e9",
  "xl:active:bg-red-300": "_xl:active:bg-red-300_6d3e9",
  "xl:active:bg-red-400": "_xl:active:bg-red-400_6d3e9",
  "xl:active:bg-red-500": "_xl:active:bg-red-500_6d3e9",
  "xl:active:bg-red-600": "_xl:active:bg-red-600_6d3e9",
  "xl:active:bg-red-700": "_xl:active:bg-red-700_6d3e9",
  "xl:active:bg-red-800": "_xl:active:bg-red-800_6d3e9",
  "xl:active:bg-red-900": "_xl:active:bg-red-900_6d3e9",
  "xl:active:bg-orange-100": "_xl:active:bg-orange-100_6d3e9",
  "xl:active:bg-orange-200": "_xl:active:bg-orange-200_6d3e9",
  "xl:active:bg-orange-300": "_xl:active:bg-orange-300_6d3e9",
  "xl:active:bg-orange-400": "_xl:active:bg-orange-400_6d3e9",
  "xl:active:bg-orange-500": "_xl:active:bg-orange-500_6d3e9",
  "xl:active:bg-orange-600": "_xl:active:bg-orange-600_6d3e9",
  "xl:active:bg-orange-700": "_xl:active:bg-orange-700_6d3e9",
  "xl:active:bg-orange-800": "_xl:active:bg-orange-800_6d3e9",
  "xl:active:bg-orange-900": "_xl:active:bg-orange-900_6d3e9",
  "xl:active:bg-yellow-100": "_xl:active:bg-yellow-100_6d3e9",
  "xl:active:bg-yellow-200": "_xl:active:bg-yellow-200_6d3e9",
  "xl:active:bg-yellow-300": "_xl:active:bg-yellow-300_6d3e9",
  "xl:active:bg-yellow-400": "_xl:active:bg-yellow-400_6d3e9",
  "xl:active:bg-yellow-500": "_xl:active:bg-yellow-500_6d3e9",
  "xl:active:bg-yellow-600": "_xl:active:bg-yellow-600_6d3e9",
  "xl:active:bg-yellow-700": "_xl:active:bg-yellow-700_6d3e9",
  "xl:active:bg-yellow-800": "_xl:active:bg-yellow-800_6d3e9",
  "xl:active:bg-yellow-900": "_xl:active:bg-yellow-900_6d3e9",
  "xl:active:bg-green-100": "_xl:active:bg-green-100_6d3e9",
  "xl:active:bg-green-200": "_xl:active:bg-green-200_6d3e9",
  "xl:active:bg-green-300": "_xl:active:bg-green-300_6d3e9",
  "xl:active:bg-green-400": "_xl:active:bg-green-400_6d3e9",
  "xl:active:bg-green-500": "_xl:active:bg-green-500_6d3e9",
  "xl:active:bg-green-600": "_xl:active:bg-green-600_6d3e9",
  "xl:active:bg-green-700": "_xl:active:bg-green-700_6d3e9",
  "xl:active:bg-green-800": "_xl:active:bg-green-800_6d3e9",
  "xl:active:bg-green-900": "_xl:active:bg-green-900_6d3e9",
  "xl:active:bg-teal-100": "_xl:active:bg-teal-100_6d3e9",
  "xl:active:bg-teal-200": "_xl:active:bg-teal-200_6d3e9",
  "xl:active:bg-teal-300": "_xl:active:bg-teal-300_6d3e9",
  "xl:active:bg-teal-400": "_xl:active:bg-teal-400_6d3e9",
  "xl:active:bg-teal-500": "_xl:active:bg-teal-500_6d3e9",
  "xl:active:bg-teal-600": "_xl:active:bg-teal-600_6d3e9",
  "xl:active:bg-teal-700": "_xl:active:bg-teal-700_6d3e9",
  "xl:active:bg-teal-800": "_xl:active:bg-teal-800_6d3e9",
  "xl:active:bg-teal-900": "_xl:active:bg-teal-900_6d3e9",
  "xl:active:bg-blue-100": "_xl:active:bg-blue-100_6d3e9",
  "xl:active:bg-blue-200": "_xl:active:bg-blue-200_6d3e9",
  "xl:active:bg-blue-300": "_xl:active:bg-blue-300_6d3e9",
  "xl:active:bg-blue-400": "_xl:active:bg-blue-400_6d3e9",
  "xl:active:bg-blue-500": "_xl:active:bg-blue-500_6d3e9",
  "xl:active:bg-blue-600": "_xl:active:bg-blue-600_6d3e9",
  "xl:active:bg-blue-700": "_xl:active:bg-blue-700_6d3e9",
  "xl:active:bg-blue-800": "_xl:active:bg-blue-800_6d3e9",
  "xl:active:bg-blue-900": "_xl:active:bg-blue-900_6d3e9",
  "xl:active:bg-indigo-100": "_xl:active:bg-indigo-100_6d3e9",
  "xl:active:bg-indigo-200": "_xl:active:bg-indigo-200_6d3e9",
  "xl:active:bg-indigo-300": "_xl:active:bg-indigo-300_6d3e9",
  "xl:active:bg-indigo-400": "_xl:active:bg-indigo-400_6d3e9",
  "xl:active:bg-indigo-500": "_xl:active:bg-indigo-500_6d3e9",
  "xl:active:bg-indigo-600": "_xl:active:bg-indigo-600_6d3e9",
  "xl:active:bg-indigo-700": "_xl:active:bg-indigo-700_6d3e9",
  "xl:active:bg-indigo-800": "_xl:active:bg-indigo-800_6d3e9",
  "xl:active:bg-indigo-900": "_xl:active:bg-indigo-900_6d3e9",
  "xl:active:bg-purple-100": "_xl:active:bg-purple-100_6d3e9",
  "xl:active:bg-purple-200": "_xl:active:bg-purple-200_6d3e9",
  "xl:active:bg-purple-300": "_xl:active:bg-purple-300_6d3e9",
  "xl:active:bg-purple-400": "_xl:active:bg-purple-400_6d3e9",
  "xl:active:bg-purple-500": "_xl:active:bg-purple-500_6d3e9",
  "xl:active:bg-purple-600": "_xl:active:bg-purple-600_6d3e9",
  "xl:active:bg-purple-700": "_xl:active:bg-purple-700_6d3e9",
  "xl:active:bg-purple-800": "_xl:active:bg-purple-800_6d3e9",
  "xl:active:bg-purple-900": "_xl:active:bg-purple-900_6d3e9",
  "xl:active:bg-pink-100": "_xl:active:bg-pink-100_6d3e9",
  "xl:active:bg-pink-200": "_xl:active:bg-pink-200_6d3e9",
  "xl:active:bg-pink-300": "_xl:active:bg-pink-300_6d3e9",
  "xl:active:bg-pink-400": "_xl:active:bg-pink-400_6d3e9",
  "xl:active:bg-pink-500": "_xl:active:bg-pink-500_6d3e9",
  "xl:active:bg-pink-600": "_xl:active:bg-pink-600_6d3e9",
  "xl:active:bg-pink-700": "_xl:active:bg-pink-700_6d3e9",
  "xl:active:bg-pink-800": "_xl:active:bg-pink-800_6d3e9",
  "xl:active:bg-pink-900": "_xl:active:bg-pink-900_6d3e9",
  "xl:active:bg-primary-100": "_xl:active:bg-primary-100_6d3e9",
  "xl:active:bg-primary-200": "_xl:active:bg-primary-200_6d3e9",
  "xl:active:bg-primary-300": "_xl:active:bg-primary-300_6d3e9",
  "xl:active:bg-primary-400": "_xl:active:bg-primary-400_6d3e9",
  "xl:active:bg-primary-500": "_xl:active:bg-primary-500_6d3e9",
  "xl:active:bg-primary-600": "_xl:active:bg-primary-600_6d3e9",
  "xl:active:bg-primary-700": "_xl:active:bg-primary-700_6d3e9",
  "xl:active:bg-primary-800": "_xl:active:bg-primary-800_6d3e9",
  "xl:active:bg-primary-900": "_xl:active:bg-primary-900_6d3e9",
  "xl:active:bg-secondary-100": "_xl:active:bg-secondary-100_6d3e9",
  "xl:active:bg-secondary-200": "_xl:active:bg-secondary-200_6d3e9",
  "xl:active:bg-secondary-300": "_xl:active:bg-secondary-300_6d3e9",
  "xl:active:bg-secondary-400": "_xl:active:bg-secondary-400_6d3e9",
  "xl:active:bg-secondary-500": "_xl:active:bg-secondary-500_6d3e9",
  "xl:active:bg-secondary-600": "_xl:active:bg-secondary-600_6d3e9",
  "xl:active:bg-secondary-700": "_xl:active:bg-secondary-700_6d3e9",
  "xl:active:bg-secondary-800": "_xl:active:bg-secondary-800_6d3e9",
  "xl:active:bg-secondary-900": "_xl:active:bg-secondary-900_6d3e9",
  "xl:bg-bottom": "_xl:bg-bottom_6d3e9",
  "xl:bg-center": "_xl:bg-center_6d3e9",
  "xl:bg-left": "_xl:bg-left_6d3e9",
  "xl:bg-left-bottom": "_xl:bg-left-bottom_6d3e9",
  "xl:bg-left-top": "_xl:bg-left-top_6d3e9",
  "xl:bg-right": "_xl:bg-right_6d3e9",
  "xl:bg-right-bottom": "_xl:bg-right-bottom_6d3e9",
  "xl:bg-right-top": "_xl:bg-right-top_6d3e9",
  "xl:bg-top": "_xl:bg-top_6d3e9",
  "xl:bg-repeat": "_xl:bg-repeat_6d3e9",
  "xl:bg-no-repeat": "_xl:bg-no-repeat_6d3e9",
  "xl:bg-repeat-x": "_xl:bg-repeat-x_6d3e9",
  "xl:bg-repeat-y": "_xl:bg-repeat-y_6d3e9",
  "xl:bg-repeat-round": "_xl:bg-repeat-round_6d3e9",
  "xl:bg-repeat-space": "_xl:bg-repeat-space_6d3e9",
  "xl:bg-auto": "_xl:bg-auto_6d3e9",
  "xl:bg-cover": "_xl:bg-cover_6d3e9",
  "xl:bg-contain": "_xl:bg-contain_6d3e9",
  "xl:border-collapse": "_xl:border-collapse_6d3e9",
  "xl:border-separate": "_xl:border-separate_6d3e9",
  "xl:border-transparent": "_xl:border-transparent_6d3e9",
  "xl:border-black": "_xl:border-black_6d3e9",
  "xl:border-white": "_xl:border-white_6d3e9",
  "xl:border-gray-100": "_xl:border-gray-100_6d3e9",
  "xl:border-gray-200": "_xl:border-gray-200_6d3e9",
  "xl:border-gray-300": "_xl:border-gray-300_6d3e9",
  "xl:border-gray-400": "_xl:border-gray-400_6d3e9",
  "xl:border-gray-500": "_xl:border-gray-500_6d3e9",
  "xl:border-gray-600": "_xl:border-gray-600_6d3e9",
  "xl:border-gray-700": "_xl:border-gray-700_6d3e9",
  "xl:border-gray-800": "_xl:border-gray-800_6d3e9",
  "xl:border-gray-900": "_xl:border-gray-900_6d3e9",
  "xl:border-red-100": "_xl:border-red-100_6d3e9",
  "xl:border-red-200": "_xl:border-red-200_6d3e9",
  "xl:border-red-300": "_xl:border-red-300_6d3e9",
  "xl:border-red-400": "_xl:border-red-400_6d3e9",
  "xl:border-red-500": "_xl:border-red-500_6d3e9",
  "xl:border-red-600": "_xl:border-red-600_6d3e9",
  "xl:border-red-700": "_xl:border-red-700_6d3e9",
  "xl:border-red-800": "_xl:border-red-800_6d3e9",
  "xl:border-red-900": "_xl:border-red-900_6d3e9",
  "xl:border-orange-100": "_xl:border-orange-100_6d3e9",
  "xl:border-orange-200": "_xl:border-orange-200_6d3e9",
  "xl:border-orange-300": "_xl:border-orange-300_6d3e9",
  "xl:border-orange-400": "_xl:border-orange-400_6d3e9",
  "xl:border-orange-500": "_xl:border-orange-500_6d3e9",
  "xl:border-orange-600": "_xl:border-orange-600_6d3e9",
  "xl:border-orange-700": "_xl:border-orange-700_6d3e9",
  "xl:border-orange-800": "_xl:border-orange-800_6d3e9",
  "xl:border-orange-900": "_xl:border-orange-900_6d3e9",
  "xl:border-yellow-100": "_xl:border-yellow-100_6d3e9",
  "xl:border-yellow-200": "_xl:border-yellow-200_6d3e9",
  "xl:border-yellow-300": "_xl:border-yellow-300_6d3e9",
  "xl:border-yellow-400": "_xl:border-yellow-400_6d3e9",
  "xl:border-yellow-500": "_xl:border-yellow-500_6d3e9",
  "xl:border-yellow-600": "_xl:border-yellow-600_6d3e9",
  "xl:border-yellow-700": "_xl:border-yellow-700_6d3e9",
  "xl:border-yellow-800": "_xl:border-yellow-800_6d3e9",
  "xl:border-yellow-900": "_xl:border-yellow-900_6d3e9",
  "xl:border-green-100": "_xl:border-green-100_6d3e9",
  "xl:border-green-200": "_xl:border-green-200_6d3e9",
  "xl:border-green-300": "_xl:border-green-300_6d3e9",
  "xl:border-green-400": "_xl:border-green-400_6d3e9",
  "xl:border-green-500": "_xl:border-green-500_6d3e9",
  "xl:border-green-600": "_xl:border-green-600_6d3e9",
  "xl:border-green-700": "_xl:border-green-700_6d3e9",
  "xl:border-green-800": "_xl:border-green-800_6d3e9",
  "xl:border-green-900": "_xl:border-green-900_6d3e9",
  "xl:border-teal-100": "_xl:border-teal-100_6d3e9",
  "xl:border-teal-200": "_xl:border-teal-200_6d3e9",
  "xl:border-teal-300": "_xl:border-teal-300_6d3e9",
  "xl:border-teal-400": "_xl:border-teal-400_6d3e9",
  "xl:border-teal-500": "_xl:border-teal-500_6d3e9",
  "xl:border-teal-600": "_xl:border-teal-600_6d3e9",
  "xl:border-teal-700": "_xl:border-teal-700_6d3e9",
  "xl:border-teal-800": "_xl:border-teal-800_6d3e9",
  "xl:border-teal-900": "_xl:border-teal-900_6d3e9",
  "xl:border-blue-100": "_xl:border-blue-100_6d3e9",
  "xl:border-blue-200": "_xl:border-blue-200_6d3e9",
  "xl:border-blue-300": "_xl:border-blue-300_6d3e9",
  "xl:border-blue-400": "_xl:border-blue-400_6d3e9",
  "xl:border-blue-500": "_xl:border-blue-500_6d3e9",
  "xl:border-blue-600": "_xl:border-blue-600_6d3e9",
  "xl:border-blue-700": "_xl:border-blue-700_6d3e9",
  "xl:border-blue-800": "_xl:border-blue-800_6d3e9",
  "xl:border-blue-900": "_xl:border-blue-900_6d3e9",
  "xl:border-indigo-100": "_xl:border-indigo-100_6d3e9",
  "xl:border-indigo-200": "_xl:border-indigo-200_6d3e9",
  "xl:border-indigo-300": "_xl:border-indigo-300_6d3e9",
  "xl:border-indigo-400": "_xl:border-indigo-400_6d3e9",
  "xl:border-indigo-500": "_xl:border-indigo-500_6d3e9",
  "xl:border-indigo-600": "_xl:border-indigo-600_6d3e9",
  "xl:border-indigo-700": "_xl:border-indigo-700_6d3e9",
  "xl:border-indigo-800": "_xl:border-indigo-800_6d3e9",
  "xl:border-indigo-900": "_xl:border-indigo-900_6d3e9",
  "xl:border-purple-100": "_xl:border-purple-100_6d3e9",
  "xl:border-purple-200": "_xl:border-purple-200_6d3e9",
  "xl:border-purple-300": "_xl:border-purple-300_6d3e9",
  "xl:border-purple-400": "_xl:border-purple-400_6d3e9",
  "xl:border-purple-500": "_xl:border-purple-500_6d3e9",
  "xl:border-purple-600": "_xl:border-purple-600_6d3e9",
  "xl:border-purple-700": "_xl:border-purple-700_6d3e9",
  "xl:border-purple-800": "_xl:border-purple-800_6d3e9",
  "xl:border-purple-900": "_xl:border-purple-900_6d3e9",
  "xl:border-pink-100": "_xl:border-pink-100_6d3e9",
  "xl:border-pink-200": "_xl:border-pink-200_6d3e9",
  "xl:border-pink-300": "_xl:border-pink-300_6d3e9",
  "xl:border-pink-400": "_xl:border-pink-400_6d3e9",
  "xl:border-pink-500": "_xl:border-pink-500_6d3e9",
  "xl:border-pink-600": "_xl:border-pink-600_6d3e9",
  "xl:border-pink-700": "_xl:border-pink-700_6d3e9",
  "xl:border-pink-800": "_xl:border-pink-800_6d3e9",
  "xl:border-pink-900": "_xl:border-pink-900_6d3e9",
  "xl:border-primary-100": "_xl:border-primary-100_6d3e9",
  "xl:border-primary-200": "_xl:border-primary-200_6d3e9",
  "xl:border-primary-300": "_xl:border-primary-300_6d3e9",
  "xl:border-primary-400": "_xl:border-primary-400_6d3e9",
  "xl:border-primary-500": "_xl:border-primary-500_6d3e9",
  "xl:border-primary-600": "_xl:border-primary-600_6d3e9",
  "xl:border-primary-700": "_xl:border-primary-700_6d3e9",
  "xl:border-primary-800": "_xl:border-primary-800_6d3e9",
  "xl:border-primary-900": "_xl:border-primary-900_6d3e9",
  "xl:border-secondary-100": "_xl:border-secondary-100_6d3e9",
  "xl:border-secondary-200": "_xl:border-secondary-200_6d3e9",
  "xl:border-secondary-300": "_xl:border-secondary-300_6d3e9",
  "xl:border-secondary-400": "_xl:border-secondary-400_6d3e9",
  "xl:border-secondary-500": "_xl:border-secondary-500_6d3e9",
  "xl:border-secondary-600": "_xl:border-secondary-600_6d3e9",
  "xl:border-secondary-700": "_xl:border-secondary-700_6d3e9",
  "xl:border-secondary-800": "_xl:border-secondary-800_6d3e9",
  "xl:border-secondary-900": "_xl:border-secondary-900_6d3e9",
  "xl:hover:border-transparent": "_xl:hover:border-transparent_6d3e9",
  "xl:hover:border-black": "_xl:hover:border-black_6d3e9",
  "xl:hover:border-white": "_xl:hover:border-white_6d3e9",
  "xl:hover:border-gray-100": "_xl:hover:border-gray-100_6d3e9",
  "xl:hover:border-gray-200": "_xl:hover:border-gray-200_6d3e9",
  "xl:hover:border-gray-300": "_xl:hover:border-gray-300_6d3e9",
  "xl:hover:border-gray-400": "_xl:hover:border-gray-400_6d3e9",
  "xl:hover:border-gray-500": "_xl:hover:border-gray-500_6d3e9",
  "xl:hover:border-gray-600": "_xl:hover:border-gray-600_6d3e9",
  "xl:hover:border-gray-700": "_xl:hover:border-gray-700_6d3e9",
  "xl:hover:border-gray-800": "_xl:hover:border-gray-800_6d3e9",
  "xl:hover:border-gray-900": "_xl:hover:border-gray-900_6d3e9",
  "xl:hover:border-red-100": "_xl:hover:border-red-100_6d3e9",
  "xl:hover:border-red-200": "_xl:hover:border-red-200_6d3e9",
  "xl:hover:border-red-300": "_xl:hover:border-red-300_6d3e9",
  "xl:hover:border-red-400": "_xl:hover:border-red-400_6d3e9",
  "xl:hover:border-red-500": "_xl:hover:border-red-500_6d3e9",
  "xl:hover:border-red-600": "_xl:hover:border-red-600_6d3e9",
  "xl:hover:border-red-700": "_xl:hover:border-red-700_6d3e9",
  "xl:hover:border-red-800": "_xl:hover:border-red-800_6d3e9",
  "xl:hover:border-red-900": "_xl:hover:border-red-900_6d3e9",
  "xl:hover:border-orange-100": "_xl:hover:border-orange-100_6d3e9",
  "xl:hover:border-orange-200": "_xl:hover:border-orange-200_6d3e9",
  "xl:hover:border-orange-300": "_xl:hover:border-orange-300_6d3e9",
  "xl:hover:border-orange-400": "_xl:hover:border-orange-400_6d3e9",
  "xl:hover:border-orange-500": "_xl:hover:border-orange-500_6d3e9",
  "xl:hover:border-orange-600": "_xl:hover:border-orange-600_6d3e9",
  "xl:hover:border-orange-700": "_xl:hover:border-orange-700_6d3e9",
  "xl:hover:border-orange-800": "_xl:hover:border-orange-800_6d3e9",
  "xl:hover:border-orange-900": "_xl:hover:border-orange-900_6d3e9",
  "xl:hover:border-yellow-100": "_xl:hover:border-yellow-100_6d3e9",
  "xl:hover:border-yellow-200": "_xl:hover:border-yellow-200_6d3e9",
  "xl:hover:border-yellow-300": "_xl:hover:border-yellow-300_6d3e9",
  "xl:hover:border-yellow-400": "_xl:hover:border-yellow-400_6d3e9",
  "xl:hover:border-yellow-500": "_xl:hover:border-yellow-500_6d3e9",
  "xl:hover:border-yellow-600": "_xl:hover:border-yellow-600_6d3e9",
  "xl:hover:border-yellow-700": "_xl:hover:border-yellow-700_6d3e9",
  "xl:hover:border-yellow-800": "_xl:hover:border-yellow-800_6d3e9",
  "xl:hover:border-yellow-900": "_xl:hover:border-yellow-900_6d3e9",
  "xl:hover:border-green-100": "_xl:hover:border-green-100_6d3e9",
  "xl:hover:border-green-200": "_xl:hover:border-green-200_6d3e9",
  "xl:hover:border-green-300": "_xl:hover:border-green-300_6d3e9",
  "xl:hover:border-green-400": "_xl:hover:border-green-400_6d3e9",
  "xl:hover:border-green-500": "_xl:hover:border-green-500_6d3e9",
  "xl:hover:border-green-600": "_xl:hover:border-green-600_6d3e9",
  "xl:hover:border-green-700": "_xl:hover:border-green-700_6d3e9",
  "xl:hover:border-green-800": "_xl:hover:border-green-800_6d3e9",
  "xl:hover:border-green-900": "_xl:hover:border-green-900_6d3e9",
  "xl:hover:border-teal-100": "_xl:hover:border-teal-100_6d3e9",
  "xl:hover:border-teal-200": "_xl:hover:border-teal-200_6d3e9",
  "xl:hover:border-teal-300": "_xl:hover:border-teal-300_6d3e9",
  "xl:hover:border-teal-400": "_xl:hover:border-teal-400_6d3e9",
  "xl:hover:border-teal-500": "_xl:hover:border-teal-500_6d3e9",
  "xl:hover:border-teal-600": "_xl:hover:border-teal-600_6d3e9",
  "xl:hover:border-teal-700": "_xl:hover:border-teal-700_6d3e9",
  "xl:hover:border-teal-800": "_xl:hover:border-teal-800_6d3e9",
  "xl:hover:border-teal-900": "_xl:hover:border-teal-900_6d3e9",
  "xl:hover:border-blue-100": "_xl:hover:border-blue-100_6d3e9",
  "xl:hover:border-blue-200": "_xl:hover:border-blue-200_6d3e9",
  "xl:hover:border-blue-300": "_xl:hover:border-blue-300_6d3e9",
  "xl:hover:border-blue-400": "_xl:hover:border-blue-400_6d3e9",
  "xl:hover:border-blue-500": "_xl:hover:border-blue-500_6d3e9",
  "xl:hover:border-blue-600": "_xl:hover:border-blue-600_6d3e9",
  "xl:hover:border-blue-700": "_xl:hover:border-blue-700_6d3e9",
  "xl:hover:border-blue-800": "_xl:hover:border-blue-800_6d3e9",
  "xl:hover:border-blue-900": "_xl:hover:border-blue-900_6d3e9",
  "xl:hover:border-indigo-100": "_xl:hover:border-indigo-100_6d3e9",
  "xl:hover:border-indigo-200": "_xl:hover:border-indigo-200_6d3e9",
  "xl:hover:border-indigo-300": "_xl:hover:border-indigo-300_6d3e9",
  "xl:hover:border-indigo-400": "_xl:hover:border-indigo-400_6d3e9",
  "xl:hover:border-indigo-500": "_xl:hover:border-indigo-500_6d3e9",
  "xl:hover:border-indigo-600": "_xl:hover:border-indigo-600_6d3e9",
  "xl:hover:border-indigo-700": "_xl:hover:border-indigo-700_6d3e9",
  "xl:hover:border-indigo-800": "_xl:hover:border-indigo-800_6d3e9",
  "xl:hover:border-indigo-900": "_xl:hover:border-indigo-900_6d3e9",
  "xl:hover:border-purple-100": "_xl:hover:border-purple-100_6d3e9",
  "xl:hover:border-purple-200": "_xl:hover:border-purple-200_6d3e9",
  "xl:hover:border-purple-300": "_xl:hover:border-purple-300_6d3e9",
  "xl:hover:border-purple-400": "_xl:hover:border-purple-400_6d3e9",
  "xl:hover:border-purple-500": "_xl:hover:border-purple-500_6d3e9",
  "xl:hover:border-purple-600": "_xl:hover:border-purple-600_6d3e9",
  "xl:hover:border-purple-700": "_xl:hover:border-purple-700_6d3e9",
  "xl:hover:border-purple-800": "_xl:hover:border-purple-800_6d3e9",
  "xl:hover:border-purple-900": "_xl:hover:border-purple-900_6d3e9",
  "xl:hover:border-pink-100": "_xl:hover:border-pink-100_6d3e9",
  "xl:hover:border-pink-200": "_xl:hover:border-pink-200_6d3e9",
  "xl:hover:border-pink-300": "_xl:hover:border-pink-300_6d3e9",
  "xl:hover:border-pink-400": "_xl:hover:border-pink-400_6d3e9",
  "xl:hover:border-pink-500": "_xl:hover:border-pink-500_6d3e9",
  "xl:hover:border-pink-600": "_xl:hover:border-pink-600_6d3e9",
  "xl:hover:border-pink-700": "_xl:hover:border-pink-700_6d3e9",
  "xl:hover:border-pink-800": "_xl:hover:border-pink-800_6d3e9",
  "xl:hover:border-pink-900": "_xl:hover:border-pink-900_6d3e9",
  "xl:hover:border-primary-100": "_xl:hover:border-primary-100_6d3e9",
  "xl:hover:border-primary-200": "_xl:hover:border-primary-200_6d3e9",
  "xl:hover:border-primary-300": "_xl:hover:border-primary-300_6d3e9",
  "xl:hover:border-primary-400": "_xl:hover:border-primary-400_6d3e9",
  "xl:hover:border-primary-500": "_xl:hover:border-primary-500_6d3e9",
  "xl:hover:border-primary-600": "_xl:hover:border-primary-600_6d3e9",
  "xl:hover:border-primary-700": "_xl:hover:border-primary-700_6d3e9",
  "xl:hover:border-primary-800": "_xl:hover:border-primary-800_6d3e9",
  "xl:hover:border-primary-900": "_xl:hover:border-primary-900_6d3e9",
  "xl:hover:border-secondary-100": "_xl:hover:border-secondary-100_6d3e9",
  "xl:hover:border-secondary-200": "_xl:hover:border-secondary-200_6d3e9",
  "xl:hover:border-secondary-300": "_xl:hover:border-secondary-300_6d3e9",
  "xl:hover:border-secondary-400": "_xl:hover:border-secondary-400_6d3e9",
  "xl:hover:border-secondary-500": "_xl:hover:border-secondary-500_6d3e9",
  "xl:hover:border-secondary-600": "_xl:hover:border-secondary-600_6d3e9",
  "xl:hover:border-secondary-700": "_xl:hover:border-secondary-700_6d3e9",
  "xl:hover:border-secondary-800": "_xl:hover:border-secondary-800_6d3e9",
  "xl:hover:border-secondary-900": "_xl:hover:border-secondary-900_6d3e9",
  "xl:focus:border-transparent": "_xl:focus:border-transparent_6d3e9",
  "xl:focus:border-black": "_xl:focus:border-black_6d3e9",
  "xl:focus:border-white": "_xl:focus:border-white_6d3e9",
  "xl:focus:border-gray-100": "_xl:focus:border-gray-100_6d3e9",
  "xl:focus:border-gray-200": "_xl:focus:border-gray-200_6d3e9",
  "xl:focus:border-gray-300": "_xl:focus:border-gray-300_6d3e9",
  "xl:focus:border-gray-400": "_xl:focus:border-gray-400_6d3e9",
  "xl:focus:border-gray-500": "_xl:focus:border-gray-500_6d3e9",
  "xl:focus:border-gray-600": "_xl:focus:border-gray-600_6d3e9",
  "xl:focus:border-gray-700": "_xl:focus:border-gray-700_6d3e9",
  "xl:focus:border-gray-800": "_xl:focus:border-gray-800_6d3e9",
  "xl:focus:border-gray-900": "_xl:focus:border-gray-900_6d3e9",
  "xl:focus:border-red-100": "_xl:focus:border-red-100_6d3e9",
  "xl:focus:border-red-200": "_xl:focus:border-red-200_6d3e9",
  "xl:focus:border-red-300": "_xl:focus:border-red-300_6d3e9",
  "xl:focus:border-red-400": "_xl:focus:border-red-400_6d3e9",
  "xl:focus:border-red-500": "_xl:focus:border-red-500_6d3e9",
  "xl:focus:border-red-600": "_xl:focus:border-red-600_6d3e9",
  "xl:focus:border-red-700": "_xl:focus:border-red-700_6d3e9",
  "xl:focus:border-red-800": "_xl:focus:border-red-800_6d3e9",
  "xl:focus:border-red-900": "_xl:focus:border-red-900_6d3e9",
  "xl:focus:border-orange-100": "_xl:focus:border-orange-100_6d3e9",
  "xl:focus:border-orange-200": "_xl:focus:border-orange-200_6d3e9",
  "xl:focus:border-orange-300": "_xl:focus:border-orange-300_6d3e9",
  "xl:focus:border-orange-400": "_xl:focus:border-orange-400_6d3e9",
  "xl:focus:border-orange-500": "_xl:focus:border-orange-500_6d3e9",
  "xl:focus:border-orange-600": "_xl:focus:border-orange-600_6d3e9",
  "xl:focus:border-orange-700": "_xl:focus:border-orange-700_6d3e9",
  "xl:focus:border-orange-800": "_xl:focus:border-orange-800_6d3e9",
  "xl:focus:border-orange-900": "_xl:focus:border-orange-900_6d3e9",
  "xl:focus:border-yellow-100": "_xl:focus:border-yellow-100_6d3e9",
  "xl:focus:border-yellow-200": "_xl:focus:border-yellow-200_6d3e9",
  "xl:focus:border-yellow-300": "_xl:focus:border-yellow-300_6d3e9",
  "xl:focus:border-yellow-400": "_xl:focus:border-yellow-400_6d3e9",
  "xl:focus:border-yellow-500": "_xl:focus:border-yellow-500_6d3e9",
  "xl:focus:border-yellow-600": "_xl:focus:border-yellow-600_6d3e9",
  "xl:focus:border-yellow-700": "_xl:focus:border-yellow-700_6d3e9",
  "xl:focus:border-yellow-800": "_xl:focus:border-yellow-800_6d3e9",
  "xl:focus:border-yellow-900": "_xl:focus:border-yellow-900_6d3e9",
  "xl:focus:border-green-100": "_xl:focus:border-green-100_6d3e9",
  "xl:focus:border-green-200": "_xl:focus:border-green-200_6d3e9",
  "xl:focus:border-green-300": "_xl:focus:border-green-300_6d3e9",
  "xl:focus:border-green-400": "_xl:focus:border-green-400_6d3e9",
  "xl:focus:border-green-500": "_xl:focus:border-green-500_6d3e9",
  "xl:focus:border-green-600": "_xl:focus:border-green-600_6d3e9",
  "xl:focus:border-green-700": "_xl:focus:border-green-700_6d3e9",
  "xl:focus:border-green-800": "_xl:focus:border-green-800_6d3e9",
  "xl:focus:border-green-900": "_xl:focus:border-green-900_6d3e9",
  "xl:focus:border-teal-100": "_xl:focus:border-teal-100_6d3e9",
  "xl:focus:border-teal-200": "_xl:focus:border-teal-200_6d3e9",
  "xl:focus:border-teal-300": "_xl:focus:border-teal-300_6d3e9",
  "xl:focus:border-teal-400": "_xl:focus:border-teal-400_6d3e9",
  "xl:focus:border-teal-500": "_xl:focus:border-teal-500_6d3e9",
  "xl:focus:border-teal-600": "_xl:focus:border-teal-600_6d3e9",
  "xl:focus:border-teal-700": "_xl:focus:border-teal-700_6d3e9",
  "xl:focus:border-teal-800": "_xl:focus:border-teal-800_6d3e9",
  "xl:focus:border-teal-900": "_xl:focus:border-teal-900_6d3e9",
  "xl:focus:border-blue-100": "_xl:focus:border-blue-100_6d3e9",
  "xl:focus:border-blue-200": "_xl:focus:border-blue-200_6d3e9",
  "xl:focus:border-blue-300": "_xl:focus:border-blue-300_6d3e9",
  "xl:focus:border-blue-400": "_xl:focus:border-blue-400_6d3e9",
  "xl:focus:border-blue-500": "_xl:focus:border-blue-500_6d3e9",
  "xl:focus:border-blue-600": "_xl:focus:border-blue-600_6d3e9",
  "xl:focus:border-blue-700": "_xl:focus:border-blue-700_6d3e9",
  "xl:focus:border-blue-800": "_xl:focus:border-blue-800_6d3e9",
  "xl:focus:border-blue-900": "_xl:focus:border-blue-900_6d3e9",
  "xl:focus:border-indigo-100": "_xl:focus:border-indigo-100_6d3e9",
  "xl:focus:border-indigo-200": "_xl:focus:border-indigo-200_6d3e9",
  "xl:focus:border-indigo-300": "_xl:focus:border-indigo-300_6d3e9",
  "xl:focus:border-indigo-400": "_xl:focus:border-indigo-400_6d3e9",
  "xl:focus:border-indigo-500": "_xl:focus:border-indigo-500_6d3e9",
  "xl:focus:border-indigo-600": "_xl:focus:border-indigo-600_6d3e9",
  "xl:focus:border-indigo-700": "_xl:focus:border-indigo-700_6d3e9",
  "xl:focus:border-indigo-800": "_xl:focus:border-indigo-800_6d3e9",
  "xl:focus:border-indigo-900": "_xl:focus:border-indigo-900_6d3e9",
  "xl:focus:border-purple-100": "_xl:focus:border-purple-100_6d3e9",
  "xl:focus:border-purple-200": "_xl:focus:border-purple-200_6d3e9",
  "xl:focus:border-purple-300": "_xl:focus:border-purple-300_6d3e9",
  "xl:focus:border-purple-400": "_xl:focus:border-purple-400_6d3e9",
  "xl:focus:border-purple-500": "_xl:focus:border-purple-500_6d3e9",
  "xl:focus:border-purple-600": "_xl:focus:border-purple-600_6d3e9",
  "xl:focus:border-purple-700": "_xl:focus:border-purple-700_6d3e9",
  "xl:focus:border-purple-800": "_xl:focus:border-purple-800_6d3e9",
  "xl:focus:border-purple-900": "_xl:focus:border-purple-900_6d3e9",
  "xl:focus:border-pink-100": "_xl:focus:border-pink-100_6d3e9",
  "xl:focus:border-pink-200": "_xl:focus:border-pink-200_6d3e9",
  "xl:focus:border-pink-300": "_xl:focus:border-pink-300_6d3e9",
  "xl:focus:border-pink-400": "_xl:focus:border-pink-400_6d3e9",
  "xl:focus:border-pink-500": "_xl:focus:border-pink-500_6d3e9",
  "xl:focus:border-pink-600": "_xl:focus:border-pink-600_6d3e9",
  "xl:focus:border-pink-700": "_xl:focus:border-pink-700_6d3e9",
  "xl:focus:border-pink-800": "_xl:focus:border-pink-800_6d3e9",
  "xl:focus:border-pink-900": "_xl:focus:border-pink-900_6d3e9",
  "xl:focus:border-primary-100": "_xl:focus:border-primary-100_6d3e9",
  "xl:focus:border-primary-200": "_xl:focus:border-primary-200_6d3e9",
  "xl:focus:border-primary-300": "_xl:focus:border-primary-300_6d3e9",
  "xl:focus:border-primary-400": "_xl:focus:border-primary-400_6d3e9",
  "xl:focus:border-primary-500": "_xl:focus:border-primary-500_6d3e9",
  "xl:focus:border-primary-600": "_xl:focus:border-primary-600_6d3e9",
  "xl:focus:border-primary-700": "_xl:focus:border-primary-700_6d3e9",
  "xl:focus:border-primary-800": "_xl:focus:border-primary-800_6d3e9",
  "xl:focus:border-primary-900": "_xl:focus:border-primary-900_6d3e9",
  "xl:focus:border-secondary-100": "_xl:focus:border-secondary-100_6d3e9",
  "xl:focus:border-secondary-200": "_xl:focus:border-secondary-200_6d3e9",
  "xl:focus:border-secondary-300": "_xl:focus:border-secondary-300_6d3e9",
  "xl:focus:border-secondary-400": "_xl:focus:border-secondary-400_6d3e9",
  "xl:focus:border-secondary-500": "_xl:focus:border-secondary-500_6d3e9",
  "xl:focus:border-secondary-600": "_xl:focus:border-secondary-600_6d3e9",
  "xl:focus:border-secondary-700": "_xl:focus:border-secondary-700_6d3e9",
  "xl:focus:border-secondary-800": "_xl:focus:border-secondary-800_6d3e9",
  "xl:focus:border-secondary-900": "_xl:focus:border-secondary-900_6d3e9",
  "xl:rounded-none": "_xl:rounded-none_6d3e9",
  "xl:rounded-sm": "_xl:rounded-sm_6d3e9",
  "xl:rounded": "_xl:rounded_6d3e9",
  "xl:rounded-lg": "_xl:rounded-lg_6d3e9",
  "xl:rounded-full": "_xl:rounded-full_6d3e9",
  "xl:rounded-t-none": "_xl:rounded-t-none_6d3e9",
  "xl:rounded-r-none": "_xl:rounded-r-none_6d3e9",
  "xl:rounded-b-none": "_xl:rounded-b-none_6d3e9",
  "xl:rounded-l-none": "_xl:rounded-l-none_6d3e9",
  "xl:rounded-t-sm": "_xl:rounded-t-sm_6d3e9",
  "xl:rounded-r-sm": "_xl:rounded-r-sm_6d3e9",
  "xl:rounded-b-sm": "_xl:rounded-b-sm_6d3e9",
  "xl:rounded-l-sm": "_xl:rounded-l-sm_6d3e9",
  "xl:rounded-t": "_xl:rounded-t_6d3e9",
  "xl:rounded-r": "_xl:rounded-r_6d3e9",
  "xl:rounded-b": "_xl:rounded-b_6d3e9",
  "xl:rounded-l": "_xl:rounded-l_6d3e9",
  "xl:rounded-t-lg": "_xl:rounded-t-lg_6d3e9",
  "xl:rounded-r-lg": "_xl:rounded-r-lg_6d3e9",
  "xl:rounded-b-lg": "_xl:rounded-b-lg_6d3e9",
  "xl:rounded-l-lg": "_xl:rounded-l-lg_6d3e9",
  "xl:rounded-t-full": "_xl:rounded-t-full_6d3e9",
  "xl:rounded-r-full": "_xl:rounded-r-full_6d3e9",
  "xl:rounded-b-full": "_xl:rounded-b-full_6d3e9",
  "xl:rounded-l-full": "_xl:rounded-l-full_6d3e9",
  "xl:rounded-tl-none": "_xl:rounded-tl-none_6d3e9",
  "xl:rounded-tr-none": "_xl:rounded-tr-none_6d3e9",
  "xl:rounded-br-none": "_xl:rounded-br-none_6d3e9",
  "xl:rounded-bl-none": "_xl:rounded-bl-none_6d3e9",
  "xl:rounded-tl-sm": "_xl:rounded-tl-sm_6d3e9",
  "xl:rounded-tr-sm": "_xl:rounded-tr-sm_6d3e9",
  "xl:rounded-br-sm": "_xl:rounded-br-sm_6d3e9",
  "xl:rounded-bl-sm": "_xl:rounded-bl-sm_6d3e9",
  "xl:rounded-tl": "_xl:rounded-tl_6d3e9",
  "xl:rounded-tr": "_xl:rounded-tr_6d3e9",
  "xl:rounded-br": "_xl:rounded-br_6d3e9",
  "xl:rounded-bl": "_xl:rounded-bl_6d3e9",
  "xl:rounded-tl-lg": "_xl:rounded-tl-lg_6d3e9",
  "xl:rounded-tr-lg": "_xl:rounded-tr-lg_6d3e9",
  "xl:rounded-br-lg": "_xl:rounded-br-lg_6d3e9",
  "xl:rounded-bl-lg": "_xl:rounded-bl-lg_6d3e9",
  "xl:rounded-tl-full": "_xl:rounded-tl-full_6d3e9",
  "xl:rounded-tr-full": "_xl:rounded-tr-full_6d3e9",
  "xl:rounded-br-full": "_xl:rounded-br-full_6d3e9",
  "xl:rounded-bl-full": "_xl:rounded-bl-full_6d3e9",
  "xl:border-solid": "_xl:border-solid_6d3e9",
  "xl:border-dashed": "_xl:border-dashed_6d3e9",
  "xl:border-dotted": "_xl:border-dotted_6d3e9",
  "xl:border-double": "_xl:border-double_6d3e9",
  "xl:border-none": "_xl:border-none_6d3e9",
  "xl:border-0": "_xl:border-0_6d3e9",
  "xl:border-2": "_xl:border-2_6d3e9",
  "xl:border-4": "_xl:border-4_6d3e9",
  "xl:border-8": "_xl:border-8_6d3e9",
  "xl:border": "_xl:border_6d3e9",
  "xl:border-t-0": "_xl:border-t-0_6d3e9",
  "xl:border-r-0": "_xl:border-r-0_6d3e9",
  "xl:border-b-0": "_xl:border-b-0_6d3e9",
  "xl:border-l-0": "_xl:border-l-0_6d3e9",
  "xl:border-t-2": "_xl:border-t-2_6d3e9",
  "xl:border-r-2": "_xl:border-r-2_6d3e9",
  "xl:border-b-2": "_xl:border-b-2_6d3e9",
  "xl:border-l-2": "_xl:border-l-2_6d3e9",
  "xl:border-t-4": "_xl:border-t-4_6d3e9",
  "xl:border-r-4": "_xl:border-r-4_6d3e9",
  "xl:border-b-4": "_xl:border-b-4_6d3e9",
  "xl:border-l-4": "_xl:border-l-4_6d3e9",
  "xl:border-t-8": "_xl:border-t-8_6d3e9",
  "xl:border-r-8": "_xl:border-r-8_6d3e9",
  "xl:border-b-8": "_xl:border-b-8_6d3e9",
  "xl:border-l-8": "_xl:border-l-8_6d3e9",
  "xl:border-t": "_xl:border-t_6d3e9",
  "xl:border-r": "_xl:border-r_6d3e9",
  "xl:border-b": "_xl:border-b_6d3e9",
  "xl:border-l": "_xl:border-l_6d3e9",
  "xl:first:border-0": "_xl:first:border-0_6d3e9",
  "xl:first:border-2": "_xl:first:border-2_6d3e9",
  "xl:first:border-4": "_xl:first:border-4_6d3e9",
  "xl:first:border-8": "_xl:first:border-8_6d3e9",
  "xl:first:border": "_xl:first:border_6d3e9",
  "xl:first:border-t-0": "_xl:first:border-t-0_6d3e9",
  "xl:first:border-r-0": "_xl:first:border-r-0_6d3e9",
  "xl:first:border-b-0": "_xl:first:border-b-0_6d3e9",
  "xl:first:border-l-0": "_xl:first:border-l-0_6d3e9",
  "xl:first:border-t-2": "_xl:first:border-t-2_6d3e9",
  "xl:first:border-r-2": "_xl:first:border-r-2_6d3e9",
  "xl:first:border-b-2": "_xl:first:border-b-2_6d3e9",
  "xl:first:border-l-2": "_xl:first:border-l-2_6d3e9",
  "xl:first:border-t-4": "_xl:first:border-t-4_6d3e9",
  "xl:first:border-r-4": "_xl:first:border-r-4_6d3e9",
  "xl:first:border-b-4": "_xl:first:border-b-4_6d3e9",
  "xl:first:border-l-4": "_xl:first:border-l-4_6d3e9",
  "xl:first:border-t-8": "_xl:first:border-t-8_6d3e9",
  "xl:first:border-r-8": "_xl:first:border-r-8_6d3e9",
  "xl:first:border-b-8": "_xl:first:border-b-8_6d3e9",
  "xl:first:border-l-8": "_xl:first:border-l-8_6d3e9",
  "xl:first:border-t": "_xl:first:border-t_6d3e9",
  "xl:first:border-r": "_xl:first:border-r_6d3e9",
  "xl:first:border-b": "_xl:first:border-b_6d3e9",
  "xl:first:border-l": "_xl:first:border-l_6d3e9",
  "xl:hover:border-0": "_xl:hover:border-0_6d3e9",
  "xl:hover:border-2": "_xl:hover:border-2_6d3e9",
  "xl:hover:border-4": "_xl:hover:border-4_6d3e9",
  "xl:hover:border-8": "_xl:hover:border-8_6d3e9",
  "xl:hover:border": "_xl:hover:border_6d3e9",
  "xl:hover:border-t-0": "_xl:hover:border-t-0_6d3e9",
  "xl:hover:border-r-0": "_xl:hover:border-r-0_6d3e9",
  "xl:hover:border-b-0": "_xl:hover:border-b-0_6d3e9",
  "xl:hover:border-l-0": "_xl:hover:border-l-0_6d3e9",
  "xl:hover:border-t-2": "_xl:hover:border-t-2_6d3e9",
  "xl:hover:border-r-2": "_xl:hover:border-r-2_6d3e9",
  "xl:hover:border-b-2": "_xl:hover:border-b-2_6d3e9",
  "xl:hover:border-l-2": "_xl:hover:border-l-2_6d3e9",
  "xl:hover:border-t-4": "_xl:hover:border-t-4_6d3e9",
  "xl:hover:border-r-4": "_xl:hover:border-r-4_6d3e9",
  "xl:hover:border-b-4": "_xl:hover:border-b-4_6d3e9",
  "xl:hover:border-l-4": "_xl:hover:border-l-4_6d3e9",
  "xl:hover:border-t-8": "_xl:hover:border-t-8_6d3e9",
  "xl:hover:border-r-8": "_xl:hover:border-r-8_6d3e9",
  "xl:hover:border-b-8": "_xl:hover:border-b-8_6d3e9",
  "xl:hover:border-l-8": "_xl:hover:border-l-8_6d3e9",
  "xl:hover:border-t": "_xl:hover:border-t_6d3e9",
  "xl:hover:border-r": "_xl:hover:border-r_6d3e9",
  "xl:hover:border-b": "_xl:hover:border-b_6d3e9",
  "xl:hover:border-l": "_xl:hover:border-l_6d3e9",
  "xl:focus:border-0": "_xl:focus:border-0_6d3e9",
  "xl:focus:border-2": "_xl:focus:border-2_6d3e9",
  "xl:focus:border-4": "_xl:focus:border-4_6d3e9",
  "xl:focus:border-8": "_xl:focus:border-8_6d3e9",
  "xl:focus:border": "_xl:focus:border_6d3e9",
  "xl:focus:border-t-0": "_xl:focus:border-t-0_6d3e9",
  "xl:focus:border-r-0": "_xl:focus:border-r-0_6d3e9",
  "xl:focus:border-b-0": "_xl:focus:border-b-0_6d3e9",
  "xl:focus:border-l-0": "_xl:focus:border-l-0_6d3e9",
  "xl:focus:border-t-2": "_xl:focus:border-t-2_6d3e9",
  "xl:focus:border-r-2": "_xl:focus:border-r-2_6d3e9",
  "xl:focus:border-b-2": "_xl:focus:border-b-2_6d3e9",
  "xl:focus:border-l-2": "_xl:focus:border-l-2_6d3e9",
  "xl:focus:border-t-4": "_xl:focus:border-t-4_6d3e9",
  "xl:focus:border-r-4": "_xl:focus:border-r-4_6d3e9",
  "xl:focus:border-b-4": "_xl:focus:border-b-4_6d3e9",
  "xl:focus:border-l-4": "_xl:focus:border-l-4_6d3e9",
  "xl:focus:border-t-8": "_xl:focus:border-t-8_6d3e9",
  "xl:focus:border-r-8": "_xl:focus:border-r-8_6d3e9",
  "xl:focus:border-b-8": "_xl:focus:border-b-8_6d3e9",
  "xl:focus:border-l-8": "_xl:focus:border-l-8_6d3e9",
  "xl:focus:border-t": "_xl:focus:border-t_6d3e9",
  "xl:focus:border-r": "_xl:focus:border-r_6d3e9",
  "xl:focus:border-b": "_xl:focus:border-b_6d3e9",
  "xl:focus:border-l": "_xl:focus:border-l_6d3e9",
  "xl:cursor-auto": "_xl:cursor-auto_6d3e9",
  "xl:cursor-default": "_xl:cursor-default_6d3e9",
  "xl:cursor-pointer": "_xl:cursor-pointer_6d3e9",
  "xl:cursor-wait": "_xl:cursor-wait_6d3e9",
  "xl:cursor-text": "_xl:cursor-text_6d3e9",
  "xl:cursor-move": "_xl:cursor-move_6d3e9",
  "xl:cursor-not-allowed": "_xl:cursor-not-allowed_6d3e9",
  "xl:block": "_xl:block_6d3e9",
  "xl:inline-block": "_xl:inline-block_6d3e9",
  "xl:inline": "_xl:inline_6d3e9",
  "xl:flex": "_xl:flex_6d3e9",
  "xl:inline-flex": "_xl:inline-flex_6d3e9",
  "xl:table": "_xl:table_6d3e9",
  "xl:table-row": "_xl:table-row_6d3e9",
  "xl:table-cell": "_xl:table-cell_6d3e9",
  "xl:hidden": "_xl:hidden_6d3e9",
  "xl:flex-row": "_xl:flex-row_6d3e9",
  "xl:flex-row-reverse": "_xl:flex-row-reverse_6d3e9",
  "xl:flex-col": "_xl:flex-col_6d3e9",
  "xl:flex-col-reverse": "_xl:flex-col-reverse_6d3e9",
  "xl:flex-wrap": "_xl:flex-wrap_6d3e9",
  "xl:flex-wrap-reverse": "_xl:flex-wrap-reverse_6d3e9",
  "xl:flex-no-wrap": "_xl:flex-no-wrap_6d3e9",
  "xl:items-start": "_xl:items-start_6d3e9",
  "xl:items-end": "_xl:items-end_6d3e9",
  "xl:items-center": "_xl:items-center_6d3e9",
  "xl:items-baseline": "_xl:items-baseline_6d3e9",
  "xl:items-stretch": "_xl:items-stretch_6d3e9",
  "xl:self-auto": "_xl:self-auto_6d3e9",
  "xl:self-start": "_xl:self-start_6d3e9",
  "xl:self-end": "_xl:self-end_6d3e9",
  "xl:self-center": "_xl:self-center_6d3e9",
  "xl:self-stretch": "_xl:self-stretch_6d3e9",
  "xl:justify-start": "_xl:justify-start_6d3e9",
  "xl:justify-end": "_xl:justify-end_6d3e9",
  "xl:justify-center": "_xl:justify-center_6d3e9",
  "xl:justify-between": "_xl:justify-between_6d3e9",
  "xl:justify-around": "_xl:justify-around_6d3e9",
  "xl:content-center": "_xl:content-center_6d3e9",
  "xl:content-start": "_xl:content-start_6d3e9",
  "xl:content-end": "_xl:content-end_6d3e9",
  "xl:content-between": "_xl:content-between_6d3e9",
  "xl:content-around": "_xl:content-around_6d3e9",
  "xl:flex-1": "_xl:flex-1_6d3e9",
  "xl:flex-auto": "_xl:flex-auto_6d3e9",
  "xl:flex-initial": "_xl:flex-initial_6d3e9",
  "xl:flex-none": "_xl:flex-none_6d3e9",
  "xl:flex-grow-0": "_xl:flex-grow-0_6d3e9",
  "xl:flex-grow": "_xl:flex-grow_6d3e9",
  "xl:flex-shrink-0": "_xl:flex-shrink-0_6d3e9",
  "xl:flex-shrink": "_xl:flex-shrink_6d3e9",
  "xl:order-1": "_xl:order-1_6d3e9",
  "xl:order-2": "_xl:order-2_6d3e9",
  "xl:order-3": "_xl:order-3_6d3e9",
  "xl:order-4": "_xl:order-4_6d3e9",
  "xl:order-5": "_xl:order-5_6d3e9",
  "xl:order-6": "_xl:order-6_6d3e9",
  "xl:order-7": "_xl:order-7_6d3e9",
  "xl:order-8": "_xl:order-8_6d3e9",
  "xl:order-9": "_xl:order-9_6d3e9",
  "xl:order-10": "_xl:order-10_6d3e9",
  "xl:order-11": "_xl:order-11_6d3e9",
  "xl:order-12": "_xl:order-12_6d3e9",
  "xl:order-first": "_xl:order-first_6d3e9",
  "xl:order-last": "_xl:order-last_6d3e9",
  "xl:order-none": "_xl:order-none_6d3e9",
  "xl:float-right": "_xl:float-right_6d3e9",
  "xl:float-left": "_xl:float-left_6d3e9",
  "xl:float-none": "_xl:float-none_6d3e9",
  "xl:clearfix": "_xl:clearfix_6d3e9",
  "xl:font-sans": "_xl:font-sans_6d3e9",
  "xl:font-serif": "_xl:font-serif_6d3e9",
  "xl:font-mono": "_xl:font-mono_6d3e9",
  "xl:font-hairline": "_xl:font-hairline_6d3e9",
  "xl:font-thin": "_xl:font-thin_6d3e9",
  "xl:font-light": "_xl:font-light_6d3e9",
  "xl:font-normal": "_xl:font-normal_6d3e9",
  "xl:font-medium": "_xl:font-medium_6d3e9",
  "xl:font-semibold": "_xl:font-semibold_6d3e9",
  "xl:font-bold": "_xl:font-bold_6d3e9",
  "xl:font-extrabold": "_xl:font-extrabold_6d3e9",
  "xl:font-black": "_xl:font-black_6d3e9",
  "xl:hover:font-hairline": "_xl:hover:font-hairline_6d3e9",
  "xl:hover:font-thin": "_xl:hover:font-thin_6d3e9",
  "xl:hover:font-light": "_xl:hover:font-light_6d3e9",
  "xl:hover:font-normal": "_xl:hover:font-normal_6d3e9",
  "xl:hover:font-medium": "_xl:hover:font-medium_6d3e9",
  "xl:hover:font-semibold": "_xl:hover:font-semibold_6d3e9",
  "xl:hover:font-bold": "_xl:hover:font-bold_6d3e9",
  "xl:hover:font-extrabold": "_xl:hover:font-extrabold_6d3e9",
  "xl:hover:font-black": "_xl:hover:font-black_6d3e9",
  "xl:focus:font-hairline": "_xl:focus:font-hairline_6d3e9",
  "xl:focus:font-thin": "_xl:focus:font-thin_6d3e9",
  "xl:focus:font-light": "_xl:focus:font-light_6d3e9",
  "xl:focus:font-normal": "_xl:focus:font-normal_6d3e9",
  "xl:focus:font-medium": "_xl:focus:font-medium_6d3e9",
  "xl:focus:font-semibold": "_xl:focus:font-semibold_6d3e9",
  "xl:focus:font-bold": "_xl:focus:font-bold_6d3e9",
  "xl:focus:font-extrabold": "_xl:focus:font-extrabold_6d3e9",
  "xl:focus:font-black": "_xl:focus:font-black_6d3e9",
  "xl:h-0": "_xl:h-0_6d3e9",
  "xl:h-1": "_xl:h-1_6d3e9",
  "xl:h-2": "_xl:h-2_6d3e9",
  "xl:h-3": "_xl:h-3_6d3e9",
  "xl:h-4": "_xl:h-4_6d3e9",
  "xl:h-5": "_xl:h-5_6d3e9",
  "xl:h-6": "_xl:h-6_6d3e9",
  "xl:h-8": "_xl:h-8_6d3e9",
  "xl:h-10": "_xl:h-10_6d3e9",
  "xl:h-12": "_xl:h-12_6d3e9",
  "xl:h-16": "_xl:h-16_6d3e9",
  "xl:h-20": "_xl:h-20_6d3e9",
  "xl:h-24": "_xl:h-24_6d3e9",
  "xl:h-32": "_xl:h-32_6d3e9",
  "xl:h-40": "_xl:h-40_6d3e9",
  "xl:h-48": "_xl:h-48_6d3e9",
  "xl:h-56": "_xl:h-56_6d3e9",
  "xl:h-64": "_xl:h-64_6d3e9",
  "xl:h-auto": "_xl:h-auto_6d3e9",
  "xl:h-px": "_xl:h-px_6d3e9",
  "xl:h-full": "_xl:h-full_6d3e9",
  "xl:h-screen": "_xl:h-screen_6d3e9",
  "xl:leading-none": "_xl:leading-none_6d3e9",
  "xl:leading-tight": "_xl:leading-tight_6d3e9",
  "xl:leading-snug": "_xl:leading-snug_6d3e9",
  "xl:leading-normal": "_xl:leading-normal_6d3e9",
  "xl:leading-relaxed": "_xl:leading-relaxed_6d3e9",
  "xl:leading-loose": "_xl:leading-loose_6d3e9",
  "xl:list-inside": "_xl:list-inside_6d3e9",
  "xl:list-outside": "_xl:list-outside_6d3e9",
  "xl:list-none": "_xl:list-none_6d3e9",
  "xl:list-disc": "_xl:list-disc_6d3e9",
  "xl:list-decimal": "_xl:list-decimal_6d3e9",
  "xl:m-0": "_xl:m-0_6d3e9",
  "xl:m-1": "_xl:m-1_6d3e9",
  "xl:m-2": "_xl:m-2_6d3e9",
  "xl:m-3": "_xl:m-3_6d3e9",
  "xl:m-4": "_xl:m-4_6d3e9",
  "xl:m-5": "_xl:m-5_6d3e9",
  "xl:m-6": "_xl:m-6_6d3e9",
  "xl:m-8": "_xl:m-8_6d3e9",
  "xl:m-10": "_xl:m-10_6d3e9",
  "xl:m-12": "_xl:m-12_6d3e9",
  "xl:m-16": "_xl:m-16_6d3e9",
  "xl:m-20": "_xl:m-20_6d3e9",
  "xl:m-24": "_xl:m-24_6d3e9",
  "xl:m-32": "_xl:m-32_6d3e9",
  "xl:m-40": "_xl:m-40_6d3e9",
  "xl:m-48": "_xl:m-48_6d3e9",
  "xl:m-56": "_xl:m-56_6d3e9",
  "xl:m-64": "_xl:m-64_6d3e9",
  "xl:m-auto": "_xl:m-auto_6d3e9",
  "xl:m-px": "_xl:m-px_6d3e9",
  "xl:-m-1": "_xl:-m-1_6d3e9",
  "xl:-m-2": "_xl:-m-2_6d3e9",
  "xl:-m-3": "_xl:-m-3_6d3e9",
  "xl:-m-4": "_xl:-m-4_6d3e9",
  "xl:-m-5": "_xl:-m-5_6d3e9",
  "xl:-m-6": "_xl:-m-6_6d3e9",
  "xl:-m-8": "_xl:-m-8_6d3e9",
  "xl:-m-10": "_xl:-m-10_6d3e9",
  "xl:-m-12": "_xl:-m-12_6d3e9",
  "xl:-m-16": "_xl:-m-16_6d3e9",
  "xl:-m-20": "_xl:-m-20_6d3e9",
  "xl:-m-24": "_xl:-m-24_6d3e9",
  "xl:-m-32": "_xl:-m-32_6d3e9",
  "xl:-m-40": "_xl:-m-40_6d3e9",
  "xl:-m-48": "_xl:-m-48_6d3e9",
  "xl:-m-56": "_xl:-m-56_6d3e9",
  "xl:-m-64": "_xl:-m-64_6d3e9",
  "xl:-m-px": "_xl:-m-px_6d3e9",
  "xl:my-0": "_xl:my-0_6d3e9",
  "xl:mx-0": "_xl:mx-0_6d3e9",
  "xl:my-1": "_xl:my-1_6d3e9",
  "xl:mx-1": "_xl:mx-1_6d3e9",
  "xl:my-2": "_xl:my-2_6d3e9",
  "xl:mx-2": "_xl:mx-2_6d3e9",
  "xl:my-3": "_xl:my-3_6d3e9",
  "xl:mx-3": "_xl:mx-3_6d3e9",
  "xl:my-4": "_xl:my-4_6d3e9",
  "xl:mx-4": "_xl:mx-4_6d3e9",
  "xl:my-5": "_xl:my-5_6d3e9",
  "xl:mx-5": "_xl:mx-5_6d3e9",
  "xl:my-6": "_xl:my-6_6d3e9",
  "xl:mx-6": "_xl:mx-6_6d3e9",
  "xl:my-8": "_xl:my-8_6d3e9",
  "xl:mx-8": "_xl:mx-8_6d3e9",
  "xl:my-10": "_xl:my-10_6d3e9",
  "xl:mx-10": "_xl:mx-10_6d3e9",
  "xl:my-12": "_xl:my-12_6d3e9",
  "xl:mx-12": "_xl:mx-12_6d3e9",
  "xl:my-16": "_xl:my-16_6d3e9",
  "xl:mx-16": "_xl:mx-16_6d3e9",
  "xl:my-20": "_xl:my-20_6d3e9",
  "xl:mx-20": "_xl:mx-20_6d3e9",
  "xl:my-24": "_xl:my-24_6d3e9",
  "xl:mx-24": "_xl:mx-24_6d3e9",
  "xl:my-32": "_xl:my-32_6d3e9",
  "xl:mx-32": "_xl:mx-32_6d3e9",
  "xl:my-40": "_xl:my-40_6d3e9",
  "xl:mx-40": "_xl:mx-40_6d3e9",
  "xl:my-48": "_xl:my-48_6d3e9",
  "xl:mx-48": "_xl:mx-48_6d3e9",
  "xl:my-56": "_xl:my-56_6d3e9",
  "xl:mx-56": "_xl:mx-56_6d3e9",
  "xl:my-64": "_xl:my-64_6d3e9",
  "xl:mx-64": "_xl:mx-64_6d3e9",
  "xl:my-auto": "_xl:my-auto_6d3e9",
  "xl:mx-auto": "_xl:mx-auto_6d3e9",
  "xl:my-px": "_xl:my-px_6d3e9",
  "xl:mx-px": "_xl:mx-px_6d3e9",
  "xl:-my-1": "_xl:-my-1_6d3e9",
  "xl:-mx-1": "_xl:-mx-1_6d3e9",
  "xl:-my-2": "_xl:-my-2_6d3e9",
  "xl:-mx-2": "_xl:-mx-2_6d3e9",
  "xl:-my-3": "_xl:-my-3_6d3e9",
  "xl:-mx-3": "_xl:-mx-3_6d3e9",
  "xl:-my-4": "_xl:-my-4_6d3e9",
  "xl:-mx-4": "_xl:-mx-4_6d3e9",
  "xl:-my-5": "_xl:-my-5_6d3e9",
  "xl:-mx-5": "_xl:-mx-5_6d3e9",
  "xl:-my-6": "_xl:-my-6_6d3e9",
  "xl:-mx-6": "_xl:-mx-6_6d3e9",
  "xl:-my-8": "_xl:-my-8_6d3e9",
  "xl:-mx-8": "_xl:-mx-8_6d3e9",
  "xl:-my-10": "_xl:-my-10_6d3e9",
  "xl:-mx-10": "_xl:-mx-10_6d3e9",
  "xl:-my-12": "_xl:-my-12_6d3e9",
  "xl:-mx-12": "_xl:-mx-12_6d3e9",
  "xl:-my-16": "_xl:-my-16_6d3e9",
  "xl:-mx-16": "_xl:-mx-16_6d3e9",
  "xl:-my-20": "_xl:-my-20_6d3e9",
  "xl:-mx-20": "_xl:-mx-20_6d3e9",
  "xl:-my-24": "_xl:-my-24_6d3e9",
  "xl:-mx-24": "_xl:-mx-24_6d3e9",
  "xl:-my-32": "_xl:-my-32_6d3e9",
  "xl:-mx-32": "_xl:-mx-32_6d3e9",
  "xl:-my-40": "_xl:-my-40_6d3e9",
  "xl:-mx-40": "_xl:-mx-40_6d3e9",
  "xl:-my-48": "_xl:-my-48_6d3e9",
  "xl:-mx-48": "_xl:-mx-48_6d3e9",
  "xl:-my-56": "_xl:-my-56_6d3e9",
  "xl:-mx-56": "_xl:-mx-56_6d3e9",
  "xl:-my-64": "_xl:-my-64_6d3e9",
  "xl:-mx-64": "_xl:-mx-64_6d3e9",
  "xl:-my-px": "_xl:-my-px_6d3e9",
  "xl:-mx-px": "_xl:-mx-px_6d3e9",
  "xl:mt-0": "_xl:mt-0_6d3e9",
  "xl:mr-0": "_xl:mr-0_6d3e9",
  "xl:mb-0": "_xl:mb-0_6d3e9",
  "xl:ml-0": "_xl:ml-0_6d3e9",
  "xl:mt-1": "_xl:mt-1_6d3e9",
  "xl:mr-1": "_xl:mr-1_6d3e9",
  "xl:mb-1": "_xl:mb-1_6d3e9",
  "xl:ml-1": "_xl:ml-1_6d3e9",
  "xl:mt-2": "_xl:mt-2_6d3e9",
  "xl:mr-2": "_xl:mr-2_6d3e9",
  "xl:mb-2": "_xl:mb-2_6d3e9",
  "xl:ml-2": "_xl:ml-2_6d3e9",
  "xl:mt-3": "_xl:mt-3_6d3e9",
  "xl:mr-3": "_xl:mr-3_6d3e9",
  "xl:mb-3": "_xl:mb-3_6d3e9",
  "xl:ml-3": "_xl:ml-3_6d3e9",
  "xl:mt-4": "_xl:mt-4_6d3e9",
  "xl:mr-4": "_xl:mr-4_6d3e9",
  "xl:mb-4": "_xl:mb-4_6d3e9",
  "xl:ml-4": "_xl:ml-4_6d3e9",
  "xl:mt-5": "_xl:mt-5_6d3e9",
  "xl:mr-5": "_xl:mr-5_6d3e9",
  "xl:mb-5": "_xl:mb-5_6d3e9",
  "xl:ml-5": "_xl:ml-5_6d3e9",
  "xl:mt-6": "_xl:mt-6_6d3e9",
  "xl:mr-6": "_xl:mr-6_6d3e9",
  "xl:mb-6": "_xl:mb-6_6d3e9",
  "xl:ml-6": "_xl:ml-6_6d3e9",
  "xl:mt-8": "_xl:mt-8_6d3e9",
  "xl:mr-8": "_xl:mr-8_6d3e9",
  "xl:mb-8": "_xl:mb-8_6d3e9",
  "xl:ml-8": "_xl:ml-8_6d3e9",
  "xl:mt-10": "_xl:mt-10_6d3e9",
  "xl:mr-10": "_xl:mr-10_6d3e9",
  "xl:mb-10": "_xl:mb-10_6d3e9",
  "xl:ml-10": "_xl:ml-10_6d3e9",
  "xl:mt-12": "_xl:mt-12_6d3e9",
  "xl:mr-12": "_xl:mr-12_6d3e9",
  "xl:mb-12": "_xl:mb-12_6d3e9",
  "xl:ml-12": "_xl:ml-12_6d3e9",
  "xl:mt-16": "_xl:mt-16_6d3e9",
  "xl:mr-16": "_xl:mr-16_6d3e9",
  "xl:mb-16": "_xl:mb-16_6d3e9",
  "xl:ml-16": "_xl:ml-16_6d3e9",
  "xl:mt-20": "_xl:mt-20_6d3e9",
  "xl:mr-20": "_xl:mr-20_6d3e9",
  "xl:mb-20": "_xl:mb-20_6d3e9",
  "xl:ml-20": "_xl:ml-20_6d3e9",
  "xl:mt-24": "_xl:mt-24_6d3e9",
  "xl:mr-24": "_xl:mr-24_6d3e9",
  "xl:mb-24": "_xl:mb-24_6d3e9",
  "xl:ml-24": "_xl:ml-24_6d3e9",
  "xl:mt-32": "_xl:mt-32_6d3e9",
  "xl:mr-32": "_xl:mr-32_6d3e9",
  "xl:mb-32": "_xl:mb-32_6d3e9",
  "xl:ml-32": "_xl:ml-32_6d3e9",
  "xl:mt-40": "_xl:mt-40_6d3e9",
  "xl:mr-40": "_xl:mr-40_6d3e9",
  "xl:mb-40": "_xl:mb-40_6d3e9",
  "xl:ml-40": "_xl:ml-40_6d3e9",
  "xl:mt-48": "_xl:mt-48_6d3e9",
  "xl:mr-48": "_xl:mr-48_6d3e9",
  "xl:mb-48": "_xl:mb-48_6d3e9",
  "xl:ml-48": "_xl:ml-48_6d3e9",
  "xl:mt-56": "_xl:mt-56_6d3e9",
  "xl:mr-56": "_xl:mr-56_6d3e9",
  "xl:mb-56": "_xl:mb-56_6d3e9",
  "xl:ml-56": "_xl:ml-56_6d3e9",
  "xl:mt-64": "_xl:mt-64_6d3e9",
  "xl:mr-64": "_xl:mr-64_6d3e9",
  "xl:mb-64": "_xl:mb-64_6d3e9",
  "xl:ml-64": "_xl:ml-64_6d3e9",
  "xl:mt-auto": "_xl:mt-auto_6d3e9",
  "xl:mr-auto": "_xl:mr-auto_6d3e9",
  "xl:mb-auto": "_xl:mb-auto_6d3e9",
  "xl:ml-auto": "_xl:ml-auto_6d3e9",
  "xl:mt-px": "_xl:mt-px_6d3e9",
  "xl:mr-px": "_xl:mr-px_6d3e9",
  "xl:mb-px": "_xl:mb-px_6d3e9",
  "xl:ml-px": "_xl:ml-px_6d3e9",
  "xl:-mt-1": "_xl:-mt-1_6d3e9",
  "xl:-mr-1": "_xl:-mr-1_6d3e9",
  "xl:-mb-1": "_xl:-mb-1_6d3e9",
  "xl:-ml-1": "_xl:-ml-1_6d3e9",
  "xl:-mt-2": "_xl:-mt-2_6d3e9",
  "xl:-mr-2": "_xl:-mr-2_6d3e9",
  "xl:-mb-2": "_xl:-mb-2_6d3e9",
  "xl:-ml-2": "_xl:-ml-2_6d3e9",
  "xl:-mt-3": "_xl:-mt-3_6d3e9",
  "xl:-mr-3": "_xl:-mr-3_6d3e9",
  "xl:-mb-3": "_xl:-mb-3_6d3e9",
  "xl:-ml-3": "_xl:-ml-3_6d3e9",
  "xl:-mt-4": "_xl:-mt-4_6d3e9",
  "xl:-mr-4": "_xl:-mr-4_6d3e9",
  "xl:-mb-4": "_xl:-mb-4_6d3e9",
  "xl:-ml-4": "_xl:-ml-4_6d3e9",
  "xl:-mt-5": "_xl:-mt-5_6d3e9",
  "xl:-mr-5": "_xl:-mr-5_6d3e9",
  "xl:-mb-5": "_xl:-mb-5_6d3e9",
  "xl:-ml-5": "_xl:-ml-5_6d3e9",
  "xl:-mt-6": "_xl:-mt-6_6d3e9",
  "xl:-mr-6": "_xl:-mr-6_6d3e9",
  "xl:-mb-6": "_xl:-mb-6_6d3e9",
  "xl:-ml-6": "_xl:-ml-6_6d3e9",
  "xl:-mt-8": "_xl:-mt-8_6d3e9",
  "xl:-mr-8": "_xl:-mr-8_6d3e9",
  "xl:-mb-8": "_xl:-mb-8_6d3e9",
  "xl:-ml-8": "_xl:-ml-8_6d3e9",
  "xl:-mt-10": "_xl:-mt-10_6d3e9",
  "xl:-mr-10": "_xl:-mr-10_6d3e9",
  "xl:-mb-10": "_xl:-mb-10_6d3e9",
  "xl:-ml-10": "_xl:-ml-10_6d3e9",
  "xl:-mt-12": "_xl:-mt-12_6d3e9",
  "xl:-mr-12": "_xl:-mr-12_6d3e9",
  "xl:-mb-12": "_xl:-mb-12_6d3e9",
  "xl:-ml-12": "_xl:-ml-12_6d3e9",
  "xl:-mt-16": "_xl:-mt-16_6d3e9",
  "xl:-mr-16": "_xl:-mr-16_6d3e9",
  "xl:-mb-16": "_xl:-mb-16_6d3e9",
  "xl:-ml-16": "_xl:-ml-16_6d3e9",
  "xl:-mt-20": "_xl:-mt-20_6d3e9",
  "xl:-mr-20": "_xl:-mr-20_6d3e9",
  "xl:-mb-20": "_xl:-mb-20_6d3e9",
  "xl:-ml-20": "_xl:-ml-20_6d3e9",
  "xl:-mt-24": "_xl:-mt-24_6d3e9",
  "xl:-mr-24": "_xl:-mr-24_6d3e9",
  "xl:-mb-24": "_xl:-mb-24_6d3e9",
  "xl:-ml-24": "_xl:-ml-24_6d3e9",
  "xl:-mt-32": "_xl:-mt-32_6d3e9",
  "xl:-mr-32": "_xl:-mr-32_6d3e9",
  "xl:-mb-32": "_xl:-mb-32_6d3e9",
  "xl:-ml-32": "_xl:-ml-32_6d3e9",
  "xl:-mt-40": "_xl:-mt-40_6d3e9",
  "xl:-mr-40": "_xl:-mr-40_6d3e9",
  "xl:-mb-40": "_xl:-mb-40_6d3e9",
  "xl:-ml-40": "_xl:-ml-40_6d3e9",
  "xl:-mt-48": "_xl:-mt-48_6d3e9",
  "xl:-mr-48": "_xl:-mr-48_6d3e9",
  "xl:-mb-48": "_xl:-mb-48_6d3e9",
  "xl:-ml-48": "_xl:-ml-48_6d3e9",
  "xl:-mt-56": "_xl:-mt-56_6d3e9",
  "xl:-mr-56": "_xl:-mr-56_6d3e9",
  "xl:-mb-56": "_xl:-mb-56_6d3e9",
  "xl:-ml-56": "_xl:-ml-56_6d3e9",
  "xl:-mt-64": "_xl:-mt-64_6d3e9",
  "xl:-mr-64": "_xl:-mr-64_6d3e9",
  "xl:-mb-64": "_xl:-mb-64_6d3e9",
  "xl:-ml-64": "_xl:-ml-64_6d3e9",
  "xl:-mt-px": "_xl:-mt-px_6d3e9",
  "xl:-mr-px": "_xl:-mr-px_6d3e9",
  "xl:-mb-px": "_xl:-mb-px_6d3e9",
  "xl:-ml-px": "_xl:-ml-px_6d3e9",
  "xl:max-h-full": "_xl:max-h-full_6d3e9",
  "xl:max-h-screen": "_xl:max-h-screen_6d3e9",
  "xl:max-w-xs": "_xl:max-w-xs_6d3e9",
  "xl:max-w-sm": "_xl:max-w-sm_6d3e9",
  "xl:max-w-md": "_xl:max-w-md_6d3e9",
  "xl:max-w-lg": "_xl:max-w-lg_6d3e9",
  "xl:max-w-xl": "_xl:max-w-xl_6d3e9",
  "xl:max-w-2xl": "_xl:max-w-2xl_6d3e9",
  "xl:max-w-3xl": "_xl:max-w-3xl_6d3e9",
  "xl:max-w-4xl": "_xl:max-w-4xl_6d3e9",
  "xl:max-w-5xl": "_xl:max-w-5xl_6d3e9",
  "xl:max-w-6xl": "_xl:max-w-6xl_6d3e9",
  "xl:max-w-full": "_xl:max-w-full_6d3e9",
  "xl:min-h-0": "_xl:min-h-0_6d3e9",
  "xl:min-h-full": "_xl:min-h-full_6d3e9",
  "xl:min-h-screen": "_xl:min-h-screen_6d3e9",
  "xl:min-w-0": "_xl:min-w-0_6d3e9",
  "xl:min-w-full": "_xl:min-w-full_6d3e9",
  "xl:object-contain": "_xl:object-contain_6d3e9",
  "xl:object-cover": "_xl:object-cover_6d3e9",
  "xl:object-fill": "_xl:object-fill_6d3e9",
  "xl:object-none": "_xl:object-none_6d3e9",
  "xl:object-scale-down": "_xl:object-scale-down_6d3e9",
  "xl:object-bottom": "_xl:object-bottom_6d3e9",
  "xl:object-center": "_xl:object-center_6d3e9",
  "xl:object-left": "_xl:object-left_6d3e9",
  "xl:object-left-bottom": "_xl:object-left-bottom_6d3e9",
  "xl:object-left-top": "_xl:object-left-top_6d3e9",
  "xl:object-right": "_xl:object-right_6d3e9",
  "xl:object-right-bottom": "_xl:object-right-bottom_6d3e9",
  "xl:object-right-top": "_xl:object-right-top_6d3e9",
  "xl:object-top": "_xl:object-top_6d3e9",
  "xl:opacity-0": "_xl:opacity-0_6d3e9",
  "xl:opacity-25": "_xl:opacity-25_6d3e9",
  "xl:opacity-50": "_xl:opacity-50_6d3e9",
  "xl:opacity-75": "_xl:opacity-75_6d3e9",
  "xl:opacity-100": "_xl:opacity-100_6d3e9",
  "xl:hover:opacity-0": "_xl:hover:opacity-0_6d3e9",
  "xl:hover:opacity-25": "_xl:hover:opacity-25_6d3e9",
  "xl:hover:opacity-50": "_xl:hover:opacity-50_6d3e9",
  "xl:hover:opacity-75": "_xl:hover:opacity-75_6d3e9",
  "xl:hover:opacity-100": "_xl:hover:opacity-100_6d3e9",
  "xl:focus:opacity-0": "_xl:focus:opacity-0_6d3e9",
  "xl:focus:opacity-25": "_xl:focus:opacity-25_6d3e9",
  "xl:focus:opacity-50": "_xl:focus:opacity-50_6d3e9",
  "xl:focus:opacity-75": "_xl:focus:opacity-75_6d3e9",
  "xl:focus:opacity-100": "_xl:focus:opacity-100_6d3e9",
  "xl:outline-none": "_xl:outline-none_6d3e9",
  "xl:focus:outline-none": "_xl:focus:outline-none_6d3e9",
  "xl:overflow-auto": "_xl:overflow-auto_6d3e9",
  "xl:overflow-hidden": "_xl:overflow-hidden_6d3e9",
  "xl:overflow-visible": "_xl:overflow-visible_6d3e9",
  "xl:overflow-scroll": "_xl:overflow-scroll_6d3e9",
  "xl:overflow-x-auto": "_xl:overflow-x-auto_6d3e9",
  "xl:overflow-y-auto": "_xl:overflow-y-auto_6d3e9",
  "xl:overflow-x-hidden": "_xl:overflow-x-hidden_6d3e9",
  "xl:overflow-y-hidden": "_xl:overflow-y-hidden_6d3e9",
  "xl:overflow-x-visible": "_xl:overflow-x-visible_6d3e9",
  "xl:overflow-y-visible": "_xl:overflow-y-visible_6d3e9",
  "xl:overflow-x-scroll": "_xl:overflow-x-scroll_6d3e9",
  "xl:overflow-y-scroll": "_xl:overflow-y-scroll_6d3e9",
  "xl:scrolling-touch": "_xl:scrolling-touch_6d3e9",
  "xl:scrolling-auto": "_xl:scrolling-auto_6d3e9",
  "xl:p-0": "_xl:p-0_6d3e9",
  "xl:p-1": "_xl:p-1_6d3e9",
  "xl:p-2": "_xl:p-2_6d3e9",
  "xl:p-3": "_xl:p-3_6d3e9",
  "xl:p-4": "_xl:p-4_6d3e9",
  "xl:p-5": "_xl:p-5_6d3e9",
  "xl:p-6": "_xl:p-6_6d3e9",
  "xl:p-8": "_xl:p-8_6d3e9",
  "xl:p-10": "_xl:p-10_6d3e9",
  "xl:p-12": "_xl:p-12_6d3e9",
  "xl:p-16": "_xl:p-16_6d3e9",
  "xl:p-20": "_xl:p-20_6d3e9",
  "xl:p-24": "_xl:p-24_6d3e9",
  "xl:p-32": "_xl:p-32_6d3e9",
  "xl:p-40": "_xl:p-40_6d3e9",
  "xl:p-48": "_xl:p-48_6d3e9",
  "xl:p-56": "_xl:p-56_6d3e9",
  "xl:p-64": "_xl:p-64_6d3e9",
  "xl:p-px": "_xl:p-px_6d3e9",
  "xl:py-0": "_xl:py-0_6d3e9",
  "xl:px-0": "_xl:px-0_6d3e9",
  "xl:py-1": "_xl:py-1_6d3e9",
  "xl:px-1": "_xl:px-1_6d3e9",
  "xl:py-2": "_xl:py-2_6d3e9",
  "xl:px-2": "_xl:px-2_6d3e9",
  "xl:py-3": "_xl:py-3_6d3e9",
  "xl:px-3": "_xl:px-3_6d3e9",
  "xl:py-4": "_xl:py-4_6d3e9",
  "xl:px-4": "_xl:px-4_6d3e9",
  "xl:py-5": "_xl:py-5_6d3e9",
  "xl:px-5": "_xl:px-5_6d3e9",
  "xl:py-6": "_xl:py-6_6d3e9",
  "xl:px-6": "_xl:px-6_6d3e9",
  "xl:py-8": "_xl:py-8_6d3e9",
  "xl:px-8": "_xl:px-8_6d3e9",
  "xl:py-10": "_xl:py-10_6d3e9",
  "xl:px-10": "_xl:px-10_6d3e9",
  "xl:py-12": "_xl:py-12_6d3e9",
  "xl:px-12": "_xl:px-12_6d3e9",
  "xl:py-16": "_xl:py-16_6d3e9",
  "xl:px-16": "_xl:px-16_6d3e9",
  "xl:py-20": "_xl:py-20_6d3e9",
  "xl:px-20": "_xl:px-20_6d3e9",
  "xl:py-24": "_xl:py-24_6d3e9",
  "xl:px-24": "_xl:px-24_6d3e9",
  "xl:py-32": "_xl:py-32_6d3e9",
  "xl:px-32": "_xl:px-32_6d3e9",
  "xl:py-40": "_xl:py-40_6d3e9",
  "xl:px-40": "_xl:px-40_6d3e9",
  "xl:py-48": "_xl:py-48_6d3e9",
  "xl:px-48": "_xl:px-48_6d3e9",
  "xl:py-56": "_xl:py-56_6d3e9",
  "xl:px-56": "_xl:px-56_6d3e9",
  "xl:py-64": "_xl:py-64_6d3e9",
  "xl:px-64": "_xl:px-64_6d3e9",
  "xl:py-px": "_xl:py-px_6d3e9",
  "xl:px-px": "_xl:px-px_6d3e9",
  "xl:pt-0": "_xl:pt-0_6d3e9",
  "xl:pr-0": "_xl:pr-0_6d3e9",
  "xl:pb-0": "_xl:pb-0_6d3e9",
  "xl:pl-0": "_xl:pl-0_6d3e9",
  "xl:pt-1": "_xl:pt-1_6d3e9",
  "xl:pr-1": "_xl:pr-1_6d3e9",
  "xl:pb-1": "_xl:pb-1_6d3e9",
  "xl:pl-1": "_xl:pl-1_6d3e9",
  "xl:pt-2": "_xl:pt-2_6d3e9",
  "xl:pr-2": "_xl:pr-2_6d3e9",
  "xl:pb-2": "_xl:pb-2_6d3e9",
  "xl:pl-2": "_xl:pl-2_6d3e9",
  "xl:pt-3": "_xl:pt-3_6d3e9",
  "xl:pr-3": "_xl:pr-3_6d3e9",
  "xl:pb-3": "_xl:pb-3_6d3e9",
  "xl:pl-3": "_xl:pl-3_6d3e9",
  "xl:pt-4": "_xl:pt-4_6d3e9",
  "xl:pr-4": "_xl:pr-4_6d3e9",
  "xl:pb-4": "_xl:pb-4_6d3e9",
  "xl:pl-4": "_xl:pl-4_6d3e9",
  "xl:pt-5": "_xl:pt-5_6d3e9",
  "xl:pr-5": "_xl:pr-5_6d3e9",
  "xl:pb-5": "_xl:pb-5_6d3e9",
  "xl:pl-5": "_xl:pl-5_6d3e9",
  "xl:pt-6": "_xl:pt-6_6d3e9",
  "xl:pr-6": "_xl:pr-6_6d3e9",
  "xl:pb-6": "_xl:pb-6_6d3e9",
  "xl:pl-6": "_xl:pl-6_6d3e9",
  "xl:pt-8": "_xl:pt-8_6d3e9",
  "xl:pr-8": "_xl:pr-8_6d3e9",
  "xl:pb-8": "_xl:pb-8_6d3e9",
  "xl:pl-8": "_xl:pl-8_6d3e9",
  "xl:pt-10": "_xl:pt-10_6d3e9",
  "xl:pr-10": "_xl:pr-10_6d3e9",
  "xl:pb-10": "_xl:pb-10_6d3e9",
  "xl:pl-10": "_xl:pl-10_6d3e9",
  "xl:pt-12": "_xl:pt-12_6d3e9",
  "xl:pr-12": "_xl:pr-12_6d3e9",
  "xl:pb-12": "_xl:pb-12_6d3e9",
  "xl:pl-12": "_xl:pl-12_6d3e9",
  "xl:pt-16": "_xl:pt-16_6d3e9",
  "xl:pr-16": "_xl:pr-16_6d3e9",
  "xl:pb-16": "_xl:pb-16_6d3e9",
  "xl:pl-16": "_xl:pl-16_6d3e9",
  "xl:pt-20": "_xl:pt-20_6d3e9",
  "xl:pr-20": "_xl:pr-20_6d3e9",
  "xl:pb-20": "_xl:pb-20_6d3e9",
  "xl:pl-20": "_xl:pl-20_6d3e9",
  "xl:pt-24": "_xl:pt-24_6d3e9",
  "xl:pr-24": "_xl:pr-24_6d3e9",
  "xl:pb-24": "_xl:pb-24_6d3e9",
  "xl:pl-24": "_xl:pl-24_6d3e9",
  "xl:pt-32": "_xl:pt-32_6d3e9",
  "xl:pr-32": "_xl:pr-32_6d3e9",
  "xl:pb-32": "_xl:pb-32_6d3e9",
  "xl:pl-32": "_xl:pl-32_6d3e9",
  "xl:pt-40": "_xl:pt-40_6d3e9",
  "xl:pr-40": "_xl:pr-40_6d3e9",
  "xl:pb-40": "_xl:pb-40_6d3e9",
  "xl:pl-40": "_xl:pl-40_6d3e9",
  "xl:pt-48": "_xl:pt-48_6d3e9",
  "xl:pr-48": "_xl:pr-48_6d3e9",
  "xl:pb-48": "_xl:pb-48_6d3e9",
  "xl:pl-48": "_xl:pl-48_6d3e9",
  "xl:pt-56": "_xl:pt-56_6d3e9",
  "xl:pr-56": "_xl:pr-56_6d3e9",
  "xl:pb-56": "_xl:pb-56_6d3e9",
  "xl:pl-56": "_xl:pl-56_6d3e9",
  "xl:pt-64": "_xl:pt-64_6d3e9",
  "xl:pr-64": "_xl:pr-64_6d3e9",
  "xl:pb-64": "_xl:pb-64_6d3e9",
  "xl:pl-64": "_xl:pl-64_6d3e9",
  "xl:pt-px": "_xl:pt-px_6d3e9",
  "xl:pr-px": "_xl:pr-px_6d3e9",
  "xl:pb-px": "_xl:pb-px_6d3e9",
  "xl:pl-px": "_xl:pl-px_6d3e9",
  "xl:placeholder-transparent": "_xl:placeholder-transparent_6d3e9",
  "xl:placeholder-black": "_xl:placeholder-black_6d3e9",
  "xl:placeholder-white": "_xl:placeholder-white_6d3e9",
  "xl:placeholder-gray-100": "_xl:placeholder-gray-100_6d3e9",
  "xl:placeholder-gray-200": "_xl:placeholder-gray-200_6d3e9",
  "xl:placeholder-gray-300": "_xl:placeholder-gray-300_6d3e9",
  "xl:placeholder-gray-400": "_xl:placeholder-gray-400_6d3e9",
  "xl:placeholder-gray-500": "_xl:placeholder-gray-500_6d3e9",
  "xl:placeholder-gray-600": "_xl:placeholder-gray-600_6d3e9",
  "xl:placeholder-gray-700": "_xl:placeholder-gray-700_6d3e9",
  "xl:placeholder-gray-800": "_xl:placeholder-gray-800_6d3e9",
  "xl:placeholder-gray-900": "_xl:placeholder-gray-900_6d3e9",
  "xl:placeholder-red-100": "_xl:placeholder-red-100_6d3e9",
  "xl:placeholder-red-200": "_xl:placeholder-red-200_6d3e9",
  "xl:placeholder-red-300": "_xl:placeholder-red-300_6d3e9",
  "xl:placeholder-red-400": "_xl:placeholder-red-400_6d3e9",
  "xl:placeholder-red-500": "_xl:placeholder-red-500_6d3e9",
  "xl:placeholder-red-600": "_xl:placeholder-red-600_6d3e9",
  "xl:placeholder-red-700": "_xl:placeholder-red-700_6d3e9",
  "xl:placeholder-red-800": "_xl:placeholder-red-800_6d3e9",
  "xl:placeholder-red-900": "_xl:placeholder-red-900_6d3e9",
  "xl:placeholder-orange-100": "_xl:placeholder-orange-100_6d3e9",
  "xl:placeholder-orange-200": "_xl:placeholder-orange-200_6d3e9",
  "xl:placeholder-orange-300": "_xl:placeholder-orange-300_6d3e9",
  "xl:placeholder-orange-400": "_xl:placeholder-orange-400_6d3e9",
  "xl:placeholder-orange-500": "_xl:placeholder-orange-500_6d3e9",
  "xl:placeholder-orange-600": "_xl:placeholder-orange-600_6d3e9",
  "xl:placeholder-orange-700": "_xl:placeholder-orange-700_6d3e9",
  "xl:placeholder-orange-800": "_xl:placeholder-orange-800_6d3e9",
  "xl:placeholder-orange-900": "_xl:placeholder-orange-900_6d3e9",
  "xl:placeholder-yellow-100": "_xl:placeholder-yellow-100_6d3e9",
  "xl:placeholder-yellow-200": "_xl:placeholder-yellow-200_6d3e9",
  "xl:placeholder-yellow-300": "_xl:placeholder-yellow-300_6d3e9",
  "xl:placeholder-yellow-400": "_xl:placeholder-yellow-400_6d3e9",
  "xl:placeholder-yellow-500": "_xl:placeholder-yellow-500_6d3e9",
  "xl:placeholder-yellow-600": "_xl:placeholder-yellow-600_6d3e9",
  "xl:placeholder-yellow-700": "_xl:placeholder-yellow-700_6d3e9",
  "xl:placeholder-yellow-800": "_xl:placeholder-yellow-800_6d3e9",
  "xl:placeholder-yellow-900": "_xl:placeholder-yellow-900_6d3e9",
  "xl:placeholder-green-100": "_xl:placeholder-green-100_6d3e9",
  "xl:placeholder-green-200": "_xl:placeholder-green-200_6d3e9",
  "xl:placeholder-green-300": "_xl:placeholder-green-300_6d3e9",
  "xl:placeholder-green-400": "_xl:placeholder-green-400_6d3e9",
  "xl:placeholder-green-500": "_xl:placeholder-green-500_6d3e9",
  "xl:placeholder-green-600": "_xl:placeholder-green-600_6d3e9",
  "xl:placeholder-green-700": "_xl:placeholder-green-700_6d3e9",
  "xl:placeholder-green-800": "_xl:placeholder-green-800_6d3e9",
  "xl:placeholder-green-900": "_xl:placeholder-green-900_6d3e9",
  "xl:placeholder-teal-100": "_xl:placeholder-teal-100_6d3e9",
  "xl:placeholder-teal-200": "_xl:placeholder-teal-200_6d3e9",
  "xl:placeholder-teal-300": "_xl:placeholder-teal-300_6d3e9",
  "xl:placeholder-teal-400": "_xl:placeholder-teal-400_6d3e9",
  "xl:placeholder-teal-500": "_xl:placeholder-teal-500_6d3e9",
  "xl:placeholder-teal-600": "_xl:placeholder-teal-600_6d3e9",
  "xl:placeholder-teal-700": "_xl:placeholder-teal-700_6d3e9",
  "xl:placeholder-teal-800": "_xl:placeholder-teal-800_6d3e9",
  "xl:placeholder-teal-900": "_xl:placeholder-teal-900_6d3e9",
  "xl:placeholder-blue-100": "_xl:placeholder-blue-100_6d3e9",
  "xl:placeholder-blue-200": "_xl:placeholder-blue-200_6d3e9",
  "xl:placeholder-blue-300": "_xl:placeholder-blue-300_6d3e9",
  "xl:placeholder-blue-400": "_xl:placeholder-blue-400_6d3e9",
  "xl:placeholder-blue-500": "_xl:placeholder-blue-500_6d3e9",
  "xl:placeholder-blue-600": "_xl:placeholder-blue-600_6d3e9",
  "xl:placeholder-blue-700": "_xl:placeholder-blue-700_6d3e9",
  "xl:placeholder-blue-800": "_xl:placeholder-blue-800_6d3e9",
  "xl:placeholder-blue-900": "_xl:placeholder-blue-900_6d3e9",
  "xl:placeholder-indigo-100": "_xl:placeholder-indigo-100_6d3e9",
  "xl:placeholder-indigo-200": "_xl:placeholder-indigo-200_6d3e9",
  "xl:placeholder-indigo-300": "_xl:placeholder-indigo-300_6d3e9",
  "xl:placeholder-indigo-400": "_xl:placeholder-indigo-400_6d3e9",
  "xl:placeholder-indigo-500": "_xl:placeholder-indigo-500_6d3e9",
  "xl:placeholder-indigo-600": "_xl:placeholder-indigo-600_6d3e9",
  "xl:placeholder-indigo-700": "_xl:placeholder-indigo-700_6d3e9",
  "xl:placeholder-indigo-800": "_xl:placeholder-indigo-800_6d3e9",
  "xl:placeholder-indigo-900": "_xl:placeholder-indigo-900_6d3e9",
  "xl:placeholder-purple-100": "_xl:placeholder-purple-100_6d3e9",
  "xl:placeholder-purple-200": "_xl:placeholder-purple-200_6d3e9",
  "xl:placeholder-purple-300": "_xl:placeholder-purple-300_6d3e9",
  "xl:placeholder-purple-400": "_xl:placeholder-purple-400_6d3e9",
  "xl:placeholder-purple-500": "_xl:placeholder-purple-500_6d3e9",
  "xl:placeholder-purple-600": "_xl:placeholder-purple-600_6d3e9",
  "xl:placeholder-purple-700": "_xl:placeholder-purple-700_6d3e9",
  "xl:placeholder-purple-800": "_xl:placeholder-purple-800_6d3e9",
  "xl:placeholder-purple-900": "_xl:placeholder-purple-900_6d3e9",
  "xl:placeholder-pink-100": "_xl:placeholder-pink-100_6d3e9",
  "xl:placeholder-pink-200": "_xl:placeholder-pink-200_6d3e9",
  "xl:placeholder-pink-300": "_xl:placeholder-pink-300_6d3e9",
  "xl:placeholder-pink-400": "_xl:placeholder-pink-400_6d3e9",
  "xl:placeholder-pink-500": "_xl:placeholder-pink-500_6d3e9",
  "xl:placeholder-pink-600": "_xl:placeholder-pink-600_6d3e9",
  "xl:placeholder-pink-700": "_xl:placeholder-pink-700_6d3e9",
  "xl:placeholder-pink-800": "_xl:placeholder-pink-800_6d3e9",
  "xl:placeholder-pink-900": "_xl:placeholder-pink-900_6d3e9",
  "xl:placeholder-primary-100": "_xl:placeholder-primary-100_6d3e9",
  "xl:placeholder-primary-200": "_xl:placeholder-primary-200_6d3e9",
  "xl:placeholder-primary-300": "_xl:placeholder-primary-300_6d3e9",
  "xl:placeholder-primary-400": "_xl:placeholder-primary-400_6d3e9",
  "xl:placeholder-primary-500": "_xl:placeholder-primary-500_6d3e9",
  "xl:placeholder-primary-600": "_xl:placeholder-primary-600_6d3e9",
  "xl:placeholder-primary-700": "_xl:placeholder-primary-700_6d3e9",
  "xl:placeholder-primary-800": "_xl:placeholder-primary-800_6d3e9",
  "xl:placeholder-primary-900": "_xl:placeholder-primary-900_6d3e9",
  "xl:placeholder-secondary-100": "_xl:placeholder-secondary-100_6d3e9",
  "xl:placeholder-secondary-200": "_xl:placeholder-secondary-200_6d3e9",
  "xl:placeholder-secondary-300": "_xl:placeholder-secondary-300_6d3e9",
  "xl:placeholder-secondary-400": "_xl:placeholder-secondary-400_6d3e9",
  "xl:placeholder-secondary-500": "_xl:placeholder-secondary-500_6d3e9",
  "xl:placeholder-secondary-600": "_xl:placeholder-secondary-600_6d3e9",
  "xl:placeholder-secondary-700": "_xl:placeholder-secondary-700_6d3e9",
  "xl:placeholder-secondary-800": "_xl:placeholder-secondary-800_6d3e9",
  "xl:placeholder-secondary-900": "_xl:placeholder-secondary-900_6d3e9",
  "xl:focus:placeholder-transparent": "_xl:focus:placeholder-transparent_6d3e9",
  "xl:focus:placeholder-black": "_xl:focus:placeholder-black_6d3e9",
  "xl:focus:placeholder-white": "_xl:focus:placeholder-white_6d3e9",
  "xl:focus:placeholder-gray-100": "_xl:focus:placeholder-gray-100_6d3e9",
  "xl:focus:placeholder-gray-200": "_xl:focus:placeholder-gray-200_6d3e9",
  "xl:focus:placeholder-gray-300": "_xl:focus:placeholder-gray-300_6d3e9",
  "xl:focus:placeholder-gray-400": "_xl:focus:placeholder-gray-400_6d3e9",
  "xl:focus:placeholder-gray-500": "_xl:focus:placeholder-gray-500_6d3e9",
  "xl:focus:placeholder-gray-600": "_xl:focus:placeholder-gray-600_6d3e9",
  "xl:focus:placeholder-gray-700": "_xl:focus:placeholder-gray-700_6d3e9",
  "xl:focus:placeholder-gray-800": "_xl:focus:placeholder-gray-800_6d3e9",
  "xl:focus:placeholder-gray-900": "_xl:focus:placeholder-gray-900_6d3e9",
  "xl:focus:placeholder-red-100": "_xl:focus:placeholder-red-100_6d3e9",
  "xl:focus:placeholder-red-200": "_xl:focus:placeholder-red-200_6d3e9",
  "xl:focus:placeholder-red-300": "_xl:focus:placeholder-red-300_6d3e9",
  "xl:focus:placeholder-red-400": "_xl:focus:placeholder-red-400_6d3e9",
  "xl:focus:placeholder-red-500": "_xl:focus:placeholder-red-500_6d3e9",
  "xl:focus:placeholder-red-600": "_xl:focus:placeholder-red-600_6d3e9",
  "xl:focus:placeholder-red-700": "_xl:focus:placeholder-red-700_6d3e9",
  "xl:focus:placeholder-red-800": "_xl:focus:placeholder-red-800_6d3e9",
  "xl:focus:placeholder-red-900": "_xl:focus:placeholder-red-900_6d3e9",
  "xl:focus:placeholder-orange-100": "_xl:focus:placeholder-orange-100_6d3e9",
  "xl:focus:placeholder-orange-200": "_xl:focus:placeholder-orange-200_6d3e9",
  "xl:focus:placeholder-orange-300": "_xl:focus:placeholder-orange-300_6d3e9",
  "xl:focus:placeholder-orange-400": "_xl:focus:placeholder-orange-400_6d3e9",
  "xl:focus:placeholder-orange-500": "_xl:focus:placeholder-orange-500_6d3e9",
  "xl:focus:placeholder-orange-600": "_xl:focus:placeholder-orange-600_6d3e9",
  "xl:focus:placeholder-orange-700": "_xl:focus:placeholder-orange-700_6d3e9",
  "xl:focus:placeholder-orange-800": "_xl:focus:placeholder-orange-800_6d3e9",
  "xl:focus:placeholder-orange-900": "_xl:focus:placeholder-orange-900_6d3e9",
  "xl:focus:placeholder-yellow-100": "_xl:focus:placeholder-yellow-100_6d3e9",
  "xl:focus:placeholder-yellow-200": "_xl:focus:placeholder-yellow-200_6d3e9",
  "xl:focus:placeholder-yellow-300": "_xl:focus:placeholder-yellow-300_6d3e9",
  "xl:focus:placeholder-yellow-400": "_xl:focus:placeholder-yellow-400_6d3e9",
  "xl:focus:placeholder-yellow-500": "_xl:focus:placeholder-yellow-500_6d3e9",
  "xl:focus:placeholder-yellow-600": "_xl:focus:placeholder-yellow-600_6d3e9",
  "xl:focus:placeholder-yellow-700": "_xl:focus:placeholder-yellow-700_6d3e9",
  "xl:focus:placeholder-yellow-800": "_xl:focus:placeholder-yellow-800_6d3e9",
  "xl:focus:placeholder-yellow-900": "_xl:focus:placeholder-yellow-900_6d3e9",
  "xl:focus:placeholder-green-100": "_xl:focus:placeholder-green-100_6d3e9",
  "xl:focus:placeholder-green-200": "_xl:focus:placeholder-green-200_6d3e9",
  "xl:focus:placeholder-green-300": "_xl:focus:placeholder-green-300_6d3e9",
  "xl:focus:placeholder-green-400": "_xl:focus:placeholder-green-400_6d3e9",
  "xl:focus:placeholder-green-500": "_xl:focus:placeholder-green-500_6d3e9",
  "xl:focus:placeholder-green-600": "_xl:focus:placeholder-green-600_6d3e9",
  "xl:focus:placeholder-green-700": "_xl:focus:placeholder-green-700_6d3e9",
  "xl:focus:placeholder-green-800": "_xl:focus:placeholder-green-800_6d3e9",
  "xl:focus:placeholder-green-900": "_xl:focus:placeholder-green-900_6d3e9",
  "xl:focus:placeholder-teal-100": "_xl:focus:placeholder-teal-100_6d3e9",
  "xl:focus:placeholder-teal-200": "_xl:focus:placeholder-teal-200_6d3e9",
  "xl:focus:placeholder-teal-300": "_xl:focus:placeholder-teal-300_6d3e9",
  "xl:focus:placeholder-teal-400": "_xl:focus:placeholder-teal-400_6d3e9",
  "xl:focus:placeholder-teal-500": "_xl:focus:placeholder-teal-500_6d3e9",
  "xl:focus:placeholder-teal-600": "_xl:focus:placeholder-teal-600_6d3e9",
  "xl:focus:placeholder-teal-700": "_xl:focus:placeholder-teal-700_6d3e9",
  "xl:focus:placeholder-teal-800": "_xl:focus:placeholder-teal-800_6d3e9",
  "xl:focus:placeholder-teal-900": "_xl:focus:placeholder-teal-900_6d3e9",
  "xl:focus:placeholder-blue-100": "_xl:focus:placeholder-blue-100_6d3e9",
  "xl:focus:placeholder-blue-200": "_xl:focus:placeholder-blue-200_6d3e9",
  "xl:focus:placeholder-blue-300": "_xl:focus:placeholder-blue-300_6d3e9",
  "xl:focus:placeholder-blue-400": "_xl:focus:placeholder-blue-400_6d3e9",
  "xl:focus:placeholder-blue-500": "_xl:focus:placeholder-blue-500_6d3e9",
  "xl:focus:placeholder-blue-600": "_xl:focus:placeholder-blue-600_6d3e9",
  "xl:focus:placeholder-blue-700": "_xl:focus:placeholder-blue-700_6d3e9",
  "xl:focus:placeholder-blue-800": "_xl:focus:placeholder-blue-800_6d3e9",
  "xl:focus:placeholder-blue-900": "_xl:focus:placeholder-blue-900_6d3e9",
  "xl:focus:placeholder-indigo-100": "_xl:focus:placeholder-indigo-100_6d3e9",
  "xl:focus:placeholder-indigo-200": "_xl:focus:placeholder-indigo-200_6d3e9",
  "xl:focus:placeholder-indigo-300": "_xl:focus:placeholder-indigo-300_6d3e9",
  "xl:focus:placeholder-indigo-400": "_xl:focus:placeholder-indigo-400_6d3e9",
  "xl:focus:placeholder-indigo-500": "_xl:focus:placeholder-indigo-500_6d3e9",
  "xl:focus:placeholder-indigo-600": "_xl:focus:placeholder-indigo-600_6d3e9",
  "xl:focus:placeholder-indigo-700": "_xl:focus:placeholder-indigo-700_6d3e9",
  "xl:focus:placeholder-indigo-800": "_xl:focus:placeholder-indigo-800_6d3e9",
  "xl:focus:placeholder-indigo-900": "_xl:focus:placeholder-indigo-900_6d3e9",
  "xl:focus:placeholder-purple-100": "_xl:focus:placeholder-purple-100_6d3e9",
  "xl:focus:placeholder-purple-200": "_xl:focus:placeholder-purple-200_6d3e9",
  "xl:focus:placeholder-purple-300": "_xl:focus:placeholder-purple-300_6d3e9",
  "xl:focus:placeholder-purple-400": "_xl:focus:placeholder-purple-400_6d3e9",
  "xl:focus:placeholder-purple-500": "_xl:focus:placeholder-purple-500_6d3e9",
  "xl:focus:placeholder-purple-600": "_xl:focus:placeholder-purple-600_6d3e9",
  "xl:focus:placeholder-purple-700": "_xl:focus:placeholder-purple-700_6d3e9",
  "xl:focus:placeholder-purple-800": "_xl:focus:placeholder-purple-800_6d3e9",
  "xl:focus:placeholder-purple-900": "_xl:focus:placeholder-purple-900_6d3e9",
  "xl:focus:placeholder-pink-100": "_xl:focus:placeholder-pink-100_6d3e9",
  "xl:focus:placeholder-pink-200": "_xl:focus:placeholder-pink-200_6d3e9",
  "xl:focus:placeholder-pink-300": "_xl:focus:placeholder-pink-300_6d3e9",
  "xl:focus:placeholder-pink-400": "_xl:focus:placeholder-pink-400_6d3e9",
  "xl:focus:placeholder-pink-500": "_xl:focus:placeholder-pink-500_6d3e9",
  "xl:focus:placeholder-pink-600": "_xl:focus:placeholder-pink-600_6d3e9",
  "xl:focus:placeholder-pink-700": "_xl:focus:placeholder-pink-700_6d3e9",
  "xl:focus:placeholder-pink-800": "_xl:focus:placeholder-pink-800_6d3e9",
  "xl:focus:placeholder-pink-900": "_xl:focus:placeholder-pink-900_6d3e9",
  "xl:focus:placeholder-primary-100": "_xl:focus:placeholder-primary-100_6d3e9",
  "xl:focus:placeholder-primary-200": "_xl:focus:placeholder-primary-200_6d3e9",
  "xl:focus:placeholder-primary-300": "_xl:focus:placeholder-primary-300_6d3e9",
  "xl:focus:placeholder-primary-400": "_xl:focus:placeholder-primary-400_6d3e9",
  "xl:focus:placeholder-primary-500": "_xl:focus:placeholder-primary-500_6d3e9",
  "xl:focus:placeholder-primary-600": "_xl:focus:placeholder-primary-600_6d3e9",
  "xl:focus:placeholder-primary-700": "_xl:focus:placeholder-primary-700_6d3e9",
  "xl:focus:placeholder-primary-800": "_xl:focus:placeholder-primary-800_6d3e9",
  "xl:focus:placeholder-primary-900": "_xl:focus:placeholder-primary-900_6d3e9",
  "xl:focus:placeholder-secondary-100": "_xl:focus:placeholder-secondary-100_6d3e9",
  "xl:focus:placeholder-secondary-200": "_xl:focus:placeholder-secondary-200_6d3e9",
  "xl:focus:placeholder-secondary-300": "_xl:focus:placeholder-secondary-300_6d3e9",
  "xl:focus:placeholder-secondary-400": "_xl:focus:placeholder-secondary-400_6d3e9",
  "xl:focus:placeholder-secondary-500": "_xl:focus:placeholder-secondary-500_6d3e9",
  "xl:focus:placeholder-secondary-600": "_xl:focus:placeholder-secondary-600_6d3e9",
  "xl:focus:placeholder-secondary-700": "_xl:focus:placeholder-secondary-700_6d3e9",
  "xl:focus:placeholder-secondary-800": "_xl:focus:placeholder-secondary-800_6d3e9",
  "xl:focus:placeholder-secondary-900": "_xl:focus:placeholder-secondary-900_6d3e9",
  "xl:pointer-events-none": "_xl:pointer-events-none_6d3e9",
  "xl:pointer-events-auto": "_xl:pointer-events-auto_6d3e9",
  "xl:static": "_xl:static_6d3e9",
  "xl:fixed": "_xl:fixed_6d3e9",
  "xl:absolute": "_xl:absolute_6d3e9",
  "xl:relative": "_xl:relative_6d3e9",
  "xl:sticky": "_xl:sticky_6d3e9",
  "xl:inset-0": "_xl:inset-0_6d3e9",
  "xl:inset-auto": "_xl:inset-auto_6d3e9",
  "xl:inset-y-0": "_xl:inset-y-0_6d3e9",
  "xl:inset-x-0": "_xl:inset-x-0_6d3e9",
  "xl:inset-y-auto": "_xl:inset-y-auto_6d3e9",
  "xl:inset-x-auto": "_xl:inset-x-auto_6d3e9",
  "xl:top-0": "_xl:top-0_6d3e9",
  "xl:right-0": "_xl:right-0_6d3e9",
  "xl:bottom-0": "_xl:bottom-0_6d3e9",
  "xl:left-0": "_xl:left-0_6d3e9",
  "xl:top-auto": "_xl:top-auto_6d3e9",
  "xl:right-auto": "_xl:right-auto_6d3e9",
  "xl:bottom-auto": "_xl:bottom-auto_6d3e9",
  "xl:left-auto": "_xl:left-auto_6d3e9",
  "xl:resize-none": "_xl:resize-none_6d3e9",
  "xl:resize-y": "_xl:resize-y_6d3e9",
  "xl:resize-x": "_xl:resize-x_6d3e9",
  "xl:resize": "_xl:resize_6d3e9",
  "xl:shadow": "_xl:shadow_6d3e9",
  "xl:shadow-md": "_xl:shadow-md_6d3e9",
  "xl:shadow-lg": "_xl:shadow-lg_6d3e9",
  "xl:shadow-xl": "_xl:shadow-xl_6d3e9",
  "xl:shadow-2xl": "_xl:shadow-2xl_6d3e9",
  "xl:shadow-inner": "_xl:shadow-inner_6d3e9",
  "xl:shadow-outline": "_xl:shadow-outline_6d3e9",
  "xl:shadow-none": "_xl:shadow-none_6d3e9",
  "xl:hover:shadow": "_xl:hover:shadow_6d3e9",
  "xl:hover:shadow-md": "_xl:hover:shadow-md_6d3e9",
  "xl:hover:shadow-lg": "_xl:hover:shadow-lg_6d3e9",
  "xl:hover:shadow-xl": "_xl:hover:shadow-xl_6d3e9",
  "xl:hover:shadow-2xl": "_xl:hover:shadow-2xl_6d3e9",
  "xl:hover:shadow-inner": "_xl:hover:shadow-inner_6d3e9",
  "xl:hover:shadow-outline": "_xl:hover:shadow-outline_6d3e9",
  "xl:hover:shadow-none": "_xl:hover:shadow-none_6d3e9",
  "xl:focus:shadow": "_xl:focus:shadow_6d3e9",
  "xl:focus:shadow-md": "_xl:focus:shadow-md_6d3e9",
  "xl:focus:shadow-lg": "_xl:focus:shadow-lg_6d3e9",
  "xl:focus:shadow-xl": "_xl:focus:shadow-xl_6d3e9",
  "xl:focus:shadow-2xl": "_xl:focus:shadow-2xl_6d3e9",
  "xl:focus:shadow-inner": "_xl:focus:shadow-inner_6d3e9",
  "xl:focus:shadow-outline": "_xl:focus:shadow-outline_6d3e9",
  "xl:focus:shadow-none": "_xl:focus:shadow-none_6d3e9",
  "xl:fill-current": "_xl:fill-current_6d3e9",
  "xl:stroke-current": "_xl:stroke-current_6d3e9",
  "xl:table-auto": "_xl:table-auto_6d3e9",
  "xl:table-fixed": "_xl:table-fixed_6d3e9",
  "xl:text-left": "_xl:text-left_6d3e9",
  "xl:text-center": "_xl:text-center_6d3e9",
  "xl:text-right": "_xl:text-right_6d3e9",
  "xl:text-justify": "_xl:text-justify_6d3e9",
  "xl:text-transparent": "_xl:text-transparent_6d3e9",
  "xl:text-black": "_xl:text-black_6d3e9",
  "xl:text-white": "_xl:text-white_6d3e9",
  "xl:text-gray-100": "_xl:text-gray-100_6d3e9",
  "xl:text-gray-200": "_xl:text-gray-200_6d3e9",
  "xl:text-gray-300": "_xl:text-gray-300_6d3e9",
  "xl:text-gray-400": "_xl:text-gray-400_6d3e9",
  "xl:text-gray-500": "_xl:text-gray-500_6d3e9",
  "xl:text-gray-600": "_xl:text-gray-600_6d3e9",
  "xl:text-gray-700": "_xl:text-gray-700_6d3e9",
  "xl:text-gray-800": "_xl:text-gray-800_6d3e9",
  "xl:text-gray-900": "_xl:text-gray-900_6d3e9",
  "xl:text-red-100": "_xl:text-red-100_6d3e9",
  "xl:text-red-200": "_xl:text-red-200_6d3e9",
  "xl:text-red-300": "_xl:text-red-300_6d3e9",
  "xl:text-red-400": "_xl:text-red-400_6d3e9",
  "xl:text-red-500": "_xl:text-red-500_6d3e9",
  "xl:text-red-600": "_xl:text-red-600_6d3e9",
  "xl:text-red-700": "_xl:text-red-700_6d3e9",
  "xl:text-red-800": "_xl:text-red-800_6d3e9",
  "xl:text-red-900": "_xl:text-red-900_6d3e9",
  "xl:text-orange-100": "_xl:text-orange-100_6d3e9",
  "xl:text-orange-200": "_xl:text-orange-200_6d3e9",
  "xl:text-orange-300": "_xl:text-orange-300_6d3e9",
  "xl:text-orange-400": "_xl:text-orange-400_6d3e9",
  "xl:text-orange-500": "_xl:text-orange-500_6d3e9",
  "xl:text-orange-600": "_xl:text-orange-600_6d3e9",
  "xl:text-orange-700": "_xl:text-orange-700_6d3e9",
  "xl:text-orange-800": "_xl:text-orange-800_6d3e9",
  "xl:text-orange-900": "_xl:text-orange-900_6d3e9",
  "xl:text-yellow-100": "_xl:text-yellow-100_6d3e9",
  "xl:text-yellow-200": "_xl:text-yellow-200_6d3e9",
  "xl:text-yellow-300": "_xl:text-yellow-300_6d3e9",
  "xl:text-yellow-400": "_xl:text-yellow-400_6d3e9",
  "xl:text-yellow-500": "_xl:text-yellow-500_6d3e9",
  "xl:text-yellow-600": "_xl:text-yellow-600_6d3e9",
  "xl:text-yellow-700": "_xl:text-yellow-700_6d3e9",
  "xl:text-yellow-800": "_xl:text-yellow-800_6d3e9",
  "xl:text-yellow-900": "_xl:text-yellow-900_6d3e9",
  "xl:text-green-100": "_xl:text-green-100_6d3e9",
  "xl:text-green-200": "_xl:text-green-200_6d3e9",
  "xl:text-green-300": "_xl:text-green-300_6d3e9",
  "xl:text-green-400": "_xl:text-green-400_6d3e9",
  "xl:text-green-500": "_xl:text-green-500_6d3e9",
  "xl:text-green-600": "_xl:text-green-600_6d3e9",
  "xl:text-green-700": "_xl:text-green-700_6d3e9",
  "xl:text-green-800": "_xl:text-green-800_6d3e9",
  "xl:text-green-900": "_xl:text-green-900_6d3e9",
  "xl:text-teal-100": "_xl:text-teal-100_6d3e9",
  "xl:text-teal-200": "_xl:text-teal-200_6d3e9",
  "xl:text-teal-300": "_xl:text-teal-300_6d3e9",
  "xl:text-teal-400": "_xl:text-teal-400_6d3e9",
  "xl:text-teal-500": "_xl:text-teal-500_6d3e9",
  "xl:text-teal-600": "_xl:text-teal-600_6d3e9",
  "xl:text-teal-700": "_xl:text-teal-700_6d3e9",
  "xl:text-teal-800": "_xl:text-teal-800_6d3e9",
  "xl:text-teal-900": "_xl:text-teal-900_6d3e9",
  "xl:text-blue-100": "_xl:text-blue-100_6d3e9",
  "xl:text-blue-200": "_xl:text-blue-200_6d3e9",
  "xl:text-blue-300": "_xl:text-blue-300_6d3e9",
  "xl:text-blue-400": "_xl:text-blue-400_6d3e9",
  "xl:text-blue-500": "_xl:text-blue-500_6d3e9",
  "xl:text-blue-600": "_xl:text-blue-600_6d3e9",
  "xl:text-blue-700": "_xl:text-blue-700_6d3e9",
  "xl:text-blue-800": "_xl:text-blue-800_6d3e9",
  "xl:text-blue-900": "_xl:text-blue-900_6d3e9",
  "xl:text-indigo-100": "_xl:text-indigo-100_6d3e9",
  "xl:text-indigo-200": "_xl:text-indigo-200_6d3e9",
  "xl:text-indigo-300": "_xl:text-indigo-300_6d3e9",
  "xl:text-indigo-400": "_xl:text-indigo-400_6d3e9",
  "xl:text-indigo-500": "_xl:text-indigo-500_6d3e9",
  "xl:text-indigo-600": "_xl:text-indigo-600_6d3e9",
  "xl:text-indigo-700": "_xl:text-indigo-700_6d3e9",
  "xl:text-indigo-800": "_xl:text-indigo-800_6d3e9",
  "xl:text-indigo-900": "_xl:text-indigo-900_6d3e9",
  "xl:text-purple-100": "_xl:text-purple-100_6d3e9",
  "xl:text-purple-200": "_xl:text-purple-200_6d3e9",
  "xl:text-purple-300": "_xl:text-purple-300_6d3e9",
  "xl:text-purple-400": "_xl:text-purple-400_6d3e9",
  "xl:text-purple-500": "_xl:text-purple-500_6d3e9",
  "xl:text-purple-600": "_xl:text-purple-600_6d3e9",
  "xl:text-purple-700": "_xl:text-purple-700_6d3e9",
  "xl:text-purple-800": "_xl:text-purple-800_6d3e9",
  "xl:text-purple-900": "_xl:text-purple-900_6d3e9",
  "xl:text-pink-100": "_xl:text-pink-100_6d3e9",
  "xl:text-pink-200": "_xl:text-pink-200_6d3e9",
  "xl:text-pink-300": "_xl:text-pink-300_6d3e9",
  "xl:text-pink-400": "_xl:text-pink-400_6d3e9",
  "xl:text-pink-500": "_xl:text-pink-500_6d3e9",
  "xl:text-pink-600": "_xl:text-pink-600_6d3e9",
  "xl:text-pink-700": "_xl:text-pink-700_6d3e9",
  "xl:text-pink-800": "_xl:text-pink-800_6d3e9",
  "xl:text-pink-900": "_xl:text-pink-900_6d3e9",
  "xl:text-primary-100": "_xl:text-primary-100_6d3e9",
  "xl:text-primary-200": "_xl:text-primary-200_6d3e9",
  "xl:text-primary-300": "_xl:text-primary-300_6d3e9",
  "xl:text-primary-400": "_xl:text-primary-400_6d3e9",
  "xl:text-primary-500": "_xl:text-primary-500_6d3e9",
  "xl:text-primary-600": "_xl:text-primary-600_6d3e9",
  "xl:text-primary-700": "_xl:text-primary-700_6d3e9",
  "xl:text-primary-800": "_xl:text-primary-800_6d3e9",
  "xl:text-primary-900": "_xl:text-primary-900_6d3e9",
  "xl:text-secondary-100": "_xl:text-secondary-100_6d3e9",
  "xl:text-secondary-200": "_xl:text-secondary-200_6d3e9",
  "xl:text-secondary-300": "_xl:text-secondary-300_6d3e9",
  "xl:text-secondary-400": "_xl:text-secondary-400_6d3e9",
  "xl:text-secondary-500": "_xl:text-secondary-500_6d3e9",
  "xl:text-secondary-600": "_xl:text-secondary-600_6d3e9",
  "xl:text-secondary-700": "_xl:text-secondary-700_6d3e9",
  "xl:text-secondary-800": "_xl:text-secondary-800_6d3e9",
  "xl:text-secondary-900": "_xl:text-secondary-900_6d3e9",
  "xl:hover:text-transparent": "_xl:hover:text-transparent_6d3e9",
  "xl:hover:text-black": "_xl:hover:text-black_6d3e9",
  "xl:hover:text-white": "_xl:hover:text-white_6d3e9",
  "xl:hover:text-gray-100": "_xl:hover:text-gray-100_6d3e9",
  "xl:hover:text-gray-200": "_xl:hover:text-gray-200_6d3e9",
  "xl:hover:text-gray-300": "_xl:hover:text-gray-300_6d3e9",
  "xl:hover:text-gray-400": "_xl:hover:text-gray-400_6d3e9",
  "xl:hover:text-gray-500": "_xl:hover:text-gray-500_6d3e9",
  "xl:hover:text-gray-600": "_xl:hover:text-gray-600_6d3e9",
  "xl:hover:text-gray-700": "_xl:hover:text-gray-700_6d3e9",
  "xl:hover:text-gray-800": "_xl:hover:text-gray-800_6d3e9",
  "xl:hover:text-gray-900": "_xl:hover:text-gray-900_6d3e9",
  "xl:hover:text-red-100": "_xl:hover:text-red-100_6d3e9",
  "xl:hover:text-red-200": "_xl:hover:text-red-200_6d3e9",
  "xl:hover:text-red-300": "_xl:hover:text-red-300_6d3e9",
  "xl:hover:text-red-400": "_xl:hover:text-red-400_6d3e9",
  "xl:hover:text-red-500": "_xl:hover:text-red-500_6d3e9",
  "xl:hover:text-red-600": "_xl:hover:text-red-600_6d3e9",
  "xl:hover:text-red-700": "_xl:hover:text-red-700_6d3e9",
  "xl:hover:text-red-800": "_xl:hover:text-red-800_6d3e9",
  "xl:hover:text-red-900": "_xl:hover:text-red-900_6d3e9",
  "xl:hover:text-orange-100": "_xl:hover:text-orange-100_6d3e9",
  "xl:hover:text-orange-200": "_xl:hover:text-orange-200_6d3e9",
  "xl:hover:text-orange-300": "_xl:hover:text-orange-300_6d3e9",
  "xl:hover:text-orange-400": "_xl:hover:text-orange-400_6d3e9",
  "xl:hover:text-orange-500": "_xl:hover:text-orange-500_6d3e9",
  "xl:hover:text-orange-600": "_xl:hover:text-orange-600_6d3e9",
  "xl:hover:text-orange-700": "_xl:hover:text-orange-700_6d3e9",
  "xl:hover:text-orange-800": "_xl:hover:text-orange-800_6d3e9",
  "xl:hover:text-orange-900": "_xl:hover:text-orange-900_6d3e9",
  "xl:hover:text-yellow-100": "_xl:hover:text-yellow-100_6d3e9",
  "xl:hover:text-yellow-200": "_xl:hover:text-yellow-200_6d3e9",
  "xl:hover:text-yellow-300": "_xl:hover:text-yellow-300_6d3e9",
  "xl:hover:text-yellow-400": "_xl:hover:text-yellow-400_6d3e9",
  "xl:hover:text-yellow-500": "_xl:hover:text-yellow-500_6d3e9",
  "xl:hover:text-yellow-600": "_xl:hover:text-yellow-600_6d3e9",
  "xl:hover:text-yellow-700": "_xl:hover:text-yellow-700_6d3e9",
  "xl:hover:text-yellow-800": "_xl:hover:text-yellow-800_6d3e9",
  "xl:hover:text-yellow-900": "_xl:hover:text-yellow-900_6d3e9",
  "xl:hover:text-green-100": "_xl:hover:text-green-100_6d3e9",
  "xl:hover:text-green-200": "_xl:hover:text-green-200_6d3e9",
  "xl:hover:text-green-300": "_xl:hover:text-green-300_6d3e9",
  "xl:hover:text-green-400": "_xl:hover:text-green-400_6d3e9",
  "xl:hover:text-green-500": "_xl:hover:text-green-500_6d3e9",
  "xl:hover:text-green-600": "_xl:hover:text-green-600_6d3e9",
  "xl:hover:text-green-700": "_xl:hover:text-green-700_6d3e9",
  "xl:hover:text-green-800": "_xl:hover:text-green-800_6d3e9",
  "xl:hover:text-green-900": "_xl:hover:text-green-900_6d3e9",
  "xl:hover:text-teal-100": "_xl:hover:text-teal-100_6d3e9",
  "xl:hover:text-teal-200": "_xl:hover:text-teal-200_6d3e9",
  "xl:hover:text-teal-300": "_xl:hover:text-teal-300_6d3e9",
  "xl:hover:text-teal-400": "_xl:hover:text-teal-400_6d3e9",
  "xl:hover:text-teal-500": "_xl:hover:text-teal-500_6d3e9",
  "xl:hover:text-teal-600": "_xl:hover:text-teal-600_6d3e9",
  "xl:hover:text-teal-700": "_xl:hover:text-teal-700_6d3e9",
  "xl:hover:text-teal-800": "_xl:hover:text-teal-800_6d3e9",
  "xl:hover:text-teal-900": "_xl:hover:text-teal-900_6d3e9",
  "xl:hover:text-blue-100": "_xl:hover:text-blue-100_6d3e9",
  "xl:hover:text-blue-200": "_xl:hover:text-blue-200_6d3e9",
  "xl:hover:text-blue-300": "_xl:hover:text-blue-300_6d3e9",
  "xl:hover:text-blue-400": "_xl:hover:text-blue-400_6d3e9",
  "xl:hover:text-blue-500": "_xl:hover:text-blue-500_6d3e9",
  "xl:hover:text-blue-600": "_xl:hover:text-blue-600_6d3e9",
  "xl:hover:text-blue-700": "_xl:hover:text-blue-700_6d3e9",
  "xl:hover:text-blue-800": "_xl:hover:text-blue-800_6d3e9",
  "xl:hover:text-blue-900": "_xl:hover:text-blue-900_6d3e9",
  "xl:hover:text-indigo-100": "_xl:hover:text-indigo-100_6d3e9",
  "xl:hover:text-indigo-200": "_xl:hover:text-indigo-200_6d3e9",
  "xl:hover:text-indigo-300": "_xl:hover:text-indigo-300_6d3e9",
  "xl:hover:text-indigo-400": "_xl:hover:text-indigo-400_6d3e9",
  "xl:hover:text-indigo-500": "_xl:hover:text-indigo-500_6d3e9",
  "xl:hover:text-indigo-600": "_xl:hover:text-indigo-600_6d3e9",
  "xl:hover:text-indigo-700": "_xl:hover:text-indigo-700_6d3e9",
  "xl:hover:text-indigo-800": "_xl:hover:text-indigo-800_6d3e9",
  "xl:hover:text-indigo-900": "_xl:hover:text-indigo-900_6d3e9",
  "xl:hover:text-purple-100": "_xl:hover:text-purple-100_6d3e9",
  "xl:hover:text-purple-200": "_xl:hover:text-purple-200_6d3e9",
  "xl:hover:text-purple-300": "_xl:hover:text-purple-300_6d3e9",
  "xl:hover:text-purple-400": "_xl:hover:text-purple-400_6d3e9",
  "xl:hover:text-purple-500": "_xl:hover:text-purple-500_6d3e9",
  "xl:hover:text-purple-600": "_xl:hover:text-purple-600_6d3e9",
  "xl:hover:text-purple-700": "_xl:hover:text-purple-700_6d3e9",
  "xl:hover:text-purple-800": "_xl:hover:text-purple-800_6d3e9",
  "xl:hover:text-purple-900": "_xl:hover:text-purple-900_6d3e9",
  "xl:hover:text-pink-100": "_xl:hover:text-pink-100_6d3e9",
  "xl:hover:text-pink-200": "_xl:hover:text-pink-200_6d3e9",
  "xl:hover:text-pink-300": "_xl:hover:text-pink-300_6d3e9",
  "xl:hover:text-pink-400": "_xl:hover:text-pink-400_6d3e9",
  "xl:hover:text-pink-500": "_xl:hover:text-pink-500_6d3e9",
  "xl:hover:text-pink-600": "_xl:hover:text-pink-600_6d3e9",
  "xl:hover:text-pink-700": "_xl:hover:text-pink-700_6d3e9",
  "xl:hover:text-pink-800": "_xl:hover:text-pink-800_6d3e9",
  "xl:hover:text-pink-900": "_xl:hover:text-pink-900_6d3e9",
  "xl:hover:text-primary-100": "_xl:hover:text-primary-100_6d3e9",
  "xl:hover:text-primary-200": "_xl:hover:text-primary-200_6d3e9",
  "xl:hover:text-primary-300": "_xl:hover:text-primary-300_6d3e9",
  "xl:hover:text-primary-400": "_xl:hover:text-primary-400_6d3e9",
  "xl:hover:text-primary-500": "_xl:hover:text-primary-500_6d3e9",
  "xl:hover:text-primary-600": "_xl:hover:text-primary-600_6d3e9",
  "xl:hover:text-primary-700": "_xl:hover:text-primary-700_6d3e9",
  "xl:hover:text-primary-800": "_xl:hover:text-primary-800_6d3e9",
  "xl:hover:text-primary-900": "_xl:hover:text-primary-900_6d3e9",
  "xl:hover:text-secondary-100": "_xl:hover:text-secondary-100_6d3e9",
  "xl:hover:text-secondary-200": "_xl:hover:text-secondary-200_6d3e9",
  "xl:hover:text-secondary-300": "_xl:hover:text-secondary-300_6d3e9",
  "xl:hover:text-secondary-400": "_xl:hover:text-secondary-400_6d3e9",
  "xl:hover:text-secondary-500": "_xl:hover:text-secondary-500_6d3e9",
  "xl:hover:text-secondary-600": "_xl:hover:text-secondary-600_6d3e9",
  "xl:hover:text-secondary-700": "_xl:hover:text-secondary-700_6d3e9",
  "xl:hover:text-secondary-800": "_xl:hover:text-secondary-800_6d3e9",
  "xl:hover:text-secondary-900": "_xl:hover:text-secondary-900_6d3e9",
  "xl:focus:text-transparent": "_xl:focus:text-transparent_6d3e9",
  "xl:focus:text-black": "_xl:focus:text-black_6d3e9",
  "xl:focus:text-white": "_xl:focus:text-white_6d3e9",
  "xl:focus:text-gray-100": "_xl:focus:text-gray-100_6d3e9",
  "xl:focus:text-gray-200": "_xl:focus:text-gray-200_6d3e9",
  "xl:focus:text-gray-300": "_xl:focus:text-gray-300_6d3e9",
  "xl:focus:text-gray-400": "_xl:focus:text-gray-400_6d3e9",
  "xl:focus:text-gray-500": "_xl:focus:text-gray-500_6d3e9",
  "xl:focus:text-gray-600": "_xl:focus:text-gray-600_6d3e9",
  "xl:focus:text-gray-700": "_xl:focus:text-gray-700_6d3e9",
  "xl:focus:text-gray-800": "_xl:focus:text-gray-800_6d3e9",
  "xl:focus:text-gray-900": "_xl:focus:text-gray-900_6d3e9",
  "xl:focus:text-red-100": "_xl:focus:text-red-100_6d3e9",
  "xl:focus:text-red-200": "_xl:focus:text-red-200_6d3e9",
  "xl:focus:text-red-300": "_xl:focus:text-red-300_6d3e9",
  "xl:focus:text-red-400": "_xl:focus:text-red-400_6d3e9",
  "xl:focus:text-red-500": "_xl:focus:text-red-500_6d3e9",
  "xl:focus:text-red-600": "_xl:focus:text-red-600_6d3e9",
  "xl:focus:text-red-700": "_xl:focus:text-red-700_6d3e9",
  "xl:focus:text-red-800": "_xl:focus:text-red-800_6d3e9",
  "xl:focus:text-red-900": "_xl:focus:text-red-900_6d3e9",
  "xl:focus:text-orange-100": "_xl:focus:text-orange-100_6d3e9",
  "xl:focus:text-orange-200": "_xl:focus:text-orange-200_6d3e9",
  "xl:focus:text-orange-300": "_xl:focus:text-orange-300_6d3e9",
  "xl:focus:text-orange-400": "_xl:focus:text-orange-400_6d3e9",
  "xl:focus:text-orange-500": "_xl:focus:text-orange-500_6d3e9",
  "xl:focus:text-orange-600": "_xl:focus:text-orange-600_6d3e9",
  "xl:focus:text-orange-700": "_xl:focus:text-orange-700_6d3e9",
  "xl:focus:text-orange-800": "_xl:focus:text-orange-800_6d3e9",
  "xl:focus:text-orange-900": "_xl:focus:text-orange-900_6d3e9",
  "xl:focus:text-yellow-100": "_xl:focus:text-yellow-100_6d3e9",
  "xl:focus:text-yellow-200": "_xl:focus:text-yellow-200_6d3e9",
  "xl:focus:text-yellow-300": "_xl:focus:text-yellow-300_6d3e9",
  "xl:focus:text-yellow-400": "_xl:focus:text-yellow-400_6d3e9",
  "xl:focus:text-yellow-500": "_xl:focus:text-yellow-500_6d3e9",
  "xl:focus:text-yellow-600": "_xl:focus:text-yellow-600_6d3e9",
  "xl:focus:text-yellow-700": "_xl:focus:text-yellow-700_6d3e9",
  "xl:focus:text-yellow-800": "_xl:focus:text-yellow-800_6d3e9",
  "xl:focus:text-yellow-900": "_xl:focus:text-yellow-900_6d3e9",
  "xl:focus:text-green-100": "_xl:focus:text-green-100_6d3e9",
  "xl:focus:text-green-200": "_xl:focus:text-green-200_6d3e9",
  "xl:focus:text-green-300": "_xl:focus:text-green-300_6d3e9",
  "xl:focus:text-green-400": "_xl:focus:text-green-400_6d3e9",
  "xl:focus:text-green-500": "_xl:focus:text-green-500_6d3e9",
  "xl:focus:text-green-600": "_xl:focus:text-green-600_6d3e9",
  "xl:focus:text-green-700": "_xl:focus:text-green-700_6d3e9",
  "xl:focus:text-green-800": "_xl:focus:text-green-800_6d3e9",
  "xl:focus:text-green-900": "_xl:focus:text-green-900_6d3e9",
  "xl:focus:text-teal-100": "_xl:focus:text-teal-100_6d3e9",
  "xl:focus:text-teal-200": "_xl:focus:text-teal-200_6d3e9",
  "xl:focus:text-teal-300": "_xl:focus:text-teal-300_6d3e9",
  "xl:focus:text-teal-400": "_xl:focus:text-teal-400_6d3e9",
  "xl:focus:text-teal-500": "_xl:focus:text-teal-500_6d3e9",
  "xl:focus:text-teal-600": "_xl:focus:text-teal-600_6d3e9",
  "xl:focus:text-teal-700": "_xl:focus:text-teal-700_6d3e9",
  "xl:focus:text-teal-800": "_xl:focus:text-teal-800_6d3e9",
  "xl:focus:text-teal-900": "_xl:focus:text-teal-900_6d3e9",
  "xl:focus:text-blue-100": "_xl:focus:text-blue-100_6d3e9",
  "xl:focus:text-blue-200": "_xl:focus:text-blue-200_6d3e9",
  "xl:focus:text-blue-300": "_xl:focus:text-blue-300_6d3e9",
  "xl:focus:text-blue-400": "_xl:focus:text-blue-400_6d3e9",
  "xl:focus:text-blue-500": "_xl:focus:text-blue-500_6d3e9",
  "xl:focus:text-blue-600": "_xl:focus:text-blue-600_6d3e9",
  "xl:focus:text-blue-700": "_xl:focus:text-blue-700_6d3e9",
  "xl:focus:text-blue-800": "_xl:focus:text-blue-800_6d3e9",
  "xl:focus:text-blue-900": "_xl:focus:text-blue-900_6d3e9",
  "xl:focus:text-indigo-100": "_xl:focus:text-indigo-100_6d3e9",
  "xl:focus:text-indigo-200": "_xl:focus:text-indigo-200_6d3e9",
  "xl:focus:text-indigo-300": "_xl:focus:text-indigo-300_6d3e9",
  "xl:focus:text-indigo-400": "_xl:focus:text-indigo-400_6d3e9",
  "xl:focus:text-indigo-500": "_xl:focus:text-indigo-500_6d3e9",
  "xl:focus:text-indigo-600": "_xl:focus:text-indigo-600_6d3e9",
  "xl:focus:text-indigo-700": "_xl:focus:text-indigo-700_6d3e9",
  "xl:focus:text-indigo-800": "_xl:focus:text-indigo-800_6d3e9",
  "xl:focus:text-indigo-900": "_xl:focus:text-indigo-900_6d3e9",
  "xl:focus:text-purple-100": "_xl:focus:text-purple-100_6d3e9",
  "xl:focus:text-purple-200": "_xl:focus:text-purple-200_6d3e9",
  "xl:focus:text-purple-300": "_xl:focus:text-purple-300_6d3e9",
  "xl:focus:text-purple-400": "_xl:focus:text-purple-400_6d3e9",
  "xl:focus:text-purple-500": "_xl:focus:text-purple-500_6d3e9",
  "xl:focus:text-purple-600": "_xl:focus:text-purple-600_6d3e9",
  "xl:focus:text-purple-700": "_xl:focus:text-purple-700_6d3e9",
  "xl:focus:text-purple-800": "_xl:focus:text-purple-800_6d3e9",
  "xl:focus:text-purple-900": "_xl:focus:text-purple-900_6d3e9",
  "xl:focus:text-pink-100": "_xl:focus:text-pink-100_6d3e9",
  "xl:focus:text-pink-200": "_xl:focus:text-pink-200_6d3e9",
  "xl:focus:text-pink-300": "_xl:focus:text-pink-300_6d3e9",
  "xl:focus:text-pink-400": "_xl:focus:text-pink-400_6d3e9",
  "xl:focus:text-pink-500": "_xl:focus:text-pink-500_6d3e9",
  "xl:focus:text-pink-600": "_xl:focus:text-pink-600_6d3e9",
  "xl:focus:text-pink-700": "_xl:focus:text-pink-700_6d3e9",
  "xl:focus:text-pink-800": "_xl:focus:text-pink-800_6d3e9",
  "xl:focus:text-pink-900": "_xl:focus:text-pink-900_6d3e9",
  "xl:focus:text-primary-100": "_xl:focus:text-primary-100_6d3e9",
  "xl:focus:text-primary-200": "_xl:focus:text-primary-200_6d3e9",
  "xl:focus:text-primary-300": "_xl:focus:text-primary-300_6d3e9",
  "xl:focus:text-primary-400": "_xl:focus:text-primary-400_6d3e9",
  "xl:focus:text-primary-500": "_xl:focus:text-primary-500_6d3e9",
  "xl:focus:text-primary-600": "_xl:focus:text-primary-600_6d3e9",
  "xl:focus:text-primary-700": "_xl:focus:text-primary-700_6d3e9",
  "xl:focus:text-primary-800": "_xl:focus:text-primary-800_6d3e9",
  "xl:focus:text-primary-900": "_xl:focus:text-primary-900_6d3e9",
  "xl:focus:text-secondary-100": "_xl:focus:text-secondary-100_6d3e9",
  "xl:focus:text-secondary-200": "_xl:focus:text-secondary-200_6d3e9",
  "xl:focus:text-secondary-300": "_xl:focus:text-secondary-300_6d3e9",
  "xl:focus:text-secondary-400": "_xl:focus:text-secondary-400_6d3e9",
  "xl:focus:text-secondary-500": "_xl:focus:text-secondary-500_6d3e9",
  "xl:focus:text-secondary-600": "_xl:focus:text-secondary-600_6d3e9",
  "xl:focus:text-secondary-700": "_xl:focus:text-secondary-700_6d3e9",
  "xl:focus:text-secondary-800": "_xl:focus:text-secondary-800_6d3e9",
  "xl:focus:text-secondary-900": "_xl:focus:text-secondary-900_6d3e9",
  "xl:text-xs": "_xl:text-xs_6d3e9",
  "xl:text-sm": "_xl:text-sm_6d3e9",
  "xl:text-base": "_xl:text-base_6d3e9",
  "xl:text-lg": "_xl:text-lg_6d3e9",
  "xl:text-xl": "_xl:text-xl_6d3e9",
  "xl:text-2xl": "_xl:text-2xl_6d3e9",
  "xl:text-3xl": "_xl:text-3xl_6d3e9",
  "xl:text-4xl": "_xl:text-4xl_6d3e9",
  "xl:text-5xl": "_xl:text-5xl_6d3e9",
  "xl:text-6xl": "_xl:text-6xl_6d3e9",
  "xl:italic": "_xl:italic_6d3e9",
  "xl:not-italic": "_xl:not-italic_6d3e9",
  "xl:uppercase": "_xl:uppercase_6d3e9",
  "xl:lowercase": "_xl:lowercase_6d3e9",
  "xl:capitalize": "_xl:capitalize_6d3e9",
  "xl:normal-case": "_xl:normal-case_6d3e9",
  "xl:underline": "_xl:underline_6d3e9",
  "xl:line-through": "_xl:line-through_6d3e9",
  "xl:no-underline": "_xl:no-underline_6d3e9",
  "xl:hover:underline": "_xl:hover:underline_6d3e9",
  "xl:hover:line-through": "_xl:hover:line-through_6d3e9",
  "xl:hover:no-underline": "_xl:hover:no-underline_6d3e9",
  "xl:focus:underline": "_xl:focus:underline_6d3e9",
  "xl:focus:line-through": "_xl:focus:line-through_6d3e9",
  "xl:focus:no-underline": "_xl:focus:no-underline_6d3e9",
  "xl:antialiased": "_xl:antialiased_6d3e9",
  "xl:subpixel-antialiased": "_xl:subpixel-antialiased_6d3e9",
  "xl:tracking-tighter": "_xl:tracking-tighter_6d3e9",
  "xl:tracking-tight": "_xl:tracking-tight_6d3e9",
  "xl:tracking-normal": "_xl:tracking-normal_6d3e9",
  "xl:tracking-wide": "_xl:tracking-wide_6d3e9",
  "xl:tracking-wider": "_xl:tracking-wider_6d3e9",
  "xl:tracking-widest": "_xl:tracking-widest_6d3e9",
  "xl:select-none": "_xl:select-none_6d3e9",
  "xl:select-text": "_xl:select-text_6d3e9",
  "xl:select-all": "_xl:select-all_6d3e9",
  "xl:select-auto": "_xl:select-auto_6d3e9",
  "xl:align-baseline": "_xl:align-baseline_6d3e9",
  "xl:align-top": "_xl:align-top_6d3e9",
  "xl:align-middle": "_xl:align-middle_6d3e9",
  "xl:align-bottom": "_xl:align-bottom_6d3e9",
  "xl:align-text-top": "_xl:align-text-top_6d3e9",
  "xl:align-text-bottom": "_xl:align-text-bottom_6d3e9",
  "xl:visible": "_xl:visible_6d3e9",
  "xl:invisible": "_xl:invisible_6d3e9",
  "xl:whitespace-normal": "_xl:whitespace-normal_6d3e9",
  "xl:whitespace-no-wrap": "_xl:whitespace-no-wrap_6d3e9",
  "xl:whitespace-pre": "_xl:whitespace-pre_6d3e9",
  "xl:whitespace-pre-line": "_xl:whitespace-pre-line_6d3e9",
  "xl:whitespace-pre-wrap": "_xl:whitespace-pre-wrap_6d3e9",
  "xl:break-normal": "_xl:break-normal_6d3e9",
  "xl:break-words": "_xl:break-words_6d3e9",
  "xl:break-all": "_xl:break-all_6d3e9",
  "xl:truncate": "_xl:truncate_6d3e9",
  "xl:w-0": "_xl:w-0_6d3e9",
  "xl:w-1": "_xl:w-1_6d3e9",
  "xl:w-2": "_xl:w-2_6d3e9",
  "xl:w-3": "_xl:w-3_6d3e9",
  "xl:w-4": "_xl:w-4_6d3e9",
  "xl:w-5": "_xl:w-5_6d3e9",
  "xl:w-6": "_xl:w-6_6d3e9",
  "xl:w-8": "_xl:w-8_6d3e9",
  "xl:w-10": "_xl:w-10_6d3e9",
  "xl:w-12": "_xl:w-12_6d3e9",
  "xl:w-16": "_xl:w-16_6d3e9",
  "xl:w-20": "_xl:w-20_6d3e9",
  "xl:w-24": "_xl:w-24_6d3e9",
  "xl:w-32": "_xl:w-32_6d3e9",
  "xl:w-40": "_xl:w-40_6d3e9",
  "xl:w-48": "_xl:w-48_6d3e9",
  "xl:w-56": "_xl:w-56_6d3e9",
  "xl:w-64": "_xl:w-64_6d3e9",
  "xl:w-auto": "_xl:w-auto_6d3e9",
  "xl:w-px": "_xl:w-px_6d3e9",
  "xl:w-1/2": "_xl:w-1/2_6d3e9",
  "xl:w-1/3": "_xl:w-1/3_6d3e9",
  "xl:w-2/3": "_xl:w-2/3_6d3e9",
  "xl:w-1/4": "_xl:w-1/4_6d3e9",
  "xl:w-2/4": "_xl:w-2/4_6d3e9",
  "xl:w-3/4": "_xl:w-3/4_6d3e9",
  "xl:w-1/5": "_xl:w-1/5_6d3e9",
  "xl:w-2/5": "_xl:w-2/5_6d3e9",
  "xl:w-3/5": "_xl:w-3/5_6d3e9",
  "xl:w-4/5": "_xl:w-4/5_6d3e9",
  "xl:w-1/6": "_xl:w-1/6_6d3e9",
  "xl:w-2/6": "_xl:w-2/6_6d3e9",
  "xl:w-3/6": "_xl:w-3/6_6d3e9",
  "xl:w-4/6": "_xl:w-4/6_6d3e9",
  "xl:w-5/6": "_xl:w-5/6_6d3e9",
  "xl:w-1/12": "_xl:w-1/12_6d3e9",
  "xl:w-2/12": "_xl:w-2/12_6d3e9",
  "xl:w-3/12": "_xl:w-3/12_6d3e9",
  "xl:w-4/12": "_xl:w-4/12_6d3e9",
  "xl:w-5/12": "_xl:w-5/12_6d3e9",
  "xl:w-6/12": "_xl:w-6/12_6d3e9",
  "xl:w-7/12": "_xl:w-7/12_6d3e9",
  "xl:w-8/12": "_xl:w-8/12_6d3e9",
  "xl:w-9/12": "_xl:w-9/12_6d3e9",
  "xl:w-10/12": "_xl:w-10/12_6d3e9",
  "xl:w-11/12": "_xl:w-11/12_6d3e9",
  "xl:w-full": "_xl:w-full_6d3e9",
  "xl:w-screen": "_xl:w-screen_6d3e9",
  "xl:z-0": "_xl:z-0_6d3e9",
  "xl:z-10": "_xl:z-10_6d3e9",
  "xl:z-20": "_xl:z-20_6d3e9",
  "xl:z-30": "_xl:z-30_6d3e9",
  "xl:z-40": "_xl:z-40_6d3e9",
  "xl:z-50": "_xl:z-50_6d3e9",
  "xl:z-auto": "_xl:z-auto_6d3e9"
};
},{"_css_loader":"../node_modules/parcel-bundler/src/builtins/css-loader.js"}],"../node_modules/redux-persist/es/integration/react.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.PersistGate = void 0;

var _react = _interopRequireWildcard(require("react"));

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function () { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; if (obj != null) { var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

function _typeof(obj) {
  if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
    _typeof = function _typeof(obj) {
      return typeof obj;
    };
  } else {
    _typeof = function _typeof(obj) {
      return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    };
  }

  return _typeof(obj);
}

function _classCallCheck(instance, Constructor) {
  if (!(instance instanceof Constructor)) {
    throw new TypeError("Cannot call a class as a function");
  }
}

function _defineProperties(target, props) {
  for (var i = 0; i < props.length; i++) {
    var descriptor = props[i];
    descriptor.enumerable = descriptor.enumerable || false;
    descriptor.configurable = true;
    if ("value" in descriptor) descriptor.writable = true;
    Object.defineProperty(target, descriptor.key, descriptor);
  }
}

function _createClass(Constructor, protoProps, staticProps) {
  if (protoProps) _defineProperties(Constructor.prototype, protoProps);
  if (staticProps) _defineProperties(Constructor, staticProps);
  return Constructor;
}

function _possibleConstructorReturn(self, call) {
  if (call && (_typeof(call) === "object" || typeof call === "function")) {
    return call;
  }

  return _assertThisInitialized(self);
}

function _getPrototypeOf(o) {
  _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf(o) {
    return o.__proto__ || Object.getPrototypeOf(o);
  };
  return _getPrototypeOf(o);
}

function _assertThisInitialized(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }

  return self;
}

function _inherits(subClass, superClass) {
  if (typeof superClass !== "function" && superClass !== null) {
    throw new TypeError("Super expression must either be null or a function");
  }

  subClass.prototype = Object.create(superClass && superClass.prototype, {
    constructor: {
      value: subClass,
      writable: true,
      configurable: true
    }
  });
  if (superClass) _setPrototypeOf(subClass, superClass);
}

function _setPrototypeOf(o, p) {
  _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf(o, p) {
    o.__proto__ = p;
    return o;
  };

  return _setPrototypeOf(o, p);
}

function _defineProperty(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
  } else {
    obj[key] = value;
  }

  return obj;
}

// eslint-disable-line import/no-unresolved
var PersistGate =
/*#__PURE__*/
function (_PureComponent) {
  _inherits(PersistGate, _PureComponent);

  function PersistGate() {
    var _getPrototypeOf2;

    var _this;

    _classCallCheck(this, PersistGate);

    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    _this = _possibleConstructorReturn(this, (_getPrototypeOf2 = _getPrototypeOf(PersistGate)).call.apply(_getPrototypeOf2, [this].concat(args)));

    _defineProperty(_assertThisInitialized(_this), "state", {
      bootstrapped: false
    });

    _defineProperty(_assertThisInitialized(_this), "_unsubscribe", void 0);

    _defineProperty(_assertThisInitialized(_this), "handlePersistorState", function () {
      var persistor = _this.props.persistor;

      var _persistor$getState = persistor.getState(),
          bootstrapped = _persistor$getState.bootstrapped;

      if (bootstrapped) {
        if (_this.props.onBeforeLift) {
          Promise.resolve(_this.props.onBeforeLift()).finally(function () {
            return _this.setState({
              bootstrapped: true
            });
          });
        } else {
          _this.setState({
            bootstrapped: true
          });
        }

        _this._unsubscribe && _this._unsubscribe();
      }
    });

    return _this;
  }

  _createClass(PersistGate, [{
    key: "componentDidMount",
    value: function componentDidMount() {
      this._unsubscribe = this.props.persistor.subscribe(this.handlePersistorState);
      this.handlePersistorState();
    }
  }, {
    key: "componentWillUnmount",
    value: function componentWillUnmount() {
      this._unsubscribe && this._unsubscribe();
    }
  }, {
    key: "render",
    value: function render() {
      if ("development" !== 'production') {
        if (typeof this.props.children === 'function' && this.props.loading) console.error('redux-persist: PersistGate expects either a function child or loading prop, but not both. The loading prop will be ignored.');
      }

      if (typeof this.props.children === 'function') {
        return this.props.children(this.state.bootstrapped);
      }

      return this.state.bootstrapped ? this.props.children : this.props.loading;
    }
  }]);

  return PersistGate;
}(_react.PureComponent);

exports.PersistGate = PersistGate;

_defineProperty(PersistGate, "defaultProps", {
  children: null,
  loading: null
});
},{"react":"../node_modules/react/index.js"}],"index.tsx":[function(require,module,exports) {
"use strict";

var _react = _interopRequireDefault(require("react"));

var _reactDom = _interopRequireDefault(require("react-dom"));

var _App = require("./App");

var _store = require("./redux/store");

var _reactRedux = require("react-redux");

require("./index.less");

require("./tailwind.less");

var _react2 = require("redux-persist/integration/react");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

_reactDom.default.render(_react.default.createElement(_reactRedux.Provider, {
  store: _store.store
}, _react.default.createElement(_react2.PersistGate, {
  loading: null,
  persistor: _store.persistor
}, _react.default.createElement(_App.App, null))), document.getElementById("app-root"));
},{"react":"../node_modules/react/index.js","react-dom":"../node_modules/react-dom/index.js","./App":"App.tsx","./redux/store":"redux/store.ts","react-redux":"../node_modules/react-redux/es/index.js","./index.less":"index.less","./tailwind.less":"tailwind.less","redux-persist/integration/react":"../node_modules/redux-persist/es/integration/react.js"}],"../node_modules/parcel-bundler/src/builtins/hmr-runtime.js":[function(require,module,exports) {
var global = arguments[3];
var OVERLAY_ID = '__parcel__error__overlay__';
var OldModule = module.bundle.Module;

function Module(moduleName) {
  OldModule.call(this, moduleName);
  this.hot = {
    data: module.bundle.hotData,
    _acceptCallbacks: [],
    _disposeCallbacks: [],
    accept: function (fn) {
      this._acceptCallbacks.push(fn || function () {});
    },
    dispose: function (fn) {
      this._disposeCallbacks.push(fn);
    }
  };
  module.bundle.hotData = null;
}

module.bundle.Module = Module;
var checkedAssets, assetsToAccept;
var parent = module.bundle.parent;

if ((!parent || !parent.isParcelRequire) && typeof WebSocket !== 'undefined') {
  var hostname = "" || location.hostname;
  var protocol = location.protocol === 'https:' ? 'wss' : 'ws';
  var ws = new WebSocket(protocol + '://' + hostname + ':' + "45361" + '/');

  ws.onmessage = function (event) {
    checkedAssets = {};
    assetsToAccept = [];
    var data = JSON.parse(event.data);

    if (data.type === 'update') {
      var handled = false;
      data.assets.forEach(function (asset) {
        if (!asset.isNew) {
          var didAccept = hmrAcceptCheck(global.parcelRequire, asset.id);

          if (didAccept) {
            handled = true;
          }
        }
      }); // Enable HMR for CSS by default.

      handled = handled || data.assets.every(function (asset) {
        return asset.type === 'css' && asset.generated.js;
      });

      if (handled) {
        console.clear();
        data.assets.forEach(function (asset) {
          hmrApply(global.parcelRequire, asset);
        });
        assetsToAccept.forEach(function (v) {
          hmrAcceptRun(v[0], v[1]);
        });
      } else {
        window.location.reload();
      }
    }

    if (data.type === 'reload') {
      ws.close();

      ws.onclose = function () {
        location.reload();
      };
    }

    if (data.type === 'error-resolved') {
      console.log('[parcel] ✨ Error resolved');
      removeErrorOverlay();
    }

    if (data.type === 'error') {
      console.error('[parcel] 🚨  ' + data.error.message + '\n' + data.error.stack);
      removeErrorOverlay();
      var overlay = createErrorOverlay(data);
      document.body.appendChild(overlay);
    }
  };
}

function removeErrorOverlay() {
  var overlay = document.getElementById(OVERLAY_ID);

  if (overlay) {
    overlay.remove();
  }
}

function createErrorOverlay(data) {
  var overlay = document.createElement('div');
  overlay.id = OVERLAY_ID; // html encode message and stack trace

  var message = document.createElement('div');
  var stackTrace = document.createElement('pre');
  message.innerText = data.error.message;
  stackTrace.innerText = data.error.stack;
  overlay.innerHTML = '<div style="background: black; font-size: 16px; color: white; position: fixed; height: 100%; width: 100%; top: 0px; left: 0px; padding: 30px; opacity: 0.85; font-family: Menlo, Consolas, monospace; z-index: 9999;">' + '<span style="background: red; padding: 2px 4px; border-radius: 2px;">ERROR</span>' + '<span style="top: 2px; margin-left: 5px; position: relative;">🚨</span>' + '<div style="font-size: 18px; font-weight: bold; margin-top: 20px;">' + message.innerHTML + '</div>' + '<pre>' + stackTrace.innerHTML + '</pre>' + '</div>';
  return overlay;
}

function getParents(bundle, id) {
  var modules = bundle.modules;

  if (!modules) {
    return [];
  }

  var parents = [];
  var k, d, dep;

  for (k in modules) {
    for (d in modules[k][1]) {
      dep = modules[k][1][d];

      if (dep === id || Array.isArray(dep) && dep[dep.length - 1] === id) {
        parents.push(k);
      }
    }
  }

  if (bundle.parent) {
    parents = parents.concat(getParents(bundle.parent, id));
  }

  return parents;
}

function hmrApply(bundle, asset) {
  var modules = bundle.modules;

  if (!modules) {
    return;
  }

  if (modules[asset.id] || !bundle.parent) {
    var fn = new Function('require', 'module', 'exports', asset.generated.js);
    asset.isNew = !modules[asset.id];
    modules[asset.id] = [fn, asset.deps];
  } else if (bundle.parent) {
    hmrApply(bundle.parent, asset);
  }
}

function hmrAcceptCheck(bundle, id) {
  var modules = bundle.modules;

  if (!modules) {
    return;
  }

  if (!modules[id] && bundle.parent) {
    return hmrAcceptCheck(bundle.parent, id);
  }

  if (checkedAssets[id]) {
    return;
  }

  checkedAssets[id] = true;
  var cached = bundle.cache[id];
  assetsToAccept.push([bundle, id]);

  if (cached && cached.hot && cached.hot._acceptCallbacks.length) {
    return true;
  }

  return getParents(global.parcelRequire, id).some(function (id) {
    return hmrAcceptCheck(global.parcelRequire, id);
  });
}

function hmrAcceptRun(bundle, id) {
  var cached = bundle.cache[id];
  bundle.hotData = {};

  if (cached) {
    cached.hot.data = bundle.hotData;
  }

  if (cached && cached.hot && cached.hot._disposeCallbacks.length) {
    cached.hot._disposeCallbacks.forEach(function (cb) {
      cb(bundle.hotData);
    });
  }

  delete bundle.cache[id];
  bundle(id);
  cached = bundle.cache[id];

  if (cached && cached.hot && cached.hot._acceptCallbacks.length) {
    cached.hot._acceptCallbacks.forEach(function (cb) {
      cb();
    });

    return true;
  }
}
},{}],"../node_modules/parcel-bundler/src/builtins/loaders/browser/js-loader.js":[function(require,module,exports) {
module.exports = function loadJSBundle(bundle) {
  return new Promise(function (resolve, reject) {
    var script = document.createElement('script');
    script.async = true;
    script.type = 'text/javascript';
    script.charset = 'utf-8';
    script.src = bundle;

    script.onerror = function (e) {
      script.onerror = script.onload = null;
      reject(e);
    };

    script.onload = function () {
      script.onerror = script.onload = null;
      resolve();
    };

    document.getElementsByTagName('head')[0].appendChild(script);
  });
};
},{}],0:[function(require,module,exports) {
var b=require("../node_modules/parcel-bundler/src/builtins/bundle-loader.js");b.register("js",require("../node_modules/parcel-bundler/src/builtins/loaders/browser/js-loader.js"));
},{}]},{},["../node_modules/parcel-bundler/src/builtins/hmr-runtime.js",0,"index.tsx"], null)
//# sourceMappingURL=/src.f69400ca.js.map